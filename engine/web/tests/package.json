{
  "name": "ftd-web-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Playwright smoke suite for the FTD web dashboard. Boots a real Chromium against index.html served from python -m http.server.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
