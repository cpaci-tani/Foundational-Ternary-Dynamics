// FTD Ontology Atlas — bootstrap.
// Phase 1 stub: exposes the test handle. The Three.js scene, field renderers,
// chain stepper, SVG overlay, and UI are wired in later phases.
window.__ftdAtlas = { stageCount: 14, ready: true };
console.log('[atlas] boot');
