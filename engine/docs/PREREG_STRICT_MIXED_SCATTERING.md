# Registered heterogeneous-probe mixed response, version 1

**[SELECTION] preparation; [OPEN] collective material recovery.**
This is a new protocol following the accepted
[common-flag response campaign](AUDIT_STRICT_MIXED_RESPONSE.md). It preserves
that campaign's artifacts, law, sources and negative remote-feedback result.
Its scientific change is explicit: replace the common-flag probe with the
fixed channel pair **(0,32)** at every probe site and observe twice as long.
These channels and the horizon are declared before any new measured evolution;
they will not be tuned to the response.

## Complete preparation and resource contract

The domain is periodic `L=17`. All SC and FCC anchors contain one positive
phase-zero primary and a blank reserve. Initial `s=0`, `ell=0`, microtick zero
and cleared pending arrays are deliberate preparation conventions. The five
relation cases are the homogeneous reference and four single-owner defects
at `(8,8,8)`: SC axis 0 phase one; SC axis 0 reserve-slot phase zero;
FCC plane 0 diagonal 1 phase one; and that FCC reserve-slot phase zero.
Each defect changes only the specified relation payload. The negative FCC
diagonal has distinct owner and endpoint sites.

Cross those five cases with no fields or exactly channels 0 and 32 at each
site of `{7,8,9}^3`. Thus five cases have zero fields and five have 54 fields.
The frozen channel table fixes all tangent, flag, phase and polarity meanings.
No random seeds, inferred particle identifiers or outcome-based choices enter.
The horizon is **32 physical microticks**, giving ten cases, 320 measured
transitions and 340 retained complete snapshots.

The cap is 1 GiB of campaign artifacts and 30 minutes of instrumented GPU
execution. Resource failure rejects completion. The source and executable
identities, all inputs, ordered manifest and this registration are SHA256
locked in a fresh directory before execution. Frozen source copies are retained.
Previous mixed-response and carrier files remain unchanged.

## Paired questions and observables

The 13 fixed comparisons retain the previous protocol's meanings: five
field/no-field comparisons at fixed relation preparation, four defect/reference
comparisons with identical probes, and four field-free defect controls.
Record both positive and negative outcomes for field-to-relation response,
relation-to-field response and **relation payload response at another stored
anchor**. Initial remote relation payloads are identical in the latter
comparison; a later difference is the specified remote-feedback witness.

The hypothesis is that collisions between the heterogeneous flags can change
field trajectories or site populations, allowing a defect-dependent signal
to reach a different relation update gate. Failure to observe this is a
retained finite negative result, not permission to alter the pair or horizon.
An event label alone does not establish scattering: every collision must agree
with the frozen table and actual complete field-bank changes.

All eight complete arrays, physical tick identities and exact events are
retained and compared. Per-entry Hamming counts, every stored-owner support,
separate relation/control endpoint supports, and periodic Moore distances are
recorded at each microtick. Every paired step must satisfy
`D(t+1) subset Moore1_periodic(D(t))` for the full-record owner union. Missing
states/events, stale hashes, incomplete locks or mismatched transitions reject
the campaign. No global particle lineage is invented through pair collisions.

## Execution and interpretation boundary

The standalone `engine/strict/recovery_mixed_scattering` executable runs the
unchanged CUDA kernels through WSL2 Ubuntu-22.04 on the RTX 5090 and checks
every actual GPU state/event against an independently advanced native CPU
reference. It retains the actual GPU complete checkpoints and per-tick hashes.
The Python auditor then regenerates every preparation and every full transition
and event from the accepted staged law before computing the paired results.
The full registration/source/input/runner preflight and postflight chain is
mandatory. Independent protocol/instrument review precedes measured execution.

All conclusions are **finite periodic**. The 32-tick generic cone covers the
torus, so observed seam contact or absence of contact is only a diagnostic.
No no-wrap, undefined-boundary or continuum result follows from it. A remote
relation-payload response would establish one mechanism beyond the previous
common-flag sector; it would not establish a bound moving body, mass,
material force, gravity, thermodynamics or continuum closure. M1/M2 and
continuum recovery flags remain false for either outcome. No new law is adopted.
