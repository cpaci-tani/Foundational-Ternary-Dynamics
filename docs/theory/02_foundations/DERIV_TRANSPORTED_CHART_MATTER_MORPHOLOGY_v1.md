# FTD-0764 — Transported-chart matter morphology

**Status:** `[DERIVED OBSERVER ALGEBRA + SELECTED MORPHOLOGY CRITERION; MATTER INTERPRETATION OPEN]`

## 1. Why the next distinction matters

FTD-0763 constructs a valid instantaneous Gauss observer at fractional matter
centers. That closes a coordinate-domain defect, but it does not prove that an
actual field configuration travels with the manifested core. The selected
minimum-energy Gauss representative is a deterministic function of the current
matter geometry. Recomputing it at every center makes its translation largely
true by construction.

The physics-bearing question is instead whether the **actual evolved field**
retains a stable shape in coordinates transported with the matter center, and
whether the part that does not retain that shape is localized behind the core,
detaches outward, or disperses symmetrically.

## 2. Intuitive questions

1. If the manifested sites change but the same relational pattern survives,
   which object moved: the sites, the field, or their correlation?
2. Is the Gauss dressing a material constituent, or a constraint relation that
   must accompany any instantaneous polarity distribution?
3. Does the evolved field remember the core's former positions after the core
   moves, and if so is that memory a wake, radiation, or ordinary relaxation?
4. When matter momentum changes, does either independently motivated field
   momentum change oppositely, or does the preferred lattice absorb a real
   translation defect?
5. If microscopic momentum does not close, does the defect decrease for wider,
   softer carriers strongly enough to permit infrared inertial matter?
6. Does a field profile remain the same after removing only the phase implied
   by the measured matter displacement, without fitting a velocity?
7. Can a localized co-moving component and detached radiation coexist, so that
   demanding the complete field translate rigidly would reject ordinary
   radiating matter for the wrong reason?
8. Is the half-cell chart seam merely an observer convention, or does any
   dynamical observable jump when the matter center crosses it?

These questions separate a particle-like dynamical pattern from a moving label
attached to a stationary or continually regenerated field.

## 3. Exact transported phase law

Let a scalar lattice density `u(r)` have discrete Fourier coefficient

\[
 Z_u(n)=\sum_r u(r)\exp[-2\pi i\,n\cdot r/L].
\]

For an integer translation by `a`, direct reindexing gives

\[
 Z_{T_a u}(n)=e^{-2\pi i n\cdot a/L}Z_u(n).                 \tag{1}
\]

For an arbitrary measured center `c`, define the transported-chart coefficient

\[
 \widetilde Z_u(n;c)=e^{+2\pi i n\cdot c/L}Z_u(n).          \tag{2}
\]

Equation (2) is exact under integer lattice translations. For fractional `c`
it is the unique phase continuation of (1) on each Fourier mode, but it is a
**selected nonlocal observer**, not a new local update rule or an ontic
fractional-translation operator. FTD-0554's locality trilemma is therefore not
evaded.

## 4. Staggered field-energy coefficients

Field components are evaluated at their actual cochain locations:

- `E_x,E_y,E_z` at `r+(1/2,0,0)`, `r+(0,1/2,0)`,
  `r+(0,0,1/2)`;
- integer-time `B_x,B_y,B_z` at `r+(0,1/2,1/2)`,
  `r+(1/2,0,1/2)`, `r+(1/2,1/2,0)`.

The integer-time magnetic field is the already locked midpoint readout

\[
 B_n=B_{n-1/2}-{C\,\Delta t\over2}C^T E_n.                 \tag{3}
\]

For each component `f`, with selected bound value `b` and residual `r=f-b`,

\[
 {1\over2}f^2={1\over2}b^2+{1\over2}r^2+br.               \tag{4}
\]

Fourier summation preserves (4) mode by mode. The observer therefore reports
actual, selected-bound, residual, and interference coefficients separately.
Only the actual and residual channels can contain dynamical evidence; the bound
channel is a construction control.

## 5. Registered morphology

For each symmetry ray choose its longitudinal integer reciprocal direction and
two integer transverse directions. Use harmonics

\[
 h\in\{1,2,4,8,16,32\}.                                    \tag{5}
\]

For a positive-energy channel `A`, normalize away overall amplitude,

\[
 q_A(n,t)={\widetilde Z_A(n;c(t))\over U_A(t)},             \tag{6}
\]

and compare two checkpoints with

\[
 D_A(t_0,t_1)=
 \sqrt{{\sum_n|q_A(n,t_1)-q_A(n,t_0)|^2
              \over\max(\sum_n|q_A(n,t_0)|^2,\epsilon)}}.  \tag{7}
\]

`D_A=0` for a rigidly translated profile of arbitrary constant amplitude; the
separate ratio `U_A(t_1)/U_A(t_0)` records amplitude evolution. No fitted
velocity, phase, scale, or spatial offset is allowed.

The residual channel is also split by the existing integer support chart into
a near region and an outer annulus. Its energy first moment relative to the
continuous matter center provides a directional discriminator:

- persistent negative longitudinal moment: trailing wake candidate;
- growing outer energy with outward characteristic flux: detached radiation
  candidate;
- comparable leading/trailing content with growing radius: dispersive
  background candidate.

These are finite-resolution classifications, not literal field substances.

## 6. Momentum scope

Two field momenta already have independent motivation:

1. the local staggered translation pseudomomentum of FTD-0473;
2. the force-matched quadratic-spline Poynting momentum of FTD-0619.

FTD-0619 shows that neither closes the earlier compact gait. FTD-0764 must
record both again on the current M4 history without changing their
normalization. A residual

\[
 R_P=\Delta P_{matter}+\Delta P_{field}                    \tag{8}
\]

is called a measured translation defect, never hidden substrate momentum. A
genuine lattice/substrate momentum account requires an independently evolving
state variable or an exact identity; it cannot be defined as `-R_P`.

## 7. Matter interpretation licensed by outcomes

If the actual near-field coefficients remain stable in the transported chart
while outer characteristic energy detaches, the supported ontology is:

> matter is a persistent relational manifestation core plus a co-moving
> dynamical near field, with radiation as the portion that leaves that
> transported correlation.

If only the recomputed bound control is stable, the current history contains a
moving core but has not established a co-moving actual dressing. If the
momentum defect remains finite, microscopic motion is substrate-relative; an
ordinary inertial particle interpretation then requires demonstrated infrared
suppression with carrier width.

No outcome by itself derives charge magnitude, spin, statistics, a quantum
wavefunction, a Lorentz cone, or particle identity.
