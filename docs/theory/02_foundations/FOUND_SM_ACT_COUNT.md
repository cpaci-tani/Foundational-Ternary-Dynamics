# FOUND — Completing the act-count: the exactly-expressible SM needs no field-act beyond {i, δ} (CLOSED)

**Tag:** `[SYNTHESIS]` — assembles existing canonical forms under one classification; introduces no theorem, derives nothing, **promotes no tag**.
**Date:** 2026-06-25
**LEDGER id:** FTD-0317
**Pre-registration:** `PREREG_SM_ACT_COUNT_TEST_v1.md`, git tag `preregister-sm-act-count-test-v1`, SHA256 `8dd5f5143ef136e02d245667ee1788d168171cc9fe2de3aae39efc370ff80998` (committed before this classification).
**Reuse (not re-derived):** FTD-0314 (`FOUND_ACT_REDUCTION_COUNT.md`); FTD-0244 (`FOUND_OPERATOR_CALCULUS_AXIOMATIZATION.md`); `CATALOG_PARAMETRIC_INSERTIONS.md`.

---

## 0 · Verdict: **CLOSED** (pre-registered outcome 1 of 2)

Every dimensionless SM quantity with an **exact closed form** reduces to the two field-acts already counted — `i` (FC-0) and `δ = √(G*(4G*−1))` (the α extension) — plus rational selections, positivity-forced magnitudes, and the calibration register. **No third field-act exists.**

> The exactly-expressible dimensionless SM's irreducible field-acts are exactly **`{i, δ}`** — *two* square roots. This completes the FTD-0314 act-count: the open mass-sector question ("do the masses need an act beyond `{i, δ}`?") closes **negative**.

Indeed the result is sharper than required: the **lepton mass ratios and mixing angles are pure rationals** over the framework integers — they need *neither a new act nor even `δ`*. `δ` is used only by the α-sector (`1/α`, `m_p/m_e`, `α_G`); `i` by the algebraic spine (the curve `y²=x³−x`, the `(1+i)`-tower). Nothing else is irreducibly chosen at the field level.

---

## 1 · Classification (representative items; full inventory = the exactly-expressible rows of `CATALOG_PARAMETRIC_INSERTIONS.md`)

| quantity | exact form | category | act? |
|---|---|---|---|
| `m_μ/m_e` | integer (≈207; matches 206.77, ~0.1%) | **rational** | none — a rational selection |
| `m_τ/m_e` | integer (≈3477; matches 3477.2) | **rational** | none |
| `sin²θ_W` | `N_c/N_eff = 3/13` | **rational** | none |
| `α_s` | `b₃/(b₃+4N_eff) = 7/59` | **rational** | none |
| PMNS `sin²θ_12/23/13` | `3/10`, `16/29`, `1/52` | **rational** | none |
| `Δm²₃₁/Δm²₂₁` | `(b₃+N_c)²/N_c = 100/3` | **rational** | none |
| loop coeffs `c1/c2/c3` | `9/47`, `5/64`, `4/141` | **rational** | none |
| framework integers `N_c,b₃,N_base,N_eff` | `3,7,4,13` | **rational** (integers) | none |
| cosmology `Ω_Λ,Ω_m,n_s,r,σ` | `2/3`, `1/3`, `1−2α`, `N_c·α`, `−ln(x₋/(x₋+1))` | **rational** / `δ`-carrying | none / uses `δ` |
| `1/α` | `x₊ = 8G*² + 4G*δ` | **uses `δ`** | the **δ-act** (already counted) |
| `m_p/m_e` | `N_eff·x₊ + …` | **uses `δ`** (via `x₊`) | δ-act |
| `α_G` | `2π·(…)·α²⁰` | **uses `δ`** (via α) + `π` magnitude | δ-act |
| `G_C = √α` | positive root of `α ∈ ℚ(G*,δ)` | **forced magnitude** | none — a positive coupling, one branch |
| `C_SPEED = 1/√3` | `1/√D`, `D=3` forced | **forced magnitude** | none — a positive speed |
| `√(2π)` (in `m_e`) | Gaussian normalization | **forced magnitude** + **calibration** | none |
| `m_e` (MeV), `ℓ_P`, VEV, `M_Z` | dimensional | **calibration** | none (separate register) |

**Category-4 (a new independent algebraic selection) is empty.** A systematic search of the catalog's exactly-expressible dimensionless rows found **no irrational of degree >1 over `ℚ(G*)`, outside `ℚ(G*)(i, δ)`, and unforced**. The only non-`{i,δ}` roots that appear *in a dimensionless row* are `√α` (in `G_C = √α`) and `√3` (in `C_SPEED = 1/√3`) — both **positivity-forced magnitudes** (a positive coupling / a positive speed, one admissible branch), not acts, per the FTD-0314 criterion. The other forced magnitudes (`√(2π)` in `m_e`, and `π`) live only in **dimensional/calibration** quantities, which are outside the dimensionless count entirely; `√2` appears in **no** classified row. None is an unforced `√`.

---

## 2 · The completed count — "how many times is the universe chosen?"

Combining FTD-0314 + FTD-0316 + this result, FTD's irreducible-choice content over the **exactly-expressible** physics is:

| kind of freedom | content |
|---|---|
| **field-acts** (unforced `√` / `ℤ/2`-break) | **exactly two: `i` and `δ`** |
| orientation acts (sign `ℤ/2`) | matter/antimatter labelling (untested); the *time* arrow's direction is **forced-given-FC-2**, not an act (FTD-0316) |
| refusal | FC-1 (declines `M`) |
| covariance | FC-3 (scale-ratio) |
| rational selections | the integer mass-ratio formulas, the rational angles (`3/13`, `7/59`, …), loop coeffs — free *rationals*, not `√`-acts |
| forced magnitudes | `√α`, `√3` (in dimensionless rows); `√(2π)`, `π` (in dimensional/calibration rows only) — determined, not chosen |
| calibration register | `a_phys≡ℓ_P`, `M_REST=m_e`, `t_phys` |

**The answer:** the entire exactly-expressible dimensionless structure of FTD is generated by **two square-root acts of intent — `i = √(−1)` and `δ = √(G*(4G*−1))`** — on top of forced magnitudes and a finite list of free rational selections. The universe, at the field level, is chosen **twice**; `i` builds the spine, `δ` is the one selection the substrate provably cannot make for itself (FTD-0244 — which is exactly why α is dynamical and stays `[SMC]`).

The **act vs forced-magnitude** distinction has a concrete witness in the AGM: `G* = 2√π/AGM(1,√2)`, and each AGM step is a *geometric-mean* `√(ab)` — a forced magnitude (category 5), not an act — so the substrate's all-square-roots machine lands on `G*` and never on `δ`. The chiral, branch-selecting √ that would reach `δ` is exactly the act the forced means cannot perform. See **FTD-0319** (`FOUND_AGM_PLACE_BRIDGE_AND_DELTA.md`), which unifies this with FTD-0318 (operator side) and the prime-duality (`EXPLR_GAUSSIAN_EISENSTEIN_DICHOTOMY.md` §5.1).

---

## 3 · The honest residue (outside the exactly-expressible set — *not* counted)

The catalog's remaining ~140 rows have **no clean closed form** and are excluded by construction; they are FTD's honest periphery, flagged not hidden: quark masses, the flavor-depth matrices (`N_E/N_U/N_D`, under-determined — 18 free slots), CKM Wolfenstein entries, neutrino absolute scale, hadron masses, decay rates, imported QED loops, dark-matter nature. These are `[PARAMETRIC]`/`[SELECTION]`/imported and do not undergo field-act analysis. **The count above is a statement about the exactly-expressible spine + leptonic/EW ratios, not the full particle data table.** (Note: the cosmology rows' provenance ids should be verified against the LEDGER; an earlier inventory pass cited a non-existent id for them.)

---

## 4 · Tie-back to FTD-0314

FTD-0314 §2 left open: "the masses are parametric (no exact algebraic form over `ℚ(G*)`), so their act-count is not yet rigorous." For the **exactly-expressible** masses/angles, it now is: they are rational over the framework integers (or `δ`-carrying), so they add **no** field-act. FTD-0314's "two field-acts `{i, δ}`" stands as the complete answer for the exactly-expressible sector; the parametric residue remains the boundary, as it was.

---

## 5 · Status line

**Nothing is promoted.** Tag `[SYNTHESIS]`. The integer mass-ratio formulas stay **selections** (`[SMC]`/`[SELECTION]`), not theorems; mixing-angle tags unchanged (`[PARAMETRIC]`/`[STRUCTURALLY MOTIVATED PARAMETRIC]`); FTD-0013 stays `[SMC]`; FTD-0244 unchanged; no α derived; P1–P5 and the FC register untouched. The verdict (CLOSED) is a pre-registered admissible outcome; the pre-registration SHA + git tag predate this commit. The classification is about **field-acts** (unforced square roots), not raw field-extensions — `√3` (and `√α`) extend `ℚ(G*)` but are forced magnitudes, not acts.
