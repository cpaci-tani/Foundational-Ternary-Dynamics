# THEOREM — The four walls are two axes: adjudication of the four-walls-are-one question (FTD-0357)

**Tag:** `[THEOREM — relative to the wall formalizations of §1]` (the distinctness core: Lemmas A–G and the adjudication matrix; **conditional on Chudnovsky 1976** wherever δ's transcendence is load-bearing, exactly as FTD-0244/0314/0353, and **FC-0-locked** wherever the hull is used, per FTD-0358) + `[SYNTHESIS]` (the two-axis reorganization of the frontier's §4 catalog; the schema-level unity restated at its existing tag) + `[CLOSED — RESOLVED AS DECLARATION]` (the frontier §7 T2 question: **partial unification with a proven distinctness core** — per the question's own protocol, a distinctness proof closes it).
**LEDGER id:** FTD-0357 (row owned by the controller; **this document does not edit `LEDGER.md`, `META_INDEX.md`, any tracker, the spine, or `FOUND_MODULUS_ARGUMENT_FRONTIER.md`**).
**Adjudicates:** [`FOUND_MODULUS_ARGUMENT_FRONTIER.md`](FOUND_MODULUS_ARGUMENT_FRONTIER.md) §7, first bullet ("the four-walls-are-one forcing theorem, T2") — the logic layer's last open item.
**Verification artifact:** [`scripts/proofs/proof_four_walls.py`](../../../scripts/proofs/proof_four_walls.py) — **56/56 PASS** (run of record 2026-07-02; mpmath dps 30–60 + sympy symbolic layer; SHA256 `8115e52457b4…`). Read-only pure mathematics — no engine contact, golden gate untouched (`0xb604d81a3d79366e`). No numerical searches were run: every check verifies a stated identity, lemma instance, or counterexample.
**Depends on:** FTD-0336 (the frontier), FTD-0255/FC-1 + FTD-0243 (measurement-map fork), FTD-0256/FC-2 + FTD-0253 (reversibility boundary) + FTD-0252 (γ as algebraic identity of second-order dynamics), FTD-0315/FC-W + FTD-0314 (carrier narrowing), FTD-0208 (L²-not-L¹ closed negative; clock hypothesis [AXIOM]), FTD-0353 (valuation theorem; FC-W = adopted ramification at (4G\*−1)), FTD-0354 (GNC/clock pinned-isometry shape), FTD-0358 (FC-0 sector lock; constructor closure), FTD-0340/0326/0327 (sqrt-as-act; native ℤ/2's and √-machine δ-blind), FTD-0200/FTD-0258 PL-1 (Rice-not-Born), Chudnovsky 1976; classical inputs: polarization identity, GNS construction, spectral theory of form-preserving maps, Kummer-type multiquadratic lemma (via FTD-0353).
**Precedence:** LEDGER > constitution > this doc.

---

## 0 · Verdict

> **Full unification is FALSE, provably. The honest structure is: one schema, two axes, no forcing.** The four walls — FC-1 (the declined measurement map M), FC-2 (native arrow / sector-scoped metric), FC-W (the adopted branch δ = √(G\*(4G\*−1))), and the L²-not-L¹ budget (FTD-0208, with the clock hypothesis as its coordinate-level [AXIOM] surrogate) — are four instances of **one question** (choose a section/adjoint of a lossy structure: the frontier's schema, [SYNTHESIS], unchanged) whose **answers live in provably non-isomorphic choice spaces**. Walls 1, 2(metric), and 4 share one structural type — a positive (sesqui)linear **pairing** on a sector — but their sector instances provably do not communicate, and the substrate itself is the witness. Wall 3 is an **arithmetic branch datum**, ramified at (4G\*−1), separated from the pairing axis **in both directions**: no pairing adoption reaches the branch class in definite signature (a new protection theorem, §4.1), and the branch scalar supplies no pairing, no non-commutativity, and no backward pairing (scalar inertness + a no-transfer lemma, §4.3). Adopting any one wall supplies **none** of the others.

The question as posed in the frontier §7 ("are FC-1, FC-2, FC-W, and the L²-not-L¹ budget the *same* import — does adopting any one force the others? … a distinctness proof also CLOSES the question") is hereby **closed as a declaration: PARTIAL.** What unifies, unifies at the type level and inside single walls; what was hoped for — a collapse of 3+ commitments into one — is refuted. The hoped-for prize is replaced by a sharper one: **the only channel through which the two axes could ever merge is literally the same open loophole that gates MC-T4.3's exit (1)** (§5.3) — the two flagship open questions of the framework gate on one door.

Sub-verdicts, tagged:

| # | Claim | Tag | Verified |
|---|-------|-----|----------|
| 1 | All four walls instantiate the frontier's one schema (chosen adjoint of a lossy map) | [SYNTHESIS] — restates FTD-0336 §4, nothing new | — |
| 2 | The adjoint/measurement import M is pairing-relative: adopting M supplies a quadratic pairing (GNS/Born); the converse fails | [THEOREM — external-standard + counterexample] | B4–B6, F2a/F2b |
| 3 | FC-2's metric half **is** the flux-sector quadratic pairing; conserved definite pairing ⟺ unimodular spectrum ⟹ sector reversibility; γ = √(1−v²) is the algebraic shadow of the quadratic form | [THEOREM] (lemma) + [DERIVED — consolidation with FTD-0252/0253] | C1a–C3, C5a/C5b |
| 4 | A lossy (many-to-one) sector can conserve **no** definite pairing: the pairing axis is *forced* sector-local | [THEOREM — trivial but load-bearing] | C4a/C4b |
| 5 | Pairing adoptions do not transfer across sectors; the substrate (flux pairing adopted, A₅ commutative, state budget L¹, Born absent) is itself the counterexample | [THEOREM by citation: FTD-0243, FTD-0208, FTD-0200] | F2b + cited rows |
| 6 | **Protection theorem:** at the real place over (4G\*−1), sums of squares of real-embedded native-hull elements have even valuation; the Euclidean budget can never manufacture the branch class | **[THEOREM]** (new; conditional on Chudnovsky for the hull model only) | D2–D4, D6 |
| 7 | **Signature asymmetry:** the indefinite (Lorentzian) budget reaches the branch class in one step from documented natives — (2G\*)² − (√G\*)² = δ² — but supplies neither of FC-W's two bits; its δ-reach gates on exactly the FTD-0314 §4 loophole | [THEOREM — identity] + [DERIVED — relative to the documented inventory] | A6a–A6c, D5, D7 |
| 8 | **Scalar inertness:** adjoining δ preserves commutativity, preserves the manifestation kernel, and selects no pairing (no-transfer lemma); the arrow-ℤ/2 cannot be equivariantly identified with the Galois-ℤ/2 | [THEOREM] (+ FTD-0314 narrowing for the carrier clause, conditional Chudnovsky) | E1–E6 |
| 9 | M and reversibility are logically independent (all four cells of the 2×2 table realized) | [THEOREM — counterexamples] | F1a–F4 |
| 10 | **Main theorem:** four walls = two axes; no wall supplies another; full unification refuted | [THEOREM — relative to §1, conditional as stated] | matrix of §5.1 |
| 11 | Corollary: the constitution's mixed pattern (decline, decline, adopt, decline+axiom) is *coherent only because* the walls are distinct | [OBSERVATION/COROLLARY] | — |
| 12 | Corollary: T2's sole surviving collapse channel = MC-T4.3 exit (1) = FTD-0353 §8's single falsifier | [DERIVED — consolidation] | D5 + cited rows |

**Nothing promoted.** `x₊ = 1/α` stays `[STRONGLY MOTIVATED CONJECTURE]` (FTD-0013); MC-T4.3 stays `[FOUNDATIONAL OBSTRUCTION]`; FC-0, FC-1, FC-2, FC-W stay adopted `[AXIOM]`-class commitments exactly as declared — this document proves facts *about* the imports' mutual relations, it neither earns nor discharges any of them; FTD-0208's `[CLOSED NEGATIVE]` and the clock hypothesis `[AXIOM]` stand; **no α is derived anywhere.**

---

## 1 · The four walls, formalized as import contents

The frontier (FTD-0336 §4) catalogs the walls; to adjudicate unification we need each wall's **import content** — the mathematical datum the choice selects — and its **choice space**. (The frontier's fifth row, the cavity-dispersion boundary FTD-0270, is a *measurement*, not an import-decision, and is out of scope here, as in the T2 question itself.)

| Wall | Canonical row(s) | Import content (the datum) | Choice space | Decision taken |
|---|---|---|---|---|
| **W1 — measurement map** | FC-1 = FTD-0255; fork licensed by FTD-0243 [THEOREM] | M = non-commutative deformation of the observable algebra A₅ **+ Hilbert-space states + operator observables** (FC-1's own characterization) | deformations + states (infinite-dimensional) | **declined** |
| **W2 — arrow / metric** | FC-2 = FTD-0256; boundary mapped by FTD-0253 | *two halves:* (metric) the sector-scoped second-order quadratic action term (Δ_tJ)² on the flux wave sector [AXIOM]; (arrow) the *declination* of a backward pairing across the many-to-one manifestation map | (metric) a positive quadratic temporal pairing; (arrow) a dynamics-inverting section | metric **adopted (sector-scoped)**; backward pairing **declined** |
| **W3 — the branch δ** | FC-W = FTD-0315; pinned by FTD-0314; = adopted ramification at (4G\*−1) per FTD-0353 §6 | a point of the ℤ/2-torsor {±δ} of square roots of G\*(4G\*−1) over the native closure — equivalently the double cover of the hull branched over {4G\*−1 = 0} | a 2-element Galois torsor, **free** σ-action (E3b) | **adopted** |
| **W4 — L²-not-L¹ budget** | FTD-0208 [CLOSED NEGATIVE ⟹ clock AXIOM]; SPEC_FTD_LAGRANGIAN §4.3 conditional | the Pythagorean (quadratic, polarization-bearing) composition law on the state-sector (1+3) budget domain — the pairing whose consequence is (dτ/dt)² + v² = 1 | positive-definite pairings / composition laws (L¹ vs L² differ on the entire budget interior, B3) | **substrate-derivation closed negative**; γ-consequence imported at coordinate level as the clock [AXIOM] |

**Definition (supply).** Adopting wall X **supplies** wall Y if every model of the substrate + X's import canonically contains Y's import datum — *canonically* meaning: via a symmetry-equivariant, dynamics-respecting assignment from X's choice data to Y's, non-constant on X's choice space (so that X's *choice* does work for Y, rather than Y being chosen again separately). This is the precise sense in which "one import" would collapse the axiom count; anything weaker (mere consistency, mere shape-similarity) is the schema level, which is not in dispute.

`[SELECTION — declared]`: the formalizations above are readings of the canonical rows, drawn as narrowly as those rows permit; the adjudication below is relative to them. Each is quoted from, not added to, its source row.

---

## 2 · One question, four answers: schema unity is not import identity

The frontier's thesis — every wall is "the chosen adjoint of the substrate's lossy forward map" — is preserved verbatim and is what made full unification plausible: if the four choices are sections of *one* lossy map, one section might induce the rest. The adjudication turns on a distinction the schema does not itself make:

> The walls are **one question asked four times** (choose the collapsing structure the forward dynamics cannot supply), **not one answer given four times**. The *question*-form is shared [SYNTHESIS, FTD-0336]; the *answer*-data live in choice spaces of different mathematical type — a deformation-plus-state (W1), a conserved temporal pairing and a dynamics section (W2), a free Galois torsor point (W3), a composition law (W4) — and "one import" is a statement about the answers.

The FTD-0340 sqrt-as-act principle already names the shared question-form (each choice is an unforced two-valued-or-larger branch act); FTD-0326/0341 already closed the concrete identifications of the *acts* (the five native ℤ/2's and the four analytic orientation carriers are all δ-blind). This document completes that pattern at the level of the walls themselves.

---

## 3 · The pairing axis: walls 1, 2(metric), 4 are one structural type — and provably sector-local

### 3.1 · Lemma A — the adjoint is pairing-relative (W1 ⊇ W4's statistics face)

**Lemma A** `[THEOREM — external-standard]`. Any adoption of M in FC-1's sense (operator observables with adjoints, positive states) determines a positive sesquilinear pairing: the GNS form ⟨a, b⟩_ω = ω(a†b) of any state ω is PSD (verified on the M₂ model, B6), the C\*-norm law is quadratic (‖a‖² = sup of the form ⟨aψ, aψ⟩, B4), and the Born functional is |c|²-homogeneous (B5). Adjointness (†) is definable only relative to such a pairing — *the chosen adjoint literally requires the pairing that defines adjointness.*

So the measurement import **contains** a quadratic pairing on the state-statistics face: had FTD adopted M, the L² statistics (Born) would have come with it. The trace of this containment is already in the corpus as PL-1 (FTD-0200/FTD-0258): the substrate's native detection statistics are Rice/threshold-shaped, not |ψ|² — the state sector *lacks* exactly the pairing M would have brought. The converse fails (§3.4): a pairing does not supply M.

Two scope fences, stated plainly: FC-1 *declines* M, so nothing is in fact supplied by W1 to anything; and M's pairing is the **statistics** face of W4 only — claiming M ⟹ the matter-clock budget (proper time of clusters) would be relativistic-QM overreach and is **not claimed**.

### 3.2 · Lemma B — conserved definite pairing ⟺ spectral reversibility (W2's metric half *is* W4's flux instance)

**Lemma B** `[THEOREM]`. Let U be a linear update on a finite-dimensional space. (i) If U preserves a positive-definite (Hermitian) form E — U†EU = E — then U is invertible with all eigenvalues on the unit circle; the E-orthogonal group is where backward pairings live for free. (ii) Conversely, a diagonalizable update with unimodular spectrum preserves such a form (construct E = (P⁻¹)†P⁻¹ from the eigenbasis). (iii) An update with an eigenvalue strictly inside the disc preserves **no** definite form.

Verified on the substrate-shaped models: the CFL-stable second-order (velocity-Verlet) lattice wave update has unimodular spectrum (C1a), an explicit backward pairing R M R M = I with R the velocity flip (C1b), and an explicitly constructed conserved positive-definite form (C1c); random E-orthogonal updates are unimodular (C3); the first-order (diffusion) update has spectrum strictly inside the disc, hence conserves no definite pairing (C2), and its pairing — present but not conserved — supplies nothing (F3).

**Consequence** `[DERIVED — consolidation]`: FC-2's adopted metric content — the (Δ_tJ)² term — *is* the temporal quadratic pairing of the flux sector; given its conservation by the bare second-order flux dynamics, **sector-scoped reversibility is a corollary of the pairing, not a second import**, and the FTD-0252 result (γ = √(1−v²) as an *algebraic identity* of second-order dynamics) is its dispersion shadow: quadratic dispersion ω² = c²k² + m² gives m/ω = √(1 − v_g²/c²) exactly (C5a), while first-order dispersion has no real carrier at all (C5b). This is FTD-0253's dividing principle ("2nd-order/wave ⟺ reversible") upgraded from a classification to a supply relation with an explicit mechanism. Honest engine caveat: this is a statement about the bare wave sector; the full engine tick includes non-conservative stages (manifestation; the Gauss-projection leak noted in the 2026-06-01 audit) — which is the sector boundary appearing in the engine, not a counterexample.

### 3.3 · Lemma C — lossiness bars conserved pairings (the sector-locality is *forced*)

**Lemma C** `[THEOREM — trivial but load-bearing]`. A many-to-one map conserves no definite pairing: metric-preserving maps are injective (d(f(x), f(y)) = d(x,y) > 0 whenever x ≠ y); linearly, A†EA = E with E ≻ 0 forces ker A = 0 (C4a; toy manifestation collapse witness C4b).

The discrete readout `R` is many-to-one while retaining `J` (FTD-0394), and the current complete update `F` is separately non-injective via evaporation (FTD-0395). Under FC-2, the state sector therefore cannot carry a globally invertible dynamical pairing; a pairing import there can only be *descriptive* (coordinate-level), which is precisely where the corpus already put it: the clock hypothesis [AXIOM] at coordinate level (FTD-0208's own downstream note) and Born statistics as the declined M-face. This conclusion is scoped to the declared/current irreversible sector, not a theorem that no future adoption could change the map. The pairing axis's adoption pattern — dynamical on the reversible flux sector, descriptive-or-declined on the lossy state sector — tracks the lossy/non-lossy split without conflating readout and update.

### 3.4 · No cross-sector transfer — the substrate is the counterexample

Does adopting the flux pairing (which FTD *does*, as the (Δ_tJ)² [AXIOM]) supply the state-sector instances? The substrate itself answers: with the flux pairing adopted, the observable algebra A₅ is provably commutative (FTD-0243 [THEOREM] — proven *about* the substrate including its quadratic action; registered F2a/F2b), the state-sector budget is L¹ (FTD-0208 [CLOSED NEGATIVE] for L²), and detection statistics are Rice-not-Born (FTD-0200). **One system holding wall-2's adopted content while provably lacking walls 1 and 4 refutes every transfer out of the flux pairing.** `[THEOREM by citation]`. FTD-0252's own scoping gives the finer-grained version: the wave-sector γ-identity concerns a *different observable* than FTD-0208's single-event budget — the flux γ does not touch the matter clock.

### 3.5 · The d = 1 degeneracy: why the clock [AXIOM] carries no pairing content

FTD-0354 proved the clock hypothesis and GNC-s are the d = 1 and d = 3 instances of one pinned-isometry shape, and flagged honestly that the (1+3) cross-terms are unpinned. In the present frame that flag is the whole point: **the pairing import's entire content is the polarization cross-term** — the bilinear form is reconstructible from norm data only via polarization (B1), and on a 1-dimensional domain every p-norm coincides (B2) while the L¹ and L² composition laws differ on the entire interior of the budget simplex (B3, exact: they agree only at (1,0) and (0,1)). So the clock hypothesis, a d = 1 statement, could be — and was — adopted *without deciding the composition law*; wall 4 proper only bites when temporal and spatial legs compose. `[THEOREM — trivial]` for the degeneracy; `[OBSERVATION]` for the reading. This dissolves the prima facie overlap between the clock [AXIOM] and wall 2's metric: the axiom sits in the pairing-invisible slice.

---

## 4 · The branch axis: wall 3 is separated in both directions

### 4.1 · Lemma D — the protection theorem (definite signature cannot reach the branch)

Work in the FC-0-locked hull model Ñ ≅ ℚ̄(w, s), w ↦ π^{1/4}, s ↦ √G\* (FTD-0353 §1; conditional on Chudnovsky 1976), with the prime P = (2s−1) over (4G\*−1) and its valuation v (δ² = s²(2s−1)(2s+1) has v = 1, odd: D2).

**Lemma D (protection at the real place)** `[THEOREM — new]`. Let x₁, …, xₙ be elements of (ℚ̄∩ℝ)(w, s), not all zero. Then v(Σᵢ xᵢ²) = 2·minᵢ v(xᵢ) — in particular **even**.

*Proof.* Let k = min v(xᵢ) and write xᵢ = (2s−1)ᵏ yᵢ with v(yᵢ) ≥ 0 and v(y_j) = 0 for some j. Then Σxᵢ² = (2s−1)²ᵏ Σyᵢ², and the residue of Σyᵢ² at P is Σ ȳᵢ² in the residue field κ = (ℚ̄∩ℝ)(w) (s ↦ ½), which is formally real (it embeds in ℝ); a sum of squares with one term nonzero is nonzero in a formally real field, so v(Σyᵢ²) = 0. ∎ (Verified exactly on 50 seeded draws including engineered (2s−1)-divisible terms, D3/D3b, with the residue mechanism exhibited, D4.)

**Consequence.** Every value the **Euclidean** budget machinery can produce — norms of vectors with real native-hull components, i.e. square roots of sums of squares — has even valuation at P, while the branch class [δ²] = [t(4t−1)] has odd parity. **Adopting wall 4 in definite signature cannot manufacture δ or its square class, for any legs whatsoever, forced or not.** This closes the "pairing supplies branch" direction *structurally* in the definite case — strictly stronger than inventory-relative. (It also strengthens FTD-0327's √-machine closure: even granting the L² import's √-operation on all real budget values, the branch stays out of reach.)

### 4.2 · Lemma E — the indefinite witness, and why it supplies nothing

Honesty requires exhibiting the strongest fact *against* the separation, and it is striking:

**Witness** `[THEOREM — one-line identity]`: with both legs documented natives up to ℚ̄ (2G\* = twice the det_ζ ratio; √G\* = 2^{−1/4}·det_ζ(D_{3/4}), rows B2–B3 of FTD-0353's inventory; verified A5c/A6b),

$$(2G^*)^2 - \big(\sqrt{G^*}\big)^2 \;=\; 4G^{*2} - G^* \;=\; G^*(4G^*-1) \;=\; \delta^2, \qquad 2G^* > \delta \; (A6a/A6c),$$

and symbolically v((2s²)² − s²) = 1, odd (D5), versus v((2s²)² + s²) = 0, even (D6). Moreover the native i converts one into the other: (2s²)² + (is)² = (2s²)² − s² (D7). So the **indefinite** (Lorentzian) budget — differences of squares, exactly the (dτ)² = 1 − v² shape wall 4's clock relation takes, equivalently the Euclidean form with one i-dressed leg (the FTD-0340 phase-branch) — is *not* protected: its value-closure over documented natives meets the branch square class in one step.

**Resolution** `[DERIVED — relative to the documented inventory + FTD-0314]`. Reaching δ's *value* among a continuum of reachable values is not supplying FC-W's *datum*. Realizing FC-W requires **two bits** the witness does not carry: (i) a **forced** occurrence — nothing in the dynamics forces the pair (2G\*, √G\*) to appear as the temporal and spatial legs of an actual budget; selecting it by hand is the banned W-CRIT-2 co-fitting move (FTD-0314 §4), and every *documented forced* value has even P-valuation (FTD-0353 T2), so the forced-leg requirement is **exactly** the FTD-0314 §4 / FTD-0353 §8 loophole, within-sector address; and (ii) the **sign of the square root** — the ± of dτ, which is a fresh order-2 choice (the time-orientation bit), not induced by the pairing (a quadratic form is blind to the sign of its root: G2 verifies dd ↦ −dd swaps x₊ ↔ x₋, i.e. the √-sign bit *is* the branch bit, still unchosen). The Lorentzian pairing therefore supplies **zero** of FC-W's two bits; what it does — and this is the honest new fact — is show the two axes are separated by a *theorem* in definite signature and by *only-the-loophole* in indefinite signature. Stated as pricing, not prophecy: **a forcing argument for a native budget leg-pair of odd P-valuation square class (e.g. a forced v² = 1/(4G\*)) would merge walls 3 and 4 and hang the Galois bit on the time-orientation bit — that is the four-walls form of the one door already priced by FTD-0314/0353, not a new door.** No such forcing exists; exhibiting the identity above is a substitution identity unless the legs are forced, and it is presented here strictly as the reason the definite-signature protection theorem cannot be extended to indefinite signature.

### 4.3 · Lemma F — scalar inertness (the branch supplies nothing)

**Lemma F** `[THEOREM]`. Adjoining δ — the base change ℚ(G\*, π) ↪ K = ℚ(G\*, π)(δ), a degree-2 field extension (D1b, E1) — (i) preserves commutativity of every commutative algebra (structure constants unchanged; K itself commutative: E2a/E2b) — so **W3 ⊬ W1**; (ii) does not alter the manifestation map's preimage structure (the state-sector map does not read the flux coefficient field: kernel counts identical over ℚ and K, E6) and a scalar is not a dynamics section — so **W3 ⊬ W2(arrow)**; (iii) selects no pairing and no composition law: σ(δ) = −δ generates a **free** ℤ/2 action on the branch torsor (E3a/E3b), and every Galois-equivariant map from a free 2-element torsor to a choice set with trivial Galois action ({L¹, L²}, {signatures}, {forms}) is **constant** — exhaustively verified (E4) — so the branch choice can do no selecting work for W4; a non-equivariant assignment would itself be a new import. **W3 ⊬ W4.** ∎

### 4.4 · Lemma G — the ℤ/2 trap (arrow ≇ branch)

The seductive identification — FC-2's arrow and FC-W are *both* order-2 branch acts (which is true, and is FTD-0340's point) — fails as an identification of *data*: the time-reversal involution acts **trivially** on every static scalar (constants are fixed by trajectory reversal, E5a), while σ acts **freely** on {±δ}; an equivariant map from a trivially-acted object into a free torsor would have to land in the empty fixed-point set — there is none (E5b). Equivalently, at the carrier level: realizing δ as the invariant of a native finite involution is precisely the finite-symmetry-carrier pattern the FTD-0314 narrowing theorem excludes (algebraic invariants cannot carry a surd transcendental over ℚ; conditional on Chudnovsky), and FTD-0326 closed all five native ℤ/2's concretely. **The two ℤ/2's share a group, not a torsor.** `[THEOREM]` (equivariance argument) + `[THEOREM by citation]` (carrier clause). **W2 ⊬ W3.**

### 4.5 · The ramification separation invariant

FTD-0353 §6 pins FC-W as **adoption of the double cover of the hull branched precisely over {4G\*−1 = 0}**. The pairing axis's structure constants (polarization coefficients, C\*-law, composition laws) are rational — its imports are **unramified everywhere** in the (4G\*−1) sense. Ramification locus is an invariant of an import: an import with branch locus {4G\*−1 = 0} and imports with empty branch locus are not one datum. `[DERIVED]` — the invariant-level restatement of §4.1–§4.4, in the vocabulary the valuation theorem made canonical.

---

## 5 · The adjudication matrix and the main theorem

### 5.1 · All twelve ordered supply relations

"X ⊢ Y" = adopting X's import supplies Y's datum (Definition, §1). Every cell carries its instrument.

| ⊢ | → W1 (M) | → W2 (arrow/metric) | → W3 (δ) | → W4 (L² budget) |
|---|---|---|---|---|
| **W1 (M)** | — | **NO** — non-commutative irreversible dynamics exist (amplitude damping: CP, contractive, inverse not CP; F1a–F1c) | **NO** — a δ-free model of M exists over ℚ(i) ⊂ ℚ̄ ⊂ Ñ; δ ∉ Ñ (FTD-0353 T2/T3) `[THEOREM, cond.]` | **PARTIAL (one-way, statistics face only)** — M contains the GNS/Born pairing (Lemma A); does **not** supply the matter-clock budget; and FC-1 *declines* M, so nothing is in fact supplied |
| **W2 (arrow/metric)** | **NO** — reversible + commutative exists: the substrate's own flux sector (F2a/F2b; FTD-0243) | — (internally: metric ⊢ flux reversibility, Lemma B — one import, not two) | **NO** — arrow-ℤ/2 ≇ branch torsor (Lemma G); metric structure constants rational, protection theorem (Lemma D) | **NO cross-sector** — flux pairing does not transfer to the state sector (§3.4; FTD-0208/0200/0252 scoping); flux instance is W2's own content, an identification not a supply |
| **W3 (δ)** | **NO** — base change preserves commutativity (Lemma F.i) | **NO** — scalar is not a section; kernel field-blind (Lemma F.ii) | — | **NO** — no-transfer lemma: free torsor cannot select in a trivially-acted choice set (Lemma F.iii) |
| **W4 (L² budget)** | **NO** — commutative L² theory exists (the flux sector; classical fields generally) (F2a/F2b) | **PARTIAL (conditional, sector-bound)** — a *conserved* definite pairing yields reversibility where it lives (Lemma B); but conservation is impossible on the lossy sector (Lemma C), so W4-as-state-budget yields no state reversibility; presence without conservation yields nothing (F3) | **NO (definite)** — protection theorem (Lemma D). **NO (indefinite)** — reach without selection; both FC-W bits missing; gates on the FTD-0314 loophole (Lemma E) | — |

The 2×2 logical independence of M and reversibility is fully witnessed: commutative-reversible (wave, F2), commutative-irreversible (diffusion, C2/F3), non-commutative-irreversible (amplitude damping, F1), non-commutative-reversible (unitary channel, F4).

### 5.2 · Main theorem

**Theorem (four walls, two axes)** `[THEOREM — relative to the §1 formalizations; conditional on Chudnovsky 1976 for every δ-clause; FC-0-locked for every hull clause]`.

1. The import contents of W1, W2(metric), and W4 are of one structural type — a positive (sesqui)linear pairing on a sector — and W3's content is a Galois-branch datum with branch locus {4G\*−1 = 0}. *(Type statement; §3–§4.)*
2. No wall supplies any other wall (all twelve ordered relations fail as supplies). The two partial links are intra-axis and do not collapse commitments: M's pairing containment is inert because FC-1 declines M, and the metric-⊢-reversibility link is internal to FC-2's own adopted content. *(§5.1.)*
3. Hence **full unification — "adopting any one forces (or supplies) the others" — is false.** The four commitments (FC-1, FC-2, FC-W, clock-[AXIOM]) are mutually non-redundant: none is eliminable in favor of the others.
4. The unique channel by which the branch axis could merge into the pairing axis is a forced native budget value of odd (4G\*−1)-valuation square class — **identical** to the FTD-0314 §4 loophole and FTD-0353 §8's single falsifier. In definite signature even that channel is closed outright (Lemma D). ∎

### 5.3 · Corollaries

**Corollary 1 (constitution coherence)** `[OBSERVATION]`. FTD's actual commitment pattern — decline (FC-1), decline-plus-scoped-axiom (FC-2), adopt (FC-W), decline-plus-coordinate-axiom (W4/clock) — would be *incoherent* under full unification: one cannot decline and adopt the same datum. The distinctness theorem is what renders the constitution's mixed pattern consistent. (The FTD-0315 row's own honesty note — FC-W is the framework's first *adoption*, and "does no work beyond the α-root" — is recovered here structurally: FC-W is the lone occupant of its axis.)

**Corollary 2 (one door)** `[DERIVED — consolidation]`. The frontier's T2 question and MC-T4.3's surviving exit (1) now gate on the **same** single open loophole (a forced native odd-valuation exhibit). One door, three labels: FTD-0314 §4, FTD-0353 §8, and §4.2 above. A positive exhibit would simultaneously earn W natively (superseding FC-W by derivation, per FTD-0315's own falsification clause) *and* partially merge walls 2/3/4 through the dτ-sign identification; its continued absence is what keeps both questions closed-negative-leaning at the same ~85–90% confidence FTD-0314 assessed.

**Corollary 3 (the frontier, sharpened)** `[SYNTHESIS]`. The modulus/argument frontier survives intact and gains precision: the substrate owns the modulus half; the argument half is not one missing object but **two missing axes** — the pairing (adjoint-defining) axis, whose state-sector absence is *forced* by lossiness (Lemma C), and the branch (root-selecting) axis, whose absence is arithmetic (valuation parity). "The chosen adjoint of the lossy map" remains the right slogan for the question; the answers it demands are provably plural.

---

## 6 · Honest ceiling

- **Conditionality.** Every clause touching δ's transcendence is conditional on Chudnovsky 1976 (as FTD-0244/0314/0353); every hull clause is FC-0-locked (FTD-0358: the hull *is* the FC-0 sector; a ℤ[ω]-reading substrate would need the analysis redone in its sector, where joint Γ-independence is open). The protection theorem's *valuation computation* is unconditional function-field algebra; only its identification with the analytic hull rides Chudnovsky.
- **Relative to the §1 formalizations.** The walls are adjudicated as their canonical rows state them. A future re-formalization of a wall (e.g. an M-variant without Hilbert states, a non-Pythagorean W4-variant) would require re-running the matrix; the lemmas are modular for exactly this purpose.
- **The inventory clause.** Lemma E's resolution inherits FTD-0353 §2.3's inventory-completeness `[SELECTION]` (with FTD-0358's constructor-closure upgrade) for the "every documented forced value has even valuation" premise. The definite-signature protection theorem does **not** inherit it — it quantifies over all real-embedded hull elements.
- **What a v2 would owe.** (i) A category-theoretic statement of the supply relation (§1's Definition is precise but bespoke); (ii) the honest joint (1+3) budget object FTD-0354 §6 already flagged (the mixed spacetime Gram), which is where any future attempt to force an indefinite leg-pair would have to live; (iii) extension of Lemma D to the F-level (non-hull) real places, which is routine but unwritten.
- **Falsifiers, per direction.** (1) Branch←pairing: the one door — a *forced* native budget/period value of odd (4G\*−1)-valuation (= FTD-0353 §8's falsifier verbatim). (2) Pairing←branch: a canonical, Galois-equivariant selection map from the {±δ} torsor to a pairing datum — the no-transfer lemma says it must break equivariance, i.e. exhibit new native structure on the torsor. (3) M-axis: a substrate-native non-commuting observable pair (FC-1's constitutional falsifier, §6.2). (4) Cross-sector: a derivation of Born or the matter-clock γ from the flux action alone (would overturn FTD-0200/0208 and §3.4). Any one of these fires against a specific lemma, not against the document as a whole.
- The schema-level unity is `[SYNTHESIS]` and was never in dispute; nothing here strengthens or weakens the frontier's §3 meta-conjecture, which remains `[CONJECTURE]`/`[SPECULATION]`.

---

## 7 · Scope and verification

Everything proven here is linear algebra, function-field valuation theory, finite group actions, and channel theory about the walls' *formalized import contents* (§1) — no engine campaign was run, no physical constant is derived, and no numerical search of any kind was performed. Whether the engine's clusters satisfy GNC (FTD-0349 §9), whether the genesis cokernel carries δ (the frozen pre-registration), and the FTD-0314 loophole all remain `[OPEN]` exactly as before.

**Verification:** [`scripts/proofs/proof_four_walls.py`](../../../scripts/proofs/proof_four_walls.py) — **56/56 PASS** (2026-07-02, SHA256 `8115e52457b4…`). Test map: Group A frame anchors + the Lorentzian witness (A1–A6c); Group B pairing structure — polarization, d=1 degeneracy, budget-law disagreement, C\*/GNS quadraticity (B1–B6); Group C conserved-pairing ⟺ spectral-reversibility with wave/diffusion models, lossiness bars, γ-identity (C1a–C5b); Group D the valuation layer — protection theorem instances and exact form, residue mechanism, signature asymmetry, i-dressing, radical-tower mirror (D1a–D8); Group E scalar inertness and torsor no-transfer (E1–E6); Group F the 2×2 M-vs-reversibility independence table (F1a–F4); Group G branch-bit bookkeeping — Vieta over K, sign-swap = root-swap (G1a–G2).

*No LEDGER, META_INDEX, tracker, spine, or frontier file is edited by this document.*
