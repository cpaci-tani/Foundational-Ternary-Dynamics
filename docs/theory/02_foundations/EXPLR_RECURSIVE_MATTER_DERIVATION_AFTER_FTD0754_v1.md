# Recursive matter derivation after FTD-0754

**Status:** `[EXPLORATORY SYNTHESIS / TESTABLE HYPOTHESES — NO CLAIM PROMOTION]`  
**Date:** 2026-07-31  
**Depends on:** FTD-0740, FTD-0743, FTD-0744, FTD-0753--FTD-0767  
**Next licensed campaign:** the qualified fresh FTD-0768 long-transport execution

## 1. Result in one sentence

The narrowest matter story presently compatible with the evidence is not “one
occupied voxel” and not “all field near an occupied voxel.” It is a localized
relational core together with a state-determined bound dressing, embedded in an
environmental field fibre, whose identity becomes matter-like only if an open
family of complete states persists under the selected dynamics.

This is a working ontology, not a particle derivation. FTD-0754 discovered a
usable state-only observer. It did not validate the open family, mobility,
formation, charge, a matter pole, or a unique primitive field split.

## 2. The recursive construction

The derivation should proceed in the following order. Each level adds a claim
that the preceding level does not contain by itself.

1. **Substrate event.** A complete instantaneous state is
   \(X=(s,C,F)\). Here `s` is ternary manifestation, `C` is the selected
   constituent/interaction record, and `F` is the complete field state.
2. **Manifestation.** A nonzero ternary value records local polarity. It is an
   event or constituent mark, not yet an enduring object and not yet electric
   charge in the physical sense.
3. **Relational core.** A bounded connected pattern \(K(X)\), with internal
   quotient coordinate \(q(X)\), distinguishes a relation among manifestations
   from an isolated occupied site. Binding and internal structure first become
   meaningful here.
4. **Constraint dressing.** The present core and source data determine a
   selected bound field \(F_{\rm b}(X)\). FTD-0754 uses the finite-support
   minimum-energy Gauss representative. This is a derived dressing under a
   declared convention, not a new primitive and not yet a unique ontology.
5. **Environment.** The remainder
   \[
   R(X)=F(X)-F_{\rm control}(X)-F_{\rm b}(X)
       =F_{\rm o}(X)+F_{\rm bg}(X)
   \]
   is split into outgoing characteristic content and complementary
   incoming-plus-radial background in the registered centered readout.
6. **Object projection.** The instantaneous object data are the quotient
   \[
   m_\theta(X)=
   \big[,a(X),K(X),q(X),F_{\rm b}(X),\mu_{\rm core}(X)\,\big]_{\mathcal G},
   \]
   not the entire complete state and not every coefficient inside a chosen
   radius. Translations, proper cubic rotations, and constituent relabellings
   act through \(\mathcal G\).
7. **Environmental fibre.** The compatible environment above that object is
   \[
   e_\theta(X)=
   \big(F_{\rm o}(X),F_{\rm bg}(X),\mu_{\rm env}(X)\big).
   \]
   Different full states may carry the same instantaneous object projection
   while supplying different incoming disturbances or detached radiation.
8. **Matter family.** Matter is licensed only when a nonzero open tube of full
   states, not merely one projected snapshot, retains positive classifier
   margins for the preregistered horizon and environment class. This is the M3
   gate of FTD-0743.
9. **Motion.** A moving object is a translated path of the quotient data while
   constituent occupancy and dressing may be continually replaced. Motion
   therefore cannot mean “the same occupied lattice sites moved.”
10. **Composition and decay.** Composition requires approximately separable
    object projections plus a measured interaction sector. Decay is exit from
    the metastable tube with every state, field, energy, and polarity transfer
    accounted for. It is not disappearance into nothing.
11. **Particle status.** Only a stable on-shell pole with positive residue,
    controlled width, and the required infrared tests can promote a selected
    classical matter pattern to a particle candidate.

## 3. Identity, persistence, and energy ownership are different

The object projection answers **what present structure is being identified**.
The M3 tube answers **whether nearby complete states continue to realize that
structure**. An energy decomposition answers **how exchange is accounted for**.
None of these answers implies the other two.

In particular, the projected object does not generally evolve as a closed
autonomous state:

\[
m_\theta(TX)\ne f\big(m_\theta(X)\big)
\]

for a universal `f`, because different incoming fields in the same object
fibre can change the next core state. The correct target is therefore a
controlled open subsystem: a family of complete states whose projected object
identity is robust over a declared class of environments.

A quiet bound object must remain eligible when \(F_{\rm o}=0\). Outgoing field
is evidence about exchange or shedding, not a necessary constituent of
identity. Conversely, a bright outgoing pulse with no persistent relational
core is not matter.

## 4. The field-energy ownership defect exposed by FTD-0754

Let \(H\) be the quadratic centered-readout field energy and let
\(R=F-F_{\rm b}\) after the declared control subtraction. Then

\[
H(F)=H(F_{\rm b})+H(R)+I_{\rm br},
\qquad
I_{\rm br}=B_H(F_{\rm b},R),
\]

where \(B_H(u,v)=H(u+v)-H(u)-H(v)\) is the polarization cross
term of the registered quadratic energy. For the conventional
\(H(F)=\tfrac12\lVert F\rVert^2\), this is the ordinary energy inner
product \(\langle F_{\rm b},R\rangle\).

FTD-0754 records `bound_residual_interference = I_br`. Across the 21 nonzero
residual discovery snapshots it is nonzero in all 21, negative in 20, and
positive in one. Its largest magnitude relative to the selected bound energy
is `17.657%` in the edge arm at tick 160. The corresponding maxima are
`16.159%` for the body arm and `15.398%` for the face arm. Therefore the
registered local bound dressing and residual are not an additive pair of
independently owned energy subsystems.

This does not invalidate the exact field reconstruction or the exact
outgoing/background partition *inside the residual*. It invalidates only the
stronger interpretation

\[
E_{\rm object}=E_{\rm core}+H(F_{\rm b}),
\qquad
E_{\rm environment}=H(R),
\qquad
E_{\rm total}=E_{\rm object}+E_{\rm environment}.
\]

The FTD-0754B boundary-ledger addendum now resolves the mechanism more
precisely. On primitive faces, discrete integration by parts gives

\[
I_{\partial K}
=\langle E_{\rm b},E_{\rm r}\rangle_{F_K}
=-\langle\phi,D_{\partial K}E_{\rm r}\rangle_K.
\]

The registered centered cross is not just this boundary term. It obeys the
exact selected-observer identity

\[
I_{\rm br}=I_{\partial K}+I_A+I_B,
\]

where \(I_A\) is the face-centering metric correction and \(I_B\) is the
integer-time magnetic-readout cross term. The 24 existing discovery snapshots
close the primitive boundary identity to `3.62e-16` and the three-term identity
to `1.30e-18`. At face tick 115 the primitive boundary term is negative while
the centering term reverses the sign of the centered total. Thus
\(I_{\partial K}\), not \(I_{\rm br}\), has the direct support-boundary meaning;
\(I_A+I_B\) belongs to the selected readout convention.

The remaining choices are therefore to retain a local primitive boundary
ledger, to derive a common-action subsystem surface term, or to use a global
energy-orthogonal Hodge dressing and accept nonlocal support. Calling the
observed streamlines a literal object-owned aura, wake, pilot wave, photon
bundle, or material strand would still outrun the result. See
[THEOREM_STATE_ONLY_BOUNDARY_ENERGY_LEDGER_v1.md](../10_eft_program/derivations/THEOREM_STATE_ONLY_BOUNDARY_ENERGY_LEDGER_v1.md).

## 5. Working definition

The current native-first candidate is:

> **Matter is a localized, state-identifiable relational core whose required
> constraint dressing and admissible environmental fibre form a nonzero open
> family of complete states with a finite, volume-controlled survival margin.**

This definition is intentionally stronger than “localized energy” and weaker
than “particle.” It allows constituent turnover, internal oscillation, field
exchange, and deterministic decay. It excludes a single occupied voxel, a
free packet, a static imposed source, a transient collision, periodic return,
and an object label reconstructed from history.

“Solid” then means long residence time and small deformation within such a
family, not metaphysical permanence. “Decay” means a dynamically resolved exit
from it. These interpretations remain selected until M3--M6 supply evidence.

## 6. Intuitive questions that now have sharp tests

1. **If detached radiation is changed outside the core's causal buffer, does
   the core initially evolve identically?** Add only source-free,
   Gauss-compatible residual packets outside a preregistered radius and compare
   the object projection until first causal contact.
2. **Is the bound dressing part of identity or only the constraint solution
   forced by the constituents?** Compare states with the same core but distinct
   admissible divergence-free fibres. If the same \(F_{\rm b}\) and margins are
   reconstructed, dressing is derived identity data rather than an independent
   constituent.
3. **Does the primitive boundary ledger stabilize as the bookkeeping surface
   moves?** FTD-0754B proves that the primitive term is a boundary bilinear and
   that the centered total includes separate readout corrections. The next
   test is a preregistered support-radius ladder: object identity should remain
   fixed while boundary and readout pieces redistribute consistently.
4. **Can an energy-orthogonal split remain local?** Construct the global Hodge
   control and compare its support and causal usability with the finite-support
   observer. Failure of simultaneous locality and orthogonality would expose a
   genuine subsystem-boundary tradeoff.
5. **Can two states with the same instantaneous object projection have
   different futures?** Phase-varied incoming packets should leave initial
   identity unchanged while altering survival margins only after contact. A
   pre-contact difference would falsify the proposed state projection or the
   causal implementation.
6. **Does quiet matter remain identifiable with no outgoing characteristic?**
   A stable negative-energy core with \(F_{\rm o}=0\) must be accepted if the
   core and environmental margins pass. Otherwise the classifier has confused
   radiation with matter.
7. **Is polarity a constituent orientation rather than conserved physical
   charge?** Track polarity through every allowed reaction and solve the exact
   reaction invariant. Until a nontrivial invariant survives, `+/-` is polarity
   and physical charge remains emergent/open.
8. **Can identity move while sites turn over?** The object projection should
   translate covariantly while its occupied-site set changes, its bound dressing
   re-centres, and detached characteristics remain behind. This is the M4 gate.
9. **Can two objects remain two?** At increasing separation, the classifier
   must factor into two localized cores plus an interaction term rather than one
   diffuse object. This is the beginning of M5 composability.
10. **Is formation an actual basin entry?** A complete predecessor must fail
    the state-only predicate and its successor must pass it without consulting
    a route label. That is stricter than observing a temporary concentration.
11. **Is decay merely dispersal or a structured conversion?** Exit histories
    must be classified by where core energy, interaction energy, polarity, and
    outgoing characteristics go.
12. **Does the object have a physical excitation pole?** Only after M3--M6 pass
    should retarded response be asked whether the family supports a narrow,
    positive-residue matter mode.

## 7. Decisive next sequence

No new trajectory should be inspected before FTD-0755 freezes the following.

1. Keep the FTD-0754 identity and characteristic formulas unchanged; freeze the
   derived FTD-0754B boundary-ledger fields before validation.
2. Freeze core and environmental margins while keeping internal energy,
   primitive boundary exchange, centered-readout corrections, and
   environmental energy separate. Do not use `bound_residual_interference` as
   a membership margin.
3. Use held-out core perturbations, phase-varied source-free incoming packets,
   a quiet bound control, free-packet/static-source/transient-contact controls,
   and nested causal volumes.
4. Prove finite-horizon regularity from strict numerical margins where the
   complete tick is smooth on the registered event branch. Otherwise report
   sampled robustness only.
5. Run the causal-fibre test: paired full states with the same initial object
   projection and different exterior residuals must agree before causal
   contact and may differ afterward.
6. Test at least two preregistered support radii or prove that one support is
   selected, and compare the primitive boundary ledger with the exact
   common-action subsystem ledger. Do not substitute centered-readout energy
   for the action.

If the held-out tube survives with nonzero margins across volumes, the program
has a finite-time selected matter family at the exact achieved level. If the
observer fails controls or the tube collapses to isolated trajectories, FTD
has not derived matter in this selected dynamics. That outcome would motivate
a separately versioned dynamics or state enlargement; it would not license a
post-hoc repair under FTD-0755.

### FTD-0755/0756 execution addendum

FTD-0755 did not reach either branch of that physical discriminator. All
twelve registered modes stopped before the tick-160 parent was initialized.
FTD-0756 reproduces the established parent scalars exactly until it localizes
the abort to a readout-domain mismatch: a continuously valued constituent
midpoint was supplied to a regional observer whose selected lattice-cell
center must be exactly integer-valued.

This sharpens the ontology rather than deciding it. Three maps must remain
distinct:

1. the relational object map, which uses continuous relative constituent
   coordinates and must translate covariantly;
2. the primitive lattice evolution, which deposits oriented current and
   updates face/edge fields without an observer center;
3. a regional environmental readout, whose fixed integer center is only a
   measurement chart unless a fractional-center interpolation is separately
   derived and proved.

The next admissible sequence is therefore: qualify the exact parent with the
fixed preparation center through tick 160; freeze a fresh held-out validation
identifier; then test the open family. Changing the object predicate, action,
or dynamics is neither required nor licensed by the FTD-0755 wrapper failure.

FTD-0757 completes the first step constructively. All six `{L=321,385}`/ray
parents reach tick 160, all 3846 registered FTD-0753 scalar comparisons are
exact, and the continuous midpoint becomes noninteger on exactly the prior
FTD-0756 abort ticks. The fixed integer chart therefore removes the
instrumentation failure without changing a physical transaction. Fresh
held-out validation is now authorized under a new identifier; M3 itself is
still open.

### FTD-0758 execution addendum

FTD-0758 completes all 18 candidate histories and all six baseline/remote
causal-fibre comparisons, but its inherited certificate contains a second
representation error. The selected dynamics explicitly permits a shared
integer anchor chart for two constituent records with distinct continuous
remainders. The runner and engine therefore use

```text
allow_shared_anchor_chart || site_projection_valid,
```

while the frozen certificate demands `site_projection_valid` alone. The raw
unique-anchor bit is false on 2230/2736 dynamic candidate rows, so all 18
candidate summaries disagree with the certificate even though the accepted
transaction bit and every other frozen gate pass. Certificate 6239/6257 makes
the official run infrastructure-unresolved.

Post-hoc removal of that contradictory conjunct makes all candidate, volume,
and fibre branches satisfy the selected-family verdict, but the output had
already been inspected. This is evidence that the relational/shared-anchor
construction deserves a fresh successor, not a licensed promotion of M3.
The ontic lesson is narrower and durable: distinct constituent identity cannot
be equated with distinct occupied site anchors when continuous remainders are
part of the selected coupling representation.

### FTD-0760 execution addendum

FTD-0760 froze the conditional relational-chart rule in both the CUDA runner
and independent certificate before any fresh candidate ran. All six
registered modes then ran once. The complete 24-artifact record passes
9002/9002 checks and returns
`M3_FINITE_TIME_SELECTED_MATTER_FAMILY`.

All 18 fresh center/hostile histories remain in the support-independent core
through tick 312. The smallest graph and internal-energy margins remain
strictly positive, and the common-action/root-regularity gates remain far
inside their locked tolerances. Shared integer anchors occur on 2238/2736
continuation rows without destroying relational constituent identity. In the
causal-fibre arms, a nonzero remote change to global field energy produces
exactly zero registered pre-contact difference in the core, constituents,
local field, and bound projection.

The finite-time selected M3 gate is therefore closed constructively. The
result is about a relational family of complete states, not a permanently
occupied pair of lattice addresses and not yet a physical particle species.

### FTD-0761 execution addendum

FTD-0761 applies equal signed momentum `q=0.015` to both constituents of the
certified tick-160 parent while deliberately leaving its face/edge field
unchanged. All face/edge/body rest, plus, and minus arms then run 256 exact
CUDA-backed transactions. Certificate 247/247 returns
`M4_BOOSTED_RELATIONAL_COHERENCE_CLOSED_AT_REGISTERED_SCALE`.

The closure is narrower than a failure of reciprocal motion. Every boosted
arm remains inside the support-independent core, conserves the registered
energy/common action, stays causal, has a well-conditioned root, and passes
one-step state-only inversion. Its center translates secularly by more than
one cell, with positive displacement in every 64-tick block and plus/minus
mirror residual below `1.35e-11`. The only exact/coherence failures are the
state-only matter/field observer and nested support ladder at all 24
post-initial boosted checkpoints. The unboosted controls pass 15/15.

Thus adding momentum to matter while leaving its dressing at rest is not a
certified moving complete state. The record does not distinguish a genuine
non-comoving dressing from a fractional-center/numerical covariance defect in
the observer, because only its verdict bits were frozen. The next step is an
observer-covariance forensic gate, not a force multiplier or a new mass
formula. A new joint matter/field preparation is admissible only under a new
identifier after that distinction is resolved.

### FTD-0762 correction

The discriminator ran on WSL2 CUDA for face, edge, and body at the first
failed checkpoint. Certificate 179/179 returns
`OBSERVER_INTEGER_CENTER_CHART_OBSTRUCTION`. Every evolved centroid is
fractional, and the selected finite-support preparation rejects it before any
field comparison. A rigid recenter preserves relative geometry and both
momenta exactly, then makes the CUDA observer, boundary ledger, and support
ladder pass on all three rays.

FTD-0761's Boolean observer failures therefore do not establish a stationary
or incoherent dressing. The corrected result is a secular reciprocal
moving-core witness with complete dressing and momentum balance still open.
The next observer must be defined on arbitrary fractional centroids; only then
can the untouched evolved fields be classified.

### FTD-0763 through FTD-0767 corrections

FTD-0763 constructs the missing fractional-center field observer. Its CPU/CUDA
qualification and untouched `L=321` face/edge/body replays pass 175/175
independent checks. The selected bound projection can therefore be
reconstructed covariantly at the continuously translated relational center.
This repairs the observer domain; it does not prove that the actual field is a
rigid material coat.

FTD-0764 then compares the actual-minus-selected-bound residual in transported
charts. Certificate 1524/1524 rejects rigid near-field co-motion on every ray.
The far outgoing component is rest-matched, and neither the local nor spline
translation-momentum candidate closes. The manifested relational kernel moves,
but the measured surrounding field is not one rigid shell carried with it.

FTD-0765 proves that the historical trailing-wake number is exactly a
rest-subtracted centroid lag. By the last registered checkpoint, residual-field
entrainment is only `-1.1%/15.7%/16.9%` on the three rays. This is evidence for
separation between the moving kernel and most of the environmental residual,
not independent creation of a wake.

FTD-0766 ages the parent and tests signed boosts. The larger boost becomes
admissible after aging, but the registered execution is invalid: the raw
field-pair mirror gate fails, asymmetry has the wrong boost ordering and poor
age stability, and residual entrainment stays below 5%. Aging changes the
admissible state basin; it does not establish wake creation or an inertial
mass.

FTD-0767 finally separates local deformation from spatial deposition. Signed
rest subtraction leaves a positive velocity-aligned local response, but every
valid arm travels less than `0.876` site. Its initial and final selected
support and near windows overlap. A spatially detached wake was therefore not
tested. The exact quadratic identity

```text
U(F_q)-U(F_0) = <F_0,F_q-F_0> + (1/2)||F_q-F_0||^2
```

also prevents signed energy difference from being relabelled as nonnegative
created-field energy.

### FTD-0768 completed invalid gate

FTD-0768 froze the first long-transport discriminator: the unchanged aged
face parent at `L=321`, a rest arm and `q=0.030` moving arm, 768 ticks,
checkpoints every 64 ticks, laboratory clearing at `d>=9`, paired actual and
selected-residual response, regional source/boundary accounting, both existing
momentum candidates, and complete state-only reversal.

Three launches were stopped before any registered checkpoint or artifact
because pre-result audit found observer/record defects. The first two are known
to have stopped before formation; the third run's buffered empty log does not
establish whether formation completed. Those attempts carry no physics status.
The first two exposed boundary-quadrature and record/certificate gaps. The
third exposed a missing moving-mask sweep term even though its fixed-mask
one-step balances were exact. Complementary boundary and mask-sweep quadrature,
measured initial momentum, interval-by-interval fixed/moving-volume
certification, every-tick rest control, complete discrete reversal metadata,
and affine/quadratic/mixed response fixtures pass. The fourth qualified WSL2
CUDA execution completed and wrote artifact SHA256 `FE7E1915...170C3F`.
Independent certification returns `2220/2221`: continuous matter/field reverse
recovery is `3.8786822642578e-9`, above the locked `1e-10` gate, so the frozen
outcome is `LONG_TRANSPORT_EXECUTION_INVALID`. The invalid record
descriptively crosses `d>=9` at ticks 704 and 768, but no cleared-response,
matter, or wake verdict is admissible.

## 8. Current epistemic boundary

Established at the present scope:

- `[NUMERICAL FACT / SELECTED DYNAMICS]`: a nonzero finite-time open family of
  complete states preserves the support-independent relational-core predicate
  through the certified M3 horizon;
- `[NUMERICAL FACT]`: the relational center translates secularly and
  reversibly under registered collective boosts while constituent site anchors
  change;
- `[DERIVED OBSERVER]`: a fractional-center selected Gauss representative is
  reconstructed from the instantaneous state without history;
- `[NUMERICAL FACT]`: the actual near field is not a rigidly co-moving coat at
  the FTD-0764 scale, and most measured residual energy is not entrained by the
  moving kernel;
- `[DERIVED OBSERVER ALGEBRA + NUMERICAL FACT]`: rest-subtracted local
  velocity-aligned deformation exists, but previous trajectories never cleared
  their own support;
- `[DERIVED LEDGER]`: bound/residual interference, field-difference norm,
  regional boundary transport, and source work are distinct quantities and
  cannot be exchanged by interpretation.

Still open:

- long mobile identity through a spatially cleared laboratory region;
- whether a passed region relaxes, retains an energy-balanced wake, or retains
  only reversible preparation memory;
- a closed translation-momentum or crystal-momentum/stress ledger;
- an asymptotic or volume-stable metastable basin beyond the finite M3 tube;
- autonomous formation, composability, collision, and dynamically accounted
  decay;
- reaction-complete conserved charge and local gauge redundancy;
- inertial mass, a positive-residue matter pole, statistics, unitarity, and
  infrared spacetime recovery.

The immediate target is now a fresh reverse-error localization protocol, not a
constant formula, force multiplier, or particle label. It must determine
whether the FTD-0768 defect grows with horizon, backend arithmetic, or one
state component before any new clearing campaign is frozen. The completed
artifact and its `1e-10` tolerance are immutable.

## 9. Matter reconstructed as a process type

Let a complete instantaneous state be

```text
X_t = (K_t, E_t, B_{t-1/2}),
```

where `K_t` contains the manifested constituent records and their derived
relation. Let `P_core(X_t)` be the certified state-only membership predicate,
`C(X_t)` its continuous relational center, and `B[K_t]` the selected
instantaneous bound-field representative. Define

```text
R_t = (E_t,B_t) - B[K_t].
```

The evidence no longer supports putting all of `R_t` inside the object. It
supports the following selected process type:

> **A finite matter process is a symmetry-equivalence class of complete-state
> histories whose relational core remains inside a nonzero state-only tube,
> whose center follows a causal worldline, and whose exact matter--field
> transaction, energy, Gauss, and inverse gates close while environmental
> field content crosses its causal boundary.**

This statement is a synthesis/selection conditional on the registered common
action. It is not yet a theorem that FTD contains physical matter. It makes the
minimal identity candidate

```text
identity data      = relational core + internal state + polarity ordering,
constitutive data  = state-determined constraint dressing,
environmental data = incoming/outgoing residual field and preparation memory.
```

The whole residual field is not identity data merely because it is visually
near the core. Conversely, the constraint dressing need not be a second
substance: it can be the field relation that the instantaneous manifested
configuration must satisfy.

Seven consequences follow at the present selected status:

1. **No permanent voxel essence.** Site occupation is a local event in the
   process; object identity is relational and may survive anchor turnover.
2. **No literal field-line strands.** Rendered streamlines visualize an
   interpolated vector field. They are not ontic strings, photons, or a pilot
   wave without a separate discriminator.
3. **Motion is world-tube transport.** It is persistence of the relation while
   `C(X_t)` moves, not merely a hop counter and not a rigid translation of all
   nearby coefficients.
4. **Solidity is residence time.** A solid object is a long-lived, low-
   deformation region of state space, not an eternally fixed substance.
5. **Decay is tube exit.** Matter decays when its relational membership fails
   and its energy/polarity/field transfers are accounted for; nothing simply
   vanishes.
6. **Inertia must be response, not a fitted label.** Candidate mass must agree
   among energy curvature along the moving family, impulse response, and an
   on-shell pole residue.
7. **Polarity precedes charge.** `+/-` is constituent orientation. Physical
   charge requires a conserved or infrared-stable response sector and cannot be
   assumed from the sign token alone.

## 10. Recursive intuitive questions after the clearing gate

1. **What persists after nine sites?** Does the same internal relation and
   polarity ordering survive, or does only the classifier name persist?
2. **Does the old laboratory region forget?** After clearing, do signed energy
   difference and difference-field norm decay toward the rest arm?
3. **If it remembers, who paid?** Does persistent lab energy equal source work
   plus oriented boundary transport on every checkpoint interval?
4. **Is the memory a wake or reversible phase information?** Does it remain
   localized behind the core or propagate away through separated surfaces?
   Complete reversal is an invertibility gate, not a radiation classifier,
   because reversible radiation also unwinds.
5. **Does matter carry a coat or continually redress?** Is the selected bound
   component reconstructed locally at the new center while old residual field
   is left behind?
6. **Why does aging admit larger boosts?** Does age deepen the core-energy
   margin, settle an internal phase, or merely redistribute environmental
   field energy?
7. **Where does translation momentum go?** Does either pseudomomentum defect
   converge, or is the missing quantity a measurable cubic-lattice stress/
   crystal-momentum channel?
8. **Is the boost energy an inertial scalar?** Is complete energy versus small
   collective boost quadratic, direction-independent in the infrared, and
   independent of preparation age?
9. **Can a field packet launch the same world tube?** Does a causal incoming
   transverse or longitudinal packet produce the already qualified moving
   state without direct constituent boosting?
10. **Can two world tubes remain two?** At large separation, do two predicates
    factorize before contact and re-emerge after scattering with their identity
    data intact?
11. **What is a species?** Are distinct long-lived families separated by
    recurrence spectrum, conserved sector, and scattering response rather than
    by arbitrary initial labels?
12. **What is charge operationally?** Does a distant test process respond to a
    stable asymptotic Gauss class even if microscopic signed occupancy changes?
13. **What is the first particle-grade fact?** Is there a narrow retarded pole
    with positive residue and controlled width shared by all members of the
    finite matter family?
14. **What fails first under stronger motion?** Core membership, inversion,
    energy closure, causal speed, or environmental clearing each imply a
    different missing type and must not be collapsed into “matter failed.”

## 11. Outcome-conditioned next recursion

- `LONG_TRANSPORT_EXECUTION_INVALID`: identify the exact failed locked gate.
  This closes the registered long-mobile claim; any repair requires a fresh
  identifier and cannot rewrite FTD-0768.
- `CORE_CLEARING_NOT_REACHED`: the selected family remains mobile but does not
  leave the lab slab at this boost/horizon. Wake language remains unavailable.
- `CLEARED_LOCAL_RESPONSE_DECAYS`: matter is best modeled as a persistent
  relational kernel that moves through and locally redresses an environment,
  without a durable trail at the measured scale.
- `CLEARED_LOCAL_RESPONSE_PERSISTS`: an energy-balanced environmental memory
  survives passage and licenses a separately frozen wake-persistence test. It
  does not make the wake part of object identity.
- `CLEARED_LOCAL_RESPONSE_MIXED`: more time/space resolution is required; no
  component may be labelled wake or dressing post hoc.

Only after a valid clearing outcome should the program freeze the three-way
mass test: boost-energy curvature, causal impulse response, and matter-pole
residue. Agreement among those routes would be the first defensible derivation
of inertial mass for this selected matter family.

## 12. Moving-boundary correction

The third pre-result FTD-0768 execution exposed a missing moving-control-volume
term before any artifact was written. A one-step field balance can be exact for
the later-centered radius-eight mask while the cumulative sum fails to equal
the energy change between the initial and final moving masks. The exact finite-
sum identity is

\[
E_{t+1}[\chi_{t+1}]-E_t[\chi_t]
=\Phi_t+W_t+
  \big(E_t[\chi_{t+1}]-E_t[\chi_t]\big).
\]

The last bracket is mask sweep, not physical field transport. The repaired
observer measures it and its complement independently. See
[`DERIV_MOVING_CAUSAL_TUBE_ENERGY_IDENTITY_v1.md`](DERIV_MOVING_CAUSAL_TUBE_ENERGY_IDENTITY_v1.md).
This correction strengthens the process ontology: a moving measurement tube
is not automatically a membrane, and energy entering its front is not evidence
of carried matter until physical flux, source work, and chart sweep are
separated.

### State-space boundary versus physical surface

The current support-invariant core predicate does not repair that gap by
itself. Its graph margin
`r_cut^2-|x_1-x_0|^2` and energy margin `-(K+V)` define the boundary of a
selected bound-pair family in configuration/phase space. They do not define a
surface in the cubic lattice. At fixed positions, increasing relative momentum
can cross the energy boundary without moving any spatial envelope; under a
common translation, the margins remain identical while the envelope moves.

The defensible extrapolation is consequently many-body and relational:
solid-like matter may arise from a persistent network of binding relations,
with a candidate surface where coordination/support terminates. A spatial
surface still requires a separately frozen state-local support rule and exact
relative energy/momentum balance. This supplies no membrane or macroscopic
matter result yet, but it identifies the next construction without equating a
single voxel or field aura with matter.

The selected compact pair law also fixes a conditional network-scale test. Its
minimum is `V(1)=-epsilon`, its cutoff is `V(3/2)=0`, and its small longitudinal
stiffness is `96 epsilon`. Under an unimplemented pairwise extension, a quiet
surface excess is exactly missing-bond energy. The opposite-polarity edge rule
would make the network bipartite: nearest-neighbour SC and BCC are compatible,
whereas nearest-neighbour FCC contains triangles and is obstructed. FCC would
therefore require a second bond channel, an irreducible many-body rule, or a
projection of a bipartite parent. This is a conditional discriminator, not a
derivation of a crystal, physical domain, or temporal domain.

For the current pair itself, negative rest-subtracted energy is stronger than
the separate graph check. Because `K>=0` and the compact potential is negative
only for `3/4<r^2<3/2`, `K+V<0` implies that the reciprocal edge exists. Exact
isolated pair-energy conservation then forbids spontaneous disconnection. In
the coupled action, breakup requires an accounted field-to-pair energy transfer
that removes the negative energy margin before or during the disconnecting
tick. This is an operational reversible dissociation gate, not a decay rate or
irreversibility result.

### The first many-body extrapolation fails without saturation

The exact discrete-gradient pair impulse extends algebraically to a sum over
opposite-polarity pairs and preserves internal momentum and binding work. That
does not make it a material bulk law. Two packets containing `m` constituents
of each polarity at unit separation form `m^2` minimum bonds, giving
`U=-epsilon m^2`. The linear Gauss field also scales quadratically; the frozen
FTD-0739 unit-pair coefficient is smaller than the well depth, so the stacked
direction remains superextensively negative.

The least new-ontology continuation is not another fitted force. It is a local
unsigned capacity derived from the existing compact shapes,
`n(v)=sum_a W_a(v)<=1`, reflecting the bounded magnitude of a ternary site
without equating a constituent with one voxel. Algebraic contact multipliers
could enforce the constraint without becoming stored state, but exact
activation/release inversion and chart independence remain open. The full
derivation and fourteen next questions are in
[`DERIV_MINIMAL_MANY_BODY_MATTER_NETWORK_v1.md`](DERIV_MINIMAL_MANY_BODY_MATTER_NETWORK_v1.md).

The capacity candidate is already sufficient to repair the large-`N` energy
scaling. Every quadratic coat deposits at least `1/8` unsigned weight at a
nearest site; capacity permits at most eight constituents per nearest-site
class, and only 64 such classes can lie inside one compact interaction ball.
Thus degree is at most 511 and `U_N>=-(511/2)epsilon N`. The remaining blocker
is not thermodynamic extensivity but a unique local contact transaction that
preserves exact work and state-only inversion when constraints switch.

The physical SC/FCC conjecture also admits an exact reinterpretation. Even and
odd SC sites form two FCC polarity sublattices. The current compact well binds
only the opposite-parity SC face edges at squared distance one; same-polarity
FCC neighbours have squared distance two and are outside the cutoff. A uniform
common displacement is a zero binding mode, whereas relative displacement of
the two FCC sectors has `Delta U=48 epsilon N |u|^2+O(|u|^3)` and changes the
signed face-field source. This supplies an acoustic-versus-polarization split
and a surface-imbalance static-electricity candidate. The periodic source has
the exact static field `E_i=chi/48`, but no finite surface, stable pole,
physical charge, or BCC temporal bridge follows.
It also does not yet supply solidity: the SC axial network has quadratic
dilation energy but quartic simple-shear energy and hence zero harmonic shear
modulus. A BCC body-diagonal graph would conditionally restore quadratic shear,
but current dynamics do not supply those bonds. The selected pure BCC temporal
kernel cannot substitute for them: `T_B(0)=0`, so a strain-independent clock
term has zero static shear exactly. Static rigidity would require a derived
deformation-dependent link/connection holonomy or another priced spatial
channel. Polar `(111)` versus neutral `(100)/(110)` terminations provide the
sharp surface-electricity comparison.

The holonomy recursion is now type-closed at the existing-variable level. A
pure clock changes the finite-frequency/inertial coefficient, not the static
one. A gauge link cannot create a local bond force on endpoints carrying only
gauge-neutral position, momentum, and polarity; an open-link coupling needs a
transforming local phase, while a closed-loop coupling is either the already-
counted field-curvature energy or a new material-loop action. The FTD-0494
non-flat work one-form requires the FTD-0495 history fiber and still does not
select one ordinary common action. Therefore the next no-new-primitive
question is not “add holonomy,” but whether the pressure-qualified relaxed
face/edge Hessian survives common-translation and independent-row relaxation.
Only a failure there prices a phase/connection extension.

The finite algebra sharpens this further. At `L=4`, all 48 axial row slides
are tangent to the eight full-rank active capacity constraints and span the
central-bond harmonic null space. Common pressure has a rank-21, zero-trace
nonzero Hessian on those modes: it necessarily creates both uphill and
downhill directions and leaves 27 exact null coordinates. Pressure is
therefore not the missing solidity mechanism. Those nulls are exactly three
`D_perp tensor D_perp` sectors: three translations and 24 internal slip
patterns with zero inner/outer moment. The relaxed field must stabilize
the downhill internal sector and determine whether the surviving null sector
contains only collective motion or genuine fluid-like slips.

The next exact recursion is encouraging but deliberately incomplete. On the
full finite coat support, both the signed-source and unsigned-occupancy first-
variation maps have rank 27 when restricted to those 27 pressure-null modes.
No nonzero pressure-blind combination is invisible to the longitudinal Gauss
source or to the slack surface. Because the signed variations have zero total,
the positive quadratic term `<rho',G rho'>` is strictly positive on the whole
kernel. But complete curvature also contains `<rho'',G rho>`, which may cancel
or reverse that term. The intuitive question is now sharper: does the field
join the rows throughout the body, or does it merely hold and pin the outer
coat? Only the complete Hessian and its size scaling can decide.

A finite checkerboard block then gives the first explicit uncontained
candidate: its exact binding energy is a negative bulk term plus missing-bond
surface tension `epsilon/2`, and removal costs rise from `3 epsilon` at a corner
to `6 epsilon` in the bulk. Capacity prevents stacking while attraction favors
compactness. For the all-even control family an exact compact Gauss field now
proves that minimum redressing is at most extensive, and the frozen selected
coefficients give negative formation energy for every even cube. The remaining
test is dynamical rather than energetic: whether field and capacity-contact
forces relax to a finite stationary shape and whether the released block
remains compact without merely being pinned to the substrate.

Capacity now supplies a precise interface but not solidity. Its finite block
profile has a saturated interior and a one-coat slack layer whose exact
`sum n(1-n)` expansion begins with area. Yet complete rows can slide
independently without changing occupancy, so the capacity sector has exact
bulk slip modes. One incoming contact admits a unique energy-preserving
relativistic impulse magnitude, but that impulse generically carries nonzero
lattice recoil and no face current. The next common-action problem is therefore
to close contact recoil and simultaneous-impact selection, not to add another
attractive force. The minimum-dressed field cannot take that recoil without
positive excitation energy, so a genuinely elastic isolated contact must have
net-zero internal impulse. The next finite static discriminator is the
half-phase block's newly derived pressure-feasibility system, held until
FTD-0768 resolves. The older integer-phase `L=4` one-ratio system remains an
exact saddle control, not the main candidate.

The reason is exact. Complete-row signed and unsigned first variations are
end-cap supported for every even `L`. In the periodic bulk the integer phase
has field-energy curvature `-beta/96` per site under common translation, while
binding and capacity do not change. Half-cell phase cancels the bulk signed
source and reduces finite field data to boundary planes with an area-scaling
Gauss bound. Matter now extrapolates more naturally as a neutral cancellation
bulk whose field response lives on surfaces and defects.

This suggests a dynamical recursion rather than another static force term: a
body may advance by propagating a half-phase sheet through its relational
network. The leading layer manifests into the next neutral phase while the
trailing layer releases the old phase, so persistent identity moves without a
rigid simultaneous volume displacement. The conjecture is testable only if a
local phase-front transaction closes current, field work, recoil, energy, and
state-only inversion; otherwise it is just a geometric path through the exact
zero-field phase manifold.

That manifold is not free ontologically. Its half-cell planes coincide with
FTD-0500's theorem that a unique integer-anchor section cannot preserve both
translation and inversion at the tie. FTD-0624's opposite-polarity duplicate
anchors were representation loss, not annihilation; FTD-0626's multiplicity-
two fibre restored execution and inversion but did not produce fixed internal
rest. A live phase-front model must therefore pick one concrete mechanism:
transport the selected fibre, remain off the seam and pay field energy, or
perform atomic creation/removal of manifestations across the front. Calling
the exact quotient corridor “motion” without one of these is an overclaim.
