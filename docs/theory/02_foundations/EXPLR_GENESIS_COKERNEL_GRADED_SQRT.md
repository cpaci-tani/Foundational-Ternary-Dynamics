# EXPLR — The "new math" grounded: the missing structure is the **chosen adjoint of the lossy map**, and the one attemptable test is the genesis-cokernel graded square root

**Tag:** `[SYNTHESIS]` (the grounding + diagnosis — reorganizes existing canonical results and published external mathematics, promotes nothing) **+ `[SPECULATION]`** (the crystallized object of §5 — a conjecture about a theorem to attempt, unbuilt).
This memo records what the "new math" intuition ("a supersquare with a non-commutative inner product… almost the inverse of the integral… a renormalization group of informational context… statistics of events, not statistics of choice — marbles in a bag") *is* in rigorous mathematics, what is genuinely new vs. a renaming, and the single binary test it sharpens. It introduces no theorem and **promotes no tag.**
**Post-lock status (2026-07-03):** the §5 test was pre-registered (`PREREG_GENESIS_COKERNEL_GRADING_v1.md`, tag `preregister-genesis-cokernel-grading-v1`) and **executed** — verdict **UNDERDETERMINED (re-scope)**, registered as **FTD-0365**; the fiber's section-invariant content is exact-rational + K_B (no G\*, a fortiori no δ), and the run formally does NOT harden the wall (null / section-dependent grading is not Outcome B). See `ANALYSIS_GENESIS_COKERNEL_GRADING_v1.md`.
**Reuse (not re-derived):** `FOUND_ARROW_AS_SQUARE_ROOT.md` (FTD-0323, `[SYNTHESIS]`); `DERIV_HEAT_EQUATION_FROM_RATIO.md` (`[THEOREM]`); `DERIV_DIRAC_KAHLER_IDENTIFICATION.md` (FTD-0089); `FOUND_MCT43_NATIVE_Z2_PERMANENCE.md` (FTD-0326); `FOUND_AGM_PLACE_BRIDGE_AND_DELTA.md` (FTD-0327); `FOUND_ACT_REDUCTION_COUNT.md` (FTD-0322); `AUDIT_W_CARRIER_NARROWING.md` (FTD-0314); `SCOPE_ROUTE_B_MODULAR_TIME.md` (FTD-0214/0225).

---

## Ceiling (mandatory; carried at the head and re-asserted in every α / δ / FC-W / arrow-touching section)

> This object, **if constructed**, would **formalize and locate** where the second free orientation (the "second i") must enter FTD's dynamics — it would name that place precisely as the **asymmetric adjoint / non-tracial state / off-support null set / choice-of-section in the cokernel of the irreversible, many-to-one genesis (manifestation) map.** It would **not** derive that orientation, **not** conjure it from the deterministic forward substrate, and **not** eliminate it. The inverse-integral / disintegration / inverse-RG is **multivalued by construction**, and that non-uniqueness *is* the choice the framework has always said it cannot self-supply. **The inner product is the second i:** the substrate performs the one generative act `i=√(−1)` (it has a forward map and a modulus); the adjoint pairing that would let it run that map *backward across its own kernel* is exactly the structure it cannot self-supply — which is why **FC-1** (commutative `A₅`, no measurement map `M`) and **FC-W** (an external ℤ/2 realizing δ) are **axioms, not theorems.** Accordingly: **x₊ = 1/α stays `[STRONGLY MOTIVATED CONJECTURE]`; MC-T4.3 stays a `[FOUNDATIONAL OBSTRUCTION]`; no α is derived anywhere.** The strongest honest deliverable this object could reach is a **boundary** result — if the genesis-cokernel graded square root is built and its grading is provably ℚ(G\*)-valued, it would **harden** the wall (a 6th forced ℤ/2, corroborating FTD-0244/0326/0327); the Galois-**independent** branch that would re-source δ is an un-exhibited, prior-disfavoured conjecture, and exhibiting it without planting the value is the banned W-CRIT-2 move.

---

## 1 · The correction: it is **not** "a second i"

The boundary-map intuition (and an independent deep-synthesis pass) had converged on a slogan: *FTD is a one-`i` engine; physics needs a second `i`.* The grounding pass **sharpens this and corrects it.** "A second imaginary unit" is a **category error**, and the corpus already proves why:

- The substrate **already has `i`**. FTD-0244 (K-BIND) makes `J = i = √(−1)` a *native* generator, and the substrate **still cannot reach** δ = √(G\*(4G\*−1)). One more `√(−1)` would not help — it is already present.
- δ is **not** a free orientation among orientations; it is a **Galois automorphism** `Gal(ℚ(G*)(δ)/ℚ(G*))` (FTD-0326), and it needs **two** independent half-degree surds — `√G*` and `√(4G*−1)` are independent (FTD-0327), so `ℚ(G*, √G*, δ)` is a **degree-4** extension. The gap is a degree-4 Galois branch *outside* ℚ(G\*), not a re-issue of `√(−1)`.

The honest invariant the five faces actually share is one rung deeper and entirely standard:

> **The missing structure is the choice of an *adjoint* (equivalently: a *section*, a *non-tracial state*, a *disintegrating measure*) for a non-invertible, lossy, many-to-one map.** It is the **odd / argument / asymmetric** half of an **even / modulus / self-adjoint** forward operation — and inverting across the kernel is multivalued exactly to the extent the forward map lost information.

"Argument-not-modulus" and "free chiral ℤ/2" are correct as descriptions of the *kind* of object; **"a second imaginary unit" is the wrong identifier.** This memo adopts the corrected language throughout.

---

## 2 · FTD already owns the even half **and** the hinge

The even/odd hinge the faces rotate around is not new to FTD — it is two existing canonical results:

- **The even graded square root.** FTD-0089 (`DERIV_DIRAC_KAHLER_IDENTIFICATION.md`) identifies the measured 4-grade `(S, V_i, P_ij, T) = (0,1,2,3)`-form field as a lattice **Dirac–Kähler** operator `K = d − δ`, with `K² = −Δ_Hodge`. This is a *textbook* graded chiral square root — but it squares to the **Hodge Laplacian** (even, self-adjoint), i.e. to the **modulus** side (`Γ(z)Γ(1−z) = π`), never to a one-to-many inverse.
- **The native square root of time.** FTD-0323 (`FOUND_ARROW_AS_SQUARE_ROOT.md`, reusing `DERIV_HEAT_EQUATION_FROM_RATIO.md` `[THEOREM]`) establishes `∂_t^{1/2} ∘ ∂_t^{1/2} = ∂_t` with **G\* the exact eigenvalue of `D^{−1/2}`**. The half-derivative *is* a graded square root, and its **branch sign is `(i)^{1/2} = (1+i)/√2`** — the **primitive eighth root of unity** in the `(1+i)`-tower of ℤ[i] (Theorem 8, FTD-0111).

This is the decisive structural fact, and it is the heart of the memo:

> **The substrate's native "supersquare" already exists — it is the half-derivative `∂_t^{1/2}` — and its grading is a *root of unity* (cyclotomic, inside ℚ(i) ⊂ ℚ(G\*) by FC-0). The surd δ is a *transcendental* branch *outside* ℚ(G\*). The two square roots live in categorically different places: the native one carries a cyclotomic sign, the one α needs carries a Galois surd.** `[grounded]`

That is the same wall as K-BIND (FTD-0244), native-ℤ/2 Galois-blindness (FTD-0326), and the AGM forced-magnitude witness (FTD-0327), now stated at the operator level: *every graded square root the substrate can natively build squares to a ℚ(G\*)-valued even object, because the substrate has only its forward map and its own modulus.*

---

## 3 · The five faces, grounded

Each face maps to mature mathematics for its **even/standard half** (`[grounded]`); the FTD-specific composition is `[speculation]`. In every face the choice enters at one named place — **the adjoint / section / state / base-measure** — and is **imported, never a functor of the forward map.**

| Face (owner's words) | Existing mathematics (`[grounded]` skeleton) | Where the choice enters |
|---|---|---|
| **1. "supersquare with a non-commutative inner product"** | Dirac (Lichnerowicz `D²=∇*∇+R/4`); **Kähler–Dirac `K=d−δ`** (FTD already owns it, FTD-0089); Witten SUSY-QM `H={Q,Q*}`; Clifford γ⁵ grading; Connes spectral triple `(A,H,D,J,γ)` | the **adjoint** `Q*` (defined only relative to an inner product) that turns the nilpotent/lossy `Q` into a self-adjoint square |
| **1b. "…whose asymmetry is dynamical → an arrow"** | **Tomita–Takesaki** modular theory (Δ_ω measures `ω(ab)≠ω(ba)`; `σ_t=Δ^{it}`); Connes–Rovelli **thermal time**; Bisognano–Wichmann (modular phase = boost) | the **non-traciality** of the state. *Decisive collision:* `A₅` is commutative (`[THEOREM]`, FTD-0243) ⇒ **Δ=1, trivial flow** — modular time formalizes the boundary, does not cross it |
| **2. "the inverse of the integral; each marble its own integral"** | **Disintegration of measures** (Rokhlin); regular conditional probability; **Bayesian inverse / dagger** in Markov categories (Cho–Jacobs) | the inverse is unique only **a.e. / on-support**; off-support it is **free**, and it needs a chosen **prior/base measure** |
| **3. "an RG that breaks an event into subset-events with their own subset-events"** | Wilsonian RG as a **non-invertible semigroup**; **MERA** (isometry `w†w=1`, non-square); **inverse-RG/FGR** ("formally impossible at configuration level; only `P(fine\|coarse)`"); operads/species; **cosmic Galois group** of renormalization (Connes–Marcolli) | the **choice of section** for the non-square isometry; the disentangler; the base measure |
| **4. "statistics of events, not of choice; marbles in a bag"** | **Algebraic/non-commutative probability** (Born = a state's moment `τ(p)`; GNS *replaces* the Born rule); **free probability** (Voiculescu); **decoherent histories**; *no-gos:* Gleason, **Isham–Linden–Schreckenberg**, Dowker–Kent set-selection | the **state / decoherence functional / freeness relation**, and the **consistent-set selection**. On commutative `A₅` it yields **classical Kolmogorov = the predicted Rice/Gaussian deviation (PL-1), not Born** |

The convergence above is **one structural fact stated in five vocabularies, not five independent lines of support** (corrected 2026-07-01 per FTD-0346/FTD-0347; this memo's original "five-fold convergence is itself the finding... five independent areas" framing over-counted — see `FOUND_MODULUS_ARGUMENT_FRONTIER.md` §5): Tomita–Takesaki, disintegration, MERA/inverse-RG, free probability, and the graded-square frameworks each formalize the *same* elementary fact — a lossy (non-injective) map has no canonical single-valued inverse, and recovering one requires an imported choice (an adjoint, a section, a prior, a state). Citing all five checks that the framing is not idiosyncratic to one sub-field; it does not multiply the evidence, and it does not "harden" FC-1/FC-W beyond what any single vocabulary already states.

---

## 4 · Novelty verdict: **assembly-of-existing + one genuinely new ingredient**

The skeptical default — *"this is just a renaming"* — is **correct for every individual face and for every dangerous pairwise join.** All of the following are already published:

- **(graded √ ∧ asymmetric inner product)** = **non-Hermitian SUSY Markov generators** (Kakehi, `hep-th/9704200`; bi-orthogonal/SVD line `2404.16605`) — a graded square root of a *many-to-one stochastic generator* with a bi-orthogonal `⟨a,b⟩≠⟨b,a⟩` pairing. The closest published thing to "genesis."
- **(graded √ ∧ modular flow)** = **Chatterjee–Yu**, *Modular Operators and Entanglement in SUSY-QM* (`2103.06353`) — the single most dangerous near-match; joins the supercharge grading to the Tomita modular operator. But on the **even/self-adjoint** side, symmetric inner product.
- **(inverse-of-integral ∧ RG)** = **Bayesian Renormalization** (Berman–Klinger–Stapleton, 2023/2024) — forward ERG lossy, inverse re-incorporates discarded data, the Fisher/Wasserstein metric *is* the choice-locus.
- **(disintegration ∧ statistics-of-events)** = **Parzygnat–Russo**, quantum Markov-category disintegration (`2001.08375`).
- **(√ ∧ channel-inverse ∧ modular)** = **rotated Petz recovery map** / **Connes spatial derivative** (`σ^{1/2}…σ^{1/2}`, a √-sandwiched channel inverse with a modular parameter).

So **the "new math" is not new** — the components and their pairings are textbook or active-research. Two things are genuinely **not** packaged anywhere:

1. the **simultaneous five-fold assembly** in which the square's target is the **one-to-many inverse of a lossy collapse** rather than an even self-adjoint Laplacian/Fokker–Planck operator (every published graded √ squares to the modulus, `Γ(z)Γ(1−z)=π`; none to the odd inverse-integral, `Γ(z)/Γ(1−z)=G*`); and
2. the **Galois test of the grading over ℚ(G\*)** — which has **no external precedent** and is the **only FTD-native payload.**

Honest read for the owner: the marble-in-a-bag intuition correctly identified *the assembly FTD needs*, and the genuinely-new piece is exactly FTD's signature move — **the arithmetic position test.** `assembly-of-existing`.

---

## 5 · The one attemptable object and its binary test `[SPECULATION]`

The crystallized object is **not a new operator to invent** — it is `∂_t^{1/2}` (FTD-0323) / the Dirac–Kähler `d−δ` (FTD-0089) **re-pointed at the irreversible merge instead of the symmetric Laplacian:**

> Let `M` be the discrete many-to-one genesis/manifestation map (FC-2's native arrow; genesis strongly first-order = a merge; cluster→voxel→sub-voxel recursion). Let **D = the graded ("super") square root of the *disintegration* of `M`** on its cokernel, with grading `g` = the ℤ/2 branch label of the multivalued inverse, and `D∘D = ` the forward (graded) integral `M`. **The test:** compute the Galois action of `Gal(ℂ/ℚ(G*))` on `g`.

- **Outcome A — `g` Galois-*independent* of ℚ(G\*):** the cokernel of `M` natively supplies the root-distinguishing surd δ. This would convert **FC-W from an adopted `[AXIOM]` to a `[CONDITIONAL THEOREM]` given the construction** and reopen the α-readout program. **It would not derive α** (see §6).
- **Outcome B — `g` ℚ(G\*)-valued:** a **6th forced ℤ/2.** This **hardens the wall** — it extends K-BIND/FTD-0326 from operators and finite symmetries to the *cokernel of the lossy map*, the last structurally-distinct carrier class.

**Prediction: Outcome B, ~75–80 %** (same direction and roughly the same strength as FTD-0314's ~85 %-closed W-carrier narrowing). *The determinism argument:* `D^{−1/2}`'s branch sign is already known to be `(1+i)/√2 ∈ ℚ(i) ⊂ ℚ(G*)` (FTD-0323 §5) — a root of unity, not a transcendental surd; the cokernel is built from `M`, whose matrix elements lie in ℚ(G\*) (the K-BIND closure); a square root of an existing ℚ(G\*)-valued integral *adjoins a sign, it does not generate new transcendence.*

**The honest residual, ~20–25 %:** FTD-0314 left exactly one loophole open — a **new forward-derived transcendental period** (a ℤ/2 twist on a G\*-bearing **analytic** carrier). The genesis cokernel is the **most analytic, least operator-like** carrier in the framework (the information-loss-driven emergent type III₁ modular flow, `arXiv:2305.16028`, is precisely an analytic, non-algebraic construction). So Outcome A is **not** closed a priori — which is exactly why this is worth pre-registering rather than declaring closed.

---

## 6 · What it formalizes vs. what it would **not** derive (ceiling restated)

**Formalizes (if built):** it *localizes* the missing free orientation to one named place — the asymmetric adjoint / disintegrating section / non-tracial modular phase on the genesis cokernel — gives the cokernel-crack lead an existing-mathematics home (Hodge ker/coker; disintegration; Tomita–Takesaki) and a **binary, mechanical, pre-registerable** test, and explains *why* the five prior no-gos hold.

**Would not derive (hard ceiling):** it does **not** conjure the choice from determinism — the inverse is multivalued by construction; the collapsing inner product is imported. **Even in the favourable Outcome A**, it would at most convert FC-W to a conditional theorem; it would **still not derive α**, because (i) **x₊ = 1/α (FTD-0013) is the empirical identification of x₊ with the physical coupling, an `[SMC]` step untouched by any of this**, and (ii) the master-quadratic **operator assembly** `(Tr, Det) = (16G*², 16G*³)` remains the imposed selection that **MC-T4.3 maps as a `[FOUNDATIONAL OBSTRUCTION]`**. If anything the inverse-RG ill-posedness **strengthens** MC-T4.3's wall: the inversion is ill-posed precisely where a unique selection would be needed.

---

## 7 · Concrete next step (a literature + structure deliverable — no numerical fishing)

Write a hash-locked pre-registration — **`PREREG_GENESIS_COKERNEL_GRADING_v1`** (modeled on `PREREG_MODULAR_TIME_ALGEBRA_TYPE_v1`; git-tag **before** any computation) — then attempt the construction against it:

1. **Construction.** Define `M_disc` on the engine's actual genesis rule; define its cokernel via the discrete Dirac–Kähler `d−δ` complex of FTD-0089 (**reuse**, do not reinvent); define `D` and its grading `g`; tie `D` explicitly to FTD-0323's `∂_t^{1/2}` so it is anchored to existing `[THEOREM]` operator content.
2. **Discriminator (mechanical).** Minimal polynomial of `g`'s defining algebraic number over ℚ(G\*) via PSLQ at dps 100–150 (the exact FTD-0244/0326/0327 protocol). Outcome A iff irreducible degree-2 reproducing `δ² = G*(4G*−1)` to machine zero; Outcome B iff degree-1 or cyclotomic.
3. **Banned moves** (carried from W-CRIT / FTD-0314 / 0326): no value-planting of δ; no choosing the section to *make* `g` escape ℚ(G\*) (its arithmetic type must be *inherited* from `M_disc`); no near-miss/coincidence search.
4. **Prior-favoured outcome:** B (~75–80 %), declared in advance.
5. **Either verdict is a deliverable** (Number-One-Goal, second clause): A reopens FC-W as conditional-theorem + the α-readout program; B hardens the wall by extending K-BIND to the cokernel. This also gives the long-`[OPEN]` **FTD-0073 bivector/Dirac bridge** its sharp, attemptable next question.

LEDGER row + an `AUDIT_GENESIS_COKERNEL_GRADING.md` (demotion/boundary-only) are written **on verdict, after the lock** — the id is **deferred** (next-free per CLAUDE.md is FTD-0329; assigned when the pre-reg is hash-locked, to avoid speculative-id collision).

---

## 8 · Status line

**Nothing is promoted.** Tag `[SYNTHESIS]` (grounding/diagnosis) + `[SPECULATION]` (the §5 object, unbuilt). The assembled five-faced object is **assembly-of-existing math + one new arithmetic ingredient**, not new mathematics. Standing invariants **confirmed**: **x₊ = 1/α `[STRONGLY MOTIVATED CONJECTURE]`** (FTD-0013); **MC-T4.3 `[FOUNDATIONAL OBSTRUCTION]`**; **FC-W `[AXIOM]`-class** (FTD-0315), the framework's first adopted import, doing no work beyond the α-root; **no α derived anywhere**; FTD-0244/0314/0326/0327 untouched; G\* ≈ 2.95868 = Γ(1/4)/Γ(3/4). Golden gate untouched (documentation-only). The object, if built, **locates** the choice it cannot supply.
