# DERIV — Corrected Native Moving-Source Response

**Document type:** Corrective derivation
**Status:** [DERIVED — SELECTED SMOOTH DRIVE] + [THEOREM — POSITIVE PHASE-SPEED FLOOR] + [THEOREM — EXTERNAL-DRIVE FIELD ENERGY] + [THEOREM — POINT/FINITE-SOURCE HOP AND TOPOLOGY-MAGNITUDE OBSTRUCTIONS] + [OPEN — NATIVE MOBILE SOURCE AND PHYSICAL POWER]
**Record:** FTD-0115 corrected by FTD-0558 through FTD-0564 on 2026-07-26
**Production changes:** none

## 1. Correction

The former FTD-0115 result is retracted.  It was not the response of the
production engine: it used continuous time, the seven-point Laplacian, and an
unwrapped `k=2pi n/L` in the moving-source phase.  In particular, its reported
`L=16, n=15` threshold treated the crystal mode `n=-1` as a momentum 15 times
larger.  The exact production symbols agree under that alias while the false
phase-speed ratio is smaller by a factor of 15.

## 2. Frozen production pole

For the isolated `FULL`-stencil kick-drift field map and an observer drive
added to the field kick,

\[
 \binom{J_{t+1}}{W_{t+1}}=
 \begin{pmatrix}1-a&1\\-a&1\end{pmatrix}
 \binom{J_t}{W_t}+\binom11 f_t,
 \qquad a=C_{\rm WAVE}^2M(\mathbf k).
\]

For `f_t=f exp(-i Omega t)`, direct inversion gives

\[
 J(\mathbf k,\Omega)=
 \frac{f}{C_{\rm WAVE}^2M(\mathbf k)-4\sin^2(\Omega/2)}.
\]

Thus the exact discrete-time pole is

\[
 \Omega=\pm\theta(\mathbf k)\pmod{2\pi},\qquad
 \theta=2\arcsin\!\left(\frac{C_{\rm WAVE}\sqrt M}{2}\right).
\]

This formula is exact for the stated additive drive.  Calling it a physical
Liénard–Wiechert potential still requires a native conserved source, its exact
coupling numerator, retarded boundary prescription, and reciprocal matter
dynamics.  Those ingredients are not supplied here.

## 3. The speed floor

On the canonical Brillouin zone `[-pi,pi)^3`, the positive face contribution
to the 18-point symbol implies

\[
 M(\mathbf k)\ge \frac23\sum_i(1-\cos k_i)
 \ge \frac4{3\pi^2}|\mathbf k|^2.
\]

Since `asin x >= x`, every nonzero mode satisfies

\[
 \frac{\theta(\mathbf k)}{|\mathbf k|}
 \ge \frac{2C_{\rm WAVE}}{\pi\sqrt3}
 =\frac2{3\pi}=0.212206590789\ldots .
\]

Therefore a smooth one-frequency source with speed below this value cannot
meet the production Cherenkov condition.  The bound is universal but not
claimed sharp.  The locked finite-grid principal-direction enumeration found
minimum `0.226221165291` cells/tick.

The same defect exists even inside the former auxiliary seven-point model:
`|khat|/|k| >= 2/pi` on the wrapped zone.  Its phase speed also does not tend
to zero.

## 4. Integer hopping is a different source

A primitive manifested site does not follow `x=vt` at fractional coordinates.
For one integer hop `d` every `T` ticks, its Bloch source is Floquet:

\[
 \Omega_l=(\mathbf k\cdot d+2\pi l)/T,
\]

\[
 c_l=\frac{1-e^{i\mathbf k\cdot d}}
 {T[1-e^{i(\mathbf k\cdot d+2\pi l)/T}]}.
\]

Those schedule-dependent harmonics can intersect field poles below the mean
smooth-source threshold.  Such excitation is generated by the abrupt hop
schedule—discrete acceleration/stutter—not by an arbitrarily slow lattice
phase speed.  The FTD-0558 observer verified reconstruction and Parseval in all
12 locked schedules, with non-fundamental amplitude reaching
`0.218387780547`.

FTD-0560 sharpens “can intersect” for a single point polarity to “must
intersect”: every finite one-site hop period has a regular `l=0` or `l=1`
resonance with nonzero native gradient forcing.  The resonant amplitude falls
as `6pi G_C/T^2`, but no finite `T` gives an exactly radiationless linear
co-moving point dressing.

FTD-0561 then removes rigid spatial width as a generic escape.  For any finite
rigid axial profile with net polarity `Q != 0`, the same resonant forcing has
the universal leading term `6pi G_C |Q|/T^2`, independent of the profile's
radius.  Neutrality can suppress, but not generically eliminate, the witness:
the first nonzero source moment of order `m` moves the leading amplitude to
`T^-(m+2)`.  This is a theorem about the prescribed linear source numerator,
not a physical radiation-power law or a no-go theorem for neutral nonlinear
carriers.

FTD-0562 closes the remaining *fixed finite neutral linear* escape.  In every
direction the `l=1` pole has a regular slow branch `k_T=r_T n/T`, where
`r_T=2pi sqrt(3)+6pi n_parallel/T+O(T^-2)`.  Every nonzero finite form factor
has a first nonzero homogeneous moment polynomial, and that polynomial is
nonzero in at least one oblique direction with nonzero hop-axis component.
The corresponding source survives for all sufficiently large `T`; no fixed
finite rigid profile can cancel the complete slow-hop surface.

FTD-0563 closes the proposed *finite neutral emergent-monopole* escape in the
linear source sectors. The selected matched-face Gauss law gives the exact
identity `sqrt(lambda)|E|=z|S|`, so the open/infinite monopole coefficient is
`z|S(0)|=z|Q|`; a finite neutral profile has none. On a periodic volume the
same zero mode appears as the necessary condition `sum rho=0`, and production
uses the compensating source `s-Q/N`. The restricted native FTD-0429 response
has finite infrared susceptibility and reaches the same neutral-source
conclusion. This remains a conditional cross-sector result because no common
native matter--field action yet forces these source identifications.

FTD-0564 closes one part of the remaining topological escape. On the same
octahedral Moore shell, `J=pA n` has direction degree `p` while
`J=pA(n+2e_z)` has degree zero, but both have affine closed flux `4pA`.
Changing `A` changes flux without changing hedgehog degree. A defect degree can
therefore label a sector or sign, but a separate nonlinear common action must
set and stabilize the electric-charge magnitude.

## 5. Correct scope

- [DERIVED]: the driven production resolvent above.
- [THEOREM]: the strictly positive universal phase-speed floor.
- [DERIVED]: the exact spectrum of the selected periodic integer-hop history.
- [OPEN]: which native manifested histories are dynamically realized.
- [THEOREM]: exact production modal energy, external-drive work, and finite-time
  retarded response (FTD-0559).
- [DERIVED]: the continuum/large-time external-drive resonance functional,
  with coarea Jacobian `|v-sigma v_g|` (FTD-0559).
- [OPEN]: native current normalization, reciprocal recoil, and radiated matter
  power from one common matter–field action.
- [CLOSED NEGATIVE]: exactly co-moving square-summable linear dressing for a
  single periodically hopping point polarity (FTD-0560).
- [CLOSED NEGATIVE]: curing that point obstruction merely by replacing it with
  a finite rigid profile of the same nonzero net polarity (FTD-0561).
- [CLOSED NEGATIVE]: exact full-surface cancellation by any fixed nonzero
  finite rigid linear profile, including neutral composites (FTD-0562).
- [CLOSED NEGATIVE]: a finite microscopically neutral source acquiring a true
  Gauss monopole within the selected face-Gauss or restricted native linear
  response (FTD-0563).
- [CLOSED NEGATIVE]: normalized-flux direction topology alone supplying or
  quantizing electric Gauss-charge magnitude (FTD-0564).
- [OPEN]: deforming/nonlinear, period-growing, boundary/nonlocal,
  protected defect/bundle carriers plus self-consistent reciprocal dynamics
  whose common action explicitly supplies and locks the effective Gauss source.

Increasing `L` alone is not a continuum limit: it leaves the Brillouin cutoff
fixed.  Continuum recovery requires an explicit lattice-spacing scaling.

## 6. Evidence

See `THEOREM_NATIVE_MOVING_SOURCE_POLE_CORRECTION.md`,
`THEOREM_EXTERNAL_DRIVE_RADIATION_FUNCTIONAL.md`, their audits and locked
pre-registrations, `THEOREM_NATIVE_HOP_DRESSING_OBSTRUCTION.md`,
`THEOREM_HOP_SOURCE_MULTIPOLE_HIERARCHY.md`,
`THEOREM_FULL_SURFACE_SOURCE_OBSTRUCTION.md`,
`THEOREM_GAUSS_MONOPOLE_MOBILE_DICHOTOMY.md`,
`test_native_moving_source_pole`, `test_external_drive_radiation`,
`test_native_hop_dressing_obstruction`, and
`test_hop_source_multipole_hierarchy`, and
`test_full_surface_source_obstruction`, and
`test_native_gauss_monopole_dichotomy`.
