# Analysis — Does spatial information do "creative work" in FTD genesis? (FTD-0317)

**Tag:** `[MEASURED / EMERGENT]` — a narrow, operational substrate measurement. **NOT** a derivation, **NOT** a claim that "information is a fundamental creative agent."
**Lock (in-source):** the test + its decisive permutation control + the two parity gates are defined in `engine/tests/campaign_genesis_amplitude_ceiling.cpp` (`--info` mode, "Test 3") and the analyzer `scripts/exploration/analyze_info_creation.py`. Golden-neutral (read-only `--info` campaign; golden certified green). Run of record produced on the RTX 5090 (`engine/results/info_full/`, L=32, settle=150, 6 seeds).
**Precedence:** LEDGER > this doc.

---

## 0 · Verdict

> **CONFIRM (narrow, operational).** At **fixed total flux energy `E = Σ½|J|²` AND fixed `|J|` amplitude histogram**, an **ordered (coherent) flux disposition produces dramatically more *organized* manifested matter than a scrambled control** — mean cluster coherence `R = 0.97` (coherent) vs `0.16` (scrambled), a **+115σ** gap; organization `N·R` gap **+66.8σ**. The gap is **cleaner with Gauss projection OFF** (the decisive control) ⇒ it is **pure spatial information**, not energy-retention or histogram. Crucially, the scrambled control makes *more* matter (manifested `N`: 20615 vs 13611, −149.6σ) — so **information is fuel for organized *form*, not for the *quantity* of matter.**

**Honest scope (mandatory):** the defensible claim is exactly the operational one above. It is **not** "information is a fundamental creative agent in FTD," and it derives nothing in the spine. `[MEASURED]` substrate fact; **nothing promoted** (FTD-0013 `[SMC]`, MC-T4.3 `[FOUNDATIONAL OBSTRUCTION]`, golden green).

---

## 1 · The test and its controls

Genesis runs *before* Gauss projection, so the **tick-1** manifested yield is fixed by the `|J|` histogram alone. The question (`--info` "Test 3"): does the **spatial structure** of the disposition `J` — at fixed energy *and* fixed histogram — govern the **organization and survival** of the matter that manifests? Two controls make the answer non-trivial:

- **The decisive permutation arm:** *scrambled* = the same coherent field with the **per-voxel flux vectors randomly permuted** ⇒ identical `Σ½|J|²` **and** identical `|J|` histogram, but **zero spatial structure**. Both parity gates measure **0.00 deviation** (energy dev 0.00, histogram dev 0.00 in both passes) — so any organization difference is *pure spatial information*.
- **The peak-confound witness:** *multimode* (M = 1,2,4,8,16) and *white* arms vary the peak `|J|`. White has a **higher** peak (`max_void_J₀ = 2.41`) than coherent (`2.00`) yet **lower** organization (org 3328 vs 10791, gauss-off) ⇒ organization tracks **coherence**, not peak amplitude.

---

## 2 · The run of record (`info_full`, L=32, 6 seeds)

**gauss = OFF (decisive control):**

| metric | coherent | scrambled | gap (σ) |
|---|---|---|---|
| mean coherence `R` | **0.97 ± 0.00** | 0.16 ± 0.01 | **+115.2** |
| organization `N·R` | 10791 ± 39 | 2664 ± 122 | **+66.8** |
| `S(k)` peak/S0 | 0.26 | 1.04 | −19.8 (coherent = structured low-k) |
| manifested `N` | 13611 ± 47 | **20615 ± 15** | −149.6 (scrambled makes *more*) |

**gauss = ON** (noisier, with projection feedback): the organization gap shrinks to +1.4σ — so the clean signal lives in the projection-OFF control, confirming **pure spatial information** rather than a Gauss-mediated energy effect.

**Reading:** identical energy + identical histogram, opposite spatial order ⇒ a 6× coherence gap and a +66.8σ organization gap, but *fewer* total clusters — the ordered disposition channels manifestation into **coherent form**, the scrambled one into **more, smaller, incoherent** matter.

---

## 3 · Discipline

- **Narrow and operational.** The result is a substrate measurement: *coherent disposition ⇒ more organized genesis-matter at fixed energy + histogram.* The "information does creative work / imagination writes form" gloss is evocative framing, **not** the claim — keep it operational.
- **`[MEASURED]`, not derived.** This characterizes the engine's genesis dynamics; it derives nothing in the LEDGER spine and promotes nothing.
- **Companion arc.** This is the `--info` mode of the genesis-ceiling campaign (FTD-0316); the amplitude-ceiling result is its sibling. The `--stamp` ("imagination writes form" glyph-imprint) mode is a further, more presentational probe, not landed here.

---

## 4 · Non-promotion

`[MEASURED / EMERGENT]`. A clean, narrow substrate finding: at fixed energy and fixed `|J|` histogram, spatial information (coherence) governs the **organization** (not the quantity) of genesis-created matter. **No theorem, no α, no promotion.** FTD-0013 `[SMC]`, MC-T4.3 `[FOUNDATIONAL OBSTRUCTION]`, the spine — unchanged. Golden gate certified green (`0xb604d81a3d79366e`).
