# EXPLR — The coat's Peierls clock: a toy probe of the un-priced tine of the FTD-0552 fork

**Tag:** `[EXPLORATORY — Python toy; canonical confirmation requires the C++ coat implementation]` + `[NUMERICAL FACT — toy measurements as listed]` + `[SPECULATION — every physical reading]` + `[CLOSED — the separability prediction, refuted here]`.
**LEDGER id:** FTD-0565 · **Date:** 2026-07-26
**Context:** FTD-0552 found an unsubtracted Peierls-like self-force on the isolated quadratic coat and closed the unsubtracted mobile law negative — the *subtraction* tine of a fork. This note probes the *mechanism* tine: before importing a subtraction scheme (itself a torsor-point choice), measure what the self-potential buys. Toy: 1D/2D neutral jellium, tensor B-spline coat, periodic lattice Green function, bare inertia `m = 1` and coupling `g = 1` (toy choices, flagged).
**Verification:** `scripts/exploration/explore_coat_peierls_clock.py` (6/6 measurements, 2026-07-26).

## 1 · Measurements

1. **The mechanism reproduces** (H1–H2): exact neutrality and partition of unity; `U(x)` periodic; stationary at integer and half-integer positions; nonzero self-force at generic `x` — the FTD-0552 pattern in one dimension. **The minimum is the half-cell** (`U(1/2) − U(0) = −1/64`): stationary matter pins to the *dual* lattice; integer anchors are maxima. Whether the 3D minimum is the body center (BCC echo) is `[OPEN]` — the 2D landscape is not separable (below), so the 3D computation is its own job.
2. **The internal clock exists** (H3): small oscillations about the minimum ring at `ω₀ = √(U″/m) = 0.7064` (toy units); direct integration matches the curvature prediction to 0.6%.
3. **Peierls width scaling** (H4): corrugation falls steeply and monotonically with coat smoothness — B1 hat `0.1211` (≈ 31/256), B2 `1/64` exactly to 10⁻¹⁰, B3 `6.51×10⁻³` (ratios 7.7×, 2.4×) — the Peierls–Nabarro signature.
4. **Strict locality** (H5): corrugation and `ω₀` are ring-size-independent to 10⁻¹⁴ and 10⁻⁹ respectively. The well is a local object; no infrared caveat attaches.
5. **The separability prediction is refuted** (H6): the 2026-07-26 savant transfer predicted a tensor-separable landscape; measured nonseparability residual is **10% of the corrugation**. The prediction dies as stated; barrier anisotropy in 3D requires the full Green function. Recorded as a closed negative of this note's own hypothesis set.
6. **Closed-form flag:** the exact-looking rationals (`1/64`, ≈ 31/256) suggest the 1D corrugation has a closed form in the spline order — one Fourier computation away; `[OPEN]`, not attempted here.

## 2 · Why this matters (readings, all `[SPECULATION]`)

A conservative periodic self-potential gives a dressed particle at rest an **internal frequency** — the object two standing gaps are shaped like: the de Broglie construction's missing rest-mass clock (memory of record), and the owned-side restoring term the FTD-0520 split showed the free wave sector lacks. If the engine's dressed coat oscillates at an `ω₀` of its own well, the matter sector acquires a native clock *without* a new import — while free flux stays gapless, which is the asymmetry actual physics displays. None of this is claimed: the toy's `m` and `g` are conventions, no identification of `ω₀` with any physical frequency is made, and the FTD-0552 arc's subtraction reading remains fully live. The fork's honest statement: *subtract* = one imported scheme (priceable, torsor-point); *keep* = one candidate mechanism (content, testable). This note establishes only that the keep-tine is non-empty and locally well-defined.

## 3 · What would decide it (engine-side, pre-registerable)

(i) Extract `ω₀` from the C++ coat in its own FTD-0552 dressing (small-displacement release at the half-cell minimum); (ii) check `ω₀`'s dependence on `g` (√g expected) and on coat order (steep fall, H4 pattern); (iii) the discriminating gate: propagate a *dressed* coat under the FTD-0551 transaction and test whether its long-wavelength phase evolution carries the gap — `θ(k)² → θ₀² + c_eff²k²` — against the null (gap is pinning-only, no propagating-mode mass). Outcome cells and locks belong to a future pre-registration; none is created here.

## 4 · Status

Toy measurements `[NUMERICAL FACT — this toy only]`; separability hypothesis `[CLOSED NEGATIVE — this note]`; all physics readings `[SPECULATION]`; nothing licensed for the engine, no toggle, no LEDGER tag moved anywhere else. FTD-0552's closed-negative verdict on the unsubtracted isolated mobile law is untouched — this note is input to choosing the *next* move on that line, not a revision of it.
