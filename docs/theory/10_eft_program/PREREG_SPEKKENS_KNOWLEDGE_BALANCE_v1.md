# Pre-Registration — B-QM-1′: Spekkens Knowledge-Balance from the Internal-Observer Restriction (v1)

**Tag:** `[PRE-REGISTRATION]` — locks the most promising epistemic-program target: does FTD's internal-observer restriction, on the conjugate `(J,v)` cell, yield the **sharp** Spekkens knowledge-balance (→ Spekkens-class quantum phenomenology) or only a **generic** binding limit (→ classical-with-noise)? **Contains no result.** Outcomes pre-blessed; verdict genuinely open.
**Date:** 2026-05-29
**Hash-lock target tag:** `preregister-spekkens-knowledge-balance-v1`
**LEDGER row reservation:** FTD-0227 (provisional).
**Companion:** `SCOPE_DERIVE_QM_GAP.md`. **Cross-refs:** B-QM-1 (`AUDIT_MANIFESTATION_NONCOMMUTATIVITY_CLOSED_NEGATIVE.md`); FTD-0199/0200 (Born CLOSED-NEGATIVE); Spekkens 2007 (toy model); the J²=−I quarter-conjugacy / symplectic `(J,v)` structure.

> Discipline: §§1–7 hash-stamped before the attempt; commit deferred. Result lands in a separate doc. Engineering toward an outcome — especially via the observer/system split or by inserting the conjugate symmetry — invalidates the attempt.

---

## §1 — Context

The ψ-epistemic reframe (user, 2026-05-29) recasts the goal: don't derive ontic non-commutativity (B-QM-1: impossible, the substrate is Boolean), derive the **epistemic restriction** whose knowledge-balance generates the Spekkens-class. The "opportunity-cost" framing sharpened the test: the quantum-load-bearing trade-off is **across conjugate axes** (knowing X *costs* knowing Z), and it must be **binding** (in-principle irrecoverable), not merely lossy.

Two grades must be separated:
- **Grade-1 (lossy/classical):** "can't know all" — but a god's-eye joint distribution exists and is recoverable in principle. Gives classical-with-noise (consistent with FTD-0199/0200 → Rice/Gaussian).
- **Grade-2 (sharp/quantum):** a *binding* trade-off across conjugates with the *exact* Spekkens knowledge-balance → the Spekkens-class (superposition, complementarity, no-cloning, interference).

---

## §2 — The question (LOCKED)

**Q-BQM1′.** For FTD's elementary conjugate cell (the `(J,v)` phase-space cell, modeled at the Spekkens elementary-system resolution: 4 ontic states), does the **internal-observer restriction** — grounded in the **classical finite-self-reference limit**, NOT Breuer's QM theorem — produce:
- **(D1) a BINDING** conjugate-knowledge trade-off (in-principle irrecoverable for internal finite observers), and
- **(D2) the SHARP Spekkens knowledge-balance** (allowed epistemic states = the 6 knowledge-balanced 2-subsets = the qubit stabilizer set), rather than a generic/fixed binding limit?

---

## §3 — The two discriminators (LOCKED — the crux)

- **D1 — binding vs lossy.** Binding ⇔ no internal (finite, self-referential) observer can recover the conjugate, *even in principle* (classical self-reference limit). Lossy ⇔ recoverable by a better internal measurement.
- **D2 — sharp vs generic.** Sharp ⇔ the allowed-epistemic-state set **equals** the Spekkens knowledge-balanced set (6 stabilizer states on 4 ontic states; the budget applies *symmetrically to all three complementary bases*). Generic ⇔ a different/broader set, e.g. a **fixed blind-spot** (the observer always loses the *same* axis), which gives only one basis — classical.

---

## §4 — Admissible ingredients (LOCKED)

- The FTD elementary conjugate cell: the symplectic `(J,v)` pair; its `J²=−I` quarter-conjugacy (the phase-space rotation relating conjugate axes).
- The **classical finite-self-reference limit** as the source of binding-ness.
- Spekkens' 4-ontic-state elementary system + knowledge-balance as the *target* structure to compare against (not to assume).

**Out of scope (NOT admissible):**
- **Breuer's QM theorem / WAY** as the source of the restriction (circular — presupposes QM). Classical self-reference only.
- Choosing the observer/system split or cell labeling to manufacture the 6-state set (BP2).
- **Inserting the conjugate symmetry** (the all-bases budget) by hand instead of deriving it from substrate structure (BP3).
- Inserting `[q,p]=i`/ℏ; fitting to Born/stabilizer; CODATA.

---

## §5 — Falsifiers (LOCKED, mechanical)

- **BP1** — a QM theorem (Breuer-QM, WAY) is used to establish binding (circular).
- **BP2** — the observer/system split or cell labeling is tuned to produce the Spekkens set.
- **BP3** — the symmetric conjugate budget (all three bases) is inserted rather than derived; if sharpness requires it and it is not derived, the verdict is **PARTIAL**, not FOUND.
- **BP4** — `[q,p]=i`/ℏ inserted.
- **BP5** — Born/stabilizer fitting.
- **BP6** — CODATA insertion.

---

## §6 — Three pre-blessed outcomes (LOCKED)

- **FOUND.** D1 binding **and** D2 sharp, with the symmetric budget **derived** from substrate structure (e.g. `J²=−I` makes all conjugate axes equivalent) → FTD derives the Spekkens-class epistemic restriction from a classical substrate + the internal-observer limit. (Bell residue remains separate, per the scope.)
- **PARTIAL / UNDERDETERMINED.** D1 binding holds (derived), but D2 sharpness does **not** follow from binding alone — it needs an additional ingredient (the symmetric conjugate budget). Locate the gap precisely; if the ingredient is FTD-native but unproven, mark it as the next target (B-QM-1″).
- **CLOSED-NEGATIVE.** Not even D1: the restriction is lossy/recoverable → classical-with-noise → no epistemic-quantum. Consistent with FTD-0199/0200.

---

## §7 — Method (LOCKED, ordered)

1. **Binding (D1).** Establish, from the *classical* finite-self-reference limit, whether an internal finite observer can or cannot recover the conjugate of what it manifests. Non-circular (no QM theorem).
2. **Spekkens target.** Construct the Spekkens 4-ontic-state elementary system; exhibit its 6 knowledge-balanced epistemic states (= 3 complementary bases) and their stabilizer correspondence.
3. **Sharp vs generic (D2).** Determine whether the internal-observer-allowed epistemic states match the 6-state Spekkens set (sharp) or a fixed-blind-spot/broader set (generic). In particular: does the restriction apply **symmetrically to all three bases** (needs the conjugate symmetry) or to a **fixed axis** (classical)?
4. **Locate the gap.** If generic, identify the missing ingredient and whether it is FTD-native (`J²=−I` symplectic rotation) or an import.
5. **Falsifier checklist (§5) then verdict (§6).**

**Substrate.** Desk computation + a small verifier on the Spekkens 4-state toy (sharp 6-state set vs generic fixed-blind-spot set), demonstrating binding ≠ sharp. No fitting, no inserted symmetry, no QM-theorem import.

---

## §8 — Hash-lock

`sha256sum` this file; record in-session before the attempt; commit deferred. Defective §3 discriminator or §5 falsifier → **v2**, not an edit.

*Authored 2026-05-29. **No result.** Genuinely open; prior-favoured **PARTIAL** (binding plausibly derivable from classical self-reference; sharpness likely needs the symmetric conjugate budget — possibly FTD-native via `J²=−I`, possibly import), but FOUND and CLOSED-NEGATIVE are both live. Engineering toward any outcome invalidates the attempt.*
