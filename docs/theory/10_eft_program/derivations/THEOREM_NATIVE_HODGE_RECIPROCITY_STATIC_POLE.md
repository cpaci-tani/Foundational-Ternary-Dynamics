# FTD-0575 — Native Hodge Reciprocity and Static-Pole Cancellation

**Status:** `[THEOREM — HODGE-DERIVED RECIPROCAL LORENTZ-FORM FORCE]` +
`[THEOREM — EXACT STATIC POLE CANCELLATION/BOUNDED KERNEL]` +
`[DERIVED — SAME-POLARITY STATIC ATTRACTION]` +
`[SCOPED NO-GO — FTD-0574 RECIPROCAL FORCE AS COULOMB ELECTROMAGNETISM]` +
`[OPEN — EXACT DYNAMIC ENERGY/MOBILE CARRIER]`  
**Date:** 2026-07-26  
**Verdict:**
`NATIVE_HODGE_FORCE_DERIVED_STATIC_POLE_CANCELED_SAME_SIGN_ATTRACTIVE`

## 1. Scope

FTD-0574 derived the exact prescribed-source interaction for the frozen
production field sector,

\[
 I_{\rm src}=G_C\langle\rho,DJ\rangle+G_C\langle CJ,j\rangle,
\]

where `D`, `G`, and `C` are the periodic central divergence, gradient, and
curl. This theorem varies that same interaction with respect to a smooth point
path and then derives its static exchange kernel. It changes no production
state, force, toggle, default, or scenario.

## 2. The source interaction already defines Hodge potentials

Define fields derived from the primitive site vector `J` by

\[
 \boxed{\Phi_J=-G_CD J,\qquad A_J=G_CCJ.}
\]

Then the source interaction is identically

\[
 I_{\rm src}=\langle\rho,-\Phi_J\rangle+\langle j,A_J\rangle.
\]

For a point carrier of polarity `q` on a smooth path `X(t)` with
`v=dot X`, its interaction Lagrangian is therefore

\[
 \boxed{L_{\rm int}=q\,[v\cdot A_J(X,t)-\Phi_J(X,t)].}
\]

This is a rewrite of the already derived source action, not an independent
electromagnetic postulate.

## 3. Reciprocal path variation

The Euler--Lagrange derivative of the point-path interaction is

\[
 \frac{d}{dt}\frac{\partial L_{\rm int}}{\partial v_i}
 -\frac{\partial L_{\rm int}}{\partial X_i}
 =q\left[-\partial_i\Phi_J-\partial_t A_{J,i}
          +(v\times(\nabla\times A_J))_i\right].
\]

Thus the reciprocal force generated by the same action has Lorentz form,

\[
 \boxed{F_J=q(E_J+v\times B_J),}
\]

with

\[
 \boxed{E_J=G_CGD J-G_C\partial_t CJ,\qquad
        B_J=G_CC^2J.}
\]

Consequently `v dot (v cross B_J)=0` exactly. The magnetic term performs no
scalar work. These derived fields are not the legacy dashboard identifications
`E proportional to -grad|J|` and `B=curl J`.

Because the fields are built from one potential pair, the homogeneous
identities hold kinematically:

\[
 DB_J=0,\qquad \partial_tB_J+CE_J=0.
\]

The registered periodic fixtures close both identities below `1.6e-17`.

## 4. Exact static response

For a nonzero periodic Fourier mode write

\[
 s_i=\sin k_i,\qquad \sigma^2=\sum_i s_i^2,
\]

and let `M(k)` be the nonnegative symbol of the production 18-point
Laplacian:

\[
 M=4-\frac23\sum_i\cos k_i
      -\frac23\sum_{i<j}\cos k_i\cos k_j.
\]

Since `C_SPEED^2=1/3`, the static field operator is `K=M/3`. Solving the
linear static field equation gives

\[
 \Phi_J(k)=-G_C^2 R(k)\rho(k),
\]

\[
 A_J(k)=G_C^2R(k)P_T(k)j(k),
\]

where

\[
 \boxed{R(k)=\frac{\sigma^2}{C_{\rm SPEED}^2M(k)}
                 =\frac{3\sigma^2}{M(k)}}
\]

and `P_T` is the transverse projector for the central-difference symbol.
Both the charge and transverse-current vertices therefore use the same
kernel.

## 5. The massless static pole cancels

Put `u_i=1-cos k_i`, `U=sum u_i`, `P=sum_(i<j)u_i u_j`, and
`Q=sum u_i^2`. Direct algebra gives

\[
 M=2U-\frac23P,\qquad \sigma^2=2U-Q,
\]

and

\[
 M-\sigma^2
 =Q-\frac23P
 =\frac13Q+\frac13\sum_{i<j}(u_i-u_j)^2\ge0.
\]

Therefore

\[
 \boxed{0\le R(k)\le3}
\]

for every nonzero mode, with `R(k)->3` along each registered principal
direction as `k->0`. The inverse wave operator supplies a nominal `1/k^2`,
but the derivative source and derivative probe together supply `k^2` and
cancel it. There is no static Coulomb pole.

The registered directional expansions are

\[
 R_{100}=3-\frac34t^2+\frac1{16}t^4+O(t^5),
\]

\[
 R_{110}=3-\frac12t^2+O(t^5),
\]

\[
 R_{111}=3-\frac14t^2-\frac1{48}t^4+O(t^5).
\]

## 6. Static sign and soft radiation

Eliminating the static field gives the charge-sector effective energy

\[
 \boxed{V_{\rm eff}=-\frac{G_C^2}{2}\langle\rho,R\rho\rangle.}
\]

Since `R` is positive semidefinite, equal polarities have negative cross
energy and opposite polarities have positive cross energy. The native
FTD-0574 reciprocal channel therefore attracts equal polarity and repels
opposite polarity in the static linear sector—the reverse of electric charge.

This static no-go does not remove finite-frequency field waves. It does show
that a source's on-shell soft residue carries the factor

\[
 G_C^2\sigma^2=O(|k|^2),
\]

and vanishes quadratically in the infrared. A dynamic radiation signal can
survive at finite nonzero momentum, but it is not evidence for a long-range
static photon coupling.

## 7. Consequence for the engine ontology

The following statements are now exact within the registered frozen sector:

- the FTD-0574 source action has a reciprocal point-path force;
- its force has Lorentz algebra and kinematic homogeneous identities;
- its static exchange kernel is bounded and contains no `1/k^2` pole;
- its static polarity sign is not electromagnetic;
- the selected Gauss/Poisson Coulomb branch is an independent mechanism and
  cannot be attributed to this common action.

This closes identification of the minimal Hodge reciprocal force as native
Coulomb electromagnetism. It does not prove that no enlarged or nonlinear
FTD action can recover electromagnetic charge.

## 8. Verification and remaining gate

The locked native campaign executes 27 infrared symbol arms, 24 proper-cubic
rotations, 12 static charge arms, 12 transverse-current arms, four
Brillouin-corner controls, four periodic operator-identity arms, and eight
smooth path variations. Native worst residuals are below `1.2e-17`; the
independent SymPy/NumPy proof reproduces the sum-of-squares inequality,
directional limits, force identity, response signs, and production hashes.

The run of record is
`engine/results/ftd_0575/windows_msvc_cpu.json`. The preregistration SHA-256 is
`BE33049A5C93E887574BDE5509E93F666150A5CAF02E2B93989D96980D1788F6`.

FTD-0576 subsequently derives an exact conditional finite-step energy identity
but proves that its required central current is nonlocal for a cardinal hop and
that no finite-range face-to-native projection exists. Stable mobile
manifestation, reactions, collision composition, and every infrared particle
or Lorentz claim remain open. The live routes now require a separate matched
face dynamics, a staggered primitive, nonlocality, or a genuinely different
native nonlinear carrier.
