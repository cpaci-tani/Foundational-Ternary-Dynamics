# V3 redundant joint gravity-bundle protection and Green inheritance v1

**Date:** 2026-08-25  
**Status:** **[THEOREM — EXACT PREPARED ONE-FAULT PROTECTION OF THE JOINT
SCALAR/VECTOR/STF PACKET]** + **[THEOREM — CLEAN REVERSIBILITY AND
SIGNED-CUBIC COVARIANCE]** + **[THEOREM, CONDITIONAL — UNCHANGED FINITE
DIRICHLET HISTORY AND COMMON $1/\Lambda$ INHERITANCE]** + **[SELECTION —
THREE PREPARED PARALLEL TRANSACTION RAILS]** + **[OPEN — FORMATION,
BACKGROUND PROTECTION, CONSTRAINT DYNAMICS, TENSOR POLE, ACTION,
NORMALIZATION, UNIVERSAL COUPLING, COMMON CONE, AND LENSING]**  
**Carrier price:** three copies of the existing parent transaction plus one
fixed-occupancy existing A2 phase/work owner; seven sites of one Moore block  
**Production status:** unchanged  
**Ledger status:** no row minted  
**Parent:**
[`THEOREM_V3_NEUTRAL_SCALAR_VECTOR_STF_BUNDLE_AND_COMMON_GREEN_SEAM_v1.md`](THEOREM_V3_NEUTRAL_SCALAR_VECTOR_STF_BUNDLE_AND_COMMON_GREEN_SEAM_v1.md)  
**Exact certificate:**
[`proof_v3_redundant_joint_gravity_bundle_protection_and_green_inheritance.py`](../../../../../scripts/proofs/proof_v3_redundant_joint_gravity_bundle_protection_and_green_inheritance.py)

---

## 1. What is being protected

The parent theorem supplies a state-only neutral marked packet

\[
 {\cal G}(q;a,b)
 =U(q)\cup U(R^4q)\cup U(a)\cup U(R^2a)\cup U(b),       \tag{1}
\]

where each $U(x)=\{(x,+),(x,-)\}$ is an existing opposite-polarity
controller pair. Its readout is the joint nine-coordinate source

\[
 {\cal P}(a,b;\ell)
 =\left(1,\operatorname{STF}_5(a,b;\ell),v(R^2a;\ell)\right). \tag{2}
\]

The complete admissible source census has 34,560 signatures and exact rank

\[
 \operatorname{rank}\operatorname{span}{\cal P}=1+5+3=9. \tag{3}
\]

One parent local transaction is the finite symbol

\[
 X=(q,a,b,p),                                           \tag{4}
\]

meaning a marked departure ${\cal G}(q;a,b)$ beside an unmarked destination
$U(p)$. Its exact parent move is

\[
 F(q,a,b,p)=\left(Rq,p,Ra,Rb,d(Rq)\right),             \tag{5}
\]

with the first entry retained at the source, the middle three entries forming
the destination packet, and $d(Rq)$ the served SC displacement. The parent
already proves that $F$ has an explicit local inverse on the selected domain.

This successor protects the complete symbol $X$. It does not separately vote
on nine numerical readouts and it does not introduce a gravitational field
type.

---

## 2. Three-copy READ/COMMIT wrapper

Place three complete copies on three prepared parallel rails:

\[
 (X_1,X_2,X_3).                                        \tag{6}
\]

One existing fixed-occupancy A2 owner stores four control states,

\[
 (\text{READ},0),\ (\text{COMMIT},0),\
 (\text{READ},1),\ (\text{COMMIT},1),                 \tag{7}
\]

where the second coordinate records whether one repair has occurred. Four of
the existing 4,096 A2 states suffice.

At READ, define the strict majority $M(X_1,X_2,X_3)$. If exactly one copy was
replaced by any other valid transaction symbol,

\[
 (X,X,Y),\quad (X,Y,X),\quad (Y,X,X),\qquad Y\ne X,    \tag{8}
\]

then

\[
 M=X.                                                   \tag{9}
\]

READ rewrites all three copies to $X$, sets the work bit to one, and advances
to COMMIT. COMMIT applies the parent map $F$ to all copies:

\[
 (X,X,X;\text{COMMIT},w)
 \longmapsto
 (F(X),F(X),F(X);\text{READ},w).                       \tag{10}
\]

The work bit remains occupied and retained. A no-majority presentation or a
second repair request while $w=1$ fails closed.

The correction branch is intentionally noninjective: the three presentations
in equation (8) and the clean presentation all join the same corrected body.
This is not information annihilation. The surviving A2 work record
distinguishes a repaired body from an unrepaired one. Export or reset of that
work remains open.

---

## 3. Exact finite census

The certificate recovers the parent's same nine canonical pivot packets and
tests all $9\times12\times12=1{,}296$ selected local transactions. It proves:

1. every clean protected transaction reproduces the parent output exactly;
2. every clean output, including the A2 control record, has an exact inverse;
3. all three one-copy positions repair before all 1,296 parent moves, giving
   3,888 exact substitution rows;
4. the repetition decoder depends only on the equality pattern $X\ne Y$, so
   the replacement's identity cannot alter the decoded value; and
5. no-majority and busy-work presentations fail closed.

The theorem is a valid-symbol substitution basin. Loss or gain of occupied
records, corruption of more than one copy, faults between READ and COMMIT,
and faults in the router background are not included.

---

## 4. Moore-block price and covariance

For an SC direction $d$ and one prepared transverse SC axis $n\perp d$, use
the three departure sites

\[
 -n,\quad 0,\quad +n                                  \tag{11}
\]

and the corresponding destinations

\[
 -n+d,\quad d,\quad +n+d.                             \tag{12}
\]

Place the A2 work owner at $-d$. These are seven distinct sites in the same
$3\times3\times3$ Moore block. The certificate checks this geometry over all
48 signed-cubic images.

The transverse chart is prepared relational structure, hence the explicit
**[SELECTION]** tag. It is not a hidden invariant coordinate direction. When
the chart and all transaction symbols transform together, the protected map
is signed-cubic covariant. The certificate checks 432 protected packet rows.

---

## 5. Exact inheritance of the finite Green history

The parent controlled history has visit measure $n_N(x)$ and normalized
readout

\[
 G_N(x)=\frac{n_N(x)}{6N}.                             \tag{13}
\]

For its finite fixtures,

\[
 \left\|L_DG_N-\delta_s\right\|_\infty\le \frac{8}{N}. \tag{14}
\]

The certificate inserts one arbitrary-copy fault at every possible tick of
the parent fixture family. Across 2,088 fault positions, READ restores the
clean transaction before movement. Therefore the protected and clean visit
measures are equal, not merely close:

\[
 \boxed{n_N^{\rm protected}(x)=n_N^{\rm parent}(x)}.  \tag{15}
\]

For every source component ${\cal P}_A$,

\[
 \left\|L_D({\cal P}_A G_N)-{\cal P}_A\delta_s\right\|_\infty
 \le |{\cal P}_A|\frac{8}{N}.                         \tag{16}
\]

Because the protected packet basis still has rank nine, the prepared
large-domain history conditionally inherits the parent's one common diagonal
kernel

\[
 \boxed{
 {\cal H}_A(k)=\frac{{\cal P}_A(k)}{\Lambda(k)},
 \qquad
 \Lambda(k)=6-2\sum_{i=1}^3\cos k_i .}                \tag{17}
\]

Redundancy adds no new pole and changes no coefficient. It only proves that
the already-conditional blocked-history response survives the registered
one-fault basin.

---

## 6. Exact gravity boundary

The result closes one previously explicit word: **protection**, at finite
prepared packet scope. The gravity status is now

```text
joint rank-nine scalar/vector/STF packet:       present conditionally
radius-one parent move and exact inverse:       present
one-valid-symbol-fault packet protection:       present on prepared rails
finite Green-history inheritance:              exact on registered fixtures
rail formation, renewal, sink, arbitration:    absent
rotor-background protection and work export:   absent
propagated scalar/vector constraints:           absent
two protected TT tensor modes:                  absent
dynamical static and tensor wave poles:         absent
interacting action and normalization:           absent
universal stress coupling and common cone:      absent
clock response, lensing, delay, nonlinearity:   absent
```

In particular,

\[
 \boxed{
 \text{protected source-history packet}
 \not\Rightarrow
 \text{autonomous spin-2 gravitational wave}.}        \tag{18}
\]

The next gravity theorem must generate the local scalar/vector constraint
algebra and isolate two propagating transverse-traceless modes under the
homogeneous state-complete $\Phi$. The dynamical Hessian, reciprocal material
coupling, shared cone, and observables must then be derived from the same
action. The Deser bootstrap cannot substitute for those microscopic steps.

---

## 7. Reproduction

From the repository root:

```bash
python scripts/proofs/proof_v3_redundant_joint_gravity_bundle_protection_and_green_inheritance.py
```

Expected result: `13/13` exact checks pass, with 1,296 clean protected
transactions, 3,888 one-copy substitution rows, all 2,088 finite-history fault
positions, 432 covariance rows, and seven occupied transaction/control sites
inside one Moore block.
