# ANALYSIS — Corrected localized-basin relaxation data

**Identifier:** `FTD-0682`  
**Status:** `[NUMERICAL FACT — POST HOC, NOT HELD OUT]`  
**Source:** corrected FTD-0681 dataset; registered FTD-0681 verdict remains
execution-invalid

## What is independently established in the corrected data

The selected connected composite retains its charge labels, graph, sector, and
exact reversible common-action history while the translation/boost-quotiented
internal phase distance falls.  Over the locked ticks `8..64`:

```text
Gamma_core             0.0065371211 / 0.0065371283
DeltaBIC               412.6953 / 412.6955
R^2                    0.999331984 / 0.999331987
fractional decline     0.312296700 / 0.312296913
```

At tick 80 the internal phase ratio is `0.6015612` for both signs.  The
canonical doublet ratio is `0.6015653`; the close tracking is observed, not
imposed.  Maximum collective-center offset is `2.19e-9`, maximum mean-momentum
offset is `1.25e-9`, and maximum edge-length-squared change is `7.13e-7`.

The positive control-relative field norm moves outward but is not remote
dominated at tick 80:

```text
near, r <= 8             0.24831754
intermediate, 8 < r <=24 0.62241023
far, r > 24              0.12927223
```

Thus the post-hoc physical classifier is
`REMOTE_FIELD_NOT_DOMINANT`.  This is not a registered v3 verdict.

## Ontological reading

The strongest current matter story is relational and dynamical:

1. a connected polarity configuration supplies persistent constituent and
   graph identity;
2. the exact common action couples that internal configuration to matched
   face/edge fields;
3. a small internal excitation loses amplitude approximately exponentially
   over the pre-contact window;
4. a difference-field record grows and migrates from the near shell into the
   intermediate shell;
5. the complete matter-plus-field state remains reversible, so the exported
   record retains the information needed to reconstruct the excitation.

This is compatible with an object that sheds internal excitation into its
environment without destroying its relational core.  It is not yet evidence
for an asymptotically attracting matter basin.  The field norm at tick 80 is
mostly a broad intermediate cloud, not a cleanly detached far pulse and not a
qualified bound dressing.

## Why this matters

Microscopic reversibility and apparent object-level relaxation coexist here
without inserting damping.  The apparent arrow arises from projecting the
complete reversible state onto the localized internal coordinates while the
complementary field degrees of freedom carry away phase information.  That is
a concrete engine realization of open-subsystem relaxation, but only in the
selected finite pre-contact history.

## Remaining discriminators

The next measurement must distinguish three possibilities without redefining
shells after inspection:

- a persistent co-moving near dressing;
- a broad outgoing radiative packet with measurable shell-arrival speed;
- continued nonseparable matter--field spreading with no stable localized
  object/environment cut.

That requires a larger causal buffer, fixed integer shell origin or a proved
floating-origin face convention, radial arrival/current observables, and a
late-time near-field plateau test.  A particle pole and mobile translation
remain downstream gates.

## Boundary

- The FTD-0681 registered verdict is still execution-invalid.
- The exponential fit is a numerical finite-window fact, not an asymptotic
  theorem.
- Difference-field self norm is not the complete incremental field energy;
  interference with the control dressing remains separate.
- “Intermediate cloud” is morphology, not radiation, aura, wake, or pilot
  wave.
- No production rule, postulate, force, toggle, or scenario changed.
