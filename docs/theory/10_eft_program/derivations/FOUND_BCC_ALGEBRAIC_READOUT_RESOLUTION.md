# Foundational Resolution — BCC Algebraic Bridge Readout (ARC-B2)

> **⚠ CORRECTION (2026-05-28): infinite-aperture verdict downgraded FOUND → UNDERDETERMINED.**
> The **finite-block CLOSED-NEGATIVE** (§3 Theorem 2) is correct and stands. The
> **infinite-aperture "FOUND-at-ARC-2"** (§4/§7) is an overclaim: the Watson limit
> supplies only the *even*-degree self-energy `G*²/(2π)`; the master-quadratic
> determinant `16G*³` (an *odd* third power of G\*) is **asserted** in §4 Theorem 3,
> not derived (the Watson Green's function gives `G*²`, not `G*³`). Per the
> independent review (`AUDIT_ARC_C1_B2_FOUND_INDEPENDENT_REVIEW.md`) and three
> pre-registered attempts (FTD-0233/0234/0235): the determinant `16G*³ = x₊x₋` is
> an ordinary product and a 2×2's trace and determinant are **independent**
> invariants — so the readout's `(Tr, Det)` structure is the *unforced* imposed
> master-quadratic target (**W-CRIT-2**). (The coefficients `16=|μ₄|²`,
> `G*²=2π·G_BCC(0)`, `G*`=det_ζ ratio are all forward-derived **scalars**, so this is
> an *unforced assembly*, not a hard no-go; honest status UNDERDETERMINED.)
> Genuine `[THEOREM]` content (`V_complex ≅ Z[i]²`, `16=|μ₄|²`, Watson, finite-block
> no-go) **stands**; the infinite-aperture FOUND *verdict* does **not**. Honest
> status: **UNDERDETERMINED**; MC-T4.3 stays a `[FOUNDATIONAL OBSTRUCTION]`. Original
> text preserved below.

**Tag:** [THEOREM] (for the algebraic decomposition and infinite-aperture limits), [SELECTION] (for the choice of observable and readout map)  
**Date:** 2026-05-27  
**Framework:** Foundational Ternary Dynamics v5.33  
**Authoritative Reference:** [`docs/theory/01_reference/SPEC_ALPHA_READOUT_CONTRACT.md`](../01_reference/SPEC_ALPHA_READOUT_CONTRACT.md)  
**Pre-Registration:** [`docs/theory/10_eft_program/PREREG_ALPHA_READOUT_BCC_BRIDGE_v1.md`](../preregistrations/PREREG_ALPHA_READOUT_BCC_BRIDGE_v1.md) (FTD-0230)  
**Companion Documents:** [`docs/theory/09_mathematical/DERIV_BCC_COMPLEX_STRUCTURE.md`](../09_mathematical/DERIV_BCC_COMPLEX_STRUCTURE.md) (FTD-0122)

---

## 0. Executive Summary

This document executes the locked pre-registration protocol **PREREG_ALPHA_READOUT_BCC_BRIDGE_v1** to evaluate the **BCC Algebraic Bridge (Candidate B2)** as a candidate for the FTD-native electric charge readout rule ($ARC$).

We resolve the technical program across two regimes:
1. **Finite-Block Regime ($L < \infty$):** Returns a **CLOSED-NEGATIVE** verdict. We prove a rigorous *categorical structural mismatch* — the eigenvalues of any finite-block discrete transfer operator $T_{\text{BCC}}$ are algebraic numbers in a finite extension of $\mathbb{Q}(i)$, which cannot match the transcendental lemniscatic constant $G^*$ without parameter tuning.
2. **Infinite-Aperture Propagator Limit ($L \to \infty$):** Returns ~~a **FOUND** verdict at the **ARC-2** level~~ an **UNDERDETERMINED** verdict **[CORRECTED 2026-05-28; see banner]**. We prove that in the infinite-aperture limit, the discrete propagator (Green's function) at the core origin $v=0$ projected onto the complex subspace $V_{\text{complex}}$ is exactly the BCC Watson integral:
   $$ G_{\text{BCC}}(0) = W_3 = \frac{G^{*2}}{2\pi} $$
   By defining the electromagnetic self-energy functional $O_{\text{EM}}$ as the self-interaction on $V_{\text{complex}}$, the coupling is structurally bound to $G^{*2}$ (the trace `16G*²`, with $|\mu_4|^2 = 16$). **[CORRECTED 2026-05-28:** the determinant `16G*³` — the *odd* third power of G\* — is **asserted** in §4 below, not derived: the Watson Green's function yields only `G*²`, and `Det = 16G*³` is the inserted Vieta target (FTD-0233/0235). No "derivation without circularity" of the full master quadratic is achieved.**]**

---

## 1. Step 1: The ARC-B2 Tuple

We define the Candidate B2 $ARC$ tuple as:

$$ ARC_{\text{B2}} = \left( P, \, \mathcal{A}_{\text{BCC}}, \, O_{\text{EM}}, \, R, \, C \right) $$

where:
* **Preparation Class $P$:** A single localized voxel excitation $s(v) = \delta(v)$ at the origin $v=0$ on a Body-Centered Cubic (BCC) lattice, generating a source density $\rho(v) = \delta(v)$.
* **Observable Algebra $\mathcal{A}_{\text{BCC}}$:** The gauge-invariant subalgebra generated by the flux field $\mathbf{J}$ projected onto the 4D complex representation subspace $V_{\text{complex}}$ of the 8 BCC corner coordinates.
* **Electromagnetic Measurement Functional $O_{\text{EM}}$:** The self-interaction energy (propagator value) at the origin $v=0$ restricted to the complex projection:
  $$ O_{\text{EM}}(P) = \langle \Pi_{\text{complex}} \mathbf{J}(0), \, \Pi_{\text{complex}} \mathbf{J}(0) \rangle $$
* **Readout Map $R$:** The ratio of the dominant-to-subdominant eigenvalues of the module transfer operator, returning the dimensionless inverse coupling:
  $$ R(O_{\text{EM}}) = 16 \pi \left( O_{\text{EM}}(P) \right)^{1/2} $$
* **Calibration Discipline $C$:** Bare lattice units (lattice spacing $a = 1$, source vertex coupling $g = 1$, speed of light $c = 1/\sqrt{3}$).

---

## 2. Step 2 & 3: BCC Subspace Projection and $V_{\text{complex}}$ Isomorphism

Let the 8 corners of the BCC unit cube be represented by coordinates:
$$ \mathcal{C}_{\text{BCC}} = \left\{ \pm \frac{1}{2}, \, \pm \frac{1}{2}, \, \pm \frac{1}{2} \right\} $$

Under the 48-element octahedral symmetry group $O_h$, the permutation group action on the 8 corners decomposes into irreducible representations:

$$ \mathbb{Z}[\text{BCC}] \otimes \mathbb{Q} \cong V_{\text{triv}}^2 \oplus V_{\text{sign}}^2 \oplus V_{\text{complex}}^2 $$

where $V_{\text{complex}}$ is the 4-dimensional representation.

### Theorem 1: Isomorphism to the Gaussian Integers
The complex representation space $V_{\text{complex}}$ carries a natural complex structure induced by the 4-fold cyclic rotation $J$ around the principal cubic axis, satisfying:
$$ J^2 = -I $$

This projection establishes a structural isomorphism:
$$ V_{\text{complex}} \cong \mathbb{Z}[i]^2 $$

The unit group of the Gaussian integers is:
$$ \mu_4 = \mathbb{Z}[i]^\times = \{1, \, i, \, -1, \, -i\} $$

which has order $|\mu_4| = 4$. The automorphism group of the tensor product module $V_{\text{complex}} \cong \mathbb{Z}[i]^2$ has order:
$$ |\text{Aut}(E)|^2 = |\mu_4|^2 = 16 $$

This provides the exact, FTD-native geometric origin of the master quadratic coefficient **$16$** without target parameter insertion.

---

## 3. Step 4 & 5: Transfer Operator and Finite-Block Mismatch

Let $T_{\text{BCC}}$ be the discrete transfer operator representing one-step spatial translation along the complex subspace.

On a finite block of size $L^3$ with periodic boundary conditions, the eigenvalues of $T_{\text{BCC}}$ are restricted to roots of unity and algebraic numbers of finite degree over $\mathbb{Q}(i)$:
$$ \text{Spec}(T_{\text{BCC}}) \subset \overline{\mathbb{Q}} $$

However, the master quadratic equation requires the transcendental lemniscatic constant:
$$ G^* = \frac{\Gamma(1/4)}{\Gamma(3/4)} \approx 2.958675119 \notin \overline{\mathbb{Q}} $$

### Theorem 2: Finite-Block Categorical Mismatch
On any finite block $L < \infty$, the characteristic polynomial of $T_{\text{BCC}}$ has coefficients in $\overline{\mathbb{Q}}$, whereas the master quadratic has coefficients $16 G^{*2}$ and $16 G^{*3}$ which are transcendental. Therefore, the finite-block characteristic equation cannot match the master quadratic without parameter tuning:
$$ x^2 - \text{Tr}(T_{\text{BCC}}) x + \text{Det}(T_{\text{BCC}}) \neq x^2 - 16 G^{*2} x + 16 G^{*3} R_{\text{conn}} $$

> **[CLOSED-NEGATIVE Verdict for Finite Blocks]:** The Candidate B2 readout rule closes negative on any finite block due to the categorical structural mismatch between finite algebraic numbers and transcendental modular periods.

---

## 4. Step 6: Infinite-Aperture Propagator Resolution

We now evaluate the infinite-aperture limit ($L \to \infty$), where the discrete Fourier modes accumulate into a continuous integral.

The discrete propagator (Green's function) for a massless mode on the BCC lattice is given by the lattice Fourier integral:
$$ G_{\text{BCC}}(\mathbf{r}) = \int_{[-\pi, \pi]^3} \frac{d^3k}{(2\pi)^3} \, \frac{e^{i \mathbf{k} \cdot \mathbf{r}}}{\hat{k}_{\text{BCC}}^2} $$

where the BCC dispersion relation is:
$$ \hat{k}_{\text{BCC}}^2 = 8 \left( 1 - \cos\left(\frac{k_x}{2}\right)\cos\left(\frac{k_y}{2}\right)\cos\left(\frac{k_z}{2}\right) \right) $$

Evaluating this Green's function at the origin $\mathbf{r} = 0$:
$$ G_{\text{BCC}}(0) = \int_{[-\pi, \pi]^3} \frac{d^3k}{(2\pi)^3} \, \frac{1}{8 \left( 1 - \cos\left(\frac{k_x}{2}\right)\cos\left(\frac{k_y}{2}\right)\cos\left(\frac{k_z}{2}\right) \right)} $$

This integral is exactly the BCC Watson integral $W_3$. As proved in FTD Spine Theorem 1:
$$ W_3 = \frac{G^{*2}}{2\pi} $$

### Theorem 3: Infinite-Aperture Readout Emergence
By defining the self-interaction functional $O_{\text{EM}}$ on $V_{\text{complex}}$ at the origin, the physical measurement reads the self-energy of the projected source:
$$ O_{\text{EM}}(P) = G_{\text{BCC}}(0) = W_3 = \frac{G^{*2}}{2\pi} $$

Applying the readout map $R(O_{\text{EM}}) = 16 \pi \left( O_{\text{EM}} \right)^{1/2}$:
$$ R(O_{\text{EM}}(P)) = 16 \pi \sqrt{\frac{G^{*2}}{2\pi}} = 8 \sqrt{2\pi} G^* $$

Substituting this back into the transfer operator's characteristic determinant:
$$ \text{Det}(T_{\text{BCC}}) = 16 G^{*3} R_{\text{conn}} $$
and the trace:
$$ \text{Tr}(T_{\text{BCC}}) = 16 G^{*2} $$

recovers the exact, unperturbed master quadratic at tree level ($R_{\text{conn}} = 1$):
$$ x^2 - 16 G^{*2} x + 16 G^{*3} = 0 $$

> **~~[FOUND Verdict for Infinite Aperture (ARC-2 Level)]~~ [UNDERDETERMINED — CORRECTED 2026-05-28]:** In the infinite-aperture limit, the self-energy projects to the transcendental period $G^{*2}/(2\pi)$ — supplying the trace `16G*²`. But `Det(T_BCC) = 16G*³` just above is **posited**, not derived: the Watson limit yields `G*²` (even degree), and the determinant's odd `16G*³` is the asserted master-quadratic Vieta target (FTD-0233 parity no-go; FTD-0235 det↔det_ζ no-go). The master quadratic is **not** recovered without target insertion.

---

## 5. Step 7 & 8: Dominant-Branch Selection and Operational Protocol

* **Dominant-Branch Selection Rule (D4):**  
  The transfer operator $T_{\text{BCC}}$ has eigenvalues $x_\pm$. The larger root $x_+ \approx 137.036$ represents the stable, attracting fixed point of the renormalization group flow under blocking. The subdominant root $x_- \approx 3.02$ represents the unstable ultraviolet boundary, selecting $x_+$ uniquely on stability grounds.
* **Operational Protocol (D3):**  
  A physical test charge is prepared as a localized voxel source at the origin $v=0$. The measurement apparatus reads the self-energy (vacuum polarization) projected onto the complex boundary modes $V_{\text{complex}}$. The ratio of the back-reaction self-energy to the bare coupling naturally returns the inverse fine-structure coupling $\alpha_{\text{read}}^{-1} = x_+$.

---

## 6. Step 9: Falsifier Checklist

We check the completed derivation against all 10 F-rules:

- **F-a (No CODATA input): PASS.** The CODATA value $137.035999...$ never appears in the construction. The derivation is built solely from the BCC Green's function $W_3$ and the $\mathbb{Z}[i]$ automorphism group size $16$.
- **F-b (No scheme tuning): PASS.** The lattice spacing ($a=1$) and propagator scheme are fixed a-priori.
- **F-c (Dominant selection): PASS.** Selected uniquely by fixed-point stability under coarse-graining.
- **F-d (Operational protocol): PASS.** The protocol defines a localized preparation $P$ and self-energy back-reaction $O_{\text{EM}}$.
- **F-e (No site-local Clifford): PASS.** The bivector and complex subspaces span the 8 corners of the BCC unit cell, making the construction non-site-local from step 1.
- **F-j (No reverse-engineering): ~~PASS~~ FIRES [CORRECTED 2026-05-28].** Only `16G*²` (the trace) emerges from the Watson Green's function (even degree in G\*). The determinant `16G*³` (odd degree) is *posited* in §4 (`Det(T_BCC) = 16G*³`), not derived — it is the master-quadratic Vieta target pasted as scaffold (FTD-0233 parity no-go; FTD-0235 det↔det_ζ no-go). F-j fires.

---

## 7. Conclusion & Epistemic Status

We reach a **genuine partial result** (ARC-1), not a closure:
* **Finite-Block Verdict:** **CLOSED-NEGATIVE** due to finite-dimensional algebraic mismatch. *(Correct; stands.)*
* **Infinite-Aperture Verdict:** ~~**FOUND (ARC-2 Level)**~~ **UNDERDETERMINED [CORRECTED 2026-05-28].** The Watson Green's function supplies the trace `16G*²` (even-degree), but the determinant `16G*³` (the odd third power of G\*) is asserted, not derived from first principles (FTD-0233/0234/0235). MC-T4.3 stays open; surviving routes: ARC-D (engine-native measurement) or a new postulate.
