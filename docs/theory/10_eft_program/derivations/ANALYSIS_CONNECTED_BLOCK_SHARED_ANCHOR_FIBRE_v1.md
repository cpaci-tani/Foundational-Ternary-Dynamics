# FTD-0626 — The connected-block conflict was a chart alias, not annihilation

**Status:** `[SELECTED ONTIC EXTENSION — DEFAULT OFF]` +
`[MEASURED — REVERSIBLE FIBRE-ENABLED MOTION]` +
`[CLOSED NEGATIVE — INTERNALLY FIXED HALF-CELL REST]` +
`[OPEN — DYNAMICAL REST / PERIODIC OR QUASIPERIODIC BACKGROUND]`  
**Protocol SHA-256:**
`67806EA9B3D8ED02B2BF04A839B21E1053FDE1199DE46FB2E064D6E061544C52`  
**Verdict:** `CONNECTED_BLOCK_FIBRE_CLOSED_NEGATIVE`  
**Production status:** unchanged

## 1. Correction

FTD-0624/0625 correctly measured failure of the independent one-record-per-
anchor projection, but their reaction-surface interpretation was too strong.
The failed pairs were not coincident in effective position. They were nearly
one cell apart while rounding to the same chart anchor.

FTD-0626 applies the already priced FTD-0609 multiplicity-two constituent
fibre without changing the action. All nine registered histories then complete
16 forward and 16 state-only reverse transactions. The two strict default-
false controls still reject at ticks one and two with converged roots, intact
graphs, and opposite-polarity repeated anchors only. The sole changed gate is
therefore the chart capacity.

The earlier data do not demonstrate annihilation, decay, or a physical
collision. They demonstrate that aggregate ternary `s` is not a lossless
storage format for the selected constituent-complete matter state.

## 2. Exact result

Across 288 registered fibre-enabled transactions:

- worst common-action residual: `1.9125891192e-11`;
- worst total-energy drift: `1.3056222770e-13`;
- worst state-only recovery: `5.1061377349e-12`;
- signed-circulation residual: `1.2878587086e-14`;
- cyclic-covariance residual: `6.0290737780e-12`;
- maximum anchor multiplicity: two;
- minimum shared-pair separation: `0.9980407496` cell.

No constituent, charge, or bond is deleted or created. The field endpoint plus
the explicit constituent state reverses without an event journal. Thus no
reaction bit or outgoing-radiation branch is forced by this failure class.

## 3. The exact-half result

Both cyclic exact-half arms have centre displacement below
`2.52e-15` and centre momentum below numerical noise for all 16 ticks. Every
endpoint contains four multiplicity-two shared anchors, and the nearest shared
pair remains more than `0.9980` cell apart. The complete histories reverse to
about `4.4e-13`.

They nevertheless fail the locked fixed-rest conjunction:

| observer | base `x` | cyclic `y` |
|---|---:|---:|
| maximum centre displacement | `2.5121e-15` | `2.5121e-15` |
| maximum internal shape displacement | `1.261495e-3` | `1.261495e-3` |
| maximum squared-edge strain | `3.914662e-3` | `3.914662e-3` |
| reverse recovery | `4.4086e-13` | `4.3935e-13` |

The centre is at rest while the constituents breathe. This is not a fixed
point, but it is also not a decay in the measured horizon. The response is
bounded, energy preserving, cubic, and reversible for 16 ticks. The record is
too short to call it periodic or quasiperiodic.

## 4. Circulation result

Every `B_x` and `4B_x` circulation arm that FTD-0625 rejected at tick one or
two now completes and reverses. Each exercises the fibre for one accepted
endpoint with separation above `0.9985` cell. All six arms pass the exactness,
fibre, symmetry, covariance, and inverse gates.

The reported labelled-coordinate shape excursions (`0.3075` at `B_x` and
`0.5978` at `4B_x`) are dominated by orientation change and are not a rigid-
body-invariant deformation measure. The maximum bond strain remains below
`0.00366` and `0.00299`, respectively. The data therefore support reversible
internal rotation of the connected pattern, not collision-driven destruction.

## 5. Ontological consequence

The selected R2 matter state now has a sharper interpretation:

1. `s in {-1,0,+1}` remains the manifested aggregate channel at a site;
2. a material object is an extended, labelled constituent configuration with
   continuous subcell position and momentum;
3. more than one distinct constituent may occupy the same integer coordinate
   chart while remaining spatially separated inside that chart;
4. compact polarity density and face current are derived projections used for
   field coupling;
5. rest of the centre need not mean frozen internal constituents.

This is a selected ontic extension, not a derivation from the five postulates.
It is more economical than adding an annihilation rule here because it was
already independently motivated and tested by FTD-0609.

## 6. Next discriminator

The next locked campaign should extend the zero-momentum exact-half background
to at least 256 ticks and its state-only inverse, then measure:

- boundedness of internal shape and bond strain;
- recurrence of the complete matter-plus-field state;
- a preregistered discrete spectrum of cubic internal observables;
- centre drift and fibre multiplicity;
- tangent/Floquet growth only if a period is independently identified.

If a reversible periodic or quasiperiodic background survives, it becomes the
proper rest state for pole, boost, dressing, and wake studies. A reaction
transaction is deferred until a genuine effective-position encounter or a
count-changing native event is observed.

## 7. Reproducibility

- connected-action header SHA-256:
  `14A4D9DBD9286A4F5A9EFB77CCFE38369BDD1FF383E89BE691FD1A5EEACC5AC9`;
- connected-action source SHA-256:
  `521556DEE73AE06824B3443BDF6678307C773264923369DD9F275DAF1EEEAEC5`;
- runner SHA-256:
  `B96990E1DBAB177DF3D24920958D8C2C567128D293A22D47A3F36E057207D0D7`;
- JSON SHA-256:
  `DEDFF2C31C510A7944CF5FD213E1165172342324B6C38432D599F4F212570308`;
- arm CSV SHA-256:
  `6E492456F4FBA6803B047321AE7ABDE075A03E19AF0C3581AB580E50B73E82BC`;
- tick CSV SHA-256:
  `6B2C90F106E63F4066973ED0E2513BAC42FACCA8FD3F4D87E047DBF3F7F8A77B`;
- regression CSV SHA-256:
  `F42455478406675E53BA569CA205B916B4D26FB1779C6F599D2E5D2D5AD9DB7D`;
- independent certificate SHA-256:
  `1F835D8346C6115A8830F730C25C69D01C4DBB0F6145A71F7B36983494477F89`;
- independent certificate: `44/44` checks pass.
