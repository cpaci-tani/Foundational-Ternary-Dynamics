# Common-admission clock/Maxwell theorem and spatial-lensing price v1

**Date:** 2026-08-24

**Status:** **[THEOREM — EXACT REVERSIBLE FINITE PERMISSION-HISTORY
REPARAMETRIZATION]** + **[THEOREM, CONDITIONAL — ONE ADMISSION FRACTION SCALES
THE MATERIAL CLOCK AND COTANGENT MAXWELL CONE]** + **[SCOPED NO-GO — TEMPORAL
ADMISSION ALONE CANNOT PRODUCE EQUAL TEMPORAL+SPATIAL LENSING]** + **[OPEN —
NATIVE PERMISSION LAW, SPATIAL HODGE RESPONSE, INHOMOGENEOUS LIFT, STATIC
POLE, LENSING]**

**Production status:** unchanged; the current wave stencil remains unread of
latency

**Ledger status:** no row minted

**Exact certificate:**
[proof_common_admission_clock_maxwell_half_lensing_boundary.py](../../../../../scripts/proofs/proof_common_admission_clock_maxwell_half_lensing_boundary.py)
performs 65,832 exact checks. It exhausts every binary permission word through
ten global ticks on all sixteen valid phase/polarity clock-ownership states,
verifies the retained inverse, checks every admitted/global tick count through
sixteen ticks for the slow Floquet derivative, and proves the weak optical
response identities symbolically.

---

## 1. One global tick, one local admission history

Let $F$ be the exact period-eight ternary-square material-clock permutation
already registered by the
[autonomous crossing-clock theorem](THEOREM_TERNARY_SQUARE_PHASE_POLARITY_CARRIER_AND_AUTONOMOUS_CROSSING_CLOCK_v1.md).
Let

\[
 g_n(x)\in\{0,1\}                                  \tag{1}
\]

be a retained local capacity/backpressure permission at global tick $n$.
The local update is

\[
 X_{n+1}=F^{g_n}X_n.                               \tag{2}
\]

For every finite permission history,

\[
 \boxed{
 X_N=F^{\tau_N}X_0,
 \qquad
 \tau_N=\sum_{n=0}^{N-1}g_n.}                    \tag{3}
\]

### Proof

Every factor in the ordered product is either identity or the same map $F$.
The factors therefore reduce exactly to $F$ raised to the number of admitted
ticks. Reversal reads the retained permission history backward and applies
$F^{-1}=F^7$ at every admitted entry. No permission record is erased.
$\square$

Equation (3) realizes the intended global/local distinction precisely:
global tick count $N$ never changes, while the physical clock phase advances
only by the local admitted count $\tau_N$.

The action has not yet generated $g_n$. Treating it as an external schedule is
conditional scaffolding.

---

## 2. General gated Floquet theorem

The same identity holds for any reversible local Floquet advance $M(k)$:

\[
 \prod_{n=0}^{N-1}M(k)^{g_n}=M(k)^{\tau_N}.        \tag{4}
\]

Suppose one admitted advance has a normalized slow eigenvalue

\[
 \lambda(k)=1+i c_*k+O(k^2).                       \tag{5}
\]

Then

\[
 {1\over N}{\partial\over i\partial k}
 \lambda(k)^{\tau_N}\bigg|_{k=0}
 ={\tau_N\over N}c_*.                             \tag{6}
\]

For a periodic history or any long history with admission fraction

\[
 \nu(x)=\lim_{N\to\infty}{\tau_N(x)\over N},      \tag{7}
\]

the local cone measured per global tick is

\[
 \boxed{c_{\rm gated}(x)=\nu(x)c_*.}               \tag{8}
\]

Equation (8) is exact for a homogeneous permission region. Its use in a
slowly varying background is an eikonal/blocking limit; interfaces,
backscatter, and finite-window dispersion remain open.

---

## 3. Conditional common clock/Maxwell gate

The selected cotangent vacuum sector has

\[
 c_{\rm EM}={1\over6}                              \tag{9}
\]

per admitted local Maxwell tick. If—and only if—the same $g_n(x)$ that admits
the material crossing clock also admits the complete cotangent Maxwell
Floquet advance, equations (3) and (8) give

\[
 \boxed{
 {d\tau_{\rm clock}\over dn}=\nu,
 \qquad
 c_{\rm EM}^{\rm global}(x)={\nu(x)\over6}.}       \tag{10}
\]

This is the first exact shared discrete-time mechanism relating local clock
rate and the light cone without changing the global update order.

It is conditional because the production wave stencil currently does not
read latency, as measured by FTD-1020. Equation (10) must be generated by the
finite common action, not installed as a refractive-index patch.

---

## 4. Weak temporal optical response

Let $U(x)$ be a weak positive capacity depth and suppose the common admission
law blocks to

\[
 \nu(U)=1-a_tU+O(U^2).                              \tag{11}
\]

The material clock rate is reduced by $a_tU$. Under the common gate, the wave
index is

\[
 n_0(U)={1\over\nu(U)}
 =1+a_tU+O(U^2).                                   \tag{12}
\]

Therefore the lensing-response notation of the
[common-capacity discriminator](../gravity_cosmology/THEOREM_COMMON_CAPACITY_LENSING_SHAPIRO_RESPONSE_DISCRIMINATOR_v1.md)
becomes

\[
 \boxed{a_0=a_t.}                                  \tag{13}
\]

Equation (13) is not a fitted equivalence principle. It follows algebraically
from using exactly the same admitted-tick count for the material clock and
the complete Maxwell advance.

---

## 5. Temporal-only half-lensing boundary

Let slow-body falling measure coefficient $a_m$. With no additional spatial
capacity response,

\[
 a_s=0.                                            \tag{14}
\]

The blind deflection/Shapiro discriminator is then

\[
 \boxed{
 \mathscr D=\mathscr S
 ={a_0+a_s\over a_m}
 ={a_t\over a_m}.}                                \tag{15}
\]

If the same weak source also gives clock/fall coherence $a_t=a_m$, equation
(15) yields

\[
 \boxed{\mathscr D=\mathscr S=1.}                 \tag{16}
\]

This is exactly the clock-medium/1911 response class. Hence:

> A shared local admission clock can supply the temporal half of weak
> lensing, but admission/time dilation alone cannot supply the equal
> temporal+spatial class.

This is a scoped no-go. It assumes the optical effect of the capacity field is
only the common admitted-tick fraction. A genuinely spatial incidence/Hodge
response is outside that assumption and is the required repair.

---

## 6. Exact spatial response price

Let a separate spatial/Hodge factor block as

\[
 \sigma(U)=1-a_sU+O(U^2).                          \tag{17}
\]

If Maxwell propagation uses both the local admission rate and spatial
incidence capacity,

\[
 c_{\rm ray}(U)=c_*\nu(U)\sigma(U),                \tag{18}
\]

then

\[
 \boxed{
 n(U)={1\over\nu\sigma}
 =1+(a_t+a_s)U+O(U^2).}                           \tag{19}
\]

The equal temporal+spatial response requires

\[
 \boxed{a_s=a_t=a_m,}                              \tag{20}
\]

which gives $\mathscr D=\mathscr S=2$. Equation (20) is a future blind result,
not an allowed parameter assignment.

The parity-staggered even/odd tensor/Hodge carrier is a structural candidate
for $\sigma(U)$ because it changes spatial incidence rather than merely
stalling time. No finite transaction has yet derived equation (17).

---

## 7. Reconciliation with the current engine

The tested production situation is

\[
 a_t/a_m\simeq1,
 \qquad a_0=a_s=0,                                 \tag{21}
\]

at the FTD-1019/1020 fixtures. It therefore gives

\[
 \mathscr D=0,                                     \tag{22}
\]

not equation (16), because the wave operator does not share the admission
gate. This theorem does not reinterpret the class-0 result as class 1 and does
not authorize modifying production code.

---

## 8. Consequence for the one-action program

The gravity/clock/light problem has split into two exact finite mechanisms:

\[
 \text{common admission }g_n
 \longrightarrow a_0=a_t,                         \tag{23}
\]

\[
 \text{spatial cotangent/Hodge capacity}
 \longrightarrow a_s.                             \tag{24}
\]

The common action must generate both from one retained capacity ledger. A
clock-only gravity law can at most pass equation (16); full lensing requires
the second spatial transaction without spoiling the vacuum Maxwell cone,
Gauss incidence, or the tensor parity structure.

---

## 9. Next locked gate

Construct one finite local capacity transaction that:

1. derives $g_n(U)$ from local reserve/backpressure rather than an external
   word;
2. gates the complete material and cotangent Maxwell advances with the same
   retained permission;
3. generates a separate spatial Hodge factor from primal/dual capacity;
4. preserves reversibility at inhomogeneous permission boundaries;
5. yields $a_0/a_m$ and $a_s/a_m$ before any ray is evaluated; and
6. passes the independent blind deflection/delay equality.

Until then, equation (10) is a common-gate theorem and equation (16) a sharp
boundary—not native lensing.

**Successor sharpening.** The
[primal/dual permission theorem](THEOREM_PRIMAL_DUAL_PERMISSION_IDEMPOTENCE_AND_LENSING_FACTOR_PRICE_v1.md)
proves that the same binary permission cannot supply equation (24) by being
read twice, because $g^2=g$. Within the retained-binary class, the minimum
repair is a separately retained primal/dual pair. An exact reversible product
enumerator then yields $c_{\rm ray}=\nu_t\nu_s/6$; equal marginals would reach
class 2, but their action-generated equality and physical Hodge ownership
remain open.

The later
[self-dual trace-capacity action](../gravity_cosmology/THEOREM_SELF_DUAL_TRACE_CAPACITY_STATIC_POLE_AND_EQUAL_RESPONSE_LENSING_BOUNDARY_v1.md)
constructs the corresponding blocked reference solution. A symmetric trace
source excites only the massless common mode, so \(U_t=U_s\); normalized
readouts then give \(\nu_t=\nu_s=1-U+O(U^2)\) and conditional class 2. The
self-dual source/readout law is selected, not generated by the finite
permission collision, so native lensing remains open.
