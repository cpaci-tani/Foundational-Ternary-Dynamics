# V3 triplet motion-source convoy Phi-v8 candidate and protected-response boundary v1

**Date:** 2026-08-24  
**Status:** **[SELECTION — PREPARED PHI-v8 FINITE-CORRIDOR CONVOY]** +
**[THEOREM, CONDITIONAL — EXACT ONE-TIME MOTION-SOURCE STARTUP]** +
**[THEOREM — EXACT RELATIONAL WORK EXPORT/RESET]** +
**[THEOREM, CONDITIONAL — ARBITRARY FINITE-HORIZON ZERO-DEBIT CONVOY]** +
**[THEOREM — EXACT FACTORIZED REVERSE]** +
**[BOUNDARY — NATIVE ROUTER FORMATION, PERTURBATIVE PROTECTION, INERTIA,
RESPONSE POLES, FORCE, AND ABSOLUTE SCALE OPEN]**  
**Carrier price:** the Phi-v7 request/reserve/work startup, two existing
minimum motion-source packets, one neighboring existing A2 work owner, and
two prepared neutral router rails containing `4H` field records for declared
finite horizon `H`; no new primitive type  
**Production status:** unchanged  
**Ledger status:** no row minted  
**Startup parent:**
[`THEOREM_V3_TRIPLET_MOTION_PACKET_WORK_RECIPROCAL_PHI_v7_CANDIDATE_AND_RELEASE_BOUNDARY_v1.md`](THEOREM_V3_TRIPLET_MOTION_PACKET_WORK_RECIPROCAL_PHI_v7_CANDIDATE_AND_RELEASE_BOUNDARY_v1.md)  
**Bundle-transport parent:**
[`THEOREM_V3_NEUTRAL_SCALAR_VECTOR_STF_BUNDLE_AND_COMMON_GREEN_SEAM_v1.md`](../gravity_cosmology/THEOREM_V3_NEUTRAL_SCALAR_VECTOR_STF_BUNDLE_AND_COMMON_GREEN_SEAM_v1.md)  
**Exact certificate:**
[`proof_v3_triplet_motion_source_convoy_phi_v8_candidate.py`](../../../../../scripts/proofs/proof_v3_triplet_motion_source_convoy_phi_v8_candidate.py)

---

## 1. Formation is not inertial transport

Phi-v7 establishes one reciprocal production event:

\[
24A2\longrightarrow24F,                              \tag{1}
\]

while a prepared triplet moves one SC hop, a physical request turns dark,
the body work port becomes EXCITED, and the theorem-minimum two-packet motion
source appears. Repeating equation (1) on every clean hop would assign a new
formation cost to unchanged inertial motion.

That is neither required by the carrier nor physically well motivated.
Uniform motion should conserve a moving state; work belongs to formation,
acceleration, repair, or interaction. The question here is therefore:

> Can the already formed triplet and motion-source packet kit persist and
> co-translate under exact finite local permutations without another A2 debit?

The answer is yes on a declared prepared finite corridor.

---

## 2. Future-parallel startup lanes

For oriented chart `C` with carried axes `(e_1,e_2,n_C)`, replace the
first-fit Phi-v7 packet placement by the two chart-native lanes

\[
\begin{aligned}
L_+&:(x_C+e_1)\longrightarrow(x_C+n_C+e_1),\\
L_-&:(x_C-e_1)\longrightarrow(x_C+n_C-e_1).
\end{aligned}                                         \tag{2}
\]

The four endpoints are distinct. Every endpoint is in the prior center's
closed Moore neighborhood, both lane directions are `n_C`, and the two
forward continuations have distinct transverse coordinates. Equation (2) is
homogeneous and signed-cubic covariant because it uses only the carried chart.

The same two payloads used by Phi-v7 are placed on `L_+` and `L_-`. Their sum
is exactly

\[
P_+(C)+P_-(C)=2{\cal M}(n_C),                         \tag{3}
\]

the selected finite-chord motion signature. The startup transaction retains
the exact Phi-v7 role delta

\[
\boxed{
(\Delta N_F,\Delta N_{A1,SC},\Delta N_{A1,FCC},\Delta N_{A2})
=(24,0,0,-24).}                                      \tag{4}
\]

The certificate verifies startup and its exact inverse for all 18,432
oriented chart/clean-clock states.

---

## 3. Work export without reversing the body

Let `w_B` be the body's A2 work token and `w_E` one neighboring existing A2
environment token. Immediately after startup,

\[
(w_B,w_E)=(\mathrm{EXCITED},\mathrm{READY}).
\]

The selected local swap is

\[
\boxed{
(\mathrm{EXCITED},\mathrm{READY})
\longleftrightarrow
(\mathrm{READY},\mathrm{EXCITED}).}                  \tag{5}
\]

Equation (5) preserves both A2 occupancies, exports the physical consequence,
and resets the body without erasing the excitation. The body then takes its
next clean Phi-v6 hop. The relative work ledger is exactly

\[
1+0=0+1.                                              \tag{6}
\]

This closes the causal fate of the startup EXCITED state on the prepared
transaction. It does not form the neighboring READY owner or assign an
absolute energy to the distinction.

---

## 4. Neutral router corridor

Each existing joint bundle packet occupies twelve field records:

- ten records in the marked scalar/vector/STF packet; and
- two records in an unmarked neutral router.

Let `B_k` denote a marked packet at rail position `k` and `U_{k+1}` the
two-record router at the next site. The registered bundle law gives the exact
local permutation

\[
\boxed{
(B_k,U_{k+1})\longleftrightarrow(U_{k+1},B_{k+1}).}   \tag{7}

\]

The packet's rotor and both payload controllers take one internal tick in
equation (7). The observed C3 layer changes oppositely, so the complete
scalar/vector/STF payload is invariant:

\[
{\cal P}_{k+1}(\ell-1)={\cal P}_k(\ell).             \tag{8}
\]

For declared horizon `H`, preload `H` two-record routers on each of the two
future-parallel rails. The corridor price is therefore

\[
\boxed{N_{F,\mathrm{corridor}}=2\times H\times2=4H.} \tag{9}
\]

Those records are not consumed. The marked packet swaps forward and leaves a
neutral router behind. Thus the complete field occupancy is the same before
and after every convoy hop.

---

## 5. Co-moving source dressing

After `k` convoy ticks the body center and marked packet sites are

\[
\begin{aligned}
x_B(k)&=x_C+(k+1)n_C,\\
x_+(k)&=x_B(k)+e_1,\\
x_-(k)&=x_B(k)-e_1.
\end{aligned}                                         \tag{10}
\]

The two packets therefore retain fixed chart-relative positions around the
moving triplet. Their summed readout remains equation (3) on every tick.

Including the dark startup request, six moving pending-register records, two
packet/residue kits, and the prepared future routers, the field occupancy is

\[
N_F=32+4H                                             \tag{11}
\]

at every convoy time. The triplet retains three SC A1 arms, no FCC A1 owner,
one body A2 work owner, and one environment A2 work owner. Consequently each
post-startup hop has

\[
\boxed{
(\Delta N_F,\Delta N_{A1,SC},\Delta N_{A1,FCC},\Delta N_{A2})
=(0,0,0,0).}                                         \tag{12}
\]

Equation (12) is the crucial correction to the Phi-v7 release boundary:

```text
source-packet formation: one-time 24-A2 debit
uniform convoy transport: zero per-hop A2 debit
```

---

## 6. Arbitrary finite horizon and exact reverse

The certificate explicitly checks one complete C4 section for every one of
the 18,432 chart/phase states. More importantly, equation (7) is independent
of tick and corridor length. Induction therefore extends the construction to
every declared finite `H` for which the router records are present.

No completed infinite rail is invoked. At each stage FTD asserts only a
finite realized corridor and its next local continuation.

Every factor has an exact inverse:

1. each packet swap is inverted by the registered bundle inverse;
2. every clean body hop has the Phi-v6 inverse;
3. the first post-startup hop reverses equation (5);
4. the startup inverse absorbs the exact packet kit and restores all 24 A2
   reserve occupancies and the active request.

Hence the complete prepared corridor history is injective and exactly
reversible. The genuine expiry present elsewhere in canonical v3 does not
occur in this selected clean convoy sector.

---

## 7. What this says about inertia

The result changes the formulation of the inertia problem.

Positive work **per unchanged hop** is not required and would be the wrong
target for inertial motion. What remains open is a relational action that:

1. distinguishes rest dressing from moving dressing;
2. assigns conserved curvature to changes of the motion chord;
3. makes acceleration require reciprocal work/momentum transfer;
4. produces a dispersion relation for the complete dressed object; and
5. fixes the common coefficient multiplying that curvature.

Phi-v8 supplies a persistent moving state on prepared support. It does not
derive inertial mass or resistance to acceleration.

---

## 8. Protected-gravity boundary

Equation (10) keeps the selected material source coordinate attached to a
moving triplet. It still does not establish a protected gravitational field.
A protected-response successor must prove, from the same common law, that:

1. surrounding scalar/vector/STF histories form autonomous slow modes rather
   than merely prepared router traffic;
2. the source changes those modes reciprocally and receives recoil;
3. the response survives allowed perturbations and overlapping traffic;
4. the blocked Hessian has the required pole with a fixed residue;
5. matter and radiation couple to the same response; and
6. the low-energy cone, clock response, lensing, and nonlinear closure follow.

The present exact convoy is a source-persistence prerequisite for those
questions, not their answer.

The
[`finite A2-memory successor`](../gravity_cosmology/THEOREM_V3_ROTOR_GREEN_A2_PHYSICAL_MEMORY_AND_PHASE_PROTECTION_BOUNDARY_v1.md)
now closes part of item 3 for the separate scalar rotor apparatus: arbitrary
initial rotor phase changes the recorded current by at most an exact
`O(1/N)` bound. It also writes the current and source count into present
fixed-occupancy A2 states. It does not protect this Phi-v8 convoy against
packet loss, malformed routers, collisions, overlapping traffic, or
backreaction, and the occupancy action is exactly blind to the phase-only
memory write. The protected reciprocal force/pole target therefore remains
open.

---

## 9. Exact boundary after Phi-v8

```text
future-parallel minimum packet startup:            exact prepared candidate
one-time startup role delta (24,0,0,-24):          exact
work excitation export/reset without reverse hop: exact prepared swap
finite-H co-moving source dressing:                exact by local induction
per-convoy-hop role delta (0,0,0,0):               exact
per-convoy-hop A2 debit:                            zero
factorized complete reverse:                       exact
spatial homogeneity and signed-cubic covariance:   exact
native router/corridor formation or recycling:     open
packet-loss and environmental perturbation repair: open
multi-object traffic/collision arbitration:        open
rest/motion action curvature and inertial mass:    open
protected reciprocal gravity pole/force:           open
absolute coupling, common cone, lensing:            open
canonical state-complete Phi integration:           open
```

---

## Certificate

Run:

```bash
python scripts/proofs/proof_v3_triplet_motion_source_convoy_phi_v8_candidate.py
```

Expected: **14/14 checks pass**.
