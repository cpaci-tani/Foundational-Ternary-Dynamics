# V3 A2 Green-pulse reciprocal-impulse action Phi-v9 candidate and force boundary v1

**Date:** 2026-08-24  
**Status:** **[SELECTION — PREPARED PHI-v9 RESPONSE APPARATUS]** +
**[THEOREM, CONDITIONAL — EXACT FINITE PULSE-DENSITY RESPONSE]** +
**[THEOREM, CONDITIONAL — EQUAL-AND-OPPOSITE MOMENTUM RECORDS]** +
**[THEOREM, CONDITIONAL — QUADRATIC CLOCK/WORK ACTION LEDGER]** +
**[THEOREM, CONDITIONAL — INHERITED INITIAL-PHASE PROTECTION]** +
**[BOUNDARY — MOMENTUM RECORD IS NOT YET BODY ACCELERATION]** +
**[OPEN — CANONICAL PHI, NATIVE RESOURCES, REACTION RETURN, ABSOLUTE SCALE,
TENSOR POLE, COMMON CONE, LENSING, AND NONLINEAR GRAVITY]**  
**Local carrier price:** seven existing fixed-occupancy A2 owners at one
prepared probe: current, source count, pulse phase, probe momentum, reaction
momentum, and two clock/work digits; no new primitive type  
**Production status:** unchanged  
**Ledger status:** no row minted  
**Physical-memory parent:**
[`THEOREM_V3_ROTOR_GREEN_A2_PHYSICAL_MEMORY_AND_PHASE_PROTECTION_BOUNDARY_v1.md`](../gravity_cosmology/THEOREM_V3_ROTOR_GREEN_A2_PHYSICAL_MEMORY_AND_PHASE_PROTECTION_BOUNDARY_v1.md)  
**Motion-source parent:**
[`THEOREM_V3_TRIPLET_MOTION_SOURCE_CONVOY_PHI_v8_CANDIDATE_AND_PROTECTED_RESPONSE_BOUNDARY_v1.md`](THEOREM_V3_TRIPLET_MOTION_SOURCE_CONVOY_PHI_v8_CANDIDATE_AND_PROTECTED_RESPONSE_BOUNDARY_v1.md)  
**Exact certificate:**
[`proof_v3_a2_green_pulse_reciprocal_impulse_action_phi_v9_candidate.py`](../../../../../scripts/proofs/proof_v3_a2_green_pulse_reciprocal_impulse_action_phi_v9_candidate.py)

---

## 1. Question and scope

The A2-memory theorem closes physical retention. At one measured edge it
provides present finite records

\[
 C=m_N(e_+)-m_N(e_-),\qquad N=N_{\rm source},
 \qquad J_N={C\over N}.                                \tag{1}
\]

The triplet scalar-rest source contributes the exact factor `1/12`. The
desired local response coordinate is therefore

\[
 a_N=-{C\over12N}.                                    \tag{2}
\]

Equation (2) must not be implemented by silently granting one voxel exact
rational arithmetic or a real-valued force register. The finite question is:

> Can existing A2 phase states convert the two integer records `(C,N)` into
> a target-blind deterministic impulse stream whose exact time average is
> equation (2), while retaining the reaction and work consequences?

The answer is yes on a prepared finite response component. The construction
is a **Phi-v9 candidate**, not an amendment to canonical `Phi`.

---

## 2. Division emerges as pulse density

Take the response period

\[
 D=12N.                                                \tag{3}
\]

For the one-A2 phase register used here, the admitted range is

\[
 1\le N\le170,
 \qquad |C|\le N,                                     \tag{4}
\]

so `D-1<=2039<2047`. Let `r_t` be the phase accumulator. At each tick,

\[
\begin{aligned}
 u_t&=r_t+|C|,\\
 b_t&=\mathbf1[u_t\ge D],\\
 r_{t+1}&=u_t-Db_t,\\
 \iota_t&=-\operatorname{sgn}(C)b_t.
\end{aligned}                                         \tag{5}
\]

This is addition by `|C|` on the finite cycle `Z_D`. Its inverse is
subtraction by `|C|`. Over exactly one complete `D`-tick interval,

\[
 r_D=r_0,
 \qquad
 \sum_{t=0}^{D-1}b_t=|C|,
 \qquad
 \boxed{{1\over D}\sum_{t=0}^{D-1}\iota_t
 =-{C\over12N}.}                                     \tag{6}
\]

No division occurs in the transition rule. The rational response is the
block average of a finite deterministic sequence of ternary-compatible
events. The certificate exhausts every `-N<=C<=N` for `1<=N<=64`; equation
(6) is the general modular-arithmetic identity.

The one-quantum pulse is a selection of the smallest nonzero integral event.
Other pulse amplitudes are not proved impossible, so no absolute physical
normalization is claimed.

---

## 3. Reciprocal momentum records

At a pulse, update two signed finite A2 registers by

\[
 p_P'=p_P+\iota_t,
 \qquad
 p_R'=p_R-\iota_t.                                    \tag{7}
\]

Here `P` denotes the probe record and `R` the retained reaction record. Thus

\[
 \boxed{p_P'+p_R'=p_P+p_R}                            \tag{8}
\]

on every tick. Starting from zero momentum, a complete response period gives

\[
 \boxed{(p_P,p_R)=(-C,+C).}                           \tag{9}
\]

The response-memory values `(C,N)` are controls and are not consumed. The
inverse tick reconstructs whether the prior modular addition carried, removes
the corresponding impulse, and restores both momenta.

Equation (9) closes reciprocal **momentum bookkeeping**. It does not yet move
the triplet's center or change its Phi-v6 motion chord. The reaction record
also has not been transported back to the material source. Those are physical
composition gates, not semantic details.

---

## 4. Minimal quadratic phase-action candidate

The established relative occupancy action is blind to phase-only A2 changes.
Phi-v9 therefore selects the lowest-degree positive sign-even momentum
polynomial,

\[
 K=p_P^2+p_R^2.                                       \tag{10}
\]

Let `W` be a signed two-A2 clock/work record. On each pulse set

\[
 W'=W-[K(p_P',p_R')-K(p_P,p_R)].                      \tag{11}
\]

Then

\[
 \boxed{K+W=\text{constant}}                         \tag{12}
\]

tick by tick. A horizon-only prepared reserve

\[
 W_0=2N^2                                             \tag{13}
\]

does not depend on the measured current `C`. After one response period,

\[
 \boxed{W_D=2(N^2-C^2)\ge0.}                         \tag{14}
\]

For the certified `N<=64` census, the largest ledger value is 8,192. Two
fixed-occupancy A2 owners encode the signed range
`-8,388,607..+8,388,607` plus explicit overflow, so the complete work history
fits without a real register.

Equations (7) and (11) have one exact retained-history inverse. Malformed
phase states and controls outside equation (4) fail closed.

The dimensionless action may be multiplied by any positive common `Gamma`:

\[
 S_{\rm cand}=\Gamma(K+W).                            \tag{15}
\]

No finite transition above selects `Gamma`. Moreover, equation (10) is a
minimum-degree symmetry selection, not a derivation from canonical `Phi`.
Thus equations (10)--(15) close a relative action ledger but not the absolute
gravitational coupling.

---

## 5. Inherited response protection

For exact finite-domain Green gradient `g_e=q_e^TG_D`, the physical-memory
parent proves

\[
 \left|{C\over N}-g_e\right|
 \le B_e(N),
 \qquad
 B_e(N)={8\over3N}+{8K_e\over N}.                     \tag{16}
\]

Combining equations (6) and (16) gives the mechanical-record bound

\[
 \boxed{
 \left|\bar\iota+{g_e\over12}\right|
 \le {B_e(N)\over12}.}                               \tag{17}
\]

For two arbitrary initial rotor phases,

\[
 \boxed{
 |\bar\iota^{(p)}-\bar\iota^{(p')}|
 \le {2B_e(N)\over12}.}                              \tag{18}
\]

The certificate verifies equations (17)--(18) for all 192 native uniform
rotor phases on the canonical radius-one source edge at `N=37`. Every run
ends with the exact reaction pair equation (9), and the largest response
error is `5/2664` in the exact rational arithmetic of that fixture.

This is still only initial-phase protection. Packet loss, malformed routers,
competing walkers, owner contention, overflow, and material backreaction are
outside the theorem.

---

## 6. Existing-carrier price and the occupancy boundary

One local prepared probe uses seven A2 owners:

1. physical edge current `C`;
2. source count `N`;
3. pulse phase `r`;
4. probe momentum `p_P`;
5. reaction momentum `p_R`;
6. low clock/work digit; and
7. high clock/work digit.

Every A2 contains four occupied A9 symbols before, during, and after the
response. Therefore

\[
 \boxed{\Delta N_{A2}=0}                              \tag{19}
\]

for the complete cycle, and the common relative occupancy invariant assigns
zero delta to it.

This produces a clean hierarchy:

```text
physical Green-current memory:          theorem, conditional apparatus
finite rational-to-pulse conversion:    theorem, conditional apparatus
equal-and-opposite momentum records:    theorem, conditional apparatus
quadratic clock/work conservation:      theorem under selected phase action
actual triplet acceleration:            open
reaction returned to source matter:     open
absolute physical action multiplier:    open
```

The new phase action is exactly the additional structure demanded by the
A2-memory boundary. It has been made explicit and auditable rather than
smuggled into an occupancy count.

---

## 7. What remains before this is gravity

The exact next gates are:

1. derive or reject the Phi-v9 phase action from the homogeneous canonical
   collision/clock law;
2. form and place the seven-owner apparatus from native resources;
3. compose `p_P` with the Phi-v6 material chord so the momentum record causes
   actual acceleration and inertial response;
4. transport `p_R` causally back to the source or its emitted field dressing;
5. compose all incident edge responses as a cubic vector without owner or
   traffic conflict;
6. protect the response under packet loss, collisions, traffic, and overflow;
7. derive the absolute multiplier and physical units from the common action;
8. recover the scalar constraint and tensor radiative sectors from that same
   action, with one cone and universal matter/radiation coupling; and
9. only then test clock response, lensing, Shapiro delay, and nonlinear
   completion.

Phi-v9 closes the finite reciprocal-impulse **ledger**. It does not yet close
a force law.

The
[`Phi-v10 mechanical-drift successor`](THEOREM_V3_GREEN_PULSE_TRIPLET_MECHANICAL_DRIFT_PHI_v10_CANDIDATE_AND_INERTIA_BOUNDARY_v1.md)
now composes each carry with the actual clean Phi-v6 body hop. It gives exact
prepared displacement `-C*d`, average drift `-C*d/(12N)`, and a complete
combined inverse (10/10). This closes the position-history consequence but
not the acceleration gate: retained momentum does not continue motion in a
dark response window.

The
[`Phi-v11 clocked-inertia successor`](THEOREM_V3_TRIPLET_CLOCK_LEGENDRE_INERTIA_PHI_v11_CANDIDATE_AND_GRAVITY_BOUNDARY_v1.md)
then closes that continuation subcase under an explicit selection: the exact
16-tick triplet clock is the inertial denominator, giving `v=p/16`, relative
quadratic coefficient `1/32`, and `F_bar=16a_bar` (12/12). Canonical-Phi
provenance, physical units, steering, reaction return, and universal gravity
remain open.

---

## 8. Reproduction

From the repository root:

```bash
python scripts/proofs/proof_v3_a2_green_pulse_reciprocal_impulse_action_phi_v9_candidate.py
```

Expected result: `13/13` exact checks pass. The certificate reports pulse
period `12N`, exactly `|C|` pulses, average probe impulse `-C/(12N)`, final
momentum records `(-C,+C)`, work evolution
`2N^2 -> 2(N^2-C^2)`, seven local A2 owners, free absolute multiplier, and
actual body acceleration still open.
