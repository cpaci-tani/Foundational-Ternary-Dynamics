# C18 actualization shared-moment source vertex v1

**Date:** 2026-08-23
**Status:** **[THEOREM — EXACT REVERSIBLE MANIFESTATION/VECTOR/TENSOR/CAPACITY SOURCE MAP]** +
**[THEOREM — C4 COVARIANCE AND TARGET-FREE MOMENT-NORM LEDGER]** +
**[BOUNDARY — KINEMATIC VERTEX, NOT YET A DYNAMICAL COUPLING OR PHYSICAL CONSTANT]** +
**[OPEN — AUTONOMOUS COMPATIBILITY, BLOCKED ACTION, STABLE MATTER/CLOCKS, LENSING, BORN, ALPHA]**
**Production status:** unchanged
**Ledger status:** no row minted

**Exact certificate:**
[proof_c18_actualization_moment_source_vertex.py](../../../../../scripts/proofs/proof_c18_actualization_moment_source_vertex.py)
checks all nine antipodal C18 lines, four C4 phases, and two orientations. It
verifies the actualization inverse, ternary charge, token payload, every moment
increment, all norm relations, fail-closed incompatible events, and global C4
covariance in 1,890 exact symbolic checks.

---

## 1. One owned token, not six separate sources

The exact
[controlled-actualization macro](../quantum_foundations/THEOREM_C4_CONTROLLED_ACTUALIZATION_AND_CONTINUOUS_WORK_BOUNDARY_v1.md)
moves one complete token

\[
 \tau=(k,\epsilon),\qquad
 k\in\mathbb Z_4,\quad\epsilon\in\{-1,+1\},       \tag{1}
\]

between reserve ownership and bond ownership:

\[
 (0,0;\text{reserve }\tau)
 \longleftrightarrow
 (\epsilon,-\epsilon;\text{link }\tau).             \tag{2}
\]

Previously equation (2) was certified only as a finite manifestation and
token-energy transaction. The C18 block maps now allow the **same ownership
change** to be read simultaneously in every candidate field channel. No
independent electromagnetic, gravity, clock, or matter source is appended.

The later
[ternary-square carrier theorem](THEOREM_TERNARY_SQUARE_PHASE_POLARITY_CARRIER_AND_AUTONOMOUS_CROSSING_CLOCK_v1.md)
shows that the complete abstract payload \((k,\epsilon)\), together with its
blank, fits exactly in the full nine states of two ternary slots. Thus equation
(2) needs no third orientation register. Charge-conjugation symmetry also
shows why \(\epsilon\) must be carried into the event rather than generated
from a symmetric blank.

---

## 2. Line geometry and phase coordinates

Let $d$ be one of the nine normalized antipodal C18 line representatives and

\[
 M=dd^T.                                            \tag{3}
\]

Write the C4 token phase as

\[
 i^k=u+iv,\qquad
 (u,v)\in\{(1,0),(0,1),(-1,0),(0,-1)\}.             \tag{4}
\]

Orientation $\epsilon$ chooses which directed member of the antipodal pair is
occupied. The bond was blank before actualization, so its occupied token has
zero blank-capacity coordinate and debits one half of the line-averaged
capacity.

---

## 3. Exact shared source increment

Use the relative vector doublet $(R_u,R_v)$, the common tensor doublet $(Q,P)$
from the
[common-phase tensor theorem](../gravity_cosmology/THEOREM_C18_COMMON_PHASE_TENSOR_DOUBLET_AND_CONSTRAINT_PRICE_v1.md),
and the line-averaged capacity tensor $K$. One compatible application of
equation (2) gives exactly

\[
 \boxed{
 \begin{aligned}
 \Delta R_u&={\epsilon u\over9}d,
 &\Delta R_v&={\epsilon v\over9}d,\\
 \Delta Q&={u\over18}M,
 &\Delta P&={v\over18}M,\\
 \Delta K&=-{1\over18}M,
 &(\Delta s_L,\Delta s_R)&=(\epsilon,-\epsilon).
 \end{aligned}}                                     \tag{5}
\]

The inverse unmanifestation negates every entry in equation (5), restores the
token to reserve ownership, and returns both ternary endpoints to zero. An
incompatible event leaves the complete state and every moment fixed.

Equation (5) is one finite source vertex. Manifestation, relative-vector
source, common-tensor source, and capacity debit are different block readings
of the same token transaction.

---

## 4. Exact normalization ledger

Every normalized line dyad obeys

\[
 \|d\|^2=1,\qquad \|dd^T\|_F^2=1,                 \tag{6}
\]

and every occupied C4 phase obeys $u^2+v^2=1$. Therefore equation (5) gives

\[
 \boxed{
 \|\Delta Q\|_F^2+\|\Delta P\|_F^2
 =\|\Delta K\|_F^2={1\over324},}                   \tag{7}
\]

\[
 \boxed{
 \|\Delta R_u\|^2+\|\Delta R_v\|^2
 ={1\over81}=4\|\Delta K\|_F^2.}                  \tag{8}
\]

No continuum coupling, experimental value, or master root enters equations
(7)--(8). They are forced by the registered moment normalizations and one-token
ownership.

They are **not** measurements of $alpha$ or $G_N$. Physical couplings require
canonically normalized propagating fields and an operational source-response
observable. Rescaling a blocked field changes the numerical ratios in
equations (7)--(8), so treating them as physical constants would be a
normalization substitution.

For reference, contracting the combined relative-vector increment with the
bare Gaussian Hessian $135/8$ gives the exact chart cost

\[
 {135\over8}
 \left(\|\Delta R_u\|^2+\|\Delta R_v\|^2\right)
 ={5\over24}.                                       \tag{9}
\]

This is a finite-alphabet insertion cost in the registered bare block chart,
not a force coupling. The selected two-record collision relaxes the
phase-weighted vector currents and supplies no Maxwell propagator, so equation
(9) cannot be interpreted as $alpha$.

---

## 5. C4 covariance

One global phase advance sends

\[
 (u,v)\longmapsto(-v,u).                            \tag{10}
\]

The complete vertex therefore transforms as

\[
 (\Delta R_u,\Delta R_v)
 \longmapsto(-\Delta R_v,\Delta R_u),               \tag{11}
\]

\[
 (\Delta Q,\Delta P)
 \longmapsto(-\Delta P,\Delta Q),                  \tag{12}
\]

while $\Delta K$ and the ternary endpoint charges remain unchanged. Thus one
internal C4 action rotates both vector and tensor quadrature pairs and leaves
the common capacity debit invariant.

---

## 6. What this establishes for the one-action program

Equation (5) is the first exact common vertex joining four of the requested
phenomena:

\[
 \text{one token ownership change}
 \longrightarrow
 \begin{cases}
 \text{ternary manifestation},\\
 \text{oriented relative-vector source},\\
 \text{common tensor-doublet source},\\
 \text{capacity/work debit}.
 \end{cases}                                        \tag{13}
\]

This is stronger than placing independent terms in one Lagrangian. The four
outputs cannot be varied independently at the microscopic vertex; they share
one payload, orientation, inverse, and token ledger.

The vertex is also universal at the kinematic level: every token manifested by
the same macro produces equation (5). Whether stable composite matter sources
the long-distance tensor response universally still depends on dynamics and
cannot be inferred from one bond event.

The
[phase-neutral charge/stress successor](THEOREM_C18_PHASE_NEUTRAL_SHARED_CHARGE_STRESS_VERTEX_v1.md)
now contracts both source doublets with the same token phase. It derives the
C4-invariant outputs
\[
 j_{\rm evt}={\epsilon\over9}d,\qquad
 t_{\rm evt}={1\over18}dd^{\mathsf T}=-\Delta K.
\]
The first is charge odd and the second charge even. This closes the
matter-mediated source type required by the phase-complete C4 selection
theorem; reciprocal work and field response remain open.

---

## 7. Remaining physical boundaries

### Matter and clocks

Equation (2) creates a neutral ternary endpoint pair, not a stable localized
body. Phase $k$ is retained payload, but no autonomous rule advances it around
a recurrent loop. Matter and material clocks require stable self-maintaining
orbits of the same transaction action.

### Electromagnetism and native alpha

Equation (5) gives a source increment for a relative vector doublet, but no
Maxwell pole, Gauss law, transverse radiation, or source-response force has
been derived. A physical charge normalization and native-alpha readout remain
open until those kinetic structures exist.

The subsequent
[FCC Gaussian-current collision theorem](../charge_gauss_native_em/THEOREM_C18_FCC_GAUSSIAN_CURRENT_COLLISION_AND_MAXWELL_MODE_PRICE_v1.md)
now supplies an exact protected zero-wavevector carrier for this source. On
the equal-length FCC shell, equation (5) is exactly one ninth of a normalized
Gaussian-current quantum. This closes the representation and normalization
match only. The reserve does not yet carry the compensating directed current
or field work, and the streaming-collision operator has no derived finite-$k$
Maxwell pole.

### Gravity and lensing

The tensor doublet receives a source increment, but its native bracket, four
constraints, two-mode derivative action, and universal composite source law
remain open. There is no lensing result.

### Contextual measurement and Born pushforward

The actualization macro is controlled by a compatibility bit. Equation (5)
specifies the exact physical change **if** that bit is armed, but the common
action has not generated the apparatus router, residual-record preparation,
detector work/reset, or general-amplitude basin frequencies. The physical Born
pushforward remains open.

---

## 8. Next locked gate

The next action candidate must make the compatibility condition endogenous.
It must be a single payload-complete local permutation that:

1. derives the event bit from local phase relation, occupancy, and apparatus
   records without reading a target probability;
2. composes scattering, dark-record transfer, equation (2), and capacity
   backpressure on one explicitly owned state space;
3. retains equation (5) and its inverse exactly;
4. generates a recurrent localized orbit whose C4 return is a material clock;
   and
5. yields a blocked kernel with a relative-vector pole and a constrained
   two-mode tensor pole.

Only after this gate may source-response measurements be used to define a
native electromagnetic coupling or a lensing observable.

The later
[dual-capacity cyclic-mixing theorem](THEOREM_DUAL_CAPACITY_CORRELATION_OBSTRUCTION_AND_CYCLIC_MIXING_RESPONSE_v1.md)
adds an exact resource boundary. The one-token transfer in equation (2)
cannot simultaneously occupy independent primal and dual A9 capacity slots.
Factorized variable lensing response therefore requires a second retained
token or an explicit time-sharing ledger, followed by deterministic dual-layer
mixing. Equation (5) remains one common source vertex; it does not pay that
additional ownership price by itself.

The first recurrent use of the vertex is now exact in the
[C4 actualization material-clock theorem](THEOREM_C4_RECURRENT_ACTUALIZATION_MATERIAL_CLOCK_AND_LOCAL_RATE_BOUNDARY_v1.md).
A persistent bright controller plus common C4 advance gives a four-admitted-
tick localized orbit. Its phase-vector and phase-tensor cycle sums vanish,
while its mean capacity remains $M/12$, a deficit $-M/36$ from the blank line.
This is a prepared proto-matter clock, not autonomous matter or derived mass.
