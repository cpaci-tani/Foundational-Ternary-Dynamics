# FTD-0600 — Constituent-complete charged-trimer transaction v1

**Status:** `[SELECTED DYNAMICS] + [THEOREM — CONDITIONAL EXACT
COMMON-ACTION IDENTITIES] + [MEASURED — REPEATED STATE-ONLY REVERSIBLE BOUND
MOTION] + [CLOSED NEGATIVE — ISOLATED MATCHED PSEUDOMOMENTUM RECOIL]`  
**Protocol:**
[`PREREG_CONSTITUENT_COMPLETE_CHARGED_TRIMER_TRANSACTION_v1.md`](../../preregistrations/constituent_complete_matter/PREREG_CONSTITUENT_COMPLETE_CHARGED_TRIMER_TRANSACTION_v1.md),
SHA-256
`F24CC0BFBF0741B0F1A07DCE3B719EA6452E3DC81BB0E9F76013F211D25F6328`  
**Verdict:** `CHARGED_TRIMER_COMMON_ACTION_CONSTRUCTIVE`  
**Scope:** observer-only selected dynamics; no production change.

## 1. Statement

There exists a finite, explicitly constituent-complete charged composite on
the registered matched face/edge field representation for which one atomic
implicit transaction simultaneously determines all constituent endpoints,
deposited face currents, electric and magnetic field updates, field impulses,
internal binding impulses, work, and energy exchange. The final persistent
state alone determines a numerically certified reverse solve; no forward
current, endpoint, impulse, root, or branch record is supplied to inversion.

Under the registered quartic binding selection, quadratic coupling coat,
matched field complex, production dispersion, finite-volume compensator, and
converged root, the transaction obeys the following identities to the locked
tolerance:

\[
\rho_1-\rho_0+\operatorname{div}K=0,
\qquad
\operatorname{div}E_n=\rho_n+\rho_{\rm comp},
\]

\[
\Delta\!\left(\sum_a H_a+V_b\right)
  =g\langle \bar E,K\rangle,
\qquad
\Delta U_{\rm field}=-g\langle \bar E,K\rangle,
\]

\[
\Delta\!\left(\sum_a H_a+V_b+U_{\rm field}\right)=0,
\qquad
\sum_a I_{b,a}=0,
\qquad
\sum_a \bar v_a\cdot I_{B,a}=0.
\]

This existence theorem does **not** establish isolated total momentum. The
registered field-pseudomomentum diagnostic has defect

\[
 R_P=5.69231257591424\times10^{-3},
\]

far above the `1e-12` claim threshold. The fixed opposite coat is an external
computational compensator, so the present campaign cannot distinguish
compensator recoil from a missing field/connection momentum channel.

## 2. Ontic content and selections

The persistent matter state is

\[
 \{(q_a,X_a,p_a)\}_{a=1}^{3}/S_2,
 \qquad q=(-1,-1,+1)
\]

or its charge conjugate, where the quotient exchanges the equal-polarity
constituents. Aggregate density and current are derived coupling observables;
they are not the lossless matter state. Each constituent remains ternary.

The following are selections or imposed candidate parameters, not physical
derivations:

- three constituents and the `(-1,-1,+1)` composition;
- the rest triangle with all squared pair lengths equal to two;
- the quartic pair potential
  \(V_b=(1/4)\sum_{a<b}(|X_a-X_b|^2-2)^2\);
- unit binding stiffness;
- the quadratic coat and matched face/edge field representation;
- the distant fixed compensating coat.

Accordingly, the result is not an electron derivation, a binding-mechanism
derivation, a native `U(1)` derivation, or an electromagnetic ontology.

## 3. Exact binding identities

For a pair, put

\[
d_n=X_{a,n}-X_{b,n},\quad u_n=|d_n|^2-2,
\quad G_{ab}=\frac14(u_0+u_1)(d_0+d_1).
\]

The endpoint impulses are `-h G_ab` and `+h G_ab`, so their sum vanishes
identically. Since

\[
u_1-u_0=(d_1-d_0)\cdot(d_1+d_0),
\]

the pair work is

\[
G_{ab}\cdot(d_1-d_0)
=\frac14(u_1^2-u_0^2)
=V_{ab,1}-V_{ab,0}.
\]

The independent rational certificate verifies both identities on 64 locked
fixtures with exact residual zero. These identities are conditional on the
selected potential; they do not select that potential from the FTD postulates.

## 4. Registered measurements

The C++ run of record reports:

| Quantity | Result |
|---|---:|
| forward one-step arms | 32/32 converged |
| state-only reverse arms | 32/32 converged |
| worst one-step gate | `1.982979752623848e-13` |
| worst one-step inverse | `4.8031023602845835e-14` |
| translation covariance | `4.928349395250109e-14` |
| cubic rotation covariance | `1.7763568394002505e-15` |
| equal-charge permutation covariance | `1.7763568394002505e-15` |
| repeated forward/reverse steps | 64/64 |
| legitimate site hops | 24 |
| repeated state recovery | `2.567723811353062e-12` |
| total-energy drift | `1.6653345369377348e-15` |
| pair-distance range | `1.4138139830139969..1.4145222843347827` |
| matched pseudomomentum defect | `5.6923125759142396e-3` |

The pair distances stay near `sqrt(2)` while the explicit constituents cross
site boundaries. This establishes repeated bound mobility for this selected
finite composite. It does not establish gapless free translation, a pole,
long-time stability, collision physics, radiation, or experimental adequacy.

## 5. State-only inversion

The reverse API accepts only the later electric field, later magnetic field,
later constituent positions and momenta, immutable charges, stationary
compensator, and the registered parameters. It solves nine earlier momentum
components and reconstructs earlier endpoints and fields. The successful
64-step reverse campaign therefore refutes the narrower claim that explicit
constituent phase space necessarily requires an accumulating history tape for
this reaction-free transaction.

It does not prove global uniqueness of every nonlinear root or reversibility
outside the registered basin.

## 6. Consequence

FTD-0599 closed the registered one-voxel R0 map at energy. FTD-0600 shows that
the same general face-current/common-action program becomes constructive when
matter is retained as a finite constituent-complete phase-space state with an
explicit selected binding law. Thus the one-voxel failure is not a no-go for
mobile matter.

FTD-0601 subsequently ran the closed neutral discriminator. Its
pseudomomentum defect remains finite, so the fixed compensator is not the sole
cause. FTD-0602/0603 then isolate a compact-carrier translation-phase defect;
the justified next escalation is deforming or spectrally extended matter
before attributing the entire discrepancy to missing connection data.
