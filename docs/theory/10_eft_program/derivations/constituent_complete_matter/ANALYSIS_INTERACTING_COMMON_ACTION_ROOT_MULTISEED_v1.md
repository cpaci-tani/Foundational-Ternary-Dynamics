# FTD-0720 — Interacting common-action root multiseed

**Status:** `[SELECTED DYNAMICS] + [MEASURED — ONE REGISTERED ROOT BASIN]`  
**Verdict:** `INTERACTING_COMMON_ACTION_ONE_BASIN_WITNESSED`  
**Production status:** unchanged  
**Protocol:** `PREREG_INTERACTING_COMMON_ACTION_ROOT_MULTISEED_v1.md`,
SHA-256 `DB516877F7762BECF9E61AB54861A616F84DCA7E6701907A8CE1F3D4BA21668C`

## 1. Result

For the smallest connected neutral FTD-0622 composite, the unchanged implicit
matter--field action converged to one common complete-state transaction from
all 39 preregistered momentum seeds. The tested states were integer-phase
rest, fractional parallel response, and fractional transverse response. Each
state used the incoming momentum plus twelve common/charge-odd seeds of
magnitude `0.2` across the three axes.

Every accepted solution passed the existing continuity, Gauss, force,
kinematic, work, energy, causality, locality, and state-only inversion gates.
The result was also covariant under swapping the complete constituent records,
charges, and bond incidence and then quotienting that bookkeeping permutation.

```text
registered/accepted roots       39/39
state-only inverse passes        39/39
relabeling arms                  3/3
maximum complete-state spread    3.3040315960325046e-14
maximum deposited-current spread 1.5741226410456027e-14
maximum inverse recovery         3.3039764686765986e-14
distinct accepted roots          0
```

## 2. Exact epistemic meaning

This is finite numerical evidence for one nonlinear attraction basin. It is
not a proof that the action has one root globally. A finite seed family cannot
exclude a disconnected root basin, a bifurcation at another field amplitude,
or non-uniqueness at collision.

The result nevertheless removes one concrete concern: in the registered
smooth interacting sector, the physical transaction is not an artifact of
the solver's inherited initial guess. Common and internal guesses on both
sides of the realized root recover the same state and current, and the inverse
returns to the same earlier complete state.

## 3. Reconciliation with FTD-0503 and FTD-0719

The three results now form a non-contradictory hierarchy:

1. **FTD-0719:** two unordered polarity snapshots determine current only up to
   a divergence-free cycle. Snapshot ontology alone is incomplete.
2. **FTD-0503:** associated position--momentum records select a unique exact
   current in the free distinct-endpoint sector.
3. **FTD-0720:** adding the selected interacting face/edge field and local
   graph yields one observed common-action root basin for the smallest neutral
   composite.

Thus the cycle-current ambiguity need not be stored as an additional primitive
when the complete action selects it. That route is constructive in the free
sector and now numerically supported in one interacting sector. It remains
unproved globally.

## 4. Matter-dynamics extrapolation

The strongest current candidate is:

> Matter is a recurrent, localized common-action transaction on a signed
> event network. Position, momentum, relational connectivity, and field state
> jointly select the next oriented current and manifestation. The current is
> the realized edge of the history, not an independently chosen force input.

This implies several precise research hypotheses.

1. **Inertia `[CONJECTURE]`** is the resistance of the entire coupled root to
   a change of translation phase. Its measurable object is the tangent map of
   the common transaction, not the rest energy of one voxel.
2. **Binding `[SELECTED / OPEN NATIVE ORIGIN]`** is presently carried by a
   local graph action. A native theory must reconstruct, form, and reconnect
   that relation from event/field state or openly adopt relational edges as
   material state.
3. **Radiation `[CONJECTURE]`** is field content that cannot remain in the
   recurrent root basin and propagates into outgoing degrees of freedom. It is
   not the rendered streamline geometry itself.
4. **Decay `[CONJECTURE]`** is loss of a recurrent root or transition to a
   different connectivity basin while the whole transaction remains locally
   accounted.
5. **Collision `[OPEN — NEXT SHARP GATE]`** is not merely two persistent
   objects occupying the same site. It is a local change in the event-network
   continuation: pass-through, reflection, fusion, annihilation, or
   reconnection must be selected by one action and conservation package.

## 5. Remaining ontological debt

- The quartic Moore bond graph and its unit stiffness are selected, not
  derived from the five postulates.
- The registered width-one system has distinct endpoints and no reaction.
- No global contraction, convexity, or topological degree theorem has been
  proved for the nonlinear action.
- Same-sign multiplicity, opposite-sign cancellation at one site, bond
  rupture/reconnection, genesis, and evaporation have no common collision
  transaction.
- No physical mass, charge group, spin, statistics, pole, or scattering law
  follows from this root-basin result.

## 6. Next discriminator

The next campaign must isolate a coincident-target event with its full incoming
phase-space and field data. It must enumerate a finite preregistered set of
local topology-changing continuations and evaluate all of them with the same
action and invariants. Silently deduplicating site state, preserving hidden
particle labels, or selecting a channel after inspecting its physical match is
inadmissible.

The minimal acceptable output is either:

- one action-selected collision channel with exact current, energy, Gauss,
  locality, and state-only inversion; or
- a proof that the current state types cannot represent the collision without
  a branch/history/connection or explicit constituent phase-space extension.

## 7. Reproducibility

- protocol SHA-256: `DB516877…21668C`;
- runner SHA-256: `4B97A731…B514E`;
- header SHA-256: `487F358E…48080`;
- implementation SHA-256: `6A430812…31A88`;
- JSON SHA-256: `AB54FD70…248DA6`;
- CSV SHA-256: `12D3274A…0F2165`;
- independent certificate SHA-256: `C728B59F…9E4A2`, `30/30 PASS`;
- toolchain: pinned MSVC `14.44.35207`, Release, CPU observer.
