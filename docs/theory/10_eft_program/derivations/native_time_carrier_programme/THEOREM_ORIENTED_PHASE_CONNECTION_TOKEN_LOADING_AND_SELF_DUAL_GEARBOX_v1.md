# FTD-0962/0963 — Oriented phase-connection token loading and self-dual gearbox v1

**Date:** 2026-08-11  
**Status:** `[SELECTION — POSITIVE COMPLETE-SQUARE PHASE CONNECTION AND EXISTING INTERNAL MODE SPECIALIZATION]` +
`[THEOREM — EXACT RECIPROCAL MECHANICAL/CANONICAL MOMENTUM SPLIT]` +
`[THEOREM — SPEED-INDEPENDENT ORIENTED QUARTER-TURN HOLONOMY]` +
`[THEOREM, CONDITIONAL — TOKEN LOADING/REVERSIBLE CONTROLLER PHASE REPLACEMENT]` +
`[THEOREM — EXACT SELF-DUAL REVERSE RECOVERY/TIME REVERSAL]` +
`[THEOREM, CONDITIONAL — G* CADENCE VS CONNECTION HOLONOMY SEPARATION]` +
`[OPEN — PRODUCTION CONNECTION/MODE FORMATION/REPLENISHMENT/ONE-WAY LOSS]`  
**Verdict:** `OUTCOME_B_EXACT_SELECTED_CONNECTION_GEARBOX_NATIVE_REALIZATION_OPEN`

## 1. Result

There is an exact positive Hamiltonian mechanism by which a local clock
crossing can perform two jobs at once:

1. move a preloaded positive-energy token into an outgoing oriented record;
2. replace an arbitrary controller phase by a prepared aligned phase while
   exporting the controller's complete old state.

It needs no discontinuous `sign(Pi)` readout. Clockwise versus
counterclockwise is the orientation of a phase-space path, and the transaction
is the holonomy of a connection along that path.

Let `(delta,Pi)` be the local clock pair. Introduce four complete canonical
modes already priced by the phase-reference, actuator, and history-rail
architecture:

- token battery `B`;
- outgoing record `D`;
- target controller `C`; and
- aligned reserve/return reference `R`.

The selected Hamiltonian is

\[
 \boxed{
 H_{\rm conn}={\bigl[Pi+{\cal A}(delta)G\bigr]^2\over2M}
 +V(delta)+nu_TA_T+nu_CA_C,}                            \tag{1}
\]

where `M,nu_T,nu_C>0`, `V>=0`,

\[
 \begin{aligned}
 G_T&=b_qd_p-d_qb_p,\\
 G_C&=r_qc_p-c_qr_p,\\
 G&=G_T+G_C,                                           \tag{2}
 \end{aligned}
\]

and

\[
 A_T={|B|^2+|D|^2\over2},
 \qquad
 A_C={|C|^2+|R|^2\over2}.                              \tag{3}
\]

For a connection chart normalized by

\[
 \int_{delta_-}^{delta_+}{\cal A}(delta)d delta={pi\over2}, \tag{4}
\]

a positive traversal gives a forward quarter-turn and a negative traversal
gives its exact inverse.

This is a reference law, not a derivation from P1--P5, production C18, CM
arithmetic, or the actual ternary field. Its importance is that it identifies
the minimum dynamical form that closes orientation, reciprocal reaction,
energy, controller alignment, and reverse recovery together.

## 2. Exact exchange algebra

The generators act on the token pair as

\[
 \begin{aligned}
 D(alpha)&=D_0\cos alpha+B_0\sin alpha,\\
 B(alpha)&=B_0\cos alpha-D_0\sin alpha,                \tag{5}
 \end{aligned}
\]

and on the controller pair as

\[
 \begin{aligned}
 C(alpha)&=C_0\cos alpha+R_0\sin alpha,\\
 R(alpha)&=R_0\cos alpha-C_0\sin alpha.                \tag{6}
 \end{aligned}
\]

The two generators Poisson-commute. Moreover,

\[
 \{G,A_T\}=\{G,A_C\}=0.                               \tag{7}
\]

Thus the flow is symplectic, orthogonal, determinant one, and preserves both
positive action sums. Its inverse is the same flow with `alpha->-alpha`.

At `alpha=pi/2`,

\[
 (D,B,C,R)\mapsto(B,-D,R,-C).                          \tag{8}
\]

At `alpha=-pi/2`, equation (8) is inverted.

The common rotations generated by `nu_TA_T` and `nu_CA_C` commute with these
exchanges. Equations (5)--(8) are therefore exact in the co-rotating catalytic
phase frames already selected in FTD-0863.

## 3. Why the complete square matters

Define the mechanical phase momentum

\[
 \boxed{K=Pi+{\cal A}(delta)G.}                        \tag{9}
\]

Hamilton's equations give

\[
 \dot delta={K\over M},                                \tag{10}
\]

\[
 \dot Pi=-V'(delta)-{K\over M}{\cal A}'(delta)G,       \tag{11}
\]

and `dot G=0`. Therefore

\[
 \begin{aligned}
 \dot K
 &=\dot Pi+{\cal A}'(delta)\dot delta G\\
 &=-V'(delta).                                         \tag{12}
 \end{aligned}
\]

The mechanical clock follows its bare natural-well equation exactly even
when the controller load `G_C` is nonzero. Reciprocal reaction has not been
discarded. It is the canonical momentum shift

\[
 \boxed{Pi=K-{\cal A}(delta)G.}                        \tag{13}
\]

inside the gate. When the connection vanishes at both chart endpoints,
`Pi=K` there.

Expanding the kinetic term gives

\[
 {K^2\over2M}={Pi^2\over2M}
 +{Pi{\cal A}G\over M}
 +{{\cal A}^2G^2\over2M}.                              \tag{14}
\]

The last term is mandatory. Keeping only the linear cross term is not the same
positive reciprocal Hamiltonian and would reopen the energy/reaction debt.

## 4. Oriented holonomy

After factoring the commuting free mode rotations, the internal exchange
angle obeys

\[
 \dot alpha={\cal A}(delta){K\over M}
             ={\cal A}(delta)\dot delta.               \tag{15}
\]

Consequently

\[
 \boxed{Delta alpha=\int_{\rm path}{\cal A}(delta)d delta.} \tag{16}
\]

The speed cancels exactly. Equation (16) remains true for a loaded controller
because the same complete square that produces the load reaction also makes
`K/M=dot delta`.

One explicit selected `C1` chart is

\[
 {\cal A}_b(delta)=
 \begin{cases}
 {15pi\over32b}\left(1-{delta^2\over b^2}\right)^2,
     &|delta|\le b,\\
 0,&|delta|>b,
 \end{cases}                                           \tag{17}
\]

whose value and derivative vanish at `+/-b` and whose area is `pi/2`.

Therefore

\[
 delta_-\to delta_+:quad Delta alpha=+pi/2,            \tag{18}
\]

while

\[
 delta_+\to delta_-:quad Delta alpha=-pi/2.            \tag{19}
\]

This is the precise dynamical clockwise/counterclockwise discriminator. The
symmetric square can say that the chart was traversed; the oriented line
integral says which way.

## 5. Forward token loading and active phase replacement

Prepare at the entrance

\[
 B=(a,0),\qquad D=(0,0),\qquad a>0,                    \tag{20}
\]

and an aligned reserve

\[
 R=(r,0),\qquad r>0.                                   \tag{21}
\]

The target controller `C` may have arbitrary admitted phase and action. The
forward holonomy (18) gives

\[
 \boxed{B'=(0,0),\qquad D'=(a,0),}                     \tag{22}
\]

and

\[
 \boxed{C'=(r,0),\qquad R'=-C.}                        \tag{23}
\]

Equation (22) loads the `+1` oriented token without copying its energy. The
battery energy

\[
 epsilon_{\rm tok}=nu_Ta^2/2                           \tag{24}
\]

has moved into the output. Equation (23) puts the target controller at gate
zero relative to the catalytic phase frame. Its former phase and action are
not erased; they are retained completely in the return mode.

This is active phase replacement. It avoids the reset obstruction because a
symplectic diffeomorphism is not asked to map an open set of old phases into
one point while forgetting the preimage.

A fresh reverse-oriented traversal from the same ready preparation gives

\[
 D'=(-a,0),qquad C'=(-r,0).                            \tag{25}
\]

The second result is the inverse-oriented/antiphase gate, not the same forward
gate under a relabelling. The natural recursive interpretation is asymmetric
in role while symmetric in law:

- positive traversal performs the forward actualization/engagement stroke;
- negative traversal performs the inverse/unactualization stroke.

Which orientation is assigned the forward semantic role is a convention of
the selected chart; the Hamiltonian itself is time-reversal invariant.

## 6. Exact self-dual return

Begin with (20)--(21), execute the forward traversal, and then traverse the
same connection chart in reverse before exporting or modifying its endpoint
records. Equations (5)--(6) give

\[
 \Phi_{-pi/2}\Phi_{+pi/2}=1.                            \tag{26}
\]

Hence

\[
 (B,D,C,R)_{\rm final}=(B,D,C,R)_{\rm initial}.        \tag{27}
\]

Under canonical time reversal, all momenta change sign. Then `G->-G` and
`K->-K`, so equation (1) is invariant. The forward and inverse strokes are
not separately fitted rules; they are the two orientations of one law.

Four same-orientation crossings generate the identity on the internal modes,
while two generate global sign reversal. This `C4` recurrence follows from
the selected quarter-turn normalization. It is not a derivation of the
connection normalization from the cubic lattice or Gaussian-prime arithmetic.

## 7. Energy, reserve, and backpressure

Every term in (1) is nonnegative under the registered assumptions. The
Hamiltonian is autonomous and conserved. The exchange preserves

\[
 nu_TA_T+nu_CA_C.                                      \tag{28}
\]

Thus:

- token energy is transferred from `B` to `D`, not created or duplicated;
- controller state and energy are exchanged with `R`, not erased;
- the clock's mechanical motion is unchanged while its canonical momentum
  carries the exact reciprocal connection reaction; and
- the interaction vanishes at the connection-chart endpoints.

The one-shot semantic domain is finite and explicit:

1. `B` must contain a nonzero prepared token;
2. `D` must be blank;
3. `R` must be a nonzero aligned reference; and
4. the output/return routes must be ready.

If the output is occupied or the aligned reserve is absent, the Hamiltonian
flow remains reversible but does not represent a fresh event. It must be
rejected or returned as backpressure. Repeating forward strokes requires new
or recycled batteries and aligned references; no saturation, overwrite, or
free reset is permitted.

The existing FTD-0961 rail can transport `D'` after endpoint decoupling. That
handoff and later recycling are not proved by equation (1).

## 8. The conditional G* gearbox

The connection separates three quantities that had previously been conflated:

1. clock traversal time;
2. internal gear angle; and
3. traversal orientation.

Conditional on a maintained fixed-amplitude critical quartic clock, let
`Theta_*` be its lifted action-angle phase. On one quadrant choose

\[
 {\cal A}_*(delta)d delta=dTheta_*.                    \tag{29}
\]

Then

\[
 Delta alpha=Delta Theta_*=pi/2.                       \tag{30}
\]

The existing exact clock law is

\[
 T_*A=\sqrt{pi}\,G^*\sqrt{m/(2lambda)}.                \tag{31}
\]

Therefore

\[
 \boxed{
 omega_*={2pi\over T_*}
 ={2pi A\over\sqrt{pi}G^*}\sqrt{2lambda/m},}           \tag{32}
\]

and

\[
 t_{\rm quadrant}=T_*/4.                               \tag{33}
\]

This is the first exact reference gearbox that gives `G*` a clean, noncircular
role:

- `G*` sets how long the maintained quartic clock takes to traverse the
  quadrant;
- the normalized phase connection maps that quadrant into a quarter-turn of
  the internal modes; and
- the path orientation selects forward or inverse operation.

The result does **not** derive `{\cal A}_*` from CM arithmetic, derive the
quartic clock from production, or prove that `G*` uniquely selects the
quarter-turn. It is a conditional identification of the clock/calendar
interface once both structures are present.

## 9. What is closed

Within the selected reference architecture:

- exact positive autonomous connection Hamiltonian;
- mandatory complete-square reciprocal term;
- unchanged mechanical clock equation with exact canonical reaction;
- speed- and load-independent oriented holonomy;
- exact signed token loading from a positive battery;
- active target-controller alignment with complete old-phase export;
- exact reverse/unactualization recovery;
- time-reversal invariance and `C4` recurrence;
- positive energy, finite preparation, and backpressure accounting; and
- conditional separation of `G*` cadence from connection holonomy.

No new public ontology type is introduced. Equation (1) is a new selected
functional coupling among already-priced reference modes.

## 10. What remains open

- derivation of the connection and complete-square coupling from unchanged
  substrate fields;
- production identification and formation of `B,D,C,R`;
- battery/reference preparation, protection, replenishment, spatial routing,
  collisions, tail handling, and recycling;
- derivation and maintenance of the critical quartic action-angle chart;
- feedback work, dissipation, detuning recovery, and held-out perturbation
  stability;
- whether physical unactualization exports, thermalizes, or identifies
  details rather than exactly reversing them;
- full nonlinear stability when the connection, compiler, body, reservoirs,
  and history rail are repeatedly coupled;
- positive phase-error export for attraction;
- CM-prime or lattice selection of the quarter-turn connection;
- preferred-order operational hiding, Born/Bell recovery, and completeness.

The next decisive test is production representability: whether the unchanged
substrate already contains a local canonical pair and an antisymmetric
exchange generator whose kinetic energy completes to equation (1). Reference
success is not substrate evidence.

## 11. Certificate record

The FTD-0962 protocol has SHA-256
`535E14ADE46A886542165A815C4B807DFE35B0ACBD7B6131342C4DB9126C96B0`.
The immutable parent certificate has SHA-256
`8AD520B137323AFD02961109C84CD8B09B49D8FF7FD393803B950D08155E8F98`
and first reported `107/115`, Outcome D, on eight verifier text-anchor defects
after every mathematical gate passed.

The FTD-0963 repair protocol has SHA-256
`A01C5F9ECCA3E62E6F59052636073DBB71EB6E59A6CD9AE0C5B717D71F3586E3`.
The verifier-only wrapper has SHA-256
`7ED43084BD3AF97B362B5B778CF997FC05FE92416A8E7749B62D2A1C5AE5ECB5`
and passes inherited `115/115` plus repair integrity `25/25`, Outcome B.

No engine, CMake, `Voxel`, production field, default tick, constant, toggle,
Born law, selector, or `G*` value changed.
