# FTD-0928 — Self-dual reciprocal discrete action and formation-reservoir boundary v1

**Identifier:** `FTD-0928`  
**Date:** 2026-08-11  
**Status:** `[THEOREM — NO GENERATING FUNCTION FOR THE FROZEN TRIANGULAR MAP]` +
`[SELECTED REFERENCE CONSTRUCTION — MINIMUM POSITIVE SELF-DUAL RECIPROCAL ACTION]` +
`[THEOREM — EXACT C4 RETURN / POSITIVE-BAND STABILITY / TICK INVARIANT]` +
`[THEOREM — PHASE-COMPLETE RESERVOIR LOWER BOUND]` +
`[CONDITIONAL REFERENCE — COMPLETE-PAIR FORMATION TRANSFER]` +
`[OPEN — SPATIAL PREPARATION MAP / STATIC-HALO FORMATION / PRODUCTION]`  
**Protocol:**
[`PREREG_SELF_DUAL_RECIPROCAL_DISCRETE_ACTION_AND_FORMATION_RESERVOIR_BOUNDARY_v1.md`](../../preregistrations/native_time_carrier_programme/PREREG_SELF_DUAL_RECIPROCAL_DISCRETE_ACTION_AND_FORMATION_RESERVOIR_BOUNDARY_v1.md),
SHA-256 `27BD89002B2B432FB58950B639B56E0FD22C5511E48550AD026DB462BEE2E076`  
**Certificate:**
[`proof_self_dual_reciprocal_discrete_action_formation_reservoir_boundary.py`](../../../../../scripts/proofs/proof_self_dual_reciprocal_discrete_action_formation_reservoir_boundary.py),
SHA-256 `E41455B589705E1B3B2F4ECCFABD5F0AF28DE303AD216DE700B241EBFB113AE0`,
`100/100` exact checks  
**Registered outcome:** `B — RECIPROCAL SELF-DUAL REFERENCE ACTION / FORMATION BOUNDARY`

---

## 1. Result

The one-way FTD-0927 record–matter-to-field recurrence cannot be generated by
a discrete canonical action on its frozen direct-sum phase space. This is not
a failure of one guessed generating function: a nonconstant lower-triangular
source is incompatible with the symplectic condition itself.

There is, however, a minimum positive reciprocal completion in the
exchange-symmetric quadratic class locked by the preregistration. Shift the
field by its static part, `X=J-H`, and introduce a field-shaped canonical
companion `Q`. With symmetric field stiffness `K`, the selected discrete
action is

\[
 L_d={1\over2}\|X_{n+1}-X_n\|^2
    +{1\over2}\|Q_{n+1}-Q_n\|^2-V_1(X_n,Q_n),           \tag{1}
\]

where

\[
 V_1={1\over2}\langle X,KX\rangle
    +{1\over2}\langle Q,KQ\rangle
    -\langle X,(K-2I)Q\rangle
    +{1\over2}\|X-Q\|^2.                               \tag{2}
\]

In the self-dual and anti-self-dual coordinates

\[
 A={X+Q\over\sqrt2},\qquad B={X-Q\over\sqrt2},          \tag{3}
\]

equation (2) factorizes exactly:

\[
 \boxed{V_1=\langle A,A\rangle+\langle B,KB\rangle.}    \tag{4}
\]

The self-dual sector therefore has kick–drift stiffness `2` and exact period
four. The anti-self-dual sector has stiffness `2K` and is stable on every
positive native field mode `0<K<=16/9`.

This closes a reciprocal **reference action**, not physical formation. Its
new companion has not been identified with a production field, current, or
record. Moreover, the linear factorized action leaves the empty self-dual
phase plane empty. At least one complete canonical reservoir pair is required
to transfer a prepared phase into it; a scalar energy account cannot do so
canonically.

---

## 2. Why the one-way map has no generating function

Let `z` denote the FTD-0926 matter canonical pair and `y=(J,P)` a field
canonical pair. The frozen FTD-0927 composition has the form

\[
 z'=Az,\qquad y'=By+c(z),                                \tag{5}
\]

with symplectic `A` and `B`. Writing `C=Dc/Dz`, its Jacobian is

\[
 T=\begin{pmatrix}A&0\\C&B\end{pmatrix}.                \tag{6}
\]

For the direct-sum symplectic form
`Omega=diag(Omega_m,Omega_f)`, the upper-right block of
`T^T Omega T=Omega` is

\[
 C^{\mathsf T}\Omega_fB=0.                              \tag{7}
\]

Both `Omega_f` and `B` are invertible, so equation (7) implies `C=0`.
Therefore every nonconstant one-way source in equation (5) is
nonsymplectic. Since a canonical transformation is a necessary condition for
the existence of any discrete generating function, none exists for the
frozen map on the frozen phase space.

The certificate also tests the exact scalar-mode witness

\[
 A=\begin{pmatrix}-1&1\\-2&1\end{pmatrix},\qquad
 B=\begin{pmatrix}1-k&1\\-k&1\end{pmatrix},\qquad
 C=\begin{pmatrix}\ell&0\\\ell&0\end{pmatrix}.          \tag{8}
\]

The symplectic residual is

\[
 T^{\mathsf T}\Omega T-\Omega
 =\begin{pmatrix}
 0&0&-\ell&0\\
 0&0&0&0\\
 \ell&0&0&0\\
 0&0&0&0
 \end{pmatrix},                                         \tag{9}
\]

which vanishes only for `ell=0`. This obstruction is exact and independent
of the field-mode value `k`.

### Scope of the no-go

The theorem excludes the registered **one-way triangular** composition on
the registered direct-sum phase space. It does not exclude:

- reciprocal matter–field dynamics;
- a larger phase space containing a physical reservoir;
- constrained or presymplectic reformulations with separately proved
  reduction; or
- a different ontology whose source is itself a canonical coordinate.

The construction below takes the first route.

---

## 3. The existing matter orbit already has a canonical quarter-turn chart

For the existing remainder–velocity state `(r,v)`, define

\[
 \begin{pmatrix}Q_m\\\Pi_m\end{pmatrix}
 =S\begin{pmatrix}r\\v\end{pmatrix},\qquad
 S=\begin{pmatrix}0&1/\sqrt2\\-\sqrt2&1/\sqrt2\end{pmatrix}. \tag{10}
\]

Thus

\[
 Q_m={v\over\sqrt2},\qquad
 \Pi_m={v-2r\over\sqrt2}.                               \tag{11}
\]

The matrix `S` is symplectic and conjugates the FTD-0926 update to

\[
 (Q_m,\Pi_m)\mapsto(\Pi_m,-Q_m).                         \tag{12}
\]

It also turns the positive invariant into the Euclidean oscillator metric:

\[
 r^2-rv+{v^2\over2}={Q_m^2+\Pi_m^2\over2}.              \tag{13}
\]

This proves that the live velocity appearing in the FTD-0927 current can be
read as a canonical configuration in a valid chart. It does **not** identify
the field-shaped companion `Q` in equation (1) with `Q_m`, because their
spatial support, tensor character, normalization, and preparation map have
not been derived.

---

## 4. Minimum reciprocal completion

Keep the mismatch coefficient symbolic:

\[
 V_\gamma={1\over2}\langle X,KX\rangle
    +{1\over2}\langle Q,KQ\rangle
    -\langle X,(K-2I)Q\rangle
    +{\gamma\over2}\|X-Q\|^2.                           \tag{14}
\]

The exchange `X<->Q` is an exact symmetry. Under equation (3), the two
stiffness operators are

\[
 K_+=2I,\qquad K_-=2\bigl(K+(\gamma-1)I\bigr).           \tag{15}
\]

The first is independent of `K`, so the desired self-dual quarter turn is
retained throughout the field band. At the zero-mode endpoint, every
`gamma<1` makes `K_-` negative. Hence

\[
 \boxed{\gamma_{\min}=1}                                 \tag{16}
\]

is the minimum context-blind coefficient giving a nonnegative potential on
the full closed band `0<=K<=16/9`. It is a **selection within the frozen
reference class**, not a new fundamental constant or a value inferred from a
fit.

The discrete Euler–Lagrange equations at this minimum are

\[
 \Delta^2X=-(K+I)X+(K-I)Q,\qquad
 \Delta^2Q=-(K+I)Q+(K-I)X.                              \tag{17}
\]

Relative to the original free field acceleration `-KX`, the first equation
contains the source

\[
 U_X=(K-I)Q-X=(K-2I)Q-(X-Q).                            \tag{18}
\]

On the self-dual section `X=Q`, this becomes

\[
 \boxed{U_X=(K-2I)Q,}                                   \tag{19}
\]

exactly the frozen abstract dynamic source in FTD-0927. Off the section, the
last term in equation (18) returns the mismatch. The companion equation
contains the equal reciprocal reaction, which is precisely what the frozen
triangular map omitted.

No outcome, measurement context, target arm, `G*` phase, Born weight, or Bell
setting appears in equations (14)–(19).

---

## 5. Exact symplectic map, stability, and tick energy

For one mode with symmetric two-coordinate stiffness matrix `M`, the
kick–drift map on `(q,p)` is

\[
 \begin{pmatrix}q'\\p'\end{pmatrix}
 =\begin{pmatrix}I-M&I\\-M&I\end{pmatrix}
  \begin{pmatrix}q\\p\end{pmatrix}.                    \tag{20}
\]

Because `M=M^T`, equation (20) is exactly symplectic and has determinant one.
At `gamma=1`, the chart (3) factorizes it into

\[
 T_+=\begin{pmatrix}-1&1\\-2&1\end{pmatrix},\qquad
 T_-(k)=\begin{pmatrix}1-2k&1\\-2k&1\end{pmatrix}.      \tag{21}
\]

The self-dual map obeys

\[
 T_+^2=-I,\qquad T_+^4=I.                               \tag{22}
\]

For a kick–drift stiffness `a`, an exact tick invariant is

\[
 G(a)=\begin{pmatrix}a&-a/2\\-a/2&1\end{pmatrix},qquad
 T(a)^{\mathsf T}G(a)T(a)=G(a).                         \tag{23}
\]

At `a=2`, this is the FTD-0926 metric

\[
 G_+=\begin{pmatrix}2&-1\\-1&1\end{pmatrix}.            \tag{24}
\]

For the anti-self-dual sector,

\[
 G_-(k)=\begin{pmatrix}2k&-k\\-k&1\end{pmatrix},\qquad
 \det G_-(k)=k(2-k).                                    \tag{25}
\]

Therefore `G_-(k)` is positive definite for `0<k<2`. The native positive
band satisfies `0<k<=16/9<2`, while the map trace
`2-2k` remains strictly between `-2` and `2`. Every positive-band mode is
elliptically stable. The exact zero mode is only nonnegative and has a
neutral drift/Jordan direction; no positive-definite zero-mode energy or
zero-mode stability claim is made.

The full tick invariant is the direct sum of equations (24) and (25) in the
self/anti chart. Hence exact period four is a property of the self-dual
section, not of every anti-self-dual field mode.

---

## 6. Why stable recursion is not formation

Equation (4) is linear and factorized. Consequently,

\[
 (A,p_A)=(0,0)\quad\Longrightarrow\quad
 (A_n,p_{A,n})=(0,0)\ \text{for every }n.               \tag{26}
\]

There is no anti-to-self energy transfer in the selected action. It can
propagate and restore an already prepared self-dual orbit, but it cannot form
that orbit from an empty phase plane.

Nor can one attach a scalar energy balance and call formation canonical. Any
antisymmetric form in odd dimension is degenerate, so one scalar appended to
a two-dimensional body phase plane cannot be a phase-complete symplectic
reservoir.

Even a formally paired but phase-blind debit is insufficient. Let `F` be the
body symplectic map and consider

\[
 (z,I,\phi)\mapsto\bigl(F(z),I-w(z),\phi\bigr).          \tag{27}
\]

For product form `Omega_z+dI wedge dphi`, its pullback adds

\[
 -dw\wedge d\phi.                                       \tag{28}
\]

The map is symplectic only when `dw=0`. A state-dependent formation debit
therefore needs reciprocal phase dynamics rather than a passive scalar
counter.

This establishes the lower bound:

\[
 \boxed{\text{one formed phase plane requires at least one complete
 canonical reservoir pair.}}                            \tag{29}
\]

The bound is kinematic. A spatial body with many independent phase modes may
require a correspondingly larger reservoir.

---

## 7. Conditional complete-pair transfer witness

Let `Z_b` be one body canonical pair and `Z_R` an equal-metric reservoir pair.
The species quarter turn

\[
 \mathcal S:(Z_b,Z_R)\mapsto(Z_R,-Z_b)                  \tag{30}
\]

is symplectic, obeys `S^2=-I` and `S^4=I`, and preserves every quadratic
energy of the form

\[
 E={1\over2}Z_b^{\mathsf T}GZ_b
  +{1\over2}Z_R^{\mathsf T}GZ_R                         \tag{31}
\]

for the same positive metric `G` on both pairs. In particular,

\[
 (0,Z_R)\mapsto(Z_R,0),                                 \tag{32}
\]

so a completely prepared reservoir phase can be transferred into an empty
body without losing reversibility or energy bookkeeping.

`[CONDITIONAL REFERENCE]` Equation (30) proves logical sufficiency only. It
assumes the correct body profile, orientation, phase, complete positive
reserve, and an eligibility gate. It does not produce those data, derive the
full modeled debit, form the static halo `H`, or explain why the gate fires.

The inherited modeled debit

\[
 \Delta E_{\rm form}={26\pi\over25}+1+{k_h\over2}>0      \tag{33}
\]

remains an accounting target. Equation (30) does not physically supply or
derive it.

---

## 8. Production and ontology audit

The current `Voxel` already carries `flux_L`, `flux_R`, `wave_vel_L`,
`wave_vel_R`, `velocity`, and `remainder`. Production propagates the two dual
flux sectors separately and applies the same prescribed Hodge source to each.
It does not contain the reciprocal cross-operator or mismatch term in
equation (18).

Accordingly:

- `[THEOREM]` the reciprocal construction is an exact finite-dimensional and
  finite-mode mathematical reference;
- `[OPEN]` no existing production field has been proved to be the companion
  `Q`;
- `[OPEN]` no local bond-current ontology type is adopted here;
- `[OPEN]` the restriction-consistent spatial `PreparationMap` from the
  matter record/current phase to `Q` is missing;
- `[OPEN]` autonomous nonlinear transfer from a physically prepared reservoir
  into the full dynamic body and static halo is missing; and
- `[OPEN]` no engine or CMake implementation has been authorized.

The certificate changes no engine source, `Voxel` type, CMake target, toggle,
default, or production update law.

---

## 9. Epistemic ledger

| Claim | Status | Meaning |
|---|---|---|
| A nonconstant frozen triangular source has no discrete generating function on the frozen direct-sum phase space | `[THEOREM]` | Follows from the invertible cross-block symplectic condition |
| The remainder–velocity orbit has the exact canonical chart (10) | `[THEOREM]` | Exact conjugacy and positive metric identity |
| `gamma=1` is the minimum nonnegative full-band mismatch coefficient in the frozen exchange-symmetric class | `[SELECTION]` | Minimum inside a deliberately chosen quadratic class |
| The reciprocal map is symplectic and has exact self-dual `C4` return | `[THEOREM]` | Exact matrix identities |
| Every positive native field-band anti mode is stable and has a positive tick invariant | `[THEOREM]` | Holds for `0<k<=16/9` |
| The self-dual field source equals the frozen FTD-0927 abstract source | `[THEOREM]` | Exact identity on `X=Q` |
| `Q` is the physical matter current, record, or one existing dual field | `[OPEN]` | No spatial/type/normalization map has been derived |
| A scalar account is a phase-complete formation reservoir | `[CLOSED NEGATIVE]` | Odd-dimensional degeneracy and equation (28) exclude it |
| At least one complete canonical reservoir pair is required per formed phase plane | `[THEOREM]` | Symplectic lower bound |
| A species quarter-turn can transfer one prepared equal-metric phase pair reversibly | `[CONDITIONAL REFERENCE]` | Sufficiency witness, not autonomous formation |
| The full compact body and static halo form from local substrate dynamics | `[OPEN]` | Profile, gate, reserve, and nonlinear transfer remain missing |
| The reciprocal reference action is the production engine law | `[OPEN]` | No production integration or recovery test has occurred |

---

## 10. Consequence

The missing dynamics are now sharper. FTD does not need a cleverer one-way
source; one-way source injection is exactly the problem. A canonical substrate
must include the source's reciprocal reaction and a phase-complete reservoir.

The next derivation target is therefore not another clock period. It is a
local, restriction-consistent preparation map

\[
 (\rho,\rho v,r,v,\text{local reservoir})
 \longmapsto (X,Q,p_X,p_Q)                               \tag{34}
\]

that:

1. uses no target arm, final profile, measurement context, or Born weight;
2. makes `Q` a derived local object rather than a silently imported field;
3. supplies reciprocal work from a complete positive reservoir;
4. forms both the dynamic self-dual mode and the static halo;
5. preserves locality, reversibility, and the full energy ledger; and
6. reduces to equations (17)–(19) on the registered invariant section.

Until that map is derived and tested, FTD has clock-compatible reciprocal
hardware but not a physical gearbox connecting matter, field, and formation.

