# Pre-Registration — MC-T4.3 ARC-B2 BCC Algebraic Bridge Readout (v1)

**Tag:** [PRE-REGISTRATION] — this document locks the design of the ARC-B2 BCC Algebraic Bridge readout attempt against the central foundational obstruction MC-T4.3 (the operational electric charge readout rule under the `SPEC_ALPHA_READOUT_CONTRACT.md` framework). It contains **no result**. All three outcomes — FOUND / UNDERDETERMINED / CLOSED-NEGATIVE — are pre-blessed.

**Date:** 2026-05-27  
**Hash-lock target tag:** `preregister-alpha-readout-bcc-bridge-v1`  
**LEDGER row reservation:** FTD-0215 (provisional; confirm next-free identifier against `../07_assessment/LEDGER.md` at hash-lock).  
**Companion docs:** `../01_reference/SPEC_ALPHA_READOUT_CONTRACT.md`, `../09_mathematical/DERIV_BCC_COMPLEX_STRUCTURE.md` (FTD-0122, the BCC complex-decomposition theorem).

---

## §1 — Context and Doctrine

### 1.1 The Category Mismatch Obstruction
Prior attempts to solve the operational alpha-readout problem under Candidate B1 closed negative due to a **categorical structural mismatch**:
* **Substrate arithmetic:** Discrete-lattice bilinears, plaquette bivectors, and Lindbladian projections yield characteristic polynomials with finite-combinatorial structure constants or lattice-spectral exponentials ($e^{-E_k \tau}$).
* **Lemniscatic arithmetic:** The master quadratic has coefficients $16 = |\text{Aut}(E)|^2$ and $G^* = \Gamma(1/4)/\Gamma(3/4)$ which are number-theoretic periods of the lemniscatic elliptic curve $E: y^2 = x^3 - x$ over $\mathbb{Z}[i]$.

### 1.2 The BCC Algebraic Bridge (Candidate B2)
To resolve this mismatch, Candidate B2 specifically targets the proven $\mathbb{Z}[i]$-module structure of the 4D complex representation subspace $V_{\text{complex}}^2$ in the Body-Centered Cubic (BCC) regular representation decomposition ([THEOREM] FTD-0122, `DERIV_BCC_COMPLEX_STRUCTURE.md` §3.2):
$$ \mathbb{Z}[\text{BCC}] \otimes \mathbb{Q} \cong V_{\text{triv}}^2 \oplus V_{\text{sign}}^2 \oplus V_{\text{complex}}^2 $$

Because $V_{\text{complex}}$ is arithmetically isomorphic to the Gaussian integers $\mathbb{Z}[i]^2$, it carries exactly the unit group $\mu_4 = \{1, i, -1, -i\}$ of order 4, giving the structural origin of the master quadratic coefficient:
$$ 16 = |\mu_4|^2 = |\text{Aut}(E)|^2 $$

This pre-registration locks the design and verification protocol for attempting to derive a non-circular readout rule restricted to the $V_{\text{complex}}$ subspace.

---

## §2 — The Question

**Q-ARC-B2.** Does there exist an ARC tuple `(P, A_obs, O_EM, R, C)` such that:
1. `A_obs` is the gauge-invariant observable algebra restricted to the complex $V_{\text{complex}}$ subspace of the BCC lattice;
2. the characteristic equation of the transfer operator $T_{\text{BCC}}$ derived from `A_obs` reproduces the FTD master quadratic $x^2 - 16 G^{*2} x + 16 G^{*3} = 0$ as a forward mathematical consequence of $\mathbb{Z}[i]$ prime-splitting arithmetic;
3. `O_EM` is operationally tied to a charge or field-strength measurement protocol;
4. the dominant root $x_+$ is selected as the electromagnetic readout on FTD-internal grounds;
5. the construction is admissible under the hard exclusion rules of `SPEC_ALPHA_READOUT_CONTRACT.md`?

---

## §3 — Definitions & Admissible Primitives

- **D1 — ARC-B2 Tuple:** `ARC = (P, A_obs, O_EM, R, C)` per the FTD contract.
- **D2 — BCC Subspace Projection:** The projection operator $\Pi_{\text{complex}}$ onto the 4D complex subspace $V_{\text{complex}}$ of the 8 BCC corner coordinates.
- **D3 — Observable Algebra $A_{\text{obs}}$:** The gauge-invariant algebra generated by multi-voxel fields projected onto $V_{\text{complex}}$.
- **D4 — Admissible Primitives:** Primitives must draw solely from the frozen catalog of `SPEC_ALPHA_READOUT_CONTRACT.md` §4 and the BCC complex structural operators of `DERIV_BCC_COMPLEX_STRUCTURE.md`.

---

## §4 — The Three Pre-Blessed Outcomes

### FOUND
An ARC-B2 tuple is exhibited with a rigorous derivation trace. Every step is `[THEOREM]`-grade from BCC lattice geometry. The characteristic equation of the transfer operator matches the master quadratic. The selection of $x_+$ is derived rather than matched, and the relative error is $0$.

### UNDERDETERMINED
A partial mathematical readout rule is achieved (e.g., $16 = |\mu_4|^2$ derived, but $G^*$ remains un-operationalized), or the operational protocol is incomplete.

### CLOSED-NEGATIVE
No candidate transfer operator on the complex subspace produces the master quadratic without importing $M_N(t)$ or violating the circularity rules (e.g., the prime-splitting invariants of the discrete Gaussian integers do not flow to the continuous lemniscatic period ratio $G^*$ without continuum limits).

---

## §5 — The Falsifier Checklist

The attempt is immediately falsified (proceeds to UNDERDETERMINED or CLOSED-NEGATIVE) if:
- **F-a:** Uses physical $\alpha$, CODATA $1/\alpha$, or a measured QED coupling as input.
- **F-b:** Contains a free parameter tuned to force the output to $137.036...$.
- **F-c:** Fails dominant-branch selection: no FTD-internal reason selects $x_+$ over $x_-$.
- **F-d:** Fails operational protocol: the observable is "merely the thing whose eigenvalue is $x_+$" without a physical measurement interpretation.
- **F-e:** Reduces to a site-local 0-form Clifford embedding (fails the FTD-0073 no-go).
- **F-j:** Reverse-engineers $T_{\text{BCC}}$ by importing the master quadratic or $G^*$ as a scaffold. The polynomial must emerge forward from the modular arithmetic of $\mathbb{Z}[i]$.

---

## §6 — The 11-Step Method

The closure attempt must execute exactly these steps in order:
1. State the proposed ARC-B2 tuple `(P, A_obs, O_EM, R, C)`.
2. Construct the $V_{\text{complex}}$ projection operator $\Pi_{\text{complex}}$ on the BCC unit cell.
3. Prove the algebraic isomorphism $V_{\text{complex}} \cong \mathbb{Z}[i]^2$ and show the origin of the unit-group coefficient $16 = |\mu_4|^2$.
4. Construct the transfer/readout operator $T_{\text{BCC}}$ on the complex subspace.
5. Derive the characteristic equation of $T_{\text{BCC}}$ symbolically.
6. Compare the characteristic equation to the master quadratic at $t=1$.
7. State the dominant-branch selection rule distinguishing $x_+$ from $x_-$.
8. State the operational measurement protocol.
9. Apply the falsifier checklist F-a through F-j mechanically.
10. If steps 1-9 pass, perform the numerical comparison to $1/x_+$ and report the residual.
11. Report the final verdict (FOUND / UNDERDETERMINED / CLOSED-NEGATIVE).
