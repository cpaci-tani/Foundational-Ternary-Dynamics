# FTD-0666 — Internal-mode return-time discriminator v1

**Status:** `[RETRACTED — MODAL MASS-METRIC ERROR, FTD-0675]`  
**Historical verdict:** `INTERNAL_MODE_RETURN_TIME_MIXED`  
**Production impact:** none

> The “return” classifier used `v^T delta x` instead of the canonical
> `v^T M delta x`. FTD-0675 proves that this observable can manufacture return
> times for a constant-energy harmonic mode. The timing result below is
> archived provenance, not a physical return-time measurement.

## Result

The fresh `L=17` extension executes and inverts. Both signs cross the unchanged
return threshold at tick 73. The preregistered prediction was ticks `74..78`,
so it fails by one tick and the verdict is mixed.

Combined registered return times are

\[
t_{\rm return}(17)=73,\qquad
t_{\rm return}(25)=76,\qquad
t_{\rm return}(33)=76.
\]

This is not proportional to circumference: `t/L` changes from `4.29` to
`3.04` to `2.30`, while absolute return time changes by only three ticks. The
data support an intrinsic approximately 76-tick matter--field beat with a
small finite-volume shift. Because the locked absolute prediction missed, that
interpretation remains a numerical inference rather than a qualified theorem.

## Consequence

The excitation is neither a simply stable isolated mode nor a monotone decay.
It exports a dynamic field before wraparound, then exhibits a coherent
matter--field revival on a periodic finite system. An effectively uncontained
environment could carry the emitted field away and suppress the revival, but
that requires an outgoing/open-environment campaign rather than rhetoric.
