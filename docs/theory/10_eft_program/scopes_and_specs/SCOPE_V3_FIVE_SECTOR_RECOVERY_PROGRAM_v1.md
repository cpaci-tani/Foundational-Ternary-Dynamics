# Scope — V3 five-sector recovery program v1

**Date:** 2026-08-24  
**Status:** **[SCOPE / DEPENDENCY CONTRACT — ACTIVE]**  
**Framework:** ratified v3 strict-discrete common-action constitution (FTD-1023)  
**Promotion rule:** no sector closes by compatibility, standard-form
substitution, or a result that is not downstream of the selected finite rule

---

## 1. Objective and ordering

The active postulate programme attacks five physical debts:

1. charged Gauss continuity;
2. physical coupling normalization;
3. stable matter;
4. Born statistics; and
5. gravity.

They are not independent checkboxes. The dependency order is

```text
finite Phi
  -> charged source/current/Gauss
  -> charged pole + reciprocal work
  -> physical coupling curvature
  -> stable source-bearing matter and apparatus
  -> native preparation/event frequencies
  -> conserved common stress and universal gravity response
```

Born recovery may develop in parallel at the finite-counting level, and a
tensor carrier may be investigated in parallel at the representation level.
Neither becomes physical until the common rule prepares the relevant records.

---

## 2. Closure gates

### G — charged Gauss continuity

Required:

- exact finite source and current records from the common rule;
- `Delta Q + div j=0` and `Delta(div E-Q)=0` on the complete tick;
- unchanged transverse vacuum recovery;
- moving-source composition; and
- a divergence-preserving relaxation with a charged massless static pole.

Current result: the charged Phi-v3 candidate closes the first four items on
the admitted pure-bound sector. It supplies a state-only, conflict-free
radius-one schedule, exact parallel source/current/Gauss composition, one-hop
endpoint transport, repeated plaquette deformation, and unchanged transverse
vacuum recovery. The divergence-preserving relaxation, charged massless
static pole, reciprocal work, and promotion from candidate to canonical Phi
remain open. A subsequent exact neutral-rotor seam shows that two existing
field slots can implement a zero-field C12 router whose repeated finite source
histories converge deterministically to the Dirichlet Green field with
Poisson residual `8/N` and current-gradient error `8/(3N)`. This supplies a
conditional blocked-history `1/Lambda` mechanism, but its rotor background,
renewal source, sink, and field writeback are not yet integrated into Phi. A
second exact theorem closes the neutral sampler's local carrier: an `R4`
marker pair has unique state-only roles, moves one SC hop in a zero-field
radius-one transaction, and exactly reproduces the Green histories. The
remaining moving-token debt is specifically charged A9 endpoint/dressed-string
composition, not neutral marker storage.

### C — physical coupling normalization

Required:

- canonical source and field coordinates;
- one microscopic history action or physically selected measure;
- a unique blocked curvature `chi_EM` stable under blocking;
- agreement between free Hessian and static source residue; and
- no physical coupling or master root used as an input.

Canonical packet coordinates and one exact bare reference now exist. Uniform
binary counting gives `Cov(E,B)=16 I6`; in complete-packet coordinates its
covariance is `I6/4`, bare Hessian `4 I6`, and unit-packet Mahalanobis cost two.
The same finite measure yields the prepared Born readout. But the candidate
dynamics does not select that measure, identify its Hessian with the
interacting action, integrate the rotor pole into Phi, or fix the common positive
multiplier. The positive action-scale orbit remains an exact obstruction.
The rotor theorem fixes the spatial Green kernel in unit-token coordinates but
does not price its energy, so it does not remove that multiplier.

An exact transaction-census successor now classifies every homogeneous,
phase-blind additive occupancy invariant over field, SC-A1, FCC-A1, and A2
roles. Canonical absorption plus all 37,632 registered repairs force
`e_F=e_A1_SC=e_A2`; FCC remains a separate cubic orbit, leaving precisely
`Gamma*(N_F+N_A1_SC+N_A2)+Eta*N_A1_FCC` (14/14). A target-blind candidate
then uses the unique FCC-bond/A2-plaquette-diagonal incidence and one C3 clock
section to exchange FCC reserve and A2 occupancy by an exact source-neutral
finite permutation. Conditional on selecting that bridge, the exchange rank
is three and the unique relative occupancy ray is
`e_F=e_A1_SC=e_A1_FCC=e_A2` (12/12). This closes relative role normalization
inside the additive ansatz. The bridge is not derived from the common action
or integrated into canonical Phi, and the absolute multiplier `Gamma` remains
free; therefore physical `chi_EM` is still open.

The reciprocal packet/clock vertex and finite global clock narrow the scale
conditionally. If a material phase winds `w` times in `T` global ticks and
`d` momentum-neutral complete packets fund one action quantum, then

\[
 \alpha_{\rm native}={3w\over dT}.
\]

This exact rational ladder is invariant under proportional tick refinement.
It proves that clock existence alone cannot select the coupling. The triplet
clock successor now fixes the actual material cadence: every A9 commit advances
the C4 phase by one quarter-turn, and eight commits in the exact 16-tick
complete period give `(w,T)=(2,16)`, equivalently primitive `(1,8)`. Therefore
`omega_M=pi/4`, the momentum-neutral ladder reduces to
`alpha_native=3/(8d)`, and recoil gives `3/[8(d-r)]` (12/12). Phi-v12 seed
assembly has zero complete-packet debit, so it cannot supply `d`. The common
microscopic law must still derive the positive packet debit, recoil response,
and shared pole/residue. This is a structural normalization boundary, not
permission to choose `d` or `r` to match a target.

A prepared paired-packet successor now closes the minimum-debit subgate and
simultaneously rejects its most naive physical interpretation. Every complete
moving C4 packet has unit energy and nonzero reserve current `r/6`; exactly two
opposite packets are the unique minimum nonempty current-neutral batch. They
gate one clean triplet COMMIT by an atomic reserve-to-clock-port ownership
transfer with all 32 records retained, and all 36,864 physical presentations
have exact inverses (13/13). Thus `d=2` on this selected port. Combining it
with `omega_M=pi/4` gives `chi_port=pi/8` and the conditional ladder branch
`3/16`, closing the Occam-minimum port negative as the physical fine-structure
normalization. Under a selected symmetric-stress map the two momenta cancel
while axial stress survives. Assigning `chi_port` to both the rotor Dirichlet
action and the transverse packet norm makes the static/free estimators equal,
but the finite ownership permutation does not force that coefficient tie.
Field Noether charge, native trigger/refill, protected interacting residue,
curvature provenance, and canonical-Phi integration remain open.

### M — stable matter

Required:

- a state-only, symmetry-covariant object predicate;
- formation by the same rule;
- an invariant family or a nonzero open perturbation neighborhood with a
  registered survival bound;
- environmental and volume-causality controls;
- reciprocal bound-field and work accounting; and
- rejection of imposed sources, periodic-return artifacts, and dispersing
  packets.

The source macro has a 288-state period-24 invariant proto-object family. The
charged circulation frame adds a 24-state anchor family on period-four orbits.
All 37,632 one-coordinate defects in the latter's claimed footprint are
rejected by its defining macro, so that macro's one-defect correction radius
is zero. A successor coding theorem nevertheless proves minimum exact-frame
distance 36 and a unique parent for every registered defect. A selected
state-only projection therefore supplies a conditional one-tick kinematic
basin. A finite reversible successor now gives every defect an explicit
syndrome conveyor and keeps the object exact for any registered finite `H`
before closed-orbit recurrence. It proves that finite reversible repair cannot
be a permanent attractor; genuine permanent projection must instead use the
constitution's noninjective-expiry branch. Neither branch is implemented by
the canonical common law. A carrier successor closes the isolated memory hardware:
eighteen existing opposite-polarity field records form 6,435 constant-weight,
zero-`E/B` codewords, and the 1,569 operational syndromes move causally with a
radius-one inverse. A selected atomic successor now composes the decoder and
carrier for every registered defect: one Moore-local step writes the exact
next frame, emits the unique neutral syndrome, and closes the generalized
object+syndrome+work token count with output work occupancy `0/1/2`. The full
37,632-row map is injective and its defect index is cyclic-axis and
charge-conjugation covariant after the required `R^(4f)` C4/C3 clock-section
normalization. A second successor realizes the work count in two actual A2 A9
slots with complete phase/polarity, makes the registered input/output sections
a finite bijection, and conserves a selected equal-occupancy energy exactly.
The transaction census now forces that metric's relative field/SC-A1/A2
weights inside the homogeneous additive phase-blind ansatz, and a selected
FCC/A2 incidence bridge conditionally extends equality to every audited
occupancy role. Its common-action provenance, positive multiplier, and the
bundle clock-debit work remain underived. A symmetry successor identifies the
old reflection gap as a polar/axial chart error: the plaquette cross-product
is axial, while the existing ready syndrome header supplies a polar repair
normal. An orientation-corrected 1,152-state combined frame/header orbit then
closes all 1,569 codewords, 37,632 defect indices, repair ports, A2 payloads,
and exact outputs under the full signed-cubic group (12/12). Full reflection
covariance is therefore closed conditional on selecting the enlarged
oriented-header chart, without a new type. Native formation/admission of that
chart, traffic arbitration, Phi integration, environmental survival, scattering, and
mass/dispersion remain unproved. These are conditionally repairable recurrent
proto-bodies, not yet stable matter.

A prepared-sector stability successor now supplies the first explicit
nonzero basin. Three disjoint orthogonal A9 relation clocks and three copied
neutral center heralds form a radius-one two-tick repetition code. Its complete
orbit has period sixteen, and every one-register valid-symbol substitution at
every phase—1,488 exact cases—rejoins the clean orbit within two ticks. The
repair is genuinely noninjective, signed-cubic covariant, and minimal at three
copies within the repetition architecture. Its three mean capacity deficits
sum to the isotropic tensor `-I/36` (12/12). This closes a registered
prepared-symbol survival neighborhood, not native assembly, positive binding,
occupancy-error recovery, traffic/collision stability, translation, mass, or
canonical-Phi integration.

The exact action audit then separates kinematic repair from physical binding.
Every clean or one-substitution triplet state has the same role-count vector
`(6,3,0,0)`, so the additive occupancy defect rank is zero and the all-equal
ray assigns the flat value `9 Gamma`. A positive relational syndrome gap
`epsilon_syn` is conservative only if repair exports exactly
`W=epsilon_syn`; neither coefficient is selected. The exact source shape
`-I/36` therefore retains a free response multiplier, and the clean clock's
zero packet debit cannot normalize `3w/(dT)` (13/13). Physical stability and
coupling normalization now share the sharper missing object: relational
action curvature plus its local work carrier.

A Phi-v5 prepared-sector candidate now realizes the **relative** half of that
object. Three copied pending registers use 33 symbols each—dark plus decoded
clock state and a syndrome bit—and fit in the exact 170-state clear controller
pool. One existing fixed-occupancy A2 A9 token changes READY to EXCITED during
repair. All 2,256 one-register substitutions restore the body within two ticks
and leave one work excitation; `h+w` obeys `1+0=0+1`, a busy port fails closed,
and error identity expires while its generic consequence survives (12/12).
The common multiplier `epsilon_syn`, work export/reset, formation, and general
stability remain open, so absolute normalization is not closed.

A Phi-v6 prepared-sector successor now composes that actual 33-symbol/A2
work state with translation. The triplet's one-sided arm slab and existing
polar repair header place every shifted arm owner and the new center inside
the prior center's Moore neighborhood. One homogeneous chart-relative rule
moves the complete clock one SC hop per global tick. The clean period-16
sector has an exact inverse over all 18,432 chart-phase states; every one of
the 2,256 Phi-v5 substitutions rejoins the moving body within two ticks and
retains READY-to-EXCITED work. The construction is spatially homogeneous,
fully signed-cubic covariant, neutral, and preserves occupancy `(6,3,0,1)`
(15/15). This closes prepared translating correction, not native
formation/admission, destination and multi-object collision arbitration,
binding, translation work, inertia, mass/dispersion, packet emission, or
canonical state-complete `Phi`. Because clean additive translation work is
exactly zero, kinematic motion does not solve physical stability.

A Phi-v12 formation successor now closes the first exact seed-to-body
assembly subgate without weakening the blank-past obstruction. Conditional on
one finite nonblank oriented genesis chart, three existing SC A9 owners,
three neutral field-pair markers, and one A2 phase/work owner rearrange into
the exact Phi-v5 triplet in one Moore-local tick. Occupancy remains
`(6,3,0,1)` with zero role debit. All sixteen clean logical seeds and all 1,440
one-symbol seed mutations form; corrected rows retain one EXCITED A2 work
record while the detailed error identity expires. The branch is fail-closed,
signed-cubic covariant on all 1,152 charts, and its output enters the exact
period-16 clock (14/14). This establishes causal assembly **from** a physical
genesis seed, not formation from blankness, origin of the chart, positive
binding, canonical-Phi provenance, traffic/collision stability, mass, or
general environmental survival.

A five-copy prepared successor now enlarges the registered symbol basin from
radius one to radius two. Five 33-symbol neutral registers use 165 of the
existing 170 chart-local clear states; a sixth requires 198, so five is the
maximum copied-register clock that fits at one site. The resulting period-16
code has distance five and corrects every one-position and two-position
snapshot substitution—3,760 plus 391,840 exact rows—within two ticks while
retaining work count one or two in one fixed-occupancy A2 (14/14). This is a
real finite robustness gain. It also exposes the next obstruction: five SC
directions have chart-relative dyad `diag(2,2,1)`, whereas a six-copy equal-axis
architecture would exceed the one-site pool. Quintet formation, positive
binding/action curvature, work export/reset, occupancy errors, mid-transaction
faults, traffic, mass, canonical-Phi integration, and general stability remain
open.

### B — Born statistics

Required:

- native generation of the finite phase-compatible history bank;
- target-blind deterministic enumeration or renewal;
- one source emission assigned to one exclusive physical event without
  event-defined postselection;
- the exact `|Z_o|^2/sum |Z_r|^2` pushforward and a finite-window bound;
- apparatus records and backreaction; and
- multipartite composition with operational no-signalling.

Prepared counting identities remain conditional until the common rule forms
the preparation and detector. The isolated charged sector sharpens the
boundary: its six family/polarity cycles are disjoint, every single-orbit
label frequency is deterministic, and arbitrary convex mixtures are
invariant. Trial weights are therefore still preparation inputs. The rotor
seam is nevertheless a concrete target-blind renewal pattern: round-robin
finite histories lose their initial rotor-phase dependence as `1/N`. General
outcome-bank formation and physical heralding remain open.

The same reversible-conveyor theorem gives the general measure boundary. A
finite permutation's invariant measures are arbitrary mixtures of its uniform
cycle measures, and are unique only for one transitive cycle. Thus the
prepared coprime-ring component has a theorem-grade time frequency, while
dynamics alone still does not choose weights among distinct prepared banks.

A symmetry successor now rules out the naive global-ergodicity escape. The
centralizer of one finite transitive cycle is cyclic, whereas the 48 signed-
cubic transformations act faithfully and nonabelianly on the 384-channel v3
bank. A component cannot retain that faithful full symmetry, commute with the
successor, and be one cycle. The law may remain covariant by carrying multiple
symmetry-related cycles, but native preparation must physically select a
frame/seed, retain an environment sector, use a declared expiry basin, or
reduce to a nonfaithful symmetry quotient. An observer's later mixture does
not perform this ontic selection.

A contextual apparatus successor now realizes the permitted frame-selection
route without a new type. A selected oriented chart orders all 384 field
channels; two zero-`E/B` existing-carrier pointers run fixed coprime cycles of
lengths 384 and 385, enumerating every ordered channel pair target-blind. One
reusable balanced-ternary detector makes the full 443,520-state product a
permutation and produces one exclusive manifested Gauss-event seam per
compatible residual pair. Prepared event frequencies are exactly `|Z|^2`, and
every heralded `N`-event window has discrepancy `<B/N` (15/15). The chart
transforms with `O_h`; it does not demand a forbidden globally commuting
cycle. Native residual-bank and apparatus formation, amplification/persistent
record, reciprocal backreaction, overlapping traffic, homogeneous-Phi expiry
reset, and multipartite no-signalling remained open at that stage.

A Phi-v13 successor now closes one finite **source-history-to-bank** formation
subgate. One actual A9 source, eight retained fixed-occupancy A2 controller
bits, one A2 cursor, and eight A2 reserve occupancies write an eight-record
bank from the source's phase visits. The per-tick role delta is field `+1`, A2
reserve `-1`. All 4,096 source/controller histories and 32,768 inverse ticks
close; they form 151 distinct count vectors, every formed bank has compatible
pair count `|Z|^2`, and all 22,792 nontrivial two-history rows normalize to
Born form. Intrinsic first-clear addressing is signed-cubic covariant over
7,248 rows (12/12). This is deterministic physical bank formation, not a
chosen probability table. Formation of the source/controller/chart and
renewal apparatus, amplification/backreaction, traffic, and canonical-Phi
promotion remain open.

A prepared Phi-v14 successor now closes apparatus symbol protection and one
finite persistent-record subgate. Three copies of each coprime pointer and
three detector trits run through a READ/COMMIT majority wrapper; twelve
fixed-occupancy A2 counters retain manifested outcomes. Bank, copies,
counters, and work owner occupy 23 of one Moore block's 27 sites. All 443,520
clean parent states and 884,754 one-copy valid-symbol substitutions pass, with
generic correction work retained. Every one of the 151 Phi-v13 bank classes
writes exact `|Z|^2` click memory. A complete 147,892-step fixture returns the
live apparatus while leaving 26 physical clicks, and exact reverse evolution
clears them (13/13). This supplies a finite surviving record without a
probability primitive or fundamental erasure. Source/controller/chart and
apparatus formation, bank/occupancy faults, reciprocal detector work,
multi-block traffic, indefinite renewal, macroscopic amplification, paired
laboratory routing, and canonical-Phi integration remain open.

A selected Phi-v15 successor now closes one finite **source-history measure**
subgate. One raw 4,096-state A2 phase bank bijects with all `16*2^8` Phi-v13
source/controller histories. Two A2 owners form one transitive base-4,096
odometer over all 16,777,216 ordered history pairs. Every address forms and
inversely renews both source banks; 8,192 formation rows and 65,536 inverse
ticks pass. The resulting time measure has exact count mean `(2,2,2,2)`,
`Cov(Re Z,Im Z)=(79/16)I2`, and `E|Z|^2=79/8`. All 16,694,272 non-dark ordered
pairs retain exact Born event normalization, and reversible dark padding makes
every apparatus trial 148,096 macros long (16/16). Thus the measure is the
trial-clock count of one declared physical orbit, not an analyst-supplied
mixture. Owner formation, canonical-Phi provenance, cross-block routing,
detector backreaction, full-cycle record export, state-specific preparation,
amplification, and laboratory Bell recovery remain open.

A selected Phi-v16 successor now closes the first **owned causal paired-bank
routing** subgate. The retained two-A2 scheduler address physically precedes
both banks. Phi-v16 forms them on opposite tangent ports, attaches one existing
A2 TRANSIT owner to each, moves every complete eight-record bank one SC hop per
route tick, and hands it to a prepared A2 ENDPOINT owner with exact inverse.
All 8,192 banks, 16,384 local move/delivery rows and inverses, 1,208 finite
routes, 22,801 remote-bank-independence rows, and 7,248 covariance rows pass
(13/13). Every phase count, Gaussian integer, and `|Z|^2` cardinality survives
delivery. This supplies common origination and causal separation; it does not
form the corridors/apparatuses, integrate the writer into canonical Phi,
protect bank occupancy, arbitrate traffic, implement measurement settings,
return detector work, amplify records, or recover laboratory Bell
correlations.

A bipartite successor now closes multipartite composition and operational
no-signalling on the **prepared source-complete local sector**. Finite common-
source sectors feed two local banks whose outcome counts are native `|Z|^2`
cardinalities. Constant complete local cardinality makes both normalized
marginals and the opposite event sequence independent of the remote setting;
the certificate verifies zero drift on all four contexts. A setting/sector-
dependent incomplete-retention control shifts one sampled marginal from
`1/2` to `1/3`, pricing the postselection audit. Every one of the sixteen
setting-independent deterministic local response tables has `S=+/-2`, so
finite mixtures obey `|S|<=2` (12/12). Four uncoupled context-specific banks
without a physical cross-context record identification do not define that
common CHSH model. Native source/controller/chart formation, paired routing,
apparatus formation/protection, amplification/backreaction, traffic, and
actual Bell-correlation recovery remain open.

### R — gravity

Required:

- a native propagating tensor/transport-geometry degree of freedom or an
  explicit priced extension;
- local constraints removing nonphysical tensor components;
- universal coupling to one conserved common stress record;
- a common low-energy cone for matter and radiation;
- equal response of material and light channels, including lensing; and
- only then, nonlinear self-coupling/bootstrap toward Einstein dynamics.

The dressed source supplies a charge-even diagonal `Eg` stress. Although one
record lacks `T2g`, exact two-record composites span the full rank-five
`Eg+T2g` STF carrier and common-phase pairs span a rank-ten C4 doublet without
a new primitive type. The selected collision changes 192 pair tensors per C3
layer, so the composite is not a protected slow mode. A neutral tensor-walker
successor now transports five explicit independent STF payloads through the
deterministic rotor construction with exact zero additive `E/B`. Its blocked
history converges componentwise to the Dirichlet tensor Green field and
conditionally supplies a full-rank `1/Lambda` static pole. This is not yet an
autonomous pole. A constraint successor now supplies three independent neutral
polar-vector payloads, exact radius-one signed-cubic transport, and a
componentwise vector `1/Lambda` Green seam using the existing carrier. It also
proves that exact instantaneous finite-range TT projection is impossible: the
lattice projector has a nonremovable `1/Lambda` denominator. Gravity must
therefore propagate auxiliary constraints or declare a controlled blocked
approximation. A joint successor now composes the scalar visit unit, all five
STF components, and all three vector components in one intrinsically
recognizable ten-record neutral packet. The complete 34,560-signature census
has rank nine; one reversible radius-one rule transports the packet with exact
signed-cubic covariance and gives every component the same controlled
`1/Lambda` history kernel (14/14). Thus separate carrier composition is no
longer a gravity debt. A prepared protection successor now places three
complete copies of that transaction on parallel rails and one READ/COMMIT/work
record in an existing A2 owner. Seven Moore-block sites suffice. All 1,296
clean moves/inverses, 3,888 one-copy substitutions, 432 covariance rows, and
all 2,088 possible fault ticks in the finite Green fixtures pass (13/13). The
protected visit measure equals the parent measure exactly and therefore
inherits the same conditional common `1/Lambda` history. This closes one
finite packet-symbol protection basin, not an autonomous tensor mode. Phi
integration, rail/source/sink formation, router-background protection and work
export, propagated constraint dynamics, an interacting dynamical Hessian,
autonomous static/tensor poles, and universal coupling remain absent. The
Deser bootstrap cannot derive those missing microscopic structures; existing
selected geometric-force engine paths do not satisfy these gates.

The self-correcting matter triplet now supplies one exact static source bridge.
Its isotropic capacity deficit `-I/36` gives the selected positive rest readout
`I/36`, whose decomposition is scalar `1/12` with exactly zero vector and STF
coordinates. Six distinct signed-cubic images of one existing joint packet
sum to the pure scalar coordinate `(6,0,...,0)`. Under the common deterministic
history kernel their average obeys the finite residual bound `2/(3N)` and
conditionally gives the prepared pole `(1/12)/Lambda` (13/13). This closes a
static material-readout-to-scalar-history seam and proves tensor silence at
rest. It does not identify the readout with physical gravitational mass, form
or renew the packets under homogeneous `Phi`, generate motion stress, protect
an autonomous pole, fix the response residue, or derive a common cone,
lensing, or nonlinear gravity.

Conditional on an admitted one-tick SC displacement `u`, the motion-source
shape is now classified exactly. Signed-cubic covariance leaves one scalar,
one polar-vector shape `u`, and one STF shape `3uu^T-I`, but does not relate
their three coefficients. Selecting the Occam-minimal atomic second moment of
the finite chord ties them as `(1,3uu^T-I,u)`. The complete 34,560-row carrier
census proves that no single joint packet realizes this tied signature; two
existing neutral packets realize it minimally in every one of 48 charts. With
triplet weight `1/12`, reversal leaves scalar/STF even and flips the vector,
the deterministic residual is at most `4/(3N)`, and all components inherit
the conditional `1/Lambda` history (16/16). This closes the prepared motion-
source representation seam. The chord moment remains a **[SELECTION]**, while
the Phi-v6 prepared-sector candidate now supplies an exact translating chord
while preserving repair/work. Reciprocal creation/absorption of the two
motion packets, canonical `Phi`, physical stress/inertia, protected dynamics,
absolute universal response, cone, lensing, and nonlinearity remain absent.

### X — common prepared event seam

A cross-sector successor now composes the five finite constructions at one
prepared isolated event vertex. One exact frame/header chart, the contextual
384/385 pointer and ternary detector, one dressed charged source, and two
neutral scalar/vector/STF packets fit inside one Moore block. The bright event
and recovery form a finite permutation. Its occupancy delta is

\[
 (\Delta N_F,\Delta N_{A1,SC},\Delta N_{A1,FCC},\Delta N_{A2})
 =(8,1,0,-9),
\]

so the conditionally selected all-equal relative occupancy ray is conserved
exactly. Every direction/sign/layer event satisfies Gauss and continuity; the
charged charge-even stress is handed to two reversible neutral packets by
`T_++T_-=3S_d` and `2(v_++v_-)=3S_dq`; the whole source is signed-cubic
covariant; apparatus/forward-repair/reverse-repair admissions are disjoint;
and the physical source events retain the prepared `|Z_o|^2` counts (16/16).

This result changes the five gates only at their common finite vertex:

- **G:** one prepared source has explicit A2 resource provenance, exact
  Gauss/continuity, and recovery; homogeneous charged traffic and the pole are
  still absent;
- **C:** the relative occupancy ray now survives a physical cross-sector
  event, while `Gamma` and the interacting Hessian remain free;
- **M:** apparatus and repair admissions coexist without a local state
  conflict, and a later cubic-triplet successor supplies a prepared
  one-substitution survival basin; formation and general physical stability
  remain absent;
- **B:** compatible pointer pairs now launch actual charged/stress source
  records without changing the prepared count, but preparation,
  amplification, and no-signalling remain absent; and
- **R:** material stress now has an exact finite tensor/vector source handoff,
  but protected constraint/tensor dynamics, universal response, and lensing
  remain absent.

The theorem is
[`THEOREM_V3_MATTER_ANCHORED_BORN_GAUSS_GRAVITY_EVENT_SEAM_v1.md`](../derivations/common_action_mechanics_reciprocity/THEOREM_V3_MATTER_ANCHORED_BORN_GAUSS_GRAVITY_EVENT_SEAM_v1.md).
It is not an amendment to canonical `Phi-v2`.

The homogeneous event-halo successor now closes the prepared seam's remaining
admission, overlap-arbitration, and same-tick-broadcast debts. Seventeen exact
marker-only sites and ten writer-role sites form one radius-one halo; a
globally unique center chart code plus a retained herald gives every remote
writer a preceding-tick state-only admission. All 6,000 overlapping halo
placements have an exclusion witness, all 147,840 physical pointer states fit
the staged halo, and the complete active packing is generator-covariant. Its
active event delta is `(32,1,0,-33)` because the charged packet and both
neutral source packets are converted from a finite A2 work reserve rather than
supplied as outcome-specific preformed kits. Prepared `|Z_o|^2` counts are
unchanged (17/17).

This is
[`THEOREM_V3_HOMOGENEOUS_EVENT_HALO_AND_RENEWAL_RESOURCE_BOUNDARY_v1.md`](../derivations/common_action_mechanics_reciprocity/THEOREM_V3_HOMOGENEOUS_EVENT_HALO_AND_RENEWAL_RESOURCE_BOUNDARY_v1.md)
and the Phi-v4 event-halo **candidate**, not a canonical-law promotion. Exact
recovery leaves no persistent output. Persistent release instead debits 33
prepared work occupancies per event, so a finite reserve `R` supports at most
`floor(R/33)` releases without causal refill. Formation, persistent
amplification/refill, stable matter, protected response poles, absolute scale,
no-signalling, and lensing remain open.

The formation-boundary successor then counts the prepared hardware itself.
The registered READY halo contains 567 occupied field records and the active
halo 599. Because the selected all-equal occupancy invariant is nonnegative,
an exactly blank causal past has occupancy zero and cannot form either state by
conserving transactions. Native formation must therefore begin from a finite
nonblank genesis seed, receive causal boundary inflow, or explicitly replace
the invariant. This is an exact 13/13 obstruction and a sharper construction
target, not stable matter. A standalone identity for all 19,584 marker
presentations also needs at least three neutral controller pairs by capacity;
whether the current nine-pair marker can be compressed while retaining all
writer/covariance gates is open.

This is
[`THEOREM_V3_EVENT_HALO_FORMATION_SEED_AND_RESOURCE_BOUNDARY_v1.md`](../derivations/constituent_complete_matter/THEOREM_V3_EVENT_HALO_FORMATION_SEED_AND_RESOURCE_BOUNDARY_v1.md).
It prices the admissible starting resource; it does not add a sixth postulate
or supply the missing assembly/survival dynamics.

The next prepared-sector successor supplies a survival law without pretending
to solve assembly. Three mutually disjoint orthogonal relation clocks fit the
same radius-one chart without touching its charged frame or source relations.
Three copied neutral center heralds implement READ/COMMIT in two ticks. The
period-16 code corrects all 1,488 one-register valid-symbol substitutions and
has isotropic mean capacity deficit `-I/36` (12/12):
[`THEOREM_V3_CUBIC_TRIPLET_SELF_CORRECTING_MATERIAL_CLOCK_AND_STABILITY_BOUNDARY_v1.md`](../derivations/constituent_complete_matter/THEOREM_V3_CUBIC_TRIPLET_SELF_CORRECTING_MATERIAL_CLOCK_AND_STABILITY_BOUNDARY_v1.md).
The code remains prepared and lacks positive binding/work, occupancy-error and
traffic survival, translation, collision closure, mass, and canonical-Phi
integration. Stable matter therefore remains open.

The companion normalization boundary is
[`THEOREM_V3_TRIPLET_SYNDROME_ENERGY_AND_ABSOLUTE_NORMALIZATION_BOUNDARY_v1.md`](../derivations/constituent_complete_matter/THEOREM_V3_TRIPLET_SYNDROME_ENERGY_AND_ABSOLUTE_NORMALIZATION_BOUNDARY_v1.md).
It proves that the registered repair is flat under every audited additive
occupancy action and that a positive syndrome gap must be exported as work.
Thus the new basin does not silently solve binding or fix the absolute
electromagnetic/gravitational response multiplier.

The Phi-v5 relational-work candidate then implements the required relative
transfer using an existing A2 token:
[`THEOREM_V3_TRIPLET_RELATIONAL_REPAIR_WORK_PORT_PHI_v5_CANDIDATE_v1.md`](../derivations/common_action_mechanics_reciprocity/THEOREM_V3_TRIPLET_RELATIONAL_REPAIR_WORK_PORT_PHI_v5_CANDIDATE_v1.md).
It closes `h+w=1` and atomic backpressure on the prepared error basin, but
leaves the common coefficient and the causal fate of the EXCITED record open.

Phi-v6 then translates that actual repair/work state one SC hop per global
tick with an exact clean inverse and complete moving one-substitution basin:
[`THEOREM_V3_TRANSLATING_SELF_CORRECTING_TRIPLET_CLOCK_PHI_v6_CANDIDATE_AND_INERTIA_BOUNDARY_v1.md`](../derivations/constituent_complete_matter/THEOREM_V3_TRANSLATING_SELF_CORRECTING_TRIPLET_CLOCK_PHI_v6_CANDIDATE_AND_INERTIA_BOUNDARY_v1.md).
Its additive translation work is zero, so motion alone does not establish
inertia.

The lean Phi-v7 successor now closes the finite reciprocal motion-source
composition:
[`THEOREM_V3_TRIPLET_MOTION_PACKET_WORK_RECIPROCAL_PHI_v7_CANDIDATE_AND_RELEASE_BOUNDARY_v1.md`](../derivations/common_action_mechanics_reciprocity/THEOREM_V3_TRIPLET_MOTION_PACKET_WORK_RECIPROCAL_PHI_v7_CANDIDATE_AND_RELEASE_BOUNDARY_v1.md).
One physical neutral request consumes 24 occupancies from six existing A2
owners, advances the triplet, releases the theorem-minimum two-packet motion
source, and changes READY to EXCITED. Exact absorption restores the full
prior state. All 18,432 chart/phase rows are radius-one and signed-cubic
covariant; the exact ledgers are `(24,0,0,-24)` and `1+0=0+1` (14/14).
Persistent forward release still requires A2 refill and a work export/reset
that does not reverse the material hop. The lean layout is not yet integrated
with the full Phi-v4 event halo. Protected response, absolute normalization,
binding/inertia, traffic, and canonical-Phi promotion remain open.

The Phi-v8 finite-convoy successor separates packet **formation** from
unchanged inertial **transport**:
[`THEOREM_V3_TRIPLET_MOTION_SOURCE_CONVOY_PHI_v8_CANDIDATE_AND_PROTECTED_RESPONSE_BOUNDARY_v1.md`](../derivations/common_action_mechanics_reciprocity/THEOREM_V3_TRIPLET_MOTION_SOURCE_CONVOY_PHI_v8_CANDIDATE_AND_PROTECTED_RESPONSE_BOUNDARY_v1.md).
Two future-parallel packet lanes retain the Phi-v7 startup debit once. One
neighboring A2 owner then accepts the EXCITED record by an exact work swap,
and the reset triplet co-translates with both motion-source packets through
two prepared neutral router rails. For any declared finite horizon `H`, the
rail price is `4H` existing field records; each subsequent hop is an exact
permutation with role delta `(0,0,0,0)` and zero A2 debit (14/14). This closes
prepared finite-horizon source persistence, not native router formation,
environmental repair, inertial action curvature, an autonomous protected
gravity pole, absolute response, or canonical-Phi integration.

The finite A2-memory successor now closes the narrower ontological writeback
and initial-phase protection debts:
[`THEOREM_V3_ROTOR_GREEN_A2_PHYSICAL_MEMORY_AND_PHASE_PROTECTION_BOUNDARY_v1.md`](../derivations/gravity_cosmology/THEOREM_V3_ROTOR_GREEN_A2_PHYSICAL_MEMORY_AND_PHASE_PROTECTION_BOUNDARY_v1.md).
One constant-occupancy `A2=A9^4` supplies 4,096 phase/polarity states, used as
the signed range `-2047..+2047` plus explicit overflow. In the certified
radius-one apparatus, 108 distinct incident plaquette owners retain every SC
edge current and one additional owner retains `N`. The present carrier states
therefore obey exact unit divergence. For edge transfer norm
`K_e=||q_e^T L_D^{-1}||_1`, every initial rotor phase obeys
`|J_N-grad G_D|<=8/(3N)+8K_e/N`, and two phases differ by at most twice that
bound (15/15). The triplet scalar factor `1/12` inherits the result. This is
physical finite memory and one perturbation class, not force: the common
relative occupancy action assigns zero delta to the phase-only counter write.
Homogeneous Phi, source/sink work, traffic protection, reciprocal recoil,
absolute scale, tensor modes, cone/lensing, and nonlinearity remain open.

The Phi-v9 response-apparatus candidate then attacks reciprocal recoil without
introducing real division:
[`THEOREM_V3_A2_GREEN_PULSE_RECIPROCAL_IMPULSE_ACTION_PHI_v9_CANDIDATE_AND_FORCE_BOUNDARY_v1.md`](../derivations/common_action_mechanics_reciprocity/THEOREM_V3_A2_GREEN_PULSE_RECIPROCAL_IMPULSE_ACTION_PHI_v9_CANDIDATE_AND_FORCE_BOUNDARY_v1.md).
For physical records `(C,N)`, a finite modular accumulator emits exactly
`|C|` pulses in `12N` ticks, so the average probe impulse is `-C/(12N)`.
Every pulse writes equal-and-opposite probe/reaction momentum. Under the
explicitly selected minimum-degree phase action `p_P^2+p_R^2+W`, a
horizon-only reserve `2N^2` closes tickwise and ends at
`(-C,+C,2(N^2-C^2))`. Seven fixed-occupancy A2 owners suffice and all 192
rotor phases inherit the Green bound at `N=37` (13/13). This closes a prepared
reciprocal impulse/action ledger, not force: the phase action is not derived
from canonical Phi, the reaction is not returned to source matter, the
momentum record is not composed with actual triplet acceleration, and the
absolute multiplier remains free.

Phi-v10 now composes those carry pulses with the existing clean Phi-v6 body
hop:
[`THEOREM_V3_GREEN_PULSE_TRIPLET_MECHANICAL_DRIFT_PHI_v10_CANDIDATE_AND_INERTIA_BOUNDARY_v1.md`](../derivations/common_action_mechanics_reciprocity/THEOREM_V3_GREEN_PULSE_TRIPLET_MECHANICAL_DRIFT_PHI_v10_CANDIDATE_AND_INERTIA_BOUNDARY_v1.md).
A force-aligned prepared triplet takes a Phi-v6 hop on each pulse and only an
internal Phi-v5 clock tick otherwise. In `12N` ticks its displacement is
exactly `-C*d`, its average drift is `-C*d/(12N)`, every microtick is zero or
one SC hop, and the complete response/body history has an exact inverse. All
192 rotor phases inherit the response bound (10/10). This closes physical
position-history response on a prepared corridor. It is not acceleration:
the nonzero retained momentum causes no continuation in a later dark window.
Steering, the momentum--velocity map, inertia, reaction return, native
resources, traffic, canonical Phi, and absolute scale remain open.

Phi-v11 supplies one explicit clock-native inertia candidate:
[`THEOREM_V3_TRIPLET_CLOCK_LEGENDRE_INERTIA_PHI_v11_CANDIDATE_AND_GRAVITY_BOUNDARY_v1.md`](../derivations/common_action_mechanics_reciprocity/THEOREM_V3_TRIPLET_CLOCK_LEGENDRE_INERTIA_PHI_v11_CANDIDATE_AND_GRAVITY_BOUNDARY_v1.md).
The clean triplet's exact period `T_M=16` drives a finite A2 cadence with
exactly `|p|` signed hops per 16 ticks for `|p|<=16`, including continuation
through a later dark response window. Matching the minimum quadratic phase
action to that finite cadence selects relative coefficient `1/(2T_M)=1/32`,
gives `v=p/16`, and yields the exact block relation `F_bar=16a_bar`. Every
admitted momentum has an exact inverse and all 192 rotor phases inherit the
clock-scaled Green bound (12/12). The clock-to-inertia identification remains
a selection, not a canonical-Phi derivation; steering, reaction-body return,
general momentum/traffic, genesis-chart and force-apparatus formation,
physical units, universality,
tensor modes, cone/lensing, and nonlinearity remain open.

---

## 3. Shared bottleneck

Coupling normalization and Born statistics expose the same missing object from
opposite sides: a physically selected measure/action on finite `Phi` histories.
Gravity then asks whether the Hessian of that same object contains a constrained
tensor sector coupled to the same conserved source.

The prepared event seam and its homogeneous halo successor prove that the
charged, matter, counting, relative-occupancy, and neutral-gravity records can
share one conflict-free radius-one candidate vertex. The bottleneck is
therefore no longer prepared-sector event compatibility or local arbitration.
It is genesis-chart and source/apparatus formation beyond the exact triplet
seed branch, persistent resource flow, general environmental stability beyond
the registered one-substitution code, together with the
phase-sensitive interacting action/Hessian and protected long-range modes
that convert the now-physical finite response record into reciprocal force.

The occupancy theorems narrow, but do not replace, that object. They determine
one conditional relative role ray; multiplication of the invariant by any
positive `Gamma` leaves the finite permutation and every conservation identity
unchanged. Only an action/generator plus a blocked source-response theorem can
turn that ray into a physical coupling.

Accordingly, no separate electromagnetic, probabilistic, and gravitational
actions may be chosen. A candidate measure/action must be tested jointly for:

- electromagnetic source curvature;
- phase-compatible event counting;
- stable localized recurrence; and
- scalar/vector/tensor block response.

Uniform counting is an admissible reference measure, but invariance alone does
not select it. It becomes physical only if the finite dynamics supplies a
transitive/ergodic preparation sector or another target-independent selection
argument.

---

## 4. Stop conditions

A route closes negative if it:

- requires a target coupling, Born weight, mass, metric, or desired outcome in
  the microscopic rule or preparation;
- uses a sector action not obtained from the common history object;
- supplies only a tensor readout without a propagating constrained mode;
- calls one periodic trajectory stable matter;
- calls a prepared `|Z|^2` count a native Born derivation; or
- uses the Deser bootstrap as provenance for spin-2.

The honest outcomes are a downstream derivation, a scoped no-go, or a newly
priced finite type. Compatibility is never a fourth outcome.
