# SPEC — The Observer's Completion Map v1 (front T5)

**Status:** `[SYNTHESIS — BOUNDARY MAP]`; assembles existing results at their
tags of record; introduces no theorem, promotes nothing
**Date:** 2026-08-07 · **Parent charter:** `SCOPE_TEMPORAL_INTERIOR_PROGRAM_v1.md`
**Parents:** FTD-0336 (modulus/argument frontier), FTD-0371 (priced-import
ledger), FC-1 (M declined), OT-1.5 (BCC ℤ[i]² `[THEOREM]`), FTD-0798 §2
(free-sector Born density `[THEOREM]`), FTD-0796 (FC-1 ⇒ CHSH ≤ 2),
`DERIV_SINGLET_FROM_VOID_EVENT.md` (J→ψ `[SELECTION]`),
`AUDIT_INFINITY_REFRAME.md` (no completed totalities)

**Claim of the map (its only claim, `[SYNTHESIS]`):** the quantum
formalism decomposes, at FTD's existing tags, into components the substrate
natively seeds and components that are representation-side completions by
an observer. Nothing here moves a component across that line; the map
records where each one currently sits and what would move it.

| Formalism component | Substrate-native seed (tag) | Representation-side completion (tag) | What would move it |
|---|---|---|---|
| **the imaginary unit / complex structure** | BCC parity orbits carry `V_complex ≅ ℤ[i]²` — OT-1.5 `[THEOREM]` | the global complexification `J → ψ` — `[SELECTION]` (singlet doc) | a forcing proof that native dynamics requires the ℤ[i] structure dynamically (none exists; FTD-0242's 0/4 routes bound the analogous α question) |
| **the Born measure** | the free second-order flux carries a conserved, positive, occupation density `n = \|φ₊\|²`, Born-valued at equal occupation — FTD-0798 §2 `[THEOREM]`, **free sector only** (couplings and genesis break it, §4) | probability *as such*: P5 supplies no ensemble; the Langevin ensemble is `[IMPOSED]`; T1c `[OPEN]` (FTD-0187), engine closure gated by FTD-0200 | front T3's discrimination (`PREREG_BORN_DENSITY_UPCROSSING_v1.md`): which weighting threshold statistics follow — see the T3 execution note below |
| **superposition / the state space** | the dispositional layer `J` is continuous and primary (P3): native potentiality riding above the ternary actual | the completed infinite-dimensional Hilbert space — a totality the ontology *refuses* (undefined boundary; `AUDIT_INFINITY_REFRAME`): the observer's completion of always-extendable potentiality into an actual-infinite object | nothing — this line is constitutional; the refusal of completed infinity is a declared type, and the map records Hilbert space as permanently representation-side |
| **unitarity** | free-sector norm conservation holds; admissible couplings (real potential, U(1) link) preserve it — FTD-0798 §4 | FTD's actual couplings are additive sources (norm-breaking, measured 1.000 → 1.234); genesis is a native **projection** (37% norm loss in one act) | a native U(1) connection with substrate feedback (currently "write-only," registered) — priced elsewhere (IMP-S4 branch) |
| **collapse / measurement events** | **native**: genesis IS a threshold projection — the substrate has non-unitary actualization built in | the *measurement map* M assigning outcome statistics — **DECLINED** (FC-1); its completeness alone already forces native CHSH ≤ 2 (FTD-0796, Fine 1982) | FC-1 reversal via the priced superdeterministic door (`S ≤ min(2+3M, 4)`, FTD-0796/0329) — an owner-level constitutional act |
| **entanglement / Tsirelson** | native bound S ≤ 2 (the substrate is local-classical at its own tags) | Tsirelson `2√2` imported as standard QM conditional on the `[SELECTION]` singlet | none known; a native S > 2 is an FC-1 *falsifier*, not a prediction |

## Reading

The pattern across all six rows is FTD-0336's frontier, specialized: the
substrate owns the **seeds** — a ℤ[i] germ, a conserved occupation density,
a continuum of dispositions, a native projection event — and the observer
completes them into the **formalism**: global ψ, probability, the
infinite-dimensional space, unitary dynamics between projections. On this
map, "Hilbert space" names the observer's completion of substrate
potentiality: the representation a system builds of what could manifest,
idealized into a completed totality the ontology itself declines to
contain. That sentence is the map's thesis and carries the map's tag:
`[SYNTHESIS]` — a re-statement of registered results, falsifiable row by
row through the "what would move it" column.

## T3 execution note (2026-08-07, v1.1 locked run)

**OUTCOME E — REGIME-DEPENDENT** (verbatim from the locked runner): Stage A
passed (occupation conserved to `7.1e-14`; equal-occupation split
`0.500000 : 0.500000`; source/projection breakage documented). Stage B:
excess upcrossing weighting is amplitude-dominated with a significant,
regime-dependent shift toward occupation — Born-fraction
`0.024 [0.014, 0.036]` at `Ω·τ ≈ 0.45–0.91` rising to
`0.099 [0.090, 0.109]` at `Ω·τ ≈ 1.81–3.56` (99% CIs, non-overlapping).
**Consequence for the Born row:** at mechanism level, threshold statistics
do *not* natively perform the full `1/√(2Ω)` normalisation in any tested
regime; they perform a fraction of it that grows with mode frequency
relative to the noise bandwidth.

**v2 (same day, locked, `PREREG_BORN_DENSITY_SATURATION_v2.md`):
OUTCOME B — APPROACHES.** Five arms spanning `Ω·τ ≈ 0.64 → 78`:
Born-fraction `0.049 → 0.102 → 0.274 → 0.469 → 0.836` (strictly monotone;
final CI `[0.813, 0.861]`; descriptive crossover `Ω·τ ≈ 17`). **Within
the declared mechanism class, threshold-crossing statistics interpolate
from amplitude weighting (slow modes) to Born/occupation weighting (fast
modes): the Born rule appears as the fast-mode/slow-noise regime of
threshold manifestation, not as an axiom.** This also retrodicts the
FTD-0200 6-neighbour failure (Rice statistics measured in the wrong
regime). Residual registered question: asymptote exactly 1 vs `≈ 0.86`.
Scope unchanged — `[MEASURED — mechanism-level, imposed ensemble,
quick-check platform]`; the FTD-0200 engine campaign is now designable
with a quantitative target (`Ω·τ ≳ 30`). No substrate tag moves.

## Standing guards

No row is promoted by appearing here; the ledger rows govern. A future
quantitative wing (pricing these imports in FTD-0371's currency) is owner
work. The charter's ceiling sentence binds: the map describes structure,
and any experiential identification remains `[CONJECTURE — INTERPRETIVE]`.

**Foundational companion (2026-08-07):**
`FOUND_SELECTION_POTENTIALITY_ACTUALIZATION_CHAIN_v1.md` — the operational
scaffold beneath this map: recovers complex quantum theory at the
reconstruction level (CDP + Busch), isolates singular actualization as
the one added postulate, and refines this map's "completed Hilbert space"
row into the six-axiom import invoice of its Part H §34. Where this map
says *what* the observer completes, the chain says *from which axioms*.
