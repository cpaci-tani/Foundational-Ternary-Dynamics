# SCOPE — The Universality Programme v1

**Status:** `[DRAFT — AWAITING OWNER RATIFICATION]`; no LEDGER row is minted
by this document (row on ratification).
**Date:** 2026-08-15 (amended 2026-08-16, §1 P2 — see the amendment record, §4)
**Position:** successor main line after FTD-1009 (the two-body bion dilation
chain). Absorbs `SCOPE_TWO_BODY_LORENTZ_CAMPAIGN_v1.md` as the parent of its
P1 front (referenced, not superseded). Grounded in: FTD-0814 (one-body
universality, p = −1.000 ± 0.002), the two-category shattering
(p ∈ [−2.70, −0.94]), FTD-0815 (free-sector exact anisotropy (ka)⁴/3240),
FTD-1009 (two-body signal at 3%, verdict withheld), and the clock-gate
criterion of record (*kinetic, binding, stress-energy and boost generators
from one interacting dynamics before covariance is claimed*).
**Precedence:** LEDGER > constitution > this charter > other prose.
**Production impact:** none. No engine change is authorized by this charter.

## Thesis

> **Relativity is the shadow of dynamical unity.** Whatever is built from
> one energy functional dilates universally, hides the substrate frame, and
> obeys the cone — automatically, with lattice corrections of computable
> order. Every measured deviation from relativity is therefore a meter
> reading on how many distinct functionals the substrate actually runs.
> Lorentz invariance stops being an obligation to discharge and becomes an
> instrument pointed at the substrate itself.

## 0. Binding ceiling

This programme may not call any of the following a derivation of relativity:

1. No result here derives Lorentz invariance for FTD. Surrogate results are
   conditional on the φ⁴ one-functional class; production-engine results are
   conditional on the sectors the census classifies as common-functional.
2. Deviation coefficients are lattice-model measurements, never QED or
   experimental predictions, until an explicit bridge is separately priced.
3. All hiding results are pre-Bell. The Bell barrier stands as recorded
   (hiding ⇒ Born pushforward ⇒ Bell); nothing here touches it except P4's
   boundary formalization.
4. The adopted clock law (FC-2/FTD-1000) is tested for consistency, never
   re-derived; a clean deviation escalates as a falsifier candidate under
   replication gates, per the FTD-1009 chain's conventions.

## 1. Fronts

**P1 — The deviation model** *(CLOSED — FTD-1010, terminal state GATES
FAILED at the third strike; parent: the two-body campaign).*
Read the FTD-1009 residual structure (unlocked diagnostic — event sequences
regenerated and persisted), let the data select the deviation-model form,
lock it (PREREG v2.1), run, book. Terminal states: CONSISTENT (the adopted
law plus a booked lattice-deviation coefficient — the first quantitative
composite-clock deviation measurement) / DEVIATION CANDIDATE (replication-
gated) / GATES FAILED (hand off with diagnosis).

**P2 — The Hiding Theorem.** *(landed, and it amended this paragraph —
FTD-1011.)* For the M18 one-functional wave sector: every internal
comparison observable — built operationally from the sector's own
solutions, with no reference to continuum coordinates — is invariant under
substrate boosts **exactly at O((ka)⁰), with the leading frame leak at
O((ka)²)**, computed exactly and attributed to the **isotropic k⁴
dispersion curvature**. The neo-Lorentzian "conspiracy" as a theorem with a
computable remainder: the s-independent tower of the two-color Michelson
ratio is the γ-series term by term, so dispersion-free internal kinematics
is exactly special-relativistic; dispersion then leaks the frame two orders
earlier than this charter first drafted. Machinery: exact truncated-series
symbol algebra (`derive_hiding_internal_interferometer.py`). Failure of the
cancellation at any lower order is a major negative and is booked as such.

⚠ **The as-drafted P2 expectation was refuted by P2's own first
computation** — see §4. The (ka)⁴ figure that appeared here belongs to
FTD-0815's *anisotropy*, which this result demotes to a subleading leak;
the leading leak is isotropic and arises at (ka)². Do not cite this
paragraph's pre-amendment form.

**P3 — The Functional Census.** *(delivered — `AUDIT_FUNCTIONAL_CENSUS.md`;
its standing target list has since produced FTD-1012, the bath-frame
break chain.)* Audit-grade enumeration of every energy
term the production engine computes (the MAP_LAGRANGIAN_TO_ENGINE six-term
decomposition, the Rayleigh dissipation, the gravitational sector, the
genesis/evaporation thresholds, the selected strong sector, the EnergyAudit
surface), each classified common-functional vs second-category, each
second-category line annotated with the universality-breaking channel it
opens. The census's "where universality must break" register becomes the
programme's standing target list. `[SYNTHESIS]`; moves no tag.

**P4 — Horizon (boundary work only)** *(the only unstarted front).* (a)
Formalize what hiding requires from Born pushforward and locate exactly where
Bell blocks — boundary sharpening, no assault. (b) The α-through-time no-go:
if the C3 impossibility trend completes, close the clock route into MC-T4.3
as a priced negative.

**Ripeness assessment (2026-08-16) — (b) is NOT yet ripe.** A first pass
recorded here that the trend had "substantially completed"; a source audit
against the carrier corpus withdrew that reading the same day, and it is
retained struck rather than deleted because the error is instructive. What
the 2026-08-14/15 chain actually closed is narrower than it looks in summary:

- FTD-1004 closes six families and a 288-cell prism grid **within a declared
  scope**, and its own D3 clause names the escapes it does not touch
  (non-rectangular multi-strut quadrilaterals, non-prismatic topologies,
  larger spans/n, asymmetric wirings).
- FTD-1005 closes the ≥3-term axial escape in full generality (T1, Besicovitch)
  — genuinely unconditional, but **axial**.
- FTD-1006 closes the strutted axially-symmetric class and states its own
  residue: native C3 "survives only in genuinely **non-axial** configurations
  or chain-networked structures."
- FTD-1007 then reclassifies that entire mod-2 obstruction algebra as a
  **binary-sector** result; FTD-1006 stands as a binary-sector theorem.
- FTD-1008 closes a **four-cell menu, not the sector** (its prereg D3 says so
  in those words), and its Bricard leg is a *declared probe — a sweep report,
  not a certificate*.

Meanwhile `SPEC_CARRIER_CONSTRAINTS_v1.md`'s own current-C3 footer records two
live routes outside the registered law (**a two-scale interaction; pre-tension**),
both `[OPEN]`, and the screens state plainly that **there is no `N ≤ 6` no-go**.
The open territory is therefore not two cells but at least: the whole non-axial
class, chain-networked structures, the ternary sector beyond its first menu,
the axis-fixed Bricard branch, and the two out-of-law routes. Separately, the
"prestress-stability conjecture" has **no formal statement anywhere in the
corpus** — it exists as a name plus the definition of the object it would deny,
so any row stating it formally states it for the first time.

**Consequence for (b) — first finding.** Its stated precondition ("if the C3
impossibility trend completes") is **not met**. Writing the priced negative on
that basis would assert a route closure over an uncharacterized search space —
the precise defect that withdrew FTD-0791 and FTD-0802.

**Second finding, and the decisive one: (b) is malformed as written.** A
corpus search establishes that *"the clock route into MC-T4.3"* is **not an
object in this project**. Specifically:

- The strings `α-through-time` and `clock route into MC-T4` occur in **exactly
  one place in `docs/` — this paragraph's own front statement.** The name was
  coined here; it cites nothing.
- **No file** in the 117-document `native_time_carrier_programme/` links its
  results to α: a grep for `fine structure`, `MC-T4`, `1/alpha`, `137.03`
  across that directory returns **zero files**.
- `DERIV_QUARTIC_CLOCK_CM_GEARBOX_v1.md` (FTD-0827) — the result that would
  have to carry a clock into the α sector — contains **no occurrence** of
  `alpha`, `α`, `fine structure`, `electromagnetic`, `137`, or `MC-T4`. The
  gearbox is never connected to α in its own document.
- `SPEC_ALPHA_READOUT_CONTRACT.md`'s mechanism classes are **ARC-A**
  (boundary-condition), **ARC-B** (observable-selection), **ARC-C**
  (quantization/readout rule), **ARC-D** (discrete-native measurement).
  **None is a clock or temporal class**, and the surviving-mechanism list is
  entirely spatial/algebraic.

There is therefore no clock→α route to close. **A route nobody opened cannot
be priced as a negative** — booking one would manufacture a closure, which is
the mirror image of manufacturing a derivation and equally inadmissible.

**What actually exists.** Period/time structure *has* been argued toward α
four times, all outside this programme, in the α-readout programme, and none
at a tag above `[SELECTION]`/`[UNDERDETERMINED]`:

| route | where | status |
|---|---|---|
| tick-rate (`G*²` = energy per tick) | `FOUND_GSTAR_DYNAMICAL_ROLE.md` §1/§4 | `[SELECTION]`, self-guarded |
| one-cycle-period return map | `SPEC_ALPHA_READOUT_OSCILLATORY_CLOUD.md` | `[CLOSED NEGATIVE]` |
| odd lemniscatic period (J-twisted `det_ζ`) | FTD-0234 | `[UNDERDETERMINED]` — **live** |
| CM period lattice → master quadratic | `DERIV_MASTER_QUADRATIC_FROM_PERIOD_ALGEBRA.md` | `[THEOREM]` identities + `[SELECTION]` assembly; forcing reading withdrawn |

The live one is the third: FTD-0234 supplies a clean FTD-native odd-degree
period, and the remaining obstruction reduces to **one stated structural
identity** (whether `Det(T)` is the J-twisted `det_ζ`). That is a sharp,
single-identity frontier with its own attack scope
(`SCOPE_DET_IDENTITY_ATTACK_v1.md`) — and it is *spatial-algebraic*, not
temporal.

**Re-scoping of (b), pending owner ruling.** The defensible successor is not a
no-go but a **connection audit**: the carrier programme and the α-readout
programme have run for months with **zero cross-references**, and (b) was an
attempt to price a bridge between them that has never been built. Either
(i) drop (b) as malformed, or (ii) restate it as *"determine whether any
carrier-programme result bears on the α-readout period routes at all"* — an
open question, not a scheduled closure. **It is not (b)'s job to decide that;
this note records that the front as drafted rests on a name with no referent.**

**Parallel slow arcs** (not gating): the C3 endgame (axis-fixed Bricard
cell; the prestress-stability-of-unit-frameworks conjecture — independent
mathematical interest); the contextual-actualization door (owner-led;
`SPEC_FTD_FRAMEWORK_V2_CONTEXTUAL_ACTUALIZATION.md` territory).

## 2. Stop conditions

- P1 stops at its third gates-failed lock (the FTD-1009 chain's stopping
  discipline inherits); the residual question then routes to the engine
  replication (S2′) instead of further surrogate repairs.
- P2 stops positive at a booked theorem + verifier, or negative at a booked
  lower-order cancellation failure; it does not iterate past two failed
  proof strategies without owner review.
- P3 is single-pass; it stops when every EnergyAudit field and every
  energy-relevant toggle is classified with a source line.
- P4 items are boundary documents; each stops at one booked statement.

## 3. Deliverables

1. PREREG v2.1 + execution + row (P1).
2. `THEOREM_HIDING_ONE_FUNCTIONAL_SECTOR_v1.md` + verifier + row (P2).
3. `AUDIT_FUNCTIONAL_CENSUS.md` + row (P3).
4. Boundary notes per P4 item, each with its row.
5. On ratification: this charter's own row, and the INDEX status note
   updated to name the programme as the active main line.

## 4. Amendment record

**A1 — 2026-08-16, §1 P2 (the hiding order).** *Owed by FTD-1011; the
theorem doc named this amendment at booking time.*

| | as drafted 2026-08-15 | as computed (FTD-1011) |
|---|---|---|
| invariance holds through | O((ka)²) | **O((ka)⁰)** — exactly, term by term |
| leading frame leak at | O((ka)⁴) | **O((ka)²)**, coefficient (3/4)·(u/C)²·(ka)² |
| cause of the leak | FTD-0815 anisotropy family | **isotropic k⁴ dispersion curvature** (verifier V4: surgical removal kills the coefficient; ⟨100⟩-class arms exclude anisotropy at this order) |
| status of FTD-0815's (ka)⁴/3240 | the leading term | **subleading** frame leak |

The drafted expectation was refuted **against the programme's interest** —
the substrate frame is detectable two orders earlier than hoped, by an
apparatus-free internal procedure. This is recorded here rather than
quietly corrected because the charter's own stop condition for P2 admits a
negative outcome as a bookable result, and this is the mechanism by which
that clause fired. No tag moves: the theorem carries `[DERIVED — EXACT
SERIES COMPUTATION]` + `[CORRECTION]`, and this charter remains a direction
with no claim of its own.

**A2 — 2026-08-16, §1 P4 (the ripeness of (b)).** *Self-correction, same day,
before anything was built on it.* A first pass annotated P4(b) as ripe on the
strength of the FTD-1004→1008 chain, writing that the C3 impossibility trend
had "substantially completed." A source audit against
`SPEC_CARRIER_CONSTRAINTS_v1.md`, the four LEDGER rows, and the ternary-sector
preregistrations withdrew that reading: every closure in the chain is
scope-declared, FTD-1007 reclassifies the strongest of them as binary-sector
only, the Bricard leg is a declared probe rather than a certificate, and the
carrier spec's own footer keeps two out-of-law routes `[OPEN]`. The corrected
assessment is recorded in §1 P4.

The failure mode is worth naming because it is the programme's own: a chain of
narrow, honestly-scoped negatives reads, in summary, as a broad one. Each row
states its limits; the limits do not survive being summarized. Any future
"trend has completed" judgement should be made against the union of the
declared escapes, not against the impression left by the row titles.

**A3 — 2026-08-16, §1 P4(b) (the front rests on a name with no referent).**
Following A2, a corpus search asked what the "clock route into MC-T4.3"
actually cites. Answer: nothing. The phrase occurs only in P4(b) itself; the
117-document carrier programme contains no reference to α, the fine-structure
constant, or MC-T4.3; FTD-0827's gearbox never mentions α; and the α-readout
contract has no clock or temporal mechanism class. The four genuine period→α
arguments all live in the α-readout programme and are tabulated in §1 P4.

This is a **charter-authoring defect, not a research result**: a front was
scheduled to close a route that had never been opened, and its precondition
was borrowed from a different programme's no-go arc. Recorded because the same
defect would have produced a booked row asserting a closure — the mirror image
of a manufactured derivation, and no more admissible. The standing lesson is
that a front's object should be required to cite something before the front is
scheduled.

**Standing caution.** Three fronts (P1, P2, P3) delivered booked rows —
FTD-1010, FTD-1011, FTD-1012 — while this charter sat unratified. The rows
stand on their own locks and verifiers, not on this document; ratification
would regularize the bookkeeping, not retroactively license the results.

---

*This charter registers a direction. It proves nothing, promotes nothing,
and prices nothing by itself; every claim it motivates is booked at the tag
its own evidence earns, under the standing epistemic discipline.*
