# FTD-0753 — Explicit-rounding causal-horizon M2 witness v1

**Status:** `[PRE-REGISTRATION — LOCKED BEFORE EXECUTION]`  
**Date:** 2026-07-30  
**Parent:** FTD-0752 `[CONSTRUCTIVE NUMERICAL FACT — BOUNDED BACKEND GATE CLOSED]`  
**Scope:** one fresh large-volume environmental-persistence witness for the
selected reciprocal `(s,C,F)` candidate; no M3 family, particle, charge,
Lorentz, pole, or production claim

## 1. Question and non-question

Starting from the unchanged finite-support compact pair, does one newly
executed trajectory on each principal cubic ray retain a localized energetic
core and stable near field while a detached disturbance reaches radius 48 and
continues outward before any registered exterior return?

This campaign does **not** define the state-only M3 object predicate. In
particular, it does not infer a unique bound/outgoing/background field
decomposition from radius alone, prove an open basin, establish an
infinite-volume limit, or test autonomous translation.

## 2. Frozen implementation

Use the FTD-0750 ordered one-writer-per-face current deposition and
deterministic selected-radius observer with the FTD-0752 research-only
explicit-rounding CUDA library compiled using `--fmad=false`. The established
`ftd_cuda` library, production tick, default toggles, field equations, compact
interaction, tolerances, and scenarios remain unchanged.

The obsolete CPU-prefix equality gate from FTD-0747--0750 is not part of this
physical question. It is neither weakened nor reinterpreted: FTD-0752 closed
the bounded backend arithmetic prerequisite, and FTD-0753 executes a fresh
trajectory whose result stands on its own.

## 3. Frozen arms and horizon

Run exactly one face, edge, and body arm:

- directions `(0,0,1)`, `(0,1,-1)`, and `(1,1,1)`;
- plus-minus compact pair, separation `1.30`, inward momentum `0.0120`;
- periodic `L=321`, finite-support preparation radius 4;
- `dt=1/4`, live `C_SPEED`, compact-pair depth `0.01`, cutoff squared
  `3/2`, shared-anchor chart;
- gate tolerance `1e-10`, solve tolerance `2e-14`, 384 root iterations;
- sparse local current/residual evaluation;
- ticks `0..312`, registered exterior-contact tick 313;
- radii `{8,12,16,24,32,48}`.

Qualification may run at most eight ticks and writes no result artifact. The
directory `engine/results/ftd_0753/` must be absent before the three registered
arms execute.

## 4. Frozen gates

The first failed gate fixes the arm verdict.

### H0 — execution and causal isolation

Preparation, initial state, all 312 forward transactions, regional ledgers,
source support, and common-action diagnostics must be valid. The maximum
source radius is 3, the horizon precedes contact, maximum common residual is
at most `1e-10`, total-energy residual at most `1e-8`, recoil defect at most
`1e-9`, speed excess at most `1e-12`, regional residual at most `1e-10`, and
pair-plus-field balance at most `1e-8`.

### A0 — canonical current aggregation

Every ordered-current aggregation is valid, discarded L1 current is at most
`1e-10`, aggregation first-moment residual is at most `1e-12`, and net source
support remains within radius 3.

### H2 — persistent energetic core

The graph-inside pair energy must become negative and remain negative for at
least 160 terminal ticks.

### H3 — stable late near field

During ticks `281..312`, radius-eight inside field energy must remain at least
`5e-4`; its maximum may not exceed four times its minimum.

### H4 — causal radius-48 arrival

Radius-48 outside energy begins at at most `1e-12`, exceeds `1e-8`, first does
so by tick 300, and no source exchange occurs outside the compact support
above `1e-10`.

### H5 — post-arrival outward persistence

From tick 301 through 312, radius-48 outside energy remains above `1e-9`.
The minimum registered outward increment after first passage is at least
`-1e-10`.

The three-ray conjunction is constructive only if every arm passes
H0/A0/H2/H3/H4/H5. No CPU-prefix, repeated-run, inverse, field-component, M3,
or motion condition may be silently added to or removed from this verdict.

## 5. Verdict map

The ordered arm verdicts are:

1. `M2_HORIZON_EXECUTION_INVALID`;
2. `M2_HORIZON_CURRENT_AGGREGATION_INVALID`;
3. `M2_HORIZON_CORE_NOT_PERSISTENT`;
4. `M2_HORIZON_NEAR_FIELD_NOT_STABLE`;
5. `M2_HORIZON_R48_ARRIVAL_FAIL`;
6. `M2_HORIZON_POST_ARRIVAL_NOT_PERSISTENT`;
7. `M2_CAUSAL_HORIZON_WITNESS_CONSTRUCTIVE`.

A constructive conjunction establishes a finite, three-ray, large-volume
environmental-persistence witness and permits the M3 observer discovery split.
It does not close full M2 volume stability unless reconciled with the earlier
`L=145/193` records, and it cannot close M3 without a frozen state-only
predicate and nonzero perturbation neighborhood.

Failure closes this horizon witness under the frozen candidate. It does not
authorize tolerance relaxation, force amplification, or a post-hoc new field
separator.

## 6. Records

Write one main CSV/JSON pair and one support CSV/JSON pair per arm under
`engine/results/ftd_0753/`, followed by a hash manifest and an independent
Python certificate. Every record stores the protocol hash, backend identity,
volume, horizon, contact tick, radii, thresholds, gate booleans, extrema, and
verdict.
