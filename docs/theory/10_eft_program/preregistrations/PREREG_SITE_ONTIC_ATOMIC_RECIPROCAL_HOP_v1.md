# Pre-registration — Site-ontic atomic reciprocal hop v1 (FTD-0599)

**Status:** `[PRE-REGISTRATION — LOCKED / PRE-EVALUATION]`  
**Date opened:** 2026-07-26  
**Parent:** FTD-0598  
**Priority:** active Number-One research gate  
**Production changes permitted before closure:** none

This protocol fixes the candidate before implementation. No result may be
reported under FTD-0599 until the hash of this complete pre-execution content
is recorded at the end of the file.

## 1. Question

Can the existing isolated persistent state

```text
manifested matter: site-valued s plus velocity/remainder information
native field:       J and W
```

support one newly selected **atomic** transaction that determines, in one
coupled solve,

1. a stationary endpoint or one of the 26 signed Moore-neighbour endpoints;
2. matter momentum before and after;
3. the complete transported polarity current;
4. the native field update;
5. reciprocal matter impulse and field recoil;
6. exact declared energy exchange; and
7. the inverse from final persistent state alone?

The transaction may use transient path/current/solver stages. It may not add a
persistent coordinate, route label, clock phase, history bit, constituent
record, connection, counterforce, or reset.

## 2. Capability boundary

FTD-0599 tests only whether a no-new-persistent-variable reciprocal hop exists.
A positive result would not establish a stable particle, electromagnetism,
microscopic `U(1)`, Coulomb behavior, a photon, a common Lorentz cone, or
multibody matter.

A negative result closes only the registered site-ontic atomic construction.
It triggers the FTD-0598 escalation order:

1. the already constructed position-based `(X,p,q;E_f,B_e)` experiment;
2. constituent-complete phase space for generic matter;
3. a link connection/holonomy type and electric conjugate for microscopic
   electromagnetic matter.

## 3. Frozen reusable results

The construction may reuse only the following already registered facts:

- FTD-0574: the source-free `(J,W)` tick is a local discrete variational map,
  and `W` is its canonical momentum;
- FTD-0576: `R=J-W/2` is the unique linear field-work coordinate and gives the
  exact conditional total-energy identity;
- FTD-0577: the selected radius-one Moore coat and face-to-central bridge give
  exact finite-range continuity without changing primitive ternary `s`;
- FTD-0580: the positive endpoint chord and democratic shortest-path current
  close endpoint centering and all 26 Moore directions;
- the production dispersion
  `H(p)^2=E_REST^2+C_SPEED^2|p|^2` and its one-to-one velocity/momentum map in
  the isolated causal sector.

The construction must retain, not subtract, the registered FTD-0580/0581
Peierls energy. Traversal must pay and recover that energy dynamically.

## 4. Excluded predecessor repairs

The following are prohibited inputs rather than available tuning choices:

- the frozen `phase_forces` and every Poisson, Lorentz, emergent-force,
  gravity, color, strong, or damping branch;
- the non-injective production threshold carry used in FTD-0497;
- the explicit dressing-history fibre used in FTD-0496;
- the endpoint record as an input to reversal;
- the frozen FTD-0536 action, whose cusp and energy defects are closed;
- the quadratic-coat position ontology of FTD-0550--0555;
- self-field subtraction, a Peierls counterterm, force amplification, external
  packet reset, imposed clock, or route-specific retuning;
- a connection/holonomy field or constituent register.

## 5. Frozen atomic map

### 5.1 Persistent coordinates and canonical chart

The persistent one-carrier state is

\[
 Z=(n,r,u,J,W,q),\qquad q\in\{-1,+1\}.
\]

Mechanical momentum is not added to state. It is the derived inverse of the
production dispersion,

\[
 H(p)=\sqrt{E_{\rm REST}^2+C_{\rm SPEED}^2|p|^2},\qquad
 u(p)=\frac{C_{\rm SPEED}^2p}{H(p)}.
\]

The effective coordinate is `X=n+r`. FTD-0599 replaces the overlapping
production chart only inside the observer candidate by the half-open cell

\[
 r\in[0,1)^3,\qquad n=\lfloor X\rfloor,\qquad r=X-\lfloor X\rfloor.
\]

This is a selected injective commit, not a claim that the production chart is
gauge. Periodic array indices are wrapped, while the one-step displacement is
the unique minimum image. Because `|u|<C_SPEED<1`, every accepted component
crosses at most one plane and

\[
 d=n_1-n_0\in\{-1,0,1\}^3.
\]

The 27-way endpoint decision is therefore kinematic rather than an ordering of
gauge-dependent open actions.

### 5.2 Exact deposited source

For an endpoint pair `X_0,X_1`, use the already selected FTD-0478/0577 maps:

\[
 \rho_a=B_M\rho_{\rm CIC}(X_a,q),\qquad
 Q=\mathcal A K_f[X_0\to X_1;q],
\]

where `K_f` is the exact straight-segment oriented face current and
`mathcal A` is the finite Moore face-to-central bridge. Thus

\[
 \rho_1-\rho_0+DQ=0
\]

exactly. Fix

\[
 \bar\rho=\frac{\rho_0+\rho_1}{2},\qquad
 \boxed{S=-G_CG\bar\rho+G_CCQ}.
\]

No source multiplier or route coefficient is fitted. `S` is deposited once in
the native production ordering,

\[
 \boxed{W_1=W_0-KJ_0+S,\qquad J_1=J_0+W_1},
 \qquad K=-C_{\rm WAVE}^2L_{18}.
\]

### 5.3 Vector recoil determines matter impulse

Use the registered native translation-momentum functional

\[
 P_i^{\rm field}(J,W)=-\langle W,D_iJ\rangle.
\]

The source-free kick-drift preserves it exactly. The source kick acts at fixed
`J_0`, so periodic skew-adjointness gives the exact field change

\[
 \Delta P_i^{\rm field}=-\langle S,D_iJ_0\rangle.
\]

The matter equation is frozen to its single-action reciprocal partner,

\[
 \boxed{p_{1,i}-p_{0,i}=I_i(X_0,X_1;J_0)
       :=\langle S,D_iJ_0\rangle}.                 \tag{1}
\]

Equation (1), not scalar work division, determines all three momentum
components. By construction

\[
 \Delta p+\Delta P^{\rm field}=0
\]

before floating reduction. This is the FTD-0468 identity with the exact moving
midpoint density/current source substituted; it is not a legacy force.

### 5.4 Kinematic closure and nonlinear root

Use the exact discrete gradient of the production dispersion,

\[
 \bar u(p_0,p_1)=
 \frac{C_{\rm SPEED}^2(p_0+p_1)}{H(p_0)+H(p_1)},
\]

which obeys

\[
 H(p_1)-H(p_0)=\bar u\cdot(p_1-p_0).
\]

The atomic trajectory is

\[
 \boxed{X_1=X_0+\bar u(p_0,p_1)}.                 \tag{2}
\]

Substituting (2) into the source gives the three-dimensional fixed-point root

\[
 \boxed{F(p_1)=p_1-p_0-I[X_0,X_0+\bar u(p_0,p_1);J_0]=0}. \tag{3}
\]

Only roots in `[-2,2]^3` are admitted. The registered solver is damped Newton
with a central Jacobian step `2^-20`, residual target `1e-13`, maximum 128
iterations, and backtracking factors `1,1/2,...,2^-20`. Root enumeration uses
the Cartesian starts `p_0+{-0.5,0,+0.5}^3`, clipped to the root box, plus
`p_0`. Roots within `1e-10` are one root. Any arm with zero roots, more than
one root, Jacobian condition number above `1e10`, or failure of an independent
interval/Krawczyk enclosure is negative, not tunable.

The independent verifier must split every box at crossed CIC chart planes and
prove exactly one root in the accepted component and no other admitted root.
If that proof cannot certify the numerical classification, the protocol is
invalid rather than positive.

### 5.5 Independent energy kill test

The recoil equation is not permitted to define the energy. Use the previously
derived state functions

\[
 H_f(J,W)=\frac12\langle W,W\rangle
 +\frac12\langle J,KJ\rangle
 -\frac12\langle W,KJ\rangle,
\]

\[
 R=J-\frac12W,\qquad
 U_{\rm int}(\rho,R)=-G_C\langle\rho,DR\rangle,
\]

and

\[
 \boxed{E_{\rm tot}=H(p)+H_f(J,W)+U_{\rm int}(\rho,R)}. \tag{4}
\]

For diagnosis also compute the exact FTD-0576 matter-work target

\[
 W_m=G_C\langle Q,GD\bar R-C\delta R\rangle.       \tag{5}
\]

The two independent residuals are

\[
 \epsilon_E=E_{{\rm tot},1}-E_{{\rm tot},0},\qquad
 \epsilon_W=H(p_1)-H(p_0)-W_m.                     \tag{6}
\]

Both must be `<=1e-12` relative to `max(1,|E_tot,0|)`. No shadow cross term,
source-timing parameter, lapse, self-field subtraction, or longitudinal
energy correction may be introduced after evaluation. This is the decisive
compatibility test between exact native recoil and exact native work.

### 5.6 State-only inverse

The inverse receives only `Z_1`. It may not receive `d`, `Q`, `S`, `p_0`, a
forward root, or an event journal. For a candidate `p_0`, set

\[
 X_0=X_1-\bar u(p_0,p_1),
\]

reconstruct `rho_0,rho_1,Q,S`, and then reconstruct

\[
 J_0=J_1-W_1,\qquad W_0=W_1+KJ_0-S.
\]

Solve the reversed form of (1) over the same root box and enumeration rules.
The inverse passes only if one root exists and recovers the canonical site,
remainder, velocity, `J`, and `W` without using any forward record.

## 6. Locked arms and gates

### 6.1 Initial dressing

Every non-vacuum arm begins from a self-consistent static native dressing, not
from a zero field that creates energy for free. For the initial endpoint set

\[
 S_0=-G_CG\rho(X_0,q),\qquad KJ_*=S_0,qquad W_*=0,
\]

using zero-mean conjugate gradient to relative residual `<=1e-13`. The solve
must be reproduced independently. No dressing is translated or reset after
the first state.

### 6.2 One-event arms

- `L in {17,33}` and both polarities;
- four stationary dressed controls at remainder `(1/2,1/2,1/2)` and `p_0=0`;
- all 26 signed Moore directions with zero added field: active components of
  `r_0` are `0.95` for positive and `0.05` for negative motion, inactive
  components are `0.5`; `|u_0|=0.15` along the registered direction;
- all 26 directions from rest with the same chart convention but active
  remainders `0.999`/`0.001`, plus the deterministic longitudinal bias

\[
 J_{\rm bias}(x)=\frac{d}{\sqrt D}
 \sin\left(\frac{2\pi}{L}d\cdot(x-x_c)-\frac\pi2\right),
 \qquad D=|\{i:d_i\ne0\}|,
\]

  added to `J_*`, with no added `W`;
- three translations of one generic edge arm and all 24 proper cubic rotations
  of one body-diagonal arm;
- polarity mirrors use identical field geometry and opposite `q`.

The locked expectations are: stationary arms select `d=0`; ballistic and
bias arms select the registered nonzero `d`; every nonzero arm satisfies
`|u_bar|<C_SPEED` and crosses no unregistered plane.

### 6.3 One-event acceptance

Require:

- exactly one certified forward and inverse root per arm;
- exact primitive polarity transfer and canonical chart commit;
- current continuity, source replay, field update, recoil, cubic covariance,
  translation covariance, and inverse residuals `<=1e-12`;
- both energy residuals in (6) `<=1e-12` relative;
- source support contained in the endpoint Moore supports plus one production
  stencil shell;
- observer neutrality: production state, RNG, defaults, toggles, scenarios,
  and golden hashes unchanged.

Any conjunctive failure closes the one-event candidate. No tolerance or
fixture is changed afterward.

### 6.4 Repeated map

Run only if every one-event gate passes. Compose the same map for 256 events
without dressing refresh on `L=17,33,65`:

- stationary dressed control;
- axial, edge, and body trajectories from the locked `|u_0|=0.15` state;
- both polarities, translated copies, and proper-cubic mirrors;
- forward evolution followed by the state-only inverse for 256 events;
- initial kinetic energy below, at, and above each locked FTD-0581 depinning
  threshold;
- the longitudinal bias in phases `-pi/2,0,+pi/2` fixed before execution.

Require at least 16 legitimate site hops in every above-threshold ballistic
arm, zero hops in the stationary and below-threshold controls, constant
persistent state size, no refresh/reset, bounded dressing second moment, exact
discrete-state recovery, continuous inverse residual `<=1e-10`, and per-event
recoil/continuity/energy residuals `<=1e-12`. The active field energy must
return within `1e-10` after every complete forward/inverse hop cycle.

## 7. Outcome map

- If every one-event and repeated gate passes:
  `SITE_ONTIC_ATOMIC_RECIPROCAL_HOP_REPEATS_WITHOUT_NEW_PERSISTENT_TYPE`.
- If root count, canonical commit, current, field update, recoil, energy, or
  state-only inversion fails in the one-event campaign:
  `SITE_ONTIC_NATIVE_RECOIL_MAP_FAILS_ATOMIC_COMPATIBILITY`.
- If one-event gates pass but the repeated map fails:
  `SITE_ONTIC_ATOMIC_MAP_FAILS_REPEATED_RECIPROCITY`.
- If independent root certification or frozen-provenance checks fail:
  `PROTOCOL_INVALID`.

Either negative verdict closes this registered R0 candidate and triggers the
FTD-0598 escalation boundary. It does not prove that every imaginable law on
the same variables is impossible and does not automatically adopt the larger
ontology.

## 8. Required artifacts

- `engine/include/ftd/eft/site_ontic_atomic_reciprocal_hop.h`;
- `engine/src/eft/site_ontic_atomic_reciprocal_hop.cpp`;
- `engine/tests/test_site_ontic_atomic_reciprocal_hop.cpp`;
- `scripts/proofs/proof_site_ontic_atomic_reciprocal_hop.py`;
- versioned JSON/CSV run of record under `engine/results/ftd_0599/`;
- theorem/closure record and hostile audit;
- canonical ledger, tracker, index, current-state, engine-spec, and changelog
  reconciliation.

## 9. Priority exclusions

Until FTD-0599 closes, do not promote or begin:

- new Scale-0 reciprocal-motion or photon scenarios;
- Lorentz/common-cone campaigns;
- new structural constants, mass matches, or geometric identifications;
- a production toggle or GPU implementation;
- the constituent or connection ontology branches.

## 10. Frozen production provenance

```text
phase_read.cpp                    D9B521C1DE6503987E5DB3D91A8B4F2DFE52289E527352A8011C4146C71FB8A8
phase_write.cpp                   2C519C4EF52614E383C4494CBE1F26A7CE33036A0924EBEFF80778021FCB57A4
phase_movement.cpp                6149B37C5A28B8EE9B8544CAEC24006D0964D1C8F344CA63C68DC6536A47E8FB
field_operators.h                 25866EFC8474A2AEF7443C5DA67CBF79BEB352DE3E342A97D1EF0C3C16439E48
native_energy_contract.h          3DB8F2DC573E7F4A87E17409878915E7B5A52CE1673713998C544516E0175621
face_current_segment.cpp          EA92AD25A66BA60F0CF0BF0BBEB3D63A4064276E9B45CF0E8C6D9075418E59EC
minimal_moore_compatibility_coat  4D8C24056497619736F3FE47D67E3B261A373874F1FB692EE10620790EE6BD7E
```

Any production or inherited-observer hash change makes the run invalid.

## 11. Protocol lock

The SHA-256 below hashes the complete UTF-8 file content through the newline
immediately before the `protocol_sha256` line. Implementation and evaluation
begin only after this value is recorded.

`protocol_sha256=DDD146E19C06E488C584AFBAB4092FB802E72F4DFC13F12407A5A914704E8886`
