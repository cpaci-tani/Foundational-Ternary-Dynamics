# PREREG — Genesis amplitude lock and common-action obstruction (FTD-0565)

**Status:** `[PRE-REGISTERED — EXACT OBSERVER / FROZEN PRODUCTION MAP]`  
**Date:** 2026-07-26  
**Parent results:** FTD-0425, FTD-0467, FTD-0564  
**Production effect:** none. This record authorizes observer-only algebra, source-provenance checks, and documentation reconciliation. It does not modify the production tick, defaults, scenarios, ontology, force law, or stochastic streams.

## 1. Frozen question

Does the current production genesis/evaporation transaction supply the missing nonlinear mechanism identified by FTD-0564: a unique post-genesis flux amplitude and a conservative, invertible common matter–field action on the frozen `(s,J,W)` variables?

The observer must analyze the rule exactly as written in `phase_write.cpp`. It must not fit a potential, change the threshold, condition on a preferred physical target, or introduce a reservoir variable after seeing the result.

## 2. Frozen production maps

For the single-substrate accepted genesis event, write

```text
r = |J| = kg + x, x > 0,
W' = (1-d) W,
J' = (1-kg/r) J,
p_accept(x) = 1-exp(-x/km).
```

The locked algebraic consequences to test are

```text
|J'| = x,
Delta H_J = 1/2 (|J|^2-|J'|^2) = kg*x + kg^2/2,
Delta H_W = 1/2 (|W|^2-|W'|^2) = (d-d^2/2)|W|^2.
```

For the dual-substrate accepted genesis event, the frozen code calls `manifest_at()` without either drain, so `J'=J` and `W'=W`.

For evaporation, the frozen code erases `state`, `particle_id`, `spin`, and `color` while leaving `J`, `W`, velocity, remainder, and latency unchanged. FTD-0425's signed-preimage witness is an admitted exact dependency and must be source-hash locked.

## 3. Locked arms

Use normalized structural parameters

```text
kg = 1,
km = 1,
x in {1/8,1/4,1/2,1},
|W|^2 in {0,1/4},
d in {0,1/2},
polarity in {+1,-1}.
```

Execute all `4 x 2 x 2 x 2 = 32` single-substrate arms. Execute `4 x 2 x 2 = 16` dual-substrate arms over the same `x`, `|W|^2`, and polarity values. Polarity must not alter any scalar energy or amplitude identity.

The observer also evaluates the written local action at zero velocity and zero divergence for uniform fields with magnitudes `kg/2` and `2kg`. At fixed `J`, candidate-state dependence is frozen to the actual implemented terms

```text
L_s = born_infeld_term(s) + G_C*s*divJ - LAMBDA_G*(divJ-s)^2.
```

All uniform-field kinetic and gradient terms are candidate-state independent. The production eligibility predicate is `|J|>kg`.

## 4. Locked gates

To tolerance `1e-12`, the observer and independent Python proof must establish:

1. every single-substrate arm satisfies `|J'|=x`;
2. at least four distinct post-event amplitudes occur at one fixed `(kg,km,d,|W|^2)`, so genesis is not an amplitude projection;
3. `Delta H_J=kg*x+kg^2/2` and `Delta H_W=(d-d^2/2)|W|^2` in every arm;
4. at fixed `d=0` and `W=0`, the withdrawn energy varies with `x`, so no one fixed ternary-state rest-energy quantum balances every accepted event;
5. the acceptance probability is strictly increasing in `x` but never changes the deterministic conditional output `|J'|=x`; therefore conditioning on acceptance cannot create a universal amplitude for arbitrary incoming data;
6. every dual arm leaves field/wave energy unchanged while changing `s`, so the dual path contains no implemented latent-heat payment;
7. the evaporation map has two distinct signed preimages with identical post-event frozen state and no compensating field update;
8. the written action assigns identical candidate-state values to the below-threshold and above-threshold uniform-field arms, while the production eligibility predicates differ;
9. at `divJ=0`, the written action is exactly degenerate between `s=+1` and `s=-1`, while production resolves the zero-divergence tie as `s=-1`;
10. the active Lagrangian implementation contains no genesis threshold, exponential acceptance law, kinetic drain, evaporation erasure, or `L_HIGGS` term; the current action regression explicitly scopes itself to field stationarity and production-force replay;
11. source hashes lock `phase_write.cpp`, `lagrangian.h`, `lagrangian.cpp`, `energy_ledger_compute.cpp`, `test_action_stationarity.cpp`, and `test_native_injectivity_gate.cpp`.

## 5. Frozen verdicts

- **OBSTRUCTION:** all gates pass. Production genesis does not lock charge amplitude and is not generated by the written conservative action. On frozen variables, genesis/evaporation cannot close the conservative invertible common-action mechanism required by FTD-0564.
- **AMPLITUDE LOCK:** the accepted single-substrate map produces one `x`-independent post-event amplitude and balances one fixed matter-energy quantum. The obstruction is rejected.
- **WRITTEN ACTION MATCH:** the implemented candidate-state action distinguishes the two uniform-field arms exactly as the production threshold does, and includes the stochastic/drain/erasure transaction. The action-scope objection is rejected.
- **PROVENANCE INVALID:** any frozen source assertion is false. No verdict is licensed until the current map is rederived.

## 6. Licensed conclusion if OBSTRUCTION

The result closes only the claim that the current production genesis/evaporation rules provide the missing conservative amplitude lock. It does not prove that manifestation is impossible or that an open-system stochastic dynamics is invalid.

A surviving construction must add one of the following explicitly rather than infer it from the current code:

1. an extended event/environment reservoir that stores the event choice, drained energy, and erased labels; or
2. a selected non-conservative stochastic effective law, honestly not a reversible common action; or
3. a new nonlinear bound-state action whose stationary defect family fixes flux magnitude and whose transaction is derived before implementation.

Options 1 and 3 add structure relative to the frozen `(s,J,W)` state. None is authorized by this preregistration.

## 7. Required artifacts

- `engine/include/ftd/eft/genesis_action_obstruction.h`
- `engine/src/eft/genesis_action_obstruction.cpp`
- `engine/tests/test_genesis_action_obstruction.cpp`
- `scripts/proofs/proof_genesis_action_obstruction.py`
- `engine/results/ftd_0565/windows_msvc_cpu.json`
- `docs/theory/10_eft_program/derivations/THEOREM_GENESIS_ACTION_OBSTRUCTION.md`
- `docs/theory/07_assessment/AUDIT_GENESIS_ACTION_OBSTRUCTION.md`
