# FTD-0556 — Integer Translation and Bloch Transport v1

**Status:** [PRE-REGISTRATION — LOCKED/RUN; POSITIVE FREE-FLUX VERDICT]  
**Date locked:** 2026-07-26  
**Scope:** observer-only analysis of the frozen isolated `FULL`-stencil flux-wave map  
**Production changes:** none

**Execution note:** The first executable invocation auto-selected the CUDA
backend and is therefore `PROTOCOL_IMPLEMENTATION_INVALID` under §2.  It is not
a run of record and carries no verdict.  The fixture was corrected only by
adding `RenderBridge::force_cpu()`; no formula, sample, threshold, or verdict
rule changed.  The next invocation is the locked CPU run.

## 1. Question

FTD-0554 ruled out a continuously parameterized, finite-range, scalar unitary
translation operator except for integer shifts.  This protocol tests the
distinct local escape: a field with an internal two-component state can have a
nontrivial Bloch band while every microscopic update remains an integer-lattice
operation.

The campaign must distinguish three statements:

1. exact integer translation covariance of the update;
2. continuous motion of an extended wavepacket centroid generated by a Bloch
   phase;
3. transport of a stable manifested matter configuration.

Only statements 1 and 2 are in scope.  Statement 3 remains open even if every
gate below passes.

## 2. Frozen map

Disable all toggles, then enable only `wave_propagation`, use the periodic
boundary, the production `FULL` stencil, unit tick, single substrate, and the
default kick-drift branch.  For one Fourier mode and one Cartesian flux
component define

\[
 M(\mathbf k)=4-\frac23\sum_i\cos k_i
 -\frac23\sum_{i<j}\cos k_i\cos k_j,
 \qquad a(\mathbf k)=C_{\rm WAVE}^2M(\mathbf k).
\]

The registered transfer map is

\[
 \binom{J'}{W'}=
 U(\mathbf k)\binom{J}{W},\qquad
 U(\mathbf k)=
 \begin{pmatrix}1-a&1\\-a&1\end{pmatrix}.
\]

No alternate integrator, Floquet toggle, damping, coupling, Gauss projection,
reaction, force, movement, or dual-substrate term may be enabled.

## 3. Analytic statements locked before execution

### 3.1 Scalar locality boundary

For a homogeneous one-component finite-range first-order update that preserves
the scalar `l2` norm, its Bloch symbol is a finite Laurent polynomial of unit
modulus.  The extreme autocorrelation coefficient proves that the symbol is a
phase times one monomial.  Consequently a nontrivial dispersive local stable
band requires at least one of:

- an internal fiber of dimension at least two;
- one-step temporal memory, equivalently a second-order recurrence;
- nonlocality; or
- loss of exact norm preservation/invertibility.

This statement is not a no-go against local waves.  It is a type boundary on a
scalar first-order state.

### 3.2 Native two-component escape

The registered matrix obeys

\[
 \det U=1,\qquad \cos\theta=1-\frac a2,
 \qquad \theta=2\arcsin\!\left(\frac{C_{\rm WAVE}\sqrt M}{2}\right),
\]

and preserves the positive quadratic form

\[
 I_{\mathbf k}=|W|^2+a|J|^2-a\operatorname{Re}(J^*W)
\]

when `0<a<4`.  Thus `(flux,wave_vel)` is a two-real-component symplectic
fiber for each flux polarization.  Primitive site state `s` alone is not this
fiber.

### 3.3 Continuous Bloch centroid without fractional microscopic sites

For a single-branch complex packet

\[
 J_n(t)=\frac1L\sum_m A_m e^{ik_m n-i\theta_m t},
\]

define the exact periodic first-harmonic centroid

\[
 Z(t)=\sum_n |J_n(t)|^2e^{2\pi i n/L},\qquad
 X_c(t)-X_c(0)=\frac{L}{2\pi}\arg\frac{Z(t)}{Z(0)}.
\]

This displacement is continuous and need not be an integer.  In the
large-volume narrow-band limit its velocity is the spectral average of

\[
 v_i(\mathbf k)=\partial_{k_i}\theta
 =\frac{C_{\rm WAVE}^2\,\partial_{k_i}M}{2\sin\theta},
 \quad
 \partial_{k_i}M=\frac23\sin k_i
   \left(1+\cos k_j+\cos k_k\right).
\]

This is an extended-wave centroid, not a fractional site occupation and not a
matter trajectory.

### 3.4 Infrared cubic expansion

With `r2=sum_i k_i^2`, `S6=sum_i k_i^6`, and
`T42=sum_(i!=j) k_i^4 k_j^2`, the exact Taylor coefficients through sixth
order are

\[
 M(\mathbf k)=r^2-\frac{r^4}{12}
 +\frac{S_6+5T_{42}}{360}+O(k^8).
\]

The fourth-order term is rotationally invariant; cubic anisotropy first enters
`M` at sixth order and the group velocity at relative order `k^4`.  For a
gapped nondegenerate cubic band the leading momentum correction is quadratic,
so a common relativistic matter cone still requires additional branch/type
structure and is not inferred from this campaign.

## 4. Locked observer campaign

### 4.1 Algebraic mode arms

- `L = 17`;
- mode numbers `n = 1,2,3`;
- representatives `<100>`, `<110>`, `<111>`;
- all three flux polarizations are algebraically equivalent; replay the
  complex analytic mode in two Cartesian flux components.

Require across all nine mode arms:

- determinant, characteristic phase, eigenvalue modulus, eigenvector, and
  preserved-form residuals at or below `1e-12`;
- `0<a<4` and positive invariant determinant;
- analytic group speed finite and inside the one-Moore-shell causal front.

### 4.2 Frozen-engine replay

Initialize the exact negative-frequency eigenbranch in `RenderBridge`, run 64
ticks, and compare every site after every tick with the independent spectral
prediction.  Require maximum field and wave-velocity residual at or below
`1e-10`.

### 4.3 Exact locality and covariance

- Start a compact impulse at the center of `L=33` and run six ticks.  No value
  above `1e-14` may occur outside Chebyshev radius `t`.
- Translate a compact mixed `(J,W)` seed by integer displacement `(3,-2,1)`,
  run eight ticks, and require translated histories to agree within `1e-12`.
- Permuted/sign-related mode arms must agree in `M` and `theta` within
  `1e-12`.

### 4.4 Bloch-centroid witness

Use `L=65`, a positive-mode Gaussian spectrum centered at mode 5 with width
1.5, represented by two real flux components.  Run eight ticks in the frozen
engine and independently in the exact Bloch representation.

Require:

- engine/Bloch centroid displacement agreement within `1e-10`;
- direct-space versus spectral `Z(t)` agreement within `1e-10`;
- at least one registered one-tick displacement farther than `0.05` from every
  integer;
- total spectral amplitude conservation within `1e-12`.

The noninteger witness establishes only continuous centroid transport of an
extended field packet.

### 4.5 Cardinalities

The run must report exactly 9 mode arms, 576 mode-tick replays, 6 support arms,
1 translation arm, and 8 centroid ticks.  A cardinality mismatch invalidates
the run.

## 5. Verdicts and failure consequences

- `INTEGER_TRANSLATION_SUPPORTS_CONTINUOUS_BLOCH_CENTROID_FLUX_ONLY`: every
  gate passes.  The scalar type boundary and native free-flux Bloch transport
  are established; manifested mobile matter remains open.
- `NATIVE_FLUX_BLOCH_REPLAY_FAILED`: any algebraic, replay, locality,
  covariance, centroid, or cardinality gate fails.  No transport claim is
  advanced.

No threshold may be changed after execution.  Failure does not authorize a new
toggle, stencil, mass term, force, hidden route variable, or constant formula.
