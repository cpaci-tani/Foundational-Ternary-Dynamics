# FTD-0760 — M3 relational-chart held-out validation v1

**Status:** `[PRE-REGISTRATION — FROZEN BEFORE IMPLEMENTATION; NOT RUN]`  
**Date:** 2026-07-30  
**Parents:** FTD-0755 support-invariant contract; FTD-0757 fixed-chart parent
qualification; FTD-0758 consumed certificate diagnosis; FTD-0759 CUDA parity  
**Scope:** fresh held-out validation of the selected finite-time matter-family
candidate; no production, action, dynamics, predicate, parameter-fit,
scenario, particle, charge, mass, pole, or Lorentz claim

## 1. Locked question

Does the unchanged reciprocal-pair action support a nonzero relative-open
family of complete states when constituent identity is treated relationally,
so two records may share an integer site chart while remaining distinct by
their continuous remainders?

No FTD-0760 candidate, hostile continuation, or causal-fibre history existed
or had been inspected when this protocol was frozen. Existing FTD-0758 output
is excluded from the FTD-0760 certificate and cannot be copied into the new
artifact directory.

## 2. Frozen ontology and chart rule

The primitive manifestation field remains site-valued and ternary. The
continuous `remainder` remains the already-selected deterministic subcell
coupling coordinate; it is not promoted to an independent charge field.

Freeze

```text
allow_shared_anchor_chart = true
chart_admissible = allow_shared_anchor_chart || site_projection_valid
```

where `site_projection_valid` diagnoses whether the two constituent records
occupy distinct integer anchors. The runner must record both bits. The
independent certificate must reconstruct the disjunction row by row and may
not require the diagnostic alone.

This is the sole repair to the FTD-0758 certificate contract. It changes no
equation, current, field update, implicit root, object predicate, or tolerance.

## 3. Inherited frozen contract

Inherit from FTD-0755/0758 without modification:

- `DerivedCompactPair`, the support-independent `P_core` classifier, and the
  fixed integer regional readout center `C_L=(floor(L/2),)^3`;
- formation through tick 160, volumes `{321,385}`, rays
  `<100>`, `<110>`, and `<111>`, continuation through tick 312, and
  checkpoints `{160,200,240,280,312}`;
- support ladder `{4,6,8}`, state-only field observer, exact reciprocal
  current/action gates, root-regularity gates, and nested-volume gates;
- tolerances: core margins `1e-6`, common residual `1e-10`, recoil `1e-9`,
  energy `1e-8`, speed excess `1e-12`, minimum singular value `1e-3`,
  condition number `1e4`, regularity-scale difference `1e-5`, and nested
  volume comparison `2e-13`;
- the established CUDA-linked field stepper with its complete-state
  checkpoints. FTD-0759's resident implementation is not used as evidence
  until its separate performance protocol closes.

The support-invariant source protocol remains
`PREREG_M3_SUPPORT_INVARIANT_VALIDATION_v1.md`, SHA-256
`1E713DB4B997DAED0D55F098A6E7D63FC0F2D773391CE44FFE03AADD92A504BC`.

## 4. Fresh perturbations, frozen before execution

The center arm is the unperturbed tick-160 parent. The two hostile arms use
the same constructive energy-shell method as FTD-0755 but a previously unrun
corner family and exact rational magnitudes:

```text
corner impulse magnitude = 1/2048
retained parent residual = 15/16
fraction of nearest admissible position-shell margin traversed = 5/8
```

The signed `(radial,tangent-1,tangent-2)` corners are:

| ray | energy-hostile | graph-hostile |
|---|---|---|
| `<100>` | `(-,+,+)` | `(-,-,+)` |
| `<110>` | `(-,+,+)` | `(-,+,-)` |
| `<111>` | `(-,+,+)` | `(-,-,+)` |

These values are selections for an open-neighbourhood discriminator, not
derived constants and not numerical matches to physical data. They may not be
changed after qualification or result inspection under FTD-0760.

The causal-fibre arm keeps the inherited Gauss-free plaquette construction
but uses a previously unrun exact rational amplitude `1/1024` and displacement
104. Its 64-transaction pre-contact horizon and local radius 24 remain
unchanged. The displacement exceeds the registered causal reach with margin
at both volumes. These values are frozen selections, not fitted parameters.

## 5. Frozen execution matrix and artifacts

Run exactly six registered modes after one non-evidential qualification:

```text
--qualify face 1
--candidates face
--candidates edge
--candidates body
--fibre face
--fibre edge
--fibre body
```

The physical matrix is:

```text
2 volumes x 3 rays x 3 candidate variants = 18 histories
2 volumes x 3 rays x {baseline, remote fibre} = 12 histories
```

Write exactly twelve CSV/JSON pairs under `engine/results/ftd_0760/`.
Metadata must include `ftd_id`, the frozen protocol hash,
`held_out_validation=true`, `dynamics_changed=false`, and
`allow_shared_anchor_chart=true`. Candidate rows must record both
`site_projection_valid` and `chart_admissible`.

## 6. Independent certificate and verdict map

Before qualification, freeze and hash the runner, certificate, predicate
header/source, this protocol, and the inherited source protocol. The
certificate must refuse a pre-existing FTD-0760 result directory.

Apply the first matching verdict:

1. source hash, parent, perturbation, CUDA, artifact, row-shape, chart-rule,
   or summary-parity failure:
   `M3_VALIDATION_INFRASTRUCTURE_UNRESOLVED`;
2. algebraic, classifier, support-ladder, nested-volume, or causal-fibre
   failure: `M3_STATE_ONLY_CLASSIFIER_INVALID`;
3. any valid held-out hostile state exits `P_core` before tick 312:
   `M3_FINITE_TIME_FAMILY_CLOSED_NEGATIVE`;
4. sampled histories survive but a strict margin or root-regularity gate
   fails: `M3_SAMPLED_ROBUSTNESS_ONLY`;
5. every candidate, volume, fibre, margin, action, observer, ladder, and
   regularity gate passes: `M3_FINITE_TIME_SELECTED_MATTER_FAMILY`.

The last verdict establishes only a selected finite-time open family on the
declared reciprocal-pair constraint manifold. It does not establish an
asymptotic basin, autonomous formation, generic mobility, conserved charge,
mass, spin, statistics, unitarity, or Lorentz recovery.

## 7. Execution firewall

Qualification may build the face `L=321` parent and run the center arm for one
continuation transaction. It writes no artifact and is not evidence. After
preflight passes, each registered mode runs once. A failed or interrupted
mode is not rerun or tuned under FTD-0760.

Production defaults, `RenderBridge`, scenarios, the action, and the matter
predicate remain unchanged.
