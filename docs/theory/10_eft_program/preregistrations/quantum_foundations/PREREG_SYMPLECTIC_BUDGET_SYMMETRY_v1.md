# Pre-Registration — B-QM-1″: Does FTD Geometry Supply the Full Symplectic Budget Symmetry? (v1)

**Tag:** `[PRE-REGISTRATION]` — **adversarial (P4)**. Tests the seductive "J ⋈ N_c=3 convergence" surfaced (and flagged UNVERIFIED) in B-QM-1′. The Aesthetic Filter is **inverted**: the convergence's appeal triggers extra scrutiny, not endorsement (GTCA F1/F3/F9 guards). **Contains no result.** Outcomes pre-blessed.
**Date:** 2026-05-29
**Hash-lock target tag:** `preregister-symplectic-budget-symmetry-v1`
**LEDGER row reservation:** FTD-0228 (provisional).
**Companion:** `AUDIT_SPEKKENS_KNOWLEDGE_BALANCE_PARTIAL.md` (B-QM-1′ PARTIAL: binding derived, sharpness needs the full S₃). **Cross-refs:** SCOPE_DERIVE_QM_GAP; FTD-0199/0200.

> Discipline: §§1–7 hash-stamped before the attempt; commit deferred. Engineering toward FOUND — especially by identifying "3 spatial components" with "3 complementary bases" without checking commutation — invalidates the attempt.

---

## §1 — Context

B-QM-1′ left a sharp gap: the sharp Spekkens knowledge-balance needs the full symplectic **S₃ = ℤ/2 ⋊ ℤ/3** acting *transitively on the 3 complementary bases* of the `(J,v)` cell. `J²=−I` supplies a **ℤ/2** (a transposition, fixes one basis). The **ℤ/3** was flagged as an *unverified candidate*: the cube body-diagonal C₃ / the N_c=3 axis-rotation. B-QM-1″ tests that candidate adversarially.

**The hazard (named).** "The budget needs a ℤ/3; FTD has N_c=3; therefore N_c supplies it" is a `3 = 3` numerical pattern-match (GTCA failure mode F1, Gaussian-prime/Koide apophenia class). It must survive a *kind* check, not just a *count* check.

---

## §2 — The question (LOCKED)

**Q-BQM1″.** Does FTD's geometry supply the **full symplectic S₃** action on the epistemic budget — the 3 **complementary** (mutually non-commuting) bases of the `(J,v)` cell — via:
- **(i)** `J²=−I` → the **ℤ/2** (verify it acts on the *budget bases*, not merely the spatial lattice); and
- **(ii)** the body-diagonal C₃ / N_c=3 → the **ℤ/3** 3-cycle on those bases?

---

## §3 — The decisive discriminator (LOCKED — the non-laundering test)

The ℤ/3 needed permutes **three mutually complementary (non-commuting)** observables (the Bloch X,Y,Z of the cell). The body-diagonal C₃ permutes the **three spatial flux components** `{J_x, J_y, J_z}`.

> **DECISIVE TEST: do `J_x, J_y, J_z` COMMUTE (co-measurable) or are they mutually COMPLEMENTARY?**
> - **Commuting** → they are the *wrong kind of three* (a single co-measurable basis, not three complementary ones) → the body-diagonal C₃ does **not** supply the Bloch ℤ/3 → the N_c=3 candidate **fails** (the convergence was apophenia).
> - **Mutually complementary** → right kind → the candidate survives to a sharper test.

This `kind` check is the whole game; a `count` match (3 = 3) is not admissible evidence.

---

## §4 — Admissible ingredients (LOCKED)

- FTD's `(J,v)` cell; the flux 3-vector components `J_x,J_y,J_z`; their commutation structure (classical field components).
- `J²=−I` (quarter-conjugacy) and its action on the cell's conjugate pair.
- The cube body-diagonal C₃ (the N_c=3 / SU(3) 3-fold) and what it actually permutes.

**Out of scope (NOT admissible):**
- Identifying "3 spatial components" with "3 complementary bases" by count alone (F-α).
- Inserting an SU(2)/Bloch structure on the cell not derived from FTD geometry (F-β).
- Inserting `[q,p]=i`/ℏ; CODATA; fitting (F-γ, F-δ).

---

## §5 — Falsifiers (LOCKED, mechanical)

- **F-α** — the spatial-3 / complementary-3 identification is asserted without the §3 commutation check (apophenia).
- **F-β** — an SU(2) on the cell is posited rather than derived from FTD geometry.
- **F-γ** — `[q,p]=i`/ℏ inserted.
- **F-δ** — CODATA / fitting.

---

## §6 — Three pre-blessed outcomes (LOCKED)

- **FOUND.** `J²=−I` gives ℤ/2 on the budget **and** a body-diagonal-type C₃ gives the ℤ/3 on the **complementary** bases (passing §3) → full S₃ → sharp Spekkens-class derivable from FTD geometry. (Bell residue still separate.)
- **PARTIAL.** ℤ/2 holds; the N_c=3 candidate fails §3, **but** another FTD structure supplies a genuine Bloch ℤ/3.
- **CLOSED-NEGATIVE.** The N_c=3 candidate fails §3 (spatial components commute → wrong kind of three) **and** no other FTD structure supplies the Bloch ℤ/3 → the full S₃ budget symmetry is **not** derivable from current FTD geometry → sharpness not derivable → binding-but-not-sharp confirmed. The epistemic route then **re-derives the same non-commutativity wall** (the needed budget symmetry *is* a non-commutative SU(2) the substrate lacks).

---

## §7 — Method (LOCKED, ordered)

1. **ℤ/2 (i).** Confirm `J²=−I` acts on the cell's conjugate pair `(J,v)` (the q–p rotation permuting complementary observables) — does it touch the *budget*, not just the lattice?
2. **Decisive §3 test.** Compute the commutator structure of `{J_x, J_y, J_z}` (classical field components, and under any candidate quantization). Commuting or complementary?
3. **Classify.** Commuting → N_c=3 candidate fails (the body-diagonal permutes a co-measurable triple, not complementary bases).
4. **Other sources.** Check whether *any* FTD structure supplies a genuine Bloch ℤ/3 (an SU(2)/SO(3) action mixing complementary observables) without insertion.
5. **Falsifier checklist (§5) then verdict (§6).**

**Substrate.** Desk computation + a small verifier (commutator structure of the spatial flux components; contrast with genuine complementary Bloch axes).

---

## §8 — Hash-lock

`sha256sum` this file; record in-session before the attempt; commit deferred. Defective §3 discriminator → **v2**, not an edit.

*Authored 2026-05-29. **No result.** Adversarial P4. Prior-favoured **CLOSED-NEGATIVE** (the spatial flux components commute → the body-diagonal C₃ permutes a co-measurable triple, not the complementary bases → the N_c=3 ℤ/3 candidate is apophenia), but FOUND/PARTIAL remain live if a genuine non-commutative FTD structure supplies the Bloch ℤ/3. Engineering toward FOUND via the count-match invalidates the attempt.*
