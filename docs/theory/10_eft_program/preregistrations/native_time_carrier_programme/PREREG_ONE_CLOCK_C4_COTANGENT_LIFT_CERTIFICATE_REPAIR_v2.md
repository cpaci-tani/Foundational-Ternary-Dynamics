# Pre-registration — One-clock C4 cotangent-lift certificate repair v2

**Identifier:** `FTD-0977`  
**Date locked:** 2026-08-12  
**Status:** `[PRE-REGISTRATION — VERIFIER-ONLY REPAIR LOCKED BEFORE EXECUTION]`  
**Expected classifier:** inherited `Outcome B`

## 1. Frozen parent

| Artifact | Frozen SHA-256 |
|---|---|
| FTD-0976 protocol | `FD80A0524A8BB437210FC213B0DB071F8FCBB11E03D67594A23BCF4443B084F2` |
| FTD-0976 proof | `436E54D2CF9A117CA17F53D054D7C51F670A156A81EA0F4F658F63C85BC6065A` |

The first immutable FTD-0976 execution returned `49/52`, Outcome D. Every
substantive algebraic, symplectic, holonomy, reversal, switching, topology,
and scope gate passed. The three failures were:

1. `G1 fixed-sector proof...` searched across one Markdown line wrap;
2. `G2 unique canonical momentum shift` compared an algebraically identical
   factored/expanded SymPy solution list structurally; and
3. `G8 physical production identity...` searched across one Markdown line
   wrap.

No theorem is licensed from FTD-0976 alone.

## 2. Authorized in-memory substitutions

The wrapper may make exactly two representation-only substitutions to the
frozen parent source in memory:

1. normalize the protocol text with `" ".join(protocol_text.split())` before
   all protocol-marker checks; and
2. replace the `G2` structural solution-list equality by a one-solution count
   plus exact `sympy.simplify(solution - expected) == 0` residual.

The wrapper must verify that each old source fragment occurs exactly once,
that each replacement occurs exactly once afterward, and that no parent file
is modified.

## 3. Inherited gates and firewall

All 52 parent gates, source hashes, equations, symbols, assumptions,
connection profiles, representation weights, ternary sectors, classifier,
and scope statements are inherited without change. No failed physical or
mathematical gate may be waived.

The repair adds no production law, public type, engine phase, scale, active
bundle gluing, `G*` identification, Born/Bell claim, or completeness claim.

## 4. Classifier

- **Outcome B:** wrapper integrity passes, all inherited `52/52` gates pass,
  and the parent classifier returns conditional cotangent theorem /
  representation debt.
- **Outcome D:** any hash, replacement-count, inherited gate, or scope check
  fails.

The expected result is Outcome B.
