# Pre-registration — Oriented phase-connection gearbox certificate repair v2

**Identifier:** `FTD-0963`  
**Date locked:** 2026-08-11  
**Status:** `[PRE-REGISTRATION — VERIFIER-ONLY REPAIR LOCKED BEFORE EXECUTION]`  
**Expected classifier:** inherited `Outcome B`

## 1. Immutable parent record

| Artifact | SHA-256 |
|---|---|
| `PREREG_ORIENTED_PHASE_CONNECTION_TOKEN_LOADING_AND_REVERSIBLE_GEARBOX_v1.md` | `535E14ADE46A886542165A815C4B807DFE35B0ACBD7B6131342C4DB9126C96B0` |
| `proof_oriented_phase_connection_token_loading_reversible_gearbox.py` | `8AD520B137323AFD02961109C84CD8B09B49D8FF7FD393803B950D08155E8F98` |

The immutable FTD-0962 execution reported `107/115`, Outcome D. Every
exchange-algebra, symplectic-flow, connection-profile, complete-square,
mechanical-reaction, holonomy, forward/reverse, energy, phase-export,
time-reversal, quartic-cadence, and scope equation passed. Eight verifier
anchors failed:

1. the protocol's `A merely` phrase crossed a Markdown line;
2. the final reference/substrate firewall crossed a Markdown line;
3. the quartic backreaction phrase used a longer exact source clause;
4. `signed phase current` crossed a Markdown line;
5. the `G*` gearbox firewall used a longer exact source clause;
6. the winding theorem says `does not need another public memory type`;
7. the token-energy clause says `one token and therefore requires`; and
8. the positive kinetic marker used TeX `{K^2\over2M}` rather than the plain
   string `K^2/(2M)`.

## 2. Authorized repair

The wrapper may replace exactly those eight verifier literals in memory. It
may not change either parent file, any hash, symbol, equation, matrix,
Hamiltonian, connection profile, check count, outcome rule, or scope boundary.

## 3. Integrity gates

The repair must verify:

1. both immutable parent hashes and this repair-protocol hash;
2. each old anchor occurs exactly once;
3. each replacement is absent before substitution;
4. exactly eight substitutions occur in memory;
5. the parent files remain unchanged;
6. all inherited `115/115` gates pass; and
7. the inherited classifier remains Outcome B.

Any other change or inherited failure is Outcome D. This repair licenses no
new claim beyond the FTD-0962 protocol.
