# Preregistration — C4 symmetric-stress packet momentum and source handoff v1

**Date frozen:** 2026-08-24  
**Campaign status:** pre-execution lock  
**Ledger status:** no FTD identifier or claim row reserved  
**Production status:** no engine mutation authorized

## 1. Question

Does the already certified C4 packet current acquire a unique real
translation charge if, and only if, it is completed by one symmetric
rank-one stress tensor at the selected light speed? If so, is that stress
exactly the physical rescaling of the charge-even event dyad already emitted
by the manifestation transaction, and what does its substitution into the
reciprocal absorption generator force?

The campaign must separate two statements:

1. the finite carrier trajectory and energy current alone do **not** select a
   physical real momentum or its unit; and
2. conditional on a symmetric null/ray stress completion, the momentum and
   scalar/STF source projections are unique.

No target coupling, master root, empirical momentum, gravity coefficient, or
continuum fit may enter.

## 2. Frozen source chain

The exact certificate may import or compare only:

1. `proof_c4_half_admitted_energy_current_momentum_boundary.py`, SHA-256
   `D9E0E2C4FF595A56F85712CA5195BE1406BCB7F90B8B2D5D8E66CBC2F05AE3CA`;
2. `proof_c18_phase_neutral_shared_charge_stress_vertex.py`, SHA-256
   `13334840F23DBB1D70EFD59B805D97E462EDFC5B4EEC00D5C9FFF784ECEAAF35`;
3. `proof_reciprocal_packet_clock_recoil_absorption_generator.py`, SHA-256
   `4B824C3B37A8BADEC9F50ED1785602734B75D6CCF03234D65826E0541CDC2576`;
4. `proof_bloch_quasimomentum_lift_local_momentum_map_trilemma_v3.py`, SHA-256
   `62CB476D4A5F545B03A286E6C29B7710870E90802606C5DA7561F32397AA59FC`;
   and
5. this preregistration's pre-execution SHA-256.

No numerical scan, floating-point tolerance, fitted normalization, or target
comparison is permitted.

## 3. Frozen candidate completion

Let a co-directed batch contain (N\in\mathbb N_{>0}) complete packets of
energy (Gamma>0), total energy

\[
 E=N\Gamma.                                             \tag{P1}
\]

Let (r\in\{\pm e_1,\pm e_2,\pm e_3\}) be the SC propagation direction and
use the already selected half-admitted speed and energy current

\[
 c={1\over6},\qquad v=cr,qquad J_E=Ev={E\over6}r.       \tag{P2}
\]

Freeze the **conditional symmetric-stress gate**

\[
 J_E=c^2p_F,qquad \Sigma_F=p_Fv^{\mathsf T}.             \tag{P3}
\]

The candidate consequences are

\[
 \boxed{p_F={E\over c}r=6E r,\qquad
 \Sigma_F=E,rr^{\mathsf T}.}                           \tag{P4}
\]

The phase-neutral manifestation source on the same line is

\[
 t_{\rm evt}={rr^{\mathsf T}\over18}.                   \tag{P5}
\]

Thus the proposed common source handoff is

\[
 \boxed{\Sigma_F=18E,t_{\rm evt},\qquad
 \rho_F=\operatorname{tr}\Sigma_F=E,\qquad
 \Pi_F=\operatorname{STF}(\Sigma_F).}                  \tag{P6}
\]

For a quadratic receiver initially at rest, substitution of equation (P4)
into the frozen absorption generator must give

\[
 E=\omega\Delta I+{|p_F|^2\over2m}
 =\omega\Delta I+{18E^2\over m}.                       \tag{P7}
\]

Hence nonnegative clock action requires (m\ge18E), and one absorbed clock
quantum (I_*) gives only the conditional compliance

\[
 {\Gamma\over I_*}
 ={\omega\over N-18N^2\Gamma/m}.                       \tag{P8}
\]

Equation (P8) is not a value of alpha.

## 4. Exact acceptance gates

### G1 — integrity and blindness

- Every frozen hash matches.
- The certificate uses exact symbolic, integer, and rational arithmetic.
- No observed coupling, master root, CODATA value, or tuned coefficient is an
  input.

### G2 — carrier underdetermination control

Before equation (P3), the family

\[
 p_\lambda=\lambda E r,qquad \lambda>0,                \tag{P9}
\]

must leave the packet positions, total energy, and certified energy current
unchanged while changing the declared absorption recoil. Therefore the finite
carrier alone does not derive momentum. The certificate must explicitly
retain the Bloch lift/scale obstruction rather than call (r), (E r), or
the raw (E\times B) readout a Noether charge.

### G3 — conditional uniqueness

Given equation (P3), exact algebra must uniquely solve (p_F=J_E/c^2) and
produce equation (P4) for all six SC directions and every positive symbolic
(N,\Gamma).

### G4 — common scalar/STF handoff

For every SC direction, verify equation (P6), the orthogonal decomposition

\[
 \Sigma_F={E\over3}{\bf1}
 +E\left(rr^{\mathsf T}-{\bf1\over3}\right),            \tag{P10}
\]

zero trace of the STF term, charge-even parity, and covariance under the full
signed-permutation cubic group.

### G5 — reciprocal absorption substitution

Substituting equation (P4) into the frozen type-2 absorption map must preserve
its exact energy and inverse identities and yield equations (P7)--(P8).
Exact rational fixtures must pass both sides of the receiver-mass boundary:
positive clock debit for (m>18E), zero at (m=18E), and fail-closed
negative clock action for (m<18E).

### G6 — gravity boundary

The certificate may establish only:

- identity of the scalar and STF projections of the same conditional stress;
- continuity of the scalar energy owner through absorption; and
- the exact missing tensor handoff that a common action must implement.

It must not claim a derived tensor kinetic operator, constraints, spin-2 pole,
lensing, Shapiro delay, or nonlinear gravity.

## 5. Outcome classes

- **Outcome A:** equations (P3)--(P8) pass and the symmetric-stress condition
  itself is derived from the already frozen finite action without a new
  selection.
- **Outcome B:** the exact consequences pass, but symmetric stress, momentum
  scale, or tensor handoff remains selected/open.
- **Outcome C:** momentum closes but the event dyad cannot supply both scalar
  and STF source projections covariantly.
- **Outcome D:** an integrity or exact algebra gate fails.

The expected honest ceiling is Outcome B because none of the frozen sources
derives stress-energy symmetry from the microscopic finite action.

## 6. Interpretation firewall

A passing conditional completion means:

- the same requirement needed for a Lorentz-compatible symmetric stress
  tensor fixes the packet momentum normalization;
- the existing manifestation dyad has exactly the right scalar/STF type to
  source that stress; and
- the selected absorption generator can consume the resulting momentum
  without a second energy ledger.

It does **not** mean:

- the C4 carrier by itself has a native real Noether momentum;
- symmetric stress has been derived;
- the tensor source has been coupled to a native spin-2/equivalent action;
- lensing is produced;
- the recoil compliance fixes (alpha); or
- the common finite action is complete.
