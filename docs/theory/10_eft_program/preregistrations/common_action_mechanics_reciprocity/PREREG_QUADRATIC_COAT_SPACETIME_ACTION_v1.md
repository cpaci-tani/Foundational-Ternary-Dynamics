# PRE-REGISTRATION — Quadratic-coat spacetime action

**Date locked:** 2026-07-26  
**Identifier:** `FTD-0542`  
**Status:** `[PRE-REGISTRATION — LOCKED/NOT YET RUN]`  
**Parents:** `FTD-0484`, `FTD-0536`, `FTD-0539`, `FTD-0540`, `FTD-0541`  
**Scope:** observer-only construction of the unique linear spacetime connection
coupling generated by the FTD-0541 quadratic coat. Primitive manifestation,
production state, defaults, tick order, field ontology, normalization, forces,
energy definition, tolerances, toggles, and scenarios remain unchanged.

## 1. Locked spacetime deposits

For the straight segment `x(t)=x0+t Delta x`, `0<=t<=1`, use exactly

```text
rho_i(t)=q product_d B2(x_d(t)-i_d),
j^d_(i+1/2)(t)=q Delta x_d B1(x_d(t)-i_d-1/2)
                         product_(e!=d) B2(x_e(t)-i_e).
```

Define, without endpoint repair,

```text
K0=integral_0^1 (1-t) j(t) dt,
K1=integral_0^1 t j(t) dt,
T =integral_0^1 rho(t) dt.                         (1)
```

The required exact identities are

```text
K0+K1=K,
div K0+T-rho0=0,
div K1-T+rho1=0,
sum_i T_i=q.                                       (2)
```

Equation (2) must be obtained from differentiating `(1-t)rho` and `t rho`,
not from a fitted temporal source.

Split the segment at every crossed half-integer coordinate plane. On each
piece the unweighted spatial integrand has degree at most five. Multiplication
by `t`, or the three-factor temporal coat, raises the maximum degree to six.
Use four-point Gauss-Legendre quadrature, which is exact through degree seven.
No convergence-tuned quadrature order is allowed.

## 2. Locked common interaction functional

On one auxiliary gauge slab `(A0,A1,Phi)` of duration `h`, define only

```text
S_int=g[<A0,K0>+<A1,K1>-h<Phi,T>].                 (3)
```

The source variations must be

```text
D_A0 S_int=g K0,
D_A1 S_int=g K1,
D_Phi S_int=-g h T.                                (4)
```

For the existing matched gauge transformation

```text
A0 -> A0+G chi0,
A1 -> A1+G chi1,
Phi -> Phi-(chi1-chi0)/h,
```

summation by parts and (2) require

```text
Delta S_int=g[<rho1,chi1>-<rho0,chi0>].             (5)
```

The slab electric field and both magnetic endpoint fields must remain gauge
invariant. Equation (5), rather than invariance of an open charged segment,
is the locked gauge criterion.

## 3. Locked orbit and covariance gates

Test both polarities, stationary paths, all six axial directions, two- and
three-axis diagonals, integer- and half-integer-plane crossings, a periodic
boundary crossing on `L=17`, translated copies, proper cubic rotations, and
path reversal. Require below `1e-12`:

- total-current split, temporal partition, and both split-continuity residuals;
- agreement with the independently implemented FTD-0541 total current;
- the three source-variation identities (4);
- gauge endpoint, electric invariance, magnetic invariance, and curl-gradient
  residuals;
- translation, proper-cubic, polarity, and reversal residuals;
- coat/current locality and causal excess.

Invalid polarity, duration, lattice, nonfinite field, and over-causal segment
inputs must fail closed.

## 4. Locked energy boundary

FTD-0542 does not impose an energy equation. The audit must state explicitly
that fixed-duration spatial endpoint stationarity supplies no variation with
respect to the time slab. It may identify the missing temporal Legendre
equation, but it may not call a fixed-step discrete action energy preserving
without a separate proof.

## 5. Locked verdicts

- all gates close:
  `QUADRATIC_COAT_SPACETIME_ACTION_CONSTRUCTIVE`;
- current split or spacetime continuity fails:
  `QUADRATIC_COAT_SPACETIME_CURRENT_CLOSED_NEGATIVE`;
- continuity closes but the action-source or gauge identity fails:
  `QUADRATIC_COAT_COMMON_ACTION_CLOSED_NEGATIVE`;
- only floating evaluation/covariance gates fail:
  `QUADRATIC_COAT_SPACETIME_ACTION_UNRESOLVED`.

A constructive verdict proves only an exact common interaction functional. It
does not prove particle stationarity, exact energy, algebraic inversion,
reversibility, a mobile toggle, a scenario, a pole, Lorentz recovery, or a
particle interpretation.
