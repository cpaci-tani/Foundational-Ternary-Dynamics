# AUDIT — Can the Gauss AGM derive the FTD light cone?

**Registry:** FTD-0410  
**Status:** `[THEOREM — bare cone/transfer boundary]` + `[SCOPED NO-GO — every finite scalar kick period at unit cone]` + `[CONDITIONAL THEOREM — self-dual period ratio]` + `[OPEN — dynamical period-to-pole bridge]`  
**Verdict:** `AGM-DOES-NOT-CURRENTLY-DERIVE-THE-CONE`  
**Verification:** `scripts/proofs/proof_lorentz_agm_cone.py`  
**Date:** 2026-07-22

## 0. Result

There is an exact AGM structure in FTD, but it is not presently the structure
that determines the free flux speed.

The production cone is the ratio of the local spatial and temporal quadratic
coefficients. The Gauss AGM instead evaluates the global lemniscatic period
and the coincident BCC return Green function. The live propagation operator is
the SC+FCC 18-point symbol with zero direct corner weight. Consequently, no
current equation of motion maps the AGM value into the cone coefficient.

Two natural AGM readings can nevertheless be evaluated without a search:

1. The **magnitude reading**

   \[
   c_G=\frac{1}{\operatorname{AGM}(1,\sqrt2)}
      =\frac{G^*}{2\sqrt\pi},
   \qquad c_G^2=\frac{W_3}{2}
   \]

   is an exact identity, but inserting `c_G` into the flux action is a new
   `[SELECTION]`. It is not produced by the live transfer or blocking map and
   it leaves a nonzero dimension-six pole coefficient.

2. The **self-dual period-ratio reading** uses the lemniscatic identity
   `K'=K`. If, and only if, the spatial and temporal kinetic coefficients are
   proven to be the two equally normalized conjugate periods, then

   \[
   c_{\rm lat}=\frac{K}{K'}=1.
   \]

   This is the strongest structurally natural AGM cone statement. It is
   conditional because FTD has not supplied the period-to-pole map. A unit
   cone exactly cancels the complete isotropic `q4` pole term. FTD-0414
   corrects the former claim that the full isotropic `q6` projection also
   vanished. An exact Markov
   bound proves that **every finite-period scalar kick cell** on the production
   symbol is full-band stable only when `c²<=3/4`, so no scalar temporal period
   can realize the unit cone.

The AGM therefore supplies a serious construction lead, not a completed
derivation of the speed of light.

---

## 1. What determines a lattice cone

Write the quadratic long-wavelength inverse propagator as

\[
\Gamma^{(2)}(\omega,\mathbf q)
=Z_t\omega^2-Z_s|\mathbf q|^2+O(q^4,\omega^4).
\]

The pole gives

\[
\omega^2=c_{\rm eff}^2|\mathbf q|^2+\cdots,
\qquad
\boxed{c_{\rm eff}^2=\frac{Z_s}{Z_t}}.
\]

For the bare engine, canonical temporal normalization sets `Z_t=1`; the
spatial coefficient contains the explicitly selected `C_WAVE^2`. The rational
face/edge weights normalize the spatial symbol to

\[
M_{18}(q)=S_2-\frac{S_2^2}{12}+O(q^6),
\]

but they do not determine the overall `C_WAVE^2` multiplier. A transcendental
AGM value cannot emerge from this local second moment unless it is inserted in
a weight/coefficient or generated by an interacting renormalization.

---

## 2. The native time-blocking map is not the AGM map

For one Fourier mode define `x=c^2 M18`. The exact one-tick recurrence has

\[
T(x)=\begin{pmatrix}2-x&-1\\1&0\end{pmatrix},
\qquad \det T=1,
\qquad \operatorname{tr}T=2-x.
\]

After decimating every other time slice,

\[
\operatorname{tr}T^2=(2-x)^2-2,
\]

so the same trace coordinate obeys

\[
\boxed{x\longmapsto x'=4x-x^2}.
\]

This is an angle-doubling/Chebyshev map. The Gauss AGM instead evolves a pair
of positive quantities,

\[
(a,b)\longmapsto
\left(\frac{a+b}{2},\sqrt{ab}\right).
\]

The production scalar transfer therefore does not natively perform AGM
coarse-graining. An additional paired internal degree of freedom and a derived
map on it would be required.

---

## 3. The exact AGM magnitude candidate

At the lemniscatic modulus,

\[
\operatorname{AGM}(1,\sqrt2)=\frac{2\sqrt\pi}{G^*}.
\]

The reciprocal is the existing FTD Grid/Gauss constant,

\[
c_G:=\frac{1}{\operatorname{AGM}(1,\sqrt2)}
=\frac{G^*}{2\sqrt\pi}.
\]

Using the Watson identity `W3=G*^2/(2pi)` gives

\[
\boxed{c_G^2=\frac{G^{*2}}{4\pi}=\frac{W_3}{2}}.
\]

This is the unique direct reciprocal-magnitude reading already named by the
corpus. It does not make it the speed. The exact low-momentum pole from
FTD-0407 has quartic coefficient

\[
\beta_4(r^2)=\frac{r^2(r^2-1)}{12}.
\]

At the reciprocal-AGM value,

\[
\boxed{\beta_4(c_G^2)=\frac{W_3(W_3-2)}{48}\ne0}.
\]

Thus the magnitude substitution neither follows from the action nor solves
the leading Lorentz-recovery defect. Calling `c_G` the speed would presently
be `[SELECTION]`, not `[DERIVED]`.

---

## 4. Why the AGM-bearing BCC period is not the live wave cone

The AGM/Watson identity uses

\[
\sigma_{\rm BCC}(q)=1-\cos q_x\cos q_y\cos q_z.
\]

The production flux uses

\[
M_{18}(q)=4-\frac23\sum_i\cos q_i
-\frac23\sum_{i<j}\cos q_i\cos q_j.
\]

They are not two normalizations of one operator. Exact witnesses are

\[
M_{18}(\pi,0,0)=4,
\qquad \sigma_{\rm BCC}(\pi,0,0)=2,
\]

and

\[
M_{18}(\pi,\pi,0)=\frac{16}{3},
\qquad \sigma_{\rm BCC}(\pi,\pi,0)=0.
\]

The point at which the production symbol reaches its maximum is a zero of the
BCC symbol. Moreover, the variational production stencil has direct face and
edge links but zero corner weight. The BCC return period can therefore not be
substituted for the production kinetic coefficient without an explicit new
operator bridge.

---

## 5. The strongest conditional AGM derivation

Gauss's theorem gives

\[
K(k)=\frac{\pi}{2\operatorname{AGM}(1,k')},
\qquad k'=\sqrt{1-k^2}.
\]

At the lemniscatic fixed point `k=1/sqrt(2)`, `k'=k`, hence

\[
K'=K.
\]

Introduce the missing physical bridge as an explicit hypothesis:

> **AGM cone bridge.** The infrared temporal and spatial quadratic forms are
> the two conjugate periods of one lemniscatic transfer curve, with the same
> normalization and with no independent kinetic prefactor.

Under this hypothesis,

\[
c_{\rm lat}=\frac{K}{K'}=1.
\]

This is a conditional theorem, not a choice among numerical combinations.
The AGM magnitude cancels because a speed is a ratio of spatial and temporal
scales. Self-duality selects equality rather than the number
`1/AGM(1,sqrt(2))`.

For the current fully discrete pole, `r^2=1` gives

\[
\theta^2=S_2-\frac{S_2^3}{360}
+\frac{S_2Q_4}{72}-\frac{Q_6}{90}+O(q^8),
\]

where `Q4=sum(q_i^4)` and `Q6=sum(q_i^6)`. The complete rotationally
invariant dimension-six term vanishes. The sixth-order tensor survives and
has a nonzero spherical/isotropic projection; the former contrary statement
resulted from giving a different cubic discriminator the same `Q6` name.
The structural alignment therefore holds through q4, not through the full q6
order.

### 5.1 All finite scalar temporal periods fail

The current centered update is stable only for

\[
r^2M_{18,\max}\le4,
\qquad r^2\le\frac34.
\]

At `r^2=1`, `16/3>4`. More strongly, periodic scalar kick modulation cannot
repair this conflict at any finite period.

Let an `n`-tick scalar kick cell have half-trace

\[
C_n(M)=\cos(n\theta(M)).
\]

Because each kick matrix is affine in `M`, `C_n` is a real polynomial of
degree at most `n`. Full-band stability requires `|C_n(M)|<=1` on
`0<=M<=L`, where `L=16/3`. A unit infrared cone requires

\[
C_n(M)=1-\frac{n^2}{2}M+O(M^2),
\qquad |C_n'(0)|=\frac{n^2}{2}.
\]

Map `[0,L]` affinely to `[-1,1]`. Markov's endpoint inequality for a
degree-`n` polynomial bounded by one gives

\[
|C_n'(0)|\le\frac{2n^2}{L}=\frac{3n^2}{8}.
\]

But `n²/2>3n²/8` for every positive finite `n`. Therefore

\[
\boxed{c_{\rm scalar}^2\le\frac4L=\frac34}
\]

for every full-band-stable finite-period scalar kick cell based on `M18`.
This exactly reproduces the one-step ceiling and proves that temporal
periodicity alone can never reach the self-dual unit cone. The FTD-0408 speed
reduction is not an accident of period two; it is the direction enforced by
the full-band polynomial bound.

### 5.2 Exact stable unit-cone effective target outside one-shell locality

The obstruction is to scalar temporal modulation of the existing symbol, not
to a stable unit-cone spectral function. Define

\[
F_*(M)=M-\frac{27}{16384}M^4.
\]

On `0<=M<=16/3`,

\[
F_*'(M)=1-\frac{27}{4096}M^3\ge0,
\qquad F_*(0)=0,
\qquad F_*(16/3)=4.
\]

Hence `0<=F_*<=4` exactly. Since `F_*(M)=M+O(M^4)`, replacing the centered
pole's `M` by `F_*` preserves the unit-cone cancellation through `M^3`, hence
through the q4 order while retaining the corrected q6 tensor. Direct application of `M18^4`, however,
spreads one update across four Moore shells and violates P4. It is a precise
spectral target for multi-state localization, not a legal scalar engine rule.

The unit-cone branch therefore requires a multi-component
reversible/paraunitary localization of such a bounded spectral function, a
changed spatial symbol, or an interacting common-cone mechanism. It cannot be
enabled by changing `C_WAVE` or adding any finite scalar kick period alone.

---

## 6. The only available interacting route

Let the interacting inverse propagator be

\[
\Gamma^{(2)}(\omega,q)
=(1+\Sigma_t)\omega^2
-(c_0^2+\Sigma_s)S_2+\cdots.
\]

Then

\[
\boxed{c_R^2=\frac{c_0^2+\Sigma_s}{1+\Sigma_t}}.
\]

A coincident return value such as `W3` supplies neither derivative by itself.
To derive an AGM cone dynamically, FTD must derive one common interacting
action, calculate both `Sigma_t` and `Sigma_s`, and prove that their ratio is
controlled by a lemniscatic Landen/AGM identity. The same ratio must then occur
for matter, flux, gauge, bound-state clock, and gravity poles.

This is the precise open bridge. It cannot be replaced by inserting `W3`,
`G*`, or the Grid constant into `C_WAVE`.

---

## 7. Recovery consequences

| Claim | Status after FTD-0410 |
|---|---|
| `1/AGM(1,sqrt(2)) = G*/(2sqrt(pi))` | `[THEOREM — classical identity]` |
| Reciprocal AGM squared equals `W3/2` | `[THEOREM]` |
| Reciprocal AGM is the physical cone | `[SELECTION/CONJECTURE — not derived]` |
| Native temporal blocking is an AGM iteration | `[CLOSED NEGATIVE for current scalar transfer]` |
| Self-dual equal-period bridge gives `c_lat=1` | `[CONDITIONAL THEOREM]` |
| Unit cone cancels the complete `q4` pole term | `[THEOREM]` |
| Unit cone cancels the full isotropic `q6` projection | **False; corrected by FTD-0414** |
| Any finite-period scalar kick cell is stable at unit cone | `[CLOSED NO-GO — Markov bound]` |
| Stable unit-cone effective symbol `F_*(M)` exists | `[THEOREM — violates P4 if applied directly]` |
| Interacting AGM period-to-pole bridge | `[OPEN]` |
| LR-2 common cone | `[OPEN]` |

## 8. Next admissible construction

The next derivation must not search over formulas involving `G*`. It must do
one of the following:

1. construct a multi-component reversible/paraunitary localization of the
   stable unit-cone target `F_*`, then derive the same pole for matter; or
2. derive a paired spatial/temporal internal state whose exact blocking map is
   the Gauss AGM and whose quadratic pole coefficients are the two conjugate
   periods; or
3. calculate `Sigma_t` and `Sigma_s` from a common interacting action and show
   that their ratio obeys an AGM/Landen identity.

Until one of those maps exists, the AGM does not derive the FTD speed of light.
