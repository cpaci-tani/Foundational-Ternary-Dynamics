# SYNTHESIS — The Commutativity Wall

**Tag:** `[SYNTHESIS]` — cross-document roll-up of existing claims at their **canonical
LEDGER tags**. **No new theorem, no tag promotion or demotion.** Per the `[SYNTHESIS]`
contract: verify the component claims; the synthesis is a coherent re-statement, not a
new result.
**Date:** 2026-05-30 · **LEDGER row:** FTD-0238 *(provisional — to be added once the
in-progress `07_assessment/` restructure settles; next-free id at time of writing)*.
**Companion roll-ups:** `SPEC_DOCTRINE_LEDGER.md` (FTD-0145), `TRACKER_ONTIC_TRUTH.md`.

---

## 0 · The one-sentence diagnosis

> **FTD's substrate is classical and commutative.** It *derives* the commutative period
> algebra (G\*, π√2, the master quadratic). It does **not** derive any structure that
> requires **non-commutativity** — quantum mechanics, relativistic time, or the operational
> α-readout. Those are imported or absent. **Six independent results are one wall.**

This is not a new finding; it is the recognition that six separately-tagged results share a
single root cause, and that the root cause cleanly partitions what FTD derives from what it
must import.

---

## 1 · The two sides of the wall

**Commutative side — DERIVED (this is what FTD owns):**
- The Z/4 quarter-structure (the cube's C₄ / the Gaussian `i`) → `Γ(1/4)` → **G\*** (ratio)
  and **π√2** (product) → the **master quadratic** `x² − 16G*²x + 16G*³ = 0`.
  `[THEOREM]` — FTD-0001 / FTD-0002 / FTD-0006 / FTD-0007.
- A **binding epistemic horizon** for internal observers (classical finite self-reference).
  `[DERIVED]` — the *positive* half of FTD-0227.
- The dimensional calibration `a_phys ≡ ℓ_P` (declared, theorem-enforced by FTD-0059/0096).

**Non-commutative side — IMPORTED or ABSENT (what FTD does not derive):**
- QM's non-commutativity (`[q,p]=i`), relativistic `L²`, the α-readout operator `(Tr,Det)`.
  Each needs a structure the commutative substrate lacks.

`x₊ = 1/α` (FTD-0013) sits *across* the seam: the **value** lives on the commutative side
(it's a root of the master quadratic, `[THEOREM]` as algebra); the **physical
identification** with the EM coupling is `[STRONGLY MOTIVATED CONJECTURE]` and its
*operational readout* is on the non-commutative side (MC-T4.3, below). No tag change here.

---

## 2 · The six probes — each at its canonical tag

| # | Result (LEDGER) | Canonical tag | What it shows | The missing piece |
|---|---|---|---|---|
| 1 | **FTD-0208** clock / `L²` law | `[CLOSED NEGATIVE, AXIOM-LEVEL]` | substrate update is `L¹`/`L∞`, not `L²` | the `L²` (Pythagorean) metric |
| 2 | **FTD-0225** B1 modular time | `[CLOSED NEGATIVE]` | commutative → type I → trivial modular flow | type III₁ (non-commutative) algebra |
| 3 | **FTD-0226** B-QM-1 manifestation | `[CLOSED NEGATIVE]` | genesis map → **Boolean** (distributive) event lattice | non-distributive (quantum) lattice |
| 4 | **FTD-0227** B-QM-1′ Spekkens | `[PARTIAL]` | binding **DERIVED**; sharpness not | the symplectic **S₃ / SU(2)** budget |
| 5 | **FTD-0228** B-QM-1″ budget sym. | `[CLOSED NEGATIVE]` | `N_c=3 → ℤ/3` is apophenia (commuting ≠ complementary) | the Bloch **SU(2)** |
| 6 | **MC-T4.3** α-readout (FTD-0224) | `[FOUNDATIONAL OBSTRUCTION]` (W-CRIT-2) | the readout's `(Tr,Det)` operator structure is unforced | a forced non-commutative operator |

> **Row-6 update (2026-06-01, FTD-0242):** the "operator structure is unforced" diagnosis is now **route-invariant** — 4 independent FTD-native routes were force-attempted and adversarially refuted, **0/4 forced** (`audits/AUDIT_ALPHA_OPERATOR_FORCING_ROUTE_INVARIANCE.md`). The trace `16G*²` and a clean odd source `G*` are each forward-forced; only their *assembly* (W-CRIT-2) is not. α is **dynamical, not structural**. This sharpens row 6; it promotes nothing (the boundary is `[STRONGLY MOTIVATED CONJECTURE no-go]`, RSI Leg 3 open).

**Supporting closed-negatives:** FTD-0199 / FTD-0200 (Born-rule emergence `[CLOSED NEGATIVE]`
— the substrate gives Rice/Gaussian, not Born). **Exploratory corroboration (NOT evidence,
NOT a LEDGER claim):** the 2026-05-30 dissipation-N engine pass returned a comprehensive null
(`docs/internal/EXPLORATORY_CLUSTER_THERMODYNAMICS_2026-05-30.md`, local-only) — clusters
neither fission nor fuse, cost↔N is trivial equipartition, no internal-clock signal.

**The pattern:** in every row the "missing piece" is the *same structure*.

---

## 3 · Why it is one wall (the unifying lemma)

The following are the **same structure** wearing different clothes — each equivalence is
established mathematics; the synthesis is recognising they are one absence:

```
 non-commutativity [q,p]=i
   ⟺ a complex/unitary inner-product (L²) structure        (the FTD-0208 metric)
   ⟺ a non-distributive (orthomodular) event lattice        (Birkhoff–von Neumann; FTD-0226)
   ⟺ an SU(2) acting on complementary observables           (the Bloch budget; FTD-0227/0228)
   ⟺ a type III₁ modular algebra                            (canonical modular time; FTD-0225)
   ⟹ Bell-violating correlations                            (Tsirelson; the derive-QM wall)
```

FTD's commutative substrate has **none** of them. So the six probes could not have come out
otherwise — they are one theorem, observed six ways.

---

## 4 · The honest framing (ψ-epistemic) — `[SYNTHESIS]`, not a derivation

The reframe that makes the picture coherent (and is the project's most defensible
self-understanding):

> **FTD is a commutative classical substrate that derives the period algebra; quantum
> mechanics and relativity are the imported non-commutative layer.** QM is read as
> **ensemble statistics** (Ballentine): the substrate supplies the *ontic discrete events*
> (manifestations) and a *binding epistemic restriction* (FTD-0227, derived); QM's
> non-commutativity is the effective/imported structure, available only at the
> **Bell-superdeterminism price** (a 6th-postulate-class move, à la 't Hooft).

This is interpretation, tagged as such — it promotes nothing. It replaces the overclaim
"FTD derives physics" with the accurate "FTD derives the commutative spine and imports the
non-commutative quantum/relativistic layer."

---

## 5 · What this does and does not change

- **No tag moves.** The spine stays `[THEOREM]`; `x₊=1/α` stays `[STRONGLY MOTIVATED
  CONJECTURE]`; every cited closed-negative/partial keeps its tag. This doc only *maps* them.
- **MC-T4.3 is not closed** — it remains `[FOUNDATIONAL OBSTRUCTION]`. This doc *classifies*
  it: the obstruction **is** the commutativity wall (W-CRIT-2 = the unforced non-commutative
  operator structure).
- **The single surviving frontier is derive-QM:** obtaining non-commutativity *from* the
  classical substrate. Everything downstream (objective time, the α-readout, dissipative
  identity dynamics) waits on it, and it is Bell-walled — closing it requires an added
  postulate, not a derivation from the five. That is the honest edge of the program.

---

## 6 · Provenance

Cites at canonical tags: FTD-0001/0002/0006/0007 (`[THEOREM]`), FTD-0013 (`[SMC]`),
FTD-0199/0200/0208/0225/0226/0228 (`[CLOSED NEGATIVE]`), FTD-0227 (`[PARTIAL]`), FTD-0224 /
MC-T4.3 (`[FOUNDATIONAL OBSTRUCTION]`). Exploratory (non-evidence): the 2026-05-30
dissipation-N pass. No script run; no numerical claim; no new derivation. `[SYNTHESIS]`.
