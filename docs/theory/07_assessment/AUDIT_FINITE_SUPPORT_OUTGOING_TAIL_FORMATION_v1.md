# Audit — finite-support outgoing-tail formation v1

**Identifier:** FTD-0739  
**Status:** `[AUDIT PASS — M1 CONSTRUCTIVE AT REGISTERED SELECTED SCOPE]`  
**Verdict:** `FINITE_SUPPORT_OUTGOING_TAIL_FORMATION_CONSTRUCTIVE`  
**Date:** 2026-07-29

## 1. Audit verdict

The FTD-0739 run is execution-valid and constructive under its locked
protocol. The independent certificate reproduces the verdict from all 1,365
CSV rows rather than accepting the engine summary.

The result closes the FTD-0740 M1 finite-support causal-existence gate for the
selected reciprocal `(s,C,F)` action. It does not close M2 environmental
closure or M3 metastable-family identity.

## 2. Execution integrity

- locked protocol SHA-256: `9AA9B806…A7868E`;
- pre-execution static conformance: `55/55 PASS`;
- focused preparation/regional tests: `2/2 PASS`;
- frozen Release executable: `C0E158D1…263B5`;
- complete histories: `5/5`;
- complete serialized rows: `1365/1365`;
- independent record certificate: `147/147 PASS`;
- CSV SHA-256: `E9B9B2FC…ADC622`;
- JSON SHA-256: `237F6EA3…A19D5C`.

The four pre-execution conformance corrections are recorded separately in
[`AUDIT_FINITE_SUPPORT_OUTGOING_TAIL_FORMATION_PREEXEC_v1.md`](AUDIT_FINITE_SUPPORT_OUTGOING_TAIL_FORMATION_PREEXEC_v1.md).
They changed observation/verdict conformance only and preceded the run.

## 3. Physics-gate reconstruction

The certificate independently confirms:

- `4/4` unbound rays begin graph-outside with positive pair energy and zero
  registered exterior energy;
- `4/4` enter a continuous graph-inside negative-energy tail by tick 120 and
  remain there through tick 136;
- `4/4` energetic onsets equal the exact pair--field first-passage prediction;
- `4/4` have exterior energy and cumulative outward transport above `1e-6`,
  with positive final outside energy;
- all first tail crossings occur at tick 62 while current support remains
  within radius three;
- the bound control remains graph-inside and negative on every forward and
  reverse stored state;
- the two body-polarity histories are exactly conjugate in the persisted
  scalar/discrete record;
- common action, energy, recoil, speed, regional ledger, and inverse residuals
  all pass their locked thresholds.

The final entries/onsets are `65/80`, `81/96`, and `100/115`; the measured
entry-to-onset delay is 15 ticks on all rays.

## 4. Claim boundary

The strongest licensed statement is:

> In the selected compact-pair common action, finite-support neutral Gauss data
> form a durable negative relational core on all registered cubic rays while a
> source-separated field tail crosses radius 12 before possible periodic
> self-contact.

The words `durable` and `outgoing` refer only to the registered horizon and
observers. The audit rejects promotion to:

- persistence in an infinite or asymptotic environment;
- an isolated invariant basin or physical particle;
- native emergence from production rules;
- a derived binding law or fundamental constituent ontology;
- charge, radiation quantum, mass, spin, statistics, or Lorentz symmetry.

## 5. Roadmap consequence

M1 changes from `PAUSED / UNRESOLVED` to `CONSTRUCTIVE — SELECTED SCOPE`.
M2 becomes the only licensed next engine gate. It must use increasing causal
buffers/volumes and a preregistered bound-dressing/outgoing-tail separator;
post-contact continuation on `L=145` cannot answer it.
