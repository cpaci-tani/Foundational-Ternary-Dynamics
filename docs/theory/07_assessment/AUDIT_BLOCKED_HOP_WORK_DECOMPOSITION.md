# AUDIT — Blocked-hop work decomposition

**Identifier:** `FTD-0460`  
**Date executed:** 2026-07-24  
**Status:** `[MEASURED — STATIC SELF-SOURCE DOMINANCE]`  
**Parent:** `FTD-0459`  
**Run of record:** `engine/results/ftd_0460/windows_msvc_cpu.csv`

## Result

The scalar work obstruction in FTD-0459 is produced by the manifested
polarity's accumulated static source field, not by the incoming transverse
packet. The locked v2 verdict is:

`STATIC_POLARITY_SOURCE_DOMINATES_BLOCKED_HOP_WORK`

The exact component RMS values over all 42 scheduled attempts are:

| component | RMS forward-hop work | invalid updates rescued by removal |
|---|---:|---:|
| localized transverse packet | `8.244751688246903e-20` | 0 |
| one-time longitudinal dressing | `8.976377313277575e-5` | 0 |
| static polarity source | `0.008488229634971121` | 30 |
| velocity-curl source | `1.703464636817277e-18` | 0 |

The static source RMS is `94.56x` the dressing RMS and over `10^17x` the
packet RMS. Full kinematics is valid at only 12 of 42 attempts. Packet plus
dressing kinematics is valid at all 42. Removing the static source alone
rescues the other 30.

## Exactness and provenance

The decomposition is linear rather than fitted:

- worst scalar-work superposition residual: `5.204170427930421e-18`;
- worst full `J/W` component reconstruction residual:
  `5.389475569412524e-17`;
- maximum five-history reversal residual: `7.67335666250811e-17`;
- all 42 full-work values reproduce the FTD-0459 run of record with maximum
  absolute residual exactly zero.

The first locked v1 execution exceeded 600 seconds without a complete record
and is excluded. The v1 observer repeatedly dirtied and rebuilt the ternary
cache inside its site loop, turning the intended linear stencil sweep into an
effective `O(L^6)` path. V2 changes no equation or estimator; it uses one const
snapshot for all stencil reads and one later mutable handout for writes. The
corrected full run completes in about 2.2 seconds.

## Physical interpretation

The registered localized packet is transverse and divergence-free to numerical
precision. Therefore it contributes essentially no endpoint scalar work
`G_C[(div J)_b-(div J)_a]`. Its constructive role in FTD-0457 was different:
it altered the local `J/W` phase-space coefficients enough to make a conserving
recoil shell exist. It supplied recoil capacity, not a longitudinal force.

The motion-dependent curl source is likewise invisible to this scalar work
channel at the registered longitudinal hop. Calling either one the direct
pilot force is therefore unsupported.

The blocking term is the field sourced repeatedly by the stationary polarity
itself. Applying endpoint work to the total field treats that accumulated bound
self-field as though it were an external potential. The resulting negative
work often exceeds the particle's entire kinetic budget. This exposes a
dressed-particle problem: the current event law does not distinguish a bound
self-cloud that should move with the manifestation from external radiation
that may exchange momentum with it.

This audit does not prove that self-field subtraction is correct. It proves
that any next matter-mechanics construction must state how self-field energy
and momentum are assigned. Retuning the packet cannot repair a work channel to
which the packet contributes zero at linear order.

## Next gate

Derive the static lattice Green-function dressing generated by one fixed
polarity, split the total field into bound and radiative components by an
explicit projector or collective coordinate, and compare three work contracts:

1. total-field endpoint work;
2. external/radiative-field endpoint work after bound-cloud subtraction;
3. total energy difference when the bound cloud is translated with the state.

The contracts must agree or identify the missing self-energy term before any
new guided-packet campaign is licensed.
