# LEDGER row drafts: effective fluid labs at Scales 3/5 and the Scale 0 contrast scenario (Part B)

Date: 2026-09-09. Draft only — **not booked into `LEDGER.md`**; booking is
the owner's act. Nothing in this draft is a substrate derivation, nothing is
Navier–Stokes, and the Scale 3/5 numbers below are engine-unit instrument
readings from one seed each, not physical constants and not comparisons to
any physical fluid. Source booking document (local-only, cited by path and
SHA256): `engine/docs/PROGRAM_HYDRO_SCENARIOS_PART_B.md`
(SHA256 `5bad702743a337ec10a24dbccf2af527401d7a2bc1f26c0e45d07de1a9ff3fed`,
supersedes `d86bb185f4913314e1529ce111cb3f0978a307c6a5b4706717f737ec48f9a009`
after the final-review fix round re-measured the Part B labs).
Design spec: `docs/superpowers/specs/2026-09-08-hydro-scenarios-and-fchc-successor-design.md`
Part B (§B.1–B.3, §D). Prior wave: this is the Part B continuation of the
same arc as
[LEDGER_ROW_DRAFT_hydro_successor.md](LEDGER_ROW_DRAFT_hydro_successor.md)
(Part A, FTD-1034–1037 proposed there — the priced successor law and its
strict laboratory, where a lattice-gas fluid is actually derived).

**Next-free id.** `python scripts/audit/check_registry.py` (run 2026-09-09
from the worktree root) reports:

```text
FTD ids referenced : 1031  (max FTD-1037)
NEXT FREE FTD ID   : FTD-1038   <- use this, not CLAUDE.md
FC register exists : ['FC-0', 'FC-1', 'FC-2', 'FC-3', 'FC-4']  <- check before adding FC-N
RESULT: clean
```

The three draft ids below (FTD-1038 through FTD-1040) are proposed for the
owner; **re-run `check_registry.py` immediately before booking** to confirm
1038 is still free, since other unbooked drafts (including Part A's
FTD-1034–1037, also unbooked) may have claimed it since this run.

---

| FTD-1038 | **Does the Scale 3 molecular engine, run as four classical liquid laboratories, publish transport estimates whose estimators are verified and whose readings are instrument facts in engine units?** | [IMPOSED effective classical dynamics; coefficients in engine units] + [MEASURED — instrument] | NEW 2026-09-09 — [`engine/docs/PROGRAM_HYDRO_SCENARIOS_PART_B.md`](../../../../engine/docs/PROGRAM_HYDRO_SCENARIOS_PART_B.md) ("Scale 3: molecular liquid-transport laboratories"), module `engine/web/js/scales/scale3/liquid-transport.js`, scenarios `engine/web/js/scales/scale3/scenarios.js` + `scenario-registry.js` (category "Liquid Transport Experiments"), protocol `engine/web/js/scales/scale2/experiment-runtime.js`, telemetry `engine/web/js/telemetry-hub.js` + `engine/web/js/ui/panels/diagnostics-panel/descriptors/scale3.js`. No change to the AtomEngine's own physics — scenarios, a pure-function measurement module, one additive protocol case, one additive telemetry hook, and a descriptor card only. Four scenarios, each one seed, engine reduced units, readouts taken from `telemetryHub.s2.liquid` after 200 measurement ticks at the 1-in-3 telemetry cadence (67 samples; re-measured after the `imposeLiquidFlow` fix below — double-precision reads and a wall/no-op skip — so the digits differ slightly, at the fourth decimal, from earlier citations of these labs): `mol-liquid-shear-layer` (160 waters, 10×8×2 lattice, no walls) — erf-width growth estimator `w² = 4νt` (`fitErfWidth` + linear regression on `w²` vs. time) — **ν = +0.3478 ± 0.0313**; `mol-liquid-channel-decay` (48 waters between two locked-argon walls, `Z = 18`, `h = 21`, 84 connected components total) — first-mode decay estimator, joint `{1, cos(πy/h)}` least-squares fit + exponential decay rate `γ`, `ν = γh²/π²` — **ν = 5.0167 ± 0.2438** (0 samples truncated by the decay fit); `mol-liquid-droplet-diffusion` (64 waters, no walls) — drift-removed mean-square-displacement slope `D = slope/6` — **D = 1.2450 ± 0.0489**; `mol-liquid-spinning-droplet` (64 waters, imposed differential rotation `ω₀ = 0.08`, `R = 9`) — inner–outer angular-velocity difference, exponential relaxation `τ = 1/γ` — **τ = 3.1448 ± 0.4134** (0 samples truncated by the decay fit). Every published `se` above is the ordinary-least-squares slope standard error of its fit, computed from samples of one trajectory that are serially correlated (the droplet's MSD branch is literally a running sum) — it is a lower bound on the reading's true uncertainty, not the uncertainty itself. The two decay-based estimators (channel `ν`, spinning-droplet `τ`) fit `logDecayRate` only up to (excluding) the first non-positive amplitude sample, the point past which the signal has decayed into noise; `summary()` reports both how many samples were used and how many were truncated (`samplesDropped`, 0 for both labs at this 200-tick horizon), and falls back to a `noise-limited` status rather than a fit if fewer than 4 usable samples remain. Two instrument findings are part of the claim, not excluded from it: (1) the shear-layer lab's originally planned 64-water (4×8×2) geometry measured **ν = −0.369 ± 0.098** (a formal OLS significance of 3.8σ, under the same lower-bound caveat as every `se` in this row) because its fastest molecules leave the fixed `±18` profile window and its two counter-streaming halves lose interfacial contact within the window — a diagnosed instrument failure, reproduced bit-for-bit, confirmed not to be an estimator defect (a synthetic control shows the erf-width and MSD estimators unbiased on a stable population), and retired in favor of the shipped 160-water geometry: the pre-M6 Node diagnostic's 200-tick and full-600-tick readings were +0.348 ± 0.031 and +0.389 ± 0.029 respectively — they differ by 0.041, within their combined standard error of 0.042, and neither changes sign or order of magnitude, unlike the retired 64-water geometry; (2) the channel lab's estimator originally included the 36 locked-argon wall singletons in its first-mode fit, biasing the reading by 12.8% on a reproduced frame — fixed by a declared species filter (`liquid.wallZ: 18`) that drops wall atoms from every estimator branch before fitting, with a regression test that fails without the filter (214% error) and passes with it (5.45×10⁻⁸ error). Evidence: Node 35/35 (`scale3-liquid-transport.node.test.mjs` + `scale3-liquid-labs.node.test.mjs`, estimator unit tests on synthetic data with known ν/D, plus the same-tick-guard, decay-truncation, and population-change regression tests); Playwright `scale3-liquid-transport.spec.js` 2/2 (all four labs reach their registry-declared expected topology, run the protocol, publish a `measuring` estimate), `scale3-molecule-engine.spec.js` 4/4; determinism bit-identical (same-seed reload reproduces exact `totalEnergy` and first-atom position; `value`/`se` readouts byte-identical across independent full-suite runs). Nothing here is a fluid-dynamics derivation; the labels declare an imposed effective classical model, and the numbers are one-seed instrument readings in engine units, never compared to a physical liquid. |
| FTD-1039 | **Does the Monaghan SPH port into the Scale 5 cosmic engine match an independent transcription of its C++ source, and do its three gas laboratories publish plausible instrument readouts behind a default-off toggle?** | [IMPOSED effective gas dynamics] + [MEASURED — instrument] | NEW 2026-09-09 — [`engine/docs/PROGRAM_HYDRO_SCENARIOS_PART_B.md`](../../../../engine/docs/PROGRAM_HYDRO_SCENARIOS_PART_B.md) ("Scale 5: Monaghan SPH port and gas laboratories"), module `engine/web/js/bridge/cosmic-sph.js`, C++ source of record `engine/src/cosmic/cosmic_sph.cpp` (kernel ~19–47, density ~74–104, forces ~107–167), toggle `SCALE5_TOGGLES = [['sph_monaghan', false, ...]]` in `engine/web/js/config/toggles.js`, scenarios `engine/web/js/bridge/cosmic-scenarios/gas.js`, parity test `engine/web/tests/cosmic-sph.node.test.mjs` + independent reference `cosmic-sph-reference.mjs`. The port's substance is the transcription and the declared addition, not the labs, which are instruments on top of it: cubic-spline kernel with compact support `2h`; density with a self-term; ideal-gas EOS `P = (γ−1)ρu`, `γ = 5/3`; sound speed `√(γP/ρ)`; adaptive smoothing length `h = 1.2·cbrt(m/ρ)`, **written in the density pass and read by the force pass** (matching `cosmic_engine.cpp:394–395`'s back-to-back density/force calls — an ordering the port initially got wrong by using the entry-tick h in forces, matching only the independent reference's own loop rather than the C++, and corrected once the controller traced the C++ call order directly); symmetric pairwise pressure force; Monaghan–Gingold artificial viscosity `α = 1`, `β = 2`, `0.01h²` softening; per-side density guards mirroring `cosmic_sph.cpp:115`'s `if (rho_i <= 0) continue` and `:134`'s `rho_j > 0` pressure-term guard (added after a REQUIRED review finding that a single zero-mass gas body drove `P/ρ²` to `NaN` on both bodies of every pair touching it — a zero-mass body's own fields now stay exactly finite). **The energy equation (`du`, integrated as `internal_energy += du·dt` for gas bodies) is an addition, declared as such: `cosmic_sph.cpp` has no internal-energy update, and the parity number below is between two independent transcriptions of the added equation, not a parity claim against the C++, which does not contain it.** Parity on a 50-body seeded fixture (32 neighbor pairs) against the independent reference: max relative error **5.9×10⁻¹⁵** (~170,000× under the registered 1e-9 gate); Newton-3 residual `|Σm·a|` relative to force scale **7.1×10⁻¹⁸**; energy-consistency residual `|Σm·du + Σm·v·a|` relative to scale **6.0×10⁻¹⁸**; toggle-off path bit-identical to the pre-port kernel on a non-gas fixture, and every pre-existing `cosmic-*` scenario is unaffected by construction (default off). Three labs (`cosmic-gas-collapse` 400 bodies, `cosmic-gas-cloud-collision` 500 bodies, `cosmic-gas-rotating-disk` 500 bodies), each setting the toggle on, reporting through an axis-profile card labeled `Gas profile — [IMPOSED effective gas dynamics]` (binned density/velocity along a declared axis, a documented display convention, not a physical derivation) plus thermal (`Σm·u`) and kinetic (`Σ½mv²`) energy; after 60 ticks (fresh bridge, `setupScenario` + 60 `tick()` in Node), thermal/kinetic totals are 8.0002/1.4710×10⁻³ (collapse, started at rest), 10.005/30.858 (collision), 10.001/28.651 (rotating disk) — one seed each, instrument readings, not physical constants. Evidence: Node 15/15 (`cosmic-sph.node.test.mjs`, including a regression test that `setupScenario` resets `sph_monaghan` to off on the next scenario); Playwright `scale5-gas-labs.spec.js` 4/4 (each gas lab now also asserts `bodies.some(b => b.density > 0)`, so the spec fails if the SPH pass never runs); the frozen live-inventory guard restored to green at scale4 = 13 / scale5 = 16 / total = 397 (scale4's 8→13 is a pre-existing drift from main commit `8c6513c0`, unrelated to this work, brought back into sync rather than left masking the guard; scale5's 13→16 is this task's three additions). |
| FTD-1040 | **Does the Scale 0 `s0-field-shear-layer` seed correctly propagate a sheared flux layer under the frozen linear wave map, and does its behavior contrast with the lattice-gas fluid of the strict laboratory?** | [EMERGENT] within the frozen linear wave map | NEW 2026-09-09 — [`engine/docs/PROGRAM_HYDRO_SCENARIOS_PART_B.md`](../../../../engine/docs/PROGRAM_HYDRO_SCENARIOS_PART_B.md) ("Scale 0: `s0-field-shear-layer` contrast scenario"), seed `engine/src/scenarios/s0_field.cpp`, id registration `engine/src/scenarios.cpp` + `engine/include/ftd/scenario_meta.h`, behavioral test `engine/tests/test_scenario_behavior.cpp::test_shear_layer_propagates()`, JS registration `engine/web/js/scales/scale0/scenario-registry.js` + `scenario-validation.js` + `engine/web/js/config/toggles.js` + `engine/web/js/scales/scale0/runtime/scenario-loader.js`. A native C++ seed on the *current* linear wave substrate — no successor law, no lattice-gas fluid — of a periodic double-layer flux field `J_x(y,z) = A·f(y)·g(z)`, uniform in `x` so `div J = 0` by construction: `f(y) = tanh((y−y1)/δ) − tanh((y−y2)/δ) − 1` with edges at `y1 = N/4`, `y2 = 3N/4`, `A = 0.030`, `δ = 2.5`, Gaussian envelope `σ_z = max(2, N/6)`; `configure_free_wave_terms(rb, false)` isolates the unprojected free wave map (no Gauss projection, single substrate). The seed's first form used a single `tanh` step, which left an undeclared second discontinuity at the periodic wrap point nearly as sharp as the intended step; measured at `L = 48`, `T = 12` ticks it gave RMS 12.76% (over the 8% tolerance) and an edge displacement of 18.0 sites — larger than `cT ≈ 6.93` itself, the signature of the wrap artifact dominating the measurement. The periodic double layer above closes smoothly on the periodic axis (`~10⁻⁴·A` mismatch at `δ = 2.5`) and is the seed of record. **The d'Alembert assertion** (`f` read from the lattice before dispatch; `q(y) = 0.5[f(y−cT)+f(y+cT)]` by periodic linear interpolation; compared to the post-run field): relative RMS **0.0319** (tolerance < 0.08); both edge images displaced **7 sites** each (`cT = 6.93`; floor `0.7cT = 4.85`), measured as each edge's own `argmax|dp/dy|` distance from its declared position; modified-Hamiltonian drift **4.7×10⁻¹⁴** (budget < 1e-6). Golden gate `engine\build_native.bat golden` 7/7 unchanged (golden-neutral by construction); native scenario count 142→**143**; JS registration adds the catalog entry, validation row, `ScenarioId` typedef member, `SCALE0_SCENARIO_OVERRIDES` entry (`isolatedScale0Profile('wave_propagation')`), a `SCALE0_SCENARIO_BOUNDARY` entry (`{ mode: 0, periodicAxis: 1 }`, required so the default sponge boundary does not damp the periodicity-dependent seed), and a visual-profile entry; WASM triple rebuild changed all three `.wasm` binaries with byte-identical glue `.js` files. Evidence: `wasm-scenario-coverage.spec.js` 50/50; `scale0-scenario-health.spec.js` 4/4 (full-catalog run logs `Total: 143 scenarios`, nonzero flux energy on the new row); `scenario-parity.spec.js` — every Scenario-parity and toggle-parity assertion passes, the two remaining failures are pre-existing and environmental (local-only Markdown audit documents absent from the worktree); `scientific-scenario-inventory.spec.js` frozen-inventory guard passes at 143/398, the one remaining failure is the same documented environmental gap. **The contrast, per the design spec's §D language**: the Scale 0 field obeys a wave equation with `div J = s`, not a fluid equation — the shear layer here *propagates* (splits into two edges moving at `±c`, widening only by ordinary lattice-dispersion error) rather than *diffusing*. The lattice-gas fluid — with its derived anisotropic momentum hydrodynamics, conditional on the priced adoption of `phi-hydro-staged-candidate-1` (FTD-1034–1037, Part A) — lives in the strict laboratory, `engine/strict/web/hydro/`, not here. This row asserts a native wave-field measurement only; it asserts none of Part A's fluid-limit content. |

Booking and adoption of any successor law remain owner acts (see
FTD-1034–1037); this draft prices and documents Part B's effective-dynamics
instruments and one contrast seed only, and moves no claim's tag beyond what
is stated above.
