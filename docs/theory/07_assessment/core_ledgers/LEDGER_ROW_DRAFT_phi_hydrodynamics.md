# LEDGER row drafts: hydrodynamic limit of the staged field sector (wave 3)

Date: 2026-09-08. Draft only — **not booked into `LEDGER.md`**; booking is the
owner's act. Source booking document:
[engine/docs/DERIV_STRICT_HYDRODYNAMIC_SECTOR.md](../../../../engine/docs/DERIV_STRICT_HYDRODYNAMIC_SECTOR.md).

**Next-free id.** `python scripts/audit/check_registry.py` (run 2026-09-08
from the worktree root) reports:

```text
FTD ids referenced : 1024  (max FTD-1030)
NEXT FREE FTD ID   : FTD-1031   <- use this, not CLAUDE.md
FC register exists : ['FC-0', 'FC-1', 'FC-2', 'FC-3', 'FC-4']  <- check before adding FC-N
RESULT: clean
```

The script itself resolves the next free id as **FTD-1031** (its scan already
finds ids referenced up to FTD-1030, consistent with session notes that
FTD-1029 and FTD-1030 are claimed by earlier unbooked drafts). The three
draft ids below (FTD-1031, FTD-1032, FTD-1033) are proposed for the owner;
re-run `check_registry.py` immediately before booking to confirm 1031 is
still free, since other unbooked drafts may have claimed it since this run.

---

| FTD-1031 | **Which additive quantities does the staged field sector conserve, and along which schedule?** | [THEOREM — finite, exact] | NEW 2026-09-08 — [`engine/docs/DERIV_STRICT_HYDRODYNAMIC_SECTOR.md`](../../../../engine/docs/DERIV_STRICT_HYDRODYNAMIC_SECTOR.md) gate H0, law `phi-v2-staged-candidate-1`, evidence `engine/docs/evidence/strict-recovery-wave3-exact.json` key `invariants`. Fixed-weight additive invariants of the three collision layers plus streaming, per polarity: dimension 1 (the population). Layer covariance `U C_q = C_{q-1} U`: 0 violations over the full table. Locked-schedule invariants (`w_t = w_0 . U^{-t}`, layer `(l0 - t) mod 3`): dimension 7 per polarity for every `l0` in {0, 1, 2}. The tangent 3-vector lies inside the locked span and not inside the fixed span. Fourth-rank isotropy: 8 body-diagonal velocities (T_xxxx 8, T_xxyy 8, not isotropic), 18 face+edge (10, 4, not isotropic), 26 Moore (18, 12, not isotropic), 18 FCHC-projected (12, 4, isotropic). Scope: the field sector of the staged law on the homogeneous doubly-occupied relation background, single polarity, finite periodic torus; nonlinear and mixed-record observables are not classified by this result. H1 verdict is "other"; per spec section 4.1 Phase 2 is built. |
| FTD-1032 | **What is the long-wavelength linear-response dispersion of the staged field sector, and is it Navier-Stokes-class?** | [DERIVED — linearized Boltzmann closure at the declared reference] | NEW 2026-09-08 — [`engine/docs/DERIV_STRICT_HYDRODYNAMIC_DISPERSION.md`](../../../../engine/docs/DERIV_STRICT_HYDRODYNAMIC_DISPERSION.md), gate H1, evidence `engine/docs/evidence/strict-recovery-wave3-exact.json` keys `dispersion` and `verdict`, contract ee184abf, results 481734be. Exact track at the declared proxy coupling `r0 = 1/695`: verdict label **"other"**; rank of `(I - P0)` on the 185-dimensional complement is 185; `block_dimension = 7` and `block_dimension_first_order_only = 7`, so clause 1 of the NS-class-isotropic rule fails (the smallest closed row space containing the density functional already spans all seven conserved moments under the first-order operators alone; no 4-dimensional block exists). Because `block_dimension != 4`, the sound-speed, transverse, and longitudinal normalized quantities of the NS-class rule are not computed at the exact track. Certified track (256-bit ball arithmetic) at the physical `r(p)` for `p` in {1/96, 1/192, 1/48}: `block_dimension = 7` at every density; `sound_speed_direction_independent`, `transverse_isotropic`, and `longitudinal_isotropic` each report "NOT APPLICABLE (block dimension 7 != 4)". Unit-modulus spectrum of `P(0)`: count 7 (float64 report, not an acceptance criterion), matching the seven conserved modes. This is a prediction for H2, not a theorem about Phi. No Clay Millennium claim. H1 verdict is "other"; per spec section 4.1 Phase 2 is built. |
| FTD-1033 | **Does the linearized Boltzmann period map describe the staged law's actual stroboscopic response?** | [CLOSED NEGATIVE at the registered scope] + [MEASURED — instrument and provenance verified] | NEW 2026-09-08 — preregistration `engine/docs/PREREG_STRICT_HYDRODYNAMIC_RESPONSE.md` (prereg 76d91420), evidence `engine/docs/evidence/strict-hydro-response-v1.json` (evidence 41b57c76), audit [`engine/docs/AUDIT_STRICT_HYDRODYNAMIC_RESPONSE.md`](../../../../engine/docs/AUDIT_STRICT_HYDRODYNAMIC_RESPONSE.md) (audit 6827d425). Registered `L = 32`, single polarity, 23 stroboscopes of 48 microticks each (1,104 microticks/case), 3 directions x 2 wavenumbers x 7 stage-0 conserved modes x 8 seeds = 336 cases, 370,944 physical microticks on the RTX 5090 through WSL2 (elapsed 3388.7405394000234 s). Acceptance band, fixed before execution: relative RMS deviation of the seed-mean trajectory from the locked linear-Boltzmann prediction at most 1/10, jointly with the measured e-folding rate inside a 2-standard-error band of the prediction, at every (direction, wavenumber, mode) cell. Registered acceptance met in 1 of 42 cells; `boltzmann_closure_verified_at_registered_scope = false`; relative RMS across cells from 0.077985 to 0.880760; measured rate below the predicted rate in 41 of 42 cells. The independent audit reproduces the retained report exactly on all seven checked keys, matches an out-of-pipeline recomputation of one locked prediction exactly, and matches independent CUDA replays on 2 of 336 cases (8-microtick Python-vs-CUDA and 48-microtick CUDA-vs-trace, both exact). The audit's post hoc diagnostic (not the registered acceptance) finds the seed-scatter noise floor at or above the registered tolerance (`F >= 0.1`) in 28 of 42 cells and the worst-point deviation at or below twice the worst standard error (`D <= 2`) in 1 of 42 cells; per the audit, a noise floor at or above the registered tolerance means the registered criteria had low statistical power at this signal-to-noise ratio for the affected cells, and this neither verifies nor refutes the Boltzmann closure beyond the registered acceptance rule itself. H1 verdict is "other"; per spec section 4.1 Phase 2 is built. |
