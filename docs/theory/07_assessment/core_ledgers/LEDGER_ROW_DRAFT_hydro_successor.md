# LEDGER row drafts: the FCHC-class successor law and its fluid limit (wave 4)

Date: 2026-09-09. Draft only — **not booked into `LEDGER.md`**; booking is
the owner's act, and adopting `phi-hydro-staged-candidate-1` as a canonical
successor to Φ-v2 is a separate owner act this draft does not take.
**Canonical Φ-v2 (`phi-v2-staged-candidate-1`) is unchanged by anything in
this wave.** Source booking document (local-only, cited by path and
SHA256): `engine/docs/DERIV_STRICT_HYDRO_CANDIDATE_SECTOR.md`
(SHA256 `1bdb6a15e489a0568b34981214a711745cd1643eb0e2fb7d1d0eefb3968fcada`).
Wave ledger: `engine/docs/PROGRAM_STRICT_RECOVERY_WAVE_4.md`. Prior wave:
[LEDGER_ROW_DRAFT_phi_hydrodynamics.md](LEDGER_ROW_DRAFT_phi_hydrodynamics.md)
(wave 3, H0/H1/H2 on the current law, `phi-v2-staged-candidate-1`, verdict
"other" — the trigger for this wave's successor).

**Next-free id.** `python scripts/audit/check_registry.py` (run 2026-09-09
from the worktree root) reports:

```text
FTD ids referenced : 1027  (max FTD-1033)
NEXT FREE FTD ID   : FTD-1034   <- use this, not CLAUDE.md
FC register exists : ['FC-0', 'FC-1', 'FC-2', 'FC-3', 'FC-4']  <- check before adding FC-N
RESULT: clean
```

The four draft ids below (FTD-1034 through FTD-1037) are proposed for the
owner; **re-run `check_registry.py` immediately before booking** to confirm
1034 is still free, since other unbooked drafts may have claimed it since
this run.

---

| FTD-1034 | **What is the priced adoption candidate for a momentum-conserving successor to the current Φ-v2 law, and what does it cost?** | [SELECTION — priced adoption candidate] | NEW 2026-09-09 — [`engine/docs/DERIV_STRICT_HYDRO_CANDIDATE_SECTOR.md`](../../../../engine/docs/DERIV_STRICT_HYDRO_CANDIDATE_SECTOR.md) ("The law and its price table"), law `phi-hydro-staged-candidate-1`, spec of record `docs/superpowers/specs/2026-09-08-hydro-scenarios-and-fchc-successor-design.md` §A.1/A.2/A.6, local-only spec `engine/docs/SPEC_STRICT_HYDRO_CANDIDATE_v1.md` (SHA256 `6c1ccbc6e13550bd0ef5d2f47d43ce2a3ee94648e9a4e606745e24d51cb215b4`). Keeps Φ-v2's periodic SC lattice, SC/FCC relation records, four-microtick schedule, and P5's one-way absorption expiry; replaces the field alphabet with 192 boolean channels per site (2 polarities × 4 passive phases × 24 FCHC-projected velocities: 12 face directions doubled by a binary w-label at speed 1, 12 FCC edge directions at speed √2), drops Φ-v2's `n`/`h`/`ℓ` flags, and replaces the collision rule with a frozen, deterministically generated, exhaustively verified involution on the 2^24 occupancy sets of each site's 24-velocity bank (canonical-order, stabilizer-equal, momentum-equal pairing; generator `scripts/phi_v2_lattice/hydro/tables.py`). Collision table hash `abf25cf26072c03b5b7865fe84d3f31c270263d2c061e7bf3d81e27d783b5375`; of 2^24 = 16,777,216 occupancy states, **63,620 are fixed** (the collision efficiency, a measured property, not a target); every one- and two-particle state is fixed, a consequence of flip-parity under the adopted group (any two-particle class splits into orbits distinguished by whether their stabilizer contains a w-flip element, and conjugation preserves that bit, so no admissible equal-stabilizer partner exists); non-fixed counts by particle number n = 3/4/5 = 1,200/8,766/39,720. Price table (D5 currency, `docs/theory/01_reference/SPEC_ADOPTION_PRICING_RULES.md`; owner ratifies), copied verbatim from spec-of-record §A.6: velocity set V (FCHC projection with doubled faces) — selected-type, the isotropy-carrying choice (18-point and 26-point alternatives both fail isotropy, H0′); collision pairing rule (canonical order, stabilizer-equal partner) — adopted-bit(s), the only free selection content in the table, the table itself forced by the rule; absorption extension to FCC relations — selected-type, one rule extension keeping P5's named expiry; passive k relabeling by sorted assignment — selected-type, declared non-covariant bookkeeping with no dynamical role in the fluid sector; dropped Φ-v2 types n, h, ℓ — credit, alphabet reduction; registered density d = 1/4 — calibration-free preparation choice, not a physical calibration. Conserved by construction, per polarity: particle number and the three-vector momentum `Σv`; not conserved by construction: kinetic energy and the w-label count (verified exhaustively in H0′, verified not to become a spurious slow mode in H1′). **This row prices the candidate; it does not adopt it. Booking and adoption are owner acts. Canonical Φ-v2 is unchanged.** |
| FTD-1035 | **What is the exact fluid limit of the priced successor law, and is it Navier–Stokes-class?** | [THEOREM — finite, exact, scoped] (H0′ census) + [DERIVED — Chapman–Enskog at the linearized Boltzmann level; conditional on the adoption]: anisotropic momentum hydrodynamics (H1′) — and, in the same row, the NS-class gate [CLOSED NEGATIVE at the registered scope] | NEW 2026-09-09 — [`engine/docs/DERIV_STRICT_HYDRO_CANDIDATE_SECTOR.md`](../../../../engine/docs/DERIV_STRICT_HYDRO_CANDIDATE_SECTOR.md) ("Gate H0′" and "Gate H1′"), contract `engine/docs/DERIV_STRICT_HYDRO_DISPERSION.md` (pre-marker SHA256 `830f03b6808f62d66f2a24bea01f563dc73d2cb96d072cc8d496a7d3c6361a50`, full-document SHA256 `2ab4e1e6ea189f22eea9c79e9b35ffb846b7d4f71635cb8a532819a66adf55de`, written and committed to the local record before any number was computed), evidence `engine/docs/evidence/strict-hydro4-dispersion-exact.json` (SHA256 `5659d03dfa816128078467a01d35451ec8353e20639f667e3d991013916c53ad`). **H0′**: exact fixed-weight-invariant census over all 2^24 occupancy sets (`invariants.gram()`/`fixed_kernel()`, exact 24×24 integer Gram matrix, rational nullspace via `flint`), `fixed_dimension = 4`, spanning exactly `{number, p_x, p_y, p_z}` (mutual span membership both directions true) — nothing more, nothing less; fourth-rank velocity isotropy `T_xxxx = 12 = 3·T_xxyy`, `T_xy = 0`, `isotropic4 = true` — necessary, not sufficient, for isotropic transport. **H1′**: verdict at every registered density d ∈ {1/4, 1/8, 3/8} is `label = "anisotropic momentum hydrodynamics"`; `block_dimension = 4` (matches H0′); sound pair present in all directions, `sound_speed_direction_independent = true`; `transverse_isotropic = false`, `longitudinal_isotropic = false`. Exact `c_s² = 1/2` in every direction at every density (forced by `O_h`-invariance of `δ_ab`, the only rank-2 invariant tensor). Exact Galilean factor `g(d) = (2/3)(1−2d)/(1−d)`: `g(1/4) = 4/9`, `g(1/8) = 4/7`, `g(3/8) = 4/15`. Two exact cubic shear constants at d = 1/4: `ν_T2 = 451638699245/588702687858 ≈ 0.7671762140042055` (the eigenvalue along `(1,0,0)` and the T2-type transverse polarization `(0,0,1)` of `(1,1,0)`), `ν_E = 977269782750359806624417/1055020243610136102858810 ≈ 0.9263042947936954` (the E-type transverse polarization `(1,−1,0)` of `(1,1,0)`, where the two transverse roots first split — `discriminant_normalized ≠ 0` there, `= 0` a double root at `(1,0,0)` and `(1,1,1)`); exact cubic identity `ν(1,1,1) = (2ν_E + ν_T2)/3 = 30132049374746406969358764405056593/34505180730993837400343700270851610 ≈ 0.8732616011971988`, holding at all three densities. At d = 1/8: `ν_T2 ≈ 1.1636526001198755`, `ν_E ≈ 1.2132146114983346`, `ν(1,1,1) ≈ 1.1966939410388484`. At d = 3/8: `ν_T2 ≈ 0.7867921635588232`, `ν_E ≈ 1.0317550927540515`, `ν(1,1,1) ≈ 0.9501007830223087`. All shear constants strictly positive at every density; `stable = true`; unit-modulus spectrum of `P(0)` has exactly 4 unit eigenvalues (matching H0′), largest non-unit modulus `0.8778395748204267` at d = 1/4. **NS-class gate, closed negative, theorem-grade for every `G`-equivariant collision table**: under the acting group `G = O_h × Z2(w)` (order 96, verified `G`-equivariant over all 96 elements), the 24-channel occupancy space's traceless symmetric quadratic-velocity (stress) sector decomposes into an `E_g`-type isotypic of multiplicity 2 and a `T_2g`-type isotypic of multiplicity 1 — inequivalent irreducible representations of `G` — so by Schur's lemma the linearized collision operator `K(d)` acts as an unconstrained scalar on the `T_2g` multiplicity space and an unconstrained 2×2 matrix on the `E_g` multiplicity space, with no representation-theoretic relation forcing their eigenvalues to coincide; equivariance under the adopted group *permits*, and the collision table actually built *realizes*, two distinct shear eigenvalues. No relabeling, re-pairing, or retuning that remains `G`-equivariant can equalize `ν_E` and `ν_T2`. **The priced route not taken**: recovering isotropy by construction (not coincidence) requires a strictly larger group under which the `E_g`- and `T_2g`-type stress pieces fuse into one isotypic — the full 4-dimensional isometry group of the 24 FCHC roots (order 1152), which mixes the w-label with space and conserves a passive fourth momentum component (the standard literature resolution); adopting it is new priced content, not implied by anything postulated for `phi-hydro-staged-candidate-1`, and was not adopted under this wave. **Owner ruling 2026-09-08** (design-of-record §D): accept the anisotropic law as the Phase 2 result rather than pursue the larger group. This is a prediction conditional on the priced adoption of FTD-1034, not a theorem about the current Φ-v2 law; canonical Φ-v2 is unchanged. |
| FTD-1036 | **Does the registered GPU campaign confirm the exact anisotropic fluid-limit predictions of the successor law at the registered scope?** | [CLOSED NEGATIVE at the registered scope] (overall acceptance) + [MEASURED] (the cubic anisotropy ratio and the density arm) | NEW 2026-09-09 — [`engine/docs/DERIV_STRICT_HYDRO_CANDIDATE_SECTOR.md`](../../../../engine/docs/DERIV_STRICT_HYDRO_CANDIDATE_SECTOR.md) ("Gate H2′"), preregistration `engine/docs/PREREG_STRICT_HYDRO_VISCOSITY.md` (SHA256 `88057073759137d68ea50367c6c69f5f9dc56548ead006935f718d770e49e276`, locked before execution), evidence `engine/docs/evidence/strict-hydro4-viscosity-v1.json` (SHA256 `35018597bf64bc815a1bc0674bbd619684e4d89b951438ddc6935fbffeecf9d5`), audit `engine/docs/AUDIT_STRICT_HYDRO_VISCOSITY.md` (SHA256 `e73e46b3a722bf14d33889948332d2c70a29669aedaab9428ab019a8837a71c1`). Registered `L = 48`, d = 1/4, 32 seeds/cell, ε ∈ {1/20, 1/10, 1/5}, m ∈ {1, 2}, 35 cells (1,120 cases): 4 shear cells — `(1,0,0)/(0,1,0)` and `(1,1,0)/(0,0,1)` probing `ν_T2`, `(1,1,0)/(1,−1,0)` probing `ν_E`, `(1,1,1)/(1,−1,0)` probing the cubic identity — plus sound, density, Galilean, and Taylor–Green cells, each at both wavenumbers and all three ε. Powered criterion `power = γ_pred_numeric/SE_γ_analytic ≥ 60` and `noise_ratio ≤ 1/3` and `budget_ratio ≤ 1/3`: **18 of 35 cells powered**; predictions are the bias-matched weighted fit on the full numeric `P(k)` trajectory at the registered finite k, with the exact `k → 0` constant reported alongside (`nu_exact_limit`) and the gap (`k4_systematic`) never an acceptance input. Acceptance: measured rate within `max(3·SE_seeds, 3·SE_analytic, 0.05·|γ_pred|)`, jointly with relative RMS within 0.10, powered cells only judged. Run interrupted by a host OS-update restart at 3.23 h of the ≈3.96 h registered estimate; resumed exactly (each of the 1,120 cases is an independent deterministic computation of its own frozen preparation with no cross-case coupling — verified empirically by three independent replays, two kept and one resumed, all byte-identical): 1,069 cases kept, 1 partial case discarded whole, 51 cases (1 + 50 never-started) re-run (8.8 min GPU); two declared lock amendments in `lock.json` (resume machinery; a reporting fix re-keying the top-level derived quantities by the registered canonical cells and resolving a narrative sign ambiguity), both certified scope-preserving by the audit's function-by-function diff — no acceptance-path function touched by either. **Result: 14 of 18 powered cells pass; `verdict_at_registered_scope = false`.** The four failures are three of the five powered sound cells (sound `(1,1,1)` m = 1; sound `(1,0,0)` m = 2; sound `(1,1,1)` m = 2) and the largest-|k| shear cell (shear `(1,1,1)` m = 2, E-polarized) — three of the four are the three largest-`|k4_systematic|` cells, but larger-`|k4|` sound/density cells pass, and the PREREG's advance flag overlapped only partly and was never an acceptance criterion; all 4 failures are rate-only, all 18 powered cells pass RMS. The 17 underpowered cells (16 shear + 1 sound) are present in the evidence and not judged (14 would pass the rate condition informally, 3 would fail). **Measured, from the registered canonical m = 1, ε = 1/10 cells (both powered and passing)**: `ν_E/ν_T2 = 1.3746721972000662` vs numeric-at-k prediction `1.3134151028291003` vs exact k→0 limit `1.2074205089844163` — predicted sign and order of magnitude reproduced; across the ε arm the ratio is `≈1.514` at ε = 1/20 and `≈1.369` at ε = 1/5 (a ~10 % spread, since those rows draw on underpowered or budget-excluded cells — cite the ratio with its ε and cells, not as a single constant); cubic identity from the measured constants `0.9379508354923956` vs numeric-at-k `0.9054509654209024` vs exact `0.8732616011971988`; direct measurement of `ν(1,1,1)` is `0.8429543219771374`, an 11.3 % gap from the cubic-identity route (a reported comparison, not a gate). Density arm: all 3 cells powered, all pass, no underpowered cells. **Galilean arm, stated plainly**: the registered cell passes its decay-rate gate, but `g(1/4) = 4/9` is **not confirmed** — `|advection_ratio_measured| = 0.5627948261119537` is 26.6 % above `g_exact = 0.4444444444444444` and 29.4 % above its own numeric-at-k prediction (`0.43503053682590725`, itself within 2.1 % of `g_exact`); this is narrative, not gated, and does not affect the registered verdict. Audit disposition (verbatim): "the booked verdict `[CLOSED NEGATIVE at the registered scope]` (14/18 powered cells) is SUPPORTED by the lock, the trace and the code; the cubic anisotropy `ν_E/ν_T2 = 1.3746721972000662` (measured) vs `1.3134151028291003` (numeric at k) vs `1.2074205089844163` (k→0 limit) is `[MEASURED]`," with no blocking finding on the lock, the amendment mechanism, the interruption handling, the resume assembly, the trace, the acceptance code, the per-cell results, the derived quantities, the receipt chain, or the commit hygiene. This is a measurement of the priced successor conditional on FTD-1034/1035; canonical Φ-v2 is unchanged, and the law is never presented as Navier–Stokes. |
| FTD-1037 | **Does the Scale 0 strict laboratory correctly run the priced successor law and display its derived, and campaign-measured, fluid-limit predictions?** | [MEASURED — instrument] | NEW 2026-09-09 — [`engine/docs/DERIV_STRICT_HYDRO_CANDIDATE_SECTOR.md`](../../../../engine/docs/DERIV_STRICT_HYDRO_CANDIDATE_SECTOR.md) ("The Scale 0 laboratory" and "Native parity"), spec-of-record §A.7 and its §D amendment, laboratory `engine/strict/web/hydro/{index.html,hydro-lab.js}`, generator `scripts/phi_v2_lattice/hydro/experiments/prepare_hydro_lab.py`, worker `engine/web/js/strict/{hydro-worker.js,hydro-worker-protocol.js}`, Playwright spec `engine/web/tests/strict-hydro-lab.spec.js`. Six preparations generated at L ∈ {16, 32} on the frozen doubly-occupied background at d = 1/4: `hydro-shear-wave`, `hydro-shear-wave-e` (direction `(1,1,0)`, polarization `(1,−1,0)`, showing `ν_E` — added per the owner's amendment of record), `hydro-sound-wave`, `hydro-taylor-green`, `hydro-shear-layer`, `hydro-vortex-pair`. WASM observables added to the successor bindings: block-averaged density and momentum fields (`fields`) and projected moments on the declared wavevector (`moments`). Caption, fixed text byte-identical to spec §D: "Discrete lattice-gas fluid on `phi-hydro-staged-candidate-1`, a priced successor to the selected Φ-v2 (adopted, not derived). Its fluid limit — sound speed, Galilean factor and two cubic shear viscosities at the registered density — is derived by exact Chapman–Enskog (H1′) and measured in the registered campaign (H2′). It is anisotropic momentum hydrodynamics, not Navier–Stokes: the adopted symmetry group forces a cubic viscosity tensor. One realization: statistical noise scales as 1/√N per block." Playwright: **6 passed**; the page's prediction overlay verified bitwise identical to an independent `numeric_stage_map` computation over 201 stages; laboratory manifest sidecar hashes computed from the bytes actually written to disk (newline-safe fix, guard test added). No FPS claim anywhere in the laboratory. **Native parity** (Tasks 8–10, supporting this row): Python ≡ C++ ≡ CUDA ≡ WASM, byte- and event-exact over the fixture families including a crossing-and-hold fixture; CUDA `memcheck`/`initcheck` clean; amortized throughput (`engine/docs/evidence/strict-hydro4-throughput.json`, SHA256 `f745c6f36c157227b9c778cdeda6d1468cf4abf6768dbfa667c1139d5780c64d`, NVIDIA GeForce RTX 5090 via WSL2): 91.89959827890533 / 25.170953532878297 / 15.1129741317515 microticks/s at L = 32/48/64. This row measures the instrument and the display; it asserts none of H1′'s or H2′'s physical content beyond what those rows already carry. Canonical Φ-v2 is unchanged; booking and adoption are owner acts. |
