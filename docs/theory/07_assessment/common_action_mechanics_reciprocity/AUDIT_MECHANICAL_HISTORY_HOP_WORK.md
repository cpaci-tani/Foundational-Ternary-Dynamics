# AUDIT — Mechanical history and production-hop work

**Date:** 2026-07-24  
**Identifier:** `FTD-0449`  
**Status:** `[INSTRUMENTATION — COMPLETE LOCAL VOXEL SNAPSHOT]` + `[MEASURED — PRODUCTION HOP IS KINEMATIC]` + `[CLOSED NEGATIVE — ACTION WORK APPLIED AT MOVEMENT EVENT]`  
**Verdict:** `MECHANICAL_HISTORY_SUFFICIENT_HOP_WORK_NOT_APPLIED`  
**Pre-registration:** [`PREREG_MECHANICAL_HISTORY_HOP_WORK_v1.md`](../10_eft_program/preregistrations/PREREG_MECHANICAL_HISTORY_HOP_WORK_v1.md)  
**Run of record:** `engine/results/ftd_0449/windows_msvc_cpu_L9.csv`

## 1. Observer defect repaired

The pre-existing event journal could audit additive state features but could not
reconstruct mechanics. `HistorySiteState` retained state, chirality sign, and
`J/J_L/J_R`; it omitted velocity, remainder, wave velocities, IDs, internal
labels, timing scalars, and strong/weak fields.

FTD-0449 adds a read-only full `Voxel` copy to each captured site while
preserving the legacy fields for existing consumers. A fixture populating every
explicit voxel field has identical source/captured hash
`6030272945638338050`. The actual movement event records source and target
mechanics with zero velocity and remainder transfer residual.

The journal remains observational. Journal-on and journal-off bridges have
identical complete explicit-state and RNG hashes after the production tick.
The strengthened native-charge observer test passes.

## 2. Actual production-hop result

The locked movement-only fixture isolates one positive x-directed hop. Source
and target flux are zero, while a remote fixed field gives

$$
(\nabla\cdot J)_{source}=0,
\qquad
(\nabla\cdot J)_{target}=1.
$$

The exact FTD-0443 endpoint action work is therefore

$$
W=G_C[(\nabla\cdot J)_{target}-(\nabla\cdot J)_{source}]
=0.085424543102854369.
$$

The field hash is unchanged across the tick, so this is the fixed-field case
to which the exact hop identity applies. Production transports velocity and
remainder unchanged. Its flat particle energy is
`0.18896786684739469` before and after: energy change exactly zero.

Hence

$$
\Delta E_{particle}-W=-0.085424543102854369.
$$

The movement event does not apply the interaction work.

## 3. Consequence for the production tick

`phase_movement` is kinematic transport plus collision handling. It is not a
finite-work transition generated by the declared site coupling. Existing
continuous forces act earlier in `phase_forces`; FTD-0442/0443 already show the
legacy production rule is not the declared action variation.

Therefore event-native work mechanics would be a new dynamics branch. It
cannot be described as merely exposing an exchange already hidden inside the
movement event.

The result also separates two questions cleanly:

- the journal can now observe a complete local event;
- the frozen event itself does not conserve the proposed particle-plus-field
  energy because it performs no corresponding exchange.

## 4. Next admissible construction

An observer-only candidate can now be defined losslessly:

1. snapshot complete source/target states;
2. compute exact endpoint work;
3. apply the FTD-0447 longitudinal response under its isolated-hop assumptions;
4. store equal-and-opposite recoil on an oriented half-tick link;
5. require the reverse transaction to restore both voxel snapshots and clear
   the link record exactly.

This candidate must remain selected until a discrete action or update principle
derives its time centering, momentum branch, and field Hamiltonian.

## 5. Reproducibility

- campaign SHA256: `c5bc17df6e3442efdc1fb5cc348bd023a62e182f7b7e311009e6e8b1dceb59d7`
- journal header SHA256: `4a9aedc650fe882c0cb6421901784095da4ea079d3ccbc985dd412148583955a`
- journal implementation SHA256: `94ebb526f3f31cb53d8907109ba29bd207e3d8e3828dcca6d2c2c7b31b620b91`
- observer-neutrality test SHA256: `61ebff46f3f90ca1decd4022f8b5b7fa5a2c4f2b6bbbd2b0fdbce0e071c2110d`
- record SHA256: `469e949395dac49a955a3d35f725ae57fb9117ee8eb8248051359ce41c630bea`
- compiler: pinned MSVC `14.44.35207`, Release
- backend: forced CPU production tick
- result: `MECHANICAL_HISTORY_SUFFICIENT_HOP_WORK_NOT_APPLIED`

No production dynamics were changed.
