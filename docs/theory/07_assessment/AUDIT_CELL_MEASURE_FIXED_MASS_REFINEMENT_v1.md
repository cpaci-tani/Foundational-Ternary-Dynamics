# Audit — cell-measure fixed-mass refinement v1

**Ledger ID:** FTD-0648  
**Verdict:** `CELL_MEASURE_FIXED_MASS_STATIC_DEPINNING_CONSTRUCTIVE`

The locked map `r_m=r_q=r_kappa=(2/w)^3`, `r_beta=w/2` is dimensionally
coherent for cross-resolution comparison and is not a numerical fit. All 54
arms, nine cubic orbits, exact extensive identities, strict monotonic barrier
tests, held-out slopes, and hashes pass independently.

The result repairs the static fixed-mass scaling problem. FTD-0649 later puts
every scale factor into one reciprocal action transaction and closes its
one-step gates constructively. Long-horizon mobility remains open. Neither
result licenses a particle, pole, electromagnetic charge, Lorentz, or
production claim.
