# Audit — FTD-0676 canonical pre-contact mode decay v1

**Status:** `[SELECTED DYNAMICS — CONSTRUCTIVE PRE-CONTACT EXPONENTIAL TRANSFER]`  
**Claim level:** numerical fact about the default-off connected common-action
research branch; no production or particle promotion

## Audit verdict

FTD-0676 validly establishes that the corrected mode-6/7 target energy decays
approximately exponentially over preregistered ticks 8--64 at a fresh half
amplitude. Both polarities pass the locked fit, amplitude-stability, exact
accounting, and inverse gates. The independent certificate recomputes the
result from the CSV rather than trusting the JSON summary.

The strongest licensed statement is:

> In the selected reversible finite connected action, the canonical bare
> matter doublet transfers energy into the coupled field at an amplitude-stable
> pre-contact rate `Gamma_E ~= 0.00653712` per tick.

## Scope controls

- The canonical mass metric is used; FTD-0675's retracted observable is not.
- The fit model, window, parent rate, tolerances, and verdict map were hashed
  before the `5e-7` histories ran.
- The calculation is pre-contact on `L=97`; it is not an open-boundary or
  infinite-volume calculation.
- Complete energy is conserved to `1.07e-14` and state-only reverse recovery is
  below `6.2e-13`. Therefore the measured rate is reduced-coordinate transfer,
  not fundamental information loss.
- The selected constituents, bond graph, and face/edge variables remain priced
  research types outside P1--P5 production ontology.

## Ontological consequence

The excited constituent doublet is now disfavoured as an autonomous matter
carrier. This does not close the localized dressed rest basin: radiative
relaxation of an internal excitation is compatible with a stable object. Exact
inversion requires the detached field to remain in the universe state, while
operational object identity may quotient that outgoing environmental record.
The data do not yet show that the core approaches the same localized rest
family, so a prepared radiating transient remains possible.

## Required successor

The next admissible claim-changing campaign must test localized-basin return,
not refit the bare target envelope. It must vary volume or implement a
qualified outgoing boundary, measure constituent phase-space distance and
bound-dressing distance to the rest family, separate detached field energy,
and then extract frequency/residue/width from a preregistered complete-response
estimator. Failure of localized core identity closes this prepared
configuration as persistent matter.
