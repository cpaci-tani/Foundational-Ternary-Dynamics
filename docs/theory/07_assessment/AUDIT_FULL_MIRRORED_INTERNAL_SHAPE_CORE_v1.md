# Audit — FTD-0605 full mirrored internal-shape core v1

**Status:** `[AUDIT — COMPACT LOCAL SHAPE BASIN CLOSED NEGATIVE]`

1. The protocol releases all six local zero-centroid constituent coordinates
   and adds no new primitive, force, history, or production state.
2. Exact algebra reveals a structural defect in the selected binding model:
   its six-dimensional Hessian has rank three. Central distances restore three
   strains but cannot restore three rigid rotations.
3. Twenty-nine of 32 independent optimizations exhaust the locked budget. The
   three returned candidates all hit the same `0.20` chart boundary with
   nonzero downhill gradient. No registered interior stationary core exists.
4. Internal pair distances stay within about `2e-4` of `sqrt(2)`, so the escape
   is not disintegration. It is a torque-driven orientation excursion along
   the exact soft sector.
5. The periodic Green evaluator agrees with direct minimum-energy fields to
   `2.22e-17`; the available common-action and inverse arms pass at roughly
   `1e-15`. Optimizer failure is not evidence of a field or transaction error.
6. Because only three boundary states reach the dynamics, this campaign does
   not license a 32-phase force-sign conclusion or a barrier estimate. Those
   fields are intentionally `null` in the JSON record.
7. The next least-assumptive repair is a global `SO(3) x strain` coordinate
   factorization. It tests whether the field selects a stable orientation
   without pretending that a local component box is physical. Only a negative
   global-orientation result would move the compact candidate to the extended-
   carrier gate.

Focused CTest PASS as a classification test. The independent certificate
reconstructs all 32 rows, proves the exact rank-three positive-semidefinite
Hessian, verifies the three boundary states and 29 exhausted searches, checks
direct-field/common-action residuals, and preserves the extended-matter scope.
No production or scenario change was made.

