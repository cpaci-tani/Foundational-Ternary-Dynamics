# Audit — Genesis amplitude lock and common action (FTD-0567)

**Verdict:** `[PROVED-SCOPED — GENESIS_ACTION_OBSTRUCTION]`
**Date:** 2026-07-26
**Production changes:** none.

**Identifier correction:** the hash-locked preregistration was written as
FTD-0565 before the concurrent ledger collision was detected. FTD-0565 belongs
to the Peierls-clock toy and FTD-0566 to the degeneracy criterion. This result
is canonically FTD-0567. The preregistration bytes remain unchanged at their
pre-execution SHA256; only the post-run identifier and artifact paths were
corrected.

## Locked result

The preregistered observer executed 48 exact arms:

```text
32 single-substrate arms
16 dual-substrate arms
```

over four overshoots, two wave energies, two drain settings where applicable, and both polarities.

| Diagnostic | Result |
|---|---:|
| arms | 48/48 |
| distinct single-path post amplitudes at one fixed register | 4/4 |
| maximum amplitude residual | 0 |
| maximum flux-energy residual | 0 |
| maximum wave-energy residual | 0 |
| maximum action-threshold residual | 0 |
| fixed-register energy-withdrawal spread | `0.875` |
| evaporation signed-preimage collapse | exact |

Focused CTest `genesis_action_obstruction` and the independent Python proof passed.

## Decisive identities

For `|J|=K_GENESIS+x`, accepted single-substrate genesis gives

```text
|J|' = x,
Delta H_J = K_GENESIS*x + K_GENESIS^2/2.
```

The first identity proves the rule retains the overshoot rather than locking one amplitude. The second proves one fixed state-only rest quantum cannot account for every accepted input.

For uniform `J=A e_x`, `div J=0` and `grad J=0` at every amplitude. The written action produces the same candidate-state values below and above the production threshold. This exact pair is sufficient to disprove the documented claim that the written `delta S/delta s` generates the magnitude-triggered genesis rule.

## Provenance lock

Pre-execution preregistration SHA-256:

```text
C8DC397572217AF20CD69E01BB398CBB66C3E58A49C309A07A5B8F8F974925C8
```

The run record stores hashes for the production phase-write map, Lagrangian header/source, energy ledger, action regression, injectivity regression, preregistration, observer, test, and independent proof.

Artifacts:

- `engine/include/ftd/eft/genesis_action_obstruction.h`
- `engine/src/eft/genesis_action_obstruction.cpp`
- `engine/tests/test_genesis_action_obstruction.cpp`
- `scripts/proofs/proof_genesis_action_obstruction.py`
- `engine/results/ftd_0567/windows_msvc_cpu.json`
- `docs/theory/10_eft_program/derivations/THEOREM_GENESIS_ACTION_OBSTRUCTION.md`
- `docs/theory/10_eft_program/preregistrations/PREREG_GENESIS_AMPLITUDE_ACTION_OBSTRUCTION_v1.md`

## Licensed interpretation

The current genesis rule remains a selected stochastic manifestation mechanism and has a measured ignition threshold. It is not a conservative amplitude lock and is not generated by the written action. The dual path supplies no field/wave payment, and evaporation is many-to-one on the frozen variables.

The surviving research branch must choose explicitly between an open-system stochastic law and an enlarged conservative state with an event/environment reservoir, or derive a different nonlinear bound-state action. Production remains unchanged.

FTD-0569 subsequently proves that the reservoir alternative works for one
accepted event but not as a fixed finite local reversible production cycle.
Its surviving form is an open environmental channel carrying branch records
and variable energy.
