# Audit — FTD-0705 moving dressed-matter transverse-field growth

**Verdict:** `[SELECTED DYNAMICS — MIXED; NO REGISTERED THRESHOLD SEPARATION]`

The independent certificate pins the preregistration, runner, and four result
files; verifies 3 arms, 12 fits, and 288 carrier-aware mode rows; recomputes the
native dispersion/detunings, complex late-time regressions, current
normalizations, contrast values, collinear controls, radial records, and all
execution gates. It passes.

The strongest favorable datum is the `R50@v=0.50` field growth (`R^2=0.986`,
amplitude `2.52x`). It is not threshold-specific at the locked strength:
source-normalized growth is only `2.48x` the below-threshold arm, and the
registered requirement was `5x`. The `R45` channel fails both linearity and
amplitude growth. No post-hoc mode, tolerance, or classifier replacement is
admissible on these data.

The correct statement is that coherent moving selected matter generates a
nonzero transverse field while a clean radiation threshold remains unproven.
The result does not support “flux lines are photons,” “flux is a literal wake,”
or “the object radiates above `v_edge`” as established claims.

See
[`ANALYSIS_MOVING_DRESSED_MATTER_TRANSVERSE_FIELD_GROWTH_v1.md`](../../10_eft_program/derivations/constituent_complete_matter/ANALYSIS_MOVING_DRESSED_MATTER_TRANSVERSE_FIELD_GROWTH_v1.md).
