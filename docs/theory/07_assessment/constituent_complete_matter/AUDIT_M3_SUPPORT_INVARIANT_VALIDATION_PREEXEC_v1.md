# FTD-0755 — Support-invariant matter-family pre-execution audit v1

**Status:** `[HISTORICAL PRE-EXECUTION LOCK — REGISTERED ARMS SUBSEQUENTLY RUN]`  
**Date:** 2026-07-30  
**Scope:** observer-only validation of the selected `DerivedCompactPair`
finite-time matter predicate; production and established dynamics unchanged

## Lock record

The registered result directory
`engine/results/ftd_0755/` was absent when this audit was frozen. The
independent preflight certificate passed `14/14`. No `--candidate` or
`--fibre` mode has run.

| object | SHA-256 |
|---|---|
| protocol | `1E713DB4B997DAED0D55F098A6E7D63FC0F2D773391CE44FFE03AADD92A504BC` |
| predicate interface | `B11E087E2E7E16375C173185233AD001AB8B9F049E9B9B5A3156D8618CB4F104` |
| predicate implementation | `752CE7C3B03A9944C1E7016A62CCA584FAC868EF191D8241ACEE7E6C9C550D21` |
| locked CUDA runner | `F2CCACB00E0DF697B10838E3E85EC636E38BC94E2B2707A55A86811FFE80DCEA` |
| independent certificate | `6C5C4A946C672DD5FFABCBA62F45DFB25AF1A32A8CF788D3F227BAF4F85A9AD4` |
| WSL2 executable | `6190DF3C84C8B25620C6332588283FEE57CB1BDD5F40FBBC8D098D838751BF36` |

The executable is the explicit-rounding ordered CUDA target built under
WSL2 Ubuntu 22.04. Its registered modes refuse execution unless the compiled
protocol hash equals the frozen protocol.

## Qualification record

The no-artifact qualification path ran for eight transactions at `L=321` on
the face, edge, and body rays. All three paths executed and passed. Their
final maximum common-action residuals were respectively
`4.54914e-14`, `5.10685e-14`, and `5.33185e-14`; final energy residuals were
at most `1.44600e-15`, recoil diagnostics at most `4.07233e-15`, and causal
speed excess was zero.

These qualifications start from the established nondegenerate incoming pair.
They deliberately do not satisfy `P_core` and cannot count as matter-family
evidence. The protocol's initial `L=33` qualification wording was corrected
before lock to `L=321`: the finite-support CUDA parent requires its established
non-self-contact geometry. No registered volume, state, perturbation, tick,
gate, or output was changed.

## Frozen output contract

Each fully executed registered arm must produce:

- nine candidate CSV/JSON pairs: three rays times `center`,
  `energy_hostile`, and `graph_hostile`;
- three causal-fibre CSV/JSON pairs, one per ray;
- `306` candidate rows per CSV (`153` states at each of two volumes);
- `130` causal-fibre rows per CSV (`65` states at each of two volumes).

An initialization or transaction infrastructure failure may produce a shorter
record together with explicit initialized/executed bits. The independent
certificate treats that as the frozen infrastructure verdict, not as a
successful physics arm and not as certificate corruption.

Candidate records carry the core margins, complete constituent variables,
transaction gates, root regularity, observer/ladder checkpoints, event-branch
flags, and nested-volume differences. Fibre records carry state, local-field,
bound-dressing, and initial global-energy differences. The certificate applies
the first-failed verdict map without fitting or repair.

## Pre-execution epistemic status

At the time of this lock, FTD-0755 was unexecuted. The established result was
only that the
support-independent predicate is exact, field-independent by construction,
and passes its algebraic/symmetry controls. Persistence under held-out hostile
states, nested-volume equivalence, and pre-contact causal-fibre independence
was `[OPEN]` pending registered artifacts and the independent certificate.

The later registered result is
`M3_VALIDATION_INFRASTRUCTURE_UNRESOLVED`; see
`AUDIT_M3_SUPPORT_INVARIANT_VALIDATION_RESULT_v1.md`. No physics arm ran.
