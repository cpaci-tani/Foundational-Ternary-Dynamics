# Audit — connected-block analytic matter modes

**Campaign:** FTD-0640  
**Verdict:** `[COMPLETE REVERSIBLE CLASSICAL MATTER-MODE SPECTRUM
CONSTRUCTIVE; FIELD/PHYSICAL-PARTICLE CLAIMS OPEN]`

## Findings

1. The prediction is fixed before evolution from the FTD-0638 center,
   FTD-0637 analytic Hessian, and unchanged scalar `M_INERTIAL`. No frequency
   or mass matrix is fitted to trajectories.
2. All 48 primary coordinates and all 39 registered controls execute for 256
   forward and 256 state-only reverse ticks. The independent certificate
   recomputes phases, degenerate-subspace leakage, scaling, mirrors,
   covariance, and every aggregate bound from CSV records.
3. Worst primary phase error is `1.1267%` and worst leakage is `1.5226%`, both
   inside their locked `2%` and `10%` gates. The result is not based only on
   boundedness.
4. Exact continuity, Gauss, work, energy, causality, and nonlinear-root
   residuals remain below `2.0000e-11`. Worst energy drift is `1.9585e-13`
   and state-only recovery is `5.1017e-11`.
5. The observer-only Jacobian cache cannot waive a physical gate. It reuses a
   Broyden-updated numerical Jacobian only as a root-finding accelerator;
   stale matrices fall back to the original central-difference build. The
   cached/direct repeated-step equivalence test passes at `1e-9`.
6. The post-result rigid-subspace projection is not part of the preregistered
   verdict. It identifies three translation-dominated and three
   rotation-dominated soft modes with squared subspace defect `4.3862e-7`,
   followed by 42 deformation modes and a `221.67476x` stiffness gap.
7. The soft modes have nonzero curvature. They are lattice-pinned collective
   coordinates, not Goldstone modes and not evidence of microscopic Lorentz
   or continuous Euclidean symmetry.
8. The selected object contains 16 signed constituents, continuous subcell
   phase-space records, a selected Moore binding graph, and matched field
   dressing. The result does not support the claim that a single ternary site
   is matter.
9. Production remains unchanged. The campaign licenses `complete local
   classical matter-coordinate spectrum` only. It does not license `quantum
   spectrum`, `particle mass`, `field pole`, `photon`, `spin`, `statistics`,
   `freely translating particle`, `common cone`, or `unitarity`.

Independent certificate:

- `scripts/proofs/proof_connected_block_analytic_matter_modes.py`.

The next live discriminator is the independent face/edge field spectrum about
the same center, followed by coupled-mode classification and finite boost.
