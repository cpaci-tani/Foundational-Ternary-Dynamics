# AUDIT — The FTD Boundary Map

**Tag:** `[SYNTHESIS]` — cross-document integration of existing boundary claims at their
canonical tags. **Introduces no new theorem; promotes no tag; derives nothing new.**
**Last refresh: 2026-08-11** (§R below folds in FTD-0781–0803 and FTD-0890–0904; body text after §R predates it
and is annotated in place with ⚠ where the refresh supersedes it).
Every row points at a canonical source; where a source and this map disagree on a tag,
the canonical source (LEDGER / TRACKER_ONTIC_TRUTH / SPEC_ALGEBRAIC_SPINE) wins.

This map folds in (a) the **FC-W carrier-narrowing hardening** of the α boundary (FTD-0314/0326/0327 + the `√(4G*−1)` no-graded-home result), (b) the **cosmology-sector boundary** — Λ predicts zero (FTD-0331), the whole sector is imported (FTD-0332/0334) — §3.5, (c) the **no-native-quantum-dynamics** dispersion ceiling (FTD-0270) + the pre-registered P2 mass-gap measurement (FTD-0333) — §2d, (d) the **no-sound** boundary (FTD-0298/0299), and the FTD-0244 K-BIND reconciliation. *Zero promotions.*

**Scope:** the *second clause* of the Number-One Goal — "...and rigorously establish what
we **cannot** [derive]." This document is the single-page map of what the discrete ternary
ontology provably does **not** determine. **This map distinguishes two different kinds of
claim, and conflating them was flagged as a corpus-wide problem in
`AUDIT_REDTEAM_DISSECTION_2026-07-01.md` §2 (2026-07-01):** a **proven impossibility** is a
structural no-go theorem, valid within stated assumptions; an **attempted-and-failed** result
is an empirical or computational trial that came back negative, which raises the prior that
the next attempt also fails and nothing more. Neither is "a deliverable" to be celebrated —
both are honestly reported constraints on how far discreteness reaches. See §0.5 for the
classification of every section below.

**Companion canonical sources:**
- `02_foundations/FOUND_MODULUS_ARGUMENT_FRONTIER.md` (FTD-0336) — **the one-statement positioning result this map instantiates:** a discrete/deterministic/forward-only substrate owns the *modulus* half and cannot self-supply the *argument* (the chosen adjoint of its own lossy map); FTD's boundaries below are its five instances. Replaces the "second i" framing.
- `core_ledgers/LEDGER.md` — per-claim provenance (source of truth for tags)
- `core_ledgers/TRACKER_OPEN_ITEMS.md` — every live `[OPEN]`
- `01_reference/SPEC_OPEN_MATH_BY_SECTOR.md` — sector research queue + foundational-obstruction framing
- `01_reference/MONOGRAPH_FTD_CONSTRUCTION.md` Part II — the route-invariant α boundary in narrative form
- `audits/AUDIT_ALPHA_OPERATOR_FORCING_ROUTE_INVARIANCE.md` (FTD-0242)
- `audits/AUDIT_RSI_LEG3_CONDITIONAL_THEOREM.md` (FTD-0243)
- `../10_eft_program/derivations/FOUND_OPERATOR_CALCULUS_AXIOMATIZATION.md` (FTD-0244)
- `02_foundations/FOUND_SPACETIME_FORCING_BOUNDARY.md` (FTD-0253)
- `../09_mathematical/number_theory/FOUND_AGM_PLACE_BRIDGE_AND_DELTA.md` (FTD-0327 — the FC-W carrier hardening)
- `../01_reference/SPEC_COSMOLOGY_FRAMEWORK_BOUNDARY.md` (the cosmology-sector boundary; FTD-0332/0334)
- `../03_derivations/gravity_and_cosmology/DERIV_LAMBDA_SCALE_COVARIANT.md` (FTD-0331 — Λ predicts zero)
- `../03_derivations/foundational_mechanics/ANALYSIS_QUANTIZATION_LATTICE_MODES_v1.md` (FTD-0270 — the dispersion ceiling) + `ANALYSIS_QDYN_MASS_GAP_v1.md` (FTD-0333 — the P2 mass-gap measurement)

---

## R · REFRESH 2026-08-06 — folding in FTD-0781–0803

> ⚠ **THIS REFRESH IS NOT YET BOOKED IN THE LEDGER.** A row is drafted for owner ratification;
> until it is booked, §R (and §6.1, §R.7, the two new §0.5 species, and the §7 addendum) is
> **prose, not a registered claim**, and nothing in it may be cited as settled. Precedent:
> FTD-0335 and FTD-0346, both booked at edit time.

*The body of this map was last revised 2026-07-01 (FTD-0346). The five weeks since produced
~25 ledger rows, several of which change rows below materially and two of which do not fit
this map's existing taxonomy at all. This section is the delta; the body is annotated ⚠ where
superseded. **Nothing here promotes any tag.***

### R.1 · α's *evidential* base collapsed; its *structural* boundary stands with one exception

FTD-0242 (route-invariance), FTD-0243 (Galois-blindness) and FTD-0244 (K-BIND) stand exactly
as written. **FTD-0314 does not.** ⚠ FTD-0785 (2026-08-03, inside this window) found that
FTD-0314's C2 leg asserts `Γ(1/3)` and `Γ(1/4)` algebraically independent — which is
**Waldschmidt Conjecture 5.23, an open problem** (the repo's own FTD-0377 says so) — so the
cross-CM-field exclusion is conditional on an *open conjecture*, not on Chudnovsky alone, and
**FTD-0314's headline `[THEOREM]` over-consolidates**. FTD-0784 separately found C3's period
field `F = Q̄(π, Γ(1/4))` inconsistently defined (it asserts `Ω ∈ F` while `√π ∉ F`);
the conclusion survives, the definition needs repair. With that stated, what fell *additionally*
is the evidence for the *identification* those boundaries bound:

- **FTD-0791** — FTD-0319's ~2.65M-polynomial look-elsewhere scan sits at the **chance base
  rate**: the null expects 1.42–1.67 matchers at the registered gate and the scan found 1; a
  random number near 137 is matched to 2 ppm by that family **79%** of the time; at the
  operative gate the second leg eliminates nothing (1 → 1); both tolerances were set by the
  observed deviation, and the preregistered outcome had probability 0.9942 under its own null.
  Retagged `[MEASURED]` → `[SELECTION]`. **The scan-rigid count is ZERO.**
- **FTD-0802** — OT-3.3 (the 2.87M extended scan) failed its **preregistered** base-rate
  control (`N_null = 0.0014`, Outcome B), and its published count was additionally *wrong*: the
  "0 genuinely-new cubic dual-matchers" line was a hardcoded literal that no code tested; the
  corrected runner reports **5 distinct dual-matchers, 4 beyond the master quadratic**.
  Retagged `[SELECTION]`; withdrawn as support for OT-5.1.
- **FTD-0321 + FTD-0803** — the CM leg is range-limited. The registered `|d| ≤ 907` domain
  UNIQUE-CONFIRMED stands and was *genuinely informative* (~7× inside the informative regime;
  crossover at `|d| ≈ 6,676`), but the deep extension finds **1,271 fundamental dual-matchers
  by `|d| ≤ 500k`**, so `d = −4`'s dual-match privilege is a range artifact. Per the tracker's
  tracker (**2026-08-04, FTD-0321**), **OT-1.9 as cited is gone**; OT-5.1's support is OT-1.5 plus
  the bare arithmetic coincidence `|μ_K| = |disc(K)|`. The separate **2026-08-05** correction is
  the one that *partially restores* the registered domain and bounds the enhancement at ~4–5×. Registered incidental worth carrying: the scan's target sits at
  `G*(i)` to `4.8×10⁻¹³` **by construction**, but CODATA `1/α` sits `0.998` window half-widths
  away — on the **edge** of the registered tolerance, not at the peak. Structural cause
  registered: `G*` is the value of
  an `SL₂(ℤ)`-invariant at the order-2 elliptic point — a closed-form-provable saddle with
  log-divergent value density — though the enhancement is **bounded at ~4–5×**, and the real
  swamping at scale is the `D^{3/2}` CM count, not the singularity.
- **FTD-0792** — the engine runs on CODATA `ALPHA`; `ALPHA_TREE` feeds nothing, and a
  source-lint CTest **fails the build** if the derived root reaches a force path. No engine
  result can be evidence *for* the master quadratic.

**Net:** `x₊ = 1/α` retains **no numerical-uniqueness support of any kind**. It remains `[SMC]`
— the tag was always right; what is gone is the evidence that made it *feel* stronger than its
tag. The 1.26 ppm agreement itself is unchanged `[NUMERICAL FACT]`: it is arithmetic.

### R.2 · The substrate selects *against* the quartic (FTD-0794) — the "forces ¬X" class gains a member

Most boundaries in §1–§5 have the form *"the substrate does not force X."* FTD-0794 joins the
smaller class that already contains §2b (finiteness *opposes* reversibility) and §3.5
(Λ = 0 `[DERIVED]`): **the substrate's own structure forces ¬X.**

> ⚠ **CORRECTED 2026-08-06 (same day).** An earlier draft of this paragraph also listed §2c and
> §5's no-sound result in this class. Both were over-reads and are withdrawn. §2c's premise is
> itself vacated (§R.8), so it cannot serve as an exemplar of anything. And **no-sound is not a
> "forces ¬X" result**: §0.5 and §5 both classify it **ATTEMPTED**, and its canonical tag is
> `[SELECTION] + [BOUNDARY]` (`ANALYSIS_LATTICE_WAVE_SECTORS_v1.md:120, :147`). Under this
> document's own precedence rule (header), the canonical source wins over this map. Chain
link 4 — "minimum `C₄` support and null-flatness select the quartic clock", the last unwithdrawn
lattice-to-quartic bridge — is refuted: under the plaquette rotation `(x,y) → (−y,x)`,
`x²+y²−1` is invariant and `x⁴+y²−1` is not, and no rescaling repairs it (symbolic solution set
empty). The ring's own dynamics agrees — `Q`, `R` are the degenerate eigenvalue-2 pair of the
`C₄` Laplacian, i.e. two simple harmonic oscillators. Null-flatness is the *conclusion*, not a
hypothesis. `G*` is pinned only by a minimal-degree tie-break; a degree-6 family satisfying the
same assumptions gives clock constants 2.8375–3.1185. **Registered consequence: `G*` enters the
quartic-clock chain by choice — *at link 4*.** ⚠ The scope matters and must not be stripped: it
does **not** follow that the abstract constant `G*` is mathematically invalid. §1's Watson identity and the
J-twisted `det_ζ` equality remain theorem-grade identities at their declared assumptions. FTD-0839
corrects the stronger claim: the FQCR spectral realization is conditional on a twisted circle,
chiral half-line, origin, scale, first-order operator, and multiplicity one. `import_ledger.json`
SS-3 now keeps only the abstract definition/equivalent identities self-set; the physical
clock/spectral roles are conditional or selected. FTD-0840 then gives the
sharp constructive complement: after explicitly adopting the signed pair
energy `lambda(q|q|)^2`, a unique reversible energy-preserving discrete
recursion exists and its continuum generator has the exact `G*` shape factor.
The frozen production core lacks that coupling, and the finite-step map is not
the exact quartic flow, so this closes a conditional mathematical mechanism,
not link 4 or a native tick cadence. FTD-0841 narrows the remaining type debt:
production already contains the local canonical pair `(J,W)`, and the
self-pair tensor gives the exact radial quartic identity
`||J otimes J||_F^2=|J|^4`. The resulting vector recursion is exact and
energy/angular-momentum closed only **conditional on adopting that onsite
coupling**. Cubic symmetry alone does not force it; the production spatial
energy is not jointly closed with it; and only invariant polarized sectors
have the scalar `G*` clock law. This is a constructive boundary refinement,
not link-4 recovery. FTD-0794's own record states what survives: Watson 1939 (the BCC
Green's function carrying `Γ(1/4)`) and the `z = 1/4` reflection-ratio/half-integration
eigenvalue fact — both classical mathematics.

FTD-0842 closes the combined positive edge/onsite energy only as a selected
simultaneous reference map. Its exact inverse is dense on a connected
quotient, so local residual support does not make it a one-tick Moore-local
update. The same positive edge form has only the spatially constant kernel;
every nonzero bounded profile carries quadratic stiffness. Thus the simplest
single-field positive architecture cannot retire link 4. A local transaction
type and a bounded zero/soft relative-mode mechanism remain priced imports or
open recovery targets.

FTD-0844 constructs both at selected reference scope by splitting common and
relative duties. The unique positive soft-relative boundary in the registered
swap-symmetric quadratic channel class is `b=a`; it yields common propagation,
onsite relative quarticity, exact sector-ledger closure, and compact P4-local
support. The frozen dual core is `b=0`, and decoupling blocks readout. The
boundary therefore moves from mathematical existence to physical provenance:
derive or adopt the rank-one cross-gradient, then close formation and
energy-booked readout without target coding.

FTD-0846 closes the narrow position-readout discriminator. Exchange symmetry
forces a common/even pointer to see only the symmetric-square phase. Positive
quadratic energy plus zero clock stiffness forces the bilinear signed coupling
to vanish. A selected exchange-odd pointer evades both obstructions first at
degree four, `kappa(r-q)^4/4`, and admits an exact onsite reversible energy
transaction. The new boundary is no longer whether conservative signed
readout is mathematically possible.

FTD-0848 closes the selected reference record transition. In the registered
symmetric polynomial class, a three-well latch is degree-minimum at six;
`beta*x^2*(x^2-A^2)^2` has exact barriers and a deterministic acquisition
threshold. Its compliant onsite AVF tick plus bath/controller ledgers closes
energy, while the persistent basin quotient to `{-1,0,+1}` is explicitly
many-to-one. The boundary is now physical provenance: whether native
genesis/evaporation and flux/state dynamics realize an equivalent odd pointer,
three-basin latch, and exported-energy ledger without target coding; whether
coupling the record to the common channel preserves phase/rate compliance;
and whether a microscopic bath/thermal cost or selector/Born coupling follows.

FTD-0850 closes the first production-equivalence branch negatively at its
declared scope. Current genesis gives a deterministic context-blind signed
acquisition fragment, and evaporation gives genuine many-to-one loss, but
every finite-energy unlocked record remains evaporation-eligible and no
event-level receiver inherits the withdrawn/erased energy and metadata. The
boundary is therefore not “find irreversibility.” It is to derive a native
strict record barrier and event reservoir—most narrowly in the still-untested
movement-enabled matter-field sector—or explicitly price the FTD-0848
latch/reservoir as an adopted open-system type.

FTD-0851 closes the receiver's minimum mathematical type. At zero exported
energy, no energy-only state can remember whether `+1` or `-1` was erased, so
an odd label is unavoidable. At positive export one signed amplitude carries
both sign and energy; the selected balanced L/R representation is zero-common
and signed-relative. Production's remainder-reset wall, sign-blind
annihilation exhaust, and read-only complete event journal are three separate
fragments, not that physical receiver. The live boundary is now dynamical:
derive a reciprocal barrier and an energy/current-closed relative pulse that
propagates away before receiver reuse, or declare the environment open.

FTD-0852 closes the propagation mechanism at reference scope. A unilateral
odd-amplitude rail is local, one-cell causal, injective, and energy/current
closed; it vacates the event port without overwriting the previous pulse. Its
finite-rail boundary is exact: a scalar tail-energy ledger closes energy
but loses sign, so the signed tail must continue into fresh degrees of freedom.
This does not exclude fixed-dimensional exact-real natural extensions.
Production's L/R difference is a homogeneous local candidate channel because
equal sources cancel, but the current event map never excites it, the common-
only ledger cannot see its energy, and the bidirectional wave stencil is not
the exact clearing shift. The remaining boundary is event coupling plus a
reciprocal protected-record barrier.

FTD-0853 closes the local deposit at selected ready-port scope. Six opposite
L/R face impulses preserve the common field and, on `Q0=0`, receive exactly
the released energy while encoding `Q1=s*sqrt(24B)`. The pre-event background,
sign, and energy are then recoverable on the declared reduced domain. Six faces
are minimal only in the preregistered equal-orbit first-shell full-cubic class;
the result is neither a universal receiver minimum nor a complete-state
natural extension. The remaining production boundary is now sharper: derive
the event energy `B`, form compliant ready ports without reading a target,
book relative-field energy/current, show port clearing under production
propagation, and supply a reciprocal barrier/full-state disposition—or declare
and price the missing environment.

FTD-0855 removes two qualifications from that list at reference scope. The
adopted manifested matter diagnostic supplies the exact positive decrement
`B=gamma E_REST`, and `D=Q/sqrt(12)` proves that the cubic radial shell is the
causal odd-history rail in normalized coordinates. The rule
`D_0'=s*sqrt(2B), D_{j+1}'=D_j` moves the occupied port outward while writing
the next event, so readiness is recursively formed and receiver energy closes.
The `E_REST` role is imposed rather than substrate-derived. The remaining
production boundary is a reserved directed relative channel and corresponding
dual-energy/current ledger, followed by reciprocal record protection and a
complete-state/environment disposition.

FTD-0856 closes the reciprocal-barrier algebra at minimum reference scope.
Strict deterministic protection and event exchange cannot share the identical
complete input, so a hold/exchange eligibility distinction is required.
Likewise, an emitted pulse and its time reverse require outgoing/incoming
orientation in the first-order rail class. The controlled identity/swap matrix
attains both bounds and is exactly reciprocal and energy preserving. Production
already has `locked` and a relative field/conjugate-velocity type, but no
protected characteristic split, local activation law, controller-work ledger,
or production matter/field scatterer. An isolated `ftd::eft` witness now passes
its focused Release CTest `1/1`; the live boundary is the physical construction,
not the existence of a reciprocal mathematical interface.

FTD-0858 resolves the next ambiguity branch-wise. Production already has a
deterministic local event-acceptance coordinate once seed/tick/local inputs are
fixed, but it is a function of the common field. The common projection has an
arbitrary relative kernel, so this trigger cannot determine receiver amplitude,
energy, or readiness. The exact relative incoming/outgoing chart exists, but
the frozen C18 primitive tick is dispersive and not the one-cell history shift.
Thus “eligibility exists” is true only at source-predicate scope; “the physical
reciprocal port is actuated” remains false. At this FTD-0858 stage, the boundary
was a common-to-relative transducer or a protected/dispersive compliant
receiver, together with controller work and full-state accounting. FTD-0860
below refines that branch.

FTD-0860 resolves the abstract transducer branch. On a supplied nonzero
canonical receiver, `z'=sqrt((I+B)/I)sJz` is an exact target-blind symplectic
action pump with `I'=I+B`. It cannot be extended positively and
rotation-equivariantly to an empty pair, and its unlabelled output is not
faithful: `F_+(z)=F_-(-z)` and action retains only `I+B`. Therefore the live
boundary is no longer “find any transducer.” It is the explicit choice between
a declared lossy action mixer and the already-constructed faithful signed
rail, followed in either branch by production localization, relative-energy
and loss/controller ledgers, and bounded export/clearing. The isolated CTest
passes `1/1`; production is unchanged.

FTD-0862 closes the abstract faithful-rail construction only after pricing a
prepared phase standard. For `phi_j^n=phi_0+kappa j-omega n`, one-cell
outward transport preserves the reference iff `kappa-omega in 2pi Z`. Relative
to that baseline, the quarter-turn output recovers `B=I-I_*` and
`s=sign(beta wedge Z)` at every downstream depth. The finite rail has the exact
ledger `Delta H_ex=B-E_tail` and a retained-load bound `H_ex<=N B_max`; a
complete tail pair preserves the orientation that scalar energy loses. This
moves the boundary again: the abstract faithful transducer and bounded open
export exist, but only as a separately selected nonzero calendar and directed
rail. The unresolved physics is their native phase source, maintenance work,
protected/cubic propagation, production event hook, relative/tail ledger, and
  controller. FTD-0858's C18 obstruction remains active.

FTD-0863 narrows the selected construction without adding a new type. The
phase standard is factored from the energetic signal: conserved `beta` defines
the local frame `(e,f)`, while a separate signal `D=af+be` may begin at zero.
The exact reciprocal gate swaps matter amplitude `m` with `a`, leaving `beta`
and `b` unchanged. Thus emission and absorption are one involution,
`|D'|^2/2=B`, `sign(beta wedge D')=s`, and the reference action is not spent or
counted twice. The abstract tail-energy burden of a baseline-loaded carrier is
absent in this factorization. The physical boundary is now reference formation
and robust backreacting maintenance, target-blind eligibility and controller
work, protected cubic signal propagation, production event coupling and
energy-current accounting, and operational hiding. Periodic winding constrains
`omega` but does not derive the `G*` clock gearbox.

FTD-0865 converts the instantaneous reciprocal abstraction into a closed
autonomous Hamiltonian boundary. The scalar swap is anti-symplectic, so full
matter and signal modes are mandatory. A harmonic phase gate realizes exact
one-cycle hold/swap with a transparent reserve: `I_min=I_0-I_r`, `I(T)=I_0`,
and `I_r=B/2` for empty-signal emission. The reference is recursively reusable
only after complete nonoverlapping cycles. For a strictly convex nonlinear
clock, however, `dXi/dI_r=chi^2 integral g^2 K''/(K')^3 dtheta>0`; quartic
`K proportional I^(4/3)` therefore cannot be a universal fixed-coupling,
load-blind exact gearbox in this class. The live boundary is dynamic
eligibility plus either isochronous/quartic clock separation, compensation, or
a declared load sector, followed by native mode formation, protected cubic
transport, production coupling/accounting, and operational hiding.

FTD-0867 removes only the abstract frozen-eligibility sub-debt. The existing
persistent ternary latch supplies `epsilon=s^2`, the unique even quadratic
hold/exchange clutch. Releasing it at a phase-gate zero has exactly zero clutch
interaction work, and the outgoing canonical signal retains
`B=|D'|^2/2`, `s=sign(beta wedge D')` after a local reset request. Because the
active map squares to identity, failure to acknowledge/reset before the next
cycle reverses the export. The live controller boundary is therefore native
latch acquisition, autonomous acknowledgement/reset, microscopic reset/bath
work, and synchronization—not an unspecified eligibility bit—followed by the
existing compensation, native-mode, cubic-transport, production-accounting,
and operational-hiding debts.

FTD-0869 closes the acknowledgement ambiguity at reference scope: completed
local signal formation is the acknowledgement token, so no extra bit or
target-coded magnitude is required. It also closes smooth exact finite-time
reset negatively for locally Lipschitz autonomous ODEs. The selected
nonsmooth cusp reset supplies a conditional finite-time closure with explicit
controller loss and scalar-bath gain `kappa A`, and an empty output-port
handoff restores local readiness. This does **not** close the physical
environment boundary. A microscopic bath/thermal account, native formation,
detuning/noise/overlap/backpressure robustness, nonlinear-clock compensation,
protected cubic transport, production energy-current coupling, native `G*`
synchronization, and operational hiding remain open.

FTD-0871 closes the logical bath/reset-state boundary at the actual ternary
layer. The oriented signal already retains the latch's `Z_3` label, so
controlled subtraction resets the latch while its inverse reconstructs the
input from that signal. An energy-only record would fail because it identifies
the two orientations. Consequently an extra acknowledgement bit,
reset-history trit, or scalar bath is not a minimum logical type. FTD-0869's
cusp/bath construction remains an optional continuous-`x` realization, not a
mandatory actual-layer import. The live boundary is the physical controlled
permutation and work ledger, continuous hardware only if retained, native
formation and robustness, protected cubic signal clearing/export, production
energy-current coupling, nonlinear-clock compensation, native `G*`
synchronization, and operational hiding at the FTD-0871 boundary.

FTD-0872 closes the logical controlled-permutation form at the same interface.
The unique `F_3` isometry satisfying sign-preserving ready transfer and
positive orientation is `R(s,o)=(-o,s)`, with `R^2=-I`; `R^-1` is the
reciprocal absorption branch. A conditional rule that applies `R` only to an
empty port and otherwise holds is noninjective, since `(1,0)` and `(0,1)` then
share one output. The remaining boundary is consequently physical scheduling
or reciprocal reflection, amplitude/action and controller-work closure,
protected cubic transport, native formation, production coupling, robustness,
and `G*` cadence—not the existence of a finite-state logical permutation.

FTD-0873 closes the reference continuous-actuator/work branch conditionally on
an imposed harmonic law and amplitude embedding. One canonical carrier pair
plus one independent clock pair gives exact hold, `R`, and `R^-1`; the clock
and interaction exchange at most `Omega A/2` and return to zero endpoint
residual. This does not derive the scale or production hardware. Repeating the
active branch (`R^2=-I`) and norm-only gating are closed negatively as one-shot
schedulers. The remaining boundary is native carrier/clock formation and
scale recovery, dynamic gate-zero acquisition/release, reciprocal
backpressure-safe handoff, protected cubic transport, production coupling,
robustness, synchronization to the separate quartic `G*` calendar, and
operational hiding.

FTD-0874 closes the selected finite-horizon scheduling/rail branch. Existing
global tick parity and rail-coordinate parity alternate the disjoint bonds on
which `R(a,b)=(-b,a)` acts. A prepared isolated record advances exactly one
cell per tick and is exactly reversible. An occupied bond retains both labels
by reciprocal exchange, but a fully occupied control remains fully occupied,
so retention is not congestion-free progress. The injectivity argument also
excludes a nontrivial predecessor entering a literal fixed `done` state; the
reference solution is continuing outward motion. The remaining boundary is
native intersite Hamiltonian formation, multidimensional routing,
collision/backpressure resolution, reciprocal finite boundaries, production
coupling and energy-current accounting, robustness, synchronization to the
separate quartic `G*` calendar, and operational hiding.

FTD-0875 closes the local canonical Hamiltonian reference-lift and bond-
current branch. The scalar finite rail has a common symplectic form, but only
by pairing each site with its boundary mirror; the obstruction is locality,
not abstract symplectic existence. In the registered onsite-direct-sum class,
one canonical pair per site is minimum and sufficient. The imposed positive
Hamiltonian reproduces both parity-layer orientations exactly after one clock
cycle and books a local antisymmetric bond current, prepared-record energy
transfer, clock-action reserve, and zero endpoint residual. The actual section
is a special zero-backreaction orbit, not a no-cost hardware result. The
remaining boundary is native formation of the canonical doublet and its
scale, multidimensional routing, reciprocal finite boundaries, sustained
congestion handling, production coupling, robustness, synchronization to the
separate quartic `G*` calendar, and operational hiding.

FTD-0876 closes the native carrier-coordinate availability branch. For every
`h>0`, two consecutive flux slices are in exact bijection with the stored
phase-complete pair `(J,wave_vel)`. The three vector components therefore
supply three onsite canonical pairs, and the symmetric-stiffness free
kick/drift preserves their canonical form exactly. This is a kinematic/free-
sector result, not preparation or production actuation. Uniform damping is
only conformally symplectic, nonidentity Gauss projection is noninvertible on
the unconstrained phase space, and Langevin/genesis/loss/boundary maps are
outside the theorem. The remaining boundary is native record preparation and
persistence, amplitude/scale recovery, production insertion of the FTD-0875
bond generator, constrained Gauss dynamics, environment-complete loss,
multidimensional routing and finite boundaries, congestion, robustness,
quartic-`G*` synchronization, and operational hiding.

FTD-0880 closes the matched constrained-coordinate and static-record branch
without adding a selected type. On the selected oriented-face complex,
`L=DD^T`, `q=DJ`, and `p=L^+DP` give an exact transverse/charge canonical
split with `{q,p}=Pi_Q`. For neutral ternary `s`,
`(J,P)=(D^TL^+gs,0)` is the unique minimum-energy static record section, and
matched curl recursion preserves the charge. The record coordinate is local,
but its conjugate is not uniformly finite-range and translation invariant; a
Laurent-polynomial root-count makes that obstruction exact. Abstract affine
preparation is reversible only when the discarded longitudinal discrepancy is
retained. The live cell-centred central-difference/18-point-SOR Gauss pass has
a different Fourier symbol, finite iteration, and a default manifested-site
skip, so exact-projector promotion closes negative. At the FTD-0880 boundary,
dynamic native preparation, a physical environment carrier and reversible dynamics, matched
production bond actuation, amplitude/scale recovery, multidimensional routing
and finite boundaries, finite-support uncontained completion, robustness,
quartic-`G*` synchronization, and operational hiding remain open.

FTD-0882 closes the conditional local reversible preparation branch at the
selected reference scope. A six-face residual/environment quarter-turn with
fresh zero ports makes each checkerboard color an affine orthogonal projection;
alternating colors converges from empty flux to the FTD-0880 minimum-energy
record, while retained signed ports reverse every finite history exactly. The
local source-work identity yields the declared limiting equality
`E_field=E_history=||J_s||^2/2`. No fixed size-independent finite layer count
can solve every probe. The boundary therefore moves from whether any local
reversible preparation exists to autonomous fresh-port generation/recycling,
a positive source-reservoir microdynamics, local stopping, moving-source
continuity, nonperiodic/uncontained boundaries, finite-capacity backpressure,
production migration, scale, separate quartic-`G*` synchronization, and
operational hiding. No selected type, Born rule, or production path is added.

FTD-0884 closes finite-horizon readiness and positive source-work accounting
at reference scope. An explicit cyclic bank of capacity `C` supplies `C`
fresh reversible layers from an all-zero state. A generic nonzero signed port
returns at the next cycle, so indefinite freshness closes negative only for
that explicit bank; exact-real compression is not excluded. With imposed
positive battery energy `b^2/2`, strict reserve and sign preservation uniquely
give `b'=sgn(b)sqrt(b^2-2w)`, and battery loss exactly cancels the FTD-0882
source work. The boundary moves to a canonical Hamiltonian reservoir and
native formation/recharge, unbounded/open or justified compressed signed
history, 3D routing and backpressure, moving-source continuity, production
migration, scale, separate quartic-`G*` synchronization, and hiding. The
battery law remains imposed; no selected type, Born rule, or production path
is added.

FTD-0886 refines the phase-complete boundary. Centering the active mode on the
fixed Gauss source and restoring both canonical conjugates yields an imposed
positive clocked Hamiltonian whose one-cycle map is exactly the FTD-0882
quarter-turn and whose clock action returns. In the same coordinates, the raw
source work is exactly cancelled by the change in
`U_int=-s*y+s^2/2`; the fixed-source local canonical ledger therefore closes
without an independent battery. Conversely, the FTD-0884 square-root battery
is exact only as a one-amplitude/Lagrangian-section ledger. Its symplectic
cotangent lift does not remove exactly `w` from full oscillator energy away
from the zero section, and a phase-blind state-dependent action drain is not
symplectic. A complete-pair open-history shift is a valid reversible canonical
reference, but a finite cyclic bank still has the FTD-0884 capacity boundary.
The live boundary is now an autonomous common parity/source Hamiltonian,
dynamical source formation/motion/recoil, physical open or bilateral
complete-pair history (or justified compression), 3D routing, production,
scale, separate quartic-`G*` synchronization, and hiding—not a post-hoc
canonical battery. No selected type, Born rule, or production path is added.

FTD-0888 closes the first clause at **imposed reference scope**. One autonomous
phase circle, divided into six disjoint smooth windows, compiles the two-color
ordered gate sequence without an external integer-parity switch. Adding one
more instance of the existing onsite canonical-pair type gives an exact
history/reaction splitter: a mixing angle `eta` sends fractions
`cos^2(eta)` and `sin^2(eta)` of ready-slice residual energy to history and
reaction. The FTD-0886 history-only endpoint is therefore positive-energy
saturated: nonzero zero-initialized reaction energy cannot be added without
reducing history energy. One scalar reaction coordinate is not symplectic, so
one canonical pair is minimum in the registered onsite-direct-sum class.
Equal splitting at `eta=pi/4` is unique only conditional on an imposed exchange
symmetry of the two output channels and is `[SELECTION]`, not a P1--P5 theorem.
The surviving boundary is native formation of the phase windows and their
origin/scale, physical identification of the reaction pair with a spatial
ternary source and its mass/inertia/intercell motion, physical open history,
3D routing, production, separate quartic-`G*` synchronization, and hiding.
No sixth contextual-actualization selected type, Born rule, or production path
is added.

FTD-0890 sharpens “spatial ternary source” into two separate debts. First, a
scalar-only source-reaction magnitude cannot select a nonzero cubic vector:
`Fix(T1u)={0}`. One `T1u` copy is odd-dimensional and cannot carry a
nondegenerate alternating form, so the minimum orientation-free carrier in the
registered onsite-direct-sum class is `T1u+T1u`, or three canonical pairs.
This does not retract FTD-0888's one-pair scalar-reaction minimum. A one-pair
spatial slice remains conditional on independently supplied oriented data.

Second, once the matched field impulse supplies that orientation and the
selected relativistic dispersion supplies `E0` and `c`, an exact cotangent
chart maps quadratic reaction energy to physical momentum. Free source drift
and the inherited face current close reversible transport and continuity.
Local energy/momentum compatibility fixes `sin^2 eta=K_req/E_res`; the earlier
equal split is therefore conditional rather than universal and closes
negative as a general recoil law. The surviving boundary is native vector
common-action formation and field coupling, derivation of `E0`/inertial mass
from a stable localized family, interacting source formation, physical open
history, production, separate quartic-`G*` synchronization, and hiding. No new
selected type, Born rule, or production path is added.

FTD-0892 closes the **selected phase-space origin of the three-pair carrier**.
For selected constituent canonical variables, an exact Helmert symplectic
reduction separates `(X,P)` with `X=N^{-1}sum x_a` and `P=sum p_a` from all
internal modes. The carrier required by FTD-0890 is therefore already the
collective matter sector and costs no additional selected type. Internal pair
impulses cancel and external constituent impulses sum exactly in `P`.

The mass question splits. Conditional on selected constituent dispersions,
strict convexity proves
`E_coll=sqrt((sum epsilon_a)^2+c^2|P|^2)` and
`M_coll=sum epsilon_a/c^2`. But a stable rest point and positive static Hessian
admit arbitrary positive kinetic metrics, so they cannot determine the
absolute scale. A binding/field offset that does not participate in the
boosted family changes rest energy without changing inertial curvature. The
static-to-mass inference is therefore closed negative in the registered
class; dynamically dressed boost participation is the live discriminator.

The momentum question also splits. `P` is exact as a collective canonical
matter variable, while `Z^3` translation, Bloch/quasimomentum, and positive
Peierls curvature do not supply an additive continuous field-plus-matter
Noether charge. The surviving boundary is substrate formation of the selected
constituent phase space and graph, dynamical field-to-triplet coupling, an
exact total physical momentum/stress ledger, dressed absolute-mass closure,
stable source formation, production, quartic-`G*` synchronization, and hiding.

FTD-0893 sharpens **dynamically dressed mass closure** into an exact
identifiability theorem. Let `A` be the positive quadratic energy Hessian on
the complete time-odd matter--field tangent state and let `B` be the
rank-three linearization of an independently defined additive physical total
momentum. Fixed-momentum minimization gives

```text
M = B A^-1 B^T.
```

This tells us exactly how a moving field coat can contribute: it must have an
odd tangent direction and participate through `A`, `B`, or both. It also closes
the energy-only route negative, because the same `A` under `B -> sB` gives
`M -> s^2M`, while a static energy offset contributes nothing. The current
common action supplies exact energy and measured co-motion but not a valid
total `B`; the natural spline-Poynting map remains closed negative for coupled
recoil. The next boundary is therefore an independent local stress/momentum
state or exact additive operational quasimomentum ledger, cross-checked by
energy curvature, impulse/velocity, and matter--field partition. No selected
type, Born rule, `G*` role, or production path is added.

FTD-0894--0896 sharpen the **operational quasimomentum ledger** branch. The
character dual of local `Z^3` translation is
`T^3=R^3/(2 pi Z^3)`, and character products add quasimomentum exactly modulo
reciprocal vectors. This does not impose a finite-torus ontology. There is no
continuous group-homomorphic section `T^3 -> R^3`, because its image would be
a compact additive subgroup of `R^3`. There is also no strictly finite-range
translation-invariant spectral weight equal to the global unwrapped
coordinate: finite-range weights are periodic, while the exact principal
coordinate is an infinite-range sawtooth with a zone-edge branch.

The spectral branch therefore has an exact price. It must either remain
torus-valued, select a nonlocal branch, or retain integer winding/history
`w` with `k_tilde=k+2 pi w`. The last option still needs a native winding
update and an independently fixed physical momentum unit. Neither is selected
here. A new local stress/bond-impulse state with its own exact exchange law is
not ruled out. The live boundary is now to derive one route, insert its `B`
into FTD-0893, and recover the same inertia from energy curvature,
impulse/velocity, and full matter--field momentum partition. No Born, `G*`,
production, or type-currency status changes.

FTD-0897 closes the smallest **conditional reciprocal-carry transaction** on
that branch. For a supplied opposite increment, the principal-zone carries
must update an integer reservoir by `W'=W+c_1+c_2`; this uniquely restores the
real lifted aggregate, reverses exactly when the inverse increment is
available, and telescopes over events. The result is an exact history ledger,
not a selected substrate field.

The boundary now moves from “find a winding update” to “derive the transaction
that powers it.” Periodic band energy is blind to the carry; the same update
admits inequivalent reservoir-energy laws, and opposite quasimomentum transfer
need not conserve band energy. One local matter--field action must therefore
derive `q`, exact work/backreaction, physical carry ownership or transport,
and the momentum unit `p_*`. Only that complete action can define the FTD-0893
`B` map and test the three mass routes. No Born, `G*`, production, selected
type, or import-currency status changes.

FTD-0898 closes the impulse origin **inside the selected relative-quartic
reference model**. Its discrete-gradient endpoint conserves
`H_D=Pi^2/(2m)+lambda D^4`, generates exactly equal-and-opposite channel
impulses, composes with every FTD-0897 reciprocal carry, and reverses under the
signed inverse step. The associated continuum quartic obeys the exact law
`T A=sqrt(pi) G* sqrt(m/(2 lambda))`.

The physical boundary is the exact common-mode invariant. The composition
cannot transfer its relative clock/reaction impulse into a common matter--
field mode, does not identify the two channels, does not fix `p_*`, gives no
energy law for integer carry, and does not make the finite-step recurrence an
integer-tick `G*` cadence. The next gate is one substrate-derived common local
action that breaks this decoupling while conserving the full energy and total
momentum and retaining reversible history. No Born, production, selected type,
or import-currency status changes.

FTD-0899--0901 close the **imposed reference common-action existence** branch
and expose its next obstruction. The velocity-linear connection has nonzero
curvature and exact positive canonical energy; its conserved `P` and
mechanical `K=P-gamma D` give `Delta K=-gamma Delta D`. The registered
discrete-gradient map additionally conserves total canonical angular momentum,
reverses under the signed step, and composes with reciprocal carry conditional
on `p_*`. FTD-0899 and FTD-0900 are preserved invalid; the source-marker-only
FTD-0901 repair passes inherited `87/87`.

The boundary is not “no common action” but “no continuously engaged critical
clock in this minimum class.” `i` supplies orientation but does not derive
`gamma`, and nonzero continuous `gamma` adds rest-sector clock Hessian
`gamma^2/M`. The exact critical quartic survives only at `gamma=0`, which also
removes the mechanical exchange. A context-blind phase clutch or positive
compensated action with switching work is now the live route. Physical
identification, normalization, absolute mass, finite-tick `G*`, production,
Born, hiding, and all currency counts remain unchanged.

FTD-0902--0903 sharpen this from one ansatz to an order boundary. Every
positive cyclic connection with nonzero linearization `B=DA(0)` adds the Gram
clock Hessian `B^T M^-1 B`; exact zero-Hessian criticality therefore requires
`B=0`. The existing signed self-pair gives one imposed escape:
`A(D)=gamma|D|D` has zero origin derivative and folds at `P=0` into
`Lambda=lambda+gamma^2/(2M)`, retaining the exact critical quartic and its
continuum `G*` period factor while exchanging
`Delta K=-gamma Delta(|D|D)`.

The new boundary is not “critical-clock-preserving common exchange is
impossible.” It is: the registered escape is rest-sector, `C1`/not-`C2`, and
oscillatory. Generic moving sectors regain a quadratic ray term, and symmetric
rest cycles have zero net common drift. Net transport therefore requires a
separately audited context-blind rectifier/controller, or the critical clock
must remain separate from the momentum gearbox. `gamma`, `p_*`, mass,
production, Born, hiding, and all currency counts remain unchanged.

FTD-0904 closes net rest-sector rectification only for one imposed oriented
law. With retained unit polar axis `e` and time-odd branch `chi`,
`A=chi gamma q^2e` preserves the exact critical quartic and yields exact
cycle displacement proportional to `1/G*` and mean speed per squared
amplitude proportional to `1/(G*)^2`. The exact boundary has moved rather
than disappeared: a nonzero polar function of `D` alone cannot be both even
and inversion-equivariant. The reference therefore retains precisely the
polar and clockwise/counterclockwise signs lost by a symmetric square. Native
formation, maintenance, and erasure of that orientation memory, controller
work, `gamma`, `p_*`, mass, moving-clock closure, production, Born, hiding,
and all currency counts remain open.

FTD-0905--0907 close the narrower question of whether the existing native
types can represent those retained signs. A neutral ternary dipole is origin
independent and transforms as a polar vector; its normalized value supplies
`e`. The bilateral projected flux/wave-velocity wedge
`ell=q_+p_- - q_-p_+` is spatially scalar and time odd, supplying `chi`.
Symmetric square and Gram data erase the two signs. Under an imposed central
quartic memory law, nonzero `ell` is conserved and has a strict bounded radial
minimum. But its centrifugal inverse-square term proves that the same mode is
not the pure radial G* clock. The minimum registered architecture therefore
separates clock and chirality memory. Production formation, persistence,
transport, work/erasure, memory-law provenance, rectifier coupling, `gamma`,
scale, mass, cadence, Born, hiding, and all currency counts remain open.

### R.3 · A new §2-family result (FTD-0796) — and a corpus misattribution this map does not carry

*(§2a of this map contains no Bell/CHSH claim, so nothing here corrects it; the misattribution
below lives in other documents, listed at the end of this subsection.)*

**FC-1's completeness clause *alone* forces CHSH ≤ 2** — if the four
observables are elements of `A₅` under one setting-independent measure, their pushforward *is* a
joint distribution and Fine (1982) applies. **No locality assumption enters the argument.** The
corpus misattributes the bound to Postulate 4 (Moore locality) and lists "violate locality" as
an escape; that is wrong, and nonlocality buys nothing, because using it breaks FC-1 itself. The
only live door is **superdeterminism** (already declared at FTD-0329 row 15), priced exactly at
`S ≤ min(2+3M, 4)` with Tsirelson at `M* = 0.27614237`. **Live contradiction registered:**
FTD-0329 adopts measurement dependence while constitution §2.3 and PL-2 assert statistical
independence — mutually exclusive; under ledger precedence FTD-0329 governs, which makes PL-2
unfalsifiable contrary to its own text.

**Three further registered findings from the same row, which a boundary map must carry:**
- **FTD's actual prediction is the triangle, not the cosine.** Its registered correlator
  `E(θ) = −(1 − 2|θ|/π)` optimises to a global CHSH maximum of **exactly 2.0000000000** — a
  maximally correlated *local* model, 29.29% below Tsirelson.
- **The registered `2√2` mechanism fails arithmetic:** its two stated factors give
  `2 · √2 · 2 = 5.657 > 4`, above the algebraic CHSH maximum.
- **A new unbooked debt:** explaining why nature stops at 2.828 rather than 4, given that
  Tsirelson's bound is a theorem about the formalism FC-1 declines. Also registered: `M` must be
  fine-tuned to **0.34%** across every setting-generation method *including cosmic photons from
  causally disconnected quasars*; and `gauss_project()` is never invoked by
  `bell_lattice_test.py`, so PL-2's "all tested configurations" is narrower than advertised.

**Propagation owed (per the FTD-0796 record, not yet done):** `AUDIT_BELL_ANALYSIS.md`,
`DERIV_OBSERVER_BELL_MECHANISM.md`, `SPEC_FTD_FRAMEWORK_V1.md` §2.3, and PL-2.

### R.4 · The carrier program is negative at every identified door; one class foreclosed

- **FTD-0798** closes the Kähler–Dirac first-order carrier (fails C1 and C3 at sight) and kills
  its *motivating premise as a class*: the second-order flux **already** carries a Born-valued
  positive conserved density, which forecloses every future argument that FTD needs a
  first-order sector to obtain a Born measure. Salvage: `M18` admits a local cubic-covariant
  3-square decomposition.
- **FTD-0797** refutes the de Rham common-cone route (spatial degree-blindness does not touch
  the cone; the production stencil is `M18`).
- The `G*`-carrier question is now negative at **every identified door**: linear functionals of
  the field (exact — any closing linear functional is harmonic), the affine/locked field sector
  (FTD-0781), the compact-law pair-breathing channel (FTD-0783), generic wells and capacity
  walls (the **bracket theorem**: wells pin at `n ≈ 2`, walls at `n = ∞`, `G*` lives at `n = 4`
  strictly between), and the fully-built movement sector (FTD-0786 — which also *corrects* the
  program's map of itself: the reciprocal-transaction problem is **not** a blocker, and the
  coupled sector should not be treated as unexplored).
- **FTD-0784** bounds the prize: even a carrier passing Gates A+B+C delivers `G*` but **cannot** deliver the FC-W surd — `δ ∉ Q(G*, π, √π)`. (A *transport*
  of FTD-0314 C3's argument, demoted accordingly; not an independent theorem.)
- **`eps` (the compact-law well depth) has no derivation.** FTD-0788's lattice-quantum proposal
  was refuted by FTD-0790: `z·G(1) = z·G(0) − 1` **identically** — a statement about the discrete
  Laplacian's source term, true for any lattice, with zero lattice content — and the pattern was
  already ruled inadmissible by FTD-0388 ("an adoption is never a derivation").

**What survives is one decidable question.** FTD-0789's **Maxwell trichotomy**: for central-force
networks at zero tension, `rank(H) ≤ B` so zero modes `≥ 3N − B`, and — rigid network → `n = 2`
(harmonic); flex extending to a finite mechanism → `n = ∞` (flat); **`n = 4` requires first-order
flexible *and* second-order rigid**. Both registered configurations fail it from opposite sides.
Two screens have failed *since* the trichotomy was stated, and one construction was withdrawn
*by* it (FTD-0787, withdrawn by FTD-0789; then FTD-0800 and FTD-0801, both
`[CLOSED NEGATIVE — SCOPED]`, with FTD-0800's `N=6` follow-up expressly an
`[EXPLORATORY NUMERICAL SCREEN]` — **there is no `N ≤ 6` theorem**). This converts an open-ended
carrier search into a decidable criterion, which is the one genuine structural gain of the period.

### R.5 · Two new *species* of boundary, which §0.5's taxonomy has no slot for

**(D) The empirical boundary (FTD-0795)** — verdict
`SPINE_HAS_NO_ROW_THAT_IS_DEVIANT_LIVE_AND_TESTABLE`. Two *different* things sit under this
verdict and must not be merged:

- **PL-1 is a falsification, not a testability limit** — and it is registered
  `[CONDITIONAL CLOSED NEGATIVE — PL-1 PHYSICAL READING]`, not unconditionally closed. FTD's
  ontology is one continuous local flux manifesting on threshold, so splitting an excitation
  50/50 forces `α ≥ 1` (maximal bunching) while the measured `α = 0.0188 ± 0.0067` is **146σ
  below that floor**; because `α` is coincidences over singles at one beamsplitter, efficiency
  and gain cancel, so the result invokes no Hilbert space and cannot be deflected by rejecting
  the quantum formalism. ⚠ **The registered escape is different and must be stated:** the result
  is conditional on the *manifestation-equals-detection* identification, which FTD itself tags
  `[CONJECTURE]` — and taking that escape **empties PL-1 of physical content** rather than
  saving it. PL-3 is additionally dead as physics; PL-6 is additionally **STALE** (the FTD-0564
  monopole downgrade and FTD-0355 `D = 3` `[SELECTION]` are unpropagated).
- **Only PL-4/5 are the testability boundary.** PL-4 is live but untestable by a factor
  **5×10⁷ in energy scale** (`E_QG,2 = √24 E_P = 6.0×10¹⁹ GeV` vs LHAASO `> 1.2×10¹² GeV`);
  PL-5 is live but **~39 orders** from testable (`(E/E_P)⁴ = 4.5×10⁻⁵⁷` at 100 TeV vs `~10⁻¹⁸`
  sensitivity). PL-2/3/6 are vacuous.

The *species* this instantiates is a limit on what the framework can currently be **tested on**,
which is distinct from every other boundary on this map (all of which limit what it can
**derive**).

**(E) The evidential boundary (FTD-0791/0802, with FTD-0803 as the positive control)** — both of
the corpus's numerical-uniqueness supports failed base-rate controls. Neither is a claim about
discreteness; both are claims about what the corpus may *assert*. The registered lesson is
methodological and general: **a match inside a large declared family, with tolerances set by the
observed deviation, carries no evidential weight until the null is computed.** FTD-0803 supplies
the positive instance — the conditions under which such a scan *is* informative are computable,
and FTD-0321's registered domain met them.

### R.6 · Direction of travel

Clause 3 of the Number-One Goal asks that every priced line be driven to **retirement**, a
**theorem-grade no-go**, or a **sharper falsifier**. Over the last five weeks: **zero
retirements**; several no-gos (R.4); one sharper falsifier (the Maxwell trichotomy). The map is
converging — and it is worth stating plainly that *convergence of the boundary map is not the
same as progress on the content map*: the forced content did not grow, while the boundary set
did. That is the honest reading, and it is exactly what clause 2 asks for.

**One line moved in the wrong direction and must be recorded here.** FTD-0387's D7 — itself
`[SCOPE / METHODOLOGY — DRAFT awaiting owner ratification]`, which *"ratifies nothing, adopts
nothing"* and which no purchase may cite as settled until the owner rulings are booked — places
FC-W at the **marginal point `y = c = 1`** and takes it as the pricing floor. D6 admits equality
only when the single gap-class is **high-value**, and D7's registered high-value justification
was the framework's single scan-rigid identification (FTD-0319). FTD-0791 withdrew that. The
withdrawal **has** been transcribed (`adoption_pricing.json`, 2026-08-03: a WARNING field marking
the justification withdrawn, and the removal of "scan-rigid" from `calibration_role`); what has
**not** happened is the re-pricing — the yield figure is *"LEFT UNCHANGED pending an owner
decision"*, and whether the gap-class is still high-value without that backing is `[OPEN]` **and
gates the calibration**. So the floor that calibrates every future adoption now rests on an open
question. A decision document exists — `01_reference/DRAFT_FCW_REPRICING_D7_v1.md` (2026-08-05),
itself **unregistered** (*"LEDGER id: (none — assigned only if/when the owner books a ruling)"*)
— and re-pricing is registered as an owner act, not a research act. A boundary map that is the
qualitative face of the priced-import ledger cannot report convergence while omitting this.

### R.8 · Re-audit of the §2 stoppers against the *mature* framework — §2c first

*Prompted by the observation that the three §2 proofs are dated **2026-05-25 (FTD-0208)**,
**2026-05-29 (FTD-0225/0226)** and **2026-06-07 (FTD-0251/0253)** — ten weeks and ~575 ledger rows
before FTD-0803. A no-go binds only under its stated assumptions; nobody had checked whether those
assumptions still describe the framework. This subsection does that, one row at a time.*

**§2c — PREMISE VACATED (verified from source).** The v3 proof concludes *no Pythagorean budget*
from *no inner product*, and derives *no inner product* from the **ternary state variable** not
being a vector space (:48, :60). But the per-voxel state is larger than the ternary variable: it
carries `Vec3` flux, velocity and wave-velocity, `voxel.h` supplies `mag2()` and `dot()` on them,
and the **genesis threshold** — the ontology's most primitive event — is
`v.flux.mag2() > K_GENESIS * K_GENESIS`, an exact Euclidean ball test. So the §4-catalog claim of
*"zero … L²-norm structure"* does not survive contact with current source. **The row moves off
`PROVEN`; the conclusion is `[OPEN]`, not refuted** — an inner product is necessary but not
sufficient for `(dτ/dt)² + v² = 1`. **Two checks decide it, neither done:** is Euclidean `mag2`
forced or selected (if selected, FTD-0388 governs and the boundary reforms as a selection); and do
the L¹-partition and L^∞-neighborhood legs carry the conclusion without the vacated premise?

**§2a — FORMAL LEG STANDS; ITS STATED UNIFICATION WITH §2c IS SEVERED.** The load-bearing step is
vintage-independent and survives: beables are real-valued functionals on one configuration space
under the **pointwise product** (`PREREG_COMMUTATIVITY_INDEPENDENCE_v1.md:162–163`, :153–154), and
a pointwise product of real functions is commutative unconditionally. Note the same prereg's D1
already lists `J ∈ ℝ³` (:147–151), so §2a never depended on the substrate being scalar. **But its
unification claim is now broken:** `AUDIT_MODULAR_TIME_ALGEBRA_TYPE_CLOSED_NEGATIVE.md:27` grounds
itself in *"exactly the inner-product / L² structure FTD-0208 proved the discrete substrate lacks
(the substrate's `{−1,0,+1}^Λ` has no inner product)"* and :29 concludes *"FTD-0208 … and B1 …
are **one obstruction**."* That shared premise is the one §R.8 vacated. **§2a therefore stands on its
own legs and must no longer be presented as the same obstruction as §2c.** Its field inventory
(four fields) is also stale against a voxel now carrying ~23 members.

**§2b — DID NOT VACATE; three legs got HARDER.** A re-audit went looking for a 2c-style premise
failure and did not find one: FTD-0499 and FTD-0569 converted the aphorism *"finiteness opposes
reversibility"* into theorems (an exact deficit `(m−1)|H|`, *"independent of the numerical size of
`H`"*; *"no fixed finite local sidecar"*), and FTD-0395/0465/0466 add non-injectivity as a measured
`[THEOREM]` with two failed injective-variant attempts. **One defect found:** §0.5 grades §2b
**PROVEN** citing *"the π/G\* reversible/irreversible split is exact"*, but that split's canonical
source `FOUND_SPACETIME_FORCING_BOUNDARY.md:102` tags it `[SYNTHESIS] — not a theorem`. The grade
needs re-justification from a different leg, or demotion to the legs that are theorems.

**⚠ A cross-cutting instrument, not a boundary: THE SOURCE-DRIFT SWEEP.** Seven of twelve
independent assessments hit the *same* failure mode — a May/June enumeration of `voxel.h` that no
longer matches the file. §2c (`voxel.h:22/:26`, the origin), §2a's four-field D1 inventory,
FTD-0073's field list, the no-sound leg's *"no `u(x)`"* against `voxel.h:113 velocity` and
`:116 remainder`, the spin-2 field-content premise, thermodynamics'
`FOUND_LATTICE_PHYSICS_INTUITIONS.md:137` *"Lattice dynamics are reversible"* against FTD-0395, and
baryogenesis' positive claims predating FTD-0421. **Proposed deliverable:** for every registered
claim quoting a specific engine file, check the quotation against head. ~1 desk-week, mechanical.
**Its own falsifier:** sample 20 rows first — if drift is under ~10%, abandon the sweep, since the
7/12 hit rate is its entire justification.

⚠ **Note on provenance, recorded because it bears on how much weight the other two rows carry:**
the flux has been vector-valued since the Lagrangian was written (`ΔJ = c²∇²J − g_c∇s + g_c∇×(sv)`
is unintelligible otherwise), so this premise may have been **inaccurate when written in May 2026**
rather than made inaccurate by maturation — and it passed an independent adversarial review scored
9.8/10. Whichever it is, **§2a and §2b have not yet been re-audited** and no inference should be
drawn about them from this row.

### R.9 · Load-bearing structure that is neither forced nor priced

*Found 2026-08-06 by twelve independent assessments. `import_ledger.json` carries six `self_set`
entries (claimed **forced**) and sixteen `imports` (claimed **priced**). The following are used by
registered physics and appear in **neither** column — which is a direct gap against Number-One-Goal
clause 1, "every physical structure is either forced content or a marked-and-priced import."*

| Unpriced structure | Where it is load-bearing | Proposed row |
|---|---|---|
| **The Euclidean norm on the flux 3-vector** | the manifestation threshold itself, `phase_write.cpp:329`: `v.flux.mag2() > K_GENESIS*K_GENESIS`; also `voxel.h:22/:26` | `IMP-S5 [SELECTION]` — falsifier: a derivation of the norm from P1–P5, **or** a demonstration that the registered physics is invariant under replacing it with L¹/L^∞ |
| **GNC** | the Gauss/normalization chain | to be specified |
| **The clock hypothesis** | `SPEC_FTD_LAGRANGIAN.md` §4.3 + §8 L-1, both `[THEOREM conditional on clock-hypothesis AXIOM]` | already `[AXIOM]` in prose; **not** a priced-ledger row |
| **The whole cosmology sector** | ΛCDM apparatus, Friedmann, inflation, BAO | zero rows; currently absorbed inside IMP-E3's "~50+" *category count* |
| **Confinement** | §5 / §11-confine | likewise unpriced |

⚠ **Not minted here.** Minting these is an owner act and is blocked behind the D5/D6/D7 sitting
(`SPEC_ADOPTION_PRICING_RULES.md:85` — the rules "take effect only when the owner books" them) and
behind FTD-0388's own registered registry debt. This table records the gap; it does not price it.

**Un-graphed external dependencies.** The framework leans on at least three *open* external
results with no dependency map: **Waldschmidt Conjecture 5.23** (load-bearing for FTD-0314's C2 leg
— discovered by FTD-0785 after months unnoticed), **Rohrlich–Lang/GPC**, and the **Fresán–Jossen
exponential period conjecture**. A one-page "what dies if each falls" graph is cheap and missing.

### R.7 · Propagation debt this refresh does **not** discharge

This map is now current; **the corpus is not.** Registered contradictions still live elsewhere,
listed so they are not lost (precedent: FTD-0791's own record enumerated eight flagged files):

| Item | Current per LEDGER / this map | Still says otherwise |
|---|---|---|
| PL-1 | `[CONDITIONAL CLOSED NEGATIVE]`, falsified at 146σ (FTD-0795) | `SPEC_FTD_COMPLETE_FRAMEWORK.md` — "`[NUMERICAL FACT]`, pre-reg confirmed"; "run — Rice confirmed" |
| OT-3.3 / FTD-0802 | retagged `[SELECTION]`, withdrawn as OT-5.1 support | `SPEC_FTD_COMPLETE_FRAMEWORK.md` §§4/6/8.4 updated for FTD-0791 **only**; FTD-0802 absent |
| OT-1.4 | demoted out of Tier 1 (FTD-0785); spine count 7+2 → 6+3 | `SPEC_FTD_COMPLETE_FRAMEWORK.md` still `[THEOREM]` and "Seven theorem-grade" |
| FTD-0319 | scan-rigid count **zero** | `import_ledger.json` IMP-E1 provenance still cites "structural-uniqueness scan FTD-0319"; SS-6 still "7 of 9 numbered results" |
| **⚠ `SPEC_FTD_COMPLETE_FRAMEWORK.md` contradicts *itself*** | `:92–96` and `:121` carry FTD-0791: the scan-rigid count is **ZERO** | `:99` of the **same file** still reads *"exactly **one** numerical identification … has survived an adversarial uniqueness scan"* — an unregistered intra-file contradiction, added to this table 2026-08-06 |
| FTD-0796 | Bell bound is FC-1 completeness | `AUDIT_BELL_ANALYSIS.md`, `DERIV_OBSERVER_BELL_MECHANISM.md`, `SPEC_FTD_FRAMEWORK_V1.md` §2.3, PL-2 |

---

## 0 · The organizing thesis — three closure mechanisms + one engine category

Almost every boundary below is an instance of one of three recurring mechanisms:

1. **The "6th-postulate" family.** The substrate is **commutative**, **finite**, and (in its
   genuinely finite sector) **irreversible**. Three different physics targets each need a
   structure *provably absent* from postulates P1–P5, which would have to be injected as an
   independent axiom:
   - **non-commutativity** → quantum core (QM),
   - **reversibility** → the Lorentzian metric,
   - **the Pythagorean L² budget** → the clock hypothesis / γ.

   These are the same shape of result three times over.

2. **The assembly-vs-ingredients gap** (the central α boundary). The lattice forces every
   *scalar ingredient* of α's defining quadratic but **not their assembly into one operator**.
   FTD-0244 closes K-BIND theorem-negative for the current substrate-native operator
   calculus: α is therefore **dynamical, not structural**.

3. **Parametric / numerical disproof.** A specific framework-integer reading or candidate
   identity is computed and falsified (gravity's "1/100"; x₋ ↔ SM; the η-quotient routes; …).

A fourth, softer category is the **engine-blocked boundary**: a result true in principle but
not yet expressible in the current simulator without breaking the bit-exact golden gate
(cluster rigid-translation).

---

## 0.5 · Reading key — two kinds of claim, classified per section

Items in §1–§5 are **PROVEN** or **ATTEMPTED**; §6 items are **OPEN**. §R.5 adds two further
species that classify results about *evidence* and *testability* rather than about
*derivability*, and which by construction do not appear in §1–§5:

- **PROVEN** — a structural no-go theorem, valid within stated (and stated-explicitly)
  assumptions. Overturning it requires either disproving one of those assumptions or
  exhibiting the construction the theorem says cannot exist.
- **ATTEMPTED** — an empirical or computational trial that returned negative. This raises
  the prior that the *next* attempt along the same route also fails; it proves nothing about
  routes not yet tried, and it does not "strengthen" anything the way a proof would.
- **OPEN** (§6 only) — neither of the above; still live.
- **EMPIRICAL** (added 2026-08-06, §R.5) — a limit on what the framework can currently be
  *tested on*, not on what it can derive. Overturned by exhibiting a registered deviation that
  is simultaneously deviant, live, and testable.
- **EVIDENTIAL** (added 2026-08-06, §R.5) — a limit on what the corpus may *assert*, established
  by a base-rate control against a declared null. Overturned by a support that passes such a
  control.
- **EXTERNALLY BLOCKED** (added 2026-08-06) — a limit that is not FTD's at all: the row's closure
  waits on **open mathematics outside the framework**. Zero rows carry this as a *primary* class,
  which is itself the finding: it appears only as unregistered sub-legs — FTD-0314's C2 leg on
  **Waldschmidt Conjecture 5.23** (FTD-0785, registered "left OPEN, not remediated"), `δ ∉ N` on
  **Rohrlich–Lang/GPC** and the **Fresán–Jossen exponential period conjecture**
  (`ANALYSIS_E1_E2_TRANSCENDENCE_SOTA.md:13, :72`), and `§11-confine` at RP–FO. **The framework has
  open-mathematics dependencies it has not graphed** — see §R.9.

A third, narrower flavor of ATTEMPTED is **engine-blocked**: a result plausible in principle
but not yet measurable without breaking the current simulator's golden-tick gate — an
implementation gap, not a claim about the ontology at all.

| Section | Theme | Classification | Concrete falsifier / what would overturn it |
|---|---|---|---|
| §1 rows 2, 3, 6 (route-invariance, K-BIND, Tr/Det independence) | assembly of α's readout operator | **PROVEN** (conditional on the stated axiomatized operator calculus) | An FC-W-class axiom forcing the assembly, or a flaw in the trace/determinant independence argument itself |
| §1 row 1 ("0/4 routes force assembly") | the route search feeding the above | **ATTEMPTED** (4 native + 1 red-team route, all failed) | A 6th route, not yet tried, that succeeds |
| §1 row 4 (x₋ has no SM correspondent) | x₋'s physical status | **ATTEMPTED** (25 pre-specified observables tested, all falsified) | A 26th observable not yet tested that matches |
| §1 FC-W carrier-narrowing (FTD-0314/0326/0327) | transcendence of √(G\*(4G\*−1)) | ⚠ **REVISED 2026-08-06:** PROVEN for the C1/C3 legs **conditional on Chudnovsky (1976)**; the **C2 cross-CM-field leg is conditional on an OPEN conjecture** (Waldschmidt 5.23), per FTD-0785 — the headline `[THEOREM]` over-consolidates | Disproof of Chudnovsky; **or a resolution of Waldschmidt 5.23 against C2** (the cheaper route); or an un-exhibited forward-derived weight-mixing period surviving the FTD-0314 pressure |
| §2a (QM non-commutativity, 4 angles) | commutative substrate ⇒ no quantum core | **PROVEN** (structural; each angle a distinct argument from the substrate's stated commutativity) | A demonstration that the substrate's flux/state algebra is non-commutative under a reading not yet considered |
| §2b (Lorentzian metric / reversibility) | P5 = determinism, not reversibility | **PROVEN** (structural; the π/G\* reversible/irreversible split is exact) | A forcing argument deriving reversibility from P1–P5 as stated |
| §2c (clock hypothesis / L²-not-L¹ budget) | Pythagorean norm absent from Scale-0 primitives | ⚠ **PREMISE VACATED 2026-08-06 (§R.8)** — was PROVEN (v3); its load-bearing "no inner product at the single-voxel level" step is contradicted by `voxel.h:22/:26` and the `mag2()`-threshold genesis rule. Conclusion now `[OPEN]` pending two checks | The falsifier *"a native Scale-0 construction carrying an L²-norm without importing it"* now turns on whether Euclidean `mag2` is **forced or selected** |
| §2d (dispersion ceiling, FTD-0270) | cavity- not Schrödinger-dispersion | **ATTEMPTED** (`[MEASURED]` — an empirical finding, not a proof that quadratic dispersion is impossible in this substrate class) | A measurement at a different scale/coupling/regime showing ω∝k² natively |
| §2d (P2 mass-gap swing, FTD-0333) | native rest-mass gap | **ATTEMPTED**, and the attempt itself is `[INVALID per pre-registration]` — a gate failure, not a clean negative | A stable-integrator rerun that passes the pre-registered gate |
| §3 (gravity: G_N=1/100, Yilmaz, scalar-vector, a_phys-γ) | every gravity route tried | **ATTEMPTED** (parametric/numerical falsification in each case) | A recalibration or candidate route not yet tried |
| §3 (emergent spin-2) | graviton provenance | **ATTEMPTED** (measured null, L∈{32,64}, 11/12 k-points identical to spin-1 control) | A measurement at larger L showing a genuine spin-2 signature |
| §3.5 (cosmology sector) | Λ, Ω_Λ, DM halo, the rest | **mixed** — Λ=0 is a `[DERIVED]` positive result, not a no-go; the DM halo exponent is an **ATTEMPTED** falsified measurement; "the sector is imported" is a forcing-audit fact, not an attempt | See individual FTD-0331/0332/0334/0300 entries |
| §4 (FTD-0110 local A1g purity; FTD-0277 v1 counting route) | mass-bridge reduction | **ATTEMPTED** (empirical pipeline measurement; static-gating route fails on shape, magnitude, and geometry) | A successful collective-coordinate proof |
| §4 (FTD-0250 rigid-cluster translation) | transport-inertia visual | **engine-blocked** (implementation gap, not a claim about the ontology) | A cluster-aware translation phase that preserves the golden gate |
| §5 catalog (~52 routes) | closed-negative catalog | **ATTEMPTED, overwhelmingly** (numerical/parametric disproof or null measurement); a handful cross-reference the §1/§2 structural proofs above | See each row's "reason for closure" |

---

## 1 · The central boundary — α is dynamical, not structural

> **One-liner (FTD-0242 §5):** *"The substrate forces every ingredient of α's defining
> quadratic — the even Watson trace and a clean **conditional** chiral G\* determinant
> representation — but it forces neither that spectral realization nor their assembly into one readout
> operator, so α's value is selected by a logically independent convention, not derived; the
> discrete ontology determines the menu, not the dish."*

**What is established mathematically** (with domain conditions explicit):
- the **even trace** `16G*² = 16·|μ₄|²·G_BCC(0)` — 16 from ℤ[i] automorphisms; G*² from the
  Watson BCC self-energy;
- the **existence of a clean conditional odd source**: on the registered FQCR spectral domain, the J-twisted ζ-regularized determinant ratio
  `det_ζ(D_{3/4}) / det_ζ(D_{1/4}) = Γ(¼)/Γ(¾) = G*` — degree 1, with √(2π) cancelling so there
  is **no √π prefactor**. FTD-0839 proves this source is not forced by `J²=-I`; the full-line control gives ratio `1`.

**What is NOT forced:**
- That these two assemble into **one** readout operator with `(Tr, Det) = (16G*², 16G*³)`. For a
  2×2 operator, trace and determinant are **independent invariants** — fixing one leaves the
  other free. The factorization `Det = Tr·G*` is a *role assignment*, the imposed
  master-quadratic Vieta target (**W-CRIT-2**), `[UNDERDETERMINED]` (FTD-0235).
- Attacked from **four FTD-native routes** (jtwist, bcc, cm, novel): **0/4 force the assembly**
  (FTD-0242, `[STRONGLY MOTIVATED CONJECTURE no-go]`). A sympathetic red-team's attempt at a
  *5th* route also failed (0/5) — this is **ATTEMPTED** (§0.5), not a proof: it raises the
  prior that a 6th route also fails, and nothing more. It does not "strengthen" the boundary
  the way FTD-0242/0243/0244's actual proofs do.

**The route-invariance theorem (FTD-0243, RSI Leg 3):**
- **Flip ruled out** `[THEOREM]` — the only geometric alternative (the D6 ⟨111⟩ three-plane
  det_ζ product) is excluded because a definite complex structure needs `mult_O(E)=0`, which
  breaks O_h to a single C₄ axis, while D6 needs C₃∈Stab; `⟨C₄,C₃⟩=O` → no readout.
- **Leg 3b closes its own scope** `[THEOREM]`.
- **Reduction is route-invariant** `[THEOREM]` — **Q(G\*) is the Galois-fixed field** of the
  master quadratic's ℤ/2 (disc = 64G*³(4G*−1)). *Every* forward-forced symmetric FTD datum lives
  in Q(G*) and is therefore **provably blind to which root is 1/α**. Witness: the family
  det = 16G*²·G*^k for k = 0,1,2,3 gives dominant roots 139.05 / **137.04** / 130.68 / 105.76 —
  all F-consistent; *nothing in F selects k = 1*.

**K-BIND closure (FTD-0244)** `[CLOSED THEOREM-NEGATIVE]`:
The one thing that would fix α is for the substrate to **natively realize √(G\*(4G\*−1))** — the
squarefree generator of the unique quadratic extension Q(G\*)(√disc)/Q(G\*) in which x₊ and x₋
first become distinguishable. FTD-0244 axiomatizes a substrate-native operator calculus
`𝔠` and proves that trace/determinant data of every operator in `𝔠` remain in Q(G\*). Since the
master quadratic's root-splitting field has degree 2 over Q(G\*), no operator in this calculus
can force the assembly `(Tr,Det)=(16G*²,16G*³)` or distinguish the physical root without an
external selection `W`.

**FC-W carrier-narrowing — K-BIND extended from operators to the whole finite-symmetry class
(FTD-0314/0326/0327)** `[THEOREM, conditional on Chudnovsky]` ⚠ *(see §R.1: FTD-0785 found the
C2 leg additionally conditional on Waldschmidt Conjecture 5.23, an open problem — this headline
tag over-consolidates)* + `[SYNTHESIS]`: the surd
`√(G*(4G*−1))` is **transcendental over ℚ** (`Q(G*)∩Q^ab=Q`), so *no* finite-symmetry carrier
(chirality, ±1 sign, the binary-octahedral 2O cover, permutation parity) and *no* native operator
realizes it — extending K-BIND from operators to the **entire finite-symmetry class**. FTD-0326:
all 5 native ℤ/2's are Galois-blind to δ. FTD-0327: the AGM (the substrate's *entire* √-machine)
is δ-blind by construction — its forced-magnitude geometric means land on G\*, never δ. The
structural root: `√(4G*−1)`, the second independent factor of δ, has **no graded period home**
(`4G*−1` is motivically weight-inhomogeneous; the square root of a weight-inhomogeneous element is
the period of no pure graded motive), and its natural Eisenstein/prime-3 home is unavailable
(Theorem GE-1, `3⁶≠6²`). **Verdict: W cannot be earned natively (~90% CLOSED)** — only an
un-exhibited forward-derived weight-mixing period (a `det_ζ` of a new operator) survives `[OPEN]`,
pressured toward closed. The framework therefore **adopts** W as **FC-W (FTD-0315)** — its *first
adopted import* — under which `x₊ = 1/α` is a `[CONDITIONAL THEOREM given W]`, explicitly **not**
`[DERIVED]`.

**Sub-boundary — x₋ has no physical correspondent** (FTD-0210, `[CLOSED NEGATIVE]`): the smaller
root x₋ ≈ 3.024 was tested against 25 pre-specified SM observables — all falsified (closest
39.7% off). x₋ is a pure chirality/coordinate artifact of the quadratic; the old `x₋ ↔ N_c`
identification is **retired** (FTD-0014 removed, commit `ca7eb61`). N_c = 3 is independently
sourced (Moore Layer Theorem + four topological routes, `DERIV_NC_FROM_TOPOLOGY.md`).

**Exits (shared by the whole cluster):**
1. a binding law **W** logically independent of P1–P5 and not generated by the current
   substrate-native operator calculus (both `𝔉∪{W}` and `𝔉∪{¬W}` have explicit consistent
   models — W is genuinely a 6th-postulate-class input); or
2. a fresh engine-native **ARC-D** measurement — but **ARC-D1 already returned `[CLOSED NEGATIVE]`**
   (0 macroscopic cluster fissions across 2000 seeds; the lattice is topologically rigid).

| # | What is bounded | Mechanism | Tag | Ledger |
|---|---|---|---|---|
| 1 | Operator assembly `(Tr,Det)=(16G*²,16G*³)` not forced by any of 4 routes | trace & det independent invariants | `[SMC no-go]` | **FTD-0242** |
| 2 | Conditional independence theorem (RSI Leg 3) | Q(G*) Galois-fixed → blind to which root is 1/α | `[THEOREM]` (conditional) | **FTD-0243** |
| 3 | **K-BIND** — native √(G\*(4G\*−1)) | substrate-native operator calculus stays in Q(G*) and cannot force the quadratic extension | `[CLOSED THEOREM-NEGATIVE]` | **FTD-0244** |
| 4 | x₋ ≈ 3.024 has no SM correspondent | all 25 observables falsified; chirality artifact | `[CLOSED NEGATIVE]` | **FTD-0210** |
| 5 | Clean odd source exists on the selected chiral spectral domain | J-twisted det_ζ = G*, while full-line control = 1 | `[THEOREM — CONDITIONAL DOMAIN]` | FTD-0234/0839 |
| 6 | `Det = Tr·G*` factorization unforced | assembly-level independence (W-CRIT-2) | `[UNDERDETERMINED]` | FTD-0235 |

**Central obstruction:** all of the above sit under **MC-T4.3** — the operational α-readout
mechanism — a `[FOUNDATIONAL OBSTRUCTION]` open 3+ months. `x₊ = 1/α` (FTD-0013) stays
`[STRONGLY MOTIVATED CONJECTURE]`.

> ⚠ **REFRESHED 2026-08-06 (§R.1). One structural result in this section did change:**
> FTD-0314's headline `[THEOREM]` **over-consolidates** — its C2 leg is conditional on Waldschmidt
> Conjecture 5.23, an *open problem* (FTD-0785), not on Chudnovsky alone; and FTD-0784 registered
> that C3's period field `F` is inconsistently defined (the conclusion survives, the definition
> needs repair). FTD-0242 / FTD-0243 / FTD-0244 are unchanged. Separately, every
> *numerical-uniqueness* support for the identification is withdrawn (FTD-0791, FTD-0802,
> FTD-0321/0803), and FTD-0792 records that the engine cannot evidence the master quadratic by
> construction. Any text describing the α-root as "scan-rigid" or as "the framework's single
> scan-rigid identification" is **withdrawn** — the scan-rigid count is zero.

---

## 2 · The 6th-postulate family — three structures the lattice lacks

Structurally the *same boundary* in three sectors: a target physics needs a structure the
commutative / finite / irreversible substrate provably does not contain.

### 2a · Quantum non-commutativity — re-derived from four angles (all `[CLOSED NEGATIVE]`)
The substrate's observable algebra is **commutative** (classical fields s, J, v):
- **Modular time** (FTD-0225): commutative → abelian → type-I → trivial modular flow → no
  canonical objective time.
- **Complementarity** (FTD-0226): the manifestation map is a deterministic function of
  *commuting* flux → distributive/Boolean → a joint distribution exists → classical
  coarse-graining, not quantum complementarity. Structurally *explains* the 6-neighbour Born-rule
  closed-negatives (FTD-0197/0199/0200).
- **N_c=3 → ℤ/3 budget symmetry** (FTD-0228): flagged **apophenia** — `{J_x,J_y,J_z}` commute
  (co-measurable), but ℤ/3 would permute three *complementary* bases; same count (3), wrong *kind*.
- **The native angle** (FTD-0251): the substrate's *only* native dynamical angle is the symplectic
  **quadrature clock** `arg(q+ip)`, winding at ω(k) (`{q,p}≠0`, real physics) but strictly
  **commutative** (`[q,p]=0`). It carries **zero G\* content**, so it closes the
  "symplectic-phase → readout-rotation" route *before it is walked* — disjoint from the
  α-readout parameter set.

**Verdict:** quantum-core non-commutativity is an injected 6th-postulate-class input M.

### 2b · The Lorentzian metric & reversibility (FTD-0253, `[SYNTHESIS]+[BOUNDARY]`)
- **The causal cone is FORCED** `[THEOREM]` — c = 1/√3 from P4 (locality, ≤1 voxel/tick).
- **The Lorentzian metric is POSITED** `[AXIOM]` — it rides on the *second-order, reversible*
  Born-Infeld action `(Δ_t J)²`. **P5 is determinism, not reversibility** (deterministic
  diffusion is metric-free, γ-free). Reversibility is the missing forcing principle, a
  6th-postulate-class input *the same shape as QM's non-commutativity*.
- **Structural signature:** FTD's own **π/G\* split is the reversible/irreversible split** — the
  Euler reflection *product* branch `Γ(z)Γ(1−z)→π` (reversible, spacetime metric) vs the *ratio*
  branch `Γ(z)/Γ(1−z)=G*` (irreversible, arrow of time). **Finiteness opposes reversibility**:
  FTD's genuinely finite sector (the ternary state field) is irreversible.
- **Engine demo** (`test_spacetime_forcing_demo`, 9/9): same lattice → causal cone bit-identical
  (both fronts 7.211 @ t=8), but the metric (clock / reversibility / ballistic ruler r_rms∝t ×4.68
  vs diffusive ×2.46) appears **only in 2nd order**.

### 2c · The clock hypothesis / Pythagorean budget (FTD-0208, `[CLOSED NEGATIVE]`)
Three iterations (v1 UNDERDETERMINED → v2 INVALIDATED → v3 CLOSED-NEGATIVE, adversarial review
9.8/10): the quadratic budget `(dτ/dt)² + v² = 1` is **structurally incompatible with Scale-0
primitives** — the ternary state space has no native L²-norm; the natural Scale-0 addition law is
the **L¹ linear** ceiling `v + dτ/dt ≤ 1`. The Pythagorean/isotropic structure is
macroscopic-emergent (Scale 5+). So the clock hypothesis is an independent **`[AXIOM]`** at the
coordinate level; `SPEC_FTD_LAGRANGIAN.md` §4.3 is `[THEOREM conditional on clock-hypothesis AXIOM]`.

> ⚠ **PREMISE VACATED — RE-AUDIT 2026-08-06 (§R.8). This row no longer reads PROVEN as written.**
> The v3 proof's load-bearing step is scoped to the **ternary state variable**, and the substrate's
> per-voxel state is larger than that variable. Verbatim from
> `03_derivations/archive/AUDIT_CLOCK_HYPOTHESIS_v3_CLOSED_NEGATIVE.md`: *"There is no vector space
> structure over ℝ or ℂ at the single-voxel level"* (:48); *"Orthogonality requires an inner
> product. Since the ternary state space S is not a vector space, no inner product exists"* (:60);
> *"The §4 catalog contains **zero** quadratic, Pythagorean, continuous rotational, or L²-norm
> Hilbert space structure"* (:41). **Current source contradicts this at the single-voxel level:**
> `engine/include/ftd/voxel.h:22` defines `mag2() = x*x + y*y + z*z` and `:26` defines `dot()` —
> an inner product, per voxel — and `engine/src/render_bridge_phases/phase_write.cpp:329` makes the
> **manifestation threshold itself** an exact L² ball test: `v.flux.mag2() > K_GENESIS * K_GENESIS`.
> The most primitive event in the ontology — flux becoming matter — is a Euclidean-norm threshold
> on a `Vec3`. **What this does and does not do:** it vacates the stated premise, so the row is no
> longer `PROVEN` as written; it does **not** derive the clock hypothesis, since a native inner
> product is necessary for `(dτ/dt)² + v² = 1` but not sufficient. **Two checks decide the row and
> neither is done:** (1) is the Euclidean `mag2` **forced or selected**? If selected, FTD-0388's
> guard applies (*"an adoption is never a derivation"*) and the boundary reforms as a selection
> rather than an impossibility. (2) Do the remaining legs — the L¹ partition as the natural
> addition law, the L^∞ 26-Moore causal neighborhood — carry the conclusion **without** the
> non-vector premise? Until both are answered the conclusion is `[OPEN]`, not refuted.

**Related positive-but-scoped result — dynamical time dilation (FTD-0252):** a wave-clock built
from the *massless* lattice (never reading the circular `voxel.tau`). v1 → **OTHER** (`√(1−v²)` is
an *identity of the construction*, not a contest). v2 → IR-scoped `[MEASURED]`: on the ⟨100⟩ axis
the departure from γ vanishes as **`R ∝ L⁻²` (∝k²)** — γ emerges in the IR — but
ultra-relativistic diagonals don't converge at L≤193 `[OPEN]`. FTD-0208 *clarified, not refuted*
(different observable).

### 2d · No native quantum dynamics — the dispersion ceiling (FTD-0270, FTD-0333)
A fourth instance of the same family: the target — atomic quantum dynamics, the Schrödinger sector —
needs a structure P1–P5 lack.
- **The dispersion ceiling (FTD-0270)** `[MEASURED — BOUNDARY]`: the substrate quantizes (discrete
  Dirichlet-box modes) but with the **wrong dispersion** — `ω ∝ |k|` (s≈0.944, cavity/EM-like, the
  2nd-order-in-time `√(−M)` readout), **not** the Schrödinger quadratic `ω ∝ k²` (s≈2) the hydrogen
  Rydberg `1/n²` needs. *"The √ is the entire story."* There is **no native ℏ, no native Pauli**.
  The quadratic dispersion is *latent* in the spatial operator but needs a rest-mass clock
  `ω₀ ∝ M_REST` to factor against — and **native flux is massless** (the clock winds at ω(k), zero at
  k=0). So atomic spectra (FTD-0278 hydrogen, FTD-0279 helium) are confirmed only
  `[DERIVED-GIVEN-IMPOSED]` (imposed clock + scalar coupling); de Broglie guidance is `[ABSENT]`
  (FTD-0271, no ∇S→force term). FTD never derives atomic quantum dynamics from the discrete ontology
  unconditionally.
- **The P2 mass-gap swing (FTD-0333, pre-registered `preregister-qdyn-mass-gap-v1`)**
  `[MEASURED — INVALID per pre-reg]`: the full-closure attempt — does the *nonlinear* genesis↔Gauss
  back-reaction loop generate a **native** rest-mass gap `ω₀>0` (clock OFF) that the *linear* analysis
  cannot see? — ran the hash-locked sweep and came back **INVALID**: the instability gate failed on all
  10 runs (partly the FTD-0308 leapfrog drift, partly *physical* genesis energy-injection; L=32 floods).
  The strong, uniform **no-gap hint** (native ω₀ pinned at the FFT floor — manifestation *freezes* the
  flux, the opposite of a gap) is recorded but **not banked** (un-claimable from gate-failing runs).
  FTD-0270 unchanged; a clean v2 needs a stable integrator + a `k=0`-isolating control + stable-island
  amplitudes.

---

## 3 · Gravity

- **Framework-integer `G_N = 1/(b₃+N_c)² = 1/100` is FALSIFIED** (FTD-0131, `[CLOSED NEGATIVE]`):
  off by ~10²⁰–10⁴³ under any natural calibration; "the 1/100 coincidence has no substrate
  justification." The native substrate derivation gives instead the **gravitational fine-structure
  ratio** `α_G(e,e) = (m_e/m_P)² ≈ 1.745×10⁻⁴⁵` (0.38% match), `[DERIVED]` modulo the
  clock-hypothesis axiom and inheriting FTD-0015's `[STRONGLY MOTIVATED CONJECTURE]` floor. The
  Schwarzschild form is recoverable; the remaining open piece narrowed to **one** interpretive
  step (the clock hypothesis).
- **Scalar / exponential-metric routes closed:** the Yilmaz exponential metric (FTD-0184) is
  horizon-free and diverges from GR at O(v³); a native scalar-vector gravity model (FTD-0213) is
  excluded by the **Hulse-Taylor binary pulsar at 152.88σ** (33% error in dP/dt). The
  effective-metric Deser-bootstrap scaffold is retained.
- **`a_phys` from gravity** (Mechanism γ, FTD-0035) closed-negative → `a_phys ≡ ℓ_P` adopted as
  calibration.
- **Emergent spin-2 / graviton provenance** (Frontier 4): the massless spin-2 field h_μν is
  *posited, not derived* (graviton-provenance audit; Conjecture 10.1, Gap 10.1); the empirical
  substrate floor at L∈{32,64} shows 11/12 k-points TT-correlator identical to the spin-1 control;
  the spin-2 boundary theorem (free-theory) is pre-registered. Whether the substrate carries an
  emergent spin-2 mode is `[OPEN]`. *(Ledger-ID note: this cluster is entangled with the
  FTD-0189/0193 collision — see `AUDIT_LEDGER_ID_COLLISIONS.md`.)*

---

## 3.5 · Cosmology — Λ predicts zero; the sector is imported (FTD-0331/0332/0334)

The whole cosmology sector is a boundary: **FTD's most-imported, least-substrate-derived sector** —
"ΛCDM apparatus + FTD numerology," never derived (`SPEC_COSMOLOGY_FRAMEWORK_BOUNDARY.md`).

- **Λ: FTD predicts zero (FTD-0331).** The `M_Planck⁴` catastrophe is built from quantum zero-point
  `½ℏω`; FC-1 declines ℏ, so the classical empty void `(J=0,s=0)` is identically zero-energy ⇒
  **FTD predicts Λ = 0** `[DERIVED]` (the *old* catastrophe **dissolved-by-construction**, not solved).
  For any *nonzero* Λ: FC-3 forces the **form** (a scale-ratio, `[DERIVED-from-FC-3]`); the holographic
  CKN bound fixes the **ceiling** `Λ ≲ (ℓ_P/L_H)²` `[SELECTION]` — **a ceiling, not a source**
  (saturating it reintroduces the declined zero-point). The **source is `[OPEN]`** (no native source:
  zero-point forbidden by FC-1; the condensate leaks `~L⁻⁵`, FTD-0273; matter-injection has no
  L-dependence). The **value is a `[BOUNDARY]`** (needs `L_H/ℓ_P`, which FTD-0059 proves is not native).
  The equation of state **w is `[OPEN]`** — the legacy "w=−1 exactly" is *retired*; a measured `w≠−1`
  does **not** falsify FTD.
- **Ω_Λ: no derived value.** Three uncoordinated numbers near 0.685 — `0.683` (α¹⁶, `[PARAMETRIC]`),
  `2/3` (`[CONJECTURE]`), `17/27` (the Moore dark-**state** count, `[THEOREM]`-as-count-only); the
  proximity is coincidental.
- **Dark matter (FTD-0300)** `INDETERMINATE` — the lossless self-field halo **box-fills the periodic
  lattice** (`r_eff≈L/2`, not localized); the `r⁻⁰·⁶⁹` halo exponent is **FALSIFIED** (→ −1.25 at
  L≥128); SPARC not founded. The 17/27 *count* is structural; the *dynamics* are `[OPEN]`/mapped-negative.
- **The rest of the sector** — inflation (`[SELECTION]`/ad hoc), the EW transition (`[CONJECTURE]`),
  power spectrum/BAO (`[OPEN]`), the Friedmann/expansion history (adopted ΛCDM) — all imported, none
  substrate-derived. FTD-0332/0334 reconciled the theory corpus **and** the dissemination layer to this.
- **The one internally-decidable datum** (registered `[OPEN]`): a pre-registered L-scan asking whether
  the substrate vacuum-energy density obeys the area law `∝1/L²` (a native source) or leaks `~L⁻⁵`
  (no source) — prior-favoured: leak / negative.

---

## 4 · The engine↔algebra mass bridges — the dynamical twins

`FTD-0110` and `FTD-0250` are the **same open gap** at two levels: the unclosed
**collective-coordinate reduction**.

- **FTD-0110 (cluster size ↔ mass).** Linear-level `k = 1/4` is `[DERIVED]` from O_h A₁g
  multiplicity; Bridge-I global O_h-equivariance is `[DERIVED]`. But **local A₁g purity is
  empirically FALSIFIED under the full pipeline** — the *non-local Gauss projection* drives f_A1g
  from 1.0 → 0.15 by ~tick 100 (GPU exact-FFT = CPU SOR bit-exactly, so structural, not
  convergence). The v2 "Orbit-Equipartition + Timescale-Separation" rescue is `[DISPUTED]` (4
  defects incl. a load-bearing arithmetic error ∑(A²/4)=A²). Empirically `N(A)≈k·A²` holds to
  **~5%** with a logarithmic drift `k≈¼(1−0.030·ln(A/2))`. Net: linear level `[DERIVED]`;
  nonlinear / multi-scale / SM-mass identification `[STRONGLY MOTIVATED CONJECTURE]`.
  FTD-0261/0269 replace the historical stack reading with a
  broken-power N(A) law whose calibration is engine-emergent; FTD-0277 then closes the v1
  slosh-pass/static-gating collective-coordinate counting route negative (`N~A^2`, 20-40x high,
  wrong shell geometry). The broader reduction remains open.
- **FTD-0250 (transport inertia, the dynamical twin).** The engine now *honors* the action's EP
  `[THEOREM]` — a locked N-voxel cluster carries inertial mass N·M_REST and the **velocity-EP is
  DEMONSTRATED** (unequal-N clusters free-fall bit-identically while F∝N). But this ships
  **`[IMPOSED]`**: proving that rigid translation of a Gauss-dressed N-voxel cluster *costs*
  co-moving momentum N·M_REST·v (perturbation theory in the collective coordinate) is `[OPEN]`.
  The **rigid-lattice translation visual is `[BOUNDARY — blocked]`** — the current movement phase
  same-sign-bounces a packed cluster, has an x-major visitation race, and resolves GPU collisions
  via nondeterministic atomicCAS (breaking the bit-exact golden gate); a new cluster-aware
  translation phase is needed.

Closing the reduction upgrades **both** FTD-0110 and FTD-0250 to `[DERIVED]`/`[THEOREM]`.

---

## 5 · The closed-negative catalog (~52 sealed routes, by theme)

**Classification (§0.5): overwhelmingly ATTEMPTED-and-failed** — numerical/parametric disproof
or a null measurement, each raising the prior against the next attempt in that theme and
proving nothing beyond it. A handful of rows (the QM non-commutativity and route-invariance
entries) are cross-references to the **PROVEN** structural closures already tabulated in §1/§2,
not independent empirical trials.

Preserved for provenance — to prevent zombie re-emergence of exhausted routes.

> ⚠ **NOT YET EXTENDED (2026-08-06).** This catalog still ends at the 2026-07-01 revision. The
> window folded in §R closed roughly ten further routes — FTD-0781, 0783, 0786, 0787/0789,
> 0788/0790, 0793, 0794, 0797, 0798, 0800/0801 — plus **FTD-0795 / PL-1**, a *falsified registered
> prediction* (`[CONDITIONAL CLOSED NEGATIVE]`: the `α ≥ 1` bunching floor against a measured
> `α = 0.0188 ± 0.0067`, **146σ**), which belongs in a **Prediction spine** theme this table does
> not yet have — none of which appear below. Extending the table
> is outstanding maintenance; until then §R.4 is the only record of them here.

| Theme | IDs | One-line reason for closure |
|---|---|---|
| **α-derivation routes** | 0031 (g_c, all 3 BCC routes), 0050 (RG char-poly; stencil ⊥ BCC), 0073 (Clifford/Dirac mode-erasure), 0094 (L2 identity → parametric), 0097 (look-elsewhere), 0116 (Z-factor), 0164 (3 χ₋₄→P_G* routes), 0183 (N_base=4 not ℤ[i]), 0204/0205 (ARC-B1 categorical mismatch), 0212 (Beilinson PSLQ null) | structural orthogonality or high-precision numerical disproof |
| **Operator-forcing / quantization** | 0197/0199/0200 (Born in 6-nbr), 0210 (x₋↔SM), 0233 (parity in ℚ[G*²]), 0235 (Tr/Det independent), **0242/0243 (route-invariant), 0244 (K-BIND)** | trace/det independence + Galois-blindness + operator-calculus field closure → assembly unforced |
| **QM non-commutativity** | 0225 (modular flow), 0226 (distributivity), 0228 (ℤ/3 apophenia) | commutative substrate → no quantum core |
| **Gravity / relativity** | 0035 (a_phys γ), 0131 (1/100), 0184 (Yilmaz), 0208 (clock hyp.), 0213 (scalar-vector vs pulsar) | parametric falsification / Pythagorean incompatibility |
| **BCC / unification no-gos** | 0058 (two-U(1) Ward), 0079 ((SC+FCC)/2↔BCC exact identity) | finite-L stencil mismatch ~3% |
| **Strong / confinement** | 0025 (3 substrate routes) | confinement lives in `Z=∫dU e^{−S}`; deterministic substrate has no Phase-G analog — possibly outside current ontology |
| **Engine phenomenology** | 0060, 0061, 0062, 0063, 0071/0072/0074, 0135, 0277-v1 | reduce to tautology / null GPU measurement / blocked on MC-T4.3 / v1 counting route fails N(A) shape, magnitude, and geometry |
| **Mass / flavor** | 0219 (µ-unit loophole) | retracted for post-hoc fitting |
| **Quantum dynamics** | 0270 (wrong dispersion), 0333 (P2 mass-gap INVALID / no-gap hint) | substrate quantizes with s≈1 (cavity) not s≈2 (Schrödinger); no native ℏ/Pauli; the nonlinear loop *freezes* the flux rather than gapping it |
| **Cosmology / Λ** | 0131 (G_N=1/100), 0300 (DM halo INDETERMINATE), 0331 (Λ source/value), 0332/0334 (sector imported) | FTD predicts Λ=0; the holographic `~1/L²` is a *ceiling not a source*; the value needs L_H (FTD-0059); the sector is ΛCDM apparatus + numerology |
| **Wave sectors** | 0298/0299 (no sound) | the lattice **is** space ⇒ no acoustic Goldstone; FTD has light/radio (one flux-wave sector) but no sound |

**Root-cause grouping:** structural obstruction (0025, 0050, 0073, 0225, 0226, 0228, 0243) ·
numerical disproof (0116, 0164, 0197–0200, 0212) · route-invariant boundary (0242/0243, MC-T4.3) ·
epistemic/retraction (0219, 0208 v1–v2) · pre-registered adverse outcome (0197, 0199, 0200, 0204,
0205, 0210, 0212).

---

## 6 · Open boundaries still genuinely researchable

(Neither **PROVEN** nor **ATTEMPTED**-and-failed per §0.5 — these are the third, still-live
bucket.)

- **MC-T4.3** — central `[FOUNDATIONAL OBSTRUCTION]` (3+ months). Surviving exits: a 6th-postulate
  W that forces the operator assembly, or a fresh ARC-D engine measurement (ARC-A/B
  closed-negative; ARC-D1 closed-negative).
- **FTD-0110-NL / FTD-0250 reduction** — the collective-coordinate proof (cleanest remaining
  `[OPEN]→[THEOREM]` mass/gravity conversion). FTD-0277 v1 is closed negative; any successor
  route needs a fresh pre-registration and must address flux consumption, capture/dispersal, and
  transport-inertia accounting rather than reusing the failed static-gating count.
- **Post-K-BIND α-positive route** — a new W-like framework commitment or a fresh ARC-D
  measurement; no current substrate-native operator route remains open.
- **Spin-2 / emergent graviton** (Frontier 4) — whether the substrate carries an emergent spin-2
  mode `[OPEN]` (pre-reg `spin2-boundary-theorem-v1` landed).
- **Confinement** — flagged with a recognized structural obstruction (RP–FO effort).
- **FTD-0252 diagonals** — ultra-relativistic γ convergence needs L ≳ 257.
- Sector queue (`SPEC_OPEN_MATH_BY_SECTOR.md`): §8-running β-coefficients, §11-confine, §10 flavor
  depth matrices, W-CRIT-3 (Lorentz invariance), ~~W-CRIT-4 (Bell S>2)~~, the BH 11-item cluster.

### 6.1 · Refresh 2026-08-06 — what moved into, out of, and within this bucket

**Sharpened, not newly closed (correction to an earlier draft of this subsection):**
- **W-CRIT-4 (Bell S > 2)** was **already** `[CLOSED DECLINED]` under FC-1 before this window
  (`SPEC_OPEN_MATH_BY_SECTOR.md` §7; LEDGER **FTD-0023**), and §6's body line already carries the
  strikethrough. What **FTD-0796** adds is not the closure but its
  *mechanism and price*: FC-1's completeness clause alone forces CHSH ≤ 2 with no locality
  assumption, so **the declination cannot be revisited by adopting nonlocality** — doing so
  breaks FC-1 itself. What remains is a *priced* door, not an open one (below).

**Closed in this window, though never in this bucket:**
- The **de Rham common-cone** and **Kähler–Dirac carrier** routes (FTD-0797, FTD-0798), the
  latter foreclosing the entire "FTD needs a first-order sector for a Born measure" class.
- The **lattice-to-quartic bridge at link 4** (FTD-0794) — closed *against* the framework: the
  substrate's own `C₄` symmetry selects the harmonic law and excludes the quartic there.

**Still open, but re-bounded:**
- **MC-T4.3** — unchanged structurally, and now additionally bounded by **FTD-0784**: even a
  carrier passing Gates A+B+C delivers `G*` but *cannot* deliver
  the FC-W surd (registered scope: **Gates A+B+C**). The exit "a carrier that earns W" is
  therefore narrower than the §1 text suggests — a successful carrier would still leave W unpaid.
- **Post-K-BIND α-positive route** — same, with the evidential caveat of §R.1: no numerical
  support remains for the identification the route would vindicate.

**The one decidable open door (added 2026-08-06):**
- **C3 / the Maxwell trichotomy** (FTD-0789) — does any native configuration realize
  `n = 4`, i.e. **first-order flexible and second-order rigid**? Unlike everything else in this
  bucket this is *decidable* rather than open-ended: `null(H)` nonempty with the quartic form
  positive definite on it. One construction was withdrawn *by* the criterion (FTD-0787, by FTD-0789);
  FTD-0800 and FTD-0801, both `[CLOSED NEGATIVE — SCOPED]`, have failed *since* it was stated.
  Each was scoped to its executed set — the door is not proven shut, but the prior is now poor.
- ***Proposed, not registered:*** every C3 screen so far has sampled **constructed static
  configurations** (enumerated equilibria, a designed trimer, a periodic sheet). Marginal-stability
  physics elsewhere — jamming isostaticity, self-organized criticality — populates edges through
  **dynamically arrested** states instead. The FTD-0799 arrest configurations (which grew, then
  stopped dead — `dN/dt` exactly zero for the final 399–599 ticks in all four arms — by the
  registered genesis self-quenching mechanism) have never been screened against the trichotomy. This would be the first C3 target
  suggested by a *mechanism* rather than an enumeration; it requires a locked pre-registration
  before execution and predicts nothing. ⚠ **Two adverse facts must travel with this proposal:**
  FTD-0799's own record states flatly *"This result does not bear on the C3 carrier question,"*
  and its arms are profile-scoped — movement, curl coupling, evaporation and Gauss projection
  were all **OFF**, and arm E (`R0 = 20`) was **not run**. The proposal is therefore to screen an
  *existing artifact* under a new question, not to inherit any of FTD-0799's standing.

**Priced but unexplored (a distinct status — not open, not closed):**
- **Superdeterminism** — declared at FTD-0329 row 15 and priced exactly at `S ≤ min(2+3M, 4)`
  with Tsirelson reached at `M* = 0.27614237`. It is the *only* live door to Bell violation, and
  it carries a **registered live contradiction**: FTD-0329 adopts measurement dependence while
  constitution §2.3 and PL-2 assert statistical independence. Under ledger precedence FTD-0329
  governs, which makes PL-2 unfalsifiable contrary to its own text. Resolving that contradiction
  is an owner act, not a research act.

**Closed negative but mechanistically productive (worth recording as a distinct outcome):**
- **FTD-0799** — no derived body size (`N*` tracks the seed; arms arrest a factor 3.2 apart), but
  the *mechanism* is new and general: genesis deposits `−1` upstream and `+1` downstream, so every
  event **conserves charge while diluting net polarity**, and since the source driving `|J|` is net
  polarity, **growth erodes its own driver** (`dN/dt` exactly zero for the final **399–599** ticks
  in all four arms; **arm E, `R0 = 20`, was not run**). The quasi-static law is thereby a valid
  *nucleation* criterion and an invalid *growth* law.

---

## 7 · The unifying picture

FTD's boundaries reveal a **scale-stratified, structurally honest architecture**. The five
discrete postulates force the **causal skeleton** — the cone, c = 1/√3, the *menu* of α's
ingredients, the linear-level mass coefficient. But the three structures that turn that skeleton
into known physics — **non-commutativity** (QM), **reversibility** (the metric), and the **L²
budget** (γ) — are each provably absent from P1–P5 and must be injected as independent,
mutually-analogous 6th-postulate-class inputs. And the one quantity that would *derive* α,
**√(G\*(4G\*−1))** — "the discrete limit of a square root" — is not produced by the
axiomatized substrate-native operator calculus.

This pattern recurs everywhere the substrate is asked to reach beyond its skeleton. **Quantum
dynamics** needs a Schrödinger-quadratic dispersion the 2nd-order wave sector structurally lacks
(FTD-0270). **Cosmology** is wholesale-imported, and FTD's own classical vacuum *predicts Λ = 0*
(FTD-0331) — it *dissolves* the old cosmological-constant catastrophe but supplies no dark-energy
source. And the carrier-narrowing of the α boundary (FTD-0314/0326/0327) hardened so far that the
framework now **adopts** the binding law as **FC-W**, its first declared import — α is *selected*,
not earned. The discrete ontology reaches further into **number theory** than into **physics**.

That is the map drawn honestly in **both** directions, exactly as the Number-One Goal's second
clause demands: the spine *derives*, and these boundaries *mark precisely how far discreteness
reaches.*

> **Addendum, 2026-08-06.** The five weeks summarized in §R added a second axis to this picture,
> and it is now the map's most important distinction: **structure versus evidence.** The §1–§5
> no-go results on what the substrate cannot force survive **with three registered exceptions**,
> all from FTD-0785's no-promotion pass: FTD-0314's headline `[THEOREM]` over-consolidates (C2 is
> conditional on an open conjecture); FTD-0784's surd bound was **demoted** from
> `[CONDITIONAL THEOREM]` to `[TRANSPORT]`; and OT-1.4 was demoted out of Tier 1, moving the
> canonical spine count from 7+2 to **6+3** — plus one definitional defect registered separately
> by **FTD-0784** (C3's period field `F`), which the conclusion survives. With those stated, what
> was audited away is
> overwhelmingly the *evidence* for the identifications those structures bound, and the auditing
> was severe: both numerical-uniqueness scans at their nulls, the CM leg
> range-limited, the engine unable to evidence the master quadratic by construction, and the
> lattice's own symmetry selecting against the quartic clock. A framework can be structurally
> honest and evidentially thin at the same time, and FTD is now demonstrably both. The sentence
> "the discrete ontology reaches further into number theory than into physics" survives the
> refresh — but it should now be read with §R.5(E) attached: *reach* into number theory is not
> the same as *evidence* from it, and the corpus's own base-rate controls are what established
> the difference.

---

*This is a `[SYNTHESIS]` document. It restates canonical claims at their canonical tags and
introduces no new result. If any tag here disagrees with `core_ledgers/LEDGER.md`, the LEDGER is
authoritative.*
