# Audit — connected-block independent field modes

**Campaign:** FTD-0641  
**Verdict:** `[INDEPENDENT CLASSICAL TRANSVERSE FIELD SPECTRUM CONSTRUCTIVE;
COUPLED POLE OPEN]`

1. The phase formula is derived from the exact matched curl update and uses no
   trajectory fit beyond the preregistered recurrence estimator.
2. Every seed is an exact discrete curl. Gauss preservation is therefore a
   chain-complex identity, measured here to `3.432e-15`.
3. All 54 arms pass energy, inversion, frequency, recurrence, amplitude, sign,
   polarization, cubic, and monotonicity gates. The independent certificate
   reconstructs the dispersion from integer wavevectors.
4. Background subtraction is legitimate because this campaign evaluates the
   linear source-free field operator. It is not legitimate evidence for the
   interacting matter-field spectrum.
5. Matter is held fixed by scope. No recoil, scattering, emission, absorption,
   radiation reaction, or dressed pole is tested.
6. The result licenses `independent classical transverse matched-field modes`.
   It does not license `photon`, `quantum occupation`, `physical speed of
   light`, `common cone`, or `Lorentz recovery`.

Independent certificate:

- `scripts/proofs/proof_connected_block_independent_field_modes.py`.

The next live gate is the coupled tangent response with matter released.
