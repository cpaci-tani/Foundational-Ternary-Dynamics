# AUDIT — Native Motion / Reaction-Front Trichotomy

**FTD ID:** FTD-0585  
**Status:** `[THEOREM — CURRENT/SOURCE DISCRIMINATOR]` +
`[CLOSED NEGATIVE — RECIPROCAL REACTION-FREE NATIVE MOTION]` +
`[OPEN — DISSIPATIVE REACTION PATTERN]` +
`[DEFECT — STALE VOID KINEMATICS]`  
**Date:** 2026-07-26

## Result

Moving manifested support is not evidence of transported matter. The exact
finite-volume history equation admits two distinct decompositions with the
same endpoint snapshots:

\[
\text{transport: } I\ne0,\ S=0,
\qquad
\text{reaction front: } I=0,\ S\ne0.
\]

Both can conserve total signed polarity and shift the same first moment. Only
the first supplies a local conserved current and a candidate worldline.

In the reaction-free native field sector, twelve rest arms stayed exactly at
rest for 384 aggregate ticks while the fields evolved. Initialized ballistic
controls all hopped. The frozen field source remains one-way to matter unless
a selected force/projection is enabled.

## Newly exposed defect

Evaporation clears the ternary state and visible particle labels but preserves
`velocity` and `remainder` on the void voxel. Genesis and pair production do
not initialize either field. Twelve live evaporation/remanifestation arms
confirmed bit-exact reuse of those hidden kinematics for both polarities and
all six face directions.

Any future reaction-pattern campaign must therefore record and require zero
pre-event void kinematics. Otherwise apparent motion can be inherited memory,
not dynamically generated impulse.

## Ontological consequence

A flame-like or wake-like excitation remains a coherent research route. Its
identity is a maintained reaction pattern, not automatically a conserved
particle. It must expose its energy reservoir, local source, persistence rule,
and spectral pole before particle language is permitted.

The FTD-0478 face current survives as an exact map from a specified trajectory
to link current. It does not generate that trajectory. FTD-0481--0483 remain
closed behind the failed common-action gate.

## Reproducibility

- preregistration SHA-256:
  `972F221AAE2BA9CBE1C95C9E71CA9789D3082A1DD5695B56F6996ACD29ABFC1B`;
- native CTest: PASS;
- exact observer residuals: zero;
- independent source/algebra proof: 58/58 PASS;
- maximum evaporation waiting time: 16 ticks;
- production/default/scenario changes: none.

## Verdict

`TRANSPORT_REACTION_FRONT_AND_STALE_MEMORY_DISTINGUISHED_RECIPROCAL_NATIVE_PARTICLE_MOTION_STILL_CLOSED`
