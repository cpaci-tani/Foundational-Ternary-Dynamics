# Audit — FTD-0736--0738 precontact relational-formation sequence

**Status:** `[AUDIT — MIXED VERDICT, SEQUENCE CERTIFIED]`  
**Scope:** selected compact-pair common action; no production promotion

## Findings

1. **FTD-0736 validly fails its strict verdict gate.** Four of four unbound
   histories re-enter the derived graph before possible emitted-disturbance
   self-contact, but zero of four are negative from the exact re-entry sample
   onward. The verdict `PRECONTACT_REENTRY_WITHOUT_PERSISTENT_CORE` is the
   correct locked outcome.

2. **The causal-buffer subordinate result is positive.** Exact sparse-current
   support gives maximum source radius three and earliest possible periodic
   contact tick 123. The third transitions at ticks 63, 79, and 96 are local
   to the tested dynamics rather than consequences of return of the newly
   emitted disturbance.

3. **FTD-0737 reproduces the post-hoc pattern under a fresh lock.** All three
   rays begin continuous negative-energy tails exactly 15 ticks after graph
   re-entry, remain negative and graph-inside through tick 122, and transfer
   positive energy to the matched field. Its independent certificate passes
   `9271/9271` checks and verifies exact FTD-0736 prefix equality.

4. **FTD-0738 explains the ordering but not the measured delay.** For the
   selected potential, `U=U'=0` at the cutoff while moving kinetic energy is
   positive. Generic continuous graph entry must precede energetic binding.
   The value 15 remains measured and parameter-conditional.

## Epistemic correction

The derived interaction graph is not a matter predicate. It reports where the
selected interaction is active. A matter-like core requires the stronger
complete-state condition of graph connectivity, negative relational energy,
participating current/field exchange, and finite persistence. Calling the
third graph transition “formation” without the energetic qualifier is
overstated.

Likewise, the result is precontact rather than uncontained. Periodic return of
newly emitted disturbance is excluded through tick 122, but the tick-zero
static dress is still a global quotient solve, and persistence after causal
contact has not been tested.

## Audit controls

- FTD-0736: `7951/7951 PASS`, five histories, 1125 serialized rows;
- FTD-0737: `9271/9271 PASS`, three histories, 735 serialized rows;
- FTD-0738: `48/48 PASS`, exact polynomial and sign checks;
- all maximum action, energy, recoil, speed, support, and inverse residuals
  remain within their preregistered tolerances;
- production tick, defaults, scenarios, field equations, potential, and
  ontology are unchanged.

## Remaining live gate

The next matter-ontology discriminator must eliminate the quotient-defined
static preparation or place it behind an explicit domain-of-dependence
construction. A longer periodic replay by itself cannot establish an
uncontained localized object. The target is finite-support causal formation
with an outgoing field tail, followed by post-formation persistence before
any environmental return.
