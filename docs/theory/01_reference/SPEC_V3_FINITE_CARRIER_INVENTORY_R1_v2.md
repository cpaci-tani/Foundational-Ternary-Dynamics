# SPEC — V3 finite carrier inventory R1 v2

**Date:** 2026-08-24  
**Status:** **[SELECTION — EXACT FINITE CARRIER INVENTORY FOR V3]**
+ **[THEOREM — CONDITIONAL MINIMUM PAYLOAD/EXCLUSION COUNTS]**  
**Supersedes:**
[`SPEC_V3_FINITE_CARRIER_INVENTORY_R1_v1.md`](SPEC_V3_FINITE_CARRIER_INVENTORY_R1_v1.md)  
**Constitution:**
[`SPEC_FTD_FRAMEWORK_V3_STRICT_DISCRETE_COMMON_ACTION.md`](SPEC_FTD_FRAMEWORK_V3_STRICT_DISCRETE_COMMON_ACTION.md)  
**Rule:**
[`SPEC_V3_COMMON_ACTION_PHI_R2_R5_v2.md`](SPEC_V3_COMMON_ACTION_PHI_R2_R5_v2.md)  
**Machine register:**
[`strict_discrete_common_action_register_v3.json`](strict_discrete_common_action_register_v3.json)  
**Exact certificate:**
[`proof_v3_finite_carrier_inventory_v2.py`](../../../scripts/proofs/proof_v3_finite_carrier_inventory_v2.py)

---

## 1. Why v1 had to enlarge

The v1 inventory allowed at most one Hodge packet per SC tangent. That is
sufficient for straight transport and one-packet absorption, but not for the
exact finite cotangent collision that passes the transverse wave gate. The
collision acts on independent occupation channels indexed by complete
Hodge/C4 states and must admit two records with the same tangent.

R5 therefore triggered R1's declared reopen condition. V2 pays the finite
price explicitly: a binary exclusion slot for every complete field channel
and one local C3 collision-layer record. No continuum amplitude is added.

---

## 2. Primitive payloads

The ternary actuality and A9 relation token remain

\[
 \mathbb T=\{-1,0,+1\},
 \qquad
 \mathcal A_9=\mathbb T^2
 =\{0\}\sqcup(\mathbb Z_4\times\mathbb Z_2).
\]

For a directed SC tangent `d`, there are four perpendicular axial normals
`n`, two handed flags `h`, four C4 phases `k`, and two carried polarities
`epsilon`. A complete field-channel label is

\[
 \gamma=(d,n,h,k,\epsilon).
\]

The exact channel census is

\[
 \boxed{
 |\Gamma|=6\cdot4\cdot2\cdot4\cdot2=384.}
\]

The local field bank is the exclusion configuration

\[
 \mathcal B_\Gamma=\{0,1\}^{\Gamma},
 \qquad |\mathcal B_\Gamma|=2^{384}.
\]

An occupied channel is one complete phase/polarity/Hodge token. Distinct
channels may be occupied simultaneously; no channel holds more than one.
There is no packet identity or queue.

The collision layer is

\[
 \ell\in\mathbb Z_3.
\]

It is a finite local clock state synchronized in the registered vacuum
preparation. It is not the global tick: every local layer record advances by
the rule, while P2 supplies the order in which those advances occur.

---

## 3. Complete cell alphabets

### Sites

\[
 \boxed{
 \mathcal A_0=\mathbb T\times\mathbb Z_3
 \times\mathcal B_\Gamma,
 \qquad |\mathcal A_0|=9\cdot2^{384}.}
\]

The manifestation quotient is

\[
 m(s,\ell,b)=s.
\]

It is surjective and many-to-one. The field bank and C3 layer are invisible
to the ternary actuality readout.

### SC bonds

Every unoriented SC edge owns an independent primary/reserve A9 pair:

\[
 \boxed{
 \mathcal A_1=\mathcal A_9^P\times\mathcal A_9^D,
 \qquad |\mathcal A_1|=9^2=81.}
\]

### Plaquettes

Each of the two labeled FCC diagonals owns its own primary/reserve A9 pair:

\[
 \boxed{
 \mathcal A_2=(\mathcal A_9^{P,+}\times\mathcal A_9^{D,+})
 \times(\mathcal A_9^{P,-}\times\mathcal A_9^{D,-}),
 \qquad |\mathcal A_2|=9^4=6{,}561.}
\]

### Cubes

\[
 \boxed{\mathcal A_3=\{\varnothing\},\qquad|\mathcal A_3|=1.}
\]

BCC displacement is produced by three SC Hodge steps; no body-diagonal
storage token is introduced.

---

## 4. Ownership and transformation law

Every field token is owned by one `(site,channel)` exclusion slot. Every C18
material token is owned by one primary or reserve relation slot. The C3 layer
and ternary actuality are site-owned.

The signed cubic group permutes `d` and axial `n`; improper transformations
also reverse the pseudoscalar `h`. C4 advance permutes `k`; charge conjugation
permutes `epsilon`. These are permutations of the 384 labels, so the field
bank remains a finite product under every admitted symmetry.

The Hodge/C4 internal tick is a permutation of the 192 channels at fixed
polarity. The selected two-record collision is likewise a finite permutation
within each polarity layer. Its frozen three-layer table contains 55,008 pair
rows and has SHA-256

`D0BB71DBED7938ED286E1D6D91A16700DA31F4550E83B2FB3580CCC347B2BD25`.

The table is generated by the target-blind exact rank-greedy construction in
`proof_global_c3_cotangent_layer_equivariant_collision.py`; the hash freezes
the resulting rule rather than an experimental spectrum. Canonical hash
serialization is the 55,008 sorted UTF-8 rows joined by byte `0A`, with no
terminal newline.

---

## 5. No hidden continuum or history

The large cardinality `2^384` is finite and factorized. It must not be read as
one 384-bit integer amplitude. Each bit is an exclusion fact for a named
finite channel. Blocked real fields are signed occupation averages over
finite regions/preparation histories, never values stored in one bit bank.

The inventory contains no:

- real or complex microscopic amplitude;
- unbounded counter, coordinate, birth time, or packet serial number;
- variable-length queue or history;
- replay tape or pseudorandom seed; or
- physical coupling, mass, metric, or outcome label.

---

## 6. R1 v2 closure contract

R1 is closed for the selected R5-capable v3 carrier when the certificate verifies:

1. 384 complete field channels and `2^384` exclusion configurations;
2. site alphabet `9*2^384`;
3. bond alphabet `9^2`;
4. plaquette alphabet `9^4`;
5. singleton cube alphabet;
6. one owner per field/relation token;
7. signed-cubic, C4, polarity, and Hodge closure;
8. a many-to-one ternary manifestation quotient; and
9. exact agreement with the machine register and frozen collision hash.

A successor amendment is required if the exact rule or a mandatory physical
closure requires another microscopic state type. The enlarged bank is selected
because it is used by R5; its cardinality is not evidence that it is unique in
nature.
