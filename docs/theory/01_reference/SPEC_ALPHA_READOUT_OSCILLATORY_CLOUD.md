# SPEC · Alpha Readout as an Oscillatory Cloud Return Map

**Tag:** [REFERENCE] / [OPEN PROGRAM]
**Date:** 2026-06-15
**Status:** Canonical replacement for the falsified `LOOP_SECTOR` formalism, upgraded with Resolvent Target geometry.

---

## 1. The Semantic Pivot: From Loop to Cloud

The previous architectural attempt to extract the electromagnetic coupling $\alpha$ relied on a static spatial boundary: a topological winding integer evaluated along a closed loop around a defect. Numerical evaluation on the FTD engine explicitly **falsified** this approach (the "Gauge Trap").

The FTD ontology demands a completely different physical picture for a particle:

> **The probability cloud is not an ontic object. It is the public detection shadow of a finite, stable, oscillating flux-state body.**

*   **Loop $\rightarrow$ Topological Winding (Dead End)**
*   **Cloud Body $\rightarrow$ Resonance, Modes, Phase Delay, Resolvent Susceptibility (The Target)**

The particle is a structured excitation in the $(J \perp s)$ substrate. The continuous dispositional flux $J$ carries the wave structure; the discrete state $s$ records actualized threshold events.

---

## 2. The Core Object: $\mathcal{B}_{\Omega}(t)$

The readout queries the finite oscillatory body generated by the minimal neutral source $\Omega_{\min}$.

$$ \mathcal{B}_{\Omega}(t) = \text{The stable oscillatory flux-state body generated by } \Omega_{\min} $$

This body possesses a layered ontology:
1.  **Substrate Body:** The actual finite dynamic pattern in $(J, s)$.
2.  **Internal Phase:** The quadrature phase of the continuously evaluated field $z(v,t) = q + ip$.
3.  **Oscillation Modes:** Breathing, shear, and internal exchange modes.
4.  **Coupling:** The stiffness and susceptibility of the body under external perturbation.

---

## 3. The Resolvent Operator

A physical coupling measures **compliance/susceptibility**: how much response accumulates under a perturbation. In discrete-time dynamics, the natural susceptibility of a return map is the **resolvent**.

Let $T$ (or integer $m$ ticks) be one natural oscillation period of the unperturbed body $\mathcal{B}_{\Omega}$.
The one-cycle discrete phase progression operator along the trajectory is:
$$ M_{\rm cloud} = D U(\Omega_{t+m-1})\cdots D U(\Omega_t) $$

We project this onto the forced $\mathbb{Z}[i]$ two-channel basis, removing trivial modes (translation, time-shift, overall phase) to yield the reduced return map $M_{\rm red}$.

The decisive Alpha Readout operator $W_U$ is the resolvent of this return map:
$$ \mathcal{K}_{\rm cloud} = (I - M_{\rm red})^{-1} $$
$$ W_U = \mathcal{K}_{\rm cloud} $$

---

## 4. The Target Return Map

The master quadratic forces the resolvent $W_U$ to possess the exact invariants:
$$ \operatorname{Tr} W_U = 16G^{*2}, \qquad \det W_U = 16G^{*3} $$
This yields eigenvalues $\lambda_+ \approx 137.036$ and $\lambda_- \approx 3.024$.

Because $W_U = (I - M_{\rm red})^{-1}$, we can invert the relationship:
$$ M_{\rm red} = I - W_U^{-1} $$

Using the symmetric two-channel matrix for $W_{\rm target}$, the required return map becomes:
$$ M_{\rm target} = \begin{pmatrix} 1-\frac{1}{2G^*} & \frac{\sqrt{4G^{*2}-G^*}}{4G^{*2}} \\ \frac{\sqrt{4G^{*2}-G^*}}{4G^{*2}} & 1-\frac{1}{2G^*} \end{pmatrix} \approx \begin{pmatrix} 0.8310 & 0.1617 \\ 0.1617 & 0.8310 \end{pmatrix} $$

Its Floquet multipliers (eigenvalues) are:
$$ \mu_+ = 0.9927026566 $$
$$ \mu_- = 0.6693082234 $$

---

## 5. Semantic Meaning: The One-Cycle Return Deficit

The dominant mode ($\eta_+$) returns after one full oscillation with multiplier $\mu_+ \approx 0.9927$.

The one-cycle deficit is:
$$ 1 - \mu_+ = 0.0072973434 $$

This is exactly the inverse of the fine structure constant $\lambda_+$:
$$ \alpha_{\rm FTD} = 1 - \mu_+ = \frac{1}{\lambda_+} $$

**Physical Interpretation:**
The electromagnetic coupling $\alpha$ is the small amount by which the finite oscillatory body fails to return perfectly to itself after one complete internal cycle. The large value (137) is not a raw amplitude; it is a **high-compliance common mode** caused by a nearly closed return cycle. 

The lower branch ($\lambda_- \approx 3.024$) corresponds to $1 - \mu_- \approx 0.331$, representing the stiffer, highly leaky internal exchange/antisymmetric mode.

---

## 6. The Final Derivation Target (Proof Obligations)

The Alpha Readout no longer needs the engine to emit $137$ directly. It needs the minimal oscillatory cloud to emit the reduced return map $M_{\rm target}$.

**What must be proven next:**
Show that $\Omega_{\min}$ generates a stable cloud whose reduced one-cycle map yields the two Floquet multipliers:
$$ \mu_+ = 0.9927026566 $$
$$ \mu_- = 0.6693082234 $$

**Explicit Steps:**
1.  Define the minimal neutral cloud $\mathcal{B}_\Omega(t)$.
2.  Find its exact integer period $m$ in ticks.
3.  Linearize the phase-law along the full cycle to compute $M_{\rm cloud}$.
4.  Remove trivial modes (translation, time-shift, normalization, gauge-like phase).
5.  Project onto the forced $\mathbb{Z}[i]$ two-channel basis to obtain $M_{\rm red}$.
6.  Verify that the matrix entries match $M_{\rm target}$.

If successful, the resolvent of this return map forces $\alpha \approx 1/137.036$, completing Phase 2.
