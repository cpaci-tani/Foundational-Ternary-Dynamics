# SPEC - Quadrature Canonicalization

**Tag:** [OPEN PROGRAM]
**Date:** 2026-06-15
**LEDGER:** FTD-0210 [SYNTHESIS] - formalizes the admissibility requirements for deriving the intrinsic $\mathbb{Z}[i]$ field $z(v)$.
**Companion docs:** `SPEC_CONNECTION_EXTRACTION_RULE.md`

---

## 0. Purpose

The Connection Extraction Rule (FTD-0209) establishes that a discrete phase holonomy $A_J(e) = \operatorname{Arg}(\overline{z(v)} z(v+e))$ mathematically bypasses the longitudinal holonomy obstruction of Phase 1. Diagnostic numerical tests demonstrate that spatial projections (e.g., $z = J_x + i J_y$) indeed survive relaxation with a non-zero topological winding number ($\sum A_J = 4\pi$).

However, $z = J_x + i J_y$ is an ad hoc coordinate projection that artificially breaks the global 3D rotational symmetry of the continuous $J$ field. A fundamental structural value like the fine-structure constant cannot be derived from a hand-picked spatial slicing.

This document formally specifies the canonical derivation constraints for the complex quadrature field $z(v) = q(v) + i p(v)$. Until $z(v)$ is structurally forced by the FTD postulates, Phase 2 of the $\alpha$-readout program cannot commence.

---

## 1. The Spatial Projection No-Go

**Constraint 1:** The quadrature field $z(v)$ must preserve or canonically integrate over the 3D rotational symmetry of the underlying lattice manifold.

Any formulation of $z(v)$ that artificially singles out a preferred Euclidean plane (e.g., the $xy$-plane) without a dynamical or topological justification derived directly from the state-flux interaction is epistemically invalid. 

*(Note: Geometric algebra/Clifford multivectors or integrations over the 18 local Moore neighborhood normals were historically considered, but the edge-covariant tangent projection provides the exact geometrical closure.)*

## 2. Status: [DERIVED]

The exact algebraic path for canonically identifying $\mathbb{Z}[i]$ has been formalized.

Instead of global projections or unphysical dimension reduction, the complex phase is generated by an **edge-covariant tangent projection**. The plane $\mathbb{Z}[i]$ is dynamically assigned to the orthogonal transverse plane $P_e$ of the integration edge $e$. 
This naturally synthesizes the state and continuous flux without breaking the 3D $O_h$ cubic symmetry of the framework.

See **`DERIV_QUADRATURE_COVARIANCE.md`** for the complete [THEOREM]-grade derivation.

---

## 2. The Two-Channel Ontology Requirement

**Constraint 2:** The quadrature field $z(v)$ must dynamically synthesize the entire FTD ontology: the continuous flux field $J \in \mathbb{R}^3$ and the discrete ternary state field $s \in \{-1, 0, +1\}$.

It cannot be a pure function of $J$ alone. The alpha readout is defined by the minimal neutral source $\Omega_{\min}$, meaning the intrinsic phase structure must depend fundamentally on the coupling between the discrete manifestation ($s$) and its dispositional potential environment ($J$).

---

## 3. The Lemniscatic Mapping Constraint

**Constraint 3:** The formulation of $z(v)$ must be structurally bounded by the lemniscatic curve $y^2 = x^3 - x$.

As established in the FTD Algebraic Spine, the quarter-conjugacy structure of the master quadratic and the $G^*$ coefficient natively arise from the geometry of the lemniscatic integral. Therefore, the function synthesizing $s$ and $J$ into $z(v)$ must map into this specific arithmetic geometry, ensuring the resulting phase connection $A_J$ natively carries the quarter-period invariants.

---

## 4. The Decisive Proof Obligation

This specification frames the final admissibility hurdle for Phase 1 of the Alpha Readout Contract. The formal falsification test is:

> [!IMPORTANT]
> **Can the FTD phase-law canonically force a symmetric, rotation-invariant $z(v)$ from the two-channel $(s, J)$ state that still yields a non-zero topological winding $\sum A_J \neq 0$ around the minimal neutral source?**

*   **If NO:** The program closes negative. The topological signal requires cheating the geometry via ad hoc coordinates, rendering the loop readout structurally invalid.
*   **If YES:** Phase 1 is fully conquered. The derivation officially advances to Phase 2: linearizing the response operator over this holonomy to extract $W_L^{(\infty)}$ and checking for the $16G^{*3}$ determinant.

**Status:** Awaiting canonical definition of $z(v)[s, J]$.
