# SPEC · FTD/FQCR Doctrine Ledger v1.9

**Tag:** [REFERENCE] — single-page status map. Per-element tags within (see §0 status key + §13 audit).
**Date:** 2026-08-13
**Version:** 1.9 (Metadata/framing refresh only — no tag moves. §17's one-line summary gained a
clause noting the FTD-1000 CLK-1/FC-2 fold (2026-08-13) — this was added to §17 without a
version bump at the time, which the v1.9 bump now corrects per this document's own §16 refresh
policy. Prior: v1.8 — §2 gained a framing note (§2.1) around the FTD-0899–FTD-0999
native-time-carrier/temporal-interior block, which was appended to this document's rows across
2026-08-11/12 without a corresponding header or §17 summary update; v1.8 added that framing plus
a pointer to the FTD-0825 (2026-08-09) v2 "Contextual Actualization" branch, which this document
did not previously mention at all. Prior still: v1.7 — FTD-0412 scope correction: MC-T4.3 is
`[OPEN — SCOPED NO-GO PACKAGES]`, not a universal foundational obstruction; FTD-0126 is retracted
as a physical Wilson-Dirac result and cannot serve as a fourth structural-decoupling leg.
FTD-0244 remains theorem-negative for its axiomatized operator calculus. Prior still: v1.6
reconciled FTD-0244 and FTD-0208.).
**LEDGER:** FTD-0145 [SYNTHESIS] — claim-aggregation roll-up; introduces no new theorems.
**Companion docs:**
- [`SPEC_ALGEBRAIC_SPINE.md`](SPEC_ALGEBRAIC_SPINE.md) — canonical nine-numbered-results reference (seven theorem-grade + two honestly-tiered; theorem-focused, physics-free)
- [`SPEC_FQCR.md`](SPEC_FQCR.md) — FQCR capstone reference (Models I–V)
- [`SPEC_MATH_FIRST_ONTOLOGY.md`](SPEC_MATH_FIRST_ONTOLOGY.md) — canonical math-first ontology / readout ordering principle
- [`SPEC_PHYSICS_BRIDGE.md`](SPEC_PHYSICS_BRIDGE.md) — physics-bridge synthesis (FTD-0121, structural-uniqueness scans)
- [`SPEC_ALPHA_READOUT_CONTRACT.md`](SPEC_ALPHA_READOUT_CONTRACT.md) — MC-T4.3 operational-readout closure contract
- [`SPEC_OPEN_MATH_BY_SECTOR.md`](SPEC_OPEN_MATH_BY_SECTOR.md) — 18-item bridge-complete roadmap (Phase 2 hardening targets)
- [`../07_assessment/core_ledgers/TRACKER_ONTIC_TRUTH.md`](../07_assessment/core_ledgers/TRACKER_ONTIC_TRUTH.md) — bedrock truth-tier tracker (T1–T5; OT-N.M IDs)
- [`../07_assessment/core_ledgers/LEDGER.md`](../07_assessment/core_ledgers/LEDGER.md) — atomic per-claim provenance (FTD-NNNN IDs)

---

## §0 · What this document is and is not

**This document IS:** a single-page status map, organized as fourteen sections covering FTD's algebraic spine (§1–6), candidate physics-bridge sectors (§7–12), a non-circularity audit (§13), and a compressed forward roadmap (§14). Each row points back at a canonical source — `LEDGER` row, `TRACKER_ONTIC_TRUTH` tier, `SPEC_*` section, or proof script — and carries the **same epistemic tag the source carries**. The doctrine is a roll-up.

**This document IS NOT:** a derivation, a new tracker tier, a replacement for `LEDGER.md` or `TRACKER_ONTIC_TRUTH.md`, or a vehicle for tag promotion. If a reader believes this document promotes a claim above its canonical tag, they have found a bug — please file an issue and cite the source row.

**Why the doctrine exists.** FTD's canonical claim infrastructure has accumulated nine numbered results (seven theorem-grade + two honestly-tiered), ~150 LEDGER rows, fifteen T1–T5 tracker entries, an 18-item math-completion checklist, and a five-model FQCR capstone. The doctrine compresses this into a single navigation surface that exposes which derivation chains are non-circular, which are imported scaffolding, and which are pending hardening. It prevents cycling — repeatedly re-litigating the same claim because the per-claim status was buried in a 700-line ledger.

**v1.3 physicist consolidation.** The 2026-05-18 intake of `complete_ftd_chain_v1.md` adds no canonical claim and no new theorem. It sharpens the physics-facing reading: FTD is presently defensible as a finite-invariant algebraic reconstruction program whose strongest physical conjecture is the operational identification of the master-quadratic/FQCR dominant branch with the electromagnetic coupling. The mathematical object is solid; the physical readout is the open problem. `SPEC_MATH_FIRST_ONTOLOGY.md` now names this ordering explicitly: primitives -> invariants -> admissible readouts -> operational physics.

**v1.4 ontic-system reconciliation.** The 2026-05-20 intake of `FTD_Ontic_System_v0_2_Agent_Brief.md` records useful external finite-closure/FQCR vocabulary but does not import a new ontology or gravity sector. Per FTD-0184, the branch-compliance exponential readout metric is the closed-negative Yilmaz route. The surviving gravity task is substrate-side strong-field GR / Schwarzschild-Kerr-horizon derivation, not reusing the exponential metric as a shortcut.

**v1.6 status reconciliation.** The 2026-06-14 cleanup only reconciles this roll-up to canonical result docs. It records FTD-0244's K-BIND theorem-negative operator-calculus closure and FTD-0208 v3's clock-hypothesis closed-negative/axiom-level outcome. It promotes no physics claim; `x_+ = 1/α` remains [STRONGLY MOTIVATED CONJECTURE].

**v1.8 native-time-carrier framing + v2 boundary note (2026-08-13).** Two things this document
had not caught up with, addressed here as framing only — no row's tag changes:

1. **§2.1 (new).** The FTD-0899–FTD-0999 native-time-carrier / clock-carrier / temporal-interior
   programme (`docs/theory/10_eft_program/native_time_carrier_programme/` +
   `derivations/native_time_carrier_programme/` + `temporal_interior_programme/`, ~100 files,
   dated 2026-08-11/12) had its LEDGER rows appended into §2 · G* layer with no subsection break
   or narrative acknowledgment. §2.1 now frames that block and states its net verdict: the
   programme does not claim to have found a native clock carrier (every candidate still fails
   carrier constraint C11 by self-admission) and culminates in FTD-0999, a scalar
   conservation law for "clock growth" energy accounting with the carrier itself, native reserve
   density/current, and finite-tick `G*` still open.
2. **v2 "Contextual Actualization" branch (FTD-0825, 2026-08-09).** A successor constitution,
   `SPEC_FTD_FRAMEWORK_V2_CONTEXTUAL_ACTUALIZATION.md`, adopts a noncommutative potentiality net
   as an explicit alternative to this document's v1/FC-1 (declined measurement map `M`) — on the
   v2 branch only. v1 is unmodified and remains the default branch this doctrine describes; v2's
   own currency is tracked separately (`contextual_actualization_register_v2.json`), matching
   the same frozen-for-provenance treatment `SPEC_UNIFIED_AXIOM_REGISTER.md` and
   `SPEC_IMPORT_LEDGER.md` already gave it.

---

### §0.1 · Status key (canonical LEDGER tags)

This doctrine uses canonical tags exclusively. The right-hand column is what you'll see in `LEDGER.md`.

| Tag                             | Meaning                                                                                                            |
|---------------------------------|--------------------------------------------------------------------------------------------------------------------|
| **[THEOREM]**                   | Rigorously proven from FTD axioms or external classical theorems with explicit citation. Cannot be wrong without arithmetic mistake. |
| **[AXIOM]**                     | Structural postulate; not derivable.                                                                               |
| **[DERIVED]**                   | Established from axioms / prior theorems by an explicit chain reproduced in the cited doc; weaker than [THEOREM] when the chain has non-trivial assumptions. |
| **[SELECTION]**                 | Argued from consistency or naturalness; not uniquely proven. Reviewer expectation: critique the consistency argument. |
| **[STRONGLY MOTIVATED CONJECTURE]** | [CONJECTURE] with substantial structural and/or empirical evidence (uniqueness scan, multi-route convergence, sub-ppm match) but no derivation chain. |
| **[CONJECTURE]**                | Proposed interpretation requiring validation; weaker than [SMC] (no structural-uniqueness backing).                |
| **[NUMERICAL FACT]**            | Verified by exhaustive computation across an explicitly stated finite domain. NOT a structural theorem.            |
| **[IMPORTED]**                  | Standard physics or external mathematics adopted as bridge or scaffolding. May carry [PARAMETRIC] when the import provides the formula and FTD provides the numbers. |
| **[PARAMETRIC]**                | Standard physics formula filled with FTD constants; numbers fit but mechanism is borrowed. Not a derivation.       |
| **[IMPOSED]**                   | Calibration declaration (e.g., `a_phys ≡ ℓ_P`, `K_B = m_e`).                                                       |
| **[OPEN]**                      | Unresolved question; research opportunity.                                                                         |
| **[CLOSED NEGATIVE]**           | Hypothesis tested and falsified; preserved for provenance to prevent re-attempt.                                   |
| **[SYNTHESIS]**                 | Cross-document integration of multiple lower-level claims into a single externally-defensible package; not a new theorem. |

### §0.2 · v1.2 → canonical tag map

Earlier drafts of this doctrine used a parallel vocabulary. For continuity, the equivalences are:

| v1.2 draft tag                  | Canonical tag used here                                                                                            |
|---------------------------------|--------------------------------------------------------------------------------------------------------------------|
| THEOREM                         | [THEOREM]                                                                                                          |
| SELECTION / PRINCIPLE           | [SELECTION]                                                                                                        |
| NUMERICAL RECONSTRUCTION        | [STRONGLY MOTIVATED CONJECTURE] (when structural evidence exists) or [NUMERICAL FACT] (raw scan)                   |
| CONJECTURE / PHYSICAL ID        | [CONJECTURE] or [STRONGLY MOTIVATED CONJECTURE] (per evidence level)                                               |
| IMPORTED PHYSICS                | [IMPORTED] or [PARAMETRIC] (when the import is filled with FTD numbers)                                            |
| OPEN / HARDENING                | [OPEN]                                                                                                             |
| REJECTED / TOO STRONG           | [CLOSED NEGATIVE]                                                                                                  |
| CONDITIONAL THEOREM             | [THEOREM, conditional on X] (with X named)                                                                         |

---

### §0.3 · Physicist-facing consolidation from `complete_ftd_chain_v1.md`

The useful content of the external chain draft is the following blackboard-safe posture:

1. **Defensible core.** FTD has a finite algebraic spine: quarter conjugacy, `G*`, finite-N convergence, determinant-one recurrence, Casimir invariant, transfer-matrix restatement, and the master quadratic. These are mathematical claims with explicit proof anchors.
2. **Defensible physical observation.** The master-quadratic roots land near `1/alpha` and `N_c` with strong structural-uniqueness evidence. This supports `[STRONGLY MOTIVATED CONJECTURE]`, not `[THEOREM]`.
3. **Not yet defensible as derivation.** QED, Dirac, Standard Model, flavor, confinement, full gravity, Born-rule, and noetic/memory-clock layers do not currently follow from the FTD substrate. They are `[IMPORTED]`, `[PARAMETRIC]`, `[SELECTION]`, `[CONJECTURE]`, or `[OPEN]` as tagged below.
4. **Physics bottleneck.** The central missing object is an operational readout rule: why the algebraically distinguished branch is the electromagnetic coupling measured by matter. This is MC-T4.3. More algebraic near-miss scanning is not the main path forward.
5. **External claim discipline.** The strongest honest public sentence is: FTD constructs a rigid finite algebraic object whose dominant branch is a structurally unique candidate for `1/alpha`; it does not yet derive the physical coupling without an added readout principle.

This subsection is a consolidation rule for future writing. It does not override any per-claim row below.

---

# §1 · Core quarter-duality layer

**Primitive.** The conjugacy operator `J` with `J² = −I` generates a cyclic group of order 4 with elements `{I, J, −I, −J}` and eigenvalues `±i = e^{±2πi/4}`. This is the algebraic source of the quarter split that pervades the FTD spine: the i-cycle ontology, the framework integer `N_base = 4`, the (1+i)-tower of Theorem 8, and FQCR Model I.

**Quarter sectors.** A selected `J` has eigenphases `±i`. On the additionally selected twisted-circle Fourier domain these become shifts `1/4` and `3/4`, the two non-trivial residue classes mod 4. Restricted to primes, they are the split and inert classes of `Z[i]`. On the anchored chiral half-line, `G*` is their regularized asymmetry. The domain/polarization/origin/scale/order/multiplicity are not forced by `J²=-I` (FTD-0839).

| Claim                                                                | Tag                              | Source                                                                                  |
|----------------------------------------------------------------------|----------------------------------|-----------------------------------------------------------------------------------------|
| `J² = −I ⇒ J⁴ = I`                                                   | [THEOREM]                        | Standard linear algebra; SPEC_FQCR.md §1 Def 1                                          |
| Quarter shifts `1/4`, `3/4` on the selected twisted circle           | [THEOREM — CONDITIONAL DOMAIN]   | SPEC_FQCR.md §1 Def 2; OT-1.7; FTD-0839                                                  |
| `J` as ontic primitive of finite conjugacy                            | [SELECTION]                      | SPEC_FTD.md §1.1 (graded monism); FOUND_AXIOM_ZERO.md                  |
| Physical reality is finite-trace dynamics (interpretive ontology)    | [CONJECTURE — interpretive]      | Discharged algebraically by FQCR Model II (FTD-0142, OT-1.8); does not commit ontology |

---

# §2 · G* layer

**Definition.** `G* := Γ(1/4)/Γ(3/4) = Γ(1/4)²/(π√2) ≈ 2.95867512...`

**Notational warning.** G* (project canonical, ≈ 2.959) and the Bernoulli/Gauss lemniscate constant `ϖ = Γ(1/4)²/(2√(2π)) ≈ 2.622` are sometimes both called "the lemniscate constant" in informal usage. They are distinct: the master quadratic produces `x_+ = 137.036` only at `G* = 2.959`, not at `ϖ`. See `SPEC_ALGEBRAIC_SPINE.md` §1 (FTD-0117 typo audit).

| Claim                                                                | Tag                              | Source                                                                                  |
|----------------------------------------------------------------------|----------------------------------|-----------------------------------------------------------------------------------------|
| `G* = Γ(1/4)/Γ(3/4)`                                                 | [THEOREM]                        | SPEC_ALGEBRAIC_SPINE.md §1 Theorem 1; OT-1.2; FTD-0001                                  |
| Reflection identity `Γ(1/4)·Γ(3/4) = π√2`                            | [THEOREM]                        | Γ-function reflection                                                                   |
| Finite-N attractor `G_N* → G*` at `O(1/N²)`                          | [THEOREM]                        | SPEC_FQCR.md §2 Prop 2; FTD-0142; OT-1.8; verified by `proof_fqcr_convergence.py`        |
| Conditional-domain identity `det_ζ D_{3/4}/det_ζ D_{1/4} = G*`       | [THEOREM — CONDITIONAL DOMAIN]   | SPEC_FQCR.md §2 Prop 1; FTD-0141/0839; OT-1.7; via Lerch's formula                       |

### §2.1 · Native time-carrier / temporal-interior programme (FTD-0899–FTD-0999, 2026-08-11/12)

The table below is the native-time-carrier / clock-carrier / temporal-interior programme's
booked LEDGER rows, carried here unedited from `LEDGER.md`. It sits in the G* layer because
several of its results (the connection gearbox, the critical-quartic self-pair law, the
G*-gear-ratio rectifier, the cumulative clock-growth reserve law) bear directly on G*'s role as
a clock constant, though the programme is organizationally distinct from the G*-identity rows
above and below it. **Net verdict, per independent adversarial audit (2026-08-13):** the
programme never claims the substrate forces a clock carrier — every candidate still fails
carrier constraint C11 by explicit self-admission, the native C3 unit-strut-tensegrity search
named as the concrete next campaign (`SPEC_SUBSTRATE_NATIVE_CLOCK_MINIMUM_v1.md` §8) was not run
in this stretch, and several sub-branches (native orientation memory, plaquette/C4 circulation)
were tried and genuinely closed negative. The programme culminates in FTD-0999, an exact but
purely scalar conservation law for "clock growth" energy accounting, with native reserve
density/current, production integration, finite-tick `G*`, and Born/Bell all still explicitly
open. `G*` itself is used throughout as an exogenous "cadence" constant and is neither re-derived
nor fit — no spine tag changes. Full documents:
`docs/theory/10_eft_program/native_time_carrier_programme/`,
`docs/theory/10_eft_program/derivations/native_time_carrier_programme/`,
`docs/theory/10_eft_program/temporal_interior_programme/`.

| Claim                                                                | Tag                              | Source                                                                                   |
|----------------------------------------------------------------------|----------------------------------|-------------------------------------------------------------------------------------------|
| Signed pair energy `H=lambda[(q|q|)^2+y^2]` admits a unique reversible, energy-closed, oriented discrete gradient | [THEOREM — CONDITIONAL ON ADOPTED PAIR COUPLING] | FTD-0840; exact certificate `24/24`; production coupling and finite-tick cadence open |
| Local self-pair tensor `U=J otimes J`, `||U||_F^2=|J|^4`, admits a unique reversible, energy/angular-momentum-closed vector discrete gradient | [THEOREM — CONDITIONAL ON ADOPTED RADIAL COUPLING] | FTD-0841; exact certificate `26/26`; local `(J,W)` type is production-native, coupling/support/polarization/spatial closure/cadence open |
| Positive spatial edge energy plus selected onsite radial quartic admits exact simultaneous closure but no bounded zero-stiffness mode | [THEOREM — CONDITIONAL CLOSURE + SCOPED OBSTRUCTION] | FTD-0842; exact certificate `26/26`; exact solve globally dependent, connected kernel constant, production/local transaction and clock hardware open |
| Rank-one common-only propagation plus onsite relative quarticity gives a compact P4-local selected carrier | [THEOREM — SELECTED TWO-CHANNEL CONSTRUCTION] | FTD-0844 repaired `28/28`; positivity/softness uniquely gives `b=a` in registered class; production `b=0`, formation/readout/cadence open |
| A common/even position pointer sees only the symmetric-square phase; a positive faithful critical readout first appears at degree four with a selected odd pointer | [THEOREM — PARITY/QUADRATIC OBSTRUCTIONS + SCOPED DEGREE MINIMUM] | FTD-0846 repaired `32/32`; `W=kappa(r-q)^4/4` gives an exact onsite energy transaction; odd-pointer provenance, production coupling, and cadence compliance open; FTD-0848 supplies the selected reference record map |
| A symmetric polynomial ternary phase latch first appears at degree six; its local damped transaction and basin quotient can be energy/loss booked exactly | [THEOREM — SCOPED DEGREE MINIMUM + EXACT REFERENCE CLOSURE] | FTD-0848 repaired `30/30`; `V_T=beta*x^2*(x^2-A^2)^2`; the quotient, not damping, is the explicit lossy step; latch realization/Born/selector/thermal/cadence remain selected/open |
| Current production genesis/evaporation contains signed ternary acquisition and many-to-one loss fragments but is not the FTD-0848 loss-booked latch | [ENGINE FACT + THEOREM — CONDITIONAL EQUIVALENCE CLASSIFICATION + CLOSED NEGATIVE/PARTIAL POSITIVE] | FTD-0850 repaired `30/30`; no strict unlocked basin or event-level receiver/ledger; native movement-enabled barrier plus reservoir or explicit open-system adoption remains open |
| A signed ternary erasure requires an odd receiver label in addition to energy at zero export; positive export compresses both into one signed amplitude | [THEOREM — MINIMUM RECEIVER + CONDITIONAL BILATERAL CONSTRUCTION] | FTD-0851 exact `30/30`; `a=s sqrt(2B)` and selected `(L,R)=(s sqrt(B),-s sqrt(B))`; current movement/exhaust/journal is closed negative as the physical receiver; propagation/barrier open |
| An odd event pulse can preserve every history entry before tail contact by shifting each prior amplitude one causal cell outward; a truncated rail requires signed-tail export | [THEOREM — EXACT REFERENCE HISTORY CARRIER + FINITE-RAIL BOUNDARY + ENGINE-FACT PARTIAL PRODUCTION CHANNEL] | FTD-0852 exact `32/32`; exact local energy current/inverse; no universal exact-real memory no-go; production L/R difference is homogeneous but event deposit, relative-energy ledger, clearing law, and reciprocal barrier remain open |
| A signed positive-energy erasure can be written into a cubically symmetric relative-field shell by an exact ready-port transaction | [THEOREM — EXACT REDUCED REFERENCE DEPOSIT + SCOPED FIRST-SHELL ORBIT MINIMUM] | FTD-0853 exact `32/32`; on `Q0=0`, six opposite L/R face impulses deposit `B`, recover `(s,B)`, preserve the common field, and are invertible on the reduced domain; production event energy, port formation, dual ledger/barrier, propagation compliance, and full-state lift remain open |
| The adopted manifested matter-energy decrement supplies `B=gamma E_REST`, and the cubic radial shell is exactly the normalized causal odd-history rail | [THEOREM — CONDITIONAL DIAGNOSTIC IDENTITY + CUBIC/RAIL ISOMETRY + RECURSIVE READY PORT] | FTD-0855 repaired `32/32`; `D=Q/sqrt(12)`, and the outward shift forms the next port while adding exactly `B`; `E_REST` remains imposed and production ledger/reserved rail/barrier/full-state lift remain open |
| A strictly protected deterministic record that can also exchange needs distinguishable eligibility, and a reciprocal first-order rail must retain incoming/outgoing orientation | [THEOREM — SCOPED LOWER BOUNDS + EXACT CONTROLLED TWO-PORT BARRIER] | FTD-0856 exact `32/32`; controlled identity/swap is symmetric, orthogonal, involutive, and realizes hold/emission/absorption; isolated `ftd::eft` witness passes `1/1`; production lock/dual types are fragments, with physical activation, characteristic separation, work, and full-state lift open |
| Production event acceptance can be target-blind while remaining unable to actuate the relative reciprocal port | [ENGINE FACT — FIXED-INPUT LOCAL ACCEPTANCE] + [THEOREM — COMMON/RELATIVE KERNEL AND C18 SHIFT OBSTRUCTION] | FTD-0858 repaired `40/40`; common-field hazards cannot determine relative readiness/amplitude; the exact characteristic chart exists, but `c^2=1/3` is not the one-cell rail. Isolated CTest `1/1`; relative transducer, protected rail/compliance, controller work, and full-state lift remain open |
| A nonzero relative canonical pair admits an exact target-blind action pump, but zero has no positive equivariant phase and one unlabelled pair cannot retain arbitrary background plus an erased signed event faithfully | [THEOREM — NONZERO-CARRIER PUMP + EMPTY-CARRIER/FAITHFULNESS BOUNDARIES] + [SELECTION — SIGNED QUARTER TURN] | FTD-0860 repaired `36/36`; `z'=sqrt((I+B)/I)sJz` gives `I'=I+B` and unit symplectic Jacobian on `I>0`. It is a lossy mixer; faithful history remains the separate signed rail. CTest `1/1`; production localization, export, relative ledger, declared loss, and controller remain open |
| A prepared nonzero phase calendar makes the signed quarter-turn faithful along a selected causal export rail and bounds the retained excess action | [THEOREM — CALENDAR COHERENCE + PREPARED-BASELINE RECOVERY + FINITE-RAIL LEDGER] + [SELECTION — BASELINE/DIRECTED RAIL] | FTD-0862 repaired `36/36`; `kappa-omega in 2pi Z` preserves the reference along `j,n -> j+1,n+1`, so `B=I-I_*` and `s=sign(beta wedge Z)` are exact downstream readouts. `Delta H_ex=B-E_tail`, hence a length-`N` rail retains at most `N B_max`. Environment-completed shift is symplectic; CTest `1/1`. Production C18, phase source/maintenance, cubic embedding, tail ledger, and controller remain open |
| A nonzero autonomous phase reference can orient a separate zero-baseline signal while an exact reciprocal gate transfers event energy between matter and signal without spending the reference action | [THEOREM — CATALYTIC PHASE REFERENCE + RECIPROCAL ZERO-SIGNAL EXCHANGE] + [SELECTED REALIZATION — CONSUMES EXISTING PHASE-RAIL SELECTION] | FTD-0863 exact `36/36`; `D=af+be` in the frame of `beta`, and the controlled swap exchanges `(m,a)` while preserving `I_*+(m^2+|D|^2)/2`. Emission gives `|D'|^2/2=B` and `sign(beta wedge D')=s`; the same involution absorbs it. Finite-ring winding constrains but does not select `omega`; `G*`, native formation/maintenance/backreaction, cubic transport, controller, and production ledger remain open. CTest `1/1` |
| A reciprocal scalar swap requires a full two-mode Hamiltonian lift; an autonomous harmonic phase gate realizes exact hold/swap with a transient reserve, while a strictly convex quartic clock cannot supply one fixed load-blind swap angle in the registered minimal class | [THEOREM — SYMPLECTIC MINIMUM + AUTONOMOUS HARMONIC EXCHANGE + RESERVE LEDGER + CONVEX-CLOCK LOAD BOUNDARY] + [IMPOSED REFERENCE HAMILTONIAN LAW] | FTD-0865 repaired `40/40`; `H=omega I+nu A+epsilon chi(1-cos theta)I_r`, with `nu=omega`, `chi=omega/2`, gives exact one-cycle identity/swap and `I_min=I_0-I_r`, `I(T)=I_0`. For emission `I_r=B/2`. For `K''>0`, `dXi/dI_r>0`; quartic `K proportional I^(4/3)` is strictly convex. CTest `1/1`; dynamic eligibility, compensation, quartic realization, cubic/production coupling remain open |
| The existing ternary latch supplies the unique minimum even hold/exchange clutch, and one exact active cycle moves its signed event into a signal that survives a gate-zero local reset request | [THEOREM — UNIQUE TERNARY-SQUARE ELIGIBILITY + GATE-ZERO CLUTCH RELEASE + SIGN/ENERGY HANDSHAKE] + [CONDITIONAL — SELECTED LATCH/REFERENCE AND IMPOSED HARMONIC LAW] | FTD-0867 repaired `40/40`; `epsilon=s^2`, `Delta H_clutch=(s_1^2-s_0^2)chi(1-cos theta)I_r`, and `B=|D'|^2/2`, `s=sign(beta wedge D')`. Since the active map is involutive, a second unreset cycle undoes the export. CTest `1/1`; microscopic latch reset, autonomous acknowledgement, bath, synchronization, native mode formation, cubic production, and operational hiding remain open |
| A completed outgoing signal can serve as its own local acknowledgement; exact autonomous finite-time reset is impossible for locally Lipschitz flow but closes under a selected cusp law with an explicit scalar work/bath ledger | [THEOREM — LOCAL SIGNAL-COMPLETION ACKNOWLEDGEMENT] + [THEOREM — SMOOTH AUTONOMOUS FINITE-TIME RESET OBSTRUCTION] + [THEOREM, CONDITIONAL — CUSP RESET/OUTPUT HANDOFF] + [IMPOSED TWO-STROKE WAVEFORM] + [SELECTED NONSMOOTH RESET/BATH ACCOUNT] | FTD-0869 repaired `44/44`; midpoint completion gives `a=s^2 1[M=0]1[|D|^2/2>0]`, the compressed exchange requires `I_0>B`, and `gamma xdot in -kappa partial|x|` resets in `gamma A/kappa` with controller loss and scalar-bath gain `kappa A`. An empty output handoff returns the local reference ready while retaining `(s,B)`. CTest `1/1`; microscopic bath, robustness, native formation, protected cubic production, quartic compensation, `G*` synchronization, and operational hiding remain open |
| The completed oriented signal can reversibly uncompute the matching actual ternary latch by controlled subtraction in `Z_3`, eliminating any logical need for an extra acknowledgement bit, reset-history trit, or bath | [THEOREM — REVERSIBLE ACTUAL-LAYER TERNARY UNCOMPUTATION] + [THEOREM — MINIMUM RETAINED-ORIENTATION REQUIREMENT] + [CLOSED NEGATIVE — EXTRA LOGICAL RESET STATE/BATH NECESSITY] | FTD-0871 repaired `40/40`; `U_a(s,E)=(s minus a d(E),E)` has inverse `(s' plus a d(E),E)`, so `d(E)=s` gives exact `(0,E)` without erasure. Energy-only retention cannot invert both signs; the signal already supplies all three labels. CTest `1/1`; continuous-latch realization, controller work, native formation, robust clearing, protected cubic transport, production coupling, quartic compensation, `G*` synchronization, and operational hiding remain open |
| The unique orientation-preserving ternary isometry that transfers a ready latch into its output is the quarter-turn `R(s,o)=(-o,s)`; its inverse absorbs, while an empty-port/otherwise-identity wrapper is noninjective | [THEOREM — UNIQUE ORIENTED TERNARY QUARTER-TURN GEARBOX] + [THEOREM — REVERSIBLE READY EMISSION/INVERSE ABSORPTION] + [CLOSED NEGATIVE — NAIVE FAIL-CLOSED BACKPRESSURE WRAPPER] | FTD-0872 first-run `40/40`; `R^2=-I`, `R^4=I`, `R^T R=I`, `det R=1`, and `(s,0)->(0,s)`. Ordered area retains direction although `Sym^2(R)=Sym^2(-R)`. The wrapper collision is `F(1,0)=F(0,1)=(0,1)`. CTest `1/1`; the label norm is not yet physical energy. Actuator/work, reciprocal scheduling, protected cubic transport, native formation, production coupling, robustness, and `G*` cadence remain open |
| The oriented ternary quarter-turn has an exact four-dimensional Hamiltonian phase-gate lift with a transient clock-action ledger, but repeated or norm-only gating is not a one-shot scheduler | [CONDITIONAL THEOREM — MINIMUM FOUR-DIMENSIONAL LIFT IN THE REGISTERED CLASS] + [THEOREM — EXACT HOLD/R/R-INVERSE WINDINGS AND ENERGY LEDGER] + [IMPOSED — HARMONIC LAW AND AMPLITUDE SCALE] + [CLOSED NEGATIVE — REPEATING/NORM-ONLY ONE-SHOT SCHEDULER] | FTD-0873 first-run `48/48`; `(p,q)=a(s,o)`, `H=Omega I+Omega A+e sigma (Omega/4)(1-cos theta)A`. The maximum transient reference/interaction exchange is `Omega A/2`, endpoint residual is zero, and off-phase switching is generally nonzero. CTest `1/1`, coupled chain `12/12`; native scale/formation, dynamic gate-zero scheduling, protected transport, production, robustness, and synchronization to the separate quartic `G*` calendar remain open |
| Alternating the oriented ternary quarter-turn over the two disjoint nearest-neighbour parity matchings gives exact finite-horizon one-cell-per-tick prepared-record transport; occupied bonds retain both labels but do not ensure progress, and a distinct reversible predecessor cannot enter a fixed done state | [THEOREM — EXACT PREPARED-PULSE PARITY-RAIL TRANSPORT] + [THEOREM — RECIPROCAL BACKPRESSURE RETENTION] + [THEOREM — REVERSIBLE FIXED-DONE-STATE NO-GO] + [SELECTION — EXISTING PHASE-RAIL TYPE] | FTD-0874 first-run `48/48`; `U_n` applies `R(a,b)=(-b,a)` to bonds with left coordinate parity `n mod 2`. For `x_j^(0)=s delta_(j,0)`, `x_j^(t)=s delta_(j,t)` at every registered finite horizon. A fixed matching cannot leave its two-site block; a fully occupied control remains occupied. CTest `1/1`, coupled chain `13/13`; native intersite Hamiltonian, routing, congestion resolution, finite boundaries, production, robustness, and separate quartic-`G*` synchronization remain open |
| The alternating parity rail has an exact positive local canonical Hamiltonian lift with one canonical pair per site and a local antisymmetric energy current; the undoubled scalar common symplectic form is boundary-global, so the minimum is a locality result in the registered onsite-direct-sum class | [THEOREM — SCALAR COMMON FORM IS BOUNDARY-GLOBAL] + [THEOREM — MINIMUM REGISTERED LOCAL CANONICAL DOUBLET] + [THEOREM — EXACT STROBOSCOPIC HAMILTONIAN PARITY RAIL] + [THEOREM — LOCAL BOND-ENERGY CURRENT] + [SELECTION — EXISTING PHASE-RAIL TYPE] | FTD-0875 first-run `56/56`; `H=Omega I+Omega N+sigma Omega(1-cos theta)L_n/4` implements the forward/inverse layer after one harmonic cycle. `|L_n|<=N` makes the carrier-plus-interaction term nonnegative; prepared energy `Omega a^2/2` transfers exactly and endpoint residual is zero. On the actual section `p=0`, `L_n=0` is a special zero-clock-backreaction orbit, not a free-hardware claim. CTest `1/1`, coupled chain `14/14`; native doublet formation/scale, routing, congestion, finite boundaries, production, robustness, quartic-`G*` synchronization, and hiding remain open |
| The substrate's two-slice flux history is exactly equivalent to the stored `(flux,wave_vel)` phase-complete state, which supplies three onsite canonical pairs; the free symmetric-stiffness kick/drift is symplectic, but the complete dissipative/projected/stochastic production tick is not licensed as symplectic | [THEOREM — EXACT HISTORY/MARKOV EQUIVALENCE] + [THEOREM — FREE-WAVE SYMPLECTIC CARRIER] + [ENGINE FACT — NATIVE THREE-PAIR STORAGE] + [CLOSED NEGATIVE — SYMPLECTIC COMPLETE PRODUCTION TICK] | FTD-0876 first-run `55/55`; `P_(n-1/2)=(J_n-J_(n-1))/h` is bijective with two consecutive slices. `S_h=[[I-h^2K,hI],[-hK,I]]` preserves the canonical form iff `K=K^T`. Damping pulls the form back by `rho^2`; nonidentity projection is noninvertible; Langevin/genesis/loss lie outside the proof. CTest `1/1`, coupled chain `15/15`; preparation, scale, production bond actuation, constrained Gauss dynamics, loss completion, routing, robustness, `G*`, and hiding remain open |
| Matched oriented-face Gauss incidence supplies an exact transverse/charge canonical reduction and a unique minimum-energy static neutral ternary record section, but its charge conjugate is intrinsically relational and the live cell-centred Gauss pass is not the exact matched projector | [THEOREM — MATCHED GAUSS CANONICAL REDUCTION] + [THEOREM — STATIC TERNARY RECORD SECTION] + [THEOREM — NO UNIFORMLY FINITE-RANGE TRANSLATION-INVARIANT CHARGE CONJUGATE] + [THEOREM — REVERSIBLE AFFINE PREPARATION REQUIRES A DISCREPANCY LEDGER] + [CLOSED NEGATIVE — EXACT LIVE CELL-CENTRED PROJECTOR] | FTD-0880 repaired source-locked certificate `66/66`; all invalid FTD-0877--0879 parents are preserved. For `L=DD^T`, `q=DJ`, and `p=L^+DP`, `{q,p}=Pi_Q` and the canonical form splits into transverse and charge sectors. `(J,P)=(D^T L^+ g s,0)` is the unique minimum-energy static neutral record. A Laurent-polynomial root-count excludes a uniformly finite-range translation-invariant right inverse. The production central `DG` symbol differs exactly from its 18-point SOR symbol before finite iteration and manifested-site skipping. CTest `1/1`, coupled chain `16/16`. At the FTD-0880 boundary, dynamic native preparation and reversible environment dynamics remained open; the FTD-0882 row immediately below closes only the conditional fresh-port reference preparation. Production actuation, autonomous environment/source hardware, scale, routing, finite-support uncontained formulation, quartic-`G*` synchronization, and hiding remain open. No selected type is added. |
| Alternating local residual/environment quarter-turns prepare the matched minimum-energy Gauss record asymptotically from empty flux while retained signed history makes every finite evolution exactly reversible; the fresh-port reference boundary splits supplied work equally between static field and exported history energy | [THEOREM — CONDITIONAL REVERSIBLE LOCAL GAUSS-RECORD PREPARATION] + [THEOREM — EXACT RETAINED-HISTORY INVERSE] + [THEOREM — REFERENCE FIELD/HISTORY ENERGY EQUALITY] + [CLOSED NEGATIVE — SIZE-INDEPENDENT FINITE EXACT LOCAL PREPARATION] + [SELECTION — EXISTING PHASE-RAIL TYPE] | FTD-0882 repaired inherited certificate `60/60`; invalid FTD-0881 parent preserved. Each active cell applies `(r/sqrt(6),e/sqrt(6))->(e/sqrt(6),-r/sqrt(6))`; fresh-zero ports make the even/odd layers affine orthogonal projections and their zero-start sequence converges to `D^T L^+q` without evaluating `L^+` locally. Retaining outgoing ports gives the exact inverse. At the empty-field/fresh-port limit, `E_field=E_hist=||J_s||^2/2` and `W_src=||J_s||^2`. A uniform finite exact layer count would violate the FTD-0880 locality no-go. CTest `1/1`, coupled chain `17/17`; autonomous freshness/recycling, a positive source reservoir, local stopping, moving sources, boundaries, finite-capacity backpressure, production migration, physical scale, quartic-`G*` synchronization, and hiding remain open. No selected type is added. |
| An explicit cyclic bank of `C` signed port vectors supplies exactly `C` fresh reversible Gauss-preparation layers, while a positive one-amplitude quadratic battery carries the local source work by the unique sign-preserving square-root update; generic indefinite finite cyclic freshness closes negative only in that registered bank class | [THEOREM — FINITE EXPLICIT PORT-BANK FRESHNESS CAPACITY] + [THEOREM — EXACT FULL-STATE FINITE-HORIZON REVERSIBILITY] + [CONDITIONAL THEOREM — UNIQUE SIGN-PRESERVING POSITIVE QUADRATIC BATTERY LAW] + [CLOSED NEGATIVE — INDEFINITE FINITE CYCLIC EXPLICIT-PORT FRESHNESS] + [IMPOSED — BATTERY LAW/RESERVE] | FTD-0884 repaired inherited certificate `56/56`; invalid FTD-0883 parent preserved. With initially zero bank coordinates, cursor advance modulo `C` gives `C` fresh projections and retained signed output makes the full field/bank/cursor state exactly reversible. A generic nonzero output returns on layer `C+1`. For `E_b=b^2/2` and `w=q(e-r)/6`, strict reserve plus sign preservation uniquely gives `b'=sgn(b)sqrt(b^2-2w)`; battery loss equals source work and total field+bank+battery energy is exact. This does not exclude exact-real compression and does not derive a canonical Hamiltonian reservoir. CTest `1/1`, coupled chain `18/18`; unbounded/open history, 3D routing, canonical battery formation/recharge, moving sources, production, scale, quartic-`G*` synchronization, and hiding remain open. No selected type is added. |
| The local Gauss residual/port quarter-turn has a positive source-centered canonical lift, and the raw source work is exactly interaction-energy exchange; restoring the missing battery phase proves that the FTD-0884 square-root law is exact only on a Lagrangian section and that a state-dependent phase-blind post-hoc drain is not symplectic | [THEOREM — POSITIVE SOURCE-CENTERED CANONICAL GAUSS LAYER] + [THEOREM — RAW-WORK/INTERACTION-ENERGY EXCHANGE] + [CLOSED NEGATIVE — PHASE-BLIND STATE-DEPENDENT POST-HOC CANONICAL DRAIN] + [REFINED BOUNDARY — SQUARE-ROOT BATTERY IS LAGRANGIAN-SECTION REFERENCE] + [IMPOSED — CLOCKED HAMILTONIAN INTERPOLATION/SCALE] | FTD-0886 repaired inherited certificate `64/64`; invalid FTD-0885 parent preserved. For complete residual/port modes, `N=(u^2+a^2+pi_u^2+pi_a^2)/2`, `L=a pi_u-u pi_a`, and `H=omega I+omega N+sigma omega(1-cos theta)L/4` give the exact symplectic quarter-turn with `H_carrier>=omega N/2`. `Delta E_raw=w` and `Delta U_int=-w`. The square-root cotangent lift changes oscillator energy by `-w(1+p_b^2/b^2)`; a nonconstant phase-blind action drain adds `-dw wedge dphi`. Complete-pair open history shifts symplectically, while finite cyclic capacity remains. CTest `1/1`, coupled chain `19/19`; autonomous parity/source dynamics, physical open history, moving-source recoil, production, scale, quartic-`G*`, and hiding remain open. No selected type is added. |
| One periodic phase coordinate can autonomously compile the complete color-0/color-1 Gauss sequence, while a nonzero positive source-reaction impulse requires diverting energy from the history-saturated FTD-0886 endpoint into one additional canonical pair | [THEOREM — POSITIVE AUTONOMOUS PHASE-WINDOW PARITY CONTROLLER] + [THEOREM — MINIMUM CANONICAL SOURCE-REACTION PAIR IN THE REGISTERED CLASS] + [THEOREM — EXACT HISTORY/REACTION ENERGY SPLITTER] + [CLOSED NEGATIVE — POSITIVE RECOIL AT THE UNCHANGED HISTORY-SATURATED ENDPOINT] + [SELECTION — EQUAL SPLIT GIVEN OUTPUT-CHANNEL EXCHANGE SYMMETRY] + [IMPOSED — WINDOWS/ORIGIN/CLOCK SCALE/REACTION ROLE] | FTD-0888 repaired inherited certificate `72/72`; invalid FTD-0887 parent preserved. Six disjoint `C^1` windows under `H=Omega I+6 Omega N+Omega sum kappa_j rho_j G_j` give the exact ordered six-pulse product, `H_carrier>=3 Omega N`, and returned action without an external integer-parity input. On the ready slice, `a'=-cos(eta)u`, `pi_r'=sin(eta)u`; history/reaction energies are `cos^2(eta)`/`sin^2(eta)` shares. `eta=pi/4` is unique only after selected channel-exchange symmetry. CTest `1/1`, coupled chain `20/20`; native phase hardware, spatial ternary-source identification/motion, physical open history, production, scale, quartic-`G*`, and hiding remain open. No new selected type is added. |
| A scalar source reaction cannot select a nonzero cubic recoil direction; an orientation-free spatial reaction requires three canonical pairs, while an independently supplied local field impulse conditionally orients one scalar-pair slice and fixes its energy split | [THEOREM — CUBIC SCALAR-TO-VECTOR NO-GO] + [THEOREM — THREE-PAIR MINIMUM IN THE REGISTERED ORIENTATION-FREE CLASS] + [CONDITIONAL THEOREM — EXACT RELATIVISTIC COTANGENT CHART/FREE TRANSPORT] + [THEOREM — CONSERVATION-FIXED SPLIT ANGLE] + [CLOSED NEGATIVE — UNIVERSAL EQUAL SPLIT] + [SELECTION — DISPERSION/VECTOR REACTION ROLE] + [IMPOSED — E0/c/SOURCE DATA] | FTD-0890 repaired inherited certificate `68/68`; invalid FTD-0889 parent preserved. `Fix(T1u)=0`, one three-dimensional alternating form is singular, and `T1u+T1u` carries `sum dR_i wedge dPi_i`. Conditional on `K(p)=sqrt(E0^2+c^2|p|^2)-E0`, the cotangent map `p=sqrt(E0+|Pi|^2/4)Pi/c` is symplectic and satisfies `K=|Pi|^2/2`; free drift and face-current continuity close. The matched field impulse sets direction and conservation fixes `sin^2 eta=K_req/E_res`; equal split occurs only at `K_req=E_res/2`. CTest `1/1`, coupled chain `21/21`; native vector common action, mass-scale derivation, stable source formation, production, quartic-`G*`, and hiding remain open. No new selected type is added. |
| The selected constituent canonical phase space already contains the required three-pair reaction carrier as its exact collective sector; selected relativistic constituent dispersions compose with additive inertia, but static stability and a rest offset do not determine absolute mass or a continuous total Noether momentum | [THEOREM — EXACT COLLECTIVE REACTION TRIPLET] + [CONDITIONAL THEOREM — COMPOSITE DISPERSION/INERTIAL ADDITIVITY] + [CLOSED NEGATIVE — STATIC HESSIAN/REST OFFSET DO NOT FIX MASS] + [BOUNDARY — TOTAL FIELD-MATTER NOETHER MOMENTUM OPEN] + [SELECTION — CONSTITUENT PHASE SPACE/DISPERSION] + [IMPOSED — epsilon_a/c/BINDING OFFSET/INITIAL DATA] | FTD-0892 repaired inherited certificate `68/68`; invalid FTD-0891 parent preserved. Helmert reduction gives `sum p_a dot dx_a=P dot dX+sum_(mu>0) pi_mu dot dq_mu`, with `X=N^-1 sum x_a` and `P=sum p_a`; no new selected vector type is added. Strict convexity at fixed `P` gives `p_a=epsilon_a P/sum epsilon`, `E_coll=sqrt((sum epsilon)^2+c^2|P|^2)`, and conditional `M_coll=sum epsilon/c^2`. Arbitrary positive kinetic metrics share one stable static Hessian, and a nonboosted offset gives `E_rest/c^2-M_inertial=U0/c^2`. Exact `Z^3` translation and Peierls pinning do not supply an additive continuous field-matter Noether charge. CTest `1/1`, actualization chain `22/22`; constituent formation, dressed boost/mass closure, exact total momentum, stable pole, production, quartic-`G*`, and hiding remain open. |
| Near a stable dressed state, physical inertia is jointly determined by the complete time-odd energy Hessian and the independently defined total-momentum map; energy curvature without a momentum normalization cannot identify mass | [THEOREM — CONDITIONAL DRESSED TENSOR `M=B A^-1 B^T`] + [CLOSED NEGATIVE — ENERGY HESSIAN ALONE IDENTIFIES MASS] + [BOUNDARY — TOTAL FIELD-MATTER MOMENTUM MAP OPEN] + [IMPOSED — TWO-CHANNEL REFERENCE] | FTD-0893 first locked certificate `57/57`. Constrained minimization gives `y*=A^-1 B^T(B A^-1 B^T)^-1P` and `E_eff=E0+(1/2)P^T M^-1P`. A field-like odd mode contributes through `A`/`B`; a static offset does not. Holding `A` fixed while scaling `B` by `s` scales `M` by `s^2`, so the common action's exact energy and measured co-moving dressing do not yet set absolute inertia. The spline-Poynting observer remains a failed coupled-recoil candidate. CTest `1/1`; exact total momentum, stable pole, production, quartic-`G*`, and hiding remain open. No selected type is added. |
| Integer translation supplies exact torus-valued quasimomentum, but a globally real additive spectral momentum cannot be simultaneously finite range and branch free | [THEOREM — EXACT QUASIMOMENTUM ADDITION MODULO RECIPROCAL LATTICE] + [THEOREM — NO CONTINUOUS HOMOMORPHIC `T^3 -> R^3` SECTION] + [THEOREM — FINITE-RANGE GLOBAL UNWRAPPED-GENERATOR OBSTRUCTION] + [BOUNDARY — WINDING DYNAMICS/PHYSICAL SCALE/TOTAL MOMENTUM OPEN] | FTD-0894 parent and FTD-0895 repair remain execution-invalid at `75/81` and `80/81`; the one-marker FTD-0896 repair passes inherited `81/81`. Characters of the local `Z^3` translation algebra are labelled by `T^3`, and products add labels modulo `2 pi`. Compactness excludes a continuous additive global section. Every finite-range spectral weight is periodic; the exact principal coordinate is an infinite-range sawtooth with a zone-edge branch. A real lift may retain `w in Z^3`, but its update and the scale in `P=p_*(k+2 pi w)` are not derived or selected. Local stress mechanics remains unexcluded. CTest `1/1`, actualization chain `24/24`; no production, Born, `G*`, absolute-mass, or type-currency promotion. |
| A supplied equal-and-opposite pair increment has a unique exact reciprocal-carry reservoir update, but that topological/history ledger does not determine interaction origin, energy, substrate ownership, or physical momentum scale | [THEOREM — EXACT CONDITIONAL RECIPROCAL-CARRY UPDATE] + [THEOREM — UNIQUE RESERVOIR INCREMENT GIVEN BRANCH AND CONSERVATION] + [THEOREM — CONDITIONAL FULL-STATE REVERSAL] + [BOUNDARY — IMPULSE ORIGIN/ENERGY/SCALE/SUBSTRATE PARTITION OPEN] | FTD-0897 first locked certificate `89/89`. With `k_i` principal, `c_1=c(k_1+q)`, `c_2=c(k_2-q)`, and `W'=W+c_1+c_2`, the lifted pair sum is exactly conserved. Conservation uniquely fixes the reservoir increment; `-q,+q` reverses the full state and event carries telescope. Periodic band energy is blind to `W`, and the same map admits inequivalent reservoir-energy laws. Focused CTest `1/1`, actualization chain `25/25`; `q`, carry ownership, work/backreaction, `p_*`, exact total field--matter momentum, absolute mass, production, Born, and `G*` remain open. No selected type or currency is added. |
| The selected relative-quartic recursion generates an exact equal-and-opposite impulse, positive conserved energy, reciprocal carry, and continuum `G*` factor, but exact common-mode decoupling prevents a physical matter--field gearbox | [THEOREM — EXACT CONDITIONAL QUARTIC-RELATIVE IMPULSE] + [THEOREM — EXACT RELATIVE ENERGY/RECIPROCAL-CARRY COMPOSITION] + [THEOREM — CONDITIONAL SIGNED-STEP REVERSAL] + [THEOREM — CONTINUUM `G*` PERIOD FACTOR] + [BOUNDARY — COMMON COUPLING/SCALE/FINITE-TICK CADENCE OPEN] | FTD-0898 first locked certificate `97/97`. The discrete-gradient step preserves `H_D=Pi^2/(2m)+lambda D^4`, generates `Delta P_L=-Delta P_R=Delta Pi/sqrt(2)`, reconstructs every FTD-0897 endpoint chart/carry, and reverses under the signed step. Its continuum generator obeys `T A=sqrt(pi) G* sqrt(m/(2 lambda))`, but `P_C` is exactly invariant and `G*` is absent from the finite map. Focused CTest `1/1`, actualization chain `26/26`; common coupling, channel identification, `p_*`, carry energy, integer-tick cadence, absolute mass, production, and Born remain open. No selected type or currency is added. |
| The minimum velocity-linear common/relative connection is an exact imposed mechanical gearbox, but `i` fixes only its orientation and continuous nonzero engagement detunes the exact critical quartic | [THEOREM — EXACT CONDITIONAL CONNECTION-ACTION GEARBOX] + [THEOREM — EXACT CANONICAL ENERGY/MOMENTUM/ANGULAR-MOMENTUM LEDGER] + [THEOREM — CONDITIONAL SIGNED-STEP/CHANNEL-EXCHANGE REVERSAL] + [BOUNDARY — GAMMA/SCALE/IDENTIFICATION OPEN] + [BOUNDARY — CONTINUOUS NONZERO CONNECTION DETUNES CRITICAL QUARTIC] | FTD-0899 and FTD-0900 remain execution-invalid; the exactly scoped FTD-0901 repair passes inherited `87/87`. For `L=(M/2)|Cdot|^2+(m/2)|Ddot|^2+gamma D.Cdot-lambda|D|^4`, `P=M Cdot+gamma D` is conserved and `K=P-gamma D` obeys `Delta K=-gamma Delta D`. The discrete-gradient endpoint also conserves the positive Hamiltonian and `J_tot=C cross P+D cross Pi`, composes with reciprocal carry conditional on `p_*`, and reverses under the signed step. The connection curvature is nonzero for nonzero `gamma`, but `J^2=-I` does not fix its magnitude. The rest-sector clock Hessian is `gamma^2/M`; `gamma=0` restores the exact critical quartic and turns off the impulse. Focused CTest `1/1`, actualization chain `27/27`; a context-blind clutch/compensation law, `gamma`, `p_*`, channel identification, mass, finite-tick `G*`, production, Born, and hiding remain open. No selected type or currency is added. |
| Every nonzero linearized positive connection detunes the critical clock, while the imposed signed-self-pair connection preserves the exact critical quartic at rest but produces no net symmetric-cycle transport | [THEOREM — POSITIVE LINEARIZED-CONNECTION CLOCK-HESSIAN OBSTRUCTION] + [THEOREM — EXACT REST-SECTOR SIGNED-SELF-PAIR QUARTIC GEARBOX] + [BOUNDARY — C1/NOT-C2 AND GENERIC MOVING QUADRATIC TERM] + [BOUNDARY — ZERO SYMMETRIC-CYCLE NET TRANSPORT] | FTD-0902 remains execution-invalid at `80/81`; the one-substitution FTD-0903 repair passes inherited `81/81`. For `B=DA(0)`, the rest clock Hessian is the positive Gram matrix `B^T M^-1 B`. The imposed existing self-pair `U(D)=|D|D` has `DU(0)=0`; at `P=0`, its positive connection energy gives `Lambda=lambda+gamma^2/(2M)`, exact critical-quartic energy, `Delta K=-gamma Delta U`, reciprocal carry, reversal, and the continuum `G*` period factor. The law is `C1`/not-`C2`; generic moving sectors gain a quadratic ray term and symmetric polarized rest cycles have zero net common drift. Focused CTest `1/1`, actualization/EFT chain `28/28`; reversible rectification or separate-clock control, `gamma`, `p_*`, channel identification, mass, finite-tick `G*`, production, Born, and hiding remain open. No selected type or currency is added. |
| A nonzero even polar rectifier cannot be inversion-equivariant from the clock vector alone, while a retained polar axis and time-odd chirality supply an exact imposed rest-sector rectifier with inverse-G* gear ratios | [THEOREM — INVERSION-EQUIVARIANT EVEN-POLAR RECTIFIER NO-GO] + [THEOREM — EXACT CONDITIONAL ORIENTED REST-SECTOR RECTIFIER] + [THEOREM — EXACT G* INVERSE GEAR RATIOS] + [BOUNDARY — NATIVE AXIS/CHIRALITY FORMATION OPEN] | FTD-0904 first immutable certificate passes `74/74` without repair. The imposed law `A(q,e,chi)=chi gamma q^2 e` preserves `H_rest=pi^2/(2m)+Lambda q^4`, gives exact directed endpoint displacement, and yields cycle displacement proportional to `1/G*` and mean speed per squared amplitude proportional to `1/(G*)^2`. The no-go `F(-D)=F(D)=-F(D)` proves that a nonzero even polar `F(D)` cannot retain inversion covariance without auxiliary inversion-odd data. Branch-paired time reversal flips `chi`; fixed-`chi` momentum reversal fails. Focused CTest `1/1`, actualization/EFT chain `29/29`; native `e`/`chi` formation and maintenance, `gamma`, `p_*`, controller cost, moving-clock closure, mass, finite-tick `G*`, production, Born, and hiding remain open. No selected type or currency is added. |
| A neutral ternary dipole and bilateral flux/wave-velocity wedge represent the missing polar and time-odd signs using existing native types, but nonzero wedge memory cannot be the same pure radial G* clock mode | [THEOREM — NATIVE-TYPE POLAR AXIS REPRESENTABILITY] + [THEOREM — NATIVE-TYPE TIME-ODD PHASE-WEDGE REPRESENTABILITY] + [THEOREM — CONDITIONAL CENTRAL QUARTIC ORIENTATION MEMORY] + [BOUNDARY — SEPARATE G* CLOCK/CHIRALITY-MEMORY MODES] + [OPEN — PRODUCTION FORMATION/MAINTENANCE/ERASURE] | FTD-0905 parent and FTD-0906 repair remain execution-invalid at `74/75`; the one-marker FTD-0907 repair passes the complete inherited certificate `75/75`. Neutrality makes `d=sum s_x(x-r)` origin independent; a minimum distinct `+/-` pair supplies `e=d/|d|`. The wedge `ell=q_+p_- - q_-p_+` of endpoint projections is a spatial scalar and time odd, while symmetric squares/Gram data retain only squared magnitudes. The FTD-0840 one-step swept area is time-reversal even under full canonical reversal and is not stored time-odd memory. Under the imposed central quartic law, `ell` is conserved and has a strict bounded radial minimum, but its inverse-square centrifugal term excludes the exact pure radial G* law in that same mode. Focused CTest `1/1`, actualization/EFT chain `30/30`; production formation/persistence, work/erasure, memory-law provenance, rectifier coupling, gamma, scale, mass, cadence, Born, and hiding remain open. No selected type or currency is added. |
| An actual body axis turns the existing transverse L/R-relative vector into a real complex plane; the minimum nonnegative zero-plus-ring radial action carries a conserved recursive charge, but exact finite-tick energy and universal covariant charge creation remain open | [THEOREM — EXISTING-FIELD BODY-AXIS COMPLEX STRUCTURE] + [THEOREM — DEGREE-MINIMUM DEGENERATE RADIAL SEXTIC] + [THEOREM — FINITE-REGION CONSTRAINED RECURSIVE CHARGE] + [THEOREM — CONTINUOUS LOCAL ENERGY CURRENT] + [CLOSED NEGATIVE — SPLIT TICK EXACT ENERGY] + [BOUNDARY — TWO-FRAME/PSEUDOSCALAR SOURCE] + [SELECTION — NONLINEAR ACTION/DEGENERACY] | FTD-0946 parent remains invalid `70/79`; FTD-0947 wrapper remains invalid before inherited execution; FTD-0948 passes inherited `79/79`, repair `8/8`, wrapper `6/6`. `J_e^2=-Pi_e`; `V=beta r^2(r^2-A^2)^2` is the degree minimum in the registered class; `Q_e=sum e dot(q cross p)` is exact and has a fixed-charge finite-region minimizing relative orbit. The symmetric local split tick preserves charge, inverse, and symplecticity but not exact energy. One axis cannot supply a universal C4-equivariant transverse ray, and improper covariance requires a pseudoscalar. `i` supplies the skew rotation, while nonzero `gamma` is independent symmetric damping and gives `Qdot=-gamma Q`. Uncontained localization, native source, exact finite-tick energy, mobility, production, `G*`, Born, and hiding remain open. No new field type or production change. |
| The selected C18-sextic relative field admits an everywhere-positive exponentially tailed recursive solution in a coarse sufficient regime, but compact vacuum data cannot form that exact noncompact body in finitely many local ticks | [THEOREM — CONDITIONAL UNCONTAINED EXPONENTIAL-TAIL EXISTENCE] + [THEOREM — STRICT POSITIVITY/NONCOMPACT SUPPORT] + [THEOREM — FINITE-LOCAL-TICK EXACT-FORMATION OBSTRUCTION] + [IMPOSED — `beta A_0^4>=10000`, CORE/FREQUENCY] + [OPEN — SHARP REGIME/POSITIVE RESERVOIR/STABILITY/EXACT PHYSICAL TICK/PRODUCTION] | FTD-0949 first-run `71/71`. In the weight `4^|x|_1`, C18 has norm at most `16/3`; the `a^2=6/5`, `omega^2=26 Lambda/25` anti-continuum core continues by a contraction of constant `2489/9000` into a ball of radius `10^-3`. A graph maximum principle gives `phi_x>0` everywhere and the weighted ball gives exponential tail and finite energy/charge. Every finite vacuum-preserving dependency hull from compact data is compact, so exact finite-time formation is impossible; the finite-radius tail is at most `10^-6 4^-(R+1)`. FTD-0950/0951 subsequently constructs causal work-booked finite-radius approximation at reference level. The threshold is sufficient and selected, not physical normalization. Positive reservoir dynamics, port recycling, stability, native source, exact physical finite-tick energy, mobility/collision/erasure, production, `G*`, Born, and hiding remain open. No new field type or production change. |
| The FTD-0949 tailed body admits a target-blind causal finite-radius reference relaxation whose residual, field-energy work, and axial-charge change are exactly retained and summably bounded | [THEOREM — CAUSAL GEOMETRIC RELAXATION] + [THEOREM — FINITE SUPPORT/RESTRICTION CONSISTENCY] + [THEOREM — EXACT REVERSIBLE MISMATCH PORT] + [THEOREM — FINITE WORK/CHARGE VARIATION] + [SELECTION — RESIDUAL CONTROLLER/CORE/PORTS/SIGNED LEDGERS] + [OPEN — POSITIVE RESERVOIR/RECYCLING/NATIVE SOURCE/EXACT PHYSICAL TICK/STABILITY/PRODUCTION] | FTD-0950/0951 repaired inherited `80/80` plus `9/9`. The local Picard controller gives error `(11/13022)(2489/9000)^n`; each approximant has compact depth-`n` C18 support. The residual `L(u_n-u_(n+1))` is the exact outgoing reversible-port record. Local energy and charge ledgers conserve pointwise and have finite total absolute variation. This is computation and accounting, not autonomous positive-energy formation: signed ledgers are not phase-complete reservoirs and fresh ports are not free. No new production type or engine change. |
| The nonlinear tailed-body branch admits target-blind finite-grounded relaxation through a positive complete canonical Routh port, but finite positive action capacity alone does not provide a canonical physical charge reservoir | [THEOREM — STRONG CONVEXITY/FINITE-RADIUS ERROR] + [THEOREM — EIGHT-COLOR TARGET-BLIND CONVERGENCE] + [THEOREM — POSITIVE CANONICAL NONLINEAR ROUTH PORT] + [THEOREM — FINITE-HORIZON CAPACITY] + [CLOSED NEGATIVE — PHASE-BLIND STATE-DEPENDENT ACTION DRAIN] + [OPEN — COMMON PHASE-REACTING CHARGE-TRANSFER HAMILTONIAN] | FTD-0952/0953 repaired inherited `87/87` plus `9/9`, Outcome B. The Routh Hessian is at least `2172 Lambda/3125`; a nonlinear energy coordinate turns local minimization into an orthogonal symplectic quarter-turn and stores the complete mismatch in a positive port. The identity `A_0^2 S=H_rot-sigma omega Q` proves why this is not separate physical energy/charge closure. A co-rotating action reserve has sufficient finite capacity, but a phase-blind state-dependent debit adds `-sigma d(Delta Q) wedge dtheta` and is nonsymplectic. FTD-0954 subsequently supplies the required phase reaction on a local co-rotating stroboscopic section. No new production type or engine change. |
| A local co-rotating phase/action reservoir supplies the exact canonical completion of the nonlinear Routh port, but one globally periodic separable generator cannot deliver the same nonzero debit independently of phase | [THEOREM — POLAR BODY PHASE/CHARGE CHART] + [THEOREM — PHASE-REACTING CANONICAL SHEAR] + [THEOREM — PHASE-LOCKED ENDPOINT ENERGY/CHARGE/INVERSE] + [THEOREM — FINITE FAIL-CLOSED RESERVE] + [CLOSED NEGATIVE — GLOBAL PERIODIC PHASE-INDEPENDENT SEPARABLE SHEAR] + [CONDITIONAL — PREPARED COMPLETE-MODE EXCHANGE] | FTD-0954 first-run `83/83`, Outcome B. `H_tr=(sigma*vartheta-theta)D` transfers `Delta L=D`, `Delta I=-sigma D` and adds the reciprocal radial kick `(theta-sigma*vartheta) partial_rho D`. At co-rotation the kick vanishes; with the FTD-0953 Routh port, total axial charge and endpoint `H_rot+E_port+omega I` are exact. `I>|D|` is sufficient and insufficient reserve rejects the gate. Global periodicity forces the integral of a phase derivative to zero, so a fixed nonzero phase-independent separable shear is impossible. Positive complete-mode exchange works only with a prepared target. Native reservoir formation, synchronization, recycling, and production remain open. |
| One globally periodic two-window controller autonomously compiles the phase-reacting charge pulse and positive Routh quarter-turn with exact controller-action return, but the resulting off-lock phase dynamics are neutral rather than restoring | [THEOREM — GLOBAL PERIODIC TWO-WINDOW COMPILER] + [THEOREM — READY-SECTION ENERGY/CHARGE/MISMATCH/INVERSE CLOSURE] + [THEOREM — FINITE FAIL-CLOSED CONTROLLER RESERVE] + [THEOREM — UNIT RELATIVE-PHASE MULTIPLIER] + [CLOSED NEGATIVE — PHASE-POTENTIAL-ONLY RESTORATION] + [OPEN — RELATIVE-ACTION CURVATURE/POSITIVE ERROR EXPORT] | FTD-0955 first-run `105/105`, Outcome B. `H_C=Omega J+(2Omega/pi)w_0G_Q+Omega w_1G_R` uses disjoint periodic `C1` windows with integral `pi/2`, so the charge pulse precedes the Routh quarter-turn and controller action returns at both boundaries. `J_0>max(2|D|/pi,N_R)` is sufficient reserve. In canonical coordinates `delta=theta-sigma vartheta`, `P_delta=L`, `P_chi=sigma L+I`, the matched energy is `omega P_chi`; hence relative-action curvature vanishes, `delta` is retained, and a phase potential alone cannot move it. Conservative restoration minimally needs a `P_delta`-curvature term; asymptotic attraction additionally needs an explicit positive export channel. Native formation, routing, stability, production, `G*`, Born, and hiding remain open. |
| A selected same-field relative-action energy converts neutral phase retention into a positive Lyapunov-stable recursive phase/action pair and preserves the global compiler ledger on zero-phase crossings, but it is amplitude-detuned and not attracting | [SELECTION — `H_sync` FUNCTIONAL AND POSITIVE `M_delta,K_delta`] + [THEOREM — GLOBAL PERIODICITY/POSITIVITY/RECIPROCAL REACTION] + [THEOREM — EXACT CHARGE/MATCHED-ENERGY CONSERVATION] + [THEOREM — LYAPUNOV LOCK/ELLIPTIC FLOQUET PAIR] + [THEOREM — CROSSING-SECTION COMPILER ENERGY/CHARGE/INVERSE] + [BOUNDARY — NON-ISOCHRONY/AUTONOMOUS ENGAGEMENT/NONLINEAR STABILITY/POSITIVE EXPORT] | FTD-0956/0957 repaired inherited `111/111` plus `12/12`, Outcome B. `Pi=L-sigma omega rho^2` and `delta=theta-sigma vartheta` obey `delta_dot=Pi/M_delta`, `Pi_dot=-K_delta sin delta`; the full flow includes `p_rho_dot=2 sigma omega rho Pi/M_delta`. `Q=L+sigma I` and `H_sync` are exact. Below the separatrix the lock is Lyapunov stable; the exact sampled multipliers are `exp(+/-i sqrt(K_delta/M_delta)T)`. At `delta=0`, the FTD-0955 endpoint preserves `Pi` and `H_sync`, extending its physical energy/charge ledger exactly. The nonlinear elliptic-integral period depends on energy and diverges at the separatrix. The law/scales are selected; isochrony, autonomous engagement, nonlinear repeated-cycle stability, attraction, native formation, `G*`, Born, and production remain open. |
| Exact isochrony is incompatible with a complete smooth periodic natural-well basin in the registered analytic class; a harmonic phase lift restores constant cadence only by retaining signed winding history, and one-shot crossing eligibility does not itself align an independent controller | [CLOSED NEGATIVE — GLOBAL PERIODIC NATURAL-WELL ISOCHRONY] + [SELECTION — HARMONIC LIFT/INTEGER WINDING] + [THEOREM — ORIENTED SYMPLECTIC ATLAS TRANSITION] + [THEOREM — SIGNED CURRENT VS SYMMETRIC-SQUARE LOSS] + [THEOREM — FINITE WINDING CAPACITY] + [THEOREM — NO-RESET COMMENSURABILITY] + [OPEN — ACTIVE GEARBOX/NATIVE HISTORY/G*] | FTD-0958/0959 repaired inherited `93/93` plus `12/12`, Outcome B. Barrier periods diverge throughout the registered finite-order analytic periodic class. `H_lift=Pi^2/(2M)+K(delta+2pi w)^2/2` is exactly isochronous, but fixed `w` is not single-valued on the phase circle. The transition `(delta,w,Pi)->(delta-2pi s,w+s,Pi)` preserves the lift and one-form; `s=sign(Pi)` supplies direction while `s^2` supplies only eligibility. A finite ternary rail stores `|w|<=W` only if `3^n>=2W+1`. An independent controller returns to gate zero at same-orientation/all crossings only for integral/even-integral `Omega/kappa` after phase alignment. Active gearbox, native winding carrier, `G*`, production, Born, and hiding remain open. |
| Existing opposite oriented history channels conditionally realize every finite lifted-clock crossing word and net winding without a new public memory type, but the construction spatializes history and does not supply compact carry or native loading | [THEOREM, CONDITIONAL — EXISTING EXPANDING RAIL FINITE-WINDING CARRIER] + [THEOREM — WORD/INVERSE/WINDING/LOCALITY/ORIENTATION/ENERGY] + [CLOSED NEGATIVE — FIXED REVERSIBLE FAIL-CLOSED INCREMENT WITHOUT OVERFLOW] + [BOUNDARY — PARITY TRANSPORT IS NOT COMPACT CARRY] + [OPEN — NATIVE ACQUISITION/ACTIVE GEARBOX/G*] | FTD-0960/0961 repaired inherited `60/60` plus `19/19`, Outcome B. Map `s=+/-1` to opposite existing Moore-token channels; causal depth reconstructs the complete word and signed count difference gives `w_N-w_0=sum_t s_t`. Every nonzero crossing costs one token and selected energy `epsilon_*/2`; canceling histories remain physically distinct. Holding `E(W)` after a positive increment collides with the image of `E(W-1)` unless overflow/backpressure is retained. Existing `R(a,b)=(-b,a)` layers are signed coordinate permutations, not nonlinear carry. Native latch-to-port loading, compact carry, routing/recycling, active gearbox, `G*`, Born, hiding, and production remain open. |
| A selected positive complete-square phase connection converts oriented clock traversal into a speed-independent quarter-turn that loads a signed token and reversibly replaces controller phase, while reverse traversal is the exact self-dual inverse | [SELECTION — CONNECTION PROFILE/COMPLETE-SQUARE COUPLING/EXISTING MODE SPECIALIZATION] + [THEOREM — RECIPROCAL MECHANICAL/CANONICAL SPLIT] + [THEOREM — ORIENTED HOLONOMY] + [THEOREM, CONDITIONAL — TOKEN LOAD/PHASE REPLACEMENT] + [THEOREM — REVERSE RECOVERY] + [THEOREM, CONDITIONAL — G* CADENCE SEPARATION] + [OPEN — PRODUCTION/FORMATION/RECYCLING/ONE-WAY EXPORT] | FTD-0962/0963 repaired inherited `115/115` plus `25/25`, Outcome B. `K=Pi+calA G` obeys the bare clock equation while `Pi=K-calA G` retains reaction. `Delta alpha=int calA ddelta` is speed/load independent. Forward `pi/2` transfers battery to signed output and aligned reserve to controller while old controller enters the return mode; reverse restores all. Conditional quartic action-angle use gives quadrant time `T/4` with `G*` in cadence, not connection selection. Native production, formation, recycling, repeated stability, one-way export, Born, and hiding remain open. |
| Existing dual production fields conditionally have enough scalar canonical-pair capacity for the selected phase connection, but a site-local linear cubic-covariant scalar chart and the unchanged production connection law are absent | [THEOREM, CONDITIONAL — FIXED-FRAME SIX-PAIR CAPACITY/FIVE-PAIR PACKING] + [THEOREM — SIGNED-CUBIC INVARIANT-SCALAR OBSTRUCTION] + [CLOSED NEGATIVE — UNCHANGED PRODUCTION CONNECTION] + [OPEN — REGIONAL FRAME/NATIVE OR PRICED ACTION/REPEATED STABILITY/POSITIVE EXPORT] | FTD-0964/0965 repaired inherited `72/72` plus `13/13`, Outcome B. A fixed-frame rank-ten projection selects five whole canonical pairs from the six dual field component pairs and leaves one complete pair unused. The exact 48-element `O_h` average on `V+V` is zero, so the frame is not native at one site. Diagnostic `phase`/`tau` are not canonical; the weak `L/R` swap has the wrong algebra; no complete-square coupling, connection energy, reserve, or inverse exists in the frozen tick. Capacity does not license production. Regional frame/action provenance, repeated stability, export, `G*`, Born, and hiding remain open. |
| A regular finite neutral actual ternary body conditionally supplies the regional spatial frame and pseudoscalar absent from the site-local linear chart, but not its moving-frame dynamics or temporal orientation | [THEOREM — MINIMUM SUPPORT FOUR] + [THEOREM — FULL SIGNED-CUBIC COVARIANT POLAR TRIAD/TRANSVERSE COMPLEX STRUCTURE] + [THEOREM, CONDITIONAL — FIXED-RECORD SYMPLECTIC CHART] + [OPEN — FORMATION/DEGENERACY/MOVING-FRAME CONNECTION/REACTION/PRODUCTION] | FTD-0966--0969 final chain `75/75`, `19/19`, `20/20`, `22/22`, Outcome B. `kappa=det[d,Cd,C^2d]` and `chi=sign(kappa)` yield a polar triad and `I_F^2=-Pi_e`; an exact one-cube four-site neutral witness has `kappa=-1/256`, while two sites force `kappa=0`. The snapshot transform is symplectic. `chi` is time-even and the frame is singular at `kappa=0`; temporal crossing current and moving-frame connection/reaction/work remain separate debts. No production law changed. |
| The regional body's smooth moving chart has a unique canonical angular-momentum connection, but the induced Maurer--Cartan connection is passive and flat | [THEOREM — UNIQUE COTANGENT MOMENTUM SHIFT/COMPLETE SQUARE] + [THEOREM — ZERO REGULAR-CHART CURVATURE/IDENTITY CLOSED HOLONOMY] + [OPEN — CURVED ACTIVE CONNECTION OR RETAINED SINGULAR TRANSITION] | FTD-0970 `45/45`, Outcome B. `Pi_A=P_A+omega_A dot sum(y cross z)` is forced by the canonical one-form and closes the reciprocal momentum bookkeeping. Yet `A=F^T dF` has `dA+A wedge A=0`; passive endpoint transport is `F_1^T F_0`, and a closed regular loop is identity. Finite frame jumps preserve lab fields. Active token loading therefore needs a separately declared physical coupling or retained degeneracy/chart-transition port. No production law changed. |
| Retaining the two directed passages through the neutral body's `kappa=0` degeneracy makes one ternary self-delimiting latch into an exact oriented C4 carrier whose real mode realizes `J^2=-I` | [THEOREM — THREE-SYMBOL MINIMUM WITHIN SELF-DELIMITING ONE-CELL INTERFACE] + [THEOREM, CONDITIONAL — REVERSIBLE C4/TIME-REVERSAL COVARIANCE] + [THEOREM — TRANSITION i MODE, NOT FOURTH ONTIC STATE] + [OPEN — FORMATION/ENERGY/ACTIVE COUPLING/G*] | FTD-0971/0972 repaired inherited `62/62` plus `14/14`, Outcome B. The static branch reflection squares to `+I`; the retained composite cycle `R+ -> D+ -> R- -> D-` supplies forward `J`, reverse `-J`, and identical square `-I`. This is exact logical recurrence and norm preservation only. Physical body formation, energy equality, active field coupling, cadence, and production remain open. |
| The retained C4 carrier exposes four discrete field-holonomy classes but does not uniquely choose its field representation; one complete controller pair supports a selected minimum positive faithful suspension | [THEOREM — Z4 COCYCLE/GAUGE CLASSIFICATION AND UNDERDETERMINATION] + [SELECTION — H=(A-I)^2/(2M)+nu I] + [THEOREM, CONDITIONAL — EXACT CO-ROTATION/REACTION/ENERGY/INVERSE] + [OPEN — IDENTITY/FORMATION/SWITCHING/G*/PRODUCTION] | FTD-0973/0974 repaired inherited `64/64` plus `12/12`, Outcome B. Translation-natural cocycles are constant direct products, including both trivial and faithful actions; carrier structure alone does not select one. The selected suspension needs one continuous controller pair, gives `Delta alpha=Delta theta` in the interaction picture, conserves `H` and `I`, and dresses controller momentum as `A=K+I`. Physical realization and switching remain open. |
| The selected minimum suspension fits existing production pairs only as an alternative, specialization, or shared-clock layout; independent seven-pair coexistence and summing two kinetic squares on one clock are excluded | [THEOREM — WHOLE-PAIR CAPACITY/OBSTRUCTION] + [CLOSED NEGATIVE — INDEPENDENT SUMMED CLOCK SQUARES] + [OPEN — ONE MERGED COMPLETE-SQUARE LAW] | FTD-0975 `46/46`, Outcome B. Six production pairs can hold the five-pair gearbox or the two-pair suspension, and can hold both only by sharing the clock and using the sixth pair. Two separate clock squares alter the bare clock rate. `H=(Pi+X-I)^2/(2M)+H_rest` is the lower-price candidate, but its `-XI/M` term is new selected physics rather than a capacity theorem. No production law changed. |
| A specified clock-dependent fiber action uniquely forces one covariant momentum and one clock kinetic square, but C4 endpoint data does not force its profile, integer lift, common diagonal form, or active physical gluing | [THEOREM, CONDITIONAL — COTANGENT ONE-SQUARE LAW/REACTION CANCELLATION] + [THEOREM — C4 CONNECTION UNDERDETERMINATION] + [CORRECTION — Pi+calA G-qI IS THE EXISTING COMPOSITION] + [OPEN — PHYSICAL MAPPING-TORUS REALIZATION] | FTD-0976/0977 repaired inherited `52/52` plus `16/16`, Outcome B. The general law is `K=Pi+r_GA_GG-q r_IA_II`. Existing reference mechanisms use `A_G=calA,A_I=1`; a common profile is selected. Cross terms are mandatory once a lift is chosen. The regular connection is locally pure gauge, while changing `q` requires a switching transaction. No production law changed. |
| The production left/right exchange is a valid symplectic half-turn but cannot supply the oriented quarter-turn root or a clock-indexed physical gluing | [THEOREM — EXACT ORDER-TWO PRODUCTION EXCHANGE] + [BOUNDARY — -I=J^2=(-J)^2 LOSES CLOCKWISE/COUNTERCLOCKWISE] + [CLOSED NEGATIVE — CURRENT DE BROGLIE-CLOCK/WEAK ROUTE AS C4 WITNESS] + [OPEN — EXPLICIT CLOCK CLUTCH/REACTION/WORK/HISTORY] | FTD-0978 `51/51`, Outcome B. The CPU/CUDA swap acts on both dual flux and wave velocity and becomes `diag(+1,-1,+1,-1)` in common/relative variables. The imposed symmetric clock commutes with it, but weak firing reads no local phase or proper time. Device state has no phase buffer; the energy ledger has no clock connection reaction, switching work, oriented latch, or retained inverse. This closes the named route only. No production law changed. |
| The oriented square root of the production half-turn is exact and minimum on the existing relative canonical pair, but exact scalar C18 energy compatibility is nonlocal | [THEOREM — UNIQUE +/-J ROOTS IN FIXED-COMMON ORTHOGONAL SYMPLECTIC CLASS] + [THEOREM — J_K USES K^+/-1/2] + [THEOREM — NO SCALAR FINITE-RANGE MASSIVE C18 FACTOR] + [BOUNDARY — LOCAL ROOT REQUIRES WORK/HISTORY OR ADDED FACTOR HARDWARE] + [SELECTION — TERNARY CLOCK-SEAM HANDSHAKE] | FTD-0979/0980 repaired inherited `58/58` plus `16/16`, Outcome B. `R_sigma^2=S`, time reversal sends `sigma` to `-sigma`, and reset transfers the sign into a receiving ternary record. A local `kappa` root has exact defect `[D^T(kappa^2I-K)D+P^T(K/kappa^2-I)P]/2`, zero for all states only at `K=kappa^2I`. No production law changed. |
| The exact C18 stiffness has a finite-range multicomponent factor, but the instantaneous root still needs a nonlocal inverse; one canonical work pair is the minimum local phase-complete energy lift | [THEOREM — K=B*B AND BLOCK-DIRAC FACTOR] + [THEOREM — FACTOR INVERSE NONLOCAL/SINGULAR] + [THEOREM — EXTENDED SYMPLECTIC WORK-PORT LIFT] + [THEOREM — FOUR-STROKE FIELD/RESERVE RECOVERY] + [BOUNDARY — FINITE COMPLIANCE SHELL] + [SELECTION — SEAM FAMILY] | FTD-0981/0982 final repaired inherited `79/79`, Outcome B. The work law is `I_R'=I_R+H-H'`; its phase reaction makes the event canonical, and the debit telescopes to zero after four strokes. One pair meets FTD-0928's lower bound per independently gated batch. Native identity, formation, replenishment, concurrency, production, and hiding remain open; no adoption or production law changed. |
| A single global clock/action pair can book aggregate work exactly but cannot be a same-tick Moore-local energy bus; separated concurrent work requires local phase-complete ownership | [THEOREM — AGGREGATE SYMPLECTIC SUFFICIENCY] + [THEOREM — GLOBAL-RESERVE LOCALITY OBSTRUCTION] + [THEOREM — SHARED-PHASE PRESYMPLECTIC BOUNDARY] + [THEOREM — LOCAL PRODUCT WORK LAW] + [BOUNDARY — CENTRAL ADMISSION DELAY/DOUBLE SPEND] + [CONDITIONAL CAPACITY — UNUSED SIXTH PAIR] | FTD-0983--0985 final repaired physical `62/62`, integrity `14/14` and `18/18`, Outcome B. Global time may synchronize eligibility, but usable reserve must be prepositioned as complete local `(theta_a,I_a)` pairs or an equivalent canonical field. Overlapping events form one joint batch or separate sublayers. Production has selected-frame capacity only; native formation/synchronization/replenishment or explicit work-field adoption remains open. |
| The existing body-frame longitudinal common mode supplies the conditional local work pair, but unchanged C18 production neither isolates nor owns it as an autonomous reserve | [THEOREM, CONDITIONAL — COVARIANT COMMON-MODE PAIR] + [THEOREM — POSITIVE ACTION-ANGLE / AMPLITUDE-AUDIT HALF-SEAM] + [THEOREM — NO NONZERO COMPACT CLOSED C18 MODE] + [CLOSED NEGATIVE — UNCHANGED AUTONOMOUS PORT] + [SELECTION — TERNARY PROJECTOR CLUTCH / SWITCHING WORK] | FTD-0986/0987 repaired inherited `96/96` plus integrity `13/13`, Outcome B. The existing pair has observable-audit contribution `2I`, so `I'=I+(H-H')/2` preserves diagnostic `H+2I`; FTD-0988/0989 corrects the physical wave-mode law to `H_u=omega I_u`. No seventh pair is forced. No production law changed. |
| The C18 incidence channels provide a Moore-local positive boundary clutch and exact current; a physical regional oscillator has frequency-normalized action | [THEOREM — K_ell=B^T diag(1-ell_b^2)B] + [THEOREM — REGIONAL DIRECT SUM / ANTISYMMETRIC CURRENT] + [THEOREM — SWITCHING WORK / ZERO-STRAIN SEAM] + [THEOREM — FIXED-GATE SHADOW ENERGY/INVERSE] + [CORRECTION — H_u=omega I_u] + [STATIC MEMORY PRICE RETIRED BY FTD-0990] | FTD-0988/0989 repaired inherited `73/73` plus integrity `11/11`, Outcome B. Boundary cuts are local and positive; open bonds carry exact charging current; zero-strain switches cost no work or impulse but must retain temporal orientation. `H+2I` remains amplitude-audit only. FTD-0990 derives the static gate from endpoint occupancy; dynamic aperture/history, formation, preparation, production, and hiding remain open. |
| Ternary occupancy itself is the unique static matter membrane; conditionally, dual fields split into an isolated common body clock and an open relative interaction channel | [THEOREM — m=s^2 / g_xy=1-(m_x-m_y)^2] + [THEOREM — POSITIVE MOORE-LOCAL BODY ISOLATION] + [THEOREM, CONDITIONAL — UNIQUE L/R SELF-DUAL BLOCK] + [THEOREM, CONDITIONAL — CONNECTED UNIFORM CLOCK H_u=omega0 I_u] + [TYPE PRICE RETIRED — STATIC BOND MEMORY] + [SELECTION — OCCUPANCY-CONTROLLED STIFFNESS] | FTD-0990 first locked `81/81`, Outcome B. The same actual occupancy mask defines the membrane and supports the imposed onsite clock. Common current is cut; relative C18 current remains open. FTD-0991/0992 closes conditional work/aperture form. The coupling law, physical controller/reserve, first phase, `omega0`, `G*`, production, and hiding remain open. |
| Occupancy changes have an exact local cut-set work ledger, and temporary common-channel boundary opening requires a fail-closed phase-complete oriented aperture | [THEOREM, CONDITIONAL — CUT-SET FORMATION/GROWTH/REVERSAL WORK] + [THEOREM, CONDITIONAL — I'=I-W/Omega] + [THEOREM, CONDITIONAL — TWO-TERNARY-SLOT MINIMUM ACTIVE APERTURE] + [CLOSED NEGATIVE — PRODUCTION ACTUATOR] + [CORRECTED BY FTD-0994 — CARTESIAN ZERO-ACTION SEED] | FTD-0991/0992 repaired inherited `83/83` plus integrity `13/13`, Outcome B. Uniform-void cluster formation releases its common boundary strain; growth costs `E_join-E_cut`. A prepared action books work exactly. `gamma=g+(1-g)r^2` is blank-closed and opens only with a retained sign. FTD-0994 supplies a conditional first Cartesian stroke; the selected coupling, causal growth, positive reserve, reciprocal production, `omega0`, and `G*` remain open. |
| Positive local membrane work plus retained temporal orientation can seed an existing zero Cartesian clock pair, but exact uniform preparation of an extended body must propagate causally and cannot be achieved by free phase copying | [THEOREM, CONDITIONAL — SYMPLECTIC CARTESIAN FIRST-STROKE SHEAR] + [THEOREM, CONDITIONAL — I=U/Omega / theta=-sigma pi/2] + [THEOREM — DENSE UNIFORM PROJECTOR LOCALITY OBSTRUCTION] + [THEOREM — CANONICAL NO-CLONING / PHASE-MATCHED JOIN] + [CLOSED NEGATIVE — PRODUCTION CAUSAL GROWTH] | FTD-0993/0994 repaired inherited `96/96` plus `11/11`, Outcome B. The zero-action obstruction is chart-only. A bounded local seed is exact and target blind; an extended uniform clock needs at least graph-radius time. A joined site must arrive phase matched with its energy share or retain a relative mismatch. FTD-0995/0996 closes exact compliance-surface growth; coupling, physical `U`, autonomous matching/mismatch export, receiver identity, `Omega`, `G*`, and production remain open. |
| A growing body extends its exact local clock state iff released formation work matches donor crossing energy; the inherited critical-quartic state then carries the same G* calendar without reading it | [THEOREM, CONDITIONAL — C_xy=2mU_y-p_x^2 NECESSARY/SUFFICIENT] + [THEOREM, CONDITIONAL — EXACT ENERGY/INVERSE/UNIFORM-MANIFOLD GROWTH] + [THEOREM — MOORE-CAUSAL INDEPENDENT FRONTIER] + [THEOREM, CONDITIONAL — QUARTIC G* CADENCE INHERITANCE/MISMATCH DETUNING] + [OPEN — AUTONOMOUS MATCHING/ROBUSTNESS/PRODUCTION] | FTD-0995/0996 repaired inherited `88/88` plus `17/17`, Outcome B. Exact compliance maps a blank receiver to the donor Cartesian crossing state and closes membrane-to-clock energy. Uniform coherence then propagates locally. A work ratio `r` detunes the quartic period by `r^-1/4`; the harmonic control is isochronous. FTD-0997 supplies the existing relative-pair mismatch port and proves the separable membrane does not force compliance. |
| The existing relative field pair can act as a catalytic phase-complete growth port, but the static quiescent membrane cannot refill it or force the compliance surface | [THEOREM — EXISTING PAIR CAPACITY] + [THEOREM, CONDITIONAL — SYMPLECTIC SWAP/REFILL ENERGY/INVERSE] + [THEOREM — PORT RESTORED IFF U=e / MISMATCH U-e RETAINED] + [THEOREM — BLOCK-DIAGONAL NON-FORCING] + [CLOSED NEGATIVE — ZERO-STRAIN MEMBRANE REFILL] + [OPEN — POWER/OWNERSHIP/CONTROL/PRODUCTION] | FTD-0997 first locked `99/99`, Outcome B, no repair. The transaction `(C,R,Y)=(z,z,0)->(z,z_U,z)` uses no new continuous pair. Compliance makes the relative port catalytic; mismatch remains in it. Common/relative separability leaves preparation independent, and quiescent changed bonds carry zero work. Repeated growth requires explicit local energy inflow or reserve. |
| Coherent clock growth obeys a unique cumulative resource law: restored catalysts transmit organization but every new maintained-energy share must be supplied and atomically admitted inside its causal ownership domain | [THEOREM — B'=B+Phi+U(F)-D] + [THEOREM — CUMULATIVE FINITE-RESERVE / AVERAGE-POWER BOUNDS] + [THEOREM — ATOMIC SHARED-RESERVE BACKPRESSURE] + [THEOREM — JOINT-WORK / MOORE-DELAY] + [THEOREM, CONDITIONAL — HISTORY-COMPLETE INVERSE] + [OPEN — NATIVE PHASE-COMPLETE RESERVE CURRENT] | FTD-0998/0999 repaired inherited `91/91` plus integrity `11/11`, Outcome B. `sum D=B_0-B_T+sum(Phi+U)`; closed quiescent reserve funds at most `floor(B_0/e)` equal-energy additions and long-run growth needs `P_bar>=e v_g`. Only independent frontiers have additive one-site work. A scalar account is not canonical hardware, and production has no reserve owner, joint gate, replenishment, or inverse. |

### §2.2 · G* layer resumes

The §2.1 block ends above (last row: FTD-0999's cumulative clock-growth reserve law). The
following rows are pre-existing G*-identity content unrelated to the native-time-carrier
programme.

| Claim                                                                | Tag                              | Source                                                                                   |
|----------------------------------------------------------------------|----------------------------------|-------------------------------------------------------------------------------------------|
| `G* > 2 ⇒ ∃ unique χ_G > 0 with G* = 2 cosh(χ_G)`                    | [DERIVED — parametric reformulation] | Inline: `cosh` is strictly monotone on `[0, ∞)` with image `[1, ∞)`; `G* > 2` gives unique `χ_G > 0`. Notational only; no new physics. |
| `q* := e^{−χ_G} = (G* − √(G*² − 4))/2`                               | [DERIVED — parametric reformulation] | Inline: `2 cosh χ_G = G*` ⇒ `e^{χ_G} + e^{−χ_G} = G*` ⇒ `q*` is the smaller root of `z² − G*z + 1 = 0`. Same root as Möbius reduction §3. |
| `W_BCC = G*²/(2π)` (Watson identity)                                 | [THEOREM, conditional on Watson 1939] | SPEC_ALGEBRAIC_SPINE.md §5 Theorem 5; OT-2.1; verified at 100-digit precision in PARI |
| Distinction `W_BCC ≠ G*` (one-loop / tadpole prefactor, not bridge constant) | [THEOREM]                | ../08_structural/DERIV_BCC_MULTIPLICATIVE_STRUCTURE.md                                   |

---

# §3 · Finite trace mechanics

**Primitive recurrence (FQCR Model III).** `u_{m+1} + u_{m−1} = s_m u_m`. Transfer matrix
`T_m = [[s_m, −1], [1, 0]]`, `det T_m = 1`. Casimir invariant
`I_m := u_m² + u_{m−1}² − s u_m u_{m−1}`. Projective Möbius reduction `z_{m+1} = s_m − 1/z_m`.
For constant `s`, fixed-point quadratic `z² − sz + 1 = 0` with reciprocal roots `z_+ z_− = 1`.

| Claim                                                                | Tag                              | Source                                                                                  |
|----------------------------------------------------------------------|----------------------------------|-----------------------------------------------------------------------------------------|
| Symmetric recurrence `u_{m+1} + u_{m−1} = s_m u_m`                   | [THEOREM]                        | SPEC_FQCR.md §1 Def 3 (after reparameterisation)                                         |
| `det T_m = 1`                                                        | [THEOREM]                        | Direct: `det T_m = s_m·0 − (−1)·1 = 1`                                                   |
| Casimir invariant `I_m = u_m² + u_{m−1}² − s u_m u_{m−1}` conserved  | [THEOREM]                        | SPEC_FQCR.md §2 Prop 3                                                                   |
| Projective map `z_{m+1} = s_m − 1/z_m`                               | [THEOREM]                        | SPEC_FQCR.md §2 Prop 4                                                                   |
| Regime split `\|s\| < 2` / `s = 2` / `s > 2` (oscillatory / null / hyperbolic) | [THEOREM]                | Discriminant of `z² − sz + 1` is `s² − 4`; sign analysis is direct                       |
| `\|s\| < 2` reading as wave/phase regime                             | [SELECTION — physical identification] | Mode of FQCR finite trace; reading is interpretive                                  |
| `s = 2` as null/inertial regime                                      | [SELECTION — physical identification] |                                                                                      |
| `s > 2` as mass-gap / branch-selection regime                        | [SELECTION — physical identification] |                                                                                      |
| `μ² := s − 2` (mass parameter from regime offset)                    | [SELECTION — bridge identification]   | Hardening target: substrate-derive a mass observable from the regime parameter `s`. Distinct from MC-T3.4 (Bridge Functional `M(x_+, x_-)` arithmetic-mean / FTD-0095) — both are mass-from-algebra identifications but use different algebraic input (regime parameter `s` here vs root pair `(x_+, x_-)` for MC-T3.4); no canonical cross-link. |

---

# §4 · Quarter-rotation split geometry

**Quarter rotation.** Under `y ↦ Jy` with `J² = −I`, the Euclidean form `x² + y²` becomes `x² − y²` — Lorentzian signature on a 2-plane, light-cone boundary at `x² − t² = 0 ⇔ x = ±t`. The lemniscatic kernel `cos(2θ) = cos²θ − sin²θ = x² − y²` on the unit circle realises the same algebraic structure radially.

**Honest scoping.** The statement *"the twisted circle literally becomes the Bernoulli lemniscate"* is **REJECTED / TOO STRONG**. The lemniscate `r² = a² cos(2θ)` is one specific *radial* realisation of the split kernel; the algebraic identity `x² + y² ↦ x² − y²` does not by itself produce the lemniscate without additional radial choice.

**G*-hyperbola point.** From the Möbius fixed-point quadratic `z² − sz + 1 = 0` (§3) with `s = G*`, the positive-discriminant point is
`P_G = (G*/2, √(G*² − 4)/2)` (substituting `x = G*/2`, solving for `y`).

| Claim                                                                | Tag                              | Source                                                                                  |
|----------------------------------------------------------------------|----------------------------------|-----------------------------------------------------------------------------------------|
| `x² + y² ↦ x² − y²` under `y ↦ Jy`, `J² = −I`                        | [THEOREM]                        | Direct algebra                                                                           |
| Light-cone boundary `x² − t² = 0 ⇒ x = ±t`                           | [THEOREM]                        | Direct algebra                                                                           |
| Trig identity `cos(2θ) = x² − y²` on the unit circle                  | [THEOREM]                        | External; standard double-angle formula                                                  |
| Bernoulli lemniscate `r² = a² cos(2θ)` as one radial realisation     | [SELECTION — geometric reading]  | One of many radial realisations of the split kernel                                      |
| "Twisted circle becomes lemniscate" as forced consequence            | **[CLOSED NEGATIVE]**            | Algebra does not force this realisation                                                  |
| `P_G = (G*/2, √(G*²−4)/2)` is the positive-discriminant `s=G*` point | [DERIVED — corollary, inline]    | Substitute `x = G*/2` into `z² − G*z + 1 = 0` and solve. Notational only.               |

---

# §5 · FQCR-EM / α⁻¹ branch quadratic

This is the most heavily annotated section because the physical identification of `α⁻¹` is the framework's most-cited (and most-interrogated) claim.

**Modular shape factor (FQCR §3.1, §3.3).** Define
`Ψ_N(t) := ∏_{n=1}^N (1 − Q^{4n})^6 / (1 − Q^{3n})^2`, `Q := e^{−2πt}`.
The exponent quadruple `(k, d; ℓ, m) = (4, 6; 3, 2)` is a structural choice; pre-registered uniqueness scan in `PREREG_FQCR_QUOTIENT_UNIQUENESS_v1.md` (FTD-0143, awaiting execution). Define
`A_N(t) := (1/3) d/dt log Ψ_N(t)` (anomaly pressure),
`λ_N(4it) := (θ_{2,N}(4it) / θ_{3,N}(4it))⁴` (truncated modular lambda),
`R_N(t) := 1 + λ_N(4it) + A_N(t)` (combined renormalisation).

**Branch quadratic (FQCR Model V, Prop 5).**
`x² − 16 (G_N*)² x + 16 (G_N*)³ R_N(t) = 0`
At `R_N(1) = 1` and `N → ∞`, this reduces *exactly* to the spine master quadratic `x² − 16 G*² x + 16 G*³ = 0` (Theorem 2; OT-1.1; FTD-0001). *(Note: prior reference to FTD-0014 here is removed — that LEDGER row, which carried the `x_-  N_c` identification, was retired per v1.4 §5 and removed in commit `ca7eb61`.)*

**Self-dual readout.** `t = 1` is the fixed point of the modular involution `t  1/t`. The base coupling
`α_FTD⁻¹ := lim_{N→∞} α_N⁻¹(1) ≈ 137.035999177`
where `α_N⁻¹(t) = 8(G_N*)² + 4(G_N*)^{3/2} √(4G_N* − R_N(t))` is the dominant root.

| Claim                                                                | Tag                              | Source                                                                                  |
|----------------------------------------------------------------------|----------------------------------|-----------------------------------------------------------------------------------------|
| `Ψ_N(t)` exponent-quadruple form                                     | [SELECTION]                      | SPEC_FQCR.md §3.1 (pending FTD-0143 uniqueness scan)                                    |
| `(4,6;3,2)` interpretation as primitive antisymmetric / projected transverse sectors | [SELECTION]              | SPEC_FQCR.md §3.1                                                                        |
| `A_N = (1/3) d/dt log Ψ_N` once `Ψ_N` is selected                    | [DERIVED]                        | SPEC_FQCR.md §3.3                                                                        |
| `λ_N(4it)` shape term as truncated modular lambda                    | [SELECTION]                      | SPEC_FQCR.md §3.3                                                                        |
| `R_N = 1 + λ_N + A_N` additive combination                           | [SELECTION]                      | SPEC_FQCR.md §3.3 (one of several plausible combinations)                                |
| Coefficient 16 = `\|Aut(E)\|²` for `E: y² = x³ − x`                  | [CONJECTURE] (T4: value-equality holds, structural necessity unproven) | SPEC_ALGEBRAIC_SPINE.md §4 Theorem 4; OT-4.1 (T4 — true at value level; structural identification conjectural) |
| `t = 1` fixed under modular involution                                | [THEOREM]                        | Direct: `1/t = t ⇔ t² = 1 ⇔ t = ±1`                                                     |
| Base coupling evaluated at `t = 1`                                    | [SELECTION — physical principle] | SPEC_FQCR.md §3.2 (a-priori interpretation of `t` is open)                               |
| Branch quadratic `x² − 16(G_N*)²x + 16(G_N*)³R_N(t) = 0` (notational) | [THEOREM — notational identity] | SPEC_FQCR.md §2 Prop 5; reduces to spine master quadratic at `R_N(1)=1, N→∞`             |
| `α_FTD⁻¹ ≈ 137.035999177` at `t = 1`                                  | [STRONGLY MOTIVATED CONJECTURE]  | OT-5.1; FTD-0013. **Structural evidence — the polynomial-scan leg is WITHDRAWN (FTD-0791, 2026-08-03):** the scan sits at the chance base rate (null expects 1.42–1.67 matchers, 1 found) and FTD-0319 is retagged [MEASURED] → [SELECTION]; the scan-rigid count is zero. *(Formerly cited here as: the 2.87M-poly scan returns 0 Eisenstein dual-matchers (OT-3.3), with the "~4×10⁵:1 Bayes" figure already **retracted to [NUMERICAL FACT]** — not runner-computed, ~19× scan-size, 2026-06-24 spine audit.)* Surviving legs: 63-discriminant Γ-product null at `h ≥ 2` (OT-3.2); Z[i] structural unification of CM Aut count and tower level k=4 (OT-1.5, FTD-0122). |
| Identification with physical `α⁻¹` (CODATA 2022 `137.035999177(21)`)       | [STRONGLY MOTIVATED CONJECTURE]  | OT-5.1; agreement 1.26 ppm; not [DERIVED] absent an operational native readout. **FTD-0242:** none of four attempted FTD-native routes forces the `(Tr,Det)=(16G*²,16G*³)` operator assembly; this is a four-route boundary, not an exhaustive mechanism theorem. **FTD-0244:** K-BIND is theorem-negative for the axiomatized substrate-native operator calculus; no operator in that declared calculus forces the quadratic extension without an external W-like selection. **FTD-0314/0315:** carrier narrowing motivates the adopted FC-W external ℤ/2 twist, under which `x₊=1/α` is `[CONDITIONAL THEOREM given W]`, while it remains `[STRONGLY MOTIVATED CONJECTURE]` unconditionally. **FTD-0412 scope correction:** MC-T4.3 is `[OPEN — SCOPED NO-GO PACKAGES]`; no existing result quantifies over every observable, boundary condition, enlarged algebra, or dynamical mechanism. |

---

# §6 · Minimal operator stack

**Current canonical stack (FQCR §2).**
`T_N(t) = (L_{1/4,N}, L_{3/4,N}, Ψ_N(t), λ_N(4it), T_N(t))`
with `G_N* = (N+1)^{−1/2} · det L_{3/4,N} / det L_{1/4,N}` (Lerch's formula, FTD-0141), `κ_N(t) = R_N(t)/(16 G_N*)`, and the transfer matrix
`T_N(t) = [[1, −κ_N(t)], [1, 0]]` — eigenvalue equation is exactly the §5 branch quadratic after rescaling.

| Claim                                                                | Tag                              | Source                                                                                  |
|----------------------------------------------------------------------|----------------------------------|-----------------------------------------------------------------------------------------|
| `G_N*` as determinant ratio on the registered finite stack           | [THEOREM — CONDITIONAL STACK]    | SPEC_FQCR.md §2 Prop 1; FTD-0141/0839; OT-1.7                                           |
| Stack `(L_{1/4,N}, L_{3/4,N}, Ψ_N, λ_N, T_N)` as minimal FQCR canon  | [SELECTION — current canon]       | SPEC_FQCR.md §1–§3                                                                       |
| Stack yields `G_N*, q*, R_N, x_±` algebraically                       | [DERIVED]                        | Composition of §1–§5 results                                                             |
| Stack yields physical coupling                                        | [STRONGLY MOTIVATED CONJECTURE]  | Inherits OT-5.1 / FTD-0013 tag                                                           |

---

# §7 · Lorentz / EM / QED / Dirac scaffolding

> **Scope flag.** The bivector / Dirac / QED scaffolding in this section is external mathematical machinery (Maxwell duality on `Λ²(R^{3,1})`, Clifford algebra, standard tree-level QED) that FTD's bridge program is exploring. **All physics-bridge claims here are tagged [OPEN] pending a derivation chain that does not conflict with the closed-negative result FTD-0073.** FTD-0073 (mode-erasure capstone, 2026-04-24) proved that **site-local 0-form state-field readout cannot support Clifford on any finite block under pointwise-threshold dynamics**. The bivector reading in this section is a candidate *non-site-local* scaffold; its compatibility with FTD-0073 has not been established. Pure linear-algebra facts about external Maxwell duality remain [THEOREM] but do not by themselves bridge to the FTD lattice.

**Bivector frame.** `B = (E, H) ∈ Λ²(R^{3,1}) ≅ R⁶`. The Hodge dual `*: Λ² → Λ²` defines `J(E, H) = (H, −E)` with `J² = −I` — the same algebraic primitive as §1.

**Charged holonomy.** `e^{iqA_e}` and the Dirac operator `D_A ψ(v) = Σ_μ γ^μ ∇_μ^A ψ(v)` with Clifford square `D_A² ≈ ∇_A^† ∇_A + i q Σ^{μν} F_{μν}`. Standard tree-level `g = 2` and one-loop `a^{(1)} = α/(2π)` are external QED results.

| Claim                                                                | Tag                              | Source                                                                                  |
|----------------------------------------------------------------------|----------------------------------|-----------------------------------------------------------------------------------------|
| `dim Λ²(R⁴) = 6`                                                     | [THEOREM]                        | External linear algebra (`C(4,2) = 6`)                                                   |
| Bivector duality `J(E,H) = (H, −E)`, `J² = −I`                        | [THEOREM]                        | External Maxwell duality                                                                 |
| Lorentzian signature emerges from bivector duality on the FTD lattice | **[OPEN]**                       | LEDGER FTD-0073: site-local Clifford **closed-negative** on finite blocks under pointwise-threshold dynamics. The bivector reading is a candidate non-site-local scaffold; bridging to FTD substrate requires a non-site-local construction not yet built. |
| Photon as finite edge connection / `F = dA` lattice gauge structure  | **[OPEN — IMPORTED scaffold]**   | Standard lattice-gauge formalism. FTD-0074 (flux 1-form readout) closed-negative for Clifford. |
| Dirac Clifford square gives Pauli term `i q Σ^{μν} F_{μν}`            | **[OPEN — IMPORTED scaffold]**   | External Clifford structure. Cite FTD-0073 mode-erasure for the FTD-side obstruction.    |
| Tree-level `g = 2`                                                   | **[OPEN — IMPORTED scaffold]**   | External QED, awaiting FTD-side derivation                                               |
| One-loop `a^{(1)} = α_FQCR/(2π)`                                     | **[OPEN — IMPORTED bridge]**     | Requires `α_FQCR  α` physical identification, which is itself FTD-0013 [STRONGLY MOTIVATED CONJECTURE]. Cannot be promoted above its dependencies. |
| Full QED `g − 2` precision                                           | [OPEN]                           | MC-T4.4 in SPEC_OPEN_MATH_BY_SECTOR.md                                                    |

---

# §8 · Electroweak / SM scaffold

**Internal frame.** `H_int = C³ ⊗ C² ⊗ C_Y`. **Gauge group.** `G_SM = (SU(3)_c × SU(2)_w × U(1)_Y) / Z_6` with center closure `Y_6 + 2r + 3w ≡ 0 mod 6`. **Charge.** `Q = T_3 + Y` (standard SM construction).

**Stiffness convention.** `X_2 = 4π/g_2²`, `X_Y = 4π/g_Y²`, `X_EM = X_2 + X_Y`.

**Lock ratio at GUT scale (SU(5) prediction).** `X_2 : X_Y = 3 : 5` ⇒ `sin²θ_W^lock = 3/8 = 0.375`. This is the **standard SU(5) GUT-scale value at the unification scale**, *not* the M_Z physical value. Comparison to experiment requires RG running. **Honest framing (W2.3 audit, 2026-05-08):** the 3:5 ratio is purely **imported** from standard SU(5) trace-normalisation (`5̄ = (3̄,1)_{1/3} ⊕ (1,2)_{−1/2}` forces the hypercharge trace) — no FTD substrate ingredient enters its derivation. The 3:5 → 3/8 step is therefore IMPORTED PHYSICS dressed in FTD vocabulary; cite FTD-0149 [IMPORTED] for the SU(5) trace-normalisation anchor.

**RG running to M_Z.** Standard SM running takes `sin²θ_W^lock = 0.375` at `M_GUT` to `sin²θ_W ≈ 0.231` at `M_Z`. The substrate-derivation of the running coefficients (`b_Y = 41/6`, `b_2 = −19/6`) from FTD finite spectra is **[OPEN / HARDENING]** (cross-link MC-T3.6, new ID 2026-05-08).

**Canonical IR fit.** FTD-0018: `sin²θ_W = 3/13 = 0.2308` is **[PARAMETRIC]** at M_Z (downgraded 2026-04-19; 3.5% off experimental 0.2229). The 3/13 fit and the 3/8 GUT-scale value are **distinct claims at distinct scales**; they coexist in canon — neither subsumes the other.

| Claim                                                                | Tag                              | Source                                                                                  |
|----------------------------------------------------------------------|----------------------------------|-----------------------------------------------------------------------------------------|
| SM group quotient `G_SM = (SU(3) × SU(2) × U(1))/Z_6`                | [IMPORTED — structural match]    | Standard SM construction                                                                 |
| Z_6 center closure `Y_6 + 2r + 3w ≡ 0 mod 6`                         | [THEOREM within scaffold]        | Group-theoretic computation                                                              |
| `Q = T_3 + Y`                                                        | [IMPORTED]                       | Standard SM                                                                              |
| Stiffness convention `X_a = 4π/g_a²`                                 | [DEFINITION]                     |                                                                                          |
| Lock ratio `X_2 : X_Y = 3 : 5` at GUT scale                          | **[IMPORTED]** (FTD-0149) — standard SU(5) trace normalisation | No FTD substrate ingredient enters; pure GUT-scale gauge-theory adoption |
| `sin²θ_W^lock = 3/8` at GUT scale                                    | [THEOREM once 3:5 imported]      | Direct algebra: `g_Y²/(g_2² + g_Y²) = X_2/(X_Y + X_2) = 3/8`. Honest reading: `IMPORTED` content, not FTD content |
| RG running of `sin²θ_W^lock` from GUT to M_Z                         | [OPEN / HARDENING]               | MC-T3.6 (β-coefficient substrate-derivation [OPEN]; new ID introduced 2026-05-08 to resolve W2.5 MC-T3.5 collision — old MC-T3.5 = FTD-0110 multi-scale boundary correction is sector-tracker §9, distinct research arc)                                       |
| `sin²θ_W ≈ 3/13` at M_Z (canonical IR fit)                           | [PARAMETRIC]                     | LEDGER FTD-0018 (downgraded 2026-04-19); 3.5% off CODATA                                 |
| Neutral-Higgs lock preserves `U(1)_EM`                               | [THEOREM within scaffold]        | Standard SM                                                                              |
| Substrate-derivation of beta coefficients from finite spectra        | [OPEN / HARDENING]               | **MC-T3.6** (β-coefficient arc; new ID 2026-05-08 to resolve W2.5 collision; *not* the same arc as sector-tracker MC-T3.5 = FTD-0110 multi-scale boundary correction) |

---

# §9 · Weak / Top / Higgs scaffold

**Weak scale.** `v² = Λ_h² / W_BCC` with `Λ_h = m_t · √(2 W_BCC) = m_t · G* / √π` (top saturation principle). At bare lock: `v = √2 · m_t`. **Honest framing:** `v = √2 m_t` follows from `y_t ≈ 1` (textbook top Yukawa); this is borrowed empirical input, not a novel FTD prediction.

**Higgs radial mode.** `m_H² = W_BCC · χ_H · v²` with `χ_H = 2 − 3 Ξ_t + Ξ_bos` (proposed scaffold). Computing `χ_H` from FTD substrate is **[OPEN / HARDENING]**.

**Equality to physical pole masses.** Equating bare-lock predictions directly to physical pole masses (without RG running, threshold corrections, etc.) is **[CLOSED NEGATIVE]** as a claim of strict identity.

| Claim                                                                | Tag                              | Source                                                                                  |
|----------------------------------------------------------------------|----------------------------------|-----------------------------------------------------------------------------------------|
| `v² = Λ_h² / W_BCC` (scale principle)                                | [SELECTION — scale principle]    | Internal FTD scaffold                                                                    |
| `Λ_h = m_t · G* / √π` (top saturation)                               | [SELECTION — top saturation]     | Internal FTD scaffold                                                                    |
| `v = √2 · m_t` at bare lock                                          | [BORROWED EMPIRICAL]             | Textbook `y_t ≈ 1`; numerical coincidence, not novel                                     |
| Equality to physical pole masses                                     | [CLOSED NEGATIVE]                | Bare lock ≠ pole mass without RG + threshold corrections                                 |
| Higgs `m_H² = W_BCC · χ_H · v²` with `χ_H = 2 − 3 Ξ_t + Ξ_bos`       | [SELECTION — scaffold]           | Cross-ref FTD-0017 (Higgs `(N_eff/α²)·m_e`, [STRUCTURALLY MOTIVATED PARAMETRIC])        |
| Computing `χ_H` from substrate                                        | [OPEN / HARDENING]               | No canonical anchor yet                                                                  |

---

# §10 · Flavor scaffold

**Base branch.** `q* = (G* − √(G*² − 4))/2` (smaller root of `z² − G*z + 1 = 0`; same as `e^{−χ_G}` of §2).

**Depth matrices (proposed candidate scaffolding).**
- `N_E = diag(9, 3, 0)` (charged leptons)
- `N_U = diag(12, 5, 0)` (up-type quarks)
- `N_D = diag(7, 4, 0)` (down-type quarks)

These integer-depth matrices are **proposed candidate scaffolding**. They have **no canonical derivation** in current FTD documentation. They are reverse-engineered from observed mass hierarchies via `q*`-power fits and tagged here at their honest level.

**Mass formula.** `D_F = G_F [m_{F,3} · C_F · q*^{N_F}] H_F†` (proposed unitary structure). Per-fermion projection corrections `C_F` are also **[PARAMETRIC candidate scaffold]**.

| Claim                                                                | Tag                              | Source                                                                                  |
|----------------------------------------------------------------------|----------------------------------|-----------------------------------------------------------------------------------------|
| `q*`-power hierarchy as flavor structural principle                  | [SELECTION — structural reading] | Internal FTD scaffold                                                                    |
| Depth matrices `N_E = diag(9,3,0)`, `N_U = diag(12,5,0)`, `N_D = diag(7,4,0)` | **[PARAMETRIC candidate scaffold]** | No canonical derivation; reverse-engineered from mass-ratio fits                  |
| Mass scaffold `D_F = G_F[m_{F,3} C_F q*^{N_F}] H_F†`                 | [SELECTION — scaffold]           | Internal FTD scaffold; projection corrections `C_F` candidate-only                       |
| CKM estimates from `q*` powers                                       | [NUMERICAL / STRUCTURAL APPROXIMATION] | Order-of-magnitude only; cross-ref CATALOG_PARAMETRIC_INSERTIONS.md                |
| Explicit transfer matrices forcing depths                            | [OPEN / HARDENING]               | No canonical anchor — would be a new derivation chain                                    |

---

# §11 · QCD scaffold

**Color frame.** `C³`. **Color group.** `SU(3)_c`. **Triality.** `r ∈ Z_3`.

**QCD beta coefficient (one loop).** `b_3 = (11 N_c − 2 n_f)/3`. For `N_c = 3, n_f = 6`: `b_3 = 7`. **The formula is imported standard QCD**; the integer `b_3 = 7` follows from substituting `N_c = 3` (independently sourced — see `DERIV_NC_FROM_TOPOLOGY.md` (four routes) and the Moore Layer Theorem; the historical identification `N_c = \lfloor x_- \rfloor` is **RETIRED** per v1.4 §5, LEDGER FTD-0014 removed in commit `ca7eb61`) and `n_f = 6` (SM input).

**Distinct claim.** FTD-0020 asserts `α_s = 7/59` as **[PARAMETRIC]** (downgraded 2026-04-19; rational fit with `7` matching `b_3` numerator coincidence). This is *not* the same claim as importing `b_3 = 7`; the LEDGER demotion of FTD-0020 does not affect the scaffold-level adoption of the standard one-loop formula.

**Confinement and strong CP.**
- Confinement as "non-abelian finite color transport creates trace-gap / flux closure" — **[CONJECTURE / DOCTRINE]**. The 2026-05-03 night audit of FTD-0025 explicitly recorded the structural obstruction: confinement is intrinsically non-classical (lives in `Z = ∫ dU exp(−S)`), and FTD's substrate is deterministic — there is **no Phase-G analog for area-law behavior**. The compact-U(1) link-variable formulation is *imported wholesale* from textbook lattice gauge theory and not substrate-derived. Internal `[THEOREM]` tags within `DERIV_CONFINEMENT_FROM_GAP_EQUATION.md` should each read `[THEOREM-within-compact-U(1)-LGT framework, PARAMETRIC at FTD-substrate level]`.
- Strong CP `θ_QCD = 0` by finite discrete orientation closure — **[CONJECTURE / NEEDS THEOREM PACKAGING]**.

| Claim                                                                | Tag                              | Source                                                                                  |
|----------------------------------------------------------------------|----------------------------------|-----------------------------------------------------------------------------------------|
| `N_c = 3` from `C³` / `Z_3` triality / `Z_6` closure                 | [SELECTION] (algebraic) — `N_c = 3` independently sourced; the historical `x_-  N_c` identification is **RETIRED** (v1.4 §5; LEDGER FTD-0014 removed in commit `ca7eb61`) | `DERIV_NC_FROM_TOPOLOGY.md`; Moore Layer Theorem                              |
| `SU(3)_c` as internal-frame symmetry                                 | [IMPORTED — structural match]    | Standard SM                                                                              |
| `b_3 = (11 N_c − 2 n_f)/3` formula                                   | [IMPORTED COEFFICIENT]           | Standard one-loop QCD                                                                    |
| `b_3 = 7` for `N_c = 3, n_f = 6`                                     | [THEOREM once formula imported, conditional on independent `N_c = 3` source + SM `n_f = 6` input] | Direct substitution                                            |
| `α_s = 7/59` (distinct claim)                                        | [PARAMETRIC]                     | LEDGER FTD-0020 (downgraded 2026-04-19); rational-fit coincidence                        |
| Confinement as trace-gap / flux closure                              | [CONJECTURE / DOCTRINE]          | LEDGER FTD-0025 (compact-U(1)-LGT framework imported, not substrate-derived)             |
| Color singlets `q-q̄`, `qqq` as observable states                    | [IMPORTED — structural match]    | Standard SM                                                                              |
| Strong CP `θ_QCD = 0` by finite orientation closure                  | [CONJECTURE / NEEDS THEOREM PACKAGING] | Internal FTD scaffold                                                                |

---

# §12 · Gravity / curvature

**Partial closure landed (FTD-0131, 2026-05-03; reconciled 2026-05-24 per `../07_assessment/audits/AUDIT_NEWTON_POSTULATES_RECONCILIATION.md`).** `DERIV_NEWTON_FROM_SUBSTRATE.md` derives Schwarzschild's leading-order behaviour from FTD substrate via Phase G's lattice Poisson Green's function (FTD-0004 [THEOREM]) plus FTD-0110's cluster-mass identification ([DERIVED at linear level]) plus FTD-0015's α¹¹ mass formula ([STRONGLY MOTIVATED CONJECTURE]) plus the **clock-hypothesis coordinate-level [AXIOM]** used in `SPEC_FTD_LAGRANGIAN.md` §4.3. The original two flagged postulates (gravitational coupling form, linearized tick-rate response coefficient `2/c²`) are subsumed by SPEC §4.2 + §4.3 [THEOREM]s per the 2026-05-24 reconciliation (Reading A confirmed; the linearized `2/c²` postulate's factor-of-2 was a `g_00`-vs-`dτ/dt` convention difference). Arc B P2 v1 closure attempt UNDERDETERMINED; v2 attempt INVALIDATED on process + substance axes; v3 closed **[CLOSED NEGATIVE, AXIOM-LEVEL]** on 2026-05-27: the L²/Pythagorean budget is structurally incompatible with Scale-0 primitives and must be posited at coordinate level.

**Substrate prediction.**
`α_G(e,e) = (m_e / m_P)² = [√(2π) · (16/3) · α¹¹]² ≈ 1.745 × 10⁻⁴⁵`
matches measured `1.752 × 10⁻⁴⁵` to **0.38%** (within FTD-0015's existing precision envelope).

**Closed-negative finding.** The framework-integer claim `G_N = 1/(b_3 + N_c)² = 1/100 in lattice units` is **[CLOSED NEGATIVE]** as identification with physical `G_N`. The substrate-derived `α_G` differs from `1/100` by `~10²⁰` (K_B = m_e calibration), `~300` (K_B = m_P calibration), or `~10⁴³` (dimensionless `α_G(e,e)`) — at minimum a 2.5-order discrepancy under any natural calibration.

**v1.4 closed-negative guardrail (FTD-0184).** The external ontic-system v0.2 branch-compliance/source-law stack proposes the exponential readout metric `dτ=e^{-U}`, `d ell_eff=e^U d ell`, `n_γ=e^{2U}`, and `dτ_m^2=e^{-2U}dt^2-e^{2U}d ell^2/c^2`. This is the Yilmaz-style route: useful as red-team provenance, but **[CLOSED NEGATIVE]** as a replacement FTD gravity sector because it diverges from GR beyond leading weak-field order and removes literal horizons. `Action-Closure Duality`, `ell_F`, `m_F`, and mass-depth notation receive **no promotion**; they are Planck-scale/substitution-identity bookkeeping under the existing calibration.

**v1.2 candidate principles.** The proposed Phase-3 program lists three candidate principles for substrate-deriving the remaining gravitational structure:
1. Finite trace curvature (curvature from `s_m` variation in §3 finite-trace mechanics)
2. Graph spectral curvature (curvature from lattice Laplacian eigenvalue structure)
3. Emergent spacetime curvature from finite adjacency deformation

These are **[CANDIDATE PRINCIPLE]** alternatives within the §14 Phase-3 program; none is currently a derivation chain.

| Claim                                                                | Tag                              | Source                                                                                  |
|----------------------------------------------------------------------|----------------------------------|-----------------------------------------------------------------------------------------|
| `G_+(r) → 1/(4π r)` at large r (Phase G)                             | [THEOREM]                        | SPEC_ALGEBRAIC_SPINE.md §6 Theorem 6; OT-1.4; FTD-0004                                  |
| Cluster mass `M = N · m_e` (linear regime)                            | [DERIVED at linear level]        | FTD-0110; OT-3.4                                                                        |
| Schwarzschild leading-order `dτ/dT = 1 + 2φ_g/c²` recovered           | [DERIVED, conditional on clock-hypothesis AXIOM] | FTD-0131; DERIV_NEWTON_FROM_SUBSTRATE.md §1; reconciled 2026-05-24 (`../07_assessment/audits/AUDIT_NEWTON_POSTULATES_RECONCILIATION.md`); FTD-0208 v3 closes substrate derivation negative and records the AXIOM floor |
| `α_G(e,e) = (m_e/m_P)² ≈ 1.745 × 10⁻⁴⁵`, 0.38% match                | [STRONGLY MOTIVATED CONJECTURE] (floor inherited from FTD-0015) + [DERIVED chain conditional on clock-hypothesis AXIOM] | FTD-0131; the 0.38% precision is squared FTD-0015 precision (mechanical, not new evidence); chain steps 1.1–1.5 derived from substrate; FTD-0208 v3 closed the clock-hypothesis substrate derivation negative and made the coordinate-level AXIOM explicit |
| `G_N = 1/(b_3 + N_c)² = 1/100` framework-integer reading              | **[CLOSED NEGATIVE per FTD-0131]** | Off by `~10²⁰` to `~10⁴³` under any natural calibration                              |
| Branch-compliance/Yilmaz exponential metric route (`dτ=e^{-U}`, `n_γ=e^{2U}`) | **[CLOSED NEGATIVE per FTD-0184]** | Agrees at low weak-field order but conflicts with standing GR/black-hole sector; see LEDGER FTD-0184 |
| `Action-Closure Duality`, `ell_F`, `m_F`, mass-depth notation          | [NO NEW CLAIM]                    | Planck-length/Planck-mass reparameterization and substitution identity per FTD-0184      |
| Gravitational coupling form `ρ_g = K_B^grav · 1_manifested`           | [THEOREM within SPEC §4.2, under `K_B^grav = K_B = m_e` identification] | Reconciled 2026-05-24; original POSTULATE-1 flag retired as duplicate of SPEC_FTD_LAGRANGIAN.md §4.2 |
| Linearized tick-rate response `tick = 1 + 2φ_g/c²`                    | [THEOREM conditional on clock-hypothesis AXIOM] | Reconciled 2026-05-24; factor-of-2 is `g_00` vs `dτ/dt` convention; FTD-0208 v3 supplies the AXIOM-level boundary |
| Beyond-leading-order GR (Mercury perihelion, light bending, GW)       | [OPEN]                           | DERIV_NEWTON_FROM_SUBSTRATE.md §5                                                        |
| Substrate-side strong-field GR / Schwarzschild-Kerr-horizon derivation | [OPEN]                           | Genuine open item surfaced by FTD-0184; do not use the exponential-metric shortcut       |
| Equivalence-principle analogue from substrate                         | [OPEN]                           | No canonical anchor                                                                      |
| Mass-gap to curvature source                                          | [OPEN]                           | No canonical anchor                                                                      |
| Three v1.2 candidate principles (trace curvature / graph spectral / adjacency deformation) | [CANDIDATE PRINCIPLE] | Phase-3 hardening targets; not derivations                                          |

---

# §13 · Non-circularity audit (roll-up)

This audit is a roll-up of the canonical LEDGER tags into four layers. **It introduces no new categorisation.** The canonical tag system (§0.1) already distinguishes derived from imported from numerical from open. This section reads them out at the doctrine level for navigation.

### §13.1 · Cleanly derived from internal primitives ([THEOREM] / [DERIVED] from axioms or prior theorems with explicit chain)

These have proof chains entirely inside FTD's axiom set or its rigorous algebraic spine:

`J² = −I`, `J⁴ = I`; `G* = Γ(1/4)/Γ(3/4)`; `G* = Γ(1/4)²/(π√2)`; `G_N* → G*` at `O(1/N²)`; `det_ζ D_{3/4}/det_ζ D_{1/4} = G*`; `G* = 2 cosh(χ_G)`; `q* = e^{−χ_G}`; symmetric recurrence; transfer-matrix `det = 1`; Casimir invariant `I_m`; Möbius reduction `z_{m+1} = s − 1/z_m`; regime split `\|s\| < 2 / = 2 / > 2`; `x² + y² ↦ x² − y²` under `y ↦ Jy`; light-cone boundary; `cos(2θ) = x² − y²`; `P_G = (G*/2, √(G*²−4)/2)`; coefficient 16 = `\|Aut(E)\|²`; tower harmonic invariant `1/y_+ + 1/y_- = 1`; `Q(G*) ∩ Q(π) = Q` (conditional on Chudnovsky 1976); per-voxel mass gap; the *arithmetic uniqueness* of D = 3 in `\|Aut(E)\|² = 2^D(D−1)!=16` (the dimension-forcing itself is `[SELECTION]`, FTD-0355 — see §13.2); Moore integers `{N_c=3, N_base=4, b_3=7, N_eff=13}`; `a_phys ≡ ℓ_P` no-go theorem.

**Anchor:** `SPEC_ALGEBRAIC_SPINE.md` 9 theorems + subsidiaries; `TRACKER_ONTIC_TRUTH.md` T1 + T2 + T3.4.

### §13.2 · Selected (consistency-argued, not uniqueness-proven) ([SELECTION])

`R_N(t) = 1 + λ_N + A_N`; `(4, 6; 3, 2)` exponent quadruple; `t = 1` base point; `μ² = s − 2`; Bernoulli lemniscate as one radial realisation; `v² = Λ_h²/W_BCC`; top saturation `Λ_h = m_t G*/√π`; `q*`-power flavor depths; mass scaffold `D_F = G_F[m_{F,3} C_F q*^{N_F}] H_F†`; `X_2:X_Y = 3:5` GUT lock; Higgs `χ_H = 2 − 3Ξ_t + Ξ_bos`; Bell `S = 2√2`; loop coefficients `c_1 = 9/47`, `c_2 = 5/64`, `c_3 = 4/141`; Einstein equations from Deser bootstrap; cyclotomic Hamiltonian parameters; Moore Layer Theorem; BCC multiplicative structure (`W₃ + SU(3)` from same eigenvalue).

**Anchor:** `LEDGER.md` per-claim; `CATALOG_PARAMETRIC_INSERTIONS.md` row-by-row.

### §13.3 · Imported from known physics ([IMPORTED] / [PARAMETRIC])

`G_SM = (SU(3) × SU(2) × U(1))/Z_6`; `Q = T_3 + Y`; `b_Y = 41/6`, `b_2 = −19/6`, `b_3 = (11 N_c − 2 n_f)/3`; Dirac/Clifford formalism (with FTD-0073 closure on site-local Clifford recorded); standard QED `β` and `g − 2` structures; SU(3) confinement framework (compact-U(1)-LGT); `θ_QCD = 0` adopted from standard SM.

**Anchor:** `LEDGER.md` rows tagged [IMPORTED] / [PARAMETRIC]; FTD-0018, FTD-0019, FTD-0020, FTD-0021, FTD-0022 demotions (2026-04-19).

### §13.4 · Numerically reconstructed ([STRONGLY MOTIVATED CONJECTURE] / [NUMERICAL FACT])

`α_FTD⁻¹ ≈ 137.035999177` (1.26 ppm; `x_+  1/α`, FTD-0013); `α_G(e,e) = (m_e/m_P)² ≈ 1.745 × 10⁻⁴⁵` (0.38%, postulate-conditional); CKM estimates from `q*` powers; structural uniqueness scans (2.87M-poly with 0 Eisenstein dual-matchers under the historical `(1/α, N_c)` target pair; 63-discriminant Γ-product null at `h ≥ 2`; the "~4×10⁵:1 Bayes weight" is **retracted to [NUMERICAL FACT]** (not runner-computed, ~19× scan-size; 2026-06-24 spine audit); FTD-0319 (formerly cited as FTD-0189) adversarial 2.65M-polynomial scan over an 18-constant FTD-undesigned basket: 0 non-G\* dual-matchers, rank 1 by ~130×). *(Note: `x_- = 3.024 ≈ N_c = 3` (0.80%) framing is **retired** per v1.4 §5; LEDGER FTD-0014 removed in commit `ca7eb61`. The `x_-` value is a mathematical artifact of $P(x)$; `N_c = 3` is independently sourced — see `DERIV_NC_FROM_TOPOLOGY.md`.)*

**Anchor:** `TRACKER_ONTIC_TRUTH.md` T3 + T5; `SPEC_PHYSICS_BRIDGE.md` (FTD-0121 SYNTHESIS).

### §13.5 · Main hardening targets ([OPEN] research arcs)

1. **Non-action alpha-readout mechanism** — MC-T4.3, central foundational obstruction. The target is not another match to `1/alpha`; it is a rule that maps the algebraic/FQCR branch to an operational electromagnetic coupling measured by matter, without inserting alpha. `SPEC_ALPHA_READOUT_CONTRACT.md` defines the required tuple `(P, A_obs, O_EM, R, C)`, exclusion rules, and ARC-0 to ARC-3 status levels. Candidate classes: boundary-condition readout, observable-selection readout, quantization/readout rule, discrete-native measurement path. Lead-physicist diagnosis: closure may require ontology extension beyond the 5 axioms.
2. **Derive `R_N(t)` from a variational / operator principle** — MC-T3.1 (FTD-0110 nonlinear bridge, NOT CLOSED, ~2.5× slope mismatch); FTD-0143 uniqueness scan PRE-REGISTERED. This is useful only if it feeds an operational readout or removes a selection from FQCR Model IV.
3. **Derive activation kernels and beta coefficients from finite spectra** — MC-T3.6 (new ID 2026-05-08; not the same arc as MC-T3.5 = FTD-0110 multi-scale boundary).
4. **Derive Higgs residual curvature `χ_H`** — no canonical anchor.
5. **Force flavor depth matrices from explicit transfer matrices** — no canonical anchor (depth matrices §10 [PARAMETRIC scaffold]).
6. **Substrate-derive QCD trace-gap confinement** — FTD-0025 night-2026-05-03 audit recorded structural obstruction (no Phase-G analog for area-law behavior); the strong-sector substrate-derivation gap is real and structural.
7. **Build gravity as finite trace curvature** — partial closure FTD-0131 (`α_G` to 0.38%, chain conditional on the clock-hypothesis coordinate-level AXIOM after FTD-0208 v3 closed substrate derivation negative; original 2 postulates subsumed by SPEC §4.2 + §4.3 [THEOREM]s); FTD-0184 closes the branch-compliance/Yilmaz shortcut negative; FTD-0193 substrate spin-2 search [CLOSED NEGATIVE] at L≤64; **Arc C2 spin-2 boundary theorem free-theory derivation landed 2026-05-24** (`../10_eft_program/derivations/DERIV_SPIN2_BOUNDARY_THEOREM_FREE_THEORY.md` + `../10_eft_program/derivations/DERIV_J_BILINEAR_NO_SPIN2_POLE.md`); Arc C2 P3 pre-reg (`preregister-spin2-boundary-theorem-v1`, FTD-0209) hash-locked. Substrate-side strong-field GR / horizon derivation remains [OPEN] in the dual sense per the boundary theorem framework (full nonlinear GR via Deser-bootstrap of POSITED h_μν per FTD-0189).

---

# §14 · Compressed roadmap

### Phase 1 — Doctrine ledger and audit (this document)

**Status: COMPLETE.** This v1.7 document compresses FTD's claim infrastructure into a single navigation surface. Every claim is rolled up at canonical tag from `LEDGER.md`, `TRACKER_ONTIC_TRUTH.md`, `SPEC_ALGEBRAIC_SPINE.md`, `SPEC_FQCR.md`, `SPEC_OPEN_MATH_BY_SECTOR.md`, or `CATALOG_PARAMETRIC_INSERTIONS.md`. The non-circularity audit (§13) reads out the four-layer structure (derived / selected / imported / numerical) without inventing new categories. The v1.7 scope correction preserves the theorem-negative K-BIND result while withdrawing the unjustified universal-obstruction reading and the invalid FTD-0126 evidentiary leg.

### Phase 2 — Hardening

| Priority | Hardening target                                  | Canonical anchor                                                        | Status                          |
|----------|---------------------------------------------------|-------------------------------------------------------------------------|---------------------------------|
| 0        | Operational alpha-readout mechanism               | MC-T4.3 (`SPEC_OPEN_MATH_BY_SECTOR.md` §10.1); FTD-0242 (`../07_assessment/audits/AUDIT_ALPHA_OPERATOR_FORCING_ROUTE_INVARIANCE.md`); FTD-0244; FTD-0412 | **OPEN — SCOPED NO-GO PACKAGES.** Four attempted routes fail to force the operator assembly and K-BIND excludes the axiomatized operator calculus. These results leave unquantified mechanism classes open; FTD-0126 supplies no valid matter-sector evidence. |
| 1        | `R_N(t)` variational/operator derivation          | MC-T3.1 (`SPEC_OPEN_MATH_BY_SECTOR.md`); FTD-0143 PRE-REG                | NOT CLOSED, slope mismatch ~2.5× |
| 2        | Activation kernels and beta coefficients          | MC-T3.6 (new ID 2026-05-08; β-coefficient arc separate from MC-T3.5 FTD-0110 multi-scale) | OPEN |
| 3        | Higgs residual curvature `χ_H`                    | NEW — no canonical anchor                                                | OPEN                             |
| 4        | Flavor transfer matrices forcing depths           | NEW — no canonical anchor                                                | OPEN                             |
| 5        | QCD trace-gap confinement substrate derivation    | FTD-0025 night-audit (2026-05-03); FTD-0131 cross-ref                    | OPEN, structural obstruction recognized |
| 6        | Gravity beyond Newtonian limit (substrate-side GR)| FTD-0131 partial; FTD-0184 closes branch-compliance/Yilmaz route negative | OPEN, partial closure landed; shortcut closed negative |

### Phase 3 — Gravity (subsumed by Phase 2 priority 6 + new candidate principles)

Develop substrate-derivation routes for full gravitational structure. The three candidate principles (finite trace curvature; graph spectral curvature; emergent spacetime curvature from finite adjacency deformation) are tagged [CANDIDATE PRINCIPLE]; none currently has a derivation chain. Core target:

`s_m variation ⇒ curvature ⇒ effective attraction / geodesic deviation`

This connects §3 finite-trace mechanics (where `s_m` lives) to §12 gravity. **Status: OPEN.**

### Phase 4 — Master compression

Aspirational target: a compact object `T_FTD` (an extended operator stack in the spirit of §6) such that

`T_FTD ⇒ G*, α, q*, SU(3) × SU(2) × U(1), running, masses, flavor, confinement, curvature.`

This is the strongest possible FTD self-claim. **Achievability is conditional on Phase 2 priority 0 (MC-T4.3 operational alpha-readout) and priority 5 (confinement substrate derivation, structurally obstructed).** Honest assessment: closure may not be achievable without ontology extension beyond the 5 axioms. The framework's external defensibility is fully achievable without `T_FTD` closure; the strongest possible self-claim ("derived 1/α + entire SM") requires it.

---

# §15 · Cross-references

| Doctrine §  | Primary anchor                                                                                      | LEDGER row(s)                                                  | Tracker row(s)               |
|-------------|-----------------------------------------------------------------------------------------------------|----------------------------------------------------------------|------------------------------|
| §1          | SPEC_FQCR.md §1; ../02_foundations/FOUND_AXIOM_ZERO.md                            | FTD-0141                                                       | OT-1.7                       |
| §2          | SPEC_ALGEBRAIC_SPINE.md §1, §5; SPEC_FQCR.md §2 Props 1, 2                                          | FTD-0001, FTD-0141, FTD-0142, FTD-0117                         | OT-1.2, OT-1.7, OT-1.8, OT-2.1|
| §3          | SPEC_FQCR.md §1, §2 Props 3, 4 (Model III)                                                          | FTD-0141, FTD-0142                                             | (algebraic; standard rep theory)|
| §4          | ../08_structural/DERIV_BCC_MULTIPLICATIVE_STRUCTURE.md; SPEC_ALGEBRAIC_SPINE.md §1                  | FTD-0001                                                       | OT-1.2                       |
| §5          | SPEC_ALGEBRAIC_SPINE.md §2 (Theorem 2); SPEC_FQCR.md §2 Prop 5, §3                                  | FTD-0001 (master quadratic), FTD-0013 (`x_+  1/α`), FTD-0143 PRE-REG; FTD-0014 (`x_-  N_c`) **RETIRED** v1.4 §5, row removed in commit `ca7eb61` | OT-1.1, OT-3.3, OT-5.1 |
| §6          | SPEC_FQCR.md §1, §2, §3                                                                             | FTD-0141, FTD-0142                                             | OT-1.7, OT-1.8               |
| §7          | ../09_mathematical/DERIV_MODE_ERASURE_AND_SPIN_ALGEBRA.md                                           | **FTD-0073 (CLOSED NEGATIVE for site-local Clifford)**, FTD-0074 | (no T-tier; closed-negative)|
| §8          | LEDGER.md (FTD-0018 sin²θ_W demotion); standard SU(5) GUT                                          | FTD-0018 (PARAMETRIC at M_Z)                                   | (T5/parametric)              |
| §9          | LEDGER.md (FTD-0017 Higgs)                                                                          | FTD-0017                                                       | (parametric)                 |
| §10         | CATALOG_PARAMETRIC_INSERTIONS.md (flavor section)                                                   | (catalog rows)                                                 | (parametric scaffold)        |
| §11         | LEDGER.md (FTD-0020 α_s; FTD-0025 confinement night-audit); standard QCD; `DERIV_NC_FROM_TOPOLOGY.md` (independent `N_c = 3` routes) | FTD-0020, FTD-0025 (annotated 2026-05-03); FTD-0014 (`x_-  N_c`) **RETIRED** v1.4 §5 | OT-5.2 (`x_-  N_c`) **retired** |
| §12         | ../03_derivations/DERIV_NEWTON_FROM_SUBSTRATE.md; SPEC_ALGEBRAIC_SPINE.md §6 (Phase G) | **FTD-0131** ([SMC] for prediction inherited from FTD-0015 + [DERIVED chain]), **FTD-0184** (branch-compliance/Yilmaz route [CLOSED NEGATIVE]; strong-field substrate gravity [OPEN]), FTD-0004, FTD-0110, FTD-0015 | OT-1.4, OT-3.4 |
| §13         | LEDGER.md (full); CATALOG_PARAMETRIC_INSERTIONS.md; TRACKER_ONTIC_TRUTH.md (T1–T5)                  | (roll-up)                                                      | (roll-up)                    |
| §14         | SPEC_OPEN_MATH_BY_SECTOR.md (MC-T4.3, MC-T3.1, MC-T3.6, T4.4)                                        | (roadmap)                                                      | (roadmap)                    |

---

# §16 · Refresh policy

This document is refreshed when:

- A LEDGER row's tag changes for any claim cited in §1–§13.
- `SPEC_ALGEBRAIC_SPINE.md` adds, removes, or retags a theorem.
- `SPEC_FQCR.md` updates Models I–V (e.g., upon FTD-0143 scan execution → Model IV [SELECTION] upgrade or rejection).
- A new partial closure lands in §7 (bivector / Dirac bridge), §9 (`χ_H`), §10 (depth matrices), §11 (confinement), or §12 (gravity beyond leading order).
- `TRACKER_ONTIC_TRUTH.md` adds an OT-N.M entry that this doctrine should cross-reference.
- Phase 2 priority 0–6 items (§14) close.

When refreshing, increment the `Version` line in the header (v1.3 → v1.4 etc.) and add a one-line entry to `LEDGER.md` row FTD-0145 noting what changed. **No tag promotion in this document is permitted without a corresponding canonical-source change.**

---

# §17 · Single-line summary

**FTD's algebraic spine is six theorem-grade results plus three honestly-tiered subsidiary results (nine numbered, recount 2026-08-03 per FTD-0785's demotion of the former Theorem 6; see `SPEC_ALGEBRAIC_SPINE.md` §0) centered on `G* = Γ(1/4)/Γ(3/4) ≈ 2.9587`; the FQCR capstone (Models I–V) lands operator-theoretic provenance and the finite-N reframe-compatible restatement; the master quadratic's roots match `1/α` to 1.26 ppm and `N_c` to 0.80% as [STRONGLY MOTIVATED CONJECTURE] (numerical-uniqueness support fully withdrawn as of FTD-0791/0802/0321 — the SMC now rests on structural arithmetic only); the strongest physics-facing claim is therefore a rigid candidate readout, not a finished derivation; gravity has a partial closure (`α_G(e,e) = (m_e/m_P)²` to 0.38%, FTD-0131, postulate-conditional), while the branch-compliance/Yilmaz shortcut is closed negative (FTD-0184) and substrate-side strong-field GR remains open; the §7 bivector/Dirac/QED bridge sector is [OPEN] pending a non-site-local construction compatible with FTD-0073's mode-erasure closure; §8's GUT-lock `sin²θ_W = 3/8` and IR `sin²θ_W = 3/13` [PARAMETRIC] coexist in canon at distinct scales; the central foundational obstruction (MC-T4.3, operational alpha-readout mechanism) may require ontology extension beyond the 5 axioms; the 2026-08-11/12 native-time-carrier programme (§2.1) attacked the closed-negative clock hypothesis (CLK-1, FTD-0208 v3) directly and still has not found a substrate-forced carrier, leaving CLK-1/CLK-2 [AXIOM]/[IMPOSED] as before — and as of 2026-08-13 (FTD-1000) CLK-1 is folded into FC-2's metric-half declaration as its per-worldline proper-time clause (disambiguated there from the unconditional global tick, P2, and from this programme's separate native-carrier question), a re-homing of an already-adopted axiom, not a promotion; and a parallel v2 "Contextual Actualization" branch (FTD-0825, 2026-08-09) exists as an explicit, separately-booked alternative to this document's v1/FC-1 measurement-map posture, not a replacement for it.**
