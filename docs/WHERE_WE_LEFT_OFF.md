# Current State of FTD

A single-page orientation to where the framework stands and where its canonical truth lives. This page carries **no change log** — what changed and when lives in `CHANGELOG.md` and git history; per-claim status lives in the LEDGER.

Foundational Ternary Dynamics is a philosophy-of-mathematics project: a discrete, finite, deterministic ternary-lattice ontology, the mathematics it forces, and the physics that mathematics suggests, ordered **Ontology > Logic > Math > Physics**. The project's aim is the Number-One Goal in `CLAUDE.md` (amended 2026-07-12, FTD-0383) — set the smallest honest set of types a discrete ontology can speak from; build the content forward, sector by sector, until every physical structure is either forced content or a marked-and-priced import; **drive** every priced line to retirement, a theorem-grade no-go, or a sharper falsifier — never leaving a line merely booked; and where a line provably resists retirement, search deliberately for the next honest type whose declared adoption converts it into content at a minimal, falsifiable price (the boundary marked *qualitatively* by the modulus/argument frontier, *quantitatively* by the priced-import ledger FTD-0371, and *driven* by the Consumption Program charter, `01_reference/SCOPE_CONSUMPTION_PROGRAM.md`).

**LATEST — FTD-0998/0999: coherent growth now has an exact cumulative
resource law; the native reserve current remains open.** For an accepted
frontier batch `F_n`, let `D_n` be its total new maintained-clock energy,
`B_n` its causally local usable reserve, `Phi_n` signed boundary inflow, and
`U(F_n)` exact local formation release. Conservation uniquely gives

`B_(n+1)=B_n+Phi_n+U(F_n)-D_n`.

The batch fires only when `B_n+Phi_n+U(F_n)>=D_n`. A shared reserve must be
checked atomically; independent stale reads can spend the same energy twice.
Likewise, only Moore-independent frontier sites may replace `U(F_n)` by a sum
of one-site work values. Overlapping supports need one joint work transaction.

The exact cumulative law is

`sum_(n<T) D_n = B_0-B_T+sum_(n<T)(Phi_n+U_n)`.

For identical site energy `e`, quiescent/no-inflow growth obeys
`N_add<=floor(B_0/e)`. Indefinite growth requires unbounded cumulative causal
supply; if rates exist, `P_bar>=e v_g`. Remote reserve is delayed by the Moore
cone, and complete reverse history restores every reserve/source/boundary
energy exactly.

This closes the accounting law, not its physical implementation. A restored
common/relative catalyst transfers organization but contributes zero net
fuel. A scalar `B` is not a canonical work pair. Production has no reserve
owner, joint gate, replenishment, or inverse and its diagnostic ledger omits
rest offsets. The parent passed `89/91`; both failures were dependent comment
markers. FTD-0999 passes repaired `91/91` plus `11/11`, Outcome B. Open next:
derive a native nonnegative reserve density and Moore-local current with
phase-complete ownership, atomic debit, exact refill, and reverse transport.
No engine change.

**LATEST — FTD-0997: the relative field can be the catalytic growth port, but
the static quiescent membrane cannot power recursion.** Prepare the donor
common clock and its local relative pair in the same crossing state `z`, with
a blank neighboring common receiver. A complete-pair swap gives the receiver
`z` and empties the relative port. The FTD-0994 work shear then refills that
port to `z_U` using formation work `U`.

`(C,R,Y)=(z,z,0) -> (z,z_U,z)`.

The swap is symplectic, orthogonal, and involutive; refill is canonical. The
full energy ledger is exact for arbitrary positive `U`, and inverse refill
followed by inverse swap recovers every declared variable. The port retains

`Delta H_R=U-e`.

It is recursively ready exactly when `U=e`, the FTD-0996 compliance law. Thus
the existing relative pair supplies sufficient phase-complete capacity with
no seventh continuous pair, while mismatch remains physical rather than
being deleted.

This capacity is not native preparation. The FTD-0990 Hamiltonian is block
diagonal in common/relative variables and does not force `C=R`. Formation
work depends on coordinate strains while crossing energy depends on momentum,
so compliance is a regular codimension-one surface, not an identity.

Most sharply, at a matching kinetic crossing with zero affected strain and no
onsite load, `W=U=0`. A positive-energy port can perform one swap but the
static membrane cannot refill it. Repeated coherent growth must be powered by
stored boundary strain, a latent onsite term, incoming relative/environmental
energy, or a prepositioned reserve. First locked certificate `99/99`, Outcome
B, no repair. FTD-0998/0999 now derives the cumulative reserve, finite-growth,
power, atomic-admission, causal-delay, and exact-reversal laws. The native
reserve density/current and phase-complete production mechanism remain open.
No engine change.

**LATEST — FTD-0995/0996: exact coherent clock growth occurs on a local
formation-energy compliance surface, and a matched quartic site inherits the
G* calendar without reading it.** At an occupied donor's kinetic crossing,

`q_x=0`, `sigma=sign(p_x)`.

Let a neighboring blank receiver have zero Cartesian clock state and let
`U_y=-W_y>0` be the local work released by its occupancy transition. The
FTD-0994 shear gives `p_y'=sigma sqrt(2m U_y)`. Therefore

`C_xy=2m U_y-p_x^2=0`

is necessary and sufficient for `(q_y',p_y')=(q_x,p_x)`. The disappearing
membrane energy and appearing clock energy are the same local scalar. The
same-crossing inverse clears the receiver and restores the membrane when the
orientation/aperture history is retained.

The FTD-0990 occupancy Laplacian annihilates the uniform state. Consequently
an admitted site enlarges an invariant coherent manifold under any identical
onsite Hamiltonian. Moore-independent frontier events have additive work and
grow coherence by at most one graph shell per local event.

For the selected critical quartic clock, exact Cartesian-state inheritance
also inherits amplitude, orientation, the `G*` period, and the conductor-32
CM normalization. The gate reads neither `G*` nor a target phase. A work ratio
`r=U_y/E_x` instead produces `T_y/T_x=r^(-1/4)`, so generic mismatch detunes
the quartic clock; the harmonic control remains isochronous.

The parent passed `84/88`; all new mathematics/physics passed and four
verifier representations failed. The repair passes inherited `88/88` plus
`17/17`, Outcome B. FTD-0997 now proves the existing relative pair supplies
an exact catalytic swap/refill port, but the separable membrane does not force
compliance and the quiescent seam supplies zero refill. Positive inflow,
backpressure, robustness, and production remain open. No engine change.

**LATEST — FTD-0993/0994: released membrane work can start a Cartesian clock;
an extended body must acquire that phase causally.** Let `U>0` be the local
net released work and `sigma=+/-1` the retained time-odd orientation. The
coordinate generator

`S_sigma(x,Q)=sigma integral_0^Q sqrt(2U(x,xi)) dxi`

defines an exact momentum shear. On the zero-action seam `Q=P=0`,

`P'=sigma sqrt(2U)`,
`I'=U/Omega`,
`theta'=-sigma*pi/2`.

Therefore zero action is not a physical self-start obstruction; only the
action--angle chart is singular there. Cartesian coordinates support an exact
symplectic, invertible first stroke. The source reaction is part of the same
generator and vanishes on the registered seam. The result reads no desired
phase, Born weight, context, setting, outcome, or `G*` target.

This does not permit instantaneous global preparation. The uniform projector
of an `N`-site body is dense, so one bounded seed cannot update every site in
one Moore tick. Exact propagation requires at least the body's graph radius.
Direct phase copying is also not canonical. The exact symplectic join of an
old uniform mode and one new site is pure uniform only when

`q=Q_N/sqrt(N)`,
`p=P_N/sqrt(N)`.

The incoming site must already carry the matching phase and `1/N` of the old
uniform-mode energy, or the mismatch remains as a relative excitation.

The parent certificate passed `95/96`; the sole failure was a mistyped source
hash literal. The repair passes inherited `96/96` plus `11/11`, Outcome B.
FTD-0995/0996 subsequently derives the exact crossing-matched causal growth
law and quartic-calendar inheritance. Autonomous formation-energy matching,
mismatch export, robustness, the membrane coupling, physical `U`, receiver
identity, production, Born/Bell, mass, and hiding remain open. No engine
change.

**LATEST — FTD-0991/0992: membrane formation is a local cut-set work
transaction, and an active boundary needs a fail-closed oriented aperture.**
Conditional on the selected FTD-0990 common-channel membrane Hamiltonian,

`W_(m->m')=(1/2) sum_b(g_b'-g_b)a_b d_b^2`.

For a simultaneous occupancy-bit flip on a region `S`, only bonds crossing
its cut-set change:

`W_S=(1/2) sum_(b in boundary S)(1-2g_b)a_b d_b^2`.

Internal bonds of a simultaneously flipped cluster are unchanged. The result
is local, fixed-field site-order independent, and avoids the double counting
of a naive site-by-site formation cost. One-site growth obeys

`W=E_join-E_cut`.

An occupied island formed in uniform void therefore releases exactly its
pre-existing common boundary strain. Reversing the same mask update at the
same field point costs the exact negative work. Released work can charge an
already regular local action through

`I'=I-W/Omega`,

FTD-0993/0994 subsequently shows that Cartesian coordinates do prepare a
first stroke at `I=0` when positive `U` and retained `sigma` are available.
What remains is physical source/receiver identification and causal growth,
not an abstract zero-action obstruction.

Static boundaries still use `s^2` alone. An active aperture requires a
controller. The minimum fail-closed registered gate is

`gamma=g+(1-g)r^2`,

with the reversible oriented transfer `(sigma,0)<->(0,sigma)`. Blank `(0,0)`
is closed; at a boundary the receiving sign opens the common channel.
Opening costs one bond's strain and closing returns it. Five logical states
(blank, closed +/- and open +/-) cannot fit in one ternary slot; two are
sufficient.

The parent certificate passed `80/83`; the three failures were literal
verifier predicates. The immutable repair passes inherited `83/83` plus
`13/13` integrity, Outcome B. The result remains conditional on the selected
FTD-0990 coupling. Production has no actuator, positive reserve, target-blind
phase preparation, reciprocal genesis/evaporation, or moving-boundary law.
`omega0`, `G*`, polarity, Born/Bell, mass, hiding, and completeness remain
open. No engine change.

**LATEST — FTD-0990: the body itself supplies the static membrane, and the
dual fields naturally split into recursive clock and interaction channels.**
For every site,

`m_x=s_x^2`.

This is the unique charge-blind occupancy bit. For every face/edge bond,

`eta_xy=m_x-m_y`,
`g_xy=1-eta_xy^2`.

The gate is one within matter or within void and zero exactly across a
matter--void boundary. Therefore

`K_m=B^T diag(g_b) B`

is positive, Moore-local, and separates each connected occupied body without
storing an independent latch on every static boundary bond.

Conditional on keeping the relative channel open, the minimum dual law is

`H=H_+(K_m)+H_-(K)`.

Equivalently, the unique `L/R` stiffness block is

`[[K_m+K,K_m-K],[K_m-K,K_m+K]]/2`.

The common `L+R` sector is the protected recursive clock/work sector; its
boundary current is zero. The relative `L-R` sector remains the interaction
sector and carries ordinary C18 current through the same boundary. The cross
coupling in the original `L/R` chart is supported only on that boundary.

For a connected body, the internal Laplacian has the unique constant kernel.
Production's imposed de Broglie term already uses the identical predicate
`state!=0`, so after adopting the membrane law the normalized uniform common
mode is the unique lowest body clock:

`H_u=omega0 I_u`,
`I_u'=I_u+[H-H']/omega0`.

This is a conditional substrate/clock gearbox, not a `G*` derivation:
`omega0` remains imposed. The first locked certificate passes `81/81`, Outcome
B, no repair. Static per-bond memory is retired. FTD-0991/0992 subsequently
closes the exact conditional formation/reversal work ledger and the minimum
fail-closed active-aperture form. Still open are the occupancy-controlled
production coupling, physical aperture controller/reserve, zero-action phase
preparation, moving/deforming membranes, collision/backpressure, stability/
parity, and hiding. No production change; `G*`, Born/Bell, mass, and
completeness remain open.

**LATEST — FTD-0988/0989: local ownership is a boundary-bond clutch, and the
physical work action is frequency-normalized.** Write the frozen C18 stiffness
as `K=B^T B`. For each controlled face/edge bond, a ternary latch gives

`g_b=1-ell_b^2`,
`K_ell=B^T diag(g_b) B`.

`ell=0` transmits the ordinary bond; `ell=+/-1` cuts it while retaining the
oriented sign. Cutting exactly the bonds crossing a finite boundary produces
`K_Lambda direct-sum K_complement`: a conservative regional common-field
subsystem without any dense projector or seventh continuous pair. Its local
energy current is

`J_(x->y)=g_xy a_xy(q_x-q_y)(p_x+p_y)/2`,

and summing local continuity leaves only the boundary flux. A cut bond carries
zero current; an open bond is the exact charging/replenishment channel.

Switching costs

`W=1/2 sum_b(g_b'-g_b)a_b(q_y-q_x)^2`.

The natural clutch seam is zero strain, `q_y=q_x`, where switching has zero
work and zero force impulse. A nonzero relative velocity supplies the signed
crossing token, which reverses under time reversal and must transfer to a
receiving ternary record. Fixed-clutch kick--drift is symplectic, invertible,
and preserves an exact local shadow Hamiltonian.

There is one important correction. The earlier `H+2I` identity is exact only
for production's explicitly non-Hamiltonian observable-amplitude audit. For a
regional eigenmode `K_Lambda u=lambda u`, `omega=sqrt(lambda)`, the physical
law is

`H_u=omega I_u`,
`I_u'=I_u+[H-H']/omega`.

Final repaired certificate: inherited `73/73`, integrity `11/11`, Outcome B.
FTD-0990 subsequently derives the static boundary gate directly from endpoint
occupancy, retiring independent static per-bond memory. Dynamic aperture/
history, autonomous membrane formation, mode preparation, controller reserve,
complete-tick closure, stability/parity, and hiding remain. No production change.
`G*`, Born/Bell, mass, and completeness remain open.

**LATEST — FTD-0986/0987: the substrate already contains the local work pair;
what it lacks is a law that owns and protects it.** In the neutral body's
derived regional frame, repack the dual fields into common/relative pairs and
choose

`Q=e3·(J_L+J_R)/sqrt(2)`,
`P=e3·(P_L+P_R)/sqrt(2)`.

This pair is canonical, signed-cubic invariant, and fixed by the production
`L/R` swap. On `I>0`, `Q=sqrt(2I)cos(theta)` and
`P=-sqrt(2I)sin(theta)` give an exact action-angle chart. Its observable
amplitude norm is `2I`, so the amplitude-audit reference law is

`I'=I+[H(z)-H(z')]/2`,
`H(z')+2I'=H(z)+2I`.

No seventh continuous pair is needed. But “unused by the five-pair chart” is
not “autonomous reserve.” Every production component is propagated, sourced,
possibly clock-driven, damped, and synchronized into the observable field.
An exact Laurent argument proves no nonzero compactly supported C18 mode is a
closed oscillator. The current audit also states that its amplitude norm is
not the wave-tick Hamiltonian. Production has no port owner, isolation clutch,
switching-work/current ledger, or inverse.

The lowest-price remaining candidate is a ternary-controlled projector clutch:
`K_iso=PKP+P_perp K P_perp`, with `ell^2` selecting isolation and the sign of
`ell` retaining event orientation. Switching costs exactly
`q^T(K_ell'-K_ell)q/2`; it cannot be free. This law is selected, not yet
substrate-derived. Final certificate: inherited `96/96`, repair integrity
`13/13`, Outcome B. FTD-0988/0989 now derives the bond-level causal
clutch/current law and corrects the physical action normalization to
`H_u=omega I_u`. Boundary-latch formation, mode selection, and production
remain open. `G*`, Born/Bell, mass, hiding, and completeness remain open.

**LATEST — FTD-0983--0985: the global clock can synchronize events, but it
cannot be their same-tick physical energy reservoir.** One global pair
`(theta,I_G)` can book aggregate work exactly:

`I_G'=I_G+sum_a[H_a(z_a)-H_a(z_a')]`.

That law is symplectic and total-energy exact, but if `I_G` is stored at one
site its update depends on remote event variables outside the Moore cone. If
it is declared accessible everywhere, it is an explicitly nonlocal primitive.
A central reserve also needs at least `ceil(d/r)` ticks to reach distance `d`.

The substrate-local law is one complete `(theta_a,I_a)` work pair, or an
equivalent local canonical work field, in every causally disjoint same-tick
work cone:

`z_a'=R_a(theta_a)z_a`,
`I_a'=I_a+H_a(z_a)-H_a(z_a')` at the registered seam.

The product map is symplectic, locally energy exact, invertible, and order
independent on disjoint supports. A single shared phase with many actions is
not a shortcut: its two-form has all relative-action differences in the
kernel. Global time `n` may align eligibility conditions, but usable reserve
must be locally prepositioned. Overlapping events must be compiled as one
joint batch or separate sublayers to prevent double debit.

The final locked chain passes the physical certificate `62/62`, repaired v2
integrity `14/14`, and final integrity `18/18`, Outcome B. Production only has
conditional selected-frame capacity: its sixth scalar pair is unused, but no
native local scalar chart, formation law, reserve transaction, or CUDA phase
state exists. Open next: derive a regional-frame realization of that sixth
pair with formation/synchronization/replenishment/inverse, or price a local
canonical work field explicitly. `G*` remains cadence only; Born/Bell, mass,
hiding, production, and completeness remain open.

**LATEST — FTD-0981/0982: the instantaneous local quarter-turn needs a
canonical work port; a local multicomponent stiffness factor alone is not the
event.** The exact production stiffness has the finite-range incidence factor
`K=B^*B` with three face and six edge channels, and therefore admits a local
self-adjoint block factor. The rank-three vacuum Hessian proves at least three
real analytic factor channels are required, but does not prove the
nine-channel witness minimal.

The block factor localizes a first-order generator, not its inverse. The
massless factor is singular, and for positive mass its inverse would contain
`K_mu^-1`; the nonconstant Laurent stiffness is not a Laurent unit. Thus it
does not implement the exact one-event energy root locally in the original
coordinate/momentum variables.

One complete phase/action work pair per independently gated batch gives the
minimum exact reference completion. At the registered crossing,

`z'=R_sigma z`,
`I_R'=I_R+H(z)-H(R_sigma z)`,
`H(z')+I_R'=H(z)+I_R`.

The phase-dependent seam lift is finite-range, symplectic, and exactly
invertible. Four same-orientation strokes restore both `z` and `I_R`.
However, no fixed finite reserve covers unbounded amplitudes; a physical gate
needs a bounded compliance shell and must fail closed. The work pair also
does not replace the ternary orientation/history latch. The immutable parent
recorded `76/79`, a first repair `78/79`, and the final locked wrapper passes
inherited `79/79`, Outcome B. Open next: derive `(theta,I_R)` from existing
substrate variables or price it as an adopted type, then prove local
concurrency, reserve formation/replenishment, single-clock coupling, CPU/GPU
parity, stability, and operational hiding. `G*` remains cadence only. No
production change.

**LATEST — FTD-0979/0980: the clockwise/counterclockwise law is the oriented
symplectic square root of the existing production swap, and its physical
price is now exact.** On each complete relative dual pair there are precisely
two roots in the registered fixed-common orthogonal symplectic class:

`R_sigma^2=S`, `R_sigma^-1=R_-sigma`, `R_sigma^4=I`.

They mix relative flux with relative wave velocity. In common/relative
variables, `R_+` acts by `J=[[0,-1],[1,0]]` and `R_-=-J`. Time reversal swaps
them. Their common square is the present left/right half-turn, explaining
exactly why the current swap loses orientation.

Energy fixes the normalized root. For field stiffness `K`,

`J_K=[[0,-K^-1/2],[K^1/2,0]]`.

This is exact, symplectic, and energy preserving, but the scalar dispersive
`C18` stiffness has no finite-range Laurent square root. A local scalar root
with chosen frequency `kappa` instead exports the exact work

`Delta H = [D^T(kappa^2 I-K)D + P^T(K/kappa^2-I)P]/2`,

which vanishes for all states only when `K=kappa^2 I`. The choice is now
explicit: nonlocal modal root; local root plus a canonical work/history
reservoir; or separately selected multicomponent factor hardware.

The minimum reversible seam handshake is
`(sigma,0,z)->(0,sigma,R_sigma z)`. A reset that discards `sigma` is
noninjective. Four same-direction crossings return the field but retain four
history tokens. The immutable parent stopped after 44 passes on one Python
tuple reporter; the locked FTD-0980 wrapper repairs only that expression and
passes inherited `58/58` plus `16/16`, Outcome B. FTD-0981/0982 now proves
that factor hardware alone does not localize the one-event inverse and that
one canonical phase/action work pair supplies the minimum exact reference
completion on a finite-reserve ready domain. `G*` remains cadence only. No
production change.

**LATEST — FTD-0978: the production left/right hardware supplies a
symplectic half-turn, but the current clock does not select its oriented
quarter-turn root.** The source-locked CPU/CUDA exchange swaps both dual flux
and dual wave velocity. Exactly,

`S^T Omega S=Omega`, `S^2=I`, and
`S_common/relative=diag(+1,-1,+1,-1)`.

Thus the relative pair sees `-I`. Since `J^2=(-J)^2=-I`, this is precisely
the square that forgets whether the preceding quadrant motion was clockwise
or counterclockwise.

The imposed de Broglie oscillator does not restore that bit. Its
Klein--Gordon coefficient is identical on left and right and commutes with
the swap. Weak transmutation reads stress, threshold, seeded RNG, and global
tick, but neither local phase nor proper time. The host diagnostic phase is
advanced only after the weak phase; CUDA carries `d_tau` and no `d_phase`.
The production ledger also has no clock potential, connection reaction,
switching work/reserve, oriented latch, or retained inverse channel.

The fired swap branch is its own inverse, but the predicate-gated update need
not be injective when `P(x)=1` and `P(Sx)=0`; the optional event journal is
observation-only. The exact source certificate passes `51/51`, Outcome B.
This closes the present de Broglie-clock/weak-transmutation route negative as
the physical FTD-0977 mapping-torus witness, not all possible substrate
routes. Open next: specify the minimum explicit phase-crossing `+J/-J` clutch
with one-clock reaction, work/reserve, retained ternary orientation, inverse,
and CPU/GPU realization. No engine/CMake/type/toggle/coupling/production
change.

**LATEST — FTD-0976/0977: one clock really does force one covariant momentum
and one kinetic square, but the retained C4 carrier does not force the compact
common-profile law.** For a specified regular fiber action, the canonical
one-form uniquely gives

`K=Pi+r_G A_G(delta)G-q r_I A_I(delta)I`

and `H=K^2/(2M)+V+H_int`. The full six-dimensional map is symplectic. With
fixed representation sector and cyclic internal angles, `G` and `I` are
conserved, the canonical connection forces cancel exactly from the mechanical
equation `K_dot=-V'`, and the two internal phases acquire their path
holonomies.

The immediate FTD-0963/0974 composition is

`K=Pi+calA(delta)G-qI`,

because the gearbox uses the selected window connection while the faithful
field suspension uses the controller phase itself. The previously stated
`Pi+calA(G-qI)` requires the extra choice `A_G=A_I=calA`; it is valid but not
derived. Once either merged lift is selected, its cross term is mandatory:
`-q calA G I/M` for the existing composition and `-q calA^2 G I/M` for the
common-profile specialization.

`C4` endpoint data fixes neither profile nor integer lift. Exact unequal
profiles have the same `pi/2` integral, and weights `1` and `5` give the same
endpoint `J`. The fixed `q in {-1,0,+1}` is a representation sector, not yet
the production state; changing it causes an exact momentum impulse and needs
an explicit reversible switching ledger. Finally, every regular local
one-dimensional connection is pure gauge. A physical quarter-turn requires a
twisted endpoint/mapping-torus identification and evidence that it acts on an
actual substrate record rather than only coordinates.

FTD-0976 is preserved at `49/52`, Outcome D, on three verifier-form defects.
FTD-0977 passes inherited `52/52` plus repair integrity `16/16`, Outcome B.
Open next: run a source-locked production census for an actual clock-indexed
twisted fiber identification with measured work, reaction, history, and
inverse. No engine/CMake/type/production change.

**LATEST — FTD-0975: the selected minimum suspension fits the existing
dual-field capacity, but simultaneous coexistence with the full FTD-0963
gearbox requires one merged Hamiltonian rather than two clock kinetic terms.**
As an alternative, the suspension uses two of six canonical pairs. As a
specialization, it reuses the gearbox clock and one exchange pair. Both are
exact symplectic embeddings.

A wholly disjoint realization needs seven pairs and is impossible in six.
The lower-price simultaneous layout shares the clock and uses the sixth unused
pair for `(Q,P)`, filling all six pairs exactly. But adding

`(Pi+X)^2/(2M) + (Pi-I)^2/(2M_s)`

double-counts `Pi^2` and changes the unloaded clock rate by `Pi/M_s`. The
coherent candidate is instead

`H_merge=(Pi+X-I)^2/(2M)+H_rest`.

That has one mechanical momentum `K=Pi+X-I` and one bare kinetic term, but it
also creates the new cross-coupling `-XI/M`. This is not forced by storage
capacity. FTD-0975 passes `46/46`, Outcome B, without repair. Open next: freeze
and test the merged square as a selected reference law, including its complete
Hamilton equations, reaction, endpoints, inverse, and repeated-cycle
stability. Production integration remains forbidden. No
engine/CMake/type/production change.

**LATEST — FTD-0973/0974: the retained C4 carrier admits an exact positive
canonical field suspension, but it does not uniquely choose the field
coupling.** For one normalized complete field pair, every edge of the carrier
may apply `J^a_k`. All 256 cocycles are reversible and symplectic. Their only
gauge-invariant discrete connection datum is

`m=sum_k a_k mod 4`,

so there are four `Z4` holonomy classes. Carrier-phase translation symmetry
forces constant `a`; those maps are direct products. Both the trivial action
and faithful `+/-J` actions satisfy the same carrier symmetries, proving the
physical field representation is underdetermined.

The minimum continuous Hamiltonian controller costs one complete phase/action
pair. The selected faithful positive law

`H_susp=(A-I)^2/(2M)+nu I`, `I=(Q^2+P^2)/2`,

closes exactly. With `K=A-I`, `theta_dot=K/M`, while the interaction-picture
field rotates by the same angle as the controller. One controller quadrant is
one `J`; four return to identity. `A=K+I` books reciprocal field-action load,
the full energy and `I` are conserved, and negative time is the exact inverse.

This permanently coupled reference law is active, unlike passive chart
rotation, but its physical modes, scales, and adoption are selected. Turning
it on/off or localizing it to one edge requires a new work/history
transaction. FTD-0973's parent was `63/64` Outcome D only on one source
marker; FTD-0974 passes inherited `64/64` plus `12/12`, Outcome B. Open next:
audit production representability of this specific minimum suspension without
double-booking the five FTD-0963 pairs. No engine/CMake/type/production change.

**LATEST — FTD-0971/0972: retaining direction through the neutral body's
`kappa=0` degeneracy produces a minimum ternary latch and an exact oriented
four-cycle whose real mode is `i`.** The one-sided frames differ by the
reflection `S=diag(1,1,-1)`. That static reflection squares to `+I` and merely
conjugates the two handed complex structures; it is not the quarter-turn.

At the common degenerate body, a self-delimiting record must distinguish
blank, incoming `+`, and incoming `-`, so a three-state latch is exactly
minimum within the one-cell interface. Retaining those directions gives

`R+ -> D+ -> R- -> D- -> R+`.

The cycle is bijective, has `T^4=I`, and obeys
`Theta T Theta=T^-1`. On the real plane generated by opposite-state
differences, forward evolution is `J=[[0,-1],[1,0]]`, reverse is `-J`, and
`(+/-J)^2=-I`. Thus `i` is not a new ontic state: it is the oriented
eigenstructure of a complete recursive transition. The symmetric square
forgets forward versus reverse.

This is exact kinematic stability, not physical formation or field exchange.
The equal-weight carrier norm is preserved, but regular/degenerate substrate
energies, deformation work, perturbative attraction, and active coupling are
unproved. FTD-0971's immutable parent was `60/62` Outcome D only on two prose
markers; FTD-0972 passes inherited `62/62` plus `14/14`, Outcome B. Open next:
derive the minimum controlled symplectic coupling from this C4 carrier to one
complete field pair with reaction, energy, reserve, and inverse. No
engine/CMake/type/production change.

**LATEST — FTD-0970: the regional body frame has a unique canonical
connection and reciprocal momentum correction, but that natural connection
is flat and cannot itself be the active gearbox.** With `q=F(s)y`, `p=F(s)z`,
the canonical one-form forces
`Pi_A=P_A+G_A`, where
`G_A=sum_j z_j^T(F^T partial_A F)y_j` is the field angular momentum projected
onto the frame's angular velocity. A positive body kinetic term becomes the
exact complete square `(Pi-G)^T M^-1(Pi-G)/2`.

This is the natural source of moving-frame reaction, but
`A=F^T dF` is the Maurer--Cartan pure gauge: its exact non-Abelian curvature
vanishes, transport is `F(t1)^T F(t0)`, and every closed path returning to the
same regular frame has identity holonomy. A finite frame jump is symplectic
and reversible yet leaves lab fields unchanged. It is a passive chart update,
not token loading, work export, or an outcome.

The active mechanism is now isolated rather than guessed. It must be either a
curved physical connection with a complete source/reaction ledger or a
reversible singular/chart-transition event whose retained record carries the
missing holonomy. The `kappa=0` crossing cannot be supplied by the smooth
frame. Certificate `45/45`, Outcome B, no repair. Open next: preregister those
two mechanism classes and test the lower-price retained transition first;
then return to nonlinear stability and positive export. No
engine/CMake/type/production change.

**LATEST — FTD-0966--0969: a neutral four-site body supplies the missing
regional spatial handedness and transverse complex structure, but not its own
moving-frame dynamics or time arrow.** For a finite neutral actual support,
define the dipole `d`, centered covariance `C`, and
`kappa=det[d,Cd,C^2d]`. On `kappa!=0`, `chi=sign(kappa)` and Gram--Schmidt on
`d,Cd` define an ordered polar triad. The exact map
`I_F v=chi(e1 cross v)` obeys `I_F^2=-(I-e1e1^T)` and transforms covariantly
under every proper and improper signed-cubic transformation.

Neutral `+/-1` support must be even. Two sites give a rank-one covariance and
zero Krylov determinant, while the exact four-site unit-cube witness has
`kappa=-1/256`; four is therefore both necessary and sufficient. Rotating
each existing field coordinate and conjugate momentum by the fixed body frame
is symplectic and adds no snapshot storage pair. This conditionally repairs
the selected-frame debt in FTD-0965.

The boundary is equally exact. `chi` is spatial and time-even; traversal
orientation still needs the separate time-odd current, and squaring the two
oriented complex structures loses that sign. In the family with fourth point
`(1,1,u)`, `kappa=-u^5/256`, so the frame is singular at coplanarity and flips
handedness across it. A state-dependent moving frame therefore requires its
own canonical connection, reciprocal reaction, switching work, and history.
FTD-0969 validates the immutable repair chain at mathematical `75/75` plus
repair layers `19/19`, `20/20`, and `22/22`, Outcome B. Open next: derive that
moving-frame connection/reaction without changing production, then return to
repeated-map stability and positive phase-error export. No
engine/CMake/type/production change.

**LATEST — FTD-0964/0965: the dual substrate has enough raw canonical
capacity for the selected gearbox, but the unchanged production tick does not
realize it and no native site-local linear cubic scalar chart exists.** The
two stored field pairs `(flux_L,wave_vel_L)` and `(flux_R,wave_vel_R)` contain
six scalar canonical component pairs. Conditional on a selected orthonormal
frame, five can be packed symplectically into `(delta,Pi),B,D,C,R`, leaving one
complete pair unused. Thus capacity alone does not force a new public
continuous storage type.

The fixed frame is not free. The exact average over all 48 signed-cubic
matrices gives a rank-zero invariant projector on `V+V`, so no site-local
linear `O_h`-covariant scalar coordinate can be extracted from the raw dual
vectors. A regional nonlinear body frame plus pseudoscalar, or a priced
selected frame, is still required.

Production `phase` and `tau` are one-way diagnostics, not canonical pairs.
The weak `L/R` swap has square `+I` and determinant `-1`; the required
oriented quarter-turn has square `-I` and determinant `+1`. The frozen tick
contains no complete-square connection, connection energy, reserve, inverse,
or continuous exchange generator. FTD-0964's parent halted at `70/72` on two
scope-string defects; FTD-0965 passes inherited `72/72` plus `13/13`, Outcome
B. Open next: derive the regional frame/connection provenance or explicitly
price the action, then test the full nonlinear repeated map and complete
positive phase-error export. No engine/CMake/type/production change.

**LATEST — FTD-0962/0963: an oriented phase connection is an exact
reference gearbox between a local clock crossing, signed history token, and
controller phase.** The selected positive law is

`H_conn=[Pi+calA(delta)G]^2/(2M)+V+nu_T A_T+nu_C A_C`.

The complete square is decisive. With mechanical momentum
`K=Pi+calA G`, the clock still obeys `K_dot=-V'(delta)` while canonical
momentum carries the exact reciprocal reaction `Pi=K-calA G`. The internal
exchange angle is geometric:

`Delta alpha=int_path calA(delta)d delta`.

It is independent of traversal speed and controller load. A chart normalized
to `pi/2` moves one positive battery token into the outgoing `+1` record and
swaps a pre-aligned reference into the controller. The complete old controller
phase/action is retained in the return mode. Reverse traversal restores every
variable exactly; it is the natural inverse/unactualization stroke.

Conditional on a maintained critical quartic action-angle clock, `G*` fixes
the time per quadrant, the connection fixes the quarter-turn, and path
orientation fixes forward versus inverse. This is the first exact reference
gearbox between the two clocks, not a production derivation. FTD-0964/0965
now shows conditional fixed-frame capacity but closes the unchanged production
law and native site-local linear cubic chart negative. Open next: recover a
regional frame/connection or price the selected action; separately test
repeated-cycle stability and one-way positive history export. FTD-0962's
parent halted at `107/115`; FTD-0963 passes
inherited `115/115` plus `25/25`, Outcome B. No engine/CMake/type/production
change.

**LATEST — FTD-0960/0961: the existing oriented history channels are already
an exact finite-horizon winding carrier, but they are not a compact counter.**
Map the signed crossing current to two opposite pre-existing Moore channels.
After `N` rail updates, channel and causal depth reconstruct every ternary
crossing label, and

`w_N=w_0+sum_j(c_+(j)-c_-(j))=w_0+sum_t s_t`.

Thus no new public memory type is needed for a declared finite horizon.
Clockwise and counterclockwise remain distinct even when their net winding
cancels. The price is explicit: every nonzero crossing consumes one retained
token, selected energy `epsilon_*/2`, and advancing causal support or tail
backpressure.

This is not the compact `3^n>=2W+1` counter. A reversible positive increment
cannot also hold the upper boundary fixed without giving `E(W)` two
preimages; overflow/backpressure must be retained. Existing parity rotations
only permute signed coordinates, so they transport records but do not perform
nonlinear carry. FTD-0962/0963 now supplies an exact selected connection
witness for crossing-to-port acquisition and reversible controller phase
replacement. The missing mechanism is now production representability,
formation, replenishment, routing, and one-way export.
FTD-0960's parent halted at `55/60`; FTD-0961 passes inherited `60/60` plus
`19/19` repair integrity, Outcome B. No engine/CMake/type/production change.

**LATEST — FTD-0958/0959: exact global isochrony requires a phase lift and
retained orientation/history; a crossing latch alone cannot align an
independent controller.** A real-analytic periodic natural phase well cannot
have one finite period across its complete libration basin because the period
diverges at every finite-order barrier. The exact isochronous witness is the
selected harmonic cover

`H_lift=Pi^2/(2M)+(K/2)(delta+2pi w)^2`, `w in Z`.

The oriented transition
`(delta,w,Pi)->(delta-2pi s,w+s,Pi)`, `s=sign(Pi)`, preserves lifted phase,
energy, the canonical one-form, and the inverse exactly. This is the direct
clockwise/counterclockwise answer: signed current distinguishes the two
directions; `s^2` authorizes a crossing while identifying them. The existing
ternary latch/signal interface is sufficient for one-shot eligibility and
reversible clearing, not indefinite winding history.

An independent controller returns to gate zero without reset at every
same-orientation crossing iff `Omega/kappa` is integral, and at every crossing
iff it is even integral. Thus the missing mechanism is now an exact gearbox,
not another clock. No relation to `G*` is inferred. Next: test the existing
oriented parity/history rails as a finite winding carrier. FTD-0960/0961 now
closes that finite-horizon carrier conditionally and isolates compact carry
and native acquisition. Next construct or close negative an active no-reset
gearbox with work, reserve, reaction, and inverse.
FTD-0958's parent halted at `91/93`; FTD-0959 passes inherited `93/93` plus
`12/12` repair integrity, Outcome B. No engine/CMake/type/production change.

**LATEST — FTD-0956/0957: a selected same-field relative-action energy now
turns the neutral phase memory into a positive conservative synchronization
pair.** With `delta=theta-sigma*vartheta` and
`Pi=L-sigma omega rho^2`, adopt

`H_sync=Pi^2/(2M_delta)+K_delta(1-cos delta)`.

Its full Hamiltonian flow includes the mandatory radial reciprocal reaction,
conserves total axial charge and matched energy exactly, and has a strict
zero-energy lock. Below the separatrix the lock is Lyapunov stable and every
nonzero orbit returns through `delta=0`. The exact sampled Floquet multipliers
are `exp(+/- i sqrt(K_delta/M_delta)T)`: bounded elliptic restoration, not
attraction.

At any zero-phase crossing, including one with nonzero `Pi`, the FTD-0955
charge/Routh endpoint preserves `Pi`, `H_sync`, total charge, total physical
energy, and the complete inverse exactly. This is the simplest current model
of a stable recursive “more-than-matter” internal sector: phase error and
action error exchange energy without losing the distinction.

The law and its two positive scales are selected, not derived. Its nonlinear
period depends on amplitude and diverges at the separatrix. FTD-0958/0959 now
closes global natural-well isochrony negative, constructs the exact lifted
harmonic witness, and proves the oriented crossing/gearbox conditions. The
native winding carrier and active gearbox remain open. Attraction still
requires positive complete history export. FTD-0956's immutable parent
halted at `108/111` on three verifier defects; FTD-0957 passes inherited
`111/111` plus `12/12` repair integrity, Outcome B. No engine/CMake/type/
production change.

**LATEST — FTD-0955: one globally periodic autonomous controller now
compiles charge transfer followed by positive nonlinear Routh relaxation, but
phase lock is neutral rather than restoring.** Two disjoint periodic `C1`
windows generate the exact FTD-0954 global charge pulse and FTD-0953 Routh
quarter-turn in order. Controller action returns at both boundaries and stays
positive under `J_0>max(2|D|/pi,N_R)`. On the ready section, total axial charge,
endpoint physical energy, complete mismatch history, and the inverse all
close exactly.

Off lock, the pulse gives the exact radial conjugate reaction while leaving
`delta_(n+1)=delta_n`; the Floquet multiplier is `+1`. In the matched linear-
action reservoir, total energy is `omega P_chi` and has no relative-action
curvature, so adding a phase potential alone still gives `delta_dot=0`.
FTD-0956/0957 now supplies a selected positive relative-action curvature,
closes its crossing-section energy ledger, and replaces the neutral multiplier
with an elliptic conjugate pair. Its cadence and autonomous engagement remain
open; attraction requires an explicit export channel because closed
Hamiltonian flow preserves volume. First immutable
certificate `105/105`, Outcome B, no repair. No engine/CMake/type/import/
production change.

**LATEST — FTD-0954: the nonlinear body now has an exact phase-reacting
charge reservoir on a declared co-rotating stroboscopic section.** In the
polar chart `p.dq=p_rho drho+L dtheta`, axial charge `L` is conjugate to the
body phase. For the target-blind local charge change `D`, the pulse
`H_tr=(sigma*vartheta-theta)D` generates `Delta L=D`,
`Delta I=-sigma D`, and radial reaction
`Delta p_rho=(theta-sigma*vartheta) partial_rho D`. The full six-dimensional
map is symplectic. At co-rotation the radial kick vanishes while charge
transfer remains exact. Composed with the positive FTD-0953 Routh port, it
conserves total axial charge and endpoint `H_rot+E_port+omega I` exactly,
with a complete inverse and fail-closed positive-action backpressure.

The closure is local and stroboscopic. FTD-0955 now supplies the global
periodic two-window compiler and proves that the off-lock radial reaction does
not restore phase: relative-action curvature remains necessary. Native
reservoir formation and finite 3D routing/recycling remain open. First
immutable certificate `83/83`, Outcome B, no repair. No engine/CMake/type/
import/production change.

**LATEST — FTD-0952/0953: the nonlinear body now has a positive canonical
Routh-port relaxation, and the remaining physical-reservoir obstruction is
phase reaction.** On the registered branch the rotating-frame functional is
strongly convex with Hessian lower bound `2172 Lambda/3125`. Target-blind
eight-color C18 coordinate minimization converges on every declared finite
grounded region, whose minimizer differs from the exact tailed body by at most
`10^-3 2^(-(R+1))(1+12500/(4887 Lambda))` in `l2`.

Each local nonlinear decrease is an exact positive canonical quarter-turn
between the field's Routh-energy coordinate and a fresh complete port. The
outgoing port retains the full mismatch and closes
`A_0^2 Delta S+Delta E_port=0`. This is a genuine positive environment at the
Routh level, not yet full charged-body formation: because
`A_0^2 S=H_rot-sigma omega Q`, physical energy and axial charge require a
co-rotating action/phase reservoir. Finite action capacity exists for every
finite computation, but the minimum phase-blind state-dependent action debit
is nonsymplectic. FTD-0954 closes that canonicality debt on a declared co-
rotating stroboscopic section; global autonomy, synchronization stability,
finite 3D port routing/recycling, native source/reservoir formation, and
production remain open. FTD-0952 halted before
classification on one symbol assumption; FTD-0953 passes inherited `87/87`
plus repair integrity `9/9`, Outcome B. No engine/CMake/type/import/production
change.

**LATEST — FTD-0950/0951: the exact tailed recursive body now has a causal,
finite-support, work-booked reference preparation sequence.** Rewriting the
FTD-0949 Banach map as the local residual controller
`u_(n+1)=u_n-L^-1 F(phi_0+u_n)` produces an approximant supported inside the
depth-`n` C18 causal ball without reading the completed target profile. Its
exact weighted error is at most
`(11/13022)(2489/9000)^n`, and its residual is exactly
`r_n=L(u_n-u_(n+1))`, bounded by
`(88 Lambda/9375)(2489/9000)^n`.

The residual is exported through an exactly invertible local mismatch port.
Local field-energy work and axial-charge change telescope against named
signed ledgers, with finite total absolute variation. This closes causal
finite-radius reference relaxation and accounting, not physical autonomous
formation: the controller/core marker are selected, the ledgers are not
positive phase-complete reservoirs, and each layer needs a fresh zero port or
a derived recycling law. The FTD-0950 immutable parent returned `78/80` on one
SymPy structural-equality normalization; FTD-0951 preserves it and passes
inherited `80/80` plus repair integrity `9/9`, Outcome A. FTD-0952/0953 now
closes the positive nonlinear Routh-port part; FTD-0954 closes its local
phase-reacting physical energy/charge account. Global compilation and
stability, finite mismatch-port recycling, the ordered two-frame/pseudoscalar
source, exact physical ticking, and production remain open. No engine/CMake/
type/import/production change.

**LATEST — FTD-0949: the selected C18-sextic relative field has an exact
uncontained exponentially tailed recursive solution in a declared regime,
but compact local data cannot form it exactly in finite time.** Locking
`Lambda=beta A_0^4>=10000`, `a^2=6/5`, and
`omega^2=26 Lambda/25`, a weighted-space contraction proves a unique profile
within `10^-3` of the one-site anti-continuum core. The exact C18 weighted
operator bound is `16/3`; the contraction constant is `2489/9000<1/2` and
the self-map bound is `2249/3000000<10^-3`.

The profile is strictly positive at every site and satisfies
`|phi_x|<=10^-3 2^(-|x|_1)` off the core. It therefore has finite energy,
finite nonzero axial charge, and an exact continuous-Hamiltonian rotation,
while remaining noncompact. The operational uncontained statement is the
finite-radius tail bound `sum_(|x|_1>R)|phi_x|^2 <= 10^-6 4^(-(R+1))`.

A vacuum-preserving radius-one tick starting from compact body/source data has
compact dependency support after every finite number of ticks. It cannot equal
an everywhere-positive target profile. Exact finite-time formation is closed
negative. FTD-0950/0951 subsequently constructs a causal epsilon-approximation
with exact residual/work/charge ledgers; positive physical formation remains
open. The strong-nonlinearity threshold is a coarse sufficient selected
regime, not a physical fit or production normalization. First locked
certificate `71/71`, Outcome A, no repair. Next: close a positive reservoir,
port recycling, the native two-frame/pseudoscalar source, and exact finite-tick
energy, then test stability/mobility/collision. No engine/CMake/type/import/
production change.

**LATEST — FTD-0946/0947/0948: the existing relative field supports a
minimum nonlinear recursive charge, but its natural production gearbox is
still missing.** A body-supplied unit axis `e` gives the existing transverse
relative vector the exact complex structure `J_e v=e cross v`, with
`J_e^2=-Pi_e`. In the declared radial nonnegative class, the unique
degree-minimum potential with both zero and ring vacua is
`V=beta r^2(r^2-A^2)^2`. Coupled through the existing positive C18 stiffness,
it conserves `Q_e=sum e dot(q cross p)` exactly.

For every nonzero fixed charge on a fixed finite region, the reduced energy
`Q^2/(2N)+<q,Kq>/2+sum V` is coercive and attains a minimum. Its constrained
minimizers are recursively rotating relative equilibria. The exact continuous
Hamiltonian has an antisymmetric local energy current. This is a conditional
stable recursive reference mechanism in existing fields, not an uncontained
localized particle or a production formation theorem.

The local symmetric kick--drift--kick map is reversible, symplectic, finite-
range per substep, and exactly charge preserving, but it has a nonzero exact
finite-step energy defect. A conditional two-quadrature source is reversible
and closes against a named reservoir, yet one polar axis cannot select a
nonzero transverse vector at a C4-symmetric axis; full signed-cubic covariance
also needs a pseudoscalar because `e cross v` is axial while relative momentum
is polar. Thus `i` supplies rotation, not `gamma`: nonzero damping gives
`Qdot=-gamma Q` and requires a separate bath/feedback account.

The FTD-0946 parent remains invalid at `70/79`; FTD-0947's first repair wrapper
also remains invalid before inherited execution. The FTD-0948 wrapper-
integrity repair passes inherited `79/79`, repair integrity `8/8`, and wrapper
integrity `6/6`, Outcome B. FTD-0949 subsequently proves an uncontained tailed
body and FTD-0950/0951 constructs its causal reference relaxation. The next
open layer is a positive reservoir/port-recycling mechanism, a native ordered
two-frame plus pseudoscalar source, and exact finite-tick energy before
mobility/collision/production work. No engine/CMake/type/import/production
change.

**LATEST — FTD-0944/0945: unchanged production cannot write a relative L/R
history from symmetric vacuum.** Every frozen ordinary dual-field action is
equal-additive, equal-homogeneous, an L/R exchange, or contains no dual-field
write. Therefore `D=P_D=0` at all sites is an invariant submanifold of the
whole registered event stack. Selected asymmetric constructors can prepare
`D!=0`; natural genesis, pair production, movement, collision, Gauss,
damping, and boundary actions do not create it autonomously.

A fired weak event is a real survivor: `(s,D,P_D)->(-s,-D,-P_D)` is an exact
local symplectic involution. Void movement can advect a fraction of preloaded
`D`, but it does not transfer `P_D`. Its phase map has determinant `1-f`, is
singular at the live full-transfer branch `f=1`, and is nonsymplectic for
`f!=0`. Genesis/pair production do not write L/R; evaporation, bounce,
annihilation, and absorbing boundaries contain explicit loss. The observation
journal records those losses externally and is not ontic repair.

The FTD-0944 parent passed every substantive gate and failed one capitalized
prose-marker check (`136/137`). FTD-0945 preserves it and applies exactly one
in-memory literal repair; inherited `137/137` and repair integrity `7/7` pass,
Outcome B. Together with FTD-0943, this exhausts unchanged isolated-linear and
existing event-mediated routes. Next: preregister the minimum nonlinear
self-trapping/source action on the existing `(D,P_D)` pair; explicit oriented
ports remain the separately priced fallback. No engine/CMake/type/import/
production change.

**LATEST — FTD-0943: the isolated linear C18 relative field has no exact
finite-range characteristic split and no nonzero compact state that
translates or recurs after any positive number of ticks.** The positive
stiffness has vacuum Hessian `(2/3)I_3`; the exact kick--drift discriminant has
Hessian `(-8/3)I_3`. A square of one scalar local Laurent symbol can have
Hessian rank at most one there, so neither symbol is a finite-range scalar
square. Exact directional branches exist only through a non-Laurent modal
square root.

For a finite-support complete state `X`, rigid translation would require
`U^m X=z^d X`. The determinant condition reduces to
`z^d+z^-d=2 T_m(1-K/2)`. Its right side is fully signed-cubic invariant; for
every nonzero `d`, its two-monomial left side is not. For `d=0`, the determinant
begins `m^2 K` and is nonzero. The Laurent integral-domain/adjugate argument
therefore forces `X=0` for every displacement and every positive tick count.
This extends FTD-0919 from compact eigenmodes to arbitrary-tick
phase-complete translators and recurrence.

FTD-0942 remains the hardware boundary: existing L/R storage is an exact
common/relative aggregate canonical carrier, but its ordinary equal sources,
18-neighbor fanout, collision cancellation, missing backpressure, and missing
event-energy transaction do not realize FTD-0941's exported token history.
FTD-0943 now closes the exact isolated-linear protected-pulse/characteristic
escape. Next: audit the **existing event-mediated production actions**—genesis,
weak L/R exchange, manifestation/movement, and their compositions—for an
autonomous reversible relative record with a source debit and inverse. This
does not exclude driven, approximate, exponentially tailed, or nonlinear
structures and does not force a new primitive type. Exact certificate
`478/478`, Outcome B; no engine/CMake/type/import/production change.

**FTD-0941: exact unbounded winding cannot be stored in fixed
bounded finite-alphabet hardware; finite local reversible retention requires
expanding/exported support.** A region with `r` sites and a `q`-symbol local
alphabet has only `q^r` states, so it cannot injectively encode every integer
winding. A reversible update is a finite permutation and necessarily repeats.
The three FTD-0940 owner candidates therefore split exactly: cumulative
integer flux is correct but locally unbounded; finite body labels do not
unwrap winding and are noncanonical for indistinguishable occupancy; finite
link registers retain only a quotient unless they reject, backpressure, or
export overflow.

A finite-per-port reference survivor exists. Two binary lanes on each of the
26 oriented Moore channels undergo a local source swap and one-edge stream.
The map is exactly reversible, signed-cubic covariant, collision-separating
inside its declared capacity, and preserves quadratic token energy. A neutral
hop deposits `2d`, matching FTD-0940; fixed-hub causal depth reconstructs the
whole direction word and distinguishes opposite zero-net C4 orientations.
After `N` hops the record occupies `2N` ports out to depth `N`. The history is
not compressed into matter—it is exported into the uncontained substrate.

This separates global calendar, local phase, and history. Ontic tick `n` may
count globally while a finite local clock recurs without storing its absolute
cycle number; `G*` may eventually set the local period factor but is not the
global count. Winding is historical memory, not automatically physical
momentum. FTD-0942 subsequently proves that the current linear L/R realization
fails the token gates while retaining exact aggregate bare-wave reversibility.
FTD-0943 subsequently closes the exact finite-range characteristic and compact
rigid-translator/recurrence route in the isolated linear pair. The first open
route is the existing event-mediated production transition relation, followed
by a separately preregistered nonlinear self-trapping action; selected
direction ports are the fallback price. Exact FTD-0933 wake payment, body-local
inertia, `epsilon_*`, `p_*`, mass, `gamma`, formation, recovery, production,
finite-tick `G*`, Born/Bell, context, and hiding remain open. First-run exact
certificate `681/681`, Outcome B; no engine/CMake/type/import/production
change.

**LATEST — FTD-0939/0940: the neutral body has distinct signed-charge and
occupancy transport channels; the latter owns one directed crossing but not
persistent carry.** In the registered degree-at-most-two onsite class,
vacancy zero, charge-conjugation evenness, and unit normalization uniquely
give `nu=s^2`. This is an existing-record observable, not a new primitive or
an assignment of mass.

For the neutral C4 dipole translated along the live FTD-0936 direction
`d=u_n`, democratic shortest-path face currents satisfy exact local
continuity. Their aggregates split sharply:

`sum K_signed = d sum s = 0`,

`sum K_occupancy = d sum s^2 = 2 d`.

Thus signed field-charge flow cannot own neutral center motion. The oriented
occupancy crossing recovers the one-hop direction exactly, distinguishes the
reverse crossing, and has the exact finite-range central-current bridge. The
signed channel simultaneously inherits the conditional FTD-0576 Hodge scalar
transaction `Delta H_f+Delta U_int+Delta H_m=0`, using only the pre-hop
record, selected path current, and endpoint fields—never a target wake debit.

The remaining boundary is equally exact. An instantaneous ternary/occupancy
record cannot determine accumulated torus winding: stationary and full-loop
histories can share the same endpoint record and differ in carry. Mapping a
winding integer to real momentum still requires independent `p_*`, inertia,
and `gamma`. The time-centered Hodge chord has also not yet been identified
with the abrupt field-frozen FTD-0933 wake. FTD-0941 subsequently closes the
finite-capacity classifier: fixed bounded finite-alphabet winding is
impossible, while a reversible Moore-token history carrier survives only by
using expanding support/backpressure and a free token-energy normalization.
FTD-0942 subsequently closes the current equally sourced linear L/R token
realization negative while proving exact aggregate bare-wave reversibility.
FTD-0943 closes exact finite-range characteristics and compact rigid
translation/recurrence in the isolated linear pair. Next: audit existing
event-mediated production transitions, then preregister a nonlinear
self-trapping action or explicitly price direction channels, and couple a
surviving realization, the quartic source, and the C4 companion under one
temporal ordering. The
FTD-0939 parent is preserved invalid before gate reporting; FTD-0940's one-
line component-alphabet repair passes inherited `128/128`. No engine/CMake/
type/import/production change.

**LATEST — FTD-0905--0907 (2026-08-11): the substrate's existing types can
represent the missing polar axis and clockwise/counterclockwise memory, but
the exact G* clock and persistent chirality must occupy separate modes.** The
FTD-0905 first immutable certificate passed `74/75`; only a nonexistent exact
prose marker failed. FTD-0906 preserved the failed marker. The exactly scoped
FTD-0907 source-marker repair passes the complete inherited certificate
`75/75`. The pinned MSVC 14.44 Release build, focused CTest `1/1`, and
actualization/EFT chain `30/30` pass.

For a finite neutral region, the native ternary dipole
`d=sum_x s_x(x-r)` is origin independent. Its minimum nonzero support is one
`+1` and one `-1` site, and `e=d/|d|` is a signed-cubic-covariant polar axis.
At the two endpoints, project the existing flux and wave-velocity fields:
`q_+=e.J_+`, `q_-=e.J_-`, `p_+=e.W_+`, `p_-=e.W_-`. Their antisymmetric wedge

`ell=q_+ p_- - q_- p_+`, `chi=sign(ell)`

is a spatial scalar and is odd under canonical time reversal. The symmetric
square `d tensor d` loses the sign of the axis, while the Gram determinant is
`ell^2` and loses the sign of chirality. This is the most economical native-
type answer to what the BCC symmetric square forgets.

The stability result is conditional but exact. Under the imposed central
quartic memory Hamiltonian
`H=(p_+^2+p_-^2)/(2 mu)+kappa(q_+^2+q_-^2)^2`, `ell` is conserved. Its radial
effective potential has a strict positive minimum
`rho_0^6=ell^2/(4 mu kappa)` with curvature `24 kappa rho_0^2`. So a nonzero
wedge can be a bounded recursive orientation memory.

The clock consequence is a separation theorem. Nonzero `ell` necessarily
adds `ell^2/(2 mu rho^2)`, so the same central mode is no longer the pure
one-dimensional quartic oscillator whose period carries exactly `G*`.
Accordingly the minimum honest architecture is a critical radial G* clock
plus a bilateral chirality-memory mode. Existing native field types suffice
for representation; the production tick has not yet been shown to form,
retain, transport, or erase either observable, and the central memory law is
still imposed. The next gate is a target-blind production-observational
formation and maintenance campaign with energy and information accounting.

**RESULT — FTD-0908--0910 (2026-08-11): production forms durable carrier
hardware and persistent chirality intervals, but not yet protected pair
memory.** Both preflights passed (`30/30`, `28/28`); focused CTest passed `1/1`
in `44.46 s`; the frozen independent adjudicator passed `19/19`. The locked
verdict is Outcome A: 23/24 injected arms pass, all six live family-by-volume
cells satisfy `>=3/4`, and all eight empty controls remain empty. Across 78
production-ID pairs, 75 contain an eight-tick sign-stable interval
(min/median/max `8/21/56`).

The result does not establish a protected bit. All 78 identities eventually
flip chirality, with `1,779` consecutive-tick sign changes. A disclosed post-
hoc reconstruction of the already frozen rotated-negative null yields 69
persistent pseudo-pairs and exactly the same arm-pass pattern. Accordingly
the substrate has the carrier and instantaneous clockwise/counterclockwise
discriminator, but not yet a demonstrated binding/restoring gearbox. The
next locked gate is held-out pair-specific persistence against matched nulls,
plus a discrete `Delta ell`/central-torque ledger. Perturbation, work/erasure,
rectifier coupling, and G* synchronization come only after that gate.

**NEXT LOCKED — FTD-0911:** a 64-arm held-out campaign on new volumes
`{19,23}` and seeds `0x09110001..08`. Pair specificity is an exact integer
test on identical common tick support: the actual pairing must strictly beat
every fixed cyclic negative-endpoint derangement in at least 6/8 seeds of
every live cell. A separate parameter-free midpoint ledger tests
`Delta ell`, central torque, and kinetic alignment in the no-bath arms without
fitting `mu`, `kappa`, or a force law. Preflight passes `28/28`; the runner and
held-out corpus do not yet exist. FTD-0912 additionally freezes the compiled
runner and independent six-file adjudicator; its instrument preflight passes
`23/23`. No held-out tick has run.

**RESULT — FTD-0911--0913: valid Outcome D (`P-C-`).** Focused CTest passed
`1/1` in `116.25 s`; independent adjudication passed `20/20`. All six live
cells qualified, but only 6/41 qualified arms beat every fixed cyclic
derangement and no cell reached 6/8. All 15,191 live midpoint transitions
failed exact centrality, including `8,138/8,138` in no-bath arms, while the
bilinear identity itself passed to `2.22e-15`. The two-endpoint wedge is
therefore a readout, not a protected pair memory under current production;
the imposed FTD-0907 central law is not the current one-tick production law.
Do not proceed to pair perturbation/work. The next derivation target is a
closed-loop/topological carrier with a native defect barrier or invariant.

**LATEST — FTD-0914: the minimum closed spatial recursion is an exact
four-site ternary quarter-turn, but its production protection remains open.**
On one cardinal plaquette, the neutral word `(+1,0,-1,0)` and its directed
cyclic shift induce `R=[[0,-1],[1,0]]`, so `R^2=-I` and `R^4=I`. The dipole
orbit quarter-turns exactly. Its ordered transition bivector
`L=d_n x d_(n+1)` is constant and nonzero, reverses sign for the opposite
cycle, and reconstructs the next dipole as `(L x d_n)/|d_n|^2`. All 48 signed
cubic covariance arms pass, as do the equal radial/tangential energy split
and symmetric-square controls. The independent certificate passes `48/48`;
the pinned build and focused CTest pass `1/1`.

This is the simplest spatial realization of `i` and the first structurally
distinct successor to the failed endpoint wedge. It is recurrent conditional
on directed shift, not yet protected: forward/reverse visit the same four
instantaneous words, and the real-field lift contracts through `L=0` with no
energy floor. Next: preregister an observation-only production census of
exact plaquette shifts, support-specific run lengths, defect crossings, and
field/wave-velocity alignment. Only a held-out positive recurrence result may
open a barrier/work campaign. `G*`, `gamma`, Born/Bell, and production
integration remain untouched.

**LATEST — FTD-0915--0917: the production substrate forms the plaquette
clock face but does not run it.** FTD-0915 locked an all-elementary-plaquette,
64-arm held-out census. The first FTD-0916 corpus is preserved execution-
invalid because processed labels could not be reconstructed from raw site
identities/fields. The telemetry-only FTD-0917 repair changed no physics,
arm, support, threshold, or outcome. Its preflight passes `46/46`; the raw-
data adjudicator passes `33/33`.

Across the 48 injected arms there are 2,860 exact identity-bearing plaquette
exposures in 21 arms. Of 2,806 next-tick comparisons, 2,800 are stationary and
six lose support. There are exactly zero forward or reverse quarter-turns,
zero half-turns, zero adjacent defects, and zero full cycles. The locked
verdict is Outcome D: exposure without directed transport. All 16 empty
controls remain empty.

This closes current-production plaquette rotation/recurrence negative in the
locked matrix while retaining FTD-0914's exact representability theorem. The
next missing dynamics is now precise: a native time-odd circulation or
conjugate momentum in the plaquette `C4` doublet. A period constant cannot
start an inert mode, so `G*` remains downstream of deriving the generator.
Next: decompose the existing flux/wave action into plaquette harmonics and
derive—or close negative—the antisymmetric charge before any new production
term or held-out campaign.

**LATEST — FTD-0918: the native handedness charge exists, but the elementary
plaquette is not a closed clock body.** Projecting the existing flux and
wave-velocity fields gives `(q,r)=Pi_1 J`, `(p_q,p_r)=Pi_1 W` and

`L_P=q.p_r-r.p_q`.

This is exactly `C4` invariant, reflection odd, and time-reversal odd. Every
symmetric generator commuting with the quarter-turn is isotropic, and its
isolated kick--drift map conserves `L_P` exactly. Arbitrary source impulses,
common damping, and additive noise enter a closed torque ledger. The isolated
circular branch has equal kinetic and potential energy and nonzero signed
circulation; standing/radial branches have `L_P=0`.

The production boundary is exact. The plaquette's internal 18-point stencil
eigenvalue is `-25/6`, so `kappa=25/18`, `cos(theta)=11/36`, and the bare
internal map has no exact finite integer return. Worse, an exterior site just
beyond a nonzero corner receives Laplacian `q/3` on the first kick. The four
vertices are therefore not an invariant subsystem. The certificate passes
`48/48` without changing engine or CMake sources.

Interpretation: the substrate can distinguish clockwise from
counterclockwise in native real phase space; what remains missing is a
bounded source-balanced body that holds or recirculates that charge. Next:
derive an invariant larger `C4` mode, an explicit boundary source/return
path, a protected circulating defect, or a theorem-grade obstruction from
the full existing action. Only after that may `G*` be tested as a gearbox
ratio. Gamma, Born/Bell, context, and production integration remain open.

**LATEST — FTD-0937/0938: the live primitive body current closes the minimum
connection's direction input, but a closed source actuator cannot be directly
composed with the positive hop wake.** At one declared local phase, signed-
cubic covariance, linearity in the live `u_n`, evenness in the scalar quartic
coordinate, and zero value/first derivative at the critical point uniquely
give, inside the registered lowest-degree class,

`A_g(q,u)=g gamma q^2 u`.

The conditional source action has a positive Hamiltonian, conserves canonical
`P`, generates `Delta K=-g gamma Delta(q^2)u`, retains an exact pure-quartic
rest clock, and has zero switching work at `q=0`. Its full-cycle continuum
displacement is parallel to the live current and contains the exact inverse-
`G*` beta factor. This closes orientation at reference level only: `gamma`
and integer-hop normalization remain free.

The decisive composition test is negative. A full source cycle returns its
internal state and energy, but every nonzero local integer relocation leaves
the FTD-0933 field wake `Dbar(d)>0`. With the formed field otherwise unchanged,
the direct sum gains exactly that energy. A genuine common action must include
the source-field coupling before the hop and derive a named backreaction,
incoming debit, altered source cycle, or rejection. Subtracting `Dbar` from a
reservoir after reading it is not a derivation.

Direction plus wake energy still does not determine recoil. Even assigning
all wake energy to equal/opposite quadratic impulse gives
`|I|=sqrt(2 mu Dbar)`, which depends on independent reduced inertia `mu`.
The compact character separately leaves `p_*` free, and aggregate reciprocal
carry has no unique local owner. FTD-0940 subsequently supplies a target-blind
conditional local Hodge scalar transaction and identifies oriented `s^2`
occupancy flux as the one-hop neutral transport owner. It does not retain
accumulated winding, fix real momentum, or identify its time-centered chord
with the abrupt FTD-0933 wake. FTD-0941 subsequently proves the fixed bounded
finite-alphabet carry no-go and isolates expanding-support reversible token
export as the surviving reference class. The next gate is existing-L/R
realization followed by one common temporal ordering for carrier, quartic
source, and companion wake. The FTD-0937 parent is preserved invalid
at `126/128`; the one-comparison FTD-0938 repair passes all inherited
`128/128` gates. No engine/CMake/type/import/production change.

**PRIOR — FTD-0936: the raw C4 character has an exact even-label kernel, and
the formed reference body carries the canonically repaired primitive
character orbit.** FTD-0935's character exists for every integer label, but

`Xi_p=Xi_-p iff p is componentwise even`.

Thus a raw nonzero separation does not always retain its conjugation sign.
The FTD-0925 source endpoints are separated by `2e_x`, which lands exactly in
that kernel. The universal directional reading is corrected without
retracting the character or Moore-shell theorems.

For arbitrary nonzero integer `a`, the canonical ray repair is

`p4_prim=chi * a/gcd(|a_x|,|a_y|,|a_z|)`.

It is integer, has at least one odd component, preserves ordered-presentation,
time-reversal, and signed-cubic covariance, and always distinguishes its
conjugate. It removes separation multiplicity and therefore does not derive
physical momentum magnitude.

The later compact scaffold supplies the same structure independently through
its live current. The exact integrated-current arms are

`2*(-1,1), 2*(-1,-1), 2*(1,-1), 2*(1,1)`.

Primitive normalization gives an exact rotating `C4` character orbit. The
prepared FTD-0926 remainder/velocity Hamiltonian repeats it after four ticks,
but production does not contain that force and neutral recurrence is not
attraction or robust protection.

The decisive next boundary is `sum_n u_n=0`: any equal phase-blind linear
vector export cancels over the full cycle. A phase gate, retained directional
bias, incoming current, or nonlinear common action is necessary. FTD-0938
subsequently proves that the live `u_n` uniquely supplies the minimum
registered `g gamma q^2 u` connection direction, while direct composition
with the positive FTD-0933 wake fails energy closure and real recoil scale
remains non-identifiable. `G*` may time eligibility only after a separate
finite-tick clock law; it cannot choose direction or create recoil.
Certificate `132/132`, Outcome A; no engine/CMake or production change.

**PRIOR — FTD-0935: existing native snapshot data realizes the missing
compact C4 translation character, with an exact Moore-shell parity.** For a
neutral ternary pair with nonzero bilateral phase wedge, define

`a=x_plus-x_minus`, `chi=sign(ell)`, `p4=chi a`.

Reversing the ordered endpoint presentation flips both `a` and `chi`, so
`p4` is unchanged. Signed cubic transformations send `p4` to `Q p4`, while
canonical time reversal sends it to `-p4`. The exact character

`Xi_p4(d)=i^(p4.d)`

is therefore a cubic-covariant homomorphism from integer translations to
`C4`. It supplies the conjugation sign that FTD-0934 proved is absent from
the positive dressing square. In the minimum registered integer-linear
class, cubic equivariance forces every gearbox to be `m I3`; the odd classes
`m=1,3 mod 4` are the unique orientation-sensitive choices up to complex
conjugation.

On the 26 Moore steps, the self-character is `i^(chi |a|^2)`. Face steps
give `+/-i`; FCC edges give `-1` for both chiralities and therefore lose the
sign; BCC corners give `-/+i` and retain it. This is an exact mod-four lattice
pairing, not a Gaussian-prime split/inert theorem and not a CM/substrate
operator derivation.

FTD-0936 subsequently proves that the raw character distinguishes reversal
only when at least one label component is odd and supplies the primitive-ray
repair plus the compact-body current realization. The FTD-0935 result remains
kinematic. FTD-0911/0913 still close protected two-endpoint
production memory negative; only `p4 mod 4` is visible, so unwrapped physical
momentum, winding/carry ownership, `p_*`, common action, vector recoil,
protected compact-body character, source formation/recovery, separate
critical-quartic `G*` cadence, Born/Bell, context, and hiding remain open.
Next: realize the same character on the later formed compact C4 body/current
and couple it to the FTD-0933 wake through one reciprocal local action.
Certificate `98/98`, Outcome A; no engine/CMake or production change.

**PRIOR — FTD-0934: the field dressing supplies an exact translation
geometry, but its symmetric energy square cannot supply direction.** Put all
four formed C4 companion phases into the positive native energy space and let
integer translations act by `pi(d)`. Then

`b(d)=pi(d)Y-Y`

is an exact one-cocycle and the phase-averaged hop wake is

`Dbar(d)=||b(d)||^2`.

It is conditionally negative definite, its square root is a metric on the
nondegenerate compact-source translation orbit, and its polarization recovers
the exact inner product between displacement cocycles. This is a natural
field-dressing geometry, not a fitted matter metric.

The decisive boundary is

`Dbar(d)=Dbar(-d)`.

The spectral factor is `|1-exp(-ik.d)|^2=2[1-cos(k.d)]`: a symmetric square.
It retains displacement cost but discards the conjugation sign that
distinguishes forward from backward. A scalar wake-only action therefore
prefers rest and cannot choose a directed nonzero hop or vector recoil from
cubic-isotropic rest data.

The missing datum is the unsquared translation character

`chi_k(d)=exp(i k.d)`, `k in T^3`.

Its imaginary part is odd under `d -> -d`. This is the exact spatial analogue
of why a symmetric square loses clockwise/counterclockwise information.
Internal C4 `i` supplies temporal orientation, but it is a spatial scalar; it
must be paired with a polar axis/current to direct translation. The native
character label is Bloch-torus data, not yet unwrapped physical momentum.

Next: identify whether an existing current, remainder/velocity mode, flux/wave
mode, or defect orientation realizes this character for the formed body, then
derive one common action that generates equal/opposite character increments,
owns the winding carry, pays the FTD-0933 wake, and fixes physical scale.
Source formation, autonomous hopping, physical mass/vector recoil,
exceptional/slow mobility, universal ternary closure, recovery, production,
physical L/R identity, separate critical-quartic `G*` synchronization,
Born/Bell, context, and hiding remain open. Certificate `279/279`, Outcome A;
no engine/CMake or production change.

**PRIOR — FTD-0933: one local source hop has an exact positive re-dressing
wake, but the wake metric is not yet inertial mass or vector recoil.** Let
`T_d` be integer lattice translation and let `(q_n,p_n)` be the formed gapped
`C4` companion. Moving the compact source by nonzero `d` without
instantaneously translating the extended field leaves

`e=(I-T_d)q_n`, `z=(I-T_d)p_n`.

The native source-centered invariant gives the exact hop mismatch

`D_n(d)=H_C4(e,z)=integral 2[1-cos(k.d)]h_n(k) dk`.

It is finite and strictly positive for nonzero compact source data. If it
vanished, the square-summable companion would be invariant along the infinite
`d`-translation orbit and therefore zero, forcing the source to vanish.
After the source is held at its new center, subtraction of the translated
companion again gives the source-free native map. The translation multiplier
`1-exp(-ik.d)=O(|k|)` cancels the worst free `1/|k|` infrared factor, so
coarea plus Riemann--Lebesgue gives sitewise re-dressing. The positive global
mismatch is retained as an outward wake, not erased.

The phase-averaged principal-chart continuation has exact Hessian

`G_ij^dress=2 integral k_i k_j hbar(k) dk`.

This tensor is positive definite and becomes scalar under cubic covariance,
but it is only a reference dressing curvature. Fractional spectral
translation is nonlocal, and FTD-0893 still requires an independently defined
total-momentum linearization before any physical mass can be identified. A
future common action that counts the wake as field excitation must allocate
the opposite scalar change among source, internal, incoming-field, or
environmental sectors. The field theorem does not select that payer and
cannot determine a vector recoil.

Next: derive a local source coordinate/current and a common source-field
action whose variation produces both the wake allocation and reciprocal
momentum transfer, or preregister an exceptional/slow mobile carrier. Source
formation, physical mass and momentum scale, universal ternary closure,
recovery, production, physical L/R identity, separate critical-quartic `G*`
synchronization, Born/Bell, context, and hiding remain open. Certificate
`150/150`, Outcome A; no engine/CMake or production change.

**PRIOR — FTD-0932: the fixed-center `C4` companion now forms and tracks
causally, while primitive spatial translation hits a native resonance
cone.** For the frozen field map

`W_(n+1)=W_n-KJ_n+f_n`, `J_(n+1)=J_n+W_(n+1)`,

take a compact source with `f_(n+2)=-f_n`. Its temporal phase is `z=i`.
The exact native driven pole becomes `kappa-2`, and the C18 band
`0<=kappa<=16/9` leaves the strict spectral gap `2/9`. Hence the unique
periodic companion is

`q_n=-(2I-K)^-1 f_n`, `p_n=q_n-q_(n-1)`.

The actual update never reads this inverse profile. Starting from zero field,
every finite tick stays within the compact-source C18 cone. The error relative
to `(q_n,p_n)` follows the source-free native map and preserves the exact
positive tick invariant. Its Fourier amplitude is at worst `1/|k|` at the
massless point, so in three dimensions coarea plus Riemann--Lebesgue gives
sitewise `J_n-q_n->0` and `W_n-p_n->0`. Fixed-center time-dependent
companion tracking is therefore closed without damping or a fresh-port
stream. The steady source work sums to zero every four ticks; for a skew
commuting quarter-turn doublet it vanishes step by step. Source formation is
still unpaid.

Rigid translation is different. A source moving by primitive Moore step `u`
each tick would need denominator

`D_u(k)=kappa(k)-4 sin^2(k.u/2)`.

Near zero, `D_u=|k|^2/3-(k.u)^2+O(|k|^4)`. Its signature is `(+, +, -)`
for all 26 nonzero Moore steps, producing a regular codimension-one resonance
cone. A generic compact nonzero-total source then has a non-square-integrable
normal pole and no exact finite-energy rigid co-moving halo. This is a
one-site-per-tick no-go, not a no-go for slow, deforming, exceptional neutral,
or nonlinear carriers.

Next: derive reciprocal source slowing/recoil and a common action that books
the wake energy, or construct an exceptional/slow mobile carrier that avoids
the cone. Source formation, universal ternary closure, recovery, production,
physical L/R identity, scale, and separate critical-quartic `G*`
synchronization remain open. Certificate `259/259`, Outcome A; no
engine/CMake or production change. Born/Bell, context, and hiding remain
open.

**PRIOR — FTD-0931: the fixed-source static halo now has a native positive
causal formation mechanism.** In the frozen undamped field sector,

`W_(n+1)=W_n-KJ_n+f`, `J_(n+1)=J_n+W_(n+1)`

uses the existing `(flux,wave_vel)` canonical pair. With `J_*=K^-1 f` and
`e=J-J_*`, it preserves the exact positive source-centered tick invariant

`H_rad=||W||^2/2+<e,Ke>/2-<W,Ke>/2`.

Switching on the fixed compact source places exact formation debit
`<f,K^-1 f>/2` into the field. Each finite tick remains inside the C18 causal
cone. On the uncontained three-dimensional scaffold, the exact retarded
solution converges at every fixed site: `J_n(x)->J_*(x)` and `W_n(x)->0`.
The coarea/Riemann--Lebesgue proof is qualitative and uses no damping or
fitted rate. Global mismatch energy remains conserved while leaving every
fixed region as outgoing field history.

Three spatial dimensions are the minimum generic compact-monopole threshold:
near zero, `K(k)~|k|^2/3`, so the Green/formation measure is
`r^(d-3)dr`, finite exactly for `d>2`. This does not uniquely exclude `d>3`,
and neutral sources have a different infrared count.

Finite grounded fields provide the decisive control. Their nonzero positive
invariant prevents instantaneous convergence; only the Cesaro average
settles. Static formation is therefore genuinely an uncontained local-
dispersal effect, not finite-box damping or an `L->infinity` extrapolation.

The fixed-source static-halo debt from FTD-0929/0930 is closed without an
indefinite fresh-port stream. FTD-0932 subsequently closes fixed-center
rotating-companion tracking and proves a primitive-translation resonance
boundary. Next: derive the source side—formation and reciprocal translational
recoil—then test exceptional/slow mobility, coupled recovery, and production
behavior. The blank-port
ecology remains relevant only to the coordinate-relaxation route. `G*` is not
needed for static formation and remains a separate synchronization/eligibility
question. Certificate `154/154`, Outcome A; no engine/CMake or production
change. Born/Bell, context, scale, and hiding remain open.

**EARLIER — FTD-0930: the missing positive local formation layer exists, but
the self-maintaining reservoir and uncontained massless halo do not yet.**
For either the dynamic companion operator `M=2I-K` or the static operator
`M=K`, color the C18 lattice by the eight parity triples. No coupled sites
share a color, so an active residual

`u=(Mq+b)/sqrt(d)`

can rotate exactly into one fresh complete port:

`(u,a)->(a,-u)`.

On `a=0`, this is target-blind local coordinate relaxation. The field
potential lost at the update appears exactly in the outgoing port, so
positive field-above-minimum plus port energy is conserved. The endpoint is
fourth order and has an exact symplectic cotangent lift; the frozen FTD-0886
clocked Hamiltonian makes each color layer local and positive. Within the
registered onsite canonical class, one complete port pair is necessary and
sufficient.

Each color map is an `M`-orthogonal projection. A complete eight-color sweep
strictly contracts the error on every specified finite grounded region for
both `2I-K` and `K`. This gives a causal computation of finite dynamic
companions and finite static fields without reading a target profile.

It does not close the uncontained massless halo. The static symbol still
approaches zero as `(2/3)(1-cos(theta))`, so no volume-independent geometric
rate exists, and boundary-independent local convergence/persistence remains
open. Every sweep also consumes one fresh complete port pair per site. The
next front is therefore the actual reservoir gearbox: derive blank-port
origin, three-dimensional routing and backpressure, reset/return or open
export, autonomous eight-color scheduling, and moving-source work/recoil.
The physical `(X,Q)` to `(L,R)` identity remains open. Certificate `183/183`,
Outcome B; no engine/CMake or production change. `G*`, gamma, Born/Bell,
context, and hiding remain separate and open.

**EARLIER — FTD-0929: the self-dual companion has a unique causal quasilocal
PreparationMap, but exact finite-time formation and positive reservoir work
remain open.** The FTD-0928 on-section identity forces

`Q=-(2I-K)^-1 U`.

Because the dynamic operator has exact gap `2/9`, the radius-one recurrence

`Q^(0)=0`, `Q^(N+1)=(K/2)Q^(N)-U/2`

converges target-blindly with error at most
`(9/2)(8/9)^N ||U||`. It is restriction-consistent inside complete causal
cones and preserves the registered `C4` covariance at every depth.

The exact profile cannot be formed in finite causal depth. A Laurent witness
at `x=-2+sqrt(3)` proves the registered 53-site source is not divisible by
`2-K`, so its unique companion has infinite support. Preparation is therefore
a causal convergence process, not instantaneous creation of a completed
field.

One local contraction layer has an exact cotangent lift if the overwritten
field is exported into a fresh complete history pair. Retain that outgoing
history and the enlarged dynamics is reversible; discard or disperse it and
the reduced actual record is lossy. This is the first exact mathematical
candidate for unactualization as loss of locally irrelevant detail without
deletion from the full ontology.

The lift is not yet physical formation: its expanding mode excludes a
positive conserved quadratic metric. Fresh-port origin/recycling, positive
source work, autonomous stopping, recovery, and nonlinear transfer remain
open. The massless static halo is separate and lacks the dynamic sector's
uniform geometric rate. Existing left/right field pairs have enough storage
capacity, but their identity and normalization as `(X,Q)` are not derived.
Next: construct or exclude a positive local reservoir/port microdynamics, and
treat static-halo formation as a separate massless transport front.
Certificate `98/98`, Outcome B; no engine/CMake or production change. `G*`,
gamma, Born/Bell, context, and hiding remain separate and open.

**EARLIER STILL — FTD-0928: a one-way source has no discrete generating function,
but its minimum positive reciprocal self-dual completion exists.** For a
frozen triangular composition

`z'=Az`, `y'=By+c(z)`,

the full symplectic cross-block condition is `C^T Omega_f B=0`. Since the
field map and symplectic form are invertible, every nonconstant source
`C=Dc/Dz` makes the map nonsymplectic. This closes the discrete-generating-
function escape route for the FTD-0927 one-way recurrence on its frozen phase
space.

The minimum reciprocal exchange-symmetric reference instead shifts
`X=J-H`, introduces a field-shaped canonical companion `Q`, and uses the
potential

`V=1/2<X,KX>+1/2<Q,KQ>-<X,(K-2I)Q>+1/2||X-Q||^2`.

In self/anti variables `(X+Q)/sqrt(2)` and `(X-Q)/sqrt(2)`, it factorizes into
stiffnesses `2` and `2K`. The self-dual sector is the exact FTD-0926 `C4`
map; the anti sector is stable for every positive native mode
`0<K<=16/9`. On `X=Q`, the source relative to free propagation is exactly
`(K-2I)Q`; off that section the action supplies equal reciprocal mismatch
feedback.

This is a selected reference action, not yet substrate formation. The new
companion has no derived spatial PreparationMap to the record/current, and
the factorized linear action leaves an empty self-dual phase plane empty. A
scalar account cannot be a canonical formation reservoir; at least one
complete canonical pair is required per formed phase plane. A species
quarter turn transfers one already prepared equal-metric pair reversibly,
but it does not form the full profile or static halo. Next: derive a local,
restriction-consistent matter/current-to-companion PreparationMap and a
nonlinear full-profile transfer with reciprocal work from a complete positive
reservoir. Certificate `100/100`, Outcome B; no engine/CMake or production
change. `G*`, gamma, Born/Bell, context, and hiding remain separate and open.

**PRIOR — FTD-0927: continuity now generates the ternary record and exact
midpoint field source, while the minimum canonical reciprocal coupling closes
negative for the required velocity recoil.** From the present complete record
and velocity alone,

`Q_n=rho_n v_n`, `rho_(n+1)=rho_n-div_c Q_n`

generates the exact next neutral 22-site ternary record on all four registered
arms. It is radius-one local and closes simultaneously with the existing
remainder–velocity gearbox. This is an invariant-section theorem, not a
universal ternary rule: a fixed one-site rational-current control produces
neighbor values `±1/10`.

The midpoint source no longer reads the desired next record:

`U_n=-G_C grad_c rho_n+(G_C/2)grad_c div_c Q_n+G_C curl_c Q_n`.

It equals the direct midpoint Hodge source exactly, splitting into a static
`C4`-invariant component and an antipodal rotating doublet. The inherited
affine kick–drift field representation then advances target-blindly. Its
positive field invariant is `1+k_h/2`; together with the matter carrier the
scalar total is `77/25+k_h/2`, and the corrected modeled unit-tick formation
debit is `26 pi/25+1+k_h/2`.

The reference recurrence is compositional, not yet one common action. In the
minimum local differentiable Hamiltonian class, a source `S(rho,v)` independent
of canonical remainder `r` has
`partial_R dot(v)=0` but `partial_R dot(r)=-partial_v S`. The reciprocal field
term therefore enters the remainder equation rather than generating the
FTD-0926 velocity impulse. Next: derive or close negative a discrete
generating-function, remainder-dependent-current, or bond-current canonical
gearbox, while also identifying the paying formation reservoir. Universal
production, attraction/recovery, mobility, scale, critical-quartic `G*`,
gamma, Born/Bell, context, and hiding remain open. Certificate `144/144`;
Outcome B; no engine/CMake or production change.

**PRIOR — FTD-0926: the causal scaffold now has an exact local reversible
velocity gearbox, but the ternary record and field recoil remain prescribed.**
At every scaffold site the existing subcell remainder and velocity form the
onsite doublet. Initialize

`r_0=(v_0-v_1)/2`

and apply the same rule everywhere:

`v'=v-2r`, `r'=r+v'=v-r`.

The map reads no coordinate, scaffold sign, target arm, phase table, `G*`,
context, outcome, or Born weight. It generates exactly
`v_0,v_1,-v_0,-v_1`; both fields return after four steps, every remainder
component stays within `2/5`, and the registered speed ceiling remains
`8/25<1/3`.

For `z=(r,v)`, its matrix is `M=[[-1,1],[-2,1]]`. Exactly,
`M^2=-I`, `M^4=I`, and `M` preserves both the standard onsite symplectic form
and the positive metric `G=[[2,-1],[-1,1]]`. The selected Hamiltonian

`H_rv=omega sum_x (|r_x|^2-r_x.v_x+|v_x|^2/2)`

has exact flow `M` when `omega T=pi/2`. Its registered invariant is `52/25`,
or `26 pi/25` at unit-tick sampling. Pointwise speed equality makes every
isotropic endpoint kinetic-work arm zero. Together with the static halo, the
modeled preparation debit is the positive subtotal `E_h+26 pi/25`.

This closes the reference remainder–velocity generator, not the whole clock.
The ternary snapshots `s_n` are still prescribed; production does not contain
this force; the evanescent field has not been shown to supply the vector
impulse or receive reciprocal recoil; and no reservoir forms the scaffold.
The next exact gate is one local source–matter–field recurrence that generates
the ternary transition, locks the matter/field phase, and closes total energy
and formation without reading the desired arm. Only then may a
critical-quartic `G*` envelope be attached. Certificate `106/106`; no engine,
CMake, production, import, type, gamma, Born/Bell, context, or hiding result
changed.

**PRIOR — FTD-0925: a finite existing-type scaffold carries the exact dipole
current causally, but its quarter-turn generator and formation remain open.**
The FTD-0924 point current is a parity-sublattice transfer. Splitting each of
its two unit flows equally among one direct path and four shortest transverse
detours gives a 19-site `C4`-covariant current inside Chebyshev radius two.
Radius zero and one are impossible under the selected speed bound because the
central edge remains a cut edge and forces `|v|^2=8`. At radius two the exact
peak is

`max_x |v_n(x)|^2 = 8/25 < 1/3`,

with causal margin `1/75`. The equal five-channel weights are the unique
minimax solution only inside the frozen five-shortest-channel class; no
global radius-two or cardinality optimum is claimed.

The 19 current centers have odd cardinality. Adding one zero-current axial
site produces a 20-site neutral, `C4`-invariant ternary scaffold `h`. Because
the dipole endpoints are disjoint from it, `r_n=h+s_n` remains ternary and
the prescribed velocity field closes the unchanged live product and exact
continuity:

`r_n v_n=j_n`, `r_(n+1)-r_n+div_c j_n=0`.

The velocity sequence is `v_0,v_1,-v_0,-v_1`. Starting at zero, every subcell
remainder component stays strictly inside the cell and returns exactly after
four ticks; no locked flag or manifested hop is used.

Compact support has a price. The scaffold's central gradient has support 45
and norm squared 22, so its exact field is affine: a static positive-energy
halo plus the rotating evanescent `C4` doublet. The ideal field, interaction,
and matter-reaction work still vanish on every arm. Formation of the static
halo is not paid by that zero-work recurrence.

The next exact gate is now a local reversible quarter-turn generator for the
19 velocity/remainder pairs, together with a positive scaffold/formation
Hamiltonian, source recoil, reset, and perturbation recovery. The current
itself no longer requires an independent-current purchase. `G*` remains
downstream as a critical-quartic envelope/cadence problem. Certificate:
`132/132`; no engine, CMake, production, import, selected type, gamma,
Born/Bell, context, or hiding result changed.

**PRIOR — FTD-0924: the dipole orbit has an exact compact continuity
current, but existing endpoint matter cannot carry it.** The paired dipole
transition cancels the FTD-0576 checkerboard obstruction. With

`Q_0=2(e_y-e_x) delta_0`, `Q_n=S^n Q_0`,

all four arms satisfy

`s_(n+1)-s_n+div_c Q_n=0`.

The current is point-supported at the rotation center, rotates with the same
handed `C4` phase, and has `||Q_n||^2=8`. Its central curl has vector support
six and the same norm squared. The exact midpoint Hodge seed

`q_n=grad_c((s_n+s_(n+1))/2)-curl_c Q_n`

has norm squared `39/4`; its `kappa=2` evanescent field retains the exact
source-locked quarter-turn. The complete conditional FTD-0576 ideal ledger
is lossless on every arm: field work, interaction change, and required
matter-reaction work are each exactly zero.

The production boundary is now precise. No current supported on the two
actual endpoints, or even their four-site current/next union, solves central
continuity. The successful current lies at a site where `s_n=0`, so it is not
the unchanged production `j=s v`. Manifesting one center hub compiles the
current algebraically but requires `|v|^2=8>1/3` in one tick and adds a static
electric gradient, so that control is not production-admissible and is not
the pure `C4` source.

The next exact gate is a radius-locked search for a finite manifested bridge
scaffold minimizing site count and peak speed while preserving continuity,
the antipodal source doublet, and zero-work closure. If no causal finite
scaffold survives, an independent current/connection becomes the honest
type-priority candidate and must be priced before adoption. Positive source
storage, autonomous switching/formation, reset, perturbation recovery, and
separate critical-quartic `G*` synchronization remain open. Certificate:
`138/138`; no engine, type, import, production, gamma, Born/Bell, context, or
hiding result changed.

**PRIOR — FTD-0922/0923: a compact ternary core supports an exact localized
recursive field clock, but the core sequence is not yet autonomous.** The
exact order-four stiffness `kappa=2` lies above the free C18 band ending at
`16/9`. Therefore

`R_2=(2I-K)^-1=(1/2) sum_(m>=0)(K/2)^m`

exists, and every compact source has the rigorous tail bound

`||(P_>=r)R_2 q|| <= (9/2)(8/9)^r ||q||`.

A neutral two-site ternary dipole and its four quarter-rotated snapshots
source four evanescent profiles. With `P_n=F_n+F_(n+1)` and
`U_n=(K-2I)F_n`, the unchanged kick--drift advances exactly
`(F_n,P_n)->(F_(n+1),P_(n+1))`. The profiles are orthogonal/equal norm,
their modal circulation is nonzero and constant, and the exact midpoint
field-side source work is zero on every tick: the return is radial while the
orbit increment is tangential.

This is the first exact localized recursive field architecture in the branch,
but not yet physical clock hardware. The reference freezes `v=0` while
changing `s_n`; its continuity residual has norm squared four every tick.
The next gate is a continuity-compatible local core trajectory including the
`curl(sv)` source, reciprocal field reaction, switching work, and positive
storage, while retaining the evanescent orbit or a controlled deformation.
Only afterward may a critical-quartic `G*` envelope be coupled.

FTD-0922's first certificate is preserved invalid at `71/74` because two
uncontained support checks were run on the periodic witness. FTD-0923 repairs
only that domain selection and passes the inherited `74/74`. No engine, type,
import, production, `G*`, gamma, Born/Bell, context, or hiding result changed.

**PRIOR — FTD-0921: the Moore coat does not localize the source, and the
live current tie exposes a three-way ontic choice.** Passing the eight real
zero/Nyquist budgets is not enough for compact support. For a finite Laurent
source,

`d.U=-D s`, with `D=d_x^2+d_y^2+d_z^2`.

An exact complex point with `D=0` leaves the coated plaquette return nonzero
for every real `kappa`, proving that its relaxed density/current inverse must
be noncompact.

The central operators themselves are not the end of the road. A compact
transverse carrier `J=curl_c A` has the exact compact relaxed return
`curl_c[(K-kappa)A]`. Production cannot compile it locally because current is
tied to manifested state: `j=s v`. Transversality forces compact `s=0`, which
then forces `j=0`.

The exact existing escape is a global two-periodic ternary scaffold. There
are `3^8=6561` parity-class-constant scaffolds and `2^8=256` that are nonzero
on every class and can gate arbitrary local current. Every nonzero example
fills the uncontained lattice, so treating it as vacuum hardware would be a
new background/energy selection, not a local clock derivation.

The conservative next route is therefore a compact live ternary core with a
noncompact evanescent field tail. Since the free band ends at `16/9` and the
exact one-tick quarter-turn stiffness is `2`, `kappa=2` is the first exact
outside-band control. Derive decay, source work/reaction, reversal, and
positive autonomous storage before `G*`. Certificate: `69/69`; no engine,
type, import, production, gamma, Born/Bell, context, or hiding result changed.

**PRIOR — FTD-0920: the existing central-Hodge source cannot close the bare
plaquette, but the Moore coat removes the exact linear range obstruction.**
For any desired invariant isotropic body, the unique boundary-return impulse
is

`U_ret=(K-kappa I)J`.

The coded coupling source `-G_C grad_c s+G_C curl_c(sv)` is modewise complete
except at the eight zero/Nyquist corners `k=pi epsilon`. There it is exactly
zero. On `L=4`, this is a 24-dimensional vector cokernel, equivalently eight
componentwise parity budgets: a central gradient/curl source has zero total
impulse in every site-parity class.

The elementary plaquette occupies four blind fibers. Two see free stiffness
`4/3`; the other two see `16/9`. Its required return can lie in the source
range only if one `kappa` equals both values, which is impossible. This is a
stronger closure than the earlier leakage witness: even arbitrary continuous
independent density/current fields cannot make the four-site body exact using
the frozen central operators.

The inherited FTD-0577 Moore coat kills every nonzero blind corner, while the
neutral plaquette kills the zero mode. Its coated return therefore lies in
the relaxed global periodic source range. The live task is now sharply
defined: compile that return into finite-range ternary `s` and support-gated
`j=sv`, satisfy central continuity, give the source reciprocal reaction and a
positive autonomous work reservoir, and prove formation/stability. Only then
test quartic `G*` synchronization. Certificate: `68/68`; no engine, CMake,
production law, `G*`, gamma, Born/Bell, context, or hiding result changed.

**PRIOR — FTD-0919: the free field has exact global circulation, but no
nonzero compact finite-dimensional clock body.** For any skew modal generator
`A` and symmetric free stiffness `K`, the production-order kick--drift obeys

`Delta L_A=-(h/2) J^T[A,K]J+J^T A U`, with `L_A=J^T A P`.

Thus a source-free charge is conserved for every state exactly when
`[A,K]=0`. Such generators rotate only within degenerate eigenspaces. An
exact `L=4` periodic witness exists: `sin(pi x/2)` and `sin(pi y/2)` form a
physical `C4` doublet, have stiffness `2/3`, and carry conserved circulation.

That witness is delocalized and quotient-dependent. The Laurent-polynomial
proof shows the unchanged C18 convolution has no nonzero finite-support
eigenvector. By the finite-dimensional spectral theorem it therefore has no
nonzero compact invariant doublet and no finite-support finite-rank `A`
commuting with `K`. The full free band is `0<=kappa<=16/9<2`, while an exact
unit-tick quarter-turn requires `kappa=2`. Certificate: `54/54` exact checks;
no engine, CMake, or production result changed.

This closes the proposed larger free `C4` body negative while retaining the
global modal calendar. The next honest route must be non-free: derive an
existing reciprocal source that returns boundary torque and energy, or price
a minimal nonlinear confinement term with support/tail, reaction, reversal,
reset, work, and dissipation ledgers. `G*` stays downstream of local
maintenance and critical-quartic closure; gamma, Born/Bell, context, and
hiding remain open.

**PRIOR — FTD-0904 (2026-08-11): an oriented even self-pair rectifies the
rest clock exactly, and exposes the precise missing orientation memory.** The
first immutable five-source certificate passes `74/74` without repair. The
pinned MSVC 14.44 Release build, focused CTest `1/1`, and isolated
actualization/EFT chain `29/29` pass.

For a polarized clock `D=q e`, retain a local unit polar axis `e` and a
time-odd clockwise/counterclockwise branch `chi`. The imposed reference law

`A(q,e,chi)=chi gamma q^2 e`

is even in the clock sheet, preserves signed-cubic covariance when `e`
transforms, and folds at `P=0` into the same positive critical quartic
`Lambda=lambda+gamma^2/(2M)`. Unlike the odd signed-self-pair connection, its
common velocity has one direction through the full cycle. Exact beta
identities give

`Delta C_T=-(4 sqrt(pi) chi gamma/(M G*)) a sqrt(m/(2 Lambda)) e`

and

`(mean Cdot . e)/a^2=-4 chi gamma/(M (G*)^2)`.

Thus `G*` controls traversal time, `1/G*` controls displacement per cycle,
and `1/(G*)^2` controls mean speed per squared amplitude inside this imposed
reference action. This is exact conditional mathematics, not a numerical
match or a substrate prediction.

The symmetry price is equally exact. A nonzero polar function of `D` alone
cannot be both even and inversion-equivariant: evenness gives `F(-D)=F(D)`,
while inversion covariance gives `F(-D)=-F(D)`. The reference model must
therefore retain `e` and `chi`. This is precisely the sign information lost by
a symmetric square: `q^2` keeps intensity but loses polar direction and
clockwise/counterclockwise sheet. Native formation, maintenance, and erasure
of that memory are now the decisive open gate. `i` can encode orientation but
does not derive `e`, `chi`, or the magnitude of `gamma`.

**PRIOR — FTD-0902--0903 (2026-08-11): a positive linearized connection
cannot preserve the critical clock, but the signed self-pair connection does
so exactly at rest.** FTD-0902 is preserved execution-invalid at `80/81`
because of one substitution-order defect. The exactly scoped FTD-0903 repair
passes inherited `81/81`; the pinned MSVC 14.44 Release build, focused CTest
`1/1`, and isolated actualization/EFT chain `28/28` pass.

For any positive cyclic connection with `B=DA(0)`, the rest-sector clock
Hessian is the Gram matrix `B^T M^-1 B`. It is positive semidefinite and
vanishes exactly when `B=0`. Thus the FTD-0901 detuning is an order theorem,
not an accident of the scalar linear ansatz.

The first registered escape is the existing signed self-pair coordinate
`U(D)=|D|D`. The imposed connection `A(D)=gamma U(D)` has `DU(0)=0` and, at
canonical rest `P=0`, its positive connection energy is quartic:
`Lambda=lambda+gamma^2/(2M)`. Hence the exact rest Hamiltonian is
`Pi^2/(2m)+Lambda|D|^4` and retains
`T A=sqrt(pi) G* sqrt(m/(2 Lambda))`. Mechanical common momentum still obeys
`Delta K=-gamma Delta U` and composes with reciprocal carry conditional on
supplied `p_*`.

The price is explicit. `U` is `C1` but not `C2` at the origin. Nonzero common
momentum produces a direction-dependent quadratic ray term, so exact
quarticity is rest-sector only. A symmetric polarized rest cycle has zero net
common drift: this is an oscillatory gearbox, not a matter-translation or
propulsion mechanism. `i` still supplies orientation but does not derive
`gamma`; equal self-dual energy partition remains unadopted.

FTD-0904 supersedes only the trailing zero-transport reference boundary by
supplying an imposed oriented even connection. It does not derive its retained
axis/chirality, controller work, physical normalization, production map, or
finite-tick cadence.

**PRIOR — FTD-0899--0901 (2026-08-11): the minimum common/relative connection
is an exact mechanical gearbox, and continuous engagement detunes the exact
critical-quartic clock.** FTD-0899 and FTD-0900 are preserved
execution-invalid; the exactly scoped FTD-0901 representation repair passes
the inherited certificate `87/87`. The pinned MSVC 14.44 Release build,
focused CTest `1/1`, and isolated actualization chain `27/27` pass.

For the imposed reference action
`L=(M/2)|Cdot|^2+(m/2)|Ddot|^2+gamma D.Cdot-lambda|D|^4`, the conserved
canonical momentum is `P=M Cdot+gamma D`, while the mechanical common
momentum is `K=P-gamma D`. Hence `Delta K=-gamma Delta D`: relative motion
now exchanges mechanical common impulse without violating the additive
canonical momentum or the positive Hamiltonian. The exact discrete-gradient
map also conserves total canonical angular momentum, generates equal/opposite
channel impulses, composes with reciprocal carry conditional on supplied
`p_*`, and reverses under the signed step.

The connection curvature is nonzero for nonzero `gamma`, so this is a genuine
reference coupling rather than a total derivative. But `i` fixes only the
orientation of that curvature, not its magnitude. At rest the coupled clock
potential has Hessian `gamma^2/M`; therefore an always-on nonzero connection
destroys the exact zero-Hessian critical quartic. Setting `gamma=0` restores
the quartic and simultaneously removes the gearbox.

The next gate is no longer “find any common action.” It is one preregistered,
context-blind critical-clock-preserving mechanism: either a local phase clutch
with all switching work booked, or a positive compensated action. It may not
read `G*`, measurement context, outcome, or Born weight. `gamma`, `p_*`,
physical channel identification, absolute mass, production, and finite-tick
cadence remain open.

**PRIOR — FTD-0898 (2026-08-11): the selected relative quartic now generates
the FTD-0897 impulse and carry exactly, while the common/matter mode remains
decoupled.** The frozen first certificate passes `97/97` without repair. The
pinned MSVC 14.44 Release build and focused CTest `1/1` pass.

In the orthogonal common/relative chart, the exact discrete-gradient quartic
recursion conserves `H_D=Pi^2/(2m)+lambda D^4`. Its endpoint generates
`Delta P_L=+Delta Pi/sqrt(2)` and `Delta P_R=-Delta Pi/sqrt(2)`; composing
that increment with the reciprocal-carry transaction reconstructs every
principal chart and winding, including multiple-zone crossings, and reverses
under the signed inverse step.

The same continuum quartic obeys
`T A=sqrt(pi) G* sqrt(m/(2 lambda))`. This is the first exact reference
gearbox placing the local impulse cycle, reciprocal carry, positive energy,
and the lemniscatic traversal factor in one model. It does not yet identify
the finite-step orbit with integer global ticks, and `G*` is not present in
the discrete update.

The load-bearing boundary is now sharper: `P_C` is an exact invariant. The
reference model therefore cannot transfer its internal reaction impulse into
an actual matter/field common mode. The next gate is one preregistered common
local action that breaks that decoupling while conserving full energy and
total momentum, retaining reversible carry, and fixing or honestly pricing
the physical unit `p_*`.

**PRIOR — FTD-0897 (2026-08-11): an integer reciprocal-carry triplet closes
the lifted pair ledger exactly, but it is not yet energy-bearing substrate
hardware.** The frozen first certificate passes `89/89` without repair. The
pinned MSVC 14.44 Release build, focused CTest `1/1`, and actualization/EFT
chain `25/25` pass.

For principal labels `k_1,k_2` receiving a supplied opposite increment
`+q,-q`, let `c_1,c_2` be the reciprocal-zone carries. The unique update
`W'=W+c_1+c_2` gives exact conservation
`k_1'+k_2'+2 pi W'=k_1+k_2+2 pi W`. Applying `-q,+q` returns the complete
state, and event sequences telescope. This derives a recursive discrete
history ledger, conditional on the increment, without selecting where the
reservoir physically lives.

The important negative result is energetic. Periodic band energy cannot see
`W`; the same carry law admits inequivalent reservoir energies, and an
opposite quasimomentum increment need not conserve band energy. The next
load-bearing target is therefore one local matter--field action that derives
`q`, an exact work/backreaction update, carry ownership/transport, and the
physical unit `p_*`. A conjugate phase/action cell is a reference candidate,
not yet native hardware.

**PRIOR — FTD-0894--0896 (2026-08-11): the lattice supplies exact torus-valued
quasimomentum, and a globally real momentum needs winding history or
nonlocality plus an independent unit.** The invalid `75/81` parent and `80/81`
first repair are preserved; the scoped FTD-0896 repair passes inherited
`81/81`. The pinned native MSVC 14.44 Release build, focused CTest `1/1`, and
actualization chain `24/24` pass.

For the local uncontained translation algebra modeled by `Z^3`, characters
are labelled by `T^3=R^3/(2 pi Z^3)` and add exactly modulo reciprocal lattice
vectors. No continuous group-homomorphic section `T^3->R^3` exists. A
finite-range translation-invariant spectral generator is periodic and cannot
equal the globally unwrapped coordinate; the exact principal-branch
sawtooth generator is infinite range and discontinuous at the zone edge.

The translation-spectral routes are therefore explicit: keep torus-valued
quasimomentum, choose a nonlocal branch, or retain a winding/history triplet
`w` so `k_tilde=k+2 pi w`. The third route still requires native winding
dynamics and an independently fixed momentum scale `p_*`. It is a candidate,
not a selected type. A new local stress/bond-impulse state remains another
unexcluded route.

**PRIOR — FTD-0893 (2026-08-11): dressed inertia has an exact conditional
formula, and its missing gearbox is the physical total-momentum map.** The
source-locked certificate passes `57/57` with no repair. For complete
time-odd energy Hessian `A` and independently defined physical momentum
linearization `B`, fixed-momentum minimization gives `M=B A^-1 B^T`.

For the complete time-odd matter--field tangent state near rest, let `A` be the
positive energy Hessian and let `B` be an independently defined rank-three
additive physical momentum map. Exact constrained minimization gives
`y*=A^-1 B^T(B A^-1 B^T)^-1P` and the dressed inertial tensor
`M=B A^-1 B^T`. This is the minimal way a moving field coat can contribute to
inertia: through its odd dynamics and its participation in `A`/`B`, not merely
through a static energy offset.

The same `A` under `B->sB` gives `M->s^2M`. Therefore exact energy, static
stability, and even measured co-moving dressing do not identify physical mass
without the momentum normalization. The selected common action still lacks an
exact total `B`; the natural spline-Poynting candidate fails coupled recoil.
FTD-0896 now prices the quasimomentum route: a real lift must carry winding
history or be nonlocal, and its physical unit is still independent. The next
FTD-0897 step supplies the unique conditional carry update but exposes the
remaining physical debt: one action must generate its increment, carry
ownership/transport, energy/backreaction, and impulse normalization, or a
local substrate stress state must close the same exchange. It must yield the
same `M` from constrained energy curvature, impulse/center velocity, and
matter--field momentum partition. Absolute mass, stable matter, production,
`G*`, Born, and Lorentz recovery remain open.

**PRIOR — FTD-0891--0892 (2026-08-11): the selected constituent phase space
already contains the three-pair reaction carrier, while static data cannot set
its absolute mass.** The scoped FTD-0892 repair passes inherited `68/68`; the
invalid FTD-0891 parent remains preserved. Exact Helmert reduction gives the
collective `(X,P)` sector, conditional selected dispersions compose with
`M_coll=sum epsilon_a/c^2`, and exact `Z^3` translation still does not supply a
continuous total field-matter Noether momentum.

**PRIOR — FTD-0889--0890 (2026-08-11): scalar recoil is forbidden by cubic
symmetry, while an exact relativistic source-transport gearbox exists
conditionally once a local vector is supplied.** The FTD-0889 first run remains
execution-invalid at `64/68` because of three representation defects and one
dependent total. The representation-only FTD-0890 repair passes inherited
`68/68`. The pinned native Release build, focused CTest, and coupled 21-test
actualization chain pass.

The result is sharp. `Fix(T1u)={0}`, so a scalar magnitude cannot choose a
spatial direction. The minimum orientation-free registered carrier is three
canonical pairs `T1u+T1u`; one pair still suffices conditionally when the
matched local field impulse supplies a direction. Given the selected
relativistic dispersion, the cotangent energy chart and free drift/current are
exact. Local conservation fixes `sin^2(eta)=K_req/E_res`, so the FTD-0888 equal
split is not universal. Native formation and coupling of the vector common
action, derivation of `E0`/mass from a stable localized source, interacting
matter, production, `G*`, Born, and Lorentz recovery remain open. No production
path or selected-type currency changed.

**PRIOR — FTD-0887--0888 (2026-08-11): an autonomous phase circle can
compile parity at reference level, and one minimum canonical reaction pair can
receive an exact, energy-paid recoil-like channel.** The FTD-0887 first run
remains execution-invalid at `68/72` because of two wrapped prose markers, one
unsimplified determinant comparison, and one dependent total. The
representation-only FTD-0888 repair passes inherited `72/72`. The native
Release build, focused CTest, and coupled 20-test actualization chain pass.

For each color, define source-centered residual/history/reaction generators
`L_ua`, `L_ar`, and `N_r`. One phase circle is split into six disjoint smooth
windows in the ordered sequence

```text
(L_ua^0, L_ar^0, N_r^0, L_ua^1, L_ar^1, N_r^1),
alpha = (pi/2, eta, pi/2, pi/2, eta, pi/2).
```

With window integrals normalized to their target angles,

```text
H = Omega I + 6 Omega N + Omega sum_j (6 alpha_j/pi) rho_j(theta) G_j
```

is positive, returns clock action after one cycle, and applies the complete
ordered map. The windows have disjoint interiors, so no different-color
commutation assumption is needed. The external integer `n mod 2` parity switch
is therefore not mathematically required at this reference level. The phase
circle, window origin/order/shape, frequency, and scale are still imposed—not
substrate-native clock hardware.

On the ready slice, the added reaction pair gives

```text
E_hist  = cos^2(eta) E_res,
E_react = sin^2(eta) E_res.
```

The FTD-0886 history-only endpoint is positive-energy saturated: a nonzero
zero-initialized reaction channel cannot be added while leaving full history
energy unchanged. Reaction energy is paid by reduced history. One scalar is
not symplectic, so one canonical pair is minimum in the registered onsite
direct-sum class. This is another instance of the existing selected pair type,
not a sixth contextual-actualization selected type. Equal splitting at
`eta=pi/4` is `[SELECTION]`, unique only conditional on imposed exchange
symmetry of the history and reaction output channels.

The next dynamics target is physical rather than merely canonical: identify
the reaction pair with a spatial ternary source, derive its mass/inertia and
intercell reaction transport, and couple that source to a physical open-history
environment. In parallel, the phase windows need native formation, origin, and
scale. Only after those gates come 3D routing and production. The quartic-`G*`
calendar remains a separate gearbox problem. Born/Bell/Lorentz recovery and
completeness remain untouched.

**LATEST — FTD-0885--0886 (2026-08-11): the fixed-source Gauss gate has a
positive canonical clocked lift, and its apparent source work is exactly
interaction-energy exchange rather than a missing battery.** The FTD-0885
first run remains execution-invalid at `60/64` because of three marker defects
and one dependent total; the marker-only FTD-0886 repair passes inherited
`64/64`. The native Release build, focused CTest, and coupled 19-test
actualization chain pass.

For one active normalized cell,

```text
y = d_x J / sqrt(6),  s = q_x / sqrt(6),  u = y - s,
a = e_x / sqrt(6),
N = (u^2 + a^2 + pi_u^2 + pi_a^2) / 2,
L = a pi_u - u pi_a.
```

The exact identities `{N,L}=0` and `|L|<=N` make
`H=omega I+omega N+sigma omega(1-cos(theta))L/4` positive and give the exact
forward/reverse canonical quarter-turn in one clock cycle. On the
zero-conjugate section it is precisely the FTD-0882 update. The energy
decomposition

```text
E_raw = (y^2+a^2)/2,
U_int = -s y+s^2/2,
E_raw+U_int = (u^2+a^2)/2,
Delta E_raw = w,  Delta U_int = -w
```

closes the local fixed-source ledger. Source formation, motion, and recoil are
not included, so this is not yet a natural matter Hamiltonian.

FTD-0884's square-root battery remains exact as an imposed one-amplitude/
Lagrangian-section reference, but it is not phase-complete: its cotangent lift
changes oscillator energy by `-w(1+p_b^2/b^2)`, and a state-dependent
phase-blind action drain is not symplectic. A complete-pair open-history shift
is canonically reversible, while the finite cyclic capacity boundary remains.

At the FTD-0886 boundary, the next dynamics target was an autonomous common
parity controller coupled to dynamical source formation/motion/recoil and a
physical open or bilateral complete-pair history rail. FTD-0888 above closes
the external parity switch and canonical reaction-channel existence only at
imposed reference scope. Native phase-window hardware and physical spatial
source inertia/motion remained open at that boundary. FTD-0890 above now proves
the scalar-to-vector obstruction, three-pair orientation-free minimum, and
conditional exact source-transport chart. Native vector common-action
formation/coupling and mass-scale derivation remain open. The quartic-`G*`
calendar remains a separate
gearbox problem. Born/Bell/Lorentz recovery and completeness remain untouched.
No sixth selected type or production path was added.

**LATEST — FTD-0883--0884 (2026-08-11): finite exact ready hardware is
possible, but finite cyclic hardware does not recycle freshness for free; a
positive battery closes the work ledger only at imposed reference scope.** The
FTD-0883 first run remains execution-invalid at `54/56` because of one wrapped
prose marker; the single-marker FTD-0884 repair passes inherited `56/56`. The
native Release build, focused CTest, and coupled 18-test actualization chain
pass.

An explicit cyclic bank of `C` initially zero signed ports supplies exactly
the first `C` fresh checkerboard layers and retains the complete inverse state.
On a generic nontrivial preparation, the first nonzero output returns to the
cursor at layer `C+1`, so it cannot be reused as a fresh port. This is a
capacity theorem for that explicit bank, not a universal finite-memory no-go:
an open or unbounded rail, or a justified compressed exact-real memory, remains
logically available.

For positive reference energy `E_b=b_x^2/2` and local source work
`w_x=q_x(e_x-r_x)/6`, strict reserve plus sign preservation uniquely gives

```text
b_x' = sgn(b_x) sqrt(b_x^2 - 2 w_x),
b_x  = sgn(b_x') sqrt((b_x')^2 + 2 w_x).
```

The battery loses exactly the work supplied to the field/port system, and the
complete field+bank+battery state reverses exactly. This answers where the
reference work can be booked, but not what natural substrate degree of freedom
the battery is: its quadratic type, law, sign branch, and reserve scale are
imposed, with no canonical Hamiltonian/symplectic or native recharge theorem.

At the FTD-0884 boundary, the next target was a canonical Hamiltonian source
reservoir with native formation and recharge. FTD-0886 above refines that
diagnosis: fixed-source local canonical work closes as interaction energy.
FTD-0888 further closes the external parity switch and canonical reaction
channel at imposed reference scope. FTD-0890 further closes the cubic
representation and conditional source-transport chart. The surviving target is
native phase hardware, vector common-action formation/coupling, mass-scale
derivation, stable interacting source dynamics, and complete-pair history,
eventually coupled to 3D routing and moving sources.
The quartic-`G*` calendar remains a separate gearbox problem.
Born/Bell/Lorentz recovery and completeness remain untouched. No sixth selected
type or production path was added.

**LATEST — FTD-0881--0882 (2026-08-11): the matched static Gauss record can
be prepared by local reversible dynamics at fresh-port reference scope.** The
FTD-0881 first run remains execution-invalid at `58/60` because of one wrapped
prose marker; the single-marker FTD-0882 repair passes inherited `60/60`. The
native Release build, focused CTest, and coupled 17-test actualization chain
pass.

For a cell residual `r_x=d_xJ-q_x` and signed environment port `e_x`, the
six-face local gate is

```text
J'   = J + d_x^T(e_x-r_x)/6,
e_x' = -r_x,
(r_x/sqrt(6), e_x/sqrt(6)) -> (e_x/sqrt(6), -r_x/sqrt(6)).
```

Same-color cells have disjoint face support. Fresh-zero ports therefore make
each checkerboard half-layer an affine orthogonal projection. Alternating the
two colors from empty flux converges to `J_s=D^TL^+q` without evaluating the
inverse Laplacian in a local gate. Keeping every outgoing signed port and
reversing the layers recovers the exact finite history. No fixed probe-
independent finite layer count can solve every size.

The exact local work ledger yields, at this declared boundary,

```text
E_field = E_history = ||J_s||^2 / 2,
W_source = ||J_s||^2.
```

This is the precise self-dual result: half the supplied source work remains in
the actual static record and half is carried by reversible history. It is not
a claim about all matter or the universe. The next missing dynamics are now
sharper: autonomous fresh-port generation/recycling, a positive source
reservoir, local stopping, moving-source continuity, boundaries and finite
capacity, physical scale, production migration, and synchronization to the
separate quartic-`G*` calendar. Born/Bell/Lorentz recovery and completeness
remain untouched. No new selected type was added.

**LATEST — FTD-0877--0880 (2026-08-11): Gauss structure gives an exact
matched canonical record sector, but not an exact live production
projector.** The repaired source-locked certificate passes `66/66`; the
invalid FTD-0877--0879 executions remain preserved. The native Release build,
focused CTest, and coupled 16-test actualization chain pass.

On the already selected oriented-face complex, let `D` be divergence,
`L=DD^T`, `q=DJ`, and `p=L^+DP`. Then

```text
J = J_T + D^T L^+ q,
P = P_T + D^T p,
Omega = Omega_T + dq wedge dp,
{q,p} = Pi_Q.
```

Thus a neutral ternary configuration has the exact axis-free static record
section `(J,P)=(D^T L^+ g s,0)`, and matched curl recursion preserves it. The
record coordinate is local, but its canonical conjugate is not: a fixed-range
translation-invariant right inverse of `D` is impossible across arbitrarily
large probes. The inverse-Laplacian solve is intrinsically relational.

The live cell-centred Gauss pass is not this reduction. Its central
divergence/gradient composition and 18-point SOR operator have different
Fourier symbols; on even probes the central derivative is additionally blind
to Nyquist checkerboards. Finite SOR and the default manifested-site skip make
the live path an approximate constraint relaxation, not an exact projector.

The loss statement is now precise: projecting an arbitrary field to a fixed
record erases its longitudinal discrepancy and is noninjective. Retaining that
discrepancy reconstructs the input exactly. A physical reversible completion
still needs an environment carrier/dynamics. Native dynamic formation of the
record section, production bond actuation, routing, scale, finite-support
uncontained formulation, and quartic-`G*` synchronization remain open. No new
selected type or production path was added.

**FTD-0876 (2026-08-11): the minimum local canonical carrier coordinates are
already present in the substrate's free-wave state.** The first locked
certificate passes `55/55`; the native Release build and coupled 15-test
actualization/transport chain pass.

For step `h>0`, the exact chart

```text
(J_(n-1), J_n) <-> (J_n, P_(n-1/2)),
P_(n-1/2) = (J_n-J_(n-1))/h
```

is a bijection. The production `Voxel` already stores these quantities as
`flux` and `wave_vel`, so its three vector components supply three local
canonical pairs per site. No Hilbert space or extra oscillator coordinate is
needed for the carrier type.

This requires an ontological bookkeeping correction: `(J,s)` is the
commutative configuration algebra, while the phase-complete instantaneous
Markov algebra is `(J,P,s)`. The additional coordinate contains exactly the
one-step local history needed by the second-order wave equation; it is not a
noncommutative potentiality beable.

The free symmetric-stiffness kick/drift is exactly symplectic and invertible.
The complete production tick is not: damping is conformally symplectic,
nonidentity Gauss projection is noninvertible on the unconstrained phase
space, Langevin reads bath randomness, and genesis/loss/boundary maps require
separate ledgers.

Thus carrier-coordinate availability is closed, but record preparation,
amplitude/scale recovery, insertion of the FTD-0875 bond actuator, routing,
constrained Gauss dynamics, environment-complete loss, robustness, and
quartic-`G*` synchronization remain open. No production path changed and no
sixth selected type was added.

**LATEST — FTD-0875 (2026-08-11): the selected parity rail now has an exact
local canonical Hamiltonian lift and bond-energy current.** The first locked
certificate passes `56/56`; the native Release build and coupled 14-test
actualization/transport chain pass.

The undoubled scalar rail has a common symplectic form on every even finite
open rail, but it pairs site `j` with the boundary-mirror site `L-1-j`. Thus
the correct obstruction is locality, not abstract symplectic existence. In
the registered onsite-direct-sum local class, a scalar site has no
nondegenerate skew form; one canonical pair `(q_j,p_j)` per site is the minimum
and is sufficient.

With matching `M_n`, number `N`, bond generator `L_n`, and a common harmonic
clock `(theta,I)`, the imposed Hamiltonian

```text
H_(n,sigma) = Omega I + Omega N
            + sigma Omega (1-cos(theta)) L_n / 4
```

implements the exact forward or inverse parity layer after one clock cycle.
Prepared record energy `Omega a^2/2` moves exactly one bond, with a local
antisymmetric current and zero endpoint residual. On the special actual
section `p_j=0`, `L_n=0` throughout, so the clock has zero backreaction while
the Hamiltonian gradient still transports the record. That special orbit is
not evidence that physical clock hardware is free.

This closes the native intersite-Hamiltonian *reference-lift* sub-debt, not
the substrate recovery debt. Native formation of the onsite canonical
doublet, the energy scale, multidimensional routing, finite boundaries,
congestion control, production coupling, robustness, synchronization to the
separate quartic `G*` calendar, and operational hiding remain open. No
production path changed and no sixth selected type was added.

**LATEST — FTD-0874 (2026-08-11): alternating oriented bonds turn the local
quarter-turn into an exact finite-horizon causal record rail.** The first
locked certificate passes `48/48`; the native Release build and the coupled
13-test actualization/transport chain pass.

At global tick `n`, activate disjoint nearest-neighbour bonds whose left rail
coordinate has parity `n mod 2`, and apply

```text
R(a,b) = (-b,a).
```

For a prepared isolated record `x_0=s`, this gives exactly `x_t=s` after `t`
ticks, with every other retained site zero. The sign is unchanged, speed is
one cell per global tick, and reversing the layers recovers the full history.
A fixed matching cannot move the pulse beyond its first two-site block, so
both alternating matchings are needed in the declared disjoint-bond class.

Backpressure is retained rather than erased: an occupied pair exchanges both
labels. That does not guarantee progress—a fully occupied rail remains fully
occupied. More generally, an injective time-homogeneous map cannot take a
distinct predecessor into a literal fixed `done` state. Reversible one-shot
clearing therefore requires continuing record/controller motion or retained
environment history. FTD-0874 uses outward record motion.

This is a selected reference scheduler using existing tick and coordinate
parity, not production substrate physics. Native intersite Hamiltonian
formation, axis/routing selection, collision and sustained-backpressure
resolution, reciprocal finite boundaries, production coupling, robustness,
and synchronization to the separate quartic `G*` calendar remain open. No
production path changed and no sixth selected type was added.

**LATEST — FTD-0873 (2026-08-11): the oriented ternary quarter-turn now has
an exact energy-accounted Hamiltonian reference actuator.** The first locked
certificate passes `48/48`; the native Release build and the coupled 12-test
actualization chain pass.

Embed the actual pair as one canonical carrier mode `(p,q)=a(s,o)` and add one
independent clock pair `(theta,I)`. The imposed harmonic law

```text
H = Omega I + Omega A + e sigma (Omega/4)(1-cos theta) A
```

gives identity for `e=0`, `R` for `e=1,sigma=+1`, and `R^-1` for
`e=1,sigma=-1` after one complete clock cycle. Four phase-space dimensions
are minimum inside this independent phase-gate class. The record-energy scale
`Omega a^2/2` is imposed, not derived.

The clock transiently exchanges at most `Omega A/2` with the interaction and
returns it exactly at the endpoint. Gate-zero switching costs zero in this
interaction account; switching away from gate zero generally does not. Thus
zero net cycle work is not a work-free-controller claim.

The live boundary has moved to dynamic one-shot scheduling and native
realization. Repeating an active cycle gives `R^2=-I`, while norm-only gating
remains active after emission. Native carrier/clock formation, derivation of
the scale, backpressure-safe handoff, protected cubic transport, production
energy-current coupling, robustness, and synchronization to the separate
quartic `G*` calendar remain open. No production path changed.

**LATEST — FTD-0872 (2026-08-11): the missing actual-layer controlled
permutation is the unique oriented ternary quarter-turn.** The first locked
certificate passes `40/40`; the isolated CTest and the coupled 11-test
actualization chain pass.

For the ordered latch/output pair over `F_3`,

```text
R(s,o) = (-o,s),
R^2 = -I,
R^4 = I.
```

It is the unique matrix with `R e1=e2`, `det R=1`, and `R^T R=I`. Therefore a
ready event transfers in one reversible step,

```text
(s,0) -> (0,s),
```

and `R^-1` performs absorption. The ordered swept area has opposite signs for
the two directions, while the symmetric square is blind to that sign. This is
the minimum actual-layer realization of the already-established two-register
`i` structure; it does not derive complex potentiality, Gamma, or `G*`.

Backpressure cannot be hidden inside a fail-closed wrapper: applying `R` only
when the port is empty and identity otherwise maps both `(1,0)` and `(0,1)` to
`(0,1)`. Readiness must be scheduled, or the occupied value must leave through
a reciprocal/reflected output. FTD-0873 now closes the imposed harmonic
reference trajectory, scale declaration, and transient work ledger. Native
formation/scale recovery, dynamic one-shot scheduling, protected cubic
transport, production energy-current coupling, robustness, and synchronization
to the separate quartic `G*` calendar remain open. No production path changed.

**LATEST — FTD-0870/0871 (2026-08-11): the completed signal can reversibly
uncompute the actual ternary latch, so reset needs no extra bit, reset trit, or
logical bath.** FTD-0870 is preserved invalid at `39/40` because one prose
marker crossed a Markdown line break. The exact one-repair FTD-0871 wrapper
passes `40/40`; the isolated CTest passes `1/1`.

Encode `{-1,0,+1}` as `Z_3`. If the completed oriented signal decodes the same
value `u=d(E)=s` as the latch, then

```text
U(s,E) = (s minus d(E), E) = (0,E),
U^-1(s',E) = (s' plus d(E), E).
```

The reset is exact in one discrete update and reversible on the joint
latch/signal state. Bare `s -> 0` would be three-to-one, and energy alone would
identify the two signs; the oriented signal already supplies the minimum three
retained labels. After an empty-port handoff the local state is ready while the
output still carries `(s,B)`.

This sharpens FTD-0869. Its smooth finite-time ODE obstruction remains true for
the selected continuous coordinate `x`, and its cusp law remains one optional
continuous realization. But the actual ternary layer does not logically
require a dissipative bath. Endpoint degeneracy does not prove zero physical
controller work.

FTD-0872 now supplies the exact controlled quarter-turn at the logical
actual-layer interface. Its physical actuation from native event/reference
states and clearing through a protected cubic rail remain open. A finite
`N`-trit rail cannot injectively retain every history longer than `N` without
signed tail export or other memory. Native formation, controller work,
robustness, production coupling, quartic compensation, and `G*`
synchronization remain open. No Born/Bell, Lorentz, or completeness claim
follows.

**LATEST — FTD-0864/0865 (2026-08-11): the reciprocal event exchange now has
an autonomous Hamiltonian lift, and that lift separates the harmonic phase
reference from the nonlinear `G*` clock.** FTD-0864 is preserved invalid at
`39/40`; its one-repair FTD-0865 certificate passes `40/40`, and the isolated
CTest passes `1/1`.

A scalar amplitude swap has determinant `-1`, so it cannot be a Hamiltonian
time map on one canonical pair. The minimum lift retains full matter and signal
modes. With common/relative actions `(I_c,I_r)`, the imposed reference law is

```text
H = omega*I + nu*(I_c+I_r)
    + epsilon*chi*(1-cos(theta))*I_r.
```

For `nu=omega` and `chi=omega/2`, one reference cycle is exact identity when
`epsilon=0` and exact matter/signal mode swap when `epsilon=1`. The reference
does real work during the pulse:

```text
I_min = I_0 - I_r,
I(T)  = I_0.
```

For emission from an empty signal, `I_r=B/2`, so the exact capacity condition
is `I_0>B/2`. Completed nonoverlapping cycles reuse the reference without
cumulative depletion; interrupted or overlapping pulses remain open.

The same construction cannot use an uncompensated quartic clock as a universal
load-blind gearbox. For nonlinear `K(I)`, the pulse angle satisfies

```text
dXi/dI_r = chi^2 integral[g(theta)^2 K''(I)/K'(I)^3] dtheta.
```

The quartic action law `K(I)=c I^(4/3)` is strictly convex, so this derivative
is positive. A fixed coupling therefore produces different swap angles for
different event loads. The clean next choices are a separate isochronous
orientation reference gated by the quartic calendar, an additional compensating
reservoir/controller, or a declared fixed-load sector. Dynamic eligibility,
native mode formation, cubic/production coupling, and operational hiding are
still unresolved; no Born/Bell or completeness claim follows.

**LATEST — FTD-0863 (2026-08-11): the phase standard can orient an exact
matter/signal exchange without carrying the event energy.** The locked exact
certificate passes `36/36`; the isolated `ftd::eft` witness passes `1/1`.

Let `beta=(q,p)` be a nonzero canonical reference with conserved action
`I_*=(q^2+p^2)/2`. It defines the orthonormal phase frame

```text
e = beta/sqrt(2*I_*),
f = J*e,
D = a*f + b*e.
```

The reference-orthogonal coefficient `a` carries the signed event and `b` is a
spectator. Opening the FTD-0856 reciprocal gate swaps matter amplitude `m`
with `a` while leaving `b` and `beta` unchanged. Starting from
`m=s*sqrt(2B), D=0` therefore gives

```text
m' = 0,
D' = s*sqrt(B/I_*)*J*beta,
|D'|^2/2 = B,
sign(beta wedge D') = s.
```

The same involution absorbs the signal back into matter. The signal can begin
at zero because the separate reference supplies orientation; the reference
action remains `I_*`, so only the signal receives `B` and there is no double
count. This is a refinement of the existing `SEL-CA-PHASE-RAIL`, not a sixth
selected type.

The ideal reference rotation preserves action exactly. A travelling phase
helix still requires `kappa-omega in 2*pi*Z`; a periodic `N`-site ring also
requires `N*kappa in 2*pi*Z`. Those winding equations leave `omega` selected:
they do not derive the critical quartic period or connect it to `G*`. Native
reference formation, robust maintenance/backreaction, cubic signal transport,
eligibility control and work, production energy-current accounting, and
operational hiding remain the next physical gate.

**LATEST — FTD-0862 (2026-08-11): a prepared phase standard turns the
quarter-turn pump into a faithful bounded export rail at selected reference
scope.** The repaired source-locked certificate passes `36/36`; FTD-0861
remains preserved invalid at `35/36` after one prose-marker mismatch.

Let the nonzero baseline have action `I_*>0` and phase

```text
phi[j,n] = phi0 + kappa*j - omega*n.
```

The reference follows an outward one-cell-per-tick carrier exactly when
`kappa-omega` is an integer multiple of `2*pi`. Loading the baseline with the
FTD-0860 signed quarter-turn then makes the event locally recoverable at every
later depth:

```text
B = I(carrier) - I_*,
s = sign(baseline wedge carrier).
```

This is the cleanest current answer to clockwise versus counterclockwise. The
symmetric action `I` cannot see orientation; the antisymmetric relational area
against a co-moving phase reference can.

The finite rail is recursively stable because it exports rather than
accumulates action:

```text
Delta H_ex = B_in - E_tail,
H_ex <= N*B_max.
```

Exporting the full tail pair retains sign; exporting scalar energy alone does
not. Including incoming and outgoing environment pairs makes the fixed-control
step symplectic and injective. The isolated `ftd::eft` witness passes `1/1`.

The cost is now explicit. The nonzero phase calendar and directed protected
rail are a separately booked `[SELECTION]`, not production C18. FTD still has
to derive or physically realize the phase source and maintenance work, a
cubic/protected mode, native event hook, relative/tail energy-current ledger,
controller, and operational hiding of the selected direction. `G*` has not
been connected to `omega`; Born and Bell are untouched.

**LATEST — FTD-0860 (2026-08-11): a nonzero relative carrier can absorb
event energy exactly, but the minimum map is lossy.** The repaired source-locked
certificate passes `36/36`; FTD-0859 remains preserved invalid at `31/36`.

For `I=(q^2+p^2)/2>0` and `J(q,p)=(-p,q)`, the selected event transaction is

```text
z' = sqrt((I+B)/I) s J z,
I' = I+B,
theta' = theta + s pi/2.
```

It is target blind, symplectic on the punctured pair, exactly invertible when
the event controls `(s,B)` remain known, and exchanges its two signed
orientations under time reversal. Here `J^2=-1` is the real meaning of `i`:
`i` supplies the quarter-turn, while energy conservation supplies the radial
gain. The gain is not a Lorentz gamma.

Zero cannot start this process rotation-equivariantly: an empty isotropic pair
has no phase, and the only vector fixed by every rotation is zero, which cannot
carry `B>0`. On an arbitrary background, `F_+(z)=F_-(-z)` and output action is
only `I+B`. Thus the action pump cannot recover the erased event from its
unlabelled output. This is acceptable only as a declared lossy
unactualization channel. Faithful history remains the separate signed rail
`a=s sqrt(2B)`; assigning energy to both mechanisms would double count `B`.

The isolated `ftd::eft` witness passes `1/1`. Production remains untouched.
FTD-0862 above closes the reserved prepared-rail and bounded-export branch at
selected reference scope; the remaining physical gate is its native phase
source/maintenance, protected cubic realization, production ledger, event
coupling, controller-work account, and operational hiding.

**LATEST — FTD-0858 (2026-08-10): production has an event trigger and an exact
characteristic chart, but not a physical reciprocal port.** The repaired
source-locked certificate passes `40/40`; FTD-0857 remains preserved invalid.

With fixed seed, tick, and local data, the production genesis/evaporation
predicates are deterministic, Moore-local, and target blind. However, they read
only common fields `(J_L+J_R,W_L+W_R)`. Arbitrary antisymmetric changes leave
those trigger inputs unchanged while changing the relative receiver. Therefore
an accepted event does not determine the incoming amplitude, its energy, or
whether the emission port is empty.

The relative pair does admit the exact local chart

```text
incoming = (relative momentum + oriented strain)/sqrt(2)
outgoing = (relative momentum - oriented strain)/sqrt(2),
```

which preserves energy/current and swaps correctly under orientation and time
reversal. But the production axial dispersion obeys
`sin^2(theta/2)=(1/3)sin^2(k/2)`, giving trace defect
`(8/3)sin^2(k/2)` from a one-cell shift. At the FTD-0858 stage, the next honest
dynamics were a target-blind common-to-relative transducer or a protected/
dispersive port-clearing law. FTD-0860 above now resolves the abstract
nonzero-carrier branch and sharpens the remaining physical debt to loss/rail,
localization, export, ledger, and controller choices. The isolated FTD-0858
`ftd::eft` witness passes `1/1`; production remains untouched.

**FTD-0856 (2026-08-10): the minimum reciprocal record barrier is exact.** The
source-locked certificate passes `32/32`.

Logic forces two distinctions beyond the ternary record itself. First, an
identical complete deterministic state cannot both remain fixed and change, so
the local input must distinguish hold from exchange. Second, an emitted pulse
and its time reverse have opposite causal orientations; collapsing incoming
and outgoing amplitudes to one unlabeled scalar destroys precisely that
directional information.

The minimum attaining boundary is

```text
S[g] = [[1-g, g],
        [g, 1-g]],        g in {0,1}.
```

It acts on `(matter, incoming)` and returns `(matter', outgoing)`. For `g=0`
it is identity: the record is strictly protected. For `g=1` it swaps:

```text
(s sqrt(2B), 0) -> (0, s sqrt(2B))   emission
(0, s sqrt(2B)) -> (s sqrt(2B), 0)   absorption.
```

The matrices are symmetric orthogonal involutions, so the boundary conserves
energy and signed content and is its own reciprocal law. Production already
has a `locked` hold flag and dual field plus conjugate velocity capacity, but
it has no protected incoming/outgoing modes, no physical eligibility law, no
controller-work account, and no matter/field scatterer. A local clock may
constrain eligibility only together with a physical activation event; clock
phase alone would make every quiescent record emit. The isolated
`ftd::eft::scatter_reciprocal_record_port` witness now passes its focused
Release CTest `1/1`; it is not connected to `Voxel` or the production tick.

**LATEST — FTD-0855 (2026-08-10): the event energy and ready-port gearbox are
now exact at reference scope.** The repaired source-locked certificate passes
`32/32`; the FTD-0854 `29/32` verifier-defect parent remains preserved invalid.

The adopted production diagnostic already supplies the event account:

```text
B = E_REST + kinetic energy = gamma E_REST > 0.
```

When evaporation makes a manifested record void, that exact amount disappears
from the diagnostic matter account. This is conditional on the imposed
`E_REST` role; it is not a derivation of a dimensional energy scale from the
bare ternary ontology.

The geometric gearbox is equally direct. For the six-ray radial relative mode,

```text
Q = 12 a,
D = Q/sqrt(12),
K = D^2/2 = Q^2/24.
```

So the FTD-0853 cubic shell and FTD-0852 causal history rail are the same
receiver in different coordinates. The one-cell rule

```text
D[0]'   = s sqrt(2B)
D[j+1]' = D[j]
```

moves the occupied port outward while inserting the new event. It therefore
creates the next ready port dynamically, preserves every prior signed event on
the half-line, and increases receiver energy by exactly `B`.

Production has not inherited this closure merely because the coordinates
exist. Its drift ledger omits rest and pure relative energy, its bidirectional
dual field is not a protected directed rail, evaporation does not route the
event into it, and there is no reciprocal record barrier or full erased-state
receiver. Those—not `B` or abstract readiness—are now the live dynamics.

**LATEST — FTD-0853 (2026-08-10): the odd event now has an exact local cubic
deposit at reference scope.** On the preregistered ready surface

```text
Q0 = sum_nu nu . (W_L,nu - W_R,nu) = 0,
```

the six face-neighbour update

```text
W_L,nu' = W_L,nu + s sqrt(B/6) nu
W_R,nu' = W_R,nu - s sqrt(B/6) nu
```

changes no common field and transfers exactly the released event energy `B`.
Its post-event coordinate is `Q1=s sqrt(24B)`, so the erased sign and energy
are recoverable, and subtracting the pulse recovers the arbitrary compliant
background. The map is therefore injective on its declared reduced domain.

The geometry is economical without being overclaimed: the face orbit has six
directions, versus twelve edges and eight corners, so it is the minimum
nonzero full-cubic orbit for an equal-orbit one-tick first-shell deposit. This
does not establish a universal receiver minimum or recover every production
label erased by an event.

Production already has the dual coordinates and face neighbours, but it does
not derive `B`, form a target-blind ready port, write this impulse, count pure
relative energy, prove port clearing, protect the actual record reciprocally,
or lift the map to the complete erased state. Those are now the next physical
dynamics; Born targets, contexts, outcomes, `G*`, and cadence do not enter the
deposit law.

**LATEST — FTD-0852 (2026-08-10): odd event energy now has an exact causal
history carrier.** The source-locked certificate passes `32/32`. The selected
update

```text
D[0]'   = s sqrt(2B)
D[j+1]' = D[j]
```

moves every older pulse one causal cell outward while accepting the new event.
It is local, injective on a half-line, recursively ready, and satisfies an
exact energy-current continuity equation. The global tick is therefore also a
history-depth organizer: older unactualized detail lies farther down the
causal rail.

The finite-rail boundary is not hand-waved away. When a pulse reaches it,
exporting only its squared energy destroys its sign. A deterministic completion
must continue the signed tail amplitude into further environmental degrees of
freedom, accept recurrence, or declare the system open. This is scoped to the
shift rail; exact-real natural extensions can encode history differently.

Production contains a nontrivial piece of this architecture. L/R fields are
advanced by identical local operators and receive equal matter sources, so
their difference is a homogeneous candidate carrier. But no event deposits
the odd pulse into it; the aggregate energy ledger sees only the L/R sums and
therefore assigns zero energy to a pure relative pulse; and the bidirectional
wave stencil is not the exact one-way clearing map. The next gate is the local
event-to-relative-field transaction plus a reciprocal record barrier—not a
new probability fit.

**LATEST — FTD-0851 (2026-08-10): the minimum physical receiver of signed
unactualization is now exact.** The source-locked certificate passes `30/30`.
An energy ledger alone cannot remember whether `+1` or `-1` was erased. A
receiver valid even at zero exported energy minimally needs the odd label and
energy account `(chi,B)=(s,B)`.

For positive exported energy the two duties compress into one signed real
amplitude,

```text
a = s sqrt(2B),       a^2/2 = B.
```

The simplest selected bilateral realization is
`(L,R)=(s sqrt(B),-s sqrt(B))`: its common mode is zero, its relative mode
carries the erased orientation, and its energy is exactly `B`. This is the
minimal mathematical form of “something more than matter”: the actual record
can forget while an odd potential-field pulse carries energy and branch
information outward.

Production does not yet realize it. Same-sign contact is a nonreciprocal
remainder-reset wall; annihilation emits pre-existing flux but no odd sign
term; the complete event journal is observation-only. Repeated stable
operation therefore needs a reciprocal barrier and a pulse that propagates
causally into fresh environmental degrees of freedom before the local receiver
is reused—or the model must explicitly declare an open environment. No Born,
brain, thermodynamic, or `G*` result follows.

**LATEST — FTD-0850 (2026-08-10): production already has ternary
acquisition and loss fragments, but not a protected, loss-booked latch.**
FTD-0849 is preserved invalid at `28/30` after one unsimplified positivity
query returned undecided. The verifier-only FTD-0850 repair uses the exact
positive factorization and passes `30/30`.

Once seed/site/tick are retained, current genesis is deterministic and
context blind. Its nonzero divergence sign is odd, and evaporation is a
genuine many-to-one `+/-1 -> 0` map. The engine therefore does not lack
irreversibility in the abstract.

It lacks the other half of memory. Every finite-energy unlocked record has
positive evaporation hazard, so no strict invariant basin protects it. Dual
genesis omits the single-branch radial field withdrawal, and the aggregate
energy audit contains no event-level bath/controller receiver for the energy
and metadata lost in genesis/evaporation. Current production is therefore a
partial ternary open-system witness, not the FTD-0848 latch.

The next physical gate is now narrow: derive a movement-enabled matter-field
barrier plus an event reservoir that receives the erased detail, or explicitly
adopt the selected FTD-0848 latch/reservoir as an open-system type. Either route
must remain blind to context, outcome, Born weight, `G*`, and cadence targets.

**LATEST — FTD-0848 (2026-08-10): the continuous odd pointer now has an
exact loss-booked ternary reference latch.** FTD-0847 is preserved invalid
after C25 hit a symbolic-ordering verifier defect; the source-locked FTD-0848
repair changes only that comparison and passes `30/30`.

Within the registered even-polynomial class, three isolated nondegenerate
symmetric wells require degree at least six. The selected minimum witness is
`V_T=beta*x^2*(x^2-A^2)^2`, with wells `-A,0,+A`, barriers
`+/-A/sqrt(3)`, and an exact deterministic tilt threshold. A compliant onsite
AVF tick has one next state, exports nonnegative damping energy, and closes
coupling switches against a signed controller-work account.

The conceptual result is sharper than “damping makes records.” Finite-time
damping contracts phase volume but is not itself information erasure. The
explicit lossy step is the many-to-one basin quotient to `{-1,0,+1}`;
within-basin microdetails are unactualized because the actual record does not
retain them. A scalar bath-energy ledger closes energy but is not a
microscopic bath state and implies no Landauer cost.

This is a selected reference construction, not production recovery. The
engine supplies the ternary codomain but not the latch coordinate, sextic
potential, gate schedule, AVF transaction, or basin map. FTD-0850 has now
closed direct current-genesis/evaporation equivalence negatively and isolated
the missing strict barrier plus event receiver. Common-channel,
selector/Born, and orientation/rate closure remain open.

**LATEST — FTD-0846 (2026-08-10): a faithful conservative readout exists,
but it requires an odd pointer and necessarily backreacts.** The repaired
locked certificate passes `32/32`; FTD-0845 is preserved invalid at `31/32`.
A common/exchange-even pointer can read only the symmetric-square coordinate
`q^2`, so it identifies opposite half-cycles. A positive bilinear signed
pointer is incompatible with exact critical quarticity because positivity
forces a nonzero `q^2` stiffness.

Within the registered autonomous nonnegative polynomial position-coupling
class, the first faithful bridge is degree four:
`W=kappa(r-q)^4/4`, with `(r,pi)` selected exchange odd. Its symmetric
discrete-gradient update is onsite, unique, reversible, bounded, and exactly
conserves the combined clock--pointer--interaction energy. The pointer force
is `kappa q^3` at zero pointer, and a finite local history retains velocity
and zero-crossing direction.

This does not make measurement free. The exact energy current is booked and
the pointer torque can change the clock's swept-area sign; strict orientation
requires a compliance inequality. Coupling also shifts the instantaneous
quartic rate. The new pointer is a selected reference type, not a derived
production register. FTD-0848 now supplies one selected persistent ternary
record map with dissipation/exported energy booked; production realization
and selector coupling remain open. No Born, biological, production, or
finite-tick `G*` claim follows.

**LATEST — FTD-0844 (2026-08-10): complementary common/relative modes give
the simplest exact local quartic clock carrier.** The repaired locked
certificate passes `28/28`; FTD-0843 is preserved invalid at `26/28`. Use
`C=(L+R)/sqrt(2)` for propagation and `D=(L-R)/sqrt(2)` for local recurrence.
For the swap-symmetric edge energy, positivity gives `|b|<=a`, while exact
relative softness uniquely selects the rank-one boundary `b=a`.

At that boundary, the common field follows the source-free production
kick--drift map and preserves its exact tick invariant. Each relative site
follows the FTD-0841 quartic recursion and preserves positive onsite energy.
The two ledgers add exactly, the dependency radius stays within one Moore
shell, and one polarized relative site remains compactly supported with the
exact continuum `G*` period factor. This is the first selected reference
carrier simultaneously local, bounded, oriented, and energy closed.

The price is explicit. Production propagates L/R separately (`b=0`), not at
the selected rank-one boundary. Perfect decoupling also makes the relative
phase unreadable by the common/state/matter channel. Formation, energy-closed
readout/backreaction, a single common action, and finite-tick `G*` cadence are
the next open gates. The left/right-brain analogy is architectural only, not a
biological identification.

**FTD-0842 (2026-08-10): exact combined energy closure exists, but
the simplest positive local field cannot be a bounded critical `G*` clock.**
The locked source-and-symbolic certificate passes `26/26`. For the production
positive spatial operator `K` and selected onsite self-pair quartic, the
simultaneous discrete-gradient map conserves
`H=sum|P|^2/(2m)+<Q,KQ>/2+lambda sum|Q|^4` exactly, is reversible, preserves
global internal angular momentum, and has one global next state.

Two exact obstructions prevent promotion. First, the linear control inverts
`2mI+h^2K/2`; that inverse is dense on a connected quotient, so solving the
simultaneous equations in one tick has global algebraic dependence even
though each residual row is Moore-local. Second, positive edge energy
vanishes only on the spatially constant mode. Every nonzero bounded profile
has positive quadratic stiffness and is quadratic-plus-quartic, not an exact
critical-quartic oscillator. At zero onsite coupling the selected map is
implicit midpoint, not the production kick--drift tick.

The next architecture therefore needs both a strictly local accounting
mechanism (local energy transactions or an explicit multi-tick solver) and a
bounded zero/soft relative mode (defect/topology, competing interaction, or a
genuine multi-field cancellation). No production code changed.

**FTD-0841 (2026-08-10): the production substrate already supplies
the local canonical type, but not its quartic self-pair coupling.** The
source-locked exact certificate passes `26/26`. Every production voxel already
carries the local pair `(J,W)`, with `W` canonically identified as the momentum
conjugate to flux. The rank-one self-pair tensor `U=J otimes J` has the exact
Frobenius identity `||U||_F^2=|J|^4`, yielding the selected onsite Hamiltonian
`H=|W|^2/(2m)+lambda|J|^4` without selecting a spatial axis.

The vector symmetric discrete-gradient update has a globally unique next
state, is reversible, conserves both `H` and `L=J cross W` exactly, retains a
strict swept-area orientation, and is bounded on every positive-energy shell.
Every linearly polarized sector `J=q e`, `W=p e` is invariant and reduces
exactly to FTD-0840, so its continuum period obeys
`TA=sqrt(pi)G*sqrt(m/(2lambda))`. Generic nonzero-angular-momentum sectors
contain a centrifugal term and are not pure `G*` clocks.

This closes the missing **local variable type**, not the physical clock.
Cubic symmetry alone permits two independent quartic invariants; the radial
coefficient ratio is fixed only by the selected self-pair/Frobenius norm or a
stronger isotropy requirement. Production still lacks that onsite coupling,
a bounded clock body, polarization/selection dynamics, a coupled spatial
energy ledger, maintenance, and a finite-tick cadence.

**FTD-0840 (2026-08-10): the signed self-pair closes an exact stable
recursion, but not the native finite-tick `G*` gearbox.** The locked
source-and-symbolic certificate passes `24/24`. Retain the target-blind
canonical lift `(q,p)`, define `u=q|q|` and
`y=p/sqrt(2m lambda)`, and adopt the positive pair energy
`H=p^2/(2m)+lambda q^4=lambda(u^2+y^2)`. The induced pair flow is an oriented
weighted quarter-turn whose continuum traversal gives the exact
`sqrt(pi)G*` factor without placing `G*` in the dynamics.

The registered symmetric discrete-gradient rule has a unique real next state,
conserves `H` exactly, is physically time-reversible, has one strict swept-area
orientation on every nonzero step, and confines every positive-energy orbit
to a compact shell. This proves bounded recurrence and Lyapunov stability of
the origin/energy sublevels, not pointwise stability of every phase point. No
bath is needed for the isolated recursion. A bath remains necessary for damping, selected-shell
attraction, or recovery. Production still lacks the `lambda u^2` coupling,
and its `(q,p)` carrier is modal rather than localized. The finite-step series
also differs exactly from the Hamiltonian time-`h` flow, so the continuum
period law cannot be relabeled an integer global-tick cadence.

The same map is now implemented as an isolated header-only `ftd::eft`
reference with a focused Release CTest (`1/1`). It reports fail-closed root,
equation, energy, reversal, and orientation diagnostics; its 20,000-step
energy drift is `4.06e-13`. It has no `Voxel`, `RenderBridge`, toggle,
production phase, or `G*` input and therefore does not change the physical
boundary above.

**FTD-0839 (2026-08-10): `i` supplies the orientation seed and the
complex square supplies quartic energy, but they do not automatically form a
`G*` gearbox.** The exact source-locked probe passes `24/24`. A selected
`J^2=-I` has eigenvalues `+i,-i`; only a separately selected twisted circle
turns those phases into shifts `1/4,3/4`. The anchored chiral half-line
determinant ratio is exactly `G*` by Lerch's formula, but its origin,
multiplicity, first-order character, scale, and positive-frequency
polarization are all load-bearing. The full-line orientation-blind control
has determinant ratio exactly `1`.

The natural square field `U=psi^2` gives `|U|^2=|psi|^4`, so it supplies the
desired quartic energy algebraically. It also gives
`(+i psi)^2=(-i psi)^2`: both orientations land in the same half-twist and
cannot be recovered from `U` alone. The smallest faithful mathematical state
therefore retains `(U,chi)`, where `chi` is a signed orientation current, or
retains the unsquared lift itself. FTD-0840 now gives an exact conditional
conservative recursion on that retained lift, but native localization,
chiral-spectrum realization, pair coupling, maintenance, and tick cadence
remain open.

**FTD-0838 (2026-08-10): the frozen production core does not contain
the bilateral quartic-clock gearbox; the minimum repair needs a third energy
register.** A source-hash-locked exact audit proves that identical production
L/R maps give `F'=AF+2b`, `D'=AD`: no cross-register quarter-turn exists. The
weak swap is a reflection with `S^2=I`, not an orientation carrier with
`J^2=-I`. Smooth fixed-state `phase_read` branches have at most quadratic
modal potential, and production damping selects only the zero-energy fixed
point.

Within the registered context-blind radial class, the degree-minimum repair is
`X'=[1+eta(1-E)]JX`, locally stable for `0<eta<1`, plus the bath account
`B'=B+E-E'`. This is a conditional construction, not a production change.
Primitive ternarity satisfies `s|s|=s`, so the physical quartic coordinate
still requires a declared constituent-pair/coarse-graining map; a positive
reservoir and a cadence map to global ticks also remain open. FTD-0837 is the
invalid `20/22` parent; the tooling-only FTD-0838 repair passes `22/22`.

**FTD-0836 (2026-08-10): the selected quartic clock has an exact
minimal self-dual two-channel representation.** The order-four bilateral
kernel preserves `L^2+R^2`, carries a signed phase current, and loses that sign
under `Sym^2`. For the selected quartic clock, `u=x|x|` maps
`x^4+y^2=1` to `u^2+y^2=1`; physical flow traverses this circle with angular
speed `-2 sqrt(|u|)`, and the integrated traversal is exactly
`sqrt(pi)G*`. This makes `G*` the time weight of the nonlinear quartic lift,
not the energy or radius of the self-dual circle.

The theorem is conditional and sharply bounded. The map is non-diffeomorphic
at `x=0`, the radial stabilizer is imposed, and the environment ledger proves
energy accounting rather than a native positive reservoir. No bounded clock,
substrate quartic-selection mechanism, brain dynamics, or prime/tick
identification follows. FTD-0835 remains invalid at `16/17`; the separately
locked tooling repair FTD-0836 passes `17/17`.

**LATEST — FTD-0834 (2026-08-10): the nonsingular complete-tangent run
reaches Krylov but still has no certified physics verdict.** FTD-0832's
preregistered direct-product chart resolves the old zero-component
singularity. In the source-pinned clean worktree, every preflight gate passes,
all four final Krylov constructions reach dimension 64, and 1920 derivative
evaluations complete. The producer descriptively finds one eligible
rank-four cluster, zero qualified candidates, and an unresolved solve.

FTD-0833/0834 transparently repair two verifier launch-scope defects without
changing the producer, corpus, verifier, or gates. The terminal independent
replay reaches `94/95`: one near-zero principal angle is stored as
`7.3000483e-8` and recomputed as `0`, outside the locked `2e-8` scalar replay
tolerance even though both pass the physical `1e-6` gate. No post-hoc
tolerance or metric repair is licensed. FTD-0832 is therefore execution-invalid
at certification and establishes neither a local clock nor its absence. The
next ordinary-clock route is a newly preregistered stable
coincident-subspace certificate or an independent bounded native carrier;
critical-quartic `G*` hardware remains a separate open front.

**LATEST — FTD-0828 (2026-08-10): the substrate has native oriented clock
algebra, but no qualified local clock body yet.** The production `(J,W)`
phase/action pair already supplies a target-blind modal clock: a
state-functional phase, exact action, signed phase current, repeated gates,
and radians per primitive tick. Its antisymmetric current retains the
clockwise/counterclockwise sign that BCC loses because
`Sym^2(-R)=Sym^2(R)` for the inert quarter-turn lifts.

The missing hardware is now stated minimally: bounded body-relative support
and local readout, autonomous recurrence, complete matter/field/controller
energy closure, and held-out rate/orientation stability. Harmonicity is not a
clock no-go; it only removes action-sensitive critical-quartic `G*` timing.
The FTD-0659 doublet remains a leaking band-embedded resonance, not an
autonomous clock. Native `G*` hardware additionally needs a target-blind
critical quartic natural-coordinate normal form and a finite-amplitude purity
or maintenance audit. FTD-0774 is the shortest ordinary local-clock route,
but FTD-0832's completed Krylov run fails its independent metric-row
certificate and therefore still licenses no candidate verdict. The mixed
unit-strut-tensegrity full-flex certificate remains the narrowest exact `G*`
route. Certificate `13/13`; no production change.

**LATEST — FTD-0827 (2026-08-10): the critical-quartic-clock/CM gearbox is
an exact conditional theorem.** For the selected critical quartic Hamiltonian,
the dimensionless positive-energy shell is `C:y^2=1-x^4`. The direct rational
map `(u,v)=(x^-2,-yx^-3)` lands on the fixed curve `E:v^2=u^3-u` (32a2) and
obeys `du/(2v)=dx/y`; the opposite sign reverses the clock differential. The
clock curve is birational to 32a1 and degree-two isogenous to 32a2, so its
`G*` period, integral rank-two structure, conductor-32 primary rule, and
Hecke/Frobenius calendar are one compatible algebraic object. Exact Sage
certificate: `22/22`.

This resolves the oriented rank-two lift for the **selected quartic-clock
branch**, not for the production C18 modal carrier. FTD-0794/0821/0824 remain
controlling: P1--P5 have not selected or maintained the critical surface.
Prime labels are arithmetic places, not successive ontic ticks. The live debt
is now the physical mechanism that maintains/localizes the clock and realizes
prime-indexed operational channels, not an unknown mathematical CM map.

**LATEST — FTD-0825 (2026-08-09): contextual-actualization v2 is the
ratified successor branch at formal reference scope.** FTD v1 and FC-1 remain
unchanged as historical provenance. The successor constitution explicitly adopts a noncommutative
potentiality net, split locality, measurement independence, and a globally
contextual deterministic selector, with operational no-signalling as a hard
gate. This resolves the FTD-0329/PL-2 contradiction branch-wise: v1 retains its
measurement-dependence history; v2 supersedes it rather than pretending both
commitments coexist.

The finite reference net now has the correct algebraic separation. Actual
ternary records are diagonal and commutative; `M_3(C)` is only a selected
potentiality representation; finite regions are Type I; no Type-III result
follows from random-matrix spacing. The old construction is archived/retracted.
The Python and isolated C++ reference models establish conditional Born
pushforward, global contextuality, no-signalling singlet marginals, the
Tsirelson ceiling, and context-blind audited G* gating. They deliberately do
**not** establish substrate Born recovery. The physical recovery protocol is
design-locked and unexecuted. Global tick `n`, local clock phase/duration, and
gate count are distinct; G* controls the selected critical period/cadence, not
the global time or the outcome. Promotion remains blocked on CA-PREP, CA-BORN,
CA-HIDE, and CA-GSTAR in the open tracker.

**LATEST — FTD-0802 (2026-08-04): `x_+ = 1/alpha` now has NO
numerical-uniqueness support of any kind.** The base-rate control OT-3.3 had
never faced was pre-registered (`PREREG_OT33_BASERATE_v1.md`, outcomes and both
runner SHA-256s locked before execution) and run. **Outcome B:**
`N_null = 0.0014` dual-matchers under displaced targets against a pre-blessed
threshold of `< 0.1` — finding no other matcher is exactly what chance
predicts, so the zero count discriminates nothing. The `x_-` leg eliminates
**zero** candidates at the registered gate (16 pass `x_+`, 16 pass both), so
*dual-match* and *match* are one predicate here, and `x_- <-> N_c` is retired
(FTD-0014) — the identical defect FTD-0791 found in FTD-0319. The `x_+` leg
alone matches a random target near 137 about **one time in three**.

**Separately, the published count was wrong.** OT-3.3's "only the master
quadratic dual-matches" rested on a runner line reading `EXT-B genuinely-new
cubic dual-matchers: 0 (all are master quadratic x linear factor)` — a
**hardcoded literal string**. `matchers_b` never entered `genuine_unique` and no
code tested the claim. It is false: all four EXT-B cubics leave a nonzero
remainder. Corrected runner reports **5 distinct dual-matchers, 4 beyond the
master quadratic**. Two further defects in the same artifact: it aborted on
Windows before scanning anything, and still printed the `~4e5` Bayes figure
retracted in June (the audit comment had been added directly above the print,
and the print left untouched).

**OT-3.3 is retagged `[NUMERICAL FACT]` -> `[SELECTION]` and withdrawn as
support for OT-5.1.** With FTD-0791 having taken the FTD-0319 leg, the
remaining support for `x_+ = 1/alpha` is **OT-1.5 plus OT-1.9's unit-group
arithmetic only** — FTD-0321 (2026-08-04) took OT-1.9's dual-match reading
(the `d = -4` privilege breaks by `|d| <= 500,000` with 1,271 dual-matchers;
what survives of OT-1.9 carries no dual-match content; see TRACKER
OT-5.1). Read alongside FTD-0792 (the engine runs on
the CODATA value; a source-lint test *enforces* that the derived root reaches
no force path), the empirical case is: 1.26 ppm of arithmetic, occurring at
chance frequency in its own scanned family, in an engine that never used it.
The 1.26 ppm agreement itself remains `[NUMERICAL FACT]` and FTD-0001 stays
`[SMC]` — this moves no other tag.

**FTD-0800/0801 (2026-08-04): the screened compact-law C3 candidates
close negative, but only at their actual executed scope.** FTD-0800's locked
Maxwell screen found no native `n = 4` mechanism in the executed set: 38
retained connected stationary equilibria at `N=3..6` and SC blocks at
`L=2,3,4` are flat after relaxation. The apparent SC `gamma^4` shear cost is
an affine/pinned result and falls to machine zero when free row slides are
restored. **The `N=6` follow-up is expressly an `[EXPLORATORY NUMERICAL
SCREEN]`, not an exhaustive no-go** (correction commit `0cf9e635`): graph
deduplication gave 62 classes, but only 51 sampled embeddings cleared the
chosen separation floor; none had both self-stress and flex. The other 11
classes, unsampled realization components, special rank strata, and
separation-floor dependence remain open. There is **no `N=6` or `N<=6`
theorem**.

FTD-0801 then tested the strongest elimination-produced successor, a periodic
triangulated neutral sheet. Fixed-cell modes have clean quartic cost, but a
free cell corrugates to machine-zero energy; the exact-zero `cos(2q.x)` mode
is already a finite mechanism, so the free-body candidate is `n=infinity`.
The conservative synthesis is that **every resolved quartic witness found so
far under this scoped zero-tension, central-force, single-scale compact law is
clamped**: a
held path, pinned boundary, or fixed cell supplies it, and releasing that
constraint removes it. That is `[SYNTHESIS]`, not a universal theorem. The
`6x6` random sheet mode remains unresolved, the preregistered `8x8` size was
not executed, and pre-tension, two-scale interactions, other realization
strata, and finite-patch questions remain open. No production engine state
changed.

**FTD-0799 (2026-08-04): the protonucleus saturates, and there is
still no derived body size.** The locked v1 campaign ran (arms A–D of five;
**arm E, `R0=20`, is unrun**). All three kill conditions clear and charge is
exactly conserved in every arm. **The quasi-static `R_c = 12.63` survived its
own falsifier and is bracketed** — `R0=12` gives *exactly zero* growth,
`R0=13` grows — so it is a valid **nucleation** criterion. But the accretion
instability it predicted does not happen: `R0=16` grows `+76.65%` and then
arrests dead, `dN/dt` exactly zero for the final 399 ticks. **The prereg's
gloss "a derived body size" is not supported** — `N*` tracks the seed
(2,109 / 7,153 / 9,539 / 30,167 for `R0` = 8/12/13/16), so arms C and D arrest
a factor 3.2 apart. No particle size, mass or radius is licensed. **The
mechanism is new and it generalizes:** genesis deposits `-1` upstream and `+1`
downstream, so every event **conserves charge while diluting net polarity**
(arm D: polarity fraction 1.000 → 0.566). The source driving `|J|` is net
polarity, not site count, so **growth erodes its own driver** — a negative
feedback intrinsic to pair genesis. Corollary: the quasi-static law is a valid
nucleation criterion and an **invalid growth law** (it predicts `|J| = 2.326`
at arm D's arrest radius against a measured `1.283`, low by 1.81x, because it
assumes the uniform-polarity source genesis has just destroyed). Profile-scoped:
movement, curl, evaporation and Gauss projection are all OFF, and evaporation
is the most likely omitted stabiliser. **Does not bear on C3.**

**FTD-0795..0798 (2026-08-03/04), four audits of the foundations.**
**FTD-0795** ran FTD-0258's six registered deviations from QM/SR against
existing experiment. Verdict `SPINE_HAS_NO_ROW_THAT_IS_DEVIANT_LIVE_AND_TESTABLE`.
PL-1 is **dead**: FTD's ontology is one continuous local flux manifesting on
threshold, so splitting an excitation 50/50 forces `alpha >= 1` (maximal
bunching), while the measured `alpha = 0.0188 +/- 0.0067` is **146 sigma below
that floor** — and because `alpha` is coincidences over singles at one
beamsplitter, efficiency and gain cancel in the ratio, so the result invokes
no Hilbert space and cannot be deflected by rejecting the quantum formalism.
PL-2/3/6 are vacuous, PL-4/5 live but ~39 orders from testable.
**FTD-0796** proves the trap underneath that: **FC-1's completeness clause
alone forces CHSH <= 2** — if the four observables are elements of `A_5` under
one setting-independent measure, their pushforward *is* a joint distribution
and Fine (1982) applies. **No locality assumption enters the argument.** The
corpus misattributes the bound to Postulate 4 (Moore locality) and lists
"violate locality" as an escape; that is wrong, and nonlocality buys nothing,
because using it breaks FC-1 itself. The only live door is **superdeterminism**
(already declared at FTD-0329 row 15), priced exactly at `S <= min(2+3M, 4)`
with Tsirelson at `M* = 0.27614237`. Live contradiction registered: FTD-0329
adopts measurement dependence while the constitution §2.3 and PL-2 assert
statistical independence — mutually exclusive, and under ledger precedence
FTD-0329 governs, which makes PL-2 unfalsifiable contrary to its own text.
**FTD-0797** refuted the de Rham cone (spatial degree-blindness does not touch
the cone; the production stencil is M18). **FTD-0798** refuted the
Kaehler-Dirac carrier — it fails C1 and C3 at sight, and its *motivating
premise was false*: the second-order flux already carries a Born-valued
positive conserved density, which forecloses as a class every future argument
that FTD needs a first-order sector to obtain a Born measure. Salvage: `M18`
admits a local cubic-covariant 3-square decomposition. That was the
**eleventh refuted construction of the session**.

**AND FTD-0794: no bridge connects the lattice to the quartic law.**
Chain link 4 - "minimum `C4` support and null-flatness select the quartic
clock", the last unwithdrawn lattice-to-quartic bridge - is refuted. The
plaquette `C_4` rotation **selects the HARMONIC law and excludes the
quartic**: under `(x,y) -> (-y,x)`, `x^2+y^2-1` is invariant, `x^4+y^2-1` is
not, and no rescaling repairs it (symbolic solution set empty). The ring's own
dynamics agrees - `Q`,`R` are the degenerate eigenvalue-2 pair of the `C_4`
Laplacian, so `phi_ddot = -K L phi` gives two simple harmonic oscillators.
**This argument was already retired as `GRAV-001` in the chain document's own
Part III banner** and kept in section 4 regardless. Null-flatness is not a
hypothesis but the conclusion (`F''(0) = -(A^2/E) V''(0)`, so assumption 5 IS
"no quadratic term"), which the repo's own
`ANALYSIS_COUPLED_QUARTIC_CLOCK_FIELD_v1.md` already calls an **unearned
premise**. And `G*` is pinned only by a minimal-degree tie-break: the degree-6
family `1-(1+c)x^4+cx^6` satisfies the same assumptions and yields clock
constants from **2.8375 to 3.1185**. Retagged `[CONDITIONAL THEOREM]` ->
`[SELECTION - unearned]`. **`G*` enters FTD by choice.**

**What survives of the G* chain, after 2026-08-03:** Watson 1939 (the BCC
lattice Green's function carries `Gamma(1/4)`; `W_3 = G*^2/2pi`), and the fact
that `z = 1/4` is the unique point where the Gamma reflection ratio is the
eigenvalue of the square root of integration (`D^{-1/2}[x^{-3/4}] = G*
x^{-1/4}`, verified exact). Both are classical mathematics about `Gamma(1/4)`.
Neither selects a physical law. `G*` appears **zero** times in chain links
19-30 (SR, GR, cosmology, group theory), so deleting it leaves that entire
back half unchanged.

**MOST IMPORTANT RESULT OF 2026-08-03 (FTD-0791/0792): the `137.036`
agreement is at the chance base rate, and the engine never used it.**

FTD-0319's uniqueness scan - the only evidence that the master-quadratic root
`x_+ = 137.0361714582` is structure rather than coincidence - had **never
been audited**. It has now been, refute-by-default, and independently
replicated to the digit. **Leg 1 sits exactly at the base rate:** at the
registered `2e-6` gate the null expects **1.42-1.67** matches and the scan
found **1**; Monte Carlo shows a random number near 137 is matched to 2 ppm
by that family **79% of the time**. **At the operative gate the second leg
eliminates nothing** (1 -> 1), so *dual-matcher* and *matcher* are the same
predicate. All residual surprise (~190:1, not the retracted 4e5:1) belongs
entirely to the `x_-` vs `N_c` leg - **an identification the framework
retired**. Both tolerances were set by the observed deviation, and the
preregistered outcome had probability `0.9942` under its own null: the test
had no power. **FTD-0319 is retagged `[MEASURED]` -> `[SELECTION]`; the
designation *the framework's single scan-rigid identification* is WITHDRAWN
and the scan-rigid count is now ZERO.** ~~Eight canonical files still assert
otherwise and are flagged for update.~~ **Propagated 2026-08-04** across the
spine (§11 body + the §12 export surface), `SPEC_FTD_COMPLETE_FRAMEWORK`
(bucket (i) is now empty), the unified axiom register + its JSON, the adoption
pricing rules + its JSON, the dimensional map + its JSON,
`INDEX_CONSTRUCTION_SPINE`, `SPEC_ALPHA_DYNAMICAL_BOUNDARY`,
`SPEC_DOCTRINE_LEDGER`, `SPEC_OPEN_MATH_FRONTIERS`, `MONOGRAPH_FTD_CONSTRUCTION`,
`AUDIT_RIGIDITY_CATALOG_v1`, `TRACKER_ONTIC_TRUTH` OT-5.1, and `CLAUDE.md`.
`SPEC_PHYSICS_BRIDGE` carries a governing banner instead of a rewrite — its
whole thesis is the three-leg bridge this removes, so rewriting it is a
re-derivation and stays `[OPEN]`. Two things were deliberately left alone
because each is a research act, not a transcription fix: **FC-W's price stays
at y = 1 gap-class** though its "high-value" justification was exactly the
withdrawn scan-rigidity, and **OT-3.3 was flagged rather than refuted** — a
different runner (the 2.87M extended scan) whose base-rate control had never
been run, and a zero count means nothing until the null expectation is known.
The 1.26 ppm agreement itself remains `[NUMERICAL FACT]` - it is arithmetic
and unchanged.

> **UPDATE 2026-08-04 (FTD-0802): the second of those two is now closed.** That
> control was pre-registered and run — Outcome B, `N_null = 0.0014` — and
> OT-3.3's count was additionally found to be wrong (4 non-master
> dual-matchers, not 0). OT-3.3 is retagged `[SELECTION]` and withdrawn as
> support for OT-5.1; see the FTD-0802 section at the top of this page.
> **FC-W's price remains the one item deliberately left**, and re-pricing it is
> still a research act.

**FTD-0792:** the engine runs on `ALPHA = 1/137.035999177` (CODATA-matched)
while the derived root sits unused in `ALPHA_TREE`, and a **source-lint
CTest fails the build** if the derived value reaches a force path. So no
engine result can ever be evidence *for* the master quadratic. The precision
value comes from a 4-term series whose `c1 = 9/47` does 99.96% of the work
(3.0 sigma), `c2 = 5/64` closes it to 0.004 sigma, and `c3`/`c4` move the
answer by ~1/1000 of CODATA's error bar - unfalsifiable decoration. In its
favour: no rational with denominator under 199 lands in CODATA's window, so
`9/47` is genuinely constrained, and the engine header already tags the
series `[CONJECTURE]` and forbids re-tagging it. The framework is honest
here; the issue is that the chain imports the measured value at its first
joint and enforces the import with a build lint.

**Latest arc - the carrier corner and the surd bound (FTD-0778..0784,
2026-08-03):** the two existing native corpora were re-screened with a
repaired, surrogate-nulled closure estimator (v2; the v1 metric's verdicts
were predetermined) - FTD-0776's records are UNINFORMATIVE for closure
(monotone/single-pass, vacuous regime), not evidence against it (FTD-0778 as
amended), and the FTD-0659 doublet is excluded as harmonic (FTD-0780). The
carrier search was then cornered by proof and source audit rather than
simulation: any closing linear functional of the field is harmonic
(exact, sidebranch section 32.1); the locked affine sector has zero
conservative anharmonicity and genesis is a drain, so no carrier lives there
(FTD-0781); the movement-enabled sector divides into a native core plus
imposed force phenomenology, with the bound-pair breathing mode as sole
admissible candidate (FTD-0782); and that candidate closes on three grounds -
softening everywhere, in-band fundamental, flow-curve reach a_max = 0.62
versus the required a of order 8 - generalizing to the bracket theorem: wells
pin at n ~ 2, capacity walls at n = infinity, G* lives at n = 4 strictly
between, where no identified native mechanism produces a potential
(FTD-0783). The twelve minimum carrier constraints are consolidated in
`10_eft_program/SPEC_CARRIER_CONSTRAINTS_v1.md` (C3 - intermediate-exponent
confinement - is the wall). Finally FTD-0784 bounds the prize: conditional
only on Chudnovsky 1976, the FC-W surd sqrt of G*(4G*-1) lies outside
`Q(G*, pi, sqrt(pi))`, so no rational single-clock observable - and no
spectrum - can carry it even after total Gate A+B+C success; the
master-quadratic invariants ARE measured moments (trace 64/<x^2>,
determinant 64 sqrt(pi)/(<x^2><abs x>)), and the missing datum is exactly
one Z/2 branch choice: W stays external, now field-theoretically. **FTD-0786 then corrected this program's own
map of itself:** the coupled sector is *not* blocked behind the
reciprocal-transaction problem. FTD-0545/0546/0549 closed only the frozen
fixed-step form; FTD-0551's discrete-gradient map solved it exactly, and
FTD-0600-0739 built the sector out (static dressed fixed point with positive
analytic 48-coordinate Hessian, reversible dynamical rest, internal matter
and field modes, coherent co-moving fixed-mass transport, durable
finite-support capture). The transaction is a selection, so those results
are selection-scoped, not blocked. The carrier fails there at **C2**: the
first internal doublet sits strictly inside the propagating band
(`Omega/omega_band = 0.8864`, FTD-0663 exact) and decays at
`Gamma_E = 0.0065`/tick (FTD-0676) - 26.57 cycles per e-fold, and it cannot
be lifted because it is a linear normal mode (`n = 2`, amplitude-independent
frequency). So the two open doors collapsed to one: a native
intermediate-exponent confining mechanism (C3). **FTD-0787 claimed to open that
last door - and FTD-0789 refuted it the same day.** The claim was: C3 is realized natively, with no new primitive: transverse
(flexural) displacement against an *untensioned* bond changes bond length
only at second order, so the energy - quadratic in stretch - is exactly
quartic in displacement. On a collinear trimer under the registered compact
law the potential is finite and exact, `V(d) = -2 eps + 24 eps d^4 -
32 eps d^6`, quadratic coefficient identically zero **by geometry, not
tuning**, separatrix exactly at bond dissociation. **That is refuted.** The polarity mask makes the outer
pair same-polarity, so `A_AC = 0` identically and the energy depends only on
the two bond lengths - the **bend angle is an exact flat direction**, verified
to machine zero. The transverse path stretches both bonds; the system reaches
the same offset by bending, for free. The quartic was the curvature of a chord
across a flat valley. Maxwell counting confirms it: `3N - B = 7` zero modes,
spectrum `[0 x7, 0.96, 2.88]`, five trivial plus **two bends that are finite
mechanisms**, flat to all orders. **C3 is NOT realized and FTD-0783's bracket
corollary is restored.** What survives is a decidable criterion: `n = 2` iff
rigid, `n = infinity` iff the flex extends to a finite mechanism, and
**`n = 4` requires first-order flexibility with second-order rigidity**. Both
registered configurations fail it oppositely (connected block rigid; trimer a
free mechanism). Also established: C5 is comfortable and does **not** tighten
with `eps` (31x margin, capped by coupling and causality).

> ~~**WITHDRAWN with FTD-0787 (refuted by FTD-0789). Retained for provenance;
> do not cite.** It still fails **C2**: bounded hardening caps
> `Omega_max = 0.400745` against a band top of `1.230959`. But the failure is
> now parametric rather than structural - frequency scales as `sqrt(eps/m)`
> while the quartic-purity window is eps-independent, so band clearance inside
> the clean range needs `eps > 0.2218` (field) or `0.5856` (wave), against a
> **selected** `eps = 0.01`. Since `eps/C_WAVE^2 = 0.03`, the obstruction is an
> energy-scale separation between matter binding and field stiffness, not the
> shape of any potential.~~ *(All of this described a mechanism that does not
> exist: the bend angle is an exact flat direction, so there was never a
> quartic to have a purity window.)*

**FTD-0788 then addressed `eps` itself - and FTD-0790
refuted that too.** `z*G(1)` is identically `z*G(0) - 1`, so the proposal was
`K_GENESIS - 1` with no lattice content; `K_GENESIS` is [SELECTION-ADOPTED] x
[IMPOSED] x declared convention, and is a flux-amplitude gate rather than an
energy; the argument excluded the field sector then used its propagator; and
C2 is a ONE-SIDED threshold (`eps > 0.0944`) that ~26 simple expressions
clear, so value-matching in this range is not evidence. FTD-0388 had already
ruled the pattern inadmissible - *an adoption is never a derivation*. **Also
corrected: FTD-0788's claim that `particle_masses.h` mislabels its constants
was WRONG** - the repo convention `W_SC = 0.5054620197` is consistent across
four sources; FTD-0788 switched to the literature convention and reported the
repo's correct usage as a defect. Only `G(0)`, `G(1)`, `G(0)-G(1)=1/6`
survive. **`eps` has no derivation and the well-depth question stands exactly
where it was.**

> ~~**WITHDRAWN with FTD-0788 (refuted by FTD-0790). Retained for provenance;
> do not cite.** It is an
> orphan - 17.8x above the field-mediated scale (`E_F(1) = 5.611e-4`) and 51.6x
> below the lattice nearest-neighbour quantum, matching nothing the framework
> contains. The compact law has *compact support*, so it is a matter-sector
> contact term rather than a field-mediated one; and the engine's matter
> thresholds are already exact lattice Green's-function quantities
> (`z G(0) = W_SC = K_GENESIS`, matched to 1.8e-17). The exact companion
> identity `z[G(0) - G(1)] = 1` gives the nearest-neighbour quantum
> `z G(1) = W_SC - 1 = 0.5163860592`, and the compact law's minimum sits at
> exactly `r0 = 1`. Under that motivated selection C2 is satisfied - the
> flexural mode is above the field band over 24-100% of the separatrix at 4.0%
> quartic contamination. This is a [SELECTION], not a derivation of `eps`,
> and no engine constant was changed. The fork it opens is the programme's
> decisive item: a field-mediated `eps` (~5.6e-4) forbids a carrier permanently
> (short by 400x), a matter-sector lattice quantum (~0.52) makes it viable, and
> the two differ by ~2700x. Deriving the compact law from the native force
> (FTD-0575 `[OPEN]`) now decides the carrier question. Honest costs: every
> FTD-0600-0739 matter/field ratio shifts under the new scale, and the parent
> trilemma's collapse coefficient worsens 51.6x, so ternary site capacity must
> carry more weight. FTD-0783's bracket corollary is correspondingly narrowed:
> wells pin at `n ~ 2` only in the coordinate the stiffness acts on.~~
>
> *(FTD-0790: `z*G(1) = z*G(0) - 1` identically, for any lattice, so the
> proposal was `K_GENESIS - 1` with zero lattice content — and C2 is a
> one-sided threshold that ~26 simple expressions clear, so landing in the
> range is not evidence. **FTD-0783's bracket corollary is restored, not
> narrowed**, and the `eps` fork above is not open.)*

The FTD-0779 telemetry dumper is repurposed as a test
of FTD-0781's locked prediction (dOmega/dA = 0 exactly, sub-threshold). **FTD-0785** then audited the
algebraic spine itself - never previously audited, and now the program's
main surviving asset. Outcome: **every numerical identity verified at
50-150 digits, no arithmetic error anywhere**; all failures were
classificatory and are corrected in place - Theorem 6's proof was a
regression fit (demoted; canonical count is now **6+3**, not 7+2), an
untagged electron-mass identification with a concealed 68 ppm deviation was
relocated out of the theorems-only core, the load-bearing Chudnovsky
citation was misattributed, one uniqueness claim overclaimed, and the
maximality disclaimer's stated reason was mathematically false. Two items
stay OPEN: FTD-0314's C2 leans on Waldschmidt Conjecture 5.23 (an open
problem, as the repo's own FTD-0377 records), so that [THEOREM] tag
over-consolidates; and the FTD-0319 look-elsewhere scan audit did not
finish. The same pass demoted FTD-0784, which had promoted a *transport* of
FTD-0314 C3's argument to theorem grade. Sidebranch narrative:
`Desktop/ftd-relativity-closure/RELATIVITY_CLOSURE_DERIVATION.md`
sections 30-32.

**Latest exact clock--memory boundary (FTD-0777):** a locked finite-cover
certificate passes `16/16`. Forward dyadic powers add readout resolution but
no independent state. A compatible depth-`K` root tower has `2^K` sheets,
monodromy `m -> m+1`, and first return after `2^K` fundamental carrier cycles,
so it is an exact recurrence/epoch hierarchy inside a selected model. The
stronger memory reading closes negatively: one transitive tower observes only
`m_written+n mod 2^K`, so it cannot preserve a nonconstant cycle-invariant
payload. A stable relative payload requires a separately selected,
co-transported reference lift. Root lifting lengthens recurrence and retains
arcsine rather than quartic occupancy; it derives neither minimum `dt`, native
time, nor `G*`. No engine trajectory ran. Native carrier birth, reference,
persistence, local decoding, and source/work closure remain `[OPEN]`, and
FTD-0772/0776 remain unsuperseded.

**Latest native `q_active` result (FTD-0776):** the deterministic locked
`L=32`, seed-1, single-substrate CPU/SOR campaign produced four raw-valid
200,000-tick trajectories. The preselected aggregate `q_active` had raw
positive-going crossing counts `1,1,0,0` across `A={10,12,14,16}`, hence zero
complete cycles in every arm against the required eight. Verdict
`Q_ACTIVE_RECURRENCE_UNQUALIFIED_IN_LOCKED_L32_SEED1_CPU_SOR_PROFILE`:
`[ENGINE FACT — MEASURED, OBSERVABLE- AND CONFIGURATION-SCOPED NEGATIVE]`.
The `q_active` clock candidate is `[RETIRED]` only in this exact profile; an
autonomous coupled matter--field recurrence remains `[OPEN]`. All quartic
occupancy, speed, moment, correlated-`G`, and waveform diagnostics are N/A
because recurrence failed first. No scale/body/minimum-`dt` work was run.
This is distinct from FTD-0772's retrospective 18-cell FTD-0659 `Q_u` audit.
The continuous quartic identities remain exact only under their conditional
fixed-natural-coordinate/selected-edge assumptions and were not tested here.

**Latest nonlinear waveform result (FTD-0773):** the continuous
fixed-natural-coordinate inverse chain is exact under branch reversibility
and conservative unit-mass closure. For the separately selected quadratic
coordinate-edge energy `epsilon(q_v-q_w)^2/2`, the quartic waveform has the
exact scale-free shape ratio
`B_4=[Vbar(pi)-Vbar(0)]/Vbar''(0)=48pi/G*^4`
`=1.967895315142656...`, and `H_0''K_4=epsilon/3`. This is a conditional
theorem about a selected continuous waveform and edge: the numerator and
curvature are correlated functionals of the same waveform, and the ratio
changes under nonlinear coordinate or edge-functional changes. It is not an
independent `G*` determination and not a native FTD time derivation. FTD-0772
left all `18` native cells recurrence-unqualified and no native paired-edge
coupling exists, so no engine test was licensed. The next native target remains
a complete coupled matter--field recurrence before occupancy, phase-response,
or coupling is retested. Exact symbolic certificate `95/95`.

**Latest native temporal-occupancy result (FTD-0772):** the exact selected
power-law occupancy is
`rho_m(x)=m/[2B(1/m,1/2)sqrt(1-|x|^m)]`; in a fixed natural coordinate,
quartic occupancy over an amplitude interval characterizes a quartic
potential only on the swept region. This is not a coordinate-free modulus:
nonlinear observable changes alter the density, and finite tick recurrence
has an atomic measure. A hash-locked retrospective reanalysis fixed the
signed FTD-0659 observer `Q_u=u^T(q_6,q_7)` before inspection. Execution,
phase, support, fixed-ray, quadrature, amplitude, and covariance controls pass,
but all `18` primary cells fail stationarity/return: the final-window crest is
about `0.538`, with window CDF distance `0.314`. Verdict
`NATIVE_TEMPORAL_OCCUPANCY_RECURRENCE_UNQUALIFIED`; the current bare doublet
cannot license native quartic occupancy, phase response, or coupling. The next
target is an autonomous coupled matter--field recurrence, not another statistic
of the same decaying projection.

**Latest clock--rod boundary (FTD-0771):** comparing the selected quartic
period with one abstract causal lattice interval gives the exact conditional
cycle fraction `d_4=(rho/u)(2E)^(1/4)/(sqrt(pi)G*)`, where `rho` is the
clock-to-tick rate and `u=C_sub tau/ell`. At the selected normalization
`rho=u=1`, `E=1/2`, this is `1/(sqrt(pi)G*)`. P1--P5 do not select the
quartic clock, its relative rate, or its occupied shell: exact on-site flows
at different rates remain deterministic and Moore-local but change `d_4`.
If the comparison cone is the same coupled-clock wave cone, the ratio becomes
`1/[pi sqrt(d_R eta)]` and the direct quartic period modulus cancels. Status is
`[CONDITIONAL THEOREM + SCOPED CLOSED NEGATIVE + OPEN NATIVE MATCHING]`;
“rod” here is a coordinate edge, not a material measuring rod. Exact
certificate `20/20`; no production or calibration state changed.

**Latest temporal-extension result (FTD-0770):** a selected positive-action
graph of even-power action--angle clocks exactly conserves total action and
supports conditional graph-Laplacian relative-phase waves. The amplitude-one
quartic shell is `E=1/2`; equal Moore-26 coupling multiplies the axial
continuum `c^2` by nine. The decisive control theorem is
`kappa H0''/Omega^2=(kappa/E0)(m-2)/(2m)`, giving axial values
`0,eta/4,eta/3` for `m=2,4,6`: the exponent survives but the quartic `G*`
period normalization cancels. A fixed edge connection has only kinematic
holonomy. The result is `[SELECTED MODEL + CONDITIONAL THEOREM + SCOPED CLOSED
NEGATIVE + OPEN NATIVE BRIDGE]`; it changes no production clock, proper-time
law, `U(1)`, matter, toggle, scenario, or golden state. Exact certificate
`15/15`; focused CTest `1/1`.

**Latest long-mobile-matter result (FTD-0768):** the fourth qualified aged
`L=321`, 768-tick rest/moving CUDA campaign completed and serialized artifact
SHA256 `FE7E1915...170C3F`. The entire forward history, all 13 checkpoints,
discrete reversal, rest control, common action, energy, Gauss, causality,
regularity, fixed/moving regional transport, complementary boundary, and
mask-sweep ledgers pass. Continuous matter/field reverse recovery is
`3.8786822642578e-9`, however, exceeding the locked `1e-10` gate. Independent
certification returns `2220/2221` and `LONG_TRANSPORT_EXECUTION_INVALID`.
Descriptive clearing at ticks 704/768 is quarantined; no cleared-response,
wake, particle, radiation, or long-horizon identity verdict exists. Canonical
status is LEDGER FTD-0768 and
`docs/theory/07_assessment/constituent_complete_matter/AUDIT_LONG_TRANSPORT_DYNAMIC_RESPONSE_v1.md`; the
pre-execution audit preserves the three earlier infrastructure aborts.

**Current matter ontology boundary (FTD-0669, reconciled through FTD-0768):**
the supported identity candidate is the relational constituent kernel and its
internal state, not one voxel and not the complete nearby field. The selected
state-only Gauss representative is constitutive constraint data. Remaining
dynamic field and boundary data are environmental unless a cleared-region
energy ledger proves attachment, deposition, or independent radiation. FTD-0760
certifies only a finite-time selected M3 family; FTD-0761 supplies a moving-core
witness; FTD-0764--0767 close rigid field-coat and already-observed-wake readings
at their registered scopes. This is a selected classical matter ontology, not a
derived physical particle, charge sector, pole, or infrared theory.

The current graph/energy margins bound that pair in state space; they do not
define a spatial membrane. Exact algebra further shows that negative
rest-subtracted pair energy implies `3/4<r^2<3/2`, so an isolated exactly
energy-conserving pair cannot disconnect. Coupled breakup must be funded by an
accounted field/environmental energy transfer; this is a reversible
dissociation gate, not a decay law. A conditional pairwise many-body extension
would turn missing bonds into surface energy and would be bipartite under the
current opposite-polarity rule: nearest-neighbour SC/BCC graphs are compatible,
whereas nearest-neighbour FCC is obstructed by triangles. That extension is not
implemented and licenses no crystal, domain, membrane, or material-phase claim.

The minimal `N`-constituent continuation is now algebraically explicit but
blocked as a bulk law. Summing the compact interaction over every opposite-
polarity pair preserves exact internal momentum and binding work, yet `m`
coincident positive constituents opposite `m` negative constituents produce
`m^2` minimum bonds and energy `-epsilon N^2/4`. The registered linear Gauss
field retains the same quadratic scaling and is too weak in the FTD-0739 bound
control to reverse that coefficient. Unrestricted pair additivity is therefore
non-extensive. The least new-ontology candidate is a state-derived unsigned
capacity `n(v)=sum_a W_a(v)<=1`, motivated by ternary site capacity; its exact
reversible contact solve is not implemented. Canonical derivation:
`docs/theory/02_foundations/DERIV_MINIMAL_MANY_BODY_MATTER_NETWORK_v1.md`.
The candidate nevertheless has one exact positive result: every quadratic coat
places at least `1/8` weight on a nearest site, so capacity bounds local degree
by 511 and gives `U_N>=-(511/2)epsilon N`. It repairs extensivity
kinematically; it does not yet supply collision/contact dynamics.

The SC/FCC proposal also has an exact spatial realization: even and odd SC
parity sites are two interpenetrating FCC sublattices. SC face edges join
opposite polarities at the compact bond minimum; FCC same-polarity neighbours
sit outside the bond cutoff. Full integer filling saturates coat capacity and
has binding energy `-3 epsilon` per site, but its signed coat source is the
nonzero checkerboard `rho=(-1)^parity/8`; the periodic redressing is given
below, while finite-surface equilibrium remains open. Common sublattice motion
is an acoustic candidate; relative motion is a
restored polarity/polarization candidate. Surface sublattice imbalance can
model static electrification, but physical charge conservation and `U(1)` are
not derived.

Mechanically, that SC binding network is cohesive but not yet solid: dilation
costs `144 epsilon N eta^2`, whereas simple shear costs only
`12 epsilon N gamma^4`, giving zero harmonic shear modulus. Capacity-induced
shear must be separated from substrate pinning. A BCC body-diagonal relation
would conditionally supply quadratic shear but is not derived by the current
cutoff. It also cannot be inferred from the selected BCC clock: because
`T_B(0)=0`, a strain-independent temporal kernel contributes exactly zero
static shear. The live diagonal route is therefore either the already-counted
relaxed field Hessian or a deliberately priced deformation-dependent
graph--connection/spatial coupling; it is not a free consequence of the clock
kernel or a reconstructed open link. The same parity
geometry makes `(111)` surfaces polar and balanced `(100)/(110)` surfaces
neutral, creating a precise static-electrification test after full field
relaxation.

The holonomy escape has also been narrowed exactly. Existing face/edge field
energy is already included in the zero-frequency constrained Hessian. A
noncompact link cannot couple locally to endpoints whose only matter data are
gauge-neutral position, momentum, and polarity: gauge invariance makes such a
bond energy independent of the link. Open-link elasticity therefore requires
a transforming local phase/fiber; closed-loop elasticity is either the
existing curvature energy or a new material-loop coupling. FTD-0658 has no
intrinsic rest phase in the registered candidate set, and FTD-0494/0495's
history fiber closes bookkeeping but not ordinary common action. The no-new-
primitive test is consequently the pressure-qualified relaxed Hessian,
minimized over common translation and independent row slides. It distinguishes
static rigidity from substrate pinning, temporal inertia, and viscosity.

The corresponding periodic bulk can be redressed exactly: its coat source is
`rho=chi/8`, the static face solution is `E_i=chi/48`, `B=0`, and its raw field
energy is `1/1536` per site. Symmetry cancels the gathered electric force while
all SC bonds sit at the compact minimum. This conditionally proves a static
many-body medium for the candidate algebra. It does not prove finite matter;
the next structural target is a bounded uncontained piece with a stable
self-selected surface and relaxed spectrum.

Its binding/capacity energy is now explicit. An `L_x x L_y x L_z` block has
`U=-3 epsilon N+epsilon(L_yL_z+L_xL_z+L_xL_y)`, so its missing-bond surface
tension is `epsilon/2`; corner/edge/face/bulk removal costs are
`3/4/5/6 epsilon` before field relaxation. This is a finite uncontained static
candidate, not yet a full object. For the all-even control family the deposited
source factorizes, an exact compact routed field proves the minimum Gauss
energy is volume-plus-surface bounded, and the frozen selected coefficients
give negative rest-subtracted formation energy for every even cube `L>=2`.
Thus energetic binding survives finite redressing. Boundary-force relaxation,
capacity contact, release, mobility, surface persistence, and a nonnegative
relaxed spectrum remain required; none is supplied by the energy bound.

The capacity geometry is now exact. A finite block has a saturated interior
of `(L_x-2)(L_y-2)(L_z-2)` sites and a one-coat-thick slack interface with
`sum_v n(v)(1-n(v))=(7/16)sum_areas-(49/256)sum_lengths+343/4096`.
This supplies a derived diffuse surface observer, not an ontic membrane.
Capacity is incompressible/fluid-like rather than solid-like: independently
translated complete rows preserve `n(v)=1` exactly, so it contributes no bulk
shear modulus. `L=4`, `N=64` is the first all-even control containing both
saturated bulk and slack surface.

One isolated incoming capacity constraint has a unique nonzero elastic-energy
root under the frozen relativistic dispersion, but the associated total matter
impulse is the constraint's common-translation gradient relative to the
lattice and is generically nonzero. The bare event changes no current or field,
so its opposite recoil is unaccounted. Simultaneous contacts also require a
selector beyond the inequality itself. The immediate construction gate is
therefore an atomic contact--field recoil transaction, not another binding
force or post-hoc collision correction.

The minimum Gauss field sharpens that gate into a no-go: at fixed density,
every nontrivial transverse-electric or magnetic change costs positive field
energy. An elastic fixed-position contact leaves matter and binding energy
unchanged, so the minimum field must remain unchanged and cannot absorb its
recoil. The first finite equilibrium discriminator is therefore the `L=4`
pressure-feasibility system `F_field+A^T lambda=0`, `lambda>=0`, together with
zero total capacity impulse. It is derived but intentionally not executed or
preregistered while FTD-0768 was unresolved. Its invalid completion removes
that scheduling hold without selecting a successor. Cubic symmetry reduces this to a
one-ratio falsifier: the exact invariant pressure pattern has zero total
impulse and virial 24, and every `-F_field/C` component must equal the same
nonnegative `lambda`. No optimizer or fitted pressure is available as an
escape if those ratios disagree. The field force is fixed analytically by the
minimum-Gauss adjoint, total force vanishes by centered inversion, and cubic
symmetry leaves only six representative ratios with capacity magnitudes
`{1,7,49}/128`.

The exact harmonic geometry is now known before those six field forces are
evaluated. The eight active capacity rows have rank 8. All 48 axial row slides
are tangent to them and form the null space of the rank-144 central-bond
Hessian. Common pressure restricts to a rank-21, zero-trace nonzero symmetric
matrix on that space, hence is necessarily indefinite. Its 27-dimensional
kernel is exactly three `D_perp tensor D_perp` sectors: three translations and
24 explicit nontranslation internal slips. Binding plus
pressure cannot make the block a harmonic solid. The relaxed Gauss field must
stabilize the downhill internal modes and decide the remaining null modes; its
second variation is not automatically positive. Exact finite-support
differentiation nevertheless proves that both the signed Gauss-source map and
the unsigned occupancy map are injective on all 27 pressure-null modes, with
rank 9 in each axial sector and zero total variation. The positive
`<rho',G rho'>` term therefore sees every pressure-null motion, and every such
motion changes the slack finite surface, but the background-source curvature
can still cancel or reverse it. The result does not yet distinguish
surface-restored cohesion, Peierls pinning, and bulk rigidity.

The general even-`L` calculation now resolves that scaling ambiguity at the
integer phase: complete-row signed and unsigned first variations are supported
only on end caps. More strongly, the infinite checkerboard field energy has
exact common-translation curvature `-beta/96` per site, while binding and
capacity are invariant. The integer-aligned bulk is therefore a stationary
saddle, and its six-ratio `L=4` pressure test is retained only as a control.
At half-cell phase, bulk signed source cancels exactly; a finite block retains
only boundary-plane source with an explicit `O(area)` Gauss-energy bound. The
next static candidate is the finite half-phase body, and the next mobility
candidate is a local phase-front/constituent-turnover process rather than a
rigid volume hop. The exact neutral corridor sits on the FTD-0500 half-cell
chart seam: it is valid for shape/current quotient physics but not for a
unique inversion-covariant ternary anchor. FTD-0626's shared-anchor fibre is a
constructive representation option, not production ontology, and its fixed
internal rest gate remained negative. The phase-front test must therefore
price fibre transport, an off-tie barrier, or atomic turnover explicitly. The
exact certificate now passes 105/105.

**Latest Number-One gate result (FTD-0605):** the full six-coordinate local
shape chart does not contain a stable dressed compact core. Exact algebra
shows why: the selected distance-binding Hessian has rank three, with three
restored strains and three rotational zero modes. In the locked 32-phase run,
29 searches exhaust the optimizer budget and the three returned states hit
the chart boundary with nonzero downhill gradient. Their distances remain near
`sqrt(2)`, so this is torque-driven reorientation, not collapse. Direct-field,
common-action, and inverse checks pass where executable.

**Current reciprocal-matter escalation (FTD-0598–0605):** one voxel remains an
R0 surrogate, and aggregate density/current remain derived coupling
projections. Explicit `(q,X,p)` constituents are the constructive selected R2
research state, not production ontology. The immediate defect is now compact
Peierls contamination plus an unresolved orientation sector, not merely a
missing compensator, bad initial field, or missing breathing coordinate. The
next live candidate is a global `SO(3) x strain` factorization of the existing
constituent coordinates. A native smooth low-momentum extended carrier
satisfying the FTD-0555 spectral criterion follows if that compact gate fails.
Connection/holonomy remains required for a
microscopic local `U(1)` claim but is not by itself a pinning repair. A gapless
pole, phase-robust long-time mobility, collisions, local `U(1)`, and production
adoption remain open. No scenario is licensed.

**Latest native-matter result (FTD-0597):** FTD-0592 first proved the frozen
first-event sector subcritical through `N=9`. The separately locked `N=10`
evaluation is valid but inconclusive: all four volumes maximize at `r=9`, and
the ordinary orbit upper bounds range from `1.5663934398` to `1.6127738812`,
above `K_GENESIS`. Independent reconstruction passes 130/130 checks. These are
upper bounds in a relaxed coefficient space, not ten-source witness histories.
FTD-0594 then restores every cancellation forced by exact equality of the
stencil eigenvalue `M`, using cyclotomic integer keys. At `L=65` all 6,544
cubic orbits have distinct `M`, so the decisive bound is unchanged; the exact
key/multiplicity reconstruction passes 172/172 checks. FTD-0595 next enforces
the exact capacity of axial nearest-neighbor source pairs: exhaustive cubic
animal enumeration gives at most 13 axial edges among nine removed sources.
The refined maxima remain super-threshold (`1.5596603323` through
`1.6115888534`), and the independent reconstruction passes 258/258 checks.
FTD-0596 then retains the complete cubic-orbit distance distribution and
enforces every Fourier-positivity constraint. Its 32 padded dual certificates
pass 396/396 independent checks. The stronger maxima move to `r=8` and fall to
`1.5218539833` through `1.5932999259`, but all remain above threshold; this is
still inconclusive and supplies no source history. The registered Delsarte-LP
decider is therefore closed negative. FTD-0597 then restores the exact common-
observation-time constraint: all pairwise removal-pulse products lie in
`[-1/4,1]`, not `[-1,1]`. The resulting signed-shell/Delsarte bounds are
`1.4041876553...1.4577559408`, all strictly subcritical; the 413/413 independent
proof moves the frozen first-event closure to `N<=10`.
Combined with FTD-0587, the observed object remains only an externally
prepared, selected-Gauss-stabilized evaporative remnant. The next separately
priced source-capacity count is `N=11`; the alternative is an explicitly
selected reciprocal nonlinear carrier/action. No mobile particle, scenario,
pole, or infrared claim is licensed.

## Where the canonical truth lives (in precedence)

**LEDGER > constitution > all other prose.** When two documents disagree, the higher one wins.

- **Constitution** — `docs/theory/01_reference/SPEC_FTD_FRAMEWORK_V1.md`. The framework statement of record: Postulates P1–P5 (frozen), Framework Commitments (FC-0, FC-1, FC-2, FC-3, FC-W — `[AXIOM]`-class declarations), and Calibrations.
- **LEDGER** — `docs/theory/07_assessment/core_ledgers/LEDGER.md`. Per-claim tags, dependencies, provenance, and the live `FTD-NNNN` id register. The single source of truth for claim status.
- **Bedrock tracker** — `docs/theory/07_assessment/core_ledgers/TRACKER_ONTIC_TRUTH.md`. The five truth tiers (T1 rock-solid → T5 conjecture); read before defending any FTD math claim.
- **Doctrine ledger** — `docs/theory/01_reference/SPEC_DOCTRINE_LEDGER.md`. Single-page status map rolling up the above.
- **Open items** — `docs/theory/07_assessment/core_ledgers/TRACKER_OPEN_ITEMS.md`. Every `[OPEN]` across code and theory.

## What is load-bearing — the algebraic spine

The theorem-grade core, independent of any physics interpretation, lives in `docs/theory/01_reference/SPEC_ALGEBRAIC_SPINE.md`. It is genuine mathematics, **conditional on Chudnovsky 1976** (algebraic independence of π and Γ(1/4)):

- `G* = Γ(1/4)/Γ(3/4) ≈ 2.95868` (the lemniscatic ratio — **not** the Bernoulli/Gauss constant ϖ ≈ 2.6221).
- The master quadratic `x² − 16G*²x + 16G*³ = 0`, its roots, and Vieta structure.
- The Watson identity `W₃ = G*²/(2π)` via Chowla–Selberg; CM-curve uniqueness among class-number-1 fields; the coefficient `16 = |Aut(E)|²`.
- `ℚ(G*)` as a maximal π-free subfield of `ℚ(π, Γ(1/4))`; the harmonic-invariant tower.

Of the nine numbered results, seven are theorem-grade and two are honestly tiered below theorem grade (see §0 of the spine).

## What is suggestive — physics at LEDGER status

Physics identifications ride at their actual tags and are never promoted by rhetorical momentum:

- `x₊ ≈ 137.036 = 1/α` to 1.26 ppm is a `[STRONGLY MOTIVATED CONJECTURE]` (FTD-0013) — the algebra is a theorem; the *physical identification* is not derived. ~~The structural-uniqueness evidence is the FTD-0319 look-elsewhere scan (a `[NUMERICAL FACT]`).~~ **There is no structural-uniqueness evidence — withdrawn 2026-08-03 by FTD-0791, see the top of this page.** The scan sits at the chance base rate and is retagged `[SELECTION]`; the scan-rigid count is zero. What remains is the 1.26 ppm agreement as arithmetic, and the word "motivated" in the tag is now doing noticeably less work than when the tag was assigned.
- Mass formulas, gauge ratios, and the gravity/QM identifications are `[PARAMETRIC]` or `[SMC]` (see the LEDGER and `CATALOG_PARAMETRIC_INSERTIONS.md`).
- **α is not derived anywhere.** Whether the discrete ontology forces the EM coupling is the central open obstruction, **MC-T4.3**, a `[FOUNDATIONAL OBSTRUCTION]`: 0/4 FTD-native routes force the master-quadratic operator assembly (FTD-0242), so α is dynamical, not structural.
- **FC-W** is the framework's one *adopted* import — an external order-2 twist realizing `δ = √(G*(4G*−1))`; under it `x₊ = 1/α` is a `[CONDITIONAL THEOREM given W]`, explicitly not `[DERIVED]` (FTD-0314/0315).

## The standing boundary

The map of what the discrete ontology can and cannot set for itself: `docs/theory/07_assessment/AUDIT_BOUNDARY_MAP.md`.

- **The modulus/argument frontier** (`docs/theory/02_foundations/FOUND_MODULUS_ARGUMENT_FRONTIER.md`): a finite, discrete, deterministic, forward-only substrate owns the forced/modulus/even half (π) and cannot self-supply the chosen/argument/odd half (G*, the chosen adjoint δ).
- **The priced-import ledger** (`docs/theory/01_reference/SPEC_IMPORT_LEDGER.md`, FTD-0371): the frontier made *quantitative* — every imported type in a common currency with a falsifier each: 1 adopted bit (FC-W/δ), 4 selected types (incl. **D=3 — [SELECTION — declared], FTD-0355**; IMP-S4 is the gauge-connection carrier choice, global `A_μ=𝒫_TJ_μ` or the FTD-0417 independent local-link branch), 4 named results (Chudnovsky + CM-h=1/E1/E\*/E\*\*), 3 calibrations (a_phys/t_phys/K_B), the empirical bridges, and 2 *declined* bets (M, reversibility). IMP-S4 was minted 2026-07-12 and broadened 2026-07-22 without changing the count. ⚠ Reading guard: the "1 bit" is the α-branch choice only, not the total physics import.
- **The type-priority principle** (`docs/theory/02_foundations/FOUND_TYPE_PRIORITY_PRINCIPLE.md`): context (a type) is prior to and the precondition for the value of content (a token); the Framework Commitments are precondition-types, which is why they are adopted rather than derived.
- Which predictions are dimensionless vs calibration-conditional: `docs/theory/01_reference/SPEC_DIMENSIONAL_MAP.md`. The **default calibration is electron-primary** (FTD-0137, `FOUND_ELECTRON_PRIMARY_GAUGE.md`, declared in `SPEC_FTD.md`): import `{ℏ, c, m_e}`; derive `a_phys = ℓ_P` `[DERIVED ~0.19%]`, one tick `t_phys = ℓ_P/(√3·c) = t_P/√3 ≈ 3.11×10⁻⁴⁴ s`, and Newton `G` as an `[SMC]` output. The dimensionless spine (α, mass ratios, mixing angles) is calibration-independent; every dimensional prediction is conditional on this gauge. How the gate carries dimensionless → dimensionful: `docs/theory/03_derivations/foundational_mechanics/DERIV_DIMENSIONAL_GATE.md`.

## The engine

A logic-first C++/CUDA simulation of the substrate (`engine/SPEC_ENGINE.md`); only 6 rules are derived from the axioms, all phenomenological features are toggle-gated and default OFF. Determinism is pinned by the golden hash **`0xc54ffbeda5a3ea63`** at L=17 (re-pinned 2026-07-27 for the EM-diagnostic c² fix; superseded `0xb604d81a3d79366e`, `0x1343f31fc0163a84`, `0x450fca908f536e36` — ground truth is `engine/tests/test_render_bridge_golden.cpp`, not this page; rebuild and compare to verify a change is physics-neutral). The engine is **not** a quantum-dynamics engine — it has no ℏ and no Pauli exclusion, and the lattice's dispersion is not the Schrödinger one (FTD-0270); it does not derive atomic spectra.

The charge story now has a microscopic and an emergent layer. FTD-0421 proves
that no nontrivial exact additive charge survives every frozen production
event over its tested native basis. It does **not** close dynamical low-energy
charge. FTD-0429 starts the field at zero, enables only the native wave and
state-gradient coupling terms, and measures a finite infrared response
`(div J)_k/s_k -> 3G_C`. The fitted `Z0=0.2562476` agrees with
`3G_C=0.2562736` at `1.01e-4` relative, while a response forced to vanish at
`k=0` loses by `Delta BIC=279.14`. Thus coarse-scale emergent charge is derived
and measured in the frozen reaction-free native linear sector. FTD-0430 then
moves a sparse neutral pair one cell through the actual production movement
phase. The moving-minus-stationary field remains inside the native dependency
cone, follows the exact native wave pole and step residue, and gives a fresh
`L=48,96` infrared fit `Z0=0.25626855`, within `8.17e-5` relative of FTD-0429.
The transported retarded-response sub-gate is therefore closed positive.
Conservation through reactions, microscopic `U(1)`, gauge redundancy, photons,
radiation, forces, and empirical charge normalization remain unestablished.

FTD-0431 then activated production evaporation. Its isolated rate was correct,
but the coupled source failed the preregistered one-exponential model, so the
attempt closed as outcome D without an infrared or conservation verdict.
FTD-0432 resolved the mechanism rather than fitting around that failure: an
exact pre-RNG observer predicts the one-step source and occupancy losses, and
all CPU/CUDA conditional-expectation gates pass. Generated field energy drives
the representative low-mode hazard from about `0.0991` to `0.001225`, proving
that native self-dressing dynamically suppresses and modulates evaporation.
This is mechanism validation, not exact or infrared conservation. FTD-0433
then holds `<100>,n=1` fixed and samples its exact-pole first antinode over
`L=12...48`. The large-volume tail decreases (`p=0.568,0.524`), but the full
hazard sequence is nonmonotonic and `h48/h12=0.81695`. The locked verdict is
outcome C, unresolved scaling: neither a zero infrared hazard nor a finite
nonzero floor follows, and no post-hoc polynomial intercept was fitted.

FTD-0434 then audited the dashboard's canonical `s0-vacuum-photon` directly.
The initializer is not a translating photon packet: it sets `J_z=g` but
`W_x=g`, whereas the componentwise production wave map requires a matching
`W_z=-C_WAVE D_x J_z` for right-moving `J_z`. In the isolated wave arm the
tick-20 displacement is zero to machine precision and the width grows `3.19x`.
The exact browser-equivalent profile also fails the locked translation gate.
The displayed streamlines therefore show an evolving non-transverse seed, not
a demonstrated photon. This closes the current scenario claim only; a new
photon construction must be separately preregistered.

FTD-0426 separately shows that production separation plus the **selected**
Gauss projector gives equal/opposite static surface flux, while its older live
profile fails autonomous dressing. FTD-0427 supplies a projection-free local
mechanism in a selected
oriented-face complex. FTD-0428 now integrates it behind a default-off isolated
production toggle: a one-time minimum-energy solve creates the neutral Gauss
dressing, actual movement supplies face current, a staggered curl/transpose-
curl update propagates transverse fields, and centered `E` is mirrored into
`Voxel::flux`. Static, movement, finite-support, compiler, and modified-energy
gates pass at `L=32,64` (worst Gauss residual `9.15e-13`; lock 22/22; results
90/90). This remains a selected classical engine extension. It excludes
reactions, supplies no force or quantized photon, and does not establish native
`U(1)` or a common matter/light cone. FTD-0430 closes the native reaction-free
  moving-source response positive; FTD-0431/0432 show that evaporation is
  dynamically field-dressed, while FTD-0433 leaves its same-mode infrared
  scaling unresolved. Genesis,
annihilation, and weak transmutation remain separate later event classes. The
selected branch still needs on-shell pole/residue
extraction. Neither task licenses another structural constant or mass match.

FTD-0478–0480 now sharpen the mobile-matter boundary. The exact subcell
polarity and straight-segment face current exist as selected observers, and a
single native normalization is compatible with susceptibility and work. A
normalized implicit transaction closes 24 diagonal probes, but this is not a
complete force law. In the broad campaign only 58/70 arms pass: if one
coordinate displacement is zero, current work is blind to that transverse
electric force component, and the edge-magnetic gather is also not fixed by
the current-generating interaction. The frozen E/B-only candidate is therefore
closed negative. FTD-0481–0483 were not executed: there is no new toggle,
scenario, reciprocal dressing claim, pole extraction, or Lorentz claim.

FTD-0484–0539 then carried the discrete-action route to its frozen-variable
decision gate. The exact current is a cubical worldline coupling, and the
minimal endpoint-split action closes its field equations. Its smooth corner
roots fail both energy ledgers. Its edge roots solve all in-plane equations but
sit on an exact reflection-plane cusp: converged left/right residuals bracket
zero without selecting a unique force, and edge energy also fails. The frozen
minimal action is therefore closed negative as the FTD-0479 reciprocal mobile
law. No `common_action_face_dynamics` toggle, reversal, scenario, morphology,
pole/IR, or Lorentz campaign is licensed. Any restart is a new preregistered
action or explicit new primitive, not an incremental repair.

FTD-0540 now proves why that edge failure cannot be tuned away inside the same
compact shape: nearest-cell partition plus first moment uniquely forces the
hat kernel, and smooth nonnegative cardinal weights cannot reproduce position
even with wider locally finite support. FTD-0541 constructs the least
ontologically disruptive priced exit as an observer: manifestation stays on
one ternary site, while its coupling to flux is a positive non-cardinal
quadratic B-spline coat. The exact matched face current closes below
`2.22e-16`, reversal below `5.56e-17`, and the inactive integer-plane
derivative jump below `7.58e-14`. This resolves the representation cusp on the
new branch. FTD-0542 now supplies the fresh common interaction: exact
endpoint-weighted face currents and a temporal coat derive from one spacetime
functional, with continuity and gauge identities below `2.23e-16`. FTD-0543
then proves that fixed-step spatial stationarity is not itself an energy law;
the quartic witness changes endpoint energy by exactly `1/8`, whereas the
simple exact-energy discrete gradient loses phase area (`det=9/11`). The next
separation is now sharper: FTD-0544 proves the matched field update transfers
exactly `-<Ebar,K>` and propagates Gauss. The next gate is therefore the
action-derived matter work identity `Delta H_matter=<Ebar,K>`. FTD-0545 now
closes that universal fixed-step identity negative: the exact gauge-covariant
endpoint map agrees with its analytic formula below `1.34e-15`, while its
energy defect reaches `4.10e-5`. The external harmonic witness is not a
self-consistent periodic one-charge field, so FTD-0546 executes the required
neutral coupled-pair gate. Its periodic Gauss/source algebra closes below
`9.98e-14` and exact field work below `2.90e-18`, while pair energy still misses
by `9.68e-9`. This closes the frozen minimal quadratic-coat common action as an
exact-energy mobile law. FTD-0547 shows exactly why that frozen step failed:
it used a constant midpoint velocity while momentum changed. In a constant
collinear field, the exact relativistic secant trajectory restores matter
work to `2.72e-20`; FTD-0548 deposits its nonuniform spacetime current with all
continuity/gauge/reversal identities below `2.19e-14`. The total face current
is endpoint-geometric, but the temporal occupation and endpoint current split
change by up to `1.65e-3`. The next gate is therefore not another endpoint
formula. FTD-0549 proves the obstruction exactly: even equal endpoint
positions, endpoint velocities, midpoint velocity, energy endpoints, and
total face current permit spacetime-source differences `q d epsilon/30`.
The next gate is a self-consistent atomic solve for internal path stages,
force history, current, and field. Those stages may be derived solver
unknowns rather than new ontology. The old FTD-0536 action remains closed and
no production toggle or scenario is licensed.

FTD-0550--0554 then close the point-carrier representation route more sharply.
The quadratic coat admits exact electric and magnetic orbit gathers and an
exact-energy one-step discrete-gradient transaction, but generic multi-tick
charges accelerate in their own conservative lattice self-energy. Rigid
neutral composites retain the same Peierls mechanism, and exact continuous
fractional translation removes it only through a globally supported signed
kernel and current. FTD-0555 now derives the exact remaining local criterion:
the relative barrier is the field-energy average of
`((1-cos k_i)/(3+cos k_i))^2`. Low-momentum extension suppresses it as `R^-4`;
large geometric radius with lattice-scale spectral content does not. The
registered binomial finite-volume qualification failed 9 conjunctive controls
despite exact identities at `1.42e-14` and fitted slopes inside their windows,
so no native carrier is claimed. The live gate is to obtain independently
prepared stable production `(s,J)` histories and measure this spectral index,
or derive a genuine integer-hopping band. FTD-0556 now derives that band for
the isolated free-flux `(J,W)` pair. Its exact two-component symplectic map
replays 576 forced-CPU production mode-ticks to `2.25e-17`; a compact signal
stays inside one Moore shell per tick, while an extended packet centroid moves
`0.565426` cells in one tick. This proves that integer microscopic updates and
continuous field-centroid motion coexist. It does not produce a stable
localized manifested carrier. The live gate is now the nonlinear binding
problem: find a finite-energy `(s,J,W)` configuration whose core and dressing
move together without an imposed clock or legacy force. No production change
is licensed.

FTD-0557 now proves why the free band cannot itself close that gate. On the
infinite lattice its nonconstant real-analytic `2x2` multiplier has no nonzero
`l2` point spectrum and no nonzero finite-time rigid `l2` translate. An
unchirped branch packet obeys an exact ballistic variance law with coefficient
`Var(grad theta)`. The three-arm CPU witness replays to `1.60e-16`, broadens by
`1.916` variance units, and creates no `s` sites. The `flux-soliton` scenario is
therefore a dispersion diagnostic, not a free soliton. Historical Phase-B
cluster candidates remain retracted or unqualified; the next admissible work
must begin from a specifically identified nonlinear/defect/topological carrier
candidate and preregister core, dressing, leakage, energy, volume, seed, and
translation tests before execution.

## FTD-0558 — Moving-source correction and reopened radiation gate (2026-07-26)

The older FTD-0115/0120 moving-source result was not a production theorem. It
used continuous time and the seven-point symbol, then evaluated the periodic
`L=16,n=15` mode with an unwrapped `k` even though it is the `n=-1` crystal
mode. That alias depressed the inferred phase speed by exactly 15 and created
the false claim that every nonzero smooth-source speed radiates.

The exact production selected-drive response is now

`J/f = 1/[C_WAVE² M(k) - 4 sin²(Omega/2)]`,

with the theorem-grade wrapped-BZ floor
`theta(k)/|k| >= 2/(3pi) = 0.212206590789` cells/tick. A periodic integer-hop
history is not the same smooth source: it has an exact Floquet comb whose
non-fundamental harmonics encode the hop schedule.

FTD-0558 passed 12 driven-response arms, 9 wrapped-threshold arms, and 12
Floquet schedules. It retracts the old Q6 power claim and reopens the physical
gate: a stable manifested mobile source, exact conserved deposited current,
retarded energy flux, and reciprocal recoil must still be derived from the
same transaction. No production physics changed.

## FTD-0559 — Exact external-drive field energy (2026-07-26)

The field-side energy question is now closed for a prescribed drive. The exact
production modal invariant obeys a one-step work identity, and its causal
harmonic response is known at every finite time. Off resonance, stored energy
is bounded; at exact finite-volume resonance it grows coherently as
`|F|²N²/2`. A stationary rate appears only in the continuum/large-time Fejér
limit, with resonance-surface Jacobian `|v-sigma v_g|`.

This does not reinstate physical Larmor or Cherenkov power. The drive is still
external. The live gate is now narrower: identify a stable manifested mobile
carrier and derive its deposited current plus equal-and-opposite matter work
from the same reciprocal transaction. Until then, the exact result is a field
susceptibility/energy theorem, not particle radiation. No production physics
changed.

## FTD-0560 — A point cannot hop with its entire linear dressing (2026-07-26)

The simplest remaining carrier candidate is now closed. For a single polarity
that hops one site every finite `T` ticks, the native state-gradient and
velocity-curl sources are orthogonal and cannot cancel. Every finite hop period
has a regular Floquet resonance with nonzero numerator. The corresponding
co-moving Fourier resolvent is not square-summable, so the point cannot carry
an exactly bound linear dressing on the infinite lattice.

The effect weakens cleanly: the resonant source amplitude is
`6pi G_C/T²+O(T^-3)`. Slow hopping therefore radiates less schedule/stutter
content, but never exactly zero at finite `T`. The live carrier gate is now an
extended or nonlinear structure whose form factor cancels every on-shell
component, whose self-consistent dynamics detune the spectrum, or whose
binding is defect/topological. No production physics changed.

## FTD-0561 — Extension is not enough; neutrality changes the power (2026-07-26)

For any fixed rigid source, the first nonzero axial polarity moment now
determines its slow-hop resonant leakage. Nonzero total polarity produces the
universal shape-independent amplitude `6pi G_C|Q|/T²`. A neutral dipole begins
at `T^-3`; cancelling dipole as well pushes the first quadrupole term to
`T^-4`. More generally, moment order `m` gives `T^{-(m+2)}`.

This closes “make the charged object wider” as the carrier cure. Exact
cancellation of even the axial resonance family requires zero total polarity
in every plane normal to motion, and that still does not cancel oblique
surfaces. The live route is therefore a microscopically neutral composite with
emergent long-range polarity, internal deformation, or nonlinear/topological
spectral binding. No production physics changed.

## FTD-0562 — No fixed finite shape cancels the full slow-hop surface (2026-07-26)

FTD-0561 left open the possibility that a neutral three-dimensional form
factor might cancel every oblique resonance even though it cancels only the
axial witness. That escape is now closed for every fixed nonzero finite rigid
linear profile.

For each direction `n`, the production pole has a regular slow branch
`k_T=r_T n/T` with
`r_T=2pi sqrt(3)+6pi n_parallel/T+O(T^-2)`. A finite profile has an analytic
form factor and therefore a first nonzero homogeneous Taylor polynomial
`P_m`. A nonzero homogeneous polynomial cannot vanish on both open hop-axis
hemispheres, so some direction has `n_parallel P_m(n)!=0`. Along that direction
the on-shell native source remains nonzero for every sufficiently large `T`,
and its putative co-moving response is not square-summable.

The locked campaign passed 768/768 arms and 96/96 witness groups. The next
live carrier cannot be a rigid finite object with a tuned neutral shape. It
must deform during translation, alter its spectrum nonlinearly, grow its
support with the hop scale, or be a genuinely self-consistent
defect/topological matter-field orbit. Physical matter radiation power remains
open. No production physics changed.

## FTD-0563 — Neutral finite matter does not carry a true linear Gauss monopole (2026-07-26)

The selected oriented-face Gauss complex has an exact boundary-condition
split. On a periodic volume, `sum D E=0`, so production's apparent point charge
is globally the neutralized source `s-Q/N`. On the open/infinite complex,

`sqrt(lambda)|E|=z|S|`,

and therefore `lim kappa|E|=z|Q|`. The long-range monopole coefficient is net
source polarity. Dipoles and higher neutral multipoles fall as positive powers
of momentum; regular solenoidal dressing cannot change their closed flux. The
restricted native FTD-0429 response has finite infrared susceptibility and
obeys the same neutral-source conclusion.

Combined conditionally with FTD-0561/0562, fixed finite rigid linear matter now
faces a strict choice: neutral means no true monopole; charged means no exact
square-summable co-moving linear dressing. The 384/384 locked arms, 96/96
witness groups, exact solver/curl controls, and independent proof passed.

The next live target is narrower: a nonlinear, defect/topological,
boundary-supported, nonlocal, or internally deforming carrier must explicitly
show the new term or singularity that supplies its effective closed Gauss flux,
then derive reciprocal motion from one common matter--field transaction. The
current result does not prove that such a mechanism is impossible. Production
physics remains unchanged.

## FTD-0564 — Topology can label charge sign, but cannot set charge magnitude (2026-07-26)

The remaining topology escape has been split into its two logically distinct
jobs. On FTD's octahedral Moore shell, the degree-one field `J=A n` and the
degree-zero, nowhere-vanishing hemisphere field `J=A(n+2e_z)` carry the same
exact affine closed flux `4A`. Changing `A` changes the flux continuously while
leaving the hedgehog degree fixed. Sixty amplitude/polarity/cubic-rotation arms
and an independent proof pass at `1e-12`.

Therefore a topological degree can supply an integer sector or polarity sign,
but it cannot by itself supply the electric-charge unit. A viable native
carrier now needs both a protected defect/bundle class and a nonlinear common
action that locks its flux magnitude, energy, recoil, mobility, and stability.
On a periodic three-torus the total regular defect index is zero, so local
defects must be paired unless a puncture or environmental boundary carries the
complement.

This also corrects the old universal magnetic-monopole claim. `div(curl J)=0`
is a theorem only inside the regular global-potential representation; Dirac
monopoles are classified by U(1) bundle/Chern data, not `pi_2(U(1))`.
Production physics remains unchanged.

## FTD-0567 — Current genesis is not the missing amplitude lock (2026-07-26)

The exact accepted single-substrate map is radial subtraction:
`|J|=K_GENESIS+x` becomes `|J|'=x`. It therefore preserves every incoming
overshoot instead of selecting one charge amplitude, and its quadratic energy
withdrawal varies with both overshoot and wave velocity. The dual genesis path
changes `s` without either drain.

The written action also cannot generate this rule. Uniform fields below and
above `K_GENESIS` have the same `div J`, gradient, and candidate-state action
values, while production eligibility differs. Evaporation then makes the
frozen map many-to-one by erasing signed preimages without a compensating field
transaction.

The next live route is no longer “topology plus current genesis.” It is either
a newly derived nonlinear bound-state action that fixes the defect flux and is
invertible with the field transaction, or an explicit environment/history
reservoir with an honestly open-system stochastic description. FTD-0316 is
retained as a measured ignition threshold and collective crest-regulation
result, not an exact local amplitude ceiling. Production remains unchanged.

## FTD-0569 — The reservoir route is one-event constructive and cycle-open (2026-07-26)

Accepted canonical genesis is not itself many-to-one when `d<1`. Its exact
inverse restores `J` by adding `K_GENESIS` along the surviving direction and
rescales `W` by `1/(1-d)`. A uniform hidden phase also generates the exact
Bernoulli acceptance probability through a reversible inverse-CDF map.

The full production cycle does not inherit that reversibility. Unit drain
erases `W`; every erased acceptance branch adds one required history bit;
evaporation leaves the genesis drain in place and therefore is not the reverse
event; the exact reverse transition has zero probability in the same projected
kernel; and the compensating energy payload varies continuously with overshoot
and differs between single and dual genesis. The 540 genesis arms, 16
Bernoulli arms, and 20-step history control all pass the registered identities.

The environmental interpretation is therefore coherent only as an open-system
one: energy and event records must flow outward or into an indefinitely
extensible environment. This matches a maintained-pattern/flame ontology, but
does not yet derive the bath, a mobile bound carrier, charge conservation, or
unitarity. The alternative live route remains a genuinely new nonlinear
bound-state action. Production remains unchanged.

## FTD-0570 — Exact reals rescue history, not the native action (2026-07-26)

The open-environment route is now explicit. A generalized baker map on an
environmental phase pair `(u,v)` is bijective, area preserving, and retains an
unlimited sequence of acceptance branches in exact real digits. This refines
FTD-0569: the closed-negative result is a finite-information result, not a
finite-dimensional one.

That refinement does not rescue production. The accepted `(J,W)` drain fails
the canonical symplectic condition even when `kinetic_drain=0`; its tangential
defect is strictly nonzero and its six-dimensional phase-volume Jacobian is
below one. A branchwise symplectic and energy-conserving generating-function
lift can be written, but it adds ten continuous variables: six conjugates, the
exact-real phase pair, and a time-energy pair. The state-dependent probability
also forces a conjugate `-g grad p` backreaction absent from the engine.

Binary64 cannot implement the unlimited history: the locked pair of distinct
64-event exact histories collapses to the same value `1.0`. The projected
genesis transition remains absolutely irreversible because its exact reverse
rate is zero. The live route is therefore either to derive these environmental
variables and their locality/transport from native content, accepting a driven
open system, or to derive a different nonlinear bound-state action. Production
remains unchanged.

## FTD-0571 — The environment must push back (2026-07-26)

The missing reservoir cannot be obtained by simply naming an existing
unchanged `Voxel` field “the environment.” For any differentiable symplectic
enlargement, environment-independent projected dynamics force the projected
system derivative itself to be symplectic. The accepted production genesis
map is not: its canonical defect has rank four without kinetic drain and rank
six with positive drain.

The source audit makes the consequence concrete. Genesis writes only `flux`
and `wave_vel` among continuous variables; 34 other continuous fields remain
spectators, and the random draw is a stateless function rather than stored
bath state. An exact Hamiltonian completion must therefore let incoming bath
microstate change the matter/field outcome. Matching production only on a
prepared bath slice remains possible, but repetition then needs reset,
replacement, or exported history/energy. Without that native transport law it
is an open-system model, not a hidden common action already present in the
engine. The other live route is a new nonlinear bound-state transaction.
Production remains unchanged.

## FTD-0572 — The minimum environment is explicit, but not inert (2026-07-26)

Under the selected FTD-0570 `(J,W)` canonical pairing, the bath required by
FTD-0571 can now be priced and constructed. Symplectic rank identities require
at least two canonical bath pairs when kinetic drain
is zero and three for every positive drain. An explicit pairwise dilation
attains that minimum and, from a prepared zero bath, projects exactly to one
accepted genesis step.

The construction immediately reveals the missing physics. Genesis writes the
contracted system information into the bath. Reusing the same bath on the next
event feeds that record back and produces a nonzero, analytically fixed
deviation from production. A fixed zero-bath preparation cannot be invariant
for a noncanonical projected map. The minimum bath also cannot preserve the
equal-weight positive quadratic system-plus-bath energy passively; doing so
would force the unequal radial/tangential genesis scales to be equal.

The open-system route is therefore no longer “add some reservoir.” It must
derive at least these canonical bath channels, a specific active or
cross-coupled energy, and a local reset/export/transport mechanism. A
state-dependent degenerate bath graph remains a mathematical loophole, as do a
separately derived nonstandard symplectic structure and a different nonlinear
bound-state action. The nonstandard structure can change the exact channel
count—at zero drain the defect rank can be reduced from four to two—but cannot
remove the need for enlargement because `det M=t^2a^3<1`. Production remains
unchanged.

## FTD-0573 — Cubic symmetry selects the form and prices it (2026-07-26)

The ambiguity left by FTD-0572 is now classified. If `J` and `W` are treated
as equivalent cubic vectors and the canonical form is required to be one
constant onsite bilinear object, cubic invariance uniquely selects
`Omega=[[0,I],[-I,0]]` up to scale. An exact 48-element signed-permutation
enumeration and independent block proof agree.

Dropping cubic covariance can reduce the branchwise symplectic defect: rank
two at zero drain and rank four at generic positive drain, compared with the
cubic ranks four and six. At `a=t`, the five-dimensional contracting
eigenspace forces rank six for every nondegenerate form. Thus cubic covariance
costs exactly one canonical bath pair on all 120 registered production arms.

This is a classification and cost theorem, not the missing physics. FTD-0574
subsequently derives the `(J,W)` pairing from the source-free field action.
The environmental branch still has to identify an active bath energy plus
local record transport. Otherwise it remains a selected open-system
completion. Production remains unchanged.

## FTD-0574 — The free field has a native action; the moving-source action was wrong (2026-07-26)

The frozen source-free production field tick is exactly the discrete
Euler--Lagrange map of

`L_d(J_n,J_(n+1)) = 1/2 ||J_(n+1)-J_n||^2 - 1/2 <J_n,KJ_n>`.

Both discrete Legendre transforms equal the stored `wave_vel`. Thus the
standard `(J,W)` canonical pairing used by FTD-0570--0573 is now derived for
the free field rather than merely selected by symmetry. The same calculation
recovers the unique onsite-kinetic-normalized tick invariant. An exact
continuous shadow Hamiltonian also exists modewise, but its nonpolynomial
multiplier proves that no volume-independent fixed-range continuous generator
reproduces every tick; the discrete action itself remains local.

The prescribed production source is variational under
`G_C <s,div J> + G_C <curl J,s v>`. It is not generated by the previously
documented onsite velocity term `-G_C <s v,J>`. A uniform `s=1`, constant-`v`
fixture gives exactly zero coded source but a nonzero documented variation of
magnitude `G_C`. Engine comments and the specification now state that
boundary explicitly.

This does not derive reciprocal mobile matter. Varying the correct source
functional with respect to a dynamic path yields induction and
`curl curl J` operators, not the optional production Lorentz-force branch.
The next load-bearing common-action task is therefore a joint worldline/source
functional that closes field source, matter work, energy, and inversion
without repeating the FTD-0479/0539 failures. In parallel, genesis still
requires a native active bath energy and record-transport law. Production is
unchanged.

## FTD-0575 — Reciprocity exists, but the static photon pole disappears (2026-07-26)

The exact FTD-0574 source interaction can be rewritten with derived Hodge
potentials `Phi_J=-G_C div J` and `A_J=G_C curl J`. Varying a point path then
gives the reciprocal Lorentz-form force
`q(E_J+v cross B_J)`, with
`E_J=G_C grad div J-G_C partial_t curl J` and
`B_J=G_C curl curl J`. Magnetic scalar work is zero and the homogeneous
field identities close exactly.

That algebraic success does not yield electromagnetism. In the static sector
both the source and probe are derivative coupled. The inverse wave pole is
canceled, leaving the bounded kernel
`R(k)=3 sum_i sin(k_i)^2/M(k)`, with `0<=R<=3` and `R->3` in the infrared.
The effective static energy also makes equal polarities attract and opposite
polarities repel. The same coupling's soft radiative residue vanishes as
`O(k^2)`. Thus any long-range Coulomb behavior supplied by Gauss projection or
the Poisson branch is an additional selected mechanism, not the reciprocal
completion of the native source action.

The next legitimate gate is narrower: derive an exact finite-step common
energy and a stable mobile manifested solution for the Hodge force, while
keeping its non-electromagnetic status explicit, or derive a genuinely
different nonlinear/enlarged charge mechanism. No force branch, toggle,
scenario, infrared particle claim, or production default was promoted.

## FTD-0576 — Energy closes, but local cardinal motion does not (2026-07-26)

The native driven field tick has an exact source-work theorem. Its unique
energy coordinate is `R=J-W/2`, and a midpoint Hodge source closes field,
interaction, and matter energy exactly whenever the moving density and
integrated current satisfy `delta rho+D Q=0` under the same central divergence.

That condition exposes the representation mismatch. A cardinal one-site hop
requires the site-current symbol `-2z/(z+1)`. Even periodic volumes admit no
solution because the hop excites the checkerboard null mode. Odd volumes admit
only box-spanning alternating representatives. The already exact local face
current cannot be projected into the native site current locally: divergence
commutation fixes the equally nonlocal map `2/(z+1)`.

Thus energy conservation is not the failure. The incompatible conjunction is
native central operators, cardinal ternary hopping, exact continuity/energy,
and finite-range locality. The next constructive fork is either to accept the
matched face/link complex as a separate selected dynamics, derive a staggered
current/connection primitive, or find a non-cardinal/nonlinear carrier whose
endpoint symbol cancels the checkerboard factor. No production branch or
scenario was promoted.

## FTD-0577 — Local continuity recovered by a noncardinal Moore coat (2026-07-26)

The smallest symmetric separable radius-one coupling filter that removes the
central checkerboard obstruction is unique in its class:

```text
B=(T^-1+2+T)/4.
```

Its cubic tensor product is a positive 27-site Moore coat with center, face,
edge, and corner weights `1/8`, `1/16`, `1/32`, and `1/64`. Primitive
manifestation remains one ternary site; only its coupling representation is
spread. That spread is explicitly non-cardinal.

For the exact straight-segment face current `K`, the local current

```text
Q_i=((1+T_i^-1)/2) product_(j!=i) B_j K_i
```

satisfies native central continuity exactly. The 36 path arms, translations,
all 24 proper cubic rotations, and four inherited total-energy fixtures pass.
Support remains bounded as volume grows.

This resolves the continuity/locality mismatch only. It does not select a
reciprocal force, cancel self-force, restore the missing Coulomb pole, or
produce a mobile particle. The next legitimate gate is to derive one common
coated source/path action and determine whether its discrete variation yields
a matched gather and an invertible energy-exact endpoint step without a
separate force primitive. No production branch or scenario was promoted.

## Start here

- New to the project: `README.md`, then the constitution.
- Working instructions, navigation, and discipline: `CLAUDE.md`.
- Architecture and task→file navigation: `META_PROJECT_ATLAS.md`; theory catalog: `docs/theory/META_INDEX.md`.
